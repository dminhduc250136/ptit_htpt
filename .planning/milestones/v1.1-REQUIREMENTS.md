---
milestone: v1.1
name: Real End-User Experience
status: archived
shipped: 2026-04-26
priority: visible-first
satisfied: 15
partial: 4
total: 19
---

# Milestone v1.1 — Requirements (Archived)

> Archived 2026-04-26 sau khi `/gsd-complete-milestone v1.1`. Status snapshot tại thời điểm ship.

## Goal

Biến demo flow từ "stub-verified" thành "real visible end-to-end" — mọi thứ user click trên UI đều phải hoạt động với real data thay vì mock/seeded. Ưu tiên những thay đổi end-user nhìn thấy được; defer backend hardening / security / observability invisible sang v1.2.

## Foundation từ v1.0 (đã có sẵn)

- Unified `ApiErrorResponse` envelope + `traceId` propagation across 6 services + gateway
- Springdoc Swagger UI + OpenAPI codegen pipeline (FE 6 typed modules)
- CRUD completeness + soft-delete baseline + admin/public route boundaries (in-memory layer)
- Validation hardening (gateway pass-through + common-code taxonomy)
- FE typed HTTP tier + ApiError dispatcher (5 failure branches) + middleware route protection
- Playwright E2E suite hoạt động (12/12 PASS trên flow stub-verified)

## v1.1 Requirements — Final Status

### C0. Database Foundation

- [x] **DB-01** ✓ SATISFIED — Postgres service vào docker-compose.yml; healthcheck pg_isready; volume `tmdt-pgdata` persist
- [x] **DB-02** ✓ SATISFIED — `spring-boot-starter-data-jpa` + `postgresql` + `flyway-core` ở 5 services
- [x] **DB-03** ✓ SATISFIED — application.yml datasource config + `ddl-auto: validate` + Flyway baseline V1
- [x] **DB-04** ✓ SATISFIED — `XxxRepository extends JpaRepository<XxxEntity, ID>` ở 5 services
- [⚠] **DB-05** PARTIAL — V2 seed dev data từ FE mocks OK; nhưng order-service có V2 collision (migration vs seed) — fix V100__ commit `346092b` cuối milestone
- [x] **DB-06** ✓ SATISFIED — Gateway round-trip `GET /api/products` trả seeded products thật; `mock-data/` đã xóa

### C1. Auth Flow Thật

- [x] **AUTH-01** ✓ SATISFIED — `/api/users/auth/register` persist UserEntity + BCrypt + 201; 409 trùng username/email
- [x] **AUTH-02** ✓ SATISFIED — `/api/users/auth/login` verify cred + JWT HS256 24h; 401 sai cred
- [x] **AUTH-03** ✓ SATISFIED — `/api/users/auth/logout` client-side discard
- [x] **AUTH-04** ✓ SATISFIED — FE login form gỡ mock + setTokens + setUserRole cookie + redirect /
- [x] **AUTH-05** ✓ SATISFIED — FE register form + 409 field errors discriminate (username vs email) + auto-login
- [⚠] **AUTH-06** PARTIAL — Session persist OK; middleware matcher chỉ có `/admin/:path*` (spec yêu cầu thêm `/account|/profile|/checkout`). Compensating control qua http.ts 401 redirect — flow E2E hoạt động qua API. Defer SSR-bounce sang v1.2.

### C2. Admin + Search Real Data

- [x] **UI-01** ✓ SATISFIED — `/search` page rewire `listProducts({keyword})` qua gateway; empty state + skeleton + debounce 350ms
- [⚠] **UI-02** PARTIAL — Sub-routes `admin/products` CRUD OK (list/create/edit/delete + toast + V2 migration brand/thumbnail). Admin landing dashboard `app/admin/page.tsx` còn `mockProducts: Product[] = []` → KPI stats luôn = 0. Defer landing rewire v1.2.
- [x] **UI-03** ✓ SATISFIED — `admin/orders` list real + detail page với status update + 5 STATUS_OPTIONS
- [x] **UI-04** ✓ SATISFIED — `admin/users` list real + UserEditModal PATCH fullName/phone/roles + soft-delete (CUSTOMER only)

### C3. Cart → Order Persistence Visible

- [⚠] **PERSIST-01** PARTIAL → CLOSED — V3 migration ProductEntity.stock + ProductCrudService.toResponse trả `product.stock()`; PDP stock display + disabled khi stock=0. Cart-side gap (CartItem stock snapshot + clamp + 409 handler) closed cuối milestone qua commit `346092b` (debug session resolved).
- [x] **PERSIST-02** ✓ SATISFIED — V2 migration order_items + shipping_address JSON + payment_method; OrderItemEntity cascade ALL; createOrderFromCommand persist full breakdown
- [x] **PERSIST-03** ✓ SATISFIED — Checkout redirect `/profile/orders/{id}` (override route name); order detail page async fetch + 4-column items table + shippingAddress + paymentMethod + totals

## Out of Scope (v1.1) — All Validated

- ✓ Real payment gateway integration — mock acceptable cho dự án thử nghiệm
- ✓ Production-grade auth (refresh tokens, OAuth, password reset email)
- ✓ Mobile responsive polish toàn bộ
- ✓ Admin role-based access control phức tạp
- ✓ I18n cho error messages — tiếng Việt hardcoded OK

## Traceability

| REQ | Phase | Plan(s) | Final Status | Notes |
|---|---|---|---|---|
| DB-01 | 5 | 05-02 | ✓ SATISFIED | docker-compose postgres healthcheck |
| DB-02 | 5 | 05-03..07 | ✓ SATISFIED | JPA + postgresql + flyway-core ở 5 services |
| DB-03 | 5 | 05-03..07 | ✓ SATISFIED | application.yml + ddl-auto: validate |
| DB-04 | 5 | 05-03..07 | ✓ SATISFIED | JpaRepository ở 5 services |
| DB-05 | 5 | 05-03,04,05,07 | ⚠ PARTIAL | V2 OK; order-svc collision fixed V100__ cuối milestone |
| DB-06 | 5 | 05-08, 05-09 | ✓ SATISFIED | Gateway round-trip + mock-data deleted |
| AUTH-01 | 6 | 06-01 | ✓ SATISFIED | POST /auth/register + 409 |
| AUTH-02 | 6 | 06-01 | ✓ SATISFIED | POST /auth/login + JWT HS256 + 401 |
| AUTH-03 | 6 | 06-01, 06-03 | ✓ SATISFIED | logout client-side discard |
| AUTH-04 | 6 | 06-02, 06-03 | ✓ SATISFIED | FE login wire + cookie |
| AUTH-05 | 6 | 06-02, 06-03 | ✓ SATISFIED | FE register + 409 discriminate + auto-login |
| AUTH-06 | 6 | 06-03 | ⚠ PARTIAL | Session persist OK; middleware narrow → defer v1.2 |
| UI-01 | 7 | 07-01, 07-04 | ✓ SATISFIED | /search keyword |
| UI-02 | 7 | 07-01..05 | ⚠ PARTIAL | Sub-routes OK; landing dashboard → defer v1.2 |
| UI-03 | 7 | 07-01, 07-04, 07-05 | ✓ SATISFIED | admin/orders + detail + status |
| UI-04 | 7 | 07-01, 07-03, 07-04, 07-06 | ✓ SATISFIED | admin/users PATCH + soft-delete |
| PERSIST-01 | 8 | 08-01, 08-04 | ⚠→✓ CLOSED | Cart gap closed cuối milestone (commit 346092b) |
| PERSIST-02 | 8 | 08-02 | ✓ SATISFIED | OrderItemEntity + shippingAddress + paymentMethod |
| PERSIST-03 | 8 | 08-02, 08-03 | ✓ SATISFIED | FE order detail full breakdown + redirect |

**Final score:** 15/19 SATISFIED + 4/19 PARTIAL (DB-05 và PERSIST-01 closed cuối milestone qua commit `346092b`; AUTH-06 và UI-02 landing defer v1.2)

---

*Archived: 2026-04-26*
*Reference audit: `milestones/v1.1-MILESTONE-AUDIT.md`*
