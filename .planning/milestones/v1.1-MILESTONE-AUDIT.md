---
milestone: v1.1
name: Real End-User Experience
audited: 2026-04-26T15:00:00Z
status: gaps_found
scores:
  requirements: 15/19 satisfied, 4/19 partial
  phases: 4/4 verified (2 passed + 2 human_needed)
  integration: 5/9 fully wired, 4/9 partial
  flows: 3/5 complete, 2/5 partial
gaps:
  requirements:
    - id: AUTH-06
      status: partial
      phase: 06-real-auth-flow
      claimed_by_plans: ["06-02-PLAN.md", "06-03-PLAN.md"]
      completed_by_plans: ["06-02-SUMMARY.md", "06-03-SUMMARY.md"]
      verification_status: human_needed
      evidence: "middleware.ts matcher chỉ có ['/admin/:path*']. Plan yêu cầu 4 matchers ['/checkout/:path*', '/profile/:path*', '/admin/:path*', '/account/:path*']. Phase 6 VERIFICATION.md (Truth #17) ghi 'matcher = 4 entries' nhưng integration check thực tế cho thấy chỉ có /admin. Compensating control: services/http.ts redirect 401 toàn cục — flow E2E hoạt động qua API 401 nhưng SSR direct visit /profile hoặc /checkout không bị middleware bounce."
    - id: UI-02
      status: partial
      phase: 07-search-admin-real-data
      claimed_by_plans: ["07-04-PLAN.md", "07-05-PLAN.md"]
      completed_by_plans: ["07-05-SUMMARY.md"]
      verification_status: passed (sub-routes only)
      evidence: "src/app/admin/page.tsx (admin dashboard landing) vẫn import 'const mockProducts: Product[] = []'. KPI stats (tổng products, low-stock count) tính từ mockProducts.length nên luôn = 0. Phase 5 đã xóa mock-data module nhưng landing page không được rewire sang listProducts() / orders aggregate. Sub-routes admin/products|orders|users dùng real services nên main UI-02 satisfied; landing dashboard là gap riêng."
    - id: PERSIST-01
      status: partial
      phase: 08-cart-order-persistence
      claimed_by_plans: ["08-01-PLAN.md", "08-04-PLAN.md"]
      completed_by_plans: ["08-01-SUMMARY.md", "08-04-SUMMARY.md"]
      verification_status: passed (PDP only)
      evidence: "Trên committed state: services/cart.ts không lưu stock snapshot trong CartItem; app/cart/page.tsx không có upper-bound check trên nút '+'; chỉ có app/checkout/page.tsx decode domainCode === 'STOCK_SHORTAGE'. Add-to-cart từ PDP có disabled khi stock=0 nhưng quantity tăng trong cart bypass được. Backend OrderCrudService.validateStockOrThrow() là best-effort (catch Exception → log.warn → bỏ qua nếu product-service unreachable). Debug session login-redirect-cart-stock.md đã xác định root cause + có WIP fix uncommitted."
    - id: DB-05
      status: partial
      phase: 05-database-foundation
      claimed_by_plans: ["05-05-PLAN.md"]
      completed_by_plans: ["05-05-SUMMARY.md"]
      verification_status: passed
      evidence: "order-service có cả db/migration/V2__add_order_items.sql (Phase 8) lẫn db/seed-dev/V2__seed_dev_data.sql (Phase 5) cùng version=2. Flyway có thể trip trong config nghiêm ngặt. WIP rename V2→V100 trong working tree là fix đúng nhưng chưa commit."
  integration:
    - issue: "Middleware matcher quá hẹp"
      from: middleware.ts
      to: protected routes
      affected_reqs: [AUTH-06]
      severity: warning
    - issue: "Admin dashboard KPIs trên empty mock"
      from: app/admin/page.tsx
      to: products/orders services
      affected_reqs: [UI-02]
      severity: warning
    - issue: "Cart add-path không decode 409 STOCK_SHORTAGE"
      from: services/cart.ts + app/cart/page.tsx
      to: ApiError dispatcher
      affected_reqs: [PERSIST-01]
      severity: warning
    - issue: "order-service Flyway version collision V2 migration vs V2 seed"
      from: db/migration/V2__add_order_items.sql
      to: db/seed-dev/V2__seed_dev_data.sql
      affected_reqs: [DB-05]
      severity: info
  flows:
    - flow: "Stock shortage E2E (stock=0 → add-to-cart → 'Hết hàng')"
      breaks_at: "PDP guard OK; cart-add path không có 409 handler"
      affected_reqs: [PERSIST-01]
    - flow: "SSR-level protected route bounce"
      breaks_at: "/profile, /checkout direct visit chỉ bị bounce qua API 401, không qua middleware"
      affected_reqs: [AUTH-06]
tech_debt:
  - phase: 06-real-auth-flow
    items:
      - "JWT logout chỉ client-side discard (không có blacklist) — accept cho MVP"
      - "Spec phase override: middleware matcher narrowed to /admin only — không match plan"
      - "6 behavioral checks human_needed (register/login/logout flow + session persist + admin role gate)"
  - phase: 07-search-admin-real-data
    items:
      - "Admin order detail line items + shippingAddress + paymentMethod — 'Số sản phẩm' = '—' (defer Phase 8 ✓ closed)"
      - "as any cast trong admin/orders/[id]/page.tsx (TypeScript workaround, không break runtime)"
      - "4 behavioral checks human_needed (search keyword + admin CRUD modals)"
      - "Admin dashboard landing không rewire sang real services (gap UI-02 partial)"
  - phase: 08-cart-order-persistence
    items:
      - "Override: redirect /account/orders → /profile/orders ở checkout (route name discrepancy, intent đúng)"
      - "Cart-side stock validation chưa hoàn chỉnh (gap PERSIST-01 partial — WIP fix uncommitted trong debug session)"
      - "OrderCrudService.validateStockOrThrow() best-effort fallback có thể bypass nếu product-service unreachable"
nyquist:
  compliant_phases: 0
  partial_phases: 2  # 05, 07
  missing_phases: 2  # 06, 08
  overall: partial
deferred_to_v1.2:
  - "Backend hardening invisible to end-user (D1, D2, D6, D7, D8, D9, D10, D12, D13, D14, D17 từ v1.0 audit)"
  - "Behavioral E2E tests (Playwright re-baseline) cho v1.1 features mới (defer)"
  - "Nyquist VALIDATION.md cho Phase 6 + Phase 8"
---

# Milestone v1.1 — Real End-User Experience: Audit Report

**Audited:** 2026-04-26
**Status:** ⚠ GAPS FOUND
**Scope:** Phases 5-8 (Database Foundation, Real Auth, Search+Admin, Cart→Order Persistence)

---

## Executive Summary

v1.1 đạt **15/19 REQs SATISFIED** + **4/19 PARTIAL**. Tất cả 4 phases có VERIFICATION.md với phase-level goal achievement. Nhưng cross-phase integration check phát hiện **4 wiring gaps** không được spot trong từng phase verifier (vì verifier nhìn nội-phase scope, không bridge giữa phases).

Quan trọng: **debug session `login-redirect-cart-stock.md` đã RESOLVED** với WIP changes uncommitted trong working tree (services/http.ts AUTH_PATHS_NO_REDIRECT + cart.ts stock field + cart/page.tsx upper-bound) — fix này địa chỉ trực tiếp gap PERSIST-01 + 1 phần của UI-02 dashboard, nhưng KHÔNG fix middleware matcher (AUTH-06) hoặc admin landing KPIs (UI-02 dashboard).

---

## 1. Phase Verification Aggregate

| Phase | Status | Score | Behavioral Gaps |
|---|---|---|---|
| 5 — Database Foundation | passed | 6/6 | — |
| 6 — Real Auth Flow | human_needed | 17/17 verified, 6 human checks | E2E register/login/logout + session persist + admin role gate |
| 7 — Search + Admin Real Data | human_needed | 4/4 verified, 4 human checks | Search keyword E2E + admin CRUD modals |
| 8 — Cart → Order Persistence | passed | 5/5 (1 override) | — |

**Phase 6 + Phase 8 thiếu VALIDATION.md** (Nyquist gap — partial overall).

---

## 2. Requirements Coverage (3-source cross-reference)

### Satisfied (15/19)

| REQ | Phase | Status |
|---|---|---|
| DB-01..04, DB-06 | 5 | ✓ SATISFIED |
| AUTH-01..05 | 6 | ✓ SATISFIED |
| UI-01, UI-03, UI-04 | 7 | ✓ SATISFIED |
| PERSIST-02, PERSIST-03 | 8 | ✓ SATISFIED |

### Partial (4/19)

| REQ | Phase | Gap |
|---|---|---|
| **DB-05** | 5 | order-service Flyway version=2 collision (migration vs seed). WIP V100__ rename uncommitted. |
| **AUTH-06** | 6 | middleware.ts matcher chỉ có `/admin/:path*`, spec yêu cầu thêm `/account|/profile|/checkout`. Compensating control qua http.ts 401 redirect — flow vẫn hoạt động nhưng không khớp contract plan. |
| **UI-02** | 7 | Sub-routes admin/products CRUD OK, nhưng `app/admin/page.tsx` (landing dashboard) còn `mockProducts: Product[] = []` — KPI stats luôn = 0. |
| **PERSIST-01** | 8 | PDP stock guard OK; cart-add path không decode 409 STOCK_SHORTAGE; CartItem không lưu stock snapshot. WIP fix uncommitted trong debug session. |

### Orphaned (0/19)

Không có orphaned requirement.

---

## 3. Cross-Phase Integration

| Wiring | Status | REQ |
|---|---|---|
| docker-compose → services → JPA → gateway | ✓ WIRED | DB-01..04, DB-06 |
| order-svc V2 seed/migration version | ⚠ PARTIAL | DB-05 |
| auth endpoints → auth.ts → AuthProvider → pages | ✓ WIRED | AUTH-01..05 |
| middleware protected routes | ⚠ PARTIAL | AUTH-06 |
| /search → listProducts(keyword) | ✓ WIRED | UI-01 |
| admin/products CRUD | ⚠ PARTIAL | UI-02 (landing) |
| admin/orders + admin/users | ✓ WIRED | UI-03, UI-04 |
| ProductEntity.stock → FE PDP | ✓ WIRED | PERSIST-01 (PDP) |
| Cart-add → 409 STOCK_SHORTAGE handler | ⚠ PARTIAL | PERSIST-01 (cart) |
| Order items + shipping + payment | ✓ WIRED | PERSIST-02 |
| Checkout → /profile/orders/{id} | ✓ WIRED | PERSIST-03 |

---

## 4. E2E Flows

| Flow | Status | Note |
|---|---|---|
| 1. Register → login → browse → search → cart → checkout → confirmation | ✓ COMPLETE | All happy-path wired (modulo Bug 1 đã có WIP fix) |
| 2. Admin login → admin/products CRUD → admin/orders status update → admin/users edit | ⚠ PARTIAL | Sub-routes OK; landing dashboard KPIs = 0 |
| 3. Stock shortage (stock=0 → add-to-cart → "Hết hàng") | ⚠ PARTIAL | PDP OK; cart bypass được (WIP fix uncommitted) |
| 4. Session persist (login → F5 → still authed); logout → /account redirect | ✓ COMPLETE | AuthProvider hydration + http.ts 401 |
| 5. Admin role gate (USER → /admin → /403; ADMIN → load) | ✓ COMPLETE | middleware.ts /admin check OK |

---

## 5. Nyquist Compliance

| Phase | VALIDATION.md | Status | Action |
|---|---|---|---|
| 5 | Có | partial (xem 05-VALIDATION.md) | `/gsd-validate-phase 5` |
| 6 | Không | MISSING | `/gsd-validate-phase 6` |
| 7 | Có | partial (xem 07-VALIDATION.md) | `/gsd-validate-phase 7` |
| 8 | Không | MISSING | `/gsd-validate-phase 8` |

**Overall: partial.** Có thể defer sang v1.2 vì project priority là visible-first, không phải test coverage.

---

## 6. Tech Debt Aggregate

Xem `tech_debt:` block trong frontmatter. Highlights:
- 10 behavioral checks human_needed across Phase 6 + Phase 7 (cần browser + docker stack chạy)
- 1 override (Phase 8: route name `/account/orders` → `/profile/orders`)
- WIP changes trong working tree (debug session đã resolve nhưng chưa commit)

---

## 7. Decision Required

Có 3 lựa chọn để move forward:

### Option A — Plan gap-closure phase (recommended)

Tạo Phase 9 (gap closure) hoặc decimal Phase 8.1 cover:
- AUTH-06 fix middleware matcher
- UI-02 fix admin landing dashboard rewire
- PERSIST-01 commit WIP cart fixes (đã có code sẵn từ debug session)
- DB-05 commit V2→V100 rename

Sau đó re-audit + complete milestone.

```bash
/gsd-plan-milestone-gaps
```

### Option B — Commit WIP + Accept remaining gaps làm tech debt

Working tree đã có WIP fix cho PERSIST-01 (cart) + DB-05 (V100 rename). Commit ngay (atomic), rồi:
- Document AUTH-06 narrow matcher + UI-02 landing dashboard làm known gaps trong MILESTONES.md
- Defer fixes sang v1.2 backlog
- Proceed `/gsd-complete-milestone v1.1`

Phù hợp nếu visible-first priority cho phép skip — fix AUTH-06 invisible (compensating control hoạt động), UI-02 landing dashboard hơi visible (KPI luôn = 0).

### Option C — Cancel + return to development

Đẩy lùi quyết định, tiếp tục debug/fix manual rồi audit lại.

---

**Report:** `.planning/v1.1-MILESTONE-AUDIT.md`
**Generated by:** `/gsd-audit-milestone v1.1`
