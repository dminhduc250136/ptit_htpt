---
phase: 17-s-a-order-detail-items
plan: 02
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - sources/frontend/src/app/admin/orders/[id]/page.tsx
autonomous: true
requirements:
  - ADMIN-06
tags:
  - frontend
  - admin
  - order-detail
  - bug-fix

must_haves:
  truths:
    - "Trang `/admin/orders/[id]` KHÔNG còn placeholder text 'khả dụng sau khi Phase 8'."
    - "Trang `/admin/orders/[id]` render bảng items 4 cột (Sản phẩm / Số lượng / Đơn giá / Thành tiền) + thumbnail bên trái."
    - "Trang `/admin/orders/[id]` render shipping address (street, ward, district, city join `, `) và payment method qua paymentMethodMap."
    - "TypeScript: KHÔNG còn `interface AdminOrder` inline; KHÔNG còn `as any` cast."
    - "Empty items: render `<p>Đơn hàng không có sản phẩm</p>`."
  artifacts:
    - path: "sources/frontend/src/app/admin/orders/[id]/page.tsx"
      provides: "Trang admin order detail rewrite — items table + shipping/payment cards thật"
      contains: "useEnrichedItems"
  key_links:
    - from: "sources/frontend/src/app/admin/orders/[id]/page.tsx"
      to: "sources/frontend/src/lib/useEnrichedItems.ts"
      via: "import + call hook"
      pattern: "useEnrichedItems\\(order\\??.items\\)"
    - from: "sources/frontend/src/app/admin/orders/[id]/page.tsx"
      to: "sources/frontend/src/lib/orderLabels.ts"
      via: "import paymentMethodMap"
      pattern: "from '@/lib/orderLabels'"
    - from: "sources/frontend/src/app/admin/orders/[id]/page.tsx"
      to: "sources/frontend/src/types/index.ts"
      via: "import type { Order }"
      pattern: "import type \\{ Order \\}"
---

<objective>
Rewrite admin order detail page (`/admin/orders/[id]`) để xóa placeholder hardcoded + render full items table + shipping/payment thật.

Purpose: ADMIN-06 success criteria — admin cần xem đầy đủ chi tiết đơn hàng để xử lý customer support, KHÔNG bị block bởi text "khả dụng sau khi Phase 8 hoàn thiện" (đã legacy từ Phase 8).

Output: `sources/frontend/src/app/admin/orders/[id]/page.tsx` rewrite — Order type thay AdminOrder, items table 4 cột + thumbnail, shipping/payment render thật, empty state.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-s-a-order-detail-items/17-CONTEXT.md
@.planning/phases/17-s-a-order-detail-items/17-RESEARCH.md
@.planning/phases/17-s-a-order-detail-items/17-PATTERNS.md
@sources/frontend/src/app/admin/orders/[id]/page.tsx
@sources/frontend/src/app/profile/orders/[id]/page.tsx
@sources/frontend/src/types/index.ts
@sources/frontend/src/services/orders.ts
@sources/frontend/next.config.ts

<interfaces>
<!-- Contracts đã built ở Wave 1 (Plan 01) — executor KHÔNG cần re-derive. -->

From Plan 01 — `sources/frontend/src/lib/orderLabels.ts`:
```ts
export const statusMap: Record<string, { label: string; variant: 'default'|'new'|'hot'|'sale'|'out-of-stock' }>;
export const paymentMethodMap: Record<string, string>;
export const paymentStatusMap: Record<string, string>;
```

From Plan 01 — `sources/frontend/src/lib/useEnrichedItems.ts`:
```ts
export type EnrichedItem = OrderItem & { thumbnailUrl?: string; brand?: string };
export function useEnrichedItems(items: OrderItem[] | undefined): EnrichedItem[];
```

From `sources/frontend/src/services/orders.ts:77`:
```ts
export function getAdminOrderById(id: string): Promise<Order>;  // đã unwrapped envelope
export function updateOrderState(id: string, state: string): Promise<Order>;
```

From `sources/frontend/src/types/index.ts` — `Order` (đã đủ shape, KHÔNG cần AdminOrder mới):
```ts
export interface Order {
  id: string;
  userId: string;
  status?: string;        // canonical
  orderStatus?: string;   // legacy alias
  totalAmount?: number;
  total?: number;         // legacy alias
  paymentMethod: string;
  paymentStatus?: string;
  shippingAddress?: { street?: string; ward?: string; district?: string; city?: string };
  items?: OrderItem[];
  note?: string;
  createdAt?: string;
  // ... other fields
}
```

next.config.ts đã whitelist `images.unsplash.com` + `lh3.googleusercontent.com` cho `<Image>`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace inline AdminOrder + xóa `as any`, switch state sang Order type (D-02)</name>
  <read_first>
    - sources/frontend/src/app/admin/orders/[id]/page.tsx (full file — đặc biệt lines 1-22 imports + interface, line 46 useState, line 58 cast)
    - sources/frontend/src/types/index.ts (xác nhận Order interface đủ shape)
    - .planning/phases/17-s-a-order-detail-items/17-PATTERNS.md (lines 26-71 — Imports + Type/state pattern)
  </read_first>
  <files>sources/frontend/src/app/admin/orders/[id]/page.tsx</files>
  <action>
Modify `sources/frontend/src/app/admin/orders/[id]/page.tsx`:

**Step 1 — Update imports (top of file, theo PATTERNS.md lines 27-39):**

Thêm vào import block:
```tsx
import Image from 'next/image';
import type { Order } from '@/types';
import { paymentMethodMap, statusMap } from '@/lib/orderLabels';
import { useEnrichedItems } from '@/lib/useEnrichedItems';
```

Giữ nguyên các imports hiện có (`React, useCallback, useEffect, useState`, `useParams, useRouter`, `styles`, `Button, Badge, RetrySection, useToast`, `getAdminOrderById, updateOrderState`).

**Step 2 — XÓA inline interface `AdminOrder` (lines 13-22 hiện tại):**

XÓA toàn bộ block:
```tsx
interface AdminOrder {
  id: string;
  userId: string;
  // ... các field
}
```

**Step 3 — XÓA inline STATUS_LABELS/STATUS_VARIANTS (nếu admin file đang định nghĩa riêng — lines 25-38 theo PATTERNS):**

Replace mọi reference `STATUS_LABELS[s]` → `statusMap[s]?.label ?? s` và `STATUS_VARIANTS[s]` → `statusMap[s]?.variant ?? 'default'`. Xóa định nghĩa STATUS_LABELS/STATUS_VARIANTS local.

**Step 4 — Replace state type:**

```tsx
// BEFORE: const [order, setOrder] = useState<AdminOrder | null>(null);
const [order, setOrder] = useState<Order | null>(null);
```

**Step 5 — XÓA `as any` cast (line ~58):**

```tsx
// BEFORE: const data = await getAdminOrderById(id) as any;
const data = await getAdminOrderById(id);   // đã Promise<Order>
setOrder(data);
setNewStatus(data.status ?? data.orderStatus ?? 'PENDING');
```

KHÔNG dùng `as any`, KHÔNG dùng `as Order`. `getAdminOrderById` đã trả `Promise<Order>` (services/orders.ts:77 + http.ts auto-unwrap).

**Step 6 — Validate sample reads dùng null-safe alias (Pattern C):**

Mọi chỗ đọc field cần fallback alias:
- `order.status ?? order.orderStatus ?? 'PENDING'`
- `order.totalAmount ?? order.total ?? 0`

KHÔNG được bỏ alias vì BE trả `status`+`totalAmount` còn legacy types support cả `orderStatus`+`total`.

**KHÔNG sửa**: load callback flow, error handling, skeleton loading, status update logic — đã đúng.
  </action>
  <verify>
    <automated>cd sources/frontend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "interface AdminOrder" sources/frontend/src/app/admin/orders/[id]/page.tsx` trả về `0`.
    - `grep -c "as any" sources/frontend/src/app/admin/orders/[id]/page.tsx` trả về `0`.
    - `grep "import type { Order }" sources/frontend/src/app/admin/orders/[id]/page.tsx` match.
    - `grep "useState<Order" sources/frontend/src/app/admin/orders/[id]/page.tsx` match.
    - `grep "from '@/lib/orderLabels'" sources/frontend/src/app/admin/orders/[id]/page.tsx` match.
    - `grep "from '@/lib/useEnrichedItems'" sources/frontend/src/app/admin/orders/[id]/page.tsx` match.
    - `npx tsc --noEmit` exit code 0.
  </acceptance_criteria>
  <done>AdminOrder interface gone, `as any` gone, Order type wired, imports đúng, tsc pass.</done>
</task>

<task type="auto">
  <name>Task 2: Render shipping/payment cards thật + items table 4 cột + thumbnail (D-03/04/05/06)</name>
  <read_first>
    - sources/frontend/src/app/admin/orders/[id]/page.tsx (sau Task 1 — đặc biệt cardStyle/labelStyle inline objects, lines 134-152 — placeholder + cards)
    - .planning/phases/17-s-a-order-detail-items/17-PATTERNS.md (lines 73-156 — Shipping card pattern + Items table pattern)
    - .planning/phases/17-s-a-order-detail-items/17-RESEARCH.md (Pattern 3 — Image placeholder; Pitfall #2/#3 — null shippingAddress + empty items)
    - sources/frontend/src/app/profile/orders/[id]/page.tsx (lines 200-220 — analog shipping render)
  </read_first>
  <files>sources/frontend/src/app/admin/orders/[id]/page.tsx</files>
  <action>
Modify `sources/frontend/src/app/admin/orders/[id]/page.tsx` — render real shipping/payment + items table.

**Step 1 — Replace shipping card (D-04, current lines ~134-139 hardcoded `"—"`)**

Thay block "Thông tin giao hàng" hiện hardcode bằng (giữ inline-style pattern admin per D-03):

```tsx
<div style={cardStyle}>
  <h3 style={{ fontSize: 'var(--text-title-lg)', fontWeight: 700, marginBottom: 'var(--space-3)' }}>
    Thông tin giao hàng
  </h3>
  <p style={labelStyle}>
    Địa chỉ:{' '}
    <strong>
      {order.shippingAddress
        ? [order.shippingAddress.street, order.shippingAddress.ward,
           order.shippingAddress.district, order.shippingAddress.city]
            .filter(Boolean).join(', ')
        : '—'}
    </strong>
  </p>
  <p style={labelStyle}>
    Thanh toán:{' '}
    <strong>{paymentMethodMap[order.paymentMethod] ?? order.paymentMethod ?? '—'}</strong>
  </p>
  {order.note && (
    <p style={labelStyle}>Ghi chú: <strong>{order.note}</strong></p>
  )}
</div>
```

**Pitfall #2 mitigation**: `order.shippingAddress` có thể null cho legacy order — fallback `'—'`. KHÔNG throw / KHÔNG render empty.

**Step 2 — Replace placeholder block (D-03/05/06, current line ~145 với hardcoded text "khả dụng sau khi Phase 8")**

Thêm hook call ở component top (cùng level với state):
```tsx
const enriched = useEnrichedItems(order?.items);
```

(Nếu `order` null lúc render skeleton, hook nhận undefined và return `[]` — safe.)

Replace toàn bộ card "Sản phẩm" (block chứa placeholder text) bằng:

```tsx
<div style={cardStyle}>
  <h3 style={{ fontSize: 'var(--text-title-lg)', fontWeight: 700, marginBottom: 'var(--space-3)' }}>
    Sản phẩm
  </h3>
  {enriched.length === 0 ? (
    <p style={{ color: 'var(--on-surface-variant)' }}>Đơn hàng không có sản phẩm</p>
  ) : (
    <table style={{ width: '100%', borderCollapse: 'collapse' }}>
      <thead>
        <tr>
          <th style={{ textAlign: 'left',   padding: 'var(--space-2)' }}>Sản phẩm</th>
          <th style={{ textAlign: 'center', padding: 'var(--space-2)' }}>Số lượng</th>
          <th style={{ textAlign: 'right',  padding: 'var(--space-2)' }}>Đơn giá</th>
          <th style={{ textAlign: 'right',  padding: 'var(--space-2)' }}>Thành tiền</th>
        </tr>
      </thead>
      <tbody>
        {enriched.map((it) => {
          const unitPrice = it.unitPrice ?? it.price ?? 0;
          const lineTotal = it.lineTotal ?? it.subtotal ?? 0;
          return (
            <tr key={it.id} style={{ borderTop: '1px solid rgba(195,198,214,0.15)' }}>
              <td style={{ padding: 'var(--space-2)', display: 'flex', alignItems: 'center', gap: 'var(--space-3)' }}>
                {it.thumbnailUrl ? (
                  <Image
                    src={it.thumbnailUrl}
                    width={64}
                    height={64}
                    alt={it.productName}
                    style={{ borderRadius: 'var(--radius-md)', objectFit: 'cover', flexShrink: 0 }}
                  />
                ) : (
                  <div
                    style={{
                      width: 64, height: 64,
                      borderRadius: 'var(--radius-md)',
                      background: 'var(--surface-container-high)',
                      display: 'flex', alignItems: 'center', justifyContent: 'center',
                      color: 'var(--on-surface-variant)',
                      flexShrink: 0,
                    }}
                  >
                    📦
                  </div>
                )}
                <div>
                  <div style={{ fontWeight: 500 }}>{it.productName}</div>
                  <div style={{ fontSize: 'var(--text-body-sm)', color: 'var(--on-surface-variant)' }}>
                    {it.brand ?? '—'}
                  </div>
                </div>
              </td>
              <td style={{ textAlign: 'center', padding: 'var(--space-2)' }}>{it.quantity}</td>
              <td style={{ textAlign: 'right',  padding: 'var(--space-2)' }}>{unitPrice.toLocaleString('vi-VN')}₫</td>
              <td style={{ textAlign: 'right',  padding: 'var(--space-2)', fontWeight: 600 }}>{lineTotal.toLocaleString('vi-VN')}₫</td>
            </tr>
          );
        })}
      </tbody>
    </table>
  )}
  <div style={{ borderTop: '1px solid rgba(195,198,214,0.15)', marginTop: 'var(--space-3)', paddingTop: 'var(--space-3)', textAlign: 'right' }}>
    <span style={{ fontWeight: 700, fontSize: 'var(--text-title-sm)' }}>Tổng cộng: </span>
    <span style={{ color: 'var(--primary)', fontWeight: 700 }}>
      {(order.totalAmount ?? order.total ?? 0).toLocaleString('vi-VN')}₫
    </span>
  </div>
</div>
```

CRITICAL constraints:
- D-03: GIỮ inline-style (cardStyle, labelStyle), KHÔNG tạo CSS module mới cho admin.
- D-05: Empty items render `<p>Đơn hàng không có sản phẩm</p>` — KHÔNG render table header trống.
- D-06: `next/image` 64×64; fallback `<div>` 64×64 background `var(--surface-container-high)` + emoji 📦.
- D-01: KHÔNG re-sort items (giữ thứ tự BE return theo `id`).
- KHÔNG hiển thị `productId` UUID (per Claude's Discretion lean).
- KHÔNG còn string "khả dụng sau khi Phase 8" anywhere trong file.

**Step 3 — Status badge dùng statusMap thay STATUS_LABELS:**

Mọi chỗ render status badge: `<Badge variant={statusMap[currentStatus]?.variant ?? 'default'}>{statusMap[currentStatus]?.label ?? currentStatus}</Badge>`. Replace mọi reference STATUS_LABELS/STATUS_VARIANTS còn sót.
  </action>
  <verify>
    <automated>cd sources/frontend && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep "khả dụng sau khi Phase 8" sources/frontend/src/app/admin/orders/[id]/page.tsx` trả về RỖNG (0 match).
    - `grep "Đơn hàng không có sản phẩm" sources/frontend/src/app/admin/orders/[id]/page.tsx` match (D-05 empty state).
    - `grep "useEnrichedItems(order" sources/frontend/src/app/admin/orders/[id]/page.tsx` match.
    - `grep -c "<Image" sources/frontend/src/app/admin/orders/[id]/page.tsx` ≥ 1.
    - `grep "paymentMethodMap\\[order.paymentMethod\\]" sources/frontend/src/app/admin/orders/[id]/page.tsx` match.
    - `grep "shippingAddress.street" sources/frontend/src/app/admin/orders/[id]/page.tsx` match (per D-04).
    - `grep "Thông tin giao hàng" sources/frontend/src/app/admin/orders/[id]/page.tsx` match.
    - `grep "Sản phẩm" sources/frontend/src/app/admin/orders/[id]/page.tsx` match (h3 heading).
    - `grep -E "thead|tbody" sources/frontend/src/app/admin/orders/[id]/page.tsx` ≥ 2 match (table structure).
    - `npx tsc --noEmit` exit code 0.
    - `npm run lint` exit code 0.
  </acceptance_criteria>
  <done>Placeholder gone, items table 4 cột render với thumbnail + brand, shipping/payment thật, empty state, tsc + lint pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin browser → /api/orders/admin/{id} | Admin-authenticated read — JWT middleware đã handle ở Phase 5, KHÔNG đụng |
| Admin browser → /api/products/{id} | Read-only public product data (qua useEnrichedItems) — same surface với /products page |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-03 | I | Admin renders order.shippingAddress (PII: address) | accept | Admin role-gate đã có (existing middleware Phase 5) — admin được phép xem PII customer để xử lý đơn. Phase 17 KHÔNG đổi role guard. |
| T-17-04 | T (Tampering) | Bỏ `as any` cast, dùng Order type | mitigate | Type safety mạnh hơn — TypeScript bắt error tại compile time. tsc gate trong verify. |
| T-17-05 | E (Elevation) | Reuse existing getAdminOrderById endpoint | accept | KHÔNG thêm endpoint mới, KHÔNG đổi auth. No new attack surface. |
</threat_model>

<verification>
- `npx tsc --noEmit` pass.
- `npm run lint` pass.
- Manual check: grep file confirm 0 placeholder, có `<Image>`, có shippingAddress render.
- E2E gate (Wave 3) sẽ assert browser-level.
</verification>

<success_criteria>
- [ ] Placeholder "khả dụng sau khi Phase 8" hoàn toàn gone (REQ ADMIN-06 SC1).
- [ ] Items table 4 cột với thumbnail + brand + name + qty + unit price + line total.
- [ ] Shipping address render từ `order.shippingAddress` (REQ ADMIN-06 SC2).
- [ ] Payment method qua `paymentMethodMap` (D-04).
- [ ] Empty state graceful (D-05).
- [ ] `interface AdminOrder` + `as any` đã xóa hoàn toàn (REQ ADMIN-06 SC3).
- [ ] tsc + lint xanh.
</success_criteria>

<output>
After completion, create `.planning/phases/17-s-a-order-detail-items/17-02-SUMMARY.md` ghi:
- Files modified + line delta
- Confirmation 0 placeholder + 0 `as any`
- Visual check note (URL `/admin/orders/{id}` cần Wave 3 E2E + manual UAT)
- Pending UAT cases (empty items + soft-deleted product)
</output>
