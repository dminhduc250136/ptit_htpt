---
phase: 17-s-a-order-detail-items
plan: 03
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - sources/frontend/src/app/profile/orders/[id]/page.tsx
  - sources/frontend/src/app/profile/orders/[id]/page.module.css
autonomous: true
requirements:
  - ORDER-01
tags:
  - frontend
  - user
  - order-detail
  - image

must_haves:
  truths:
    - "Trang `/profile/orders/[id]` items table render thumbnail 64×64 + brand subtitle dưới productName cho mỗi row."
    - "Trang `/profile/orders/[id]` import statusMap + paymentMethodMap + paymentStatusMap từ @/lib/orderLabels (KHÔNG inline)."
    - "Empty items state render `<p>Đơn hàng không có sản phẩm</p>`."
    - "CSS module có class .itemCellInner (flex container) + .itemThumb / .itemThumbPlaceholder + .itemBrand."
  artifacts:
    - path: "sources/frontend/src/app/profile/orders/[id]/page.tsx"
      provides: "User order detail extend với image + brand per item"
      contains: "useEnrichedItems"
    - path: "sources/frontend/src/app/profile/orders/[id]/page.module.css"
      provides: "Styles cho thumbnail cell + brand subtitle"
      contains: "itemCellInner"
  key_links:
    - from: "sources/frontend/src/app/profile/orders/[id]/page.tsx"
      to: "sources/frontend/src/lib/useEnrichedItems.ts"
      via: "import + call hook trên order.items"
      pattern: "useEnrichedItems\\(order\\??.items\\)"
    - from: "sources/frontend/src/app/profile/orders/[id]/page.tsx"
      to: "sources/frontend/src/lib/orderLabels.ts"
      via: "import 3 maps"
      pattern: "from '@/lib/orderLabels'"
---

<objective>
Extend user order detail page (`/profile/orders/[id]`) — items table thêm thumbnail 64×64 + brand subtitle. DRY: dùng maps + hook từ Plan 01.

Purpose: ORDER-01 success criteria — user phải thấy ảnh + brand cho mỗi item đã mua (hiện tại table chỉ có name/qty/price/subtotal).

Output:
- `sources/frontend/src/app/profile/orders/[id]/page.tsx` — extend cell "Sản phẩm" + replace inline maps + empty state.
- `sources/frontend/src/app/profile/orders/[id]/page.module.css` — append classes cho thumbnail + brand subtitle.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-s-a-order-detail-items/17-CONTEXT.md
@.planning/phases/17-s-a-order-detail-items/17-RESEARCH.md
@.planning/phases/17-s-a-order-detail-items/17-PATTERNS.md
@sources/frontend/src/app/profile/orders/[id]/page.tsx
@sources/frontend/src/app/profile/orders/[id]/page.module.css
@sources/frontend/next.config.ts

<interfaces>
<!-- Contracts từ Wave 1 (Plan 01). -->

From `sources/frontend/src/lib/orderLabels.ts`:
```ts
export const statusMap: Record<string, { label: string; variant: 'default'|'new'|'hot'|'sale'|'out-of-stock' }>;
export const paymentMethodMap: Record<string, string>;
export const paymentStatusMap: Record<string, string>;
```

From `sources/frontend/src/lib/useEnrichedItems.ts`:
```ts
export type EnrichedItem = OrderItem & { thumbnailUrl?: string; brand?: string };
export function useEnrichedItems(items: OrderItem[] | undefined): EnrichedItem[];
```

Existing CSS classes (KHÔNG sửa, nhưng tham khảo pattern):
- `.itemImg` (line 30): `position: relative; width: 72px; height: 72px; border-radius: var(--radius-md); overflow: hidden; flex-shrink: 0;`
- `.tableCell` (line 88-93): padding + font-size + color + vertical-align.
- `.itemsTable`, `.tableRow`, `.itemName`, `.lineTotalCell` — đã có.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace inline maps + add useEnrichedItems hook + extend items table cell (D-01/05/06)</name>
  <read_first>
    - sources/frontend/src/app/profile/orders/[id]/page.tsx (full file — lines 13-32 inline maps, lines 162-175 items table render)
    - .planning/phases/17-s-a-order-detail-items/17-PATTERNS.md (lines 162-208 — User page pattern)
    - .planning/phases/17-s-a-order-detail-items/17-RESEARCH.md (Pattern 3 — Image + placeholder; Pitfall #3 empty items, #5 brand null)
  </read_first>
  <files>sources/frontend/src/app/profile/orders/[id]/page.tsx</files>
  <action>
Modify `sources/frontend/src/app/profile/orders/[id]/page.tsx`:

**Step 1 — Update imports:**

Thêm:
```tsx
import Image from 'next/image';
import { statusMap, paymentMethodMap, paymentStatusMap } from '@/lib/orderLabels';
import { useEnrichedItems } from '@/lib/useEnrichedItems';
```

XÓA inline `const statusMap = {...}`, `const paymentMethodMap = {...}`, `const paymentStatusMap = {...}` (lines 13-32 hiện tại). Mọi reference trong file vẫn dùng tên `statusMap` / `paymentMethodMap` / `paymentStatusMap` không đổi (vì import cùng tên).

**Step 2 — Thêm hook call (cùng level state, sau `setOrder`):**

```tsx
const enriched = useEnrichedItems(order?.items);
```

`order?.items` có thể undefined trong skeleton state — hook trả `[]` an toàn.

**Step 3 — Replace items table block (lines ~162-175 hiện tại):**

Replace tbody render bằng:

```tsx
{enriched.length === 0 ? (
  <p style={{ color: 'var(--on-surface-variant)' }}>Đơn hàng không có sản phẩm</p>
) : (
  <table className={styles.itemsTable}>
    <thead>
      <tr>
        <th className={styles.tableCell}>Sản phẩm</th>
        <th className={styles.tableCell}>Số lượng</th>
        <th className={styles.tableCell}>Đơn giá</th>
        <th className={styles.tableCell}>Thành tiền</th>
      </tr>
    </thead>
    <tbody>
      {enriched.map((item) => {
        const unitPrice = item.unitPrice ?? item.price ?? 0;
        const lineTotal = item.lineTotal ?? item.subtotal ?? 0;
        return (
          <tr key={item.id} className={styles.tableRow}>
            <td className={styles.tableCell}>
              <div className={styles.itemCellInner}>
                {item.thumbnailUrl ? (
                  <Image
                    src={item.thumbnailUrl}
                    width={64}
                    height={64}
                    alt={item.productName}
                    className={styles.itemThumb}
                  />
                ) : (
                  <div className={styles.itemThumbPlaceholder} aria-hidden="true">📦</div>
                )}
                <div>
                  <div className={styles.itemName}>{item.productName}</div>
                  <div className={styles.itemBrand}>{item.brand ?? '—'}</div>
                </div>
              </div>
            </td>
            <td className={styles.tableCell}>{item.quantity}</td>
            <td className={styles.tableCell}>{unitPrice.toLocaleString('vi-VN')}₫</td>
            <td className={`${styles.tableCell} ${styles.lineTotalCell}`}>{lineTotal.toLocaleString('vi-VN')}₫</td>
          </tr>
        );
      })}
    </tbody>
  </table>
)}
```

CRITICAL:
- D-01: thumbnail+brand qua `useEnrichedItems` — KHÔNG fetch trực tiếp trong page.
- D-05: empty state `<p>Đơn hàng không có sản phẩm</p>` — KHÔNG render table header trống.
- D-06: `<Image>` 64×64; fallback `<div className={styles.itemThumbPlaceholder}>` (Task 2 sẽ định nghĩa class).
- KHÔNG re-sort items.
- Brand fallback `'—'` (Pitfall #5).
- Giữ `formatPrice` helper nếu file đã có (toLocaleString equivalent OK — match style hiện tại).

**Step 4 — Đảm bảo các format helpers còn dùng đúng:**

Nếu file hiện dùng `formatPrice(value)` helper, có thể giữ thay `value.toLocaleString('vi-VN')` — chọn 1 cách nhất quán với phần còn lại file.

**KHÔNG sửa**: shipping address render (đã đúng theo user page hiện tại), payment status badge, total row, status section, breadcrumb, skeleton loading — đã hoạt động.
  </action>
  <verify>
    <automated>cd sources/frontend && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^const statusMap" sources/frontend/src/app/profile/orders/[id]/page.tsx` trả về `0` (đã xóa inline).
    - `grep -c "^const paymentMethodMap" sources/frontend/src/app/profile/orders/[id]/page.tsx` trả về `0`.
    - `grep "from '@/lib/orderLabels'" sources/frontend/src/app/profile/orders/[id]/page.tsx` match.
    - `grep "useEnrichedItems(order" sources/frontend/src/app/profile/orders/[id]/page.tsx` match.
    - `grep -c "<Image" sources/frontend/src/app/profile/orders/[id]/page.tsx` ≥ 1.
    - `grep "Đơn hàng không có sản phẩm" sources/frontend/src/app/profile/orders/[id]/page.tsx` match.
    - `grep "itemCellInner" sources/frontend/src/app/profile/orders/[id]/page.tsx` match (class consumer).
    - `grep "itemBrand" sources/frontend/src/app/profile/orders/[id]/page.tsx` match.
    - `npx tsc --noEmit` exit code 0.
    - `npm run lint` exit code 0.
  </acceptance_criteria>
  <done>Inline maps gone, hook wired, items table render thumbnail + brand, empty state, tsc + lint pass.</done>
</task>

<task type="auto">
  <name>Task 2: Append CSS classes cho thumbnail cell + brand subtitle vào page.module.css</name>
  <read_first>
    - sources/frontend/src/app/profile/orders/[id]/page.module.css (full file — kiểm tra `.itemImg`, `.tableCell` đã có; KHÔNG duplicate)
    - .planning/phases/17-s-a-order-detail-items/17-PATTERNS.md (lines 230-262 — CSS rules to append)
  </read_first>
  <files>sources/frontend/src/app/profile/orders/[id]/page.module.css</files>
  <action>
Append (KHÔNG replace) các class sau vào CUỐI file `sources/frontend/src/app/profile/orders/[id]/page.module.css`:

```css

/* Phase 17: thumbnail cell layout cho items table */
.itemCellInner {
  display: flex;
  align-items: center;
  gap: var(--space-3);
}

.itemThumb {
  width: 64px;
  height: 64px;
  border-radius: var(--radius-md);
  overflow: hidden;
  flex-shrink: 0;
  object-fit: cover;
}

.itemThumbPlaceholder {
  width: 64px;
  height: 64px;
  border-radius: var(--radius-md);
  flex-shrink: 0;
  background: var(--surface-container-high);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--on-surface-variant);
}

.itemBrand {
  font-size: var(--text-body-sm);
  color: var(--on-surface-variant);
  margin-top: 2px;
}
```

CRITICAL:
- KHÔNG xóa hoặc sửa class `.itemImg`, `.tableCell`, `.itemsTable`, `.itemName`, `.lineTotalCell`, `.tableRow` — vẫn còn dùng ở các block khác trong page.
- KHÔNG đổi tên class — Task 1 reference đúng các tên này (`itemCellInner`, `itemThumb`, `itemThumbPlaceholder`, `itemBrand`).
- KHÔNG dùng inline-style cho user page (per pattern: user page = CSS module, admin page = inline).
- 64×64 đúng D-06 (admin cũng 64×64 — visual consistency).
  </action>
  <verify>
    <automated>cd sources/frontend && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep "^.itemCellInner" sources/frontend/src/app/profile/orders/[id]/page.module.css` match.
    - `grep "^.itemThumb " sources/frontend/src/app/profile/orders/[id]/page.module.css` match (with trailing space — class definition, không phải Placeholder).
    - `grep "^.itemThumbPlaceholder" sources/frontend/src/app/profile/orders/[id]/page.module.css` match.
    - `grep "^.itemBrand" sources/frontend/src/app/profile/orders/[id]/page.module.css` match.
    - `grep -c "^.itemImg " sources/frontend/src/app/profile/orders/[id]/page.module.css` ≥ 1 (existing class still present).
    - `grep "var(--surface-container-high)" sources/frontend/src/app/profile/orders/[id]/page.module.css` match (D-06 background token).
    - `npx tsc --noEmit` exit code 0 (CSS module typed correctly).
    - `npm run lint` exit code 0.
  </acceptance_criteria>
  <done>4 class mới appended, các class cũ giữ nguyên, build pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User browser → /api/orders/{id} | User-authenticated read (own order) — JWT middleware đã handle Phase 5, KHÔNG đụng |
| User browser → /api/products/{id} | Public read-only product data — same surface với /products listing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-06 | I | next/image render từ thumbnailUrl untrusted | mitigate | next.config.ts whitelist domain (`images.unsplash.com`, `lh3.googleusercontent.com`) — URL ngoài whitelist sẽ runtime error → fallback placeholder. |
| T-17-07 | T | brand text render trực tiếp vào DOM | accept | React auto-escape JSX text → KHÔNG XSS. Brand chỉ string. |
| T-17-08 | E | Reuse existing getOrderById endpoint | accept | KHÔNG thêm endpoint mới, KHÔNG đổi auth. |
</threat_model>

<verification>
- `npx tsc --noEmit` pass.
- `npm run lint` pass.
- Manual check: page render thumbnail + brand subtitle ở mỗi row của order detail.
- E2E gate Wave 3 sẽ assert browser-level.
</verification>

<success_criteria>
- [ ] Items table render thumbnail 64×64 cho mỗi row (REQ ORDER-01 SC1).
- [ ] Brand subtitle hiển thị dưới productName.
- [ ] Inline maps đã thay bằng import từ @/lib/orderLabels.
- [ ] Empty state graceful (D-05).
- [ ] CSS module có 4 class mới (`.itemCellInner`, `.itemThumb`, `.itemThumbPlaceholder`, `.itemBrand`).
- [ ] tsc + lint xanh.
</success_criteria>

<output>
After completion, create `.planning/phases/17-s-a-order-detail-items/17-03-SUMMARY.md` ghi:
- Files modified + line delta
- Confirm `<Image>` + brand subtitle wired
- Pending UAT cases (empty items + soft-deleted product brand fallback)
</output>
