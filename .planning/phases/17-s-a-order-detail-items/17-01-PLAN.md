---
phase: 17-s-a-order-detail-items
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sources/frontend/src/lib/orderLabels.ts
  - sources/frontend/src/lib/useEnrichedItems.ts
autonomous: true
requirements:
  - ORDER-01
  - ADMIN-06
tags:
  - frontend
  - nextjs
  - react-hook
  - order-detail

must_haves:
  truths:
    - "File @/lib/orderLabels.ts export 3 maps: paymentMethodMap, paymentStatusMap, statusMap với Vietnamese labels."
    - "File @/lib/useEnrichedItems.ts export hook useEnrichedItems(items) trả về EnrichedItem[] với thumbnailUrl + brand merged từ getProductById parallel."
    - "Hook dùng Promise.allSettled (không Promise.all) để fetch fail không kill render."
    - "Hook có cancelled flag trong useEffect cleanup để tránh setState after unmount."
  artifacts:
    - path: "sources/frontend/src/lib/orderLabels.ts"
      provides: "Vietnamese label maps cho order status / payment method / payment status"
      exports: ["paymentMethodMap", "paymentStatusMap", "statusMap"]
    - path: "sources/frontend/src/lib/useEnrichedItems.ts"
      provides: "Custom hook parallel-enrich order items với thumbnailUrl + brand"
      exports: ["useEnrichedItems", "EnrichedItem"]
  key_links:
    - from: "sources/frontend/src/lib/useEnrichedItems.ts"
      to: "sources/frontend/src/services/products.ts (getProductById)"
      via: "Promise.allSettled mapping"
      pattern: "Promise\\.allSettled.*getProductById"
---

<objective>
Tạo 2 helper modules trong `src/lib/` làm foundation cho Wave 2 (admin + user page rewrite).

Purpose: DRY hóa Vietnamese label maps (đang inline trong user page) và đóng gói FE-side enrichment pattern (D-01) thành hook reusable cho cả admin + user — tránh duplicate ~30 dòng code.

Output:
- `src/lib/orderLabels.ts` (NEW) — 3 maps extract verbatim từ `profile/orders/[id]/page.tsx:13-32`.
- `src/lib/useEnrichedItems.ts` (NEW) — custom hook parallel `getProductById` enrichment.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-s-a-order-detail-items/17-CONTEXT.md
@.planning/phases/17-s-a-order-detail-items/17-RESEARCH.md
@.planning/phases/17-s-a-order-detail-items/17-PATTERNS.md
@sources/frontend/src/services/products.ts
@sources/frontend/src/types/index.ts
@sources/frontend/src/app/profile/orders/[id]/page.tsx

<interfaces>
<!-- Contracts mà hook + label module phụ thuộc. Executor KHÔNG cần grep codebase. -->

From `sources/frontend/src/services/products.ts:55`:
```ts
export function getProductById(id: string): Promise<Product>;
```

From `sources/frontend/src/types/index.ts` (Product + OrderItem):
```ts
export interface Product {
  id: string;
  name: string;
  brand?: string;
  thumbnailUrl?: string;
  // ... other fields
}

export interface OrderItem {
  id: string;
  productId: string;
  productName: string;
  quantity: number;
  unitPrice?: number;
  price?: number;       // legacy alias
  lineTotal?: number;
  subtotal?: number;    // legacy alias
}
```

From `sources/frontend/src/services/http.ts:106-108`:
```ts
// httpGet auto-unwraps ApiResponse envelope -> resolves with .data directly.
// KHÔNG cần handle { timestamp, status, message, data } thủ công.
```

Existing inline maps (source-of-truth verbatim) — `sources/frontend/src/app/profile/orders/[id]/page.tsx:13-32`:
```ts
const statusMap: Record<string, { label: string; variant: 'default' | 'new' | 'hot' | 'sale' | 'out-of-stock' }> = {
  PENDING:   { label: 'Chờ xác nhận', variant: 'default' },
  CONFIRMED: { label: 'Đã xác nhận',  variant: 'new' },
  SHIPPING:  { label: 'Đang giao',    variant: 'hot' },
  DELIVERED: { label: 'Đã giao',      variant: 'sale' },
  CANCELLED: { label: 'Đã hủy',       variant: 'out-of-stock' },
};
const paymentMethodMap: Record<string, string> = {
  COD: 'Thanh toán khi nhận hàng',
  BANK_TRANSFER: 'Chuyển khoản ngân hàng',
  E_WALLET: 'Ví điện tử',
};
const paymentStatusMap: Record<string, string> = {
  PENDING: 'Chờ thanh toán',
  PAID: 'Đã thanh toán',
  FAILED: 'Thanh toán thất bại',
  REFUNDED: 'Đã hoàn tiền',
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tạo `src/lib/orderLabels.ts` với 3 Vietnamese label maps</name>
  <read_first>
    - sources/frontend/src/app/profile/orders/[id]/page.tsx (lines 13-32 — source-of-truth maps)
    - sources/frontend/src/lib/ (kiểm tra directory tồn tại; nếu chưa → tạo mới)
  </read_first>
  <files>sources/frontend/src/lib/orderLabels.ts</files>
  <action>
Tạo file mới `sources/frontend/src/lib/orderLabels.ts`. Implement extract verbatim từ user page `sources/frontend/src/app/profile/orders/[id]/page.tsx:13-32` (per D-04 — DRY paymentMethodMap; per CONTEXT.md `code_context.Reusable Assets`).

Nội dung file (đầy đủ, KHÔNG ngắn):

```ts
// src/lib/orderLabels.ts
// Phase 17 / D-04: extract Vietnamese label maps để reuse cho admin + user order pages.
// Source verbatim từ profile/orders/[id]/page.tsx:13-32.

export const statusMap: Record<
  string,
  { label: string; variant: 'default' | 'new' | 'hot' | 'sale' | 'out-of-stock' }
> = {
  PENDING:   { label: 'Chờ xác nhận', variant: 'default' },
  CONFIRMED: { label: 'Đã xác nhận',  variant: 'new' },
  SHIPPING:  { label: 'Đang giao',    variant: 'hot' },
  DELIVERED: { label: 'Đã giao',      variant: 'sale' },
  CANCELLED: { label: 'Đã hủy',       variant: 'out-of-stock' },
};

export const paymentMethodMap: Record<string, string> = {
  COD: 'Thanh toán khi nhận hàng',
  BANK_TRANSFER: 'Chuyển khoản ngân hàng',
  E_WALLET: 'Ví điện tử',
};

export const paymentStatusMap: Record<string, string> = {
  PENDING: 'Chờ thanh toán',
  PAID: 'Đã thanh toán',
  FAILED: 'Thanh toán thất bại',
  REFUNDED: 'Đã hoàn tiền',
};
```

KHÔNG thêm field nào ngoài 3 maps trên. KHÔNG export default. KHÔNG đổi label tiếng Việt (giữ verbatim cho idempotent migration ở Wave 2).
  </action>
  <verify>
    <automated>cd sources/frontend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - File `sources/frontend/src/lib/orderLabels.ts` tồn tại.
    - `grep -c "^export const " sources/frontend/src/lib/orderLabels.ts` trả về `3`.
    - `grep "paymentMethodMap" sources/frontend/src/lib/orderLabels.ts` match (export name chính xác).
    - `grep "Chờ xác nhận" sources/frontend/src/lib/orderLabels.ts` match (Vietnamese label preserved).
    - `npx tsc --noEmit` exit code 0 (không type error).
  </acceptance_criteria>
  <done>3 maps export đúng tên, Vietnamese labels verbatim, tsc pass.</done>
</task>

<task type="auto">
  <name>Task 2: Tạo `src/lib/useEnrichedItems.ts` custom hook (D-01 enrichment)</name>
  <read_first>
    - .planning/phases/17-s-a-order-detail-items/17-RESEARCH.md (Pattern 1 — lines 156-184; Pitfalls #5/#7/#8)
    - .planning/phases/17-s-a-order-detail-items/17-PATTERNS.md (lines 320-368 — full hook content)
    - sources/frontend/src/services/products.ts (verify getProductById signature)
    - sources/frontend/src/types/index.ts (verify OrderItem, Product types)
  </read_first>
  <files>sources/frontend/src/lib/useEnrichedItems.ts</files>
  <action>
Tạo file mới `sources/frontend/src/lib/useEnrichedItems.ts`. Implement per D-01 (CONTEXT.md): FE-side enrichment qua `Promise.allSettled([getProductById(productId), ...])` cho mỗi unique productId, merge `thumbnailUrl + brand` vào view-model.

Nội dung file (đầy đủ — copy verbatim từ PATTERNS.md §useEnrichedItems lines 320-364):

```ts
// src/lib/useEnrichedItems.ts
// Phase 17 / D-01: parallel fetch product detail (image+brand) cho mỗi unique
// productId trong order.items. Dùng Promise.allSettled để 1 product 404 không
// kill toàn bộ render. Cleanup `cancelled` flag để tránh setState after unmount.
'use client';

import { useEffect, useState } from 'react';
import { getProductById } from '@/services/products';
import type { OrderItem } from '@/types';

export type EnrichedItem = OrderItem & {
  thumbnailUrl?: string;
  brand?: string;
};

type EnrichmentMap = Record<string, { thumbnailUrl?: string; brand?: string }>;

export function useEnrichedItems(items: OrderItem[] | undefined): EnrichedItem[] {
  const [map, setMap] = useState<EnrichmentMap>({});

  useEffect(() => {
    if (!items?.length) return;
    const uniqueIds = [...new Set(items.map(i => i.productId))];
    let cancelled = false;
    Promise.allSettled(uniqueIds.map(id => getProductById(id))).then(results => {
      if (cancelled) return;
      const next: EnrichmentMap = {};
      results.forEach((r, i) => {
        if (r.status === 'fulfilled' && r.value) {
          next[uniqueIds[i]] = {
            thumbnailUrl: r.value.thumbnailUrl,
            brand: r.value.brand,
          };
        }
      });
      setMap(next);
    });
    return () => { cancelled = true; };
  }, [items]);

  return (items ?? []).map(it => ({ ...it, ...map[it.productId] }) as EnrichedItem);
}
```

CRITICAL — KHÔNG được:
- Đổi `Promise.allSettled` thành `Promise.all` (Pitfall #5: 1 product 404 sẽ kill toàn bộ render).
- Bỏ `cancelled` flag (Pitfall #7: setState after unmount warning).
- Mutate `OrderItem` type (Anti-Pattern: pollute domain type) — phải dùng view-model `EnrichedItem`.
- Dedup KHÔNG được tạo Set từ raw items (Pitfall: legacy order có duplicate productId — phải `[...new Set(items.map(i => i.productId))]`).
  </action>
  <verify>
    <automated>cd sources/frontend && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - File `sources/frontend/src/lib/useEnrichedItems.ts` tồn tại.
    - `grep "'use client'" sources/frontend/src/lib/useEnrichedItems.ts` match (Next 16 client component directive).
    - `grep "Promise.allSettled" sources/frontend/src/lib/useEnrichedItems.ts` match (KHÔNG `Promise.all`).
    - `grep "let cancelled" sources/frontend/src/lib/useEnrichedItems.ts` match (cleanup flag present).
    - `grep "export type EnrichedItem" sources/frontend/src/lib/useEnrichedItems.ts` match.
    - `grep "export function useEnrichedItems" sources/frontend/src/lib/useEnrichedItems.ts` match.
    - `grep "new Set(items.map" sources/frontend/src/lib/useEnrichedItems.ts` match (dedup).
    - `npx tsc --noEmit` exit code 0.
    - `npm run lint` exit code 0 (no react-hooks/exhaustive-deps warning).
  </acceptance_criteria>
  <done>Hook export đúng tên, dùng Promise.allSettled, có cleanup flag, dedup unique productIds, tsc + lint pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → API gateway | Read-only `GET /api/products/{id}` calls — không nhận user input nào |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | I (Information Disclosure) | useEnrichedItems → /api/products/{id} | accept | Read-only product data đã public ở `/products` listing — không expose new attack surface. Phase thuần FE rendering bug fix. |
| T-17-02 | D (Denial of Service) | Promise.allSettled với N unique productIds | accept | Real orders 1-5 items — browser concurrency limit ~6 sẽ stagger. Defer batch endpoint v1.4+ per CONTEXT.md (D-01). |
</threat_model>

<verification>
Sau khi cả 2 task hoàn thành:
- `cd sources/frontend && npx tsc --noEmit` (no type errors).
- `cd sources/frontend && npm run lint` (no lint errors).
- 2 file mới tồn tại: `src/lib/orderLabels.ts` + `src/lib/useEnrichedItems.ts`.
- Wave 2 plans (02 + 03) có thể import ngay không cần modify thêm.
</verification>

<success_criteria>
- [ ] `src/lib/orderLabels.ts` export 3 maps với Vietnamese labels verbatim.
- [ ] `src/lib/useEnrichedItems.ts` export hook + type, dùng Promise.allSettled + cancelled cleanup.
- [ ] tsc + lint pass.
- [ ] Foundation sẵn cho Wave 2 admin + user pages.
</success_criteria>

<output>
After completion, create `.planning/phases/17-s-a-order-detail-items/17-01-SUMMARY.md` ghi:
- 2 file mới + line count
- Hook signature đã định
- Lint/tsc status
- Note cho Wave 2: import path `@/lib/orderLabels` + `@/lib/useEnrichedItems`
</output>
