---
phase: 17-s-a-order-detail-items
plan: 04
type: execute
wave: 3
depends_on: ["17-02", "17-03"]
files_modified:
  - sources/frontend/e2e/admin-orders.spec.ts
  - sources/frontend/e2e/order-detail.spec.ts
autonomous: true
requirements:
  - ORDER-01
  - ADMIN-06
tags:
  - frontend
  - e2e
  - playwright
  - regression

must_haves:
  truths:
    - "E2E spec admin-orders.spec.ts có test mới assert `/admin/orders/[id]` KHÔNG còn placeholder 'khả dụng sau khi Phase 8'."
    - "E2E spec admin-orders.spec.ts assert h3 'Thông tin giao hàng' + h3 'Sản phẩm' visible."
    - "E2E spec order-detail.spec.ts ORD-DTL-2 extend assertion: items table có ≥1 row visible (skip nếu seed rỗng)."
    - "Cả 2 spec chạy xanh local: `npx playwright test e2e/order-detail.spec.ts e2e/admin-orders.spec.ts`."
  artifacts:
    - path: "sources/frontend/e2e/admin-orders.spec.ts"
      provides: "E2E regression cho ADMIN-06 — placeholder gone + cards visible"
      contains: "ADM-ORD-3"
    - path: "sources/frontend/e2e/order-detail.spec.ts"
      provides: "E2E regression cho ORDER-01 — items rendering extend"
      contains: "table tbody tr"
  key_links:
    - from: "sources/frontend/e2e/admin-orders.spec.ts"
      to: "/admin/orders/[id] page"
      via: "page.goto + click detail button"
      pattern: "Xem chi tiết đơn hàng"
    - from: "sources/frontend/e2e/order-detail.spec.ts"
      to: "/profile/orders/[id] page"
      via: "ORD-DTL-2 navigation flow"
      pattern: "ORD-DTL-2"
---

<objective>
Extend 2 Playwright E2E specs để regression-guard cho Phase 17 fixes.

Purpose: Locked-in protection chống regression khi future phases (18-22) modify order pages. Hiện tại E2E chỉ check 4 column headers + 2 info card headings — KHÔNG catch được nếu placeholder text reappear hoặc table rỗng.

Output:
- `e2e/admin-orders.spec.ts` — thêm test `ADM-ORD-3` cho admin detail page.
- `e2e/order-detail.spec.ts` — extend `ORD-DTL-2` thêm assertion table rows visible.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-s-a-order-detail-items/17-PATTERNS.md
@.planning/phases/17-s-a-order-detail-items/17-VALIDATION.md
@sources/frontend/e2e/admin-orders.spec.ts
@sources/frontend/e2e/order-detail.spec.ts
@sources/frontend/playwright.config.ts

<interfaces>
<!-- Existing E2E patterns. -->

From `e2e/admin-orders.spec.ts` (existing tests):
- `ADM-ORD-1`, `ADM-ORD-2` đã có; sử dụng admin storageState để bypass login.
- Navigation pattern: `page.goto('/admin/orders')` → click `[aria-label="Xem chi tiết đơn hàng"]` → `waitForURL(/\/admin\/orders\/[^/]+$/)`.
- Có pattern `test.skip(true, 'reason')` nếu không có data.

From `e2e/order-detail.spec.ts` ORD-DTL-2 (lines 34-74):
- Navigate qua `/profile/orders` → click "Xem chi tiết" → URL `/profile/orders/[id]`.
- Đã assert 4 column headers: "Sản phẩm", "Số lượng", "Đơn giá", "Thành tiền".
- Đã assert info card headings.

From Playwright config: storageState pre-loaded; baseURL = http://localhost:3000 (or env-driven).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append `ADM-ORD-3` test cho admin detail page (placeholder gone + headings visible)</name>
  <read_first>
    - sources/frontend/e2e/admin-orders.spec.ts (full file — copy navigation pattern từ ADM-ORD-2)
    - .planning/phases/17-s-a-order-detail-items/17-PATTERNS.md (lines 380-401 — full ADM-ORD-3 test code)
  </read_first>
  <files>sources/frontend/e2e/admin-orders.spec.ts</files>
  <action>
Append test mới vào CUỐI file `sources/frontend/e2e/admin-orders.spec.ts` (sau test cuối cùng, trước `});` đóng describe nếu có wrapper, hoặc trước EOF nếu không có).

Test content (verbatim từ PATTERNS.md):

```ts
test('ADM-ORD-3: detail page render items + KHÔNG còn placeholder Phase 8', async ({ page }) => {
  await page.goto('/admin/orders');
  await expect(page.getByRole('heading', { name: 'Quản lý đơn hàng' })).toBeVisible({ timeout: 10000 });
  await page.waitForTimeout(2000);

  const detailBtn = page.locator('[aria-label="Xem chi tiết đơn hàng"]').first();
  const btnVisible = await detailBtn.isVisible({ timeout: 5000 }).catch(() => false);
  if (!btnVisible) {
    test.skip(true, 'Chưa có đơn hàng — cần seed trước');
    return;
  }
  await detailBtn.click();
  await page.waitForURL(/\/admin\/orders\/[^/]+$/, { timeout: 10000 });

  // ADMIN-06 SC1: placeholder cũ KHÔNG còn xuất hiện
  await expect(page.getByText('khả dụng sau khi Phase 8')).toHaveCount(0);

  // ADMIN-06 SC2: shipping + items section render
  await expect(page.getByText('Thông tin giao hàng')).toBeVisible({ timeout: 5000 });
  await expect(page.getByRole('heading', { name: 'Sản phẩm' })).toBeVisible();
});
```

CRITICAL:
- KHÔNG đổi pattern navigation (giữ giống ADM-ORD-1/2 để chia sẻ storageState).
- Có `test.skip` graceful nếu chưa seed order — assumption A2 (RESEARCH).
- `toHaveCount(0)` KHÔNG `toBeHidden()` — text PHẢI gone hoàn toàn (regression-guard mạnh hơn).
- Heading "Sản phẩm" dùng `getByRole('heading')` để tránh match text trùng trong items name.
- Heading "Thông tin giao hàng" dùng `getByText` (vì có thể là `<h3>` plain text).

KHÔNG sửa các test ADM-ORD-1, ADM-ORD-2 hiện có.
  </action>
  <verify>
    <automated>cd sources/frontend && npx playwright test e2e/admin-orders.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep "ADM-ORD-3" sources/frontend/e2e/admin-orders.spec.ts` match.
    - `grep "khả dụng sau khi Phase 8" sources/frontend/e2e/admin-orders.spec.ts` match (assertion text).
    - `grep "toHaveCount(0)" sources/frontend/e2e/admin-orders.spec.ts` match.
    - `grep -c "test('ADM-ORD-" sources/frontend/e2e/admin-orders.spec.ts` ≥ 3 (3 tests total).
    - Playwright run: tất cả tests trong admin-orders.spec.ts xanh hoặc skip (skip OK nếu chưa seed). KHÔNG fail.
  </acceptance_criteria>
  <done>ADM-ORD-3 added, spec runs green/skip, regression locked.</done>
</task>

<task type="auto">
  <name>Task 2: Extend ORD-DTL-2 assertion + Playwright suite gate</name>
  <read_first>
    - sources/frontend/e2e/order-detail.spec.ts (full file — đặc biệt lines 34-74 ORD-DTL-2 block)
    - .planning/phases/17-s-a-order-detail-items/17-PATTERNS.md (lines 412-422 — ORD-DTL-2 extend snippet)
  </read_first>
  <files>sources/frontend/e2e/order-detail.spec.ts</files>
  <action>
Modify `sources/frontend/e2e/order-detail.spec.ts` — extend test `ORD-DTL-2`.

Tìm vị trí cuối cùng của test `ORD-DTL-2` (sau line ~73 — sau assertion cuối cùng, trước `});` đóng test). Append:

```ts
  // Phase 17 / ORDER-01: assert items table có ≥ 1 row visible
  // (skip nếu seed order rỗng — D-05 empty state cũng valid render)
  const rowCount = await page.locator('table tbody tr').count();
  if (rowCount > 0) {
    await expect(page.locator('table tbody tr').first()).toBeVisible();
    // Brand subtitle có thể là "—" nếu enrichment fail — chỉ check first row visible
  }
```

KHÔNG:
- Assert `<img>` thumbnail src cụ thể (per Open Question #2 — async race với Promise.allSettled khó deterministic).
- Assert brand text exact (có thể là "—" fallback).
- Đổi navigation flow của ORD-DTL-2.
- Thêm `await page.waitForTimeout` dài — Playwright auto-wait đủ.

Lý do graceful skip: nếu seed order chỉ tồn tại nhưng items rỗng (legacy), D-05 render `<p>Đơn hàng không có sản phẩm</p>` — assertion `rowCount > 0` skip, test vẫn pass.

**Phase gate verify** sau khi 2 task done: chạy full subset:
```
cd sources/frontend && npx playwright test e2e/order-detail.spec.ts e2e/admin-orders.spec.ts
```
Tất cả tests phải pass hoặc skip có lý do — KHÔNG có failure.
  </action>
  <verify>
    <automated>cd sources/frontend && npx playwright test e2e/order-detail.spec.ts e2e/admin-orders.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep "table tbody tr" sources/frontend/e2e/order-detail.spec.ts` match.
    - `grep "Phase 17" sources/frontend/e2e/order-detail.spec.ts` match (comment marker).
    - `grep -c "test('ORD-DTL-" sources/frontend/e2e/order-detail.spec.ts` không giảm so với trước (KHÔNG xóa test cũ).
    - Playwright run combined: 0 failed tests. Tất cả pass hoặc skip-with-reason.
    - Exit code playwright run = 0.
  </acceptance_criteria>
  <done>ORD-DTL-2 extended; combined Playwright subset xanh.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test runner → app under test | E2E browser khởi từ Playwright local — KHÔNG production traffic |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-09 | — | E2E test code | accept | Test-only changes — KHÔNG ship vào production runtime. No new attack surface. |
</threat_model>

<verification>
- `cd sources/frontend && npx playwright test e2e/order-detail.spec.ts e2e/admin-orders.spec.ts` exit code 0.
- 0 failed test; skip OK nếu seed thiếu.
- Manual UAT (document trong SUMMARY): empty items + soft-deleted product fallback.
</verification>

<success_criteria>
- [ ] `e2e/admin-orders.spec.ts` có test ADM-ORD-3 assert no-placeholder + headings visible (REQ ADMIN-06).
- [ ] `e2e/order-detail.spec.ts` ORD-DTL-2 extend assert items table có ≥1 row khi data có (REQ ORDER-01).
- [ ] Playwright subset chạy xanh.
- [ ] Phase 17 regression-locked tại E2E layer.
</success_criteria>

<output>
After completion, create `.planning/phases/17-s-a-order-detail-items/17-04-SUMMARY.md` ghi:
- Tests added/extended + run output summary (passed / skipped count)
- Manual UAT checklist còn lại:
  1. Empty items state — visit order rỗng (legacy) hoặc force devtools, verify `<p>Đơn hàng không có sản phẩm</p>`.
  2. Soft-deleted product fallback — soft-delete 1 product, verify thumbnail = placeholder div + brand = "—".
  3. Brand null product — visit order chứa product brand null, verify subtitle = "—".
- Phase 17 sẵn sàng cho `/gsd-verify-work`.
</output>
