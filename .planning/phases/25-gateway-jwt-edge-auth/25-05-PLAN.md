# Phase 25 — Plan 05: docs/security.md + VERIFICATION + SUMMARY

**Phase:** 25
**Plan:** 05 of 05
**Depends on:** 25-04

## Goal

Tài liệu hóa edge authentication: threat model, allow-list rationale, regression-guard cho `orders-cross-user-leak`. Verification thủ công. Phase summary.

Reference: 25-CONTEXT.md D-23, D-24.

## Tasks

### T-25-05-01: docs/security.md

- Tạo `docs/security.md` (tiếng Việt) với section:
  1. **Trust boundary** — diagram text:
     ```
     [Browser] ──HTTPS── [API Gateway :8080] ──Docker network── [Backend services]
                              ▲
                              │ JWT verify ở đây
                              │ Strip & inject X-User-Id ở đây
     ```
  2. **Threat: client-forged X-User-Id** — mô tả vector cũ + cách Phase 25 vá (filter strip + inject from sub claim).
  3. **Allow-list** — bảng public endpoint + lý do (catalog browse anonymous; login/register cần public để khởi tạo session).
  4. **Admin protection** — pattern `/api/*/admin/**` yêu cầu role ADMIN.
  5. **JWT_SECRET production warning** — nhắc override env, không dùng dev default.
  6. **Defense in depth** — note rằng backend service vẫn check ownership ở DB (commit e7f72fb) → 2 lớp bảo vệ.
  7. **Deferred** — list từ CONTEXT D-25..D-28.

### T-25-05-02: 25-VERIFICATION.md

- Manual test result (chạy thật, dán output):
  - `curl localhost:8081/users` → connection refused ✓
  - `curl localhost:8082/products` → connection refused ✓
  - `curl localhost:8080/api/products` (no Bearer) → 200 ✓
  - `curl localhost:8080/api/orders` (no Bearer) → 401 `AUTH_TOKEN_MISSING` ✓
  - `curl localhost:8080/api/orders -H "Authorization: Bearer <bob>" -H "X-User-Id: <alice>"` → returns Bob's orders only ✓
  - `curl localhost:8080/api/users/admin/users -H "Authorization: Bearer <user>"` → 403 ✓
  - Token expired → 401 `AUTH_TOKEN_EXPIRED` ✓ (generate expired token bằng test helper hoặc đợi 24h — chấp nhận skip nếu không feasible, đã cover ở unit test).
- Ghi pass/fail mỗi D-XX của CONTEXT.

### T-25-05-03: 25-SUMMARY.md

- File-changed list (count).
- Files mới: 5 file Java mới ở gateway, 1 file Playwright test, `docs/security.md`.
- Defense-in-depth note: e7f72fb là lớp 1 (service ownership check), Phase 25 là lớp 2 (gateway trust boundary).
- Deferred ideas: D-25..D-28.
- Migration FE: developer pull branch này phải clear localStorage token cũ (nếu issued bằng secret khác) — nhắc trong README.

### T-25-05-04: README.md update

- Section "Security": link tới `docs/security.md` + tóm tắt 1 dòng.

## Acceptance

- `docs/security.md` exist với 7 section.
- `25-VERIFICATION.md` ghi đủ kết quả manual test.
- `25-SUMMARY.md` exist.
- `README.md` có link security doc.

## Commit

`docs(25): security.md threat model + 25-VERIFICATION + SUMMARY`
