---
phase: 10-user-svc-schema-profile-editing
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - sources/frontend/package.json
  - sources/frontend/package-lock.json
autonomous: true
requirements:
  - ACCT-03
tags:
  - frontend
  - dependencies
  - rhf-zod-foundation

must_haves:
  truths:
    - "package.json chứa react-hook-form, zod, @hookform/resolvers ở dependencies"
    - "package-lock.json đã regenerate với 3 packages mới"
    - "`npm run build` exit 0 sau install (không break existing build)"
  artifacts:
    - path: "sources/frontend/package.json"
      provides: "Dependency manifest với 3 deps mới"
      contains: "react-hook-form"
    - path: "sources/frontend/package-lock.json"
      provides: "Locked dependency tree"
      contains: "react-hook-form"
  key_links:
    - from: "package.json dependencies"
      to: "node_modules/react-hook-form, zod, @hookform/resolvers"
      via: "npm install"
      pattern: "react-hook-form.*zod.*@hookform/resolvers"
---

<objective>
Install 3 dependencies foundational cho rhf+zod form pattern (lần đầu trong codebase, set pattern cho v1.2 — per CONTEXT §code_context):
- `react-hook-form` (~25KB)
- `zod` (~50KB)
- `@hookform/resolvers` (<5KB cầu nối zod ↔ rhf)

Tổng ~80KB — chấp nhận được cho project demo.

Purpose: Plan 10-03 cần các deps này để build Profile Info form. Tách thành plan riêng để Wave 1 chạy parallel với backend (Plan 10-01).
Output: package.json + package-lock.json updated, build vẫn pass.

Toast (`react-hot-toast` etc.) KHÔNG install — codebase đã có ToastProvider/useToast (xác nhận qua PATTERNS.md §Shared Patterns/Toast). Avatar deps (none — defer).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-user-svc-schema-profile-editing/10-CONTEXT.md
@.planning/phases/10-user-svc-schema-profile-editing/10-PATTERNS.md
@sources/frontend/package.json
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Install react-hook-form + zod + @hookform/resolvers</name>
  <read_first>
    - sources/frontend/package.json (file đang sửa — verify current deps tránh duplicate)
    - 10-PATTERNS.md §rhf+zod "Dependencies cần install"
  </read_first>
  <files>
    sources/frontend/package.json
    sources/frontend/package-lock.json
  </files>
  <action>
    Chạy install command tại `sources/frontend/`:
    ```bash
    cd sources/frontend && npm install react-hook-form zod @hookform/resolvers
    ```

    Lý do version range: KHÔNG pin major manually — để npm chọn latest stable tương thích React 19.2.4 + Next 16.2.3. Cả 3 packages đều react-version-agnostic (rhf hỗ trợ React 16-19; zod runtime-only; @hookform/resolvers peer rhf).

    KHÔNG dùng `--save-exact` (giữ caret range mặc định, consistent với deps hiện tại trong package.json).

    Sau install:
    1. Verify build vẫn pass: `npm run build`
    2. Verify lint không break: `npm run lint`

    Nếu install fail (peer warning v.v.) → resolve bằng cách thử `--legacy-peer-deps` chỉ KHI có concrete error. KHÔNG add cờ này preemptively.
  </action>
  <verify>
    <automated>cd sources/frontend && grep -q '"react-hook-form"' package.json && grep -q '"zod"' package.json && grep -q '"@hookform/resolvers"' package.json && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - package.json grep `"react-hook-form":` → 1 match dưới `dependencies` (không phải devDependencies)
    - package.json grep `"zod":` → 1 match dưới `dependencies`
    - package.json grep `"@hookform/resolvers":` → 1 match dưới `dependencies`
    - package-lock.json grep `"node_modules/react-hook-form"` → ≥ 1 match
    - `npm run build` exit 0 (build production thành công, không TS error)
    - `npm run lint` exit 0
  </acceptance_criteria>
  <done>
    3 deps đã có trong dependencies + lockfile, frontend build production pass, lint clean.
  </done>
</task>

</tasks>

<verification>
**Sanity test sau install:**
```bash
cd sources/frontend
node -e "console.log(require('react-hook-form').useForm.name)"  # Expect: useForm
node -e "console.log(require('zod').z)"                          # Expect: object
node -e "console.log(require('@hookform/resolvers/zod').zodResolver.name)" # Expect: zodResolver
```
</verification>

<success_criteria>
- [ ] 3 packages có trong `dependencies` của package.json
- [ ] package-lock.json updated với 3 packages
- [ ] `npm run build` pass
- [ ] `npm run lint` pass
- [ ] Tổng size deps tăng ~80KB (acceptable per CONTEXT)
</success_criteria>

<output>
Sau khi completion, tạo `.planning/phases/10-user-svc-schema-profile-editing/10-02-SUMMARY.md`.
</output>
