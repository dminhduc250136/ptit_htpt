---
phase: 10-user-svc-schema-profile-editing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/web/UserMeController.java
  - sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/web/UpdateMeRequest.java
  - sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/service/UserProfileService.java
  - sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/domain/UserDto.java
  - sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/domain/UserMapper.java
autonomous: true
requirements:
  - ACCT-03
deferred_requirements:
  - ACCT-04  # Avatar upload — defer Phase 10 per CONTEXT D-08
tags:
  - backend
  - user-service
  - profile

must_haves:
  truths:
    - "GET /api/users/me trả 200 + UserDto chứa hasAvatar:false khi token hợp lệ"
    - "PATCH /api/users/me với {fullName, phone} hợp lệ → 200 + UserDto đã cập nhật + DB row updated"
    - "PATCH /api/users/me với phone không match regex → 400 VALIDATION_ERROR + fieldErrors[].field='phone'"
    - "GET/PATCH /api/users/me thiếu Bearer token → 401"
  artifacts:
    - path: "sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/web/UpdateMeRequest.java"
      provides: "DTO record cho PATCH body với jakarta validation"
      contains: "record UpdateMeRequest"
    - path: "sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/service/UserProfileService.java"
      provides: "Service layer getMe/updateMe"
      contains: "class UserProfileService"
    - path: "sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/web/UserMeController.java"
      provides: "Endpoints GET /users/me + PATCH /users/me"
      contains: "@GetMapping"
    - path: "sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/domain/UserDto.java"
      provides: "UserDto với hasAvatar boolean field"
      contains: "boolean hasAvatar"
  key_links:
    - from: "UserMeController.getMe / updateMe"
      to: "UserProfileService"
      via: "constructor injection"
      pattern: "private final UserProfileService profileService"
    - from: "UserMeController"
      to: "extractUserIdFromBearer"
      via: "JWT subject claim"
      pattern: "extractUserIdFromBearer\\(authHeader\\)"
    - from: "UserMapper.toDto"
      to: "UserDto"
      via: "constructor arg hasAvatar=false"
      pattern: "false,\\s*//.*hasAvatar|new UserDto\\("
---

<objective>
Ship backend endpoints `GET /users/me` + `PATCH /users/me` cho self-service profile read/update (ACCT-03), đồng thời extend `UserDto` thêm `hasAvatar:boolean` (luôn `false` trong Phase 10 vì avatar defer per D-08).

Purpose: Cung cấp wire format cho frontend Profile Info form (Plan 10-03) — không có endpoint backend thì FE không có gì để gọi.
Output: 5 file Java (1 NEW DTO record, 1 NEW service, 3 MODIFY) + endpoints hoạt động qua gateway `/api/users/me`.

ACCT-04 (avatar upload) DEFERRED per CONTEXT.md D-08 — KHÔNG có task implement V3 migration / multipart / Thumbnailator trong plan này.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-user-svc-schema-profile-editing/10-CONTEXT.md
@.planning/phases/10-user-svc-schema-profile-editing/10-PATTERNS.md
@.planning/codebase/CONVENTIONS.md
@sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/web/UserMeController.java
@sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/web/ChangePasswordRequest.java
@sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/service/UserPasswordService.java
@sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/service/UserCrudService.java
@sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/domain/UserDto.java
@sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/domain/UserMapper.java
@sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/domain/UserEntity.java
@sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/api/ApiResponse.java

<interfaces>
<!-- Contracts để executor không phải scavenger hunt. Trích từ codebase. -->

UserMeController hiện có (sẽ EXTEND, KHÔNG rewrite):
```java
@RestController
@RequestMapping("/users/me")
public class UserMeController {
    private final UserPasswordService passwordService;
    private final JwtUtils jwtUtils;
    // Existing: POST /users/me/password (Phase 9)
    // Existing helper: private String extractUserIdFromBearer(String authHeader)
}
```

UserDto hiện tại (record — sẽ thêm field `hasAvatar`):
```java
public record UserDto(
    String id, String username, String email, String roles,
    String fullName, String phone,
    Instant createdAt, Instant updatedAt
) {}
```

UserEntity setters đã có (KHÔNG cần thêm):
```java
public void setFullName(String fullName) { this.fullName = fullName; this.updatedAt = Instant.now(); }
public void setPhone(String phone) { this.phone = phone; this.updatedAt = Instant.now(); }
public void touch() { this.updatedAt = Instant.now(); }
```

ApiResponse envelope:
```java
public static <T> ApiResponse<T> of(int status, String message, T data) { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extend UserDto + UserMapper với hasAvatar field</name>
  <read_first>
    - sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/domain/UserDto.java (file đang sửa)
    - sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/domain/UserMapper.java (file đang sửa)
    - 10-PATTERNS.md §4 "UserDto.java + UserMapper.java" (analog: chính nó — extend record)
  </read_first>
  <files>
    sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/domain/UserDto.java
    sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/domain/UserMapper.java
  </files>
  <action>
    Per D-06, thêm field `boolean hasAvatar` vào UserDto record, đặt giữa `phone` và `createdAt`:
    ```java
    public record UserDto(
        String id, String username, String email, String roles,
        String fullName, String phone,
        boolean hasAvatar,
        Instant createdAt, Instant updatedAt
    ) {}
    ```
    Update `UserMapper.toDto(UserEntity e)` để pass `false` ở vị trí thứ 7 (giữa phone và createdAt):
    ```java
    return new UserDto(
        e.id(), e.username(), e.email(), e.roles(),
        e.fullName(), e.phone(),
        false,  // D-06: hasAvatar Phase 10 luôn false (avatar defer per D-08)
        e.createdAt(), e.updatedAt()
    );
    ```
    Nếu UserMapper có thêm overload nào khác, update đồng bộ. Mọi caller hiện tại của UserMapper.toDto (UserCrudService, AuthService) tự động nhận hasAvatar=false — KHÔNG break wire format vì chỉ thêm field.
  </action>
  <verify>
    <automated>cd sources/backend/user-service && mvn -q compile</automated>
  </verify>
  <acceptance_criteria>
    - File UserDto.java grep `boolean hasAvatar` → 1 match
    - File UserMapper.java grep `false,.*hasAvatar` (hoặc tương đương comment) → 1 match
    - `mvn compile` exit 0 (không có call site nào của `new UserDto(...)` còn dùng signature cũ)
    - `grep -rn "new UserDto(" sources/backend/user-service/src` → mọi call site có 9 args (không còn 8)
  </acceptance_criteria>
  <done>
    UserDto record có 9 components, UserMapper.toDto() gọi constructor với 9 args, project compile thành công.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Tạo UpdateMeRequest record + UserProfileService</name>
  <read_first>
    - sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/web/ChangePasswordRequest.java (analog DTO record + jakarta validation)
    - sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/service/UserPasswordService.java (analog service @Service @Transactional)
    - sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/service/UserCrudService.java lines ~131-144 (analog `AdminUserPatchRequest` + conditional update pattern)
    - 10-PATTERNS.md §2 (UpdateMeRequest) + §3 (UserProfileService)
  </read_first>
  <files>
    sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/web/UpdateMeRequest.java
    sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/service/UserProfileService.java
  </files>
  <action>
    **Tạo NEW file `UpdateMeRequest.java`** trong package `com.ptit.htpt.userservice.web`:
    ```java
    package com.ptit.htpt.userservice.web;

    import jakarta.validation.constraints.Pattern;
    import jakarta.validation.constraints.Size;

    public record UpdateMeRequest(
        @Size(min = 1, max = 120, message = "Họ tên phải từ 1 đến 120 ký tự")
        String fullName,   // nullable — chỉ validate khi not-null

        @Pattern(regexp = "^\\+?[0-9\\s-]{7,20}$",
                 message = "Số điện thoại không hợp lệ")
        String phone       // nullable — chỉ validate khi not-null
    ) {}
    ```
    Lưu ý: jakarta `@Size`/`@Pattern` chỉ kick in khi field not-null → nullable optional fields tương thích (per CONTEXT Claude's Discretion + PATTERNS.md §2). KHÔNG dùng `@NotBlank`.

    **Tạo NEW file `UserProfileService.java`** trong package `com.ptit.htpt.userservice.service`:
    ```java
    package com.ptit.htpt.userservice.service;

    import com.ptit.htpt.userservice.domain.UserDto;
    import com.ptit.htpt.userservice.domain.UserEntity;
    import com.ptit.htpt.userservice.domain.UserMapper;
    import com.ptit.htpt.userservice.domain.UserRepository;
    import com.ptit.htpt.userservice.web.UpdateMeRequest;
    import org.springframework.http.HttpStatus;
    import org.springframework.stereotype.Service;
    import org.springframework.transaction.annotation.Transactional;
    import org.springframework.web.server.ResponseStatusException;

    @Service
    @Transactional
    public class UserProfileService {
        private final UserRepository userRepo;

        public UserProfileService(UserRepository userRepo) {
            this.userRepo = userRepo;
        }

        @Transactional(readOnly = true)
        public UserDto getMe(String userId) {
            UserEntity entity = userRepo.findById(userId)
                .orElseThrow(() -> new ResponseStatusException(HttpStatus.NOT_FOUND, "User not found"));
            return UserMapper.toDto(entity);
        }

        public UserDto updateMe(String userId, UpdateMeRequest req) {
            UserEntity entity = userRepo.findById(userId)
                .orElseThrow(() -> new ResponseStatusException(HttpStatus.NOT_FOUND, "User not found"));
            if (req.fullName() != null) entity.setFullName(req.fullName());
            if (req.phone() != null) entity.setPhone(req.phone());
            entity.touch();
            return UserMapper.toDto(userRepo.save(entity));
        }
    }
    ```
    Nếu package import `UserRepository` khác path thực tế trong codebase, dùng path đúng (xác minh bằng đọc UserPasswordService.java có sẵn).
  </action>
  <verify>
    <automated>cd sources/backend/user-service && mvn -q compile</automated>
  </verify>
  <acceptance_criteria>
    - File UpdateMeRequest.java tồn tại, grep `record UpdateMeRequest` → 1 match
    - File UpdateMeRequest.java grep cho substring `[0-9\s-]{7,20}` (canonical Java escape) - 1 match; KHONG duoc co `\-` (escape thua cho hyphen)
    - File UserProfileService.java grep `class UserProfileService` → 1 match
    - File UserProfileService.java grep `public UserDto getMe` và `public UserDto updateMe` → mỗi 1 match
    - File UserProfileService.java grep `if (req.fullName() != null)` và `if (req.phone() != null)` → mỗi 1 match
    - `mvn compile` exit 0
  </acceptance_criteria>
  <done>
    2 file mới compile thành công, signature `getMe(String)` + `updateMe(String, UpdateMeRequest)` exposed.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Wire GET /users/me + PATCH /users/me trong UserMeController</name>
  <read_first>
    - sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/web/UserMeController.java (file đang sửa — Phase 9 đã có changePassword + extractUserIdFromBearer)
    - 10-PATTERNS.md §1 "UserMeController.java" — endpoint pattern
    - sources/backend/api-gateway/src/main/resources/application.yml (xác nhận `user-service-me` route đã có — KHÔNG sửa, chỉ verify)
  </read_first>
  <files>
    sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/web/UserMeController.java
  </files>
  <action>
    Extend UserMeController in-place, KHÔNG rewrite:

    1. Inject `UserProfileService` qua constructor (thêm field + tham số):
       ```java
       private final UserPasswordService passwordService;
       private final UserProfileService profileService;
       private final JwtUtils jwtUtils;

       public UserMeController(UserPasswordService passwordService,
                               UserProfileService profileService,
                               JwtUtils jwtUtils) {
           this.passwordService = passwordService;
           this.profileService = profileService;
           this.jwtUtils = jwtUtils;
       }
       ```

    2. Thêm 2 method handlers TRƯỚC method `changePassword` (giữ existing intact):
       ```java
       @GetMapping
       public ApiResponse<UserDto> getMe(
               @RequestHeader(value = HttpHeaders.AUTHORIZATION, required = false) String authHeader
       ) {
           String userId = extractUserIdFromBearer(authHeader);
           return ApiResponse.of(200, "Profile loaded", profileService.getMe(userId));
       }

       @PatchMapping
       public ApiResponse<UserDto> updateMe(
               @RequestHeader(value = HttpHeaders.AUTHORIZATION, required = false) String authHeader,
               @Valid @RequestBody UpdateMeRequest body
       ) {
           String userId = extractUserIdFromBearer(authHeader);
           return ApiResponse.of(200, "Đã cập nhật", profileService.updateMe(userId, body));
       }
       ```

    3. Thêm imports cần thiết:
       ```java
       import com.ptit.htpt.userservice.domain.UserDto;
       import com.ptit.htpt.userservice.service.UserProfileService;
       import jakarta.validation.Valid;
       import org.springframework.web.bind.annotation.GetMapping;
       import org.springframework.web.bind.annotation.PatchMapping;
       ```
       (Bỏ qua import nào đã có sẵn trong file.)

    4. KHÔNG sửa `extractUserIdFromBearer` — tái dụng nguyên vẹn (D-05).
    5. KHÔNG sửa gateway application.yml — `user-service-me` route đã đứng trước `user-service-base` (verified Phase 9 per CONTEXT §integration_points). Chỉ verify bằng đọc file.
  </action>
  <verify>
    <automated>cd sources/backend/user-service && mvn -q compile -DskipTests && mvn -q test -Dtest='*UserMeController*' -DfailIfNoTests=false</automated>
  </verify>
  <acceptance_criteria>
    - File UserMeController.java grep `@GetMapping` → ≥ 1 match (method getMe)
    - File UserMeController.java grep `@PatchMapping` → 1 match (method updateMe)
    - File UserMeController.java grep `profileService.getMe` và `profileService.updateMe` → mỗi 1 match
    - File UserMeController.java grep `extractUserIdFromBearer\(authHeader\)` → ≥ 3 lần (1 cũ + 2 mới)
    - File UserMeController.java grep `@Valid @RequestBody UpdateMeRequest` → 1 match
    - `mvn compile` exit 0; nếu service đã có integration test cho /users/me → pass; nếu chưa có → skip OK (DfailIfNoTests=false)
    - File api-gateway/src/main/resources/application.yml grep `user-service-me` ĐỨNG TRƯỚC `user-service-base` (line number của me < line number của base) — verify bằng grep -n
  </acceptance_criteria>
  <done>
    UserMeController có 3 endpoints: GET /users/me, PATCH /users/me, POST /users/me/password. Project compile + existing tests pass. Gateway route order verified intact.
  </done>
</task>

</tasks>

<verification>
**Smoke check sau khi 3 task xong** (chạy bằng tay nếu docker stack up, không bắt buộc tự động hoá):

```bash
# 1. GET /users/me với valid token
curl -i -H "Authorization: Bearer $TOKEN" http://localhost:8080/api/users/me
# Expect: 200, body có "hasAvatar":false, "fullName", "phone"

# 2. PATCH /users/me hợp lệ
curl -i -X PATCH -H "Authorization: Bearer $TOKEN" -H "Content-Type: application/json" \
  -d '{"fullName":"Nguyễn Văn A","phone":"+84 901 234 567"}' \
  http://localhost:8080/api/users/me
# Expect: 200, body data.fullName="Nguyễn Văn A"

# 3. PATCH /users/me phone invalid
curl -i -X PATCH -H "Authorization: Bearer $TOKEN" -H "Content-Type: application/json" \
  -d '{"phone":"abc"}' http://localhost:8080/api/users/me
# Expect: 400, code=VALIDATION_ERROR, fieldErrors[].field="phone"

# 4. GET /users/me thiếu token
curl -i http://localhost:8080/api/users/me
# Expect: 401
```

Smoke verification này thuộc UAT pending (không gate plan completion) — sẽ được verify trong UAT phase end.
</verification>

<success_criteria>
- [ ] UserDto có 9 components bao gồm `hasAvatar:boolean`
- [ ] UpdateMeRequest record với jakarta validation cho fullName (Size 1-120) + phone (Pattern VN-loose)
- [ ] UserProfileService có getMe(userId) + updateMe(userId, req)
- [ ] UserMeController có @GetMapping + @PatchMapping ở root `/users/me`, mỗi handler gọi `extractUserIdFromBearer`
- [ ] `mvn compile` exit 0 trên user-service
- [ ] Gateway route `user-service-me` vẫn ĐỨNG TRƯỚC `user-service-base` (verify-only, KHÔNG sửa)
- [ ] ACCT-04 (avatar) KHÔNG có code thay đổi trong plan này — defer per D-08
</success_criteria>

<output>
Sau khi completion, tạo `.planning/phases/10-user-svc-schema-profile-editing/10-01-SUMMARY.md` theo template summary.md.
</output>
