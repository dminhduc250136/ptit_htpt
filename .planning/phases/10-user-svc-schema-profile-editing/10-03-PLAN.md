---
phase: 10-user-svc-schema-profile-editing
plan: 03
type: execute
wave: 2
depends_on:
  - 10-01  # cần backend GET/PATCH /api/users/me + UserDto.hasAvatar
  - 10-02  # cần react-hook-form, zod, @hookform/resolvers installed
files_modified:
  - sources/frontend/src/services/users.ts
  - sources/frontend/src/types/index.ts
  - sources/frontend/src/app/profile/settings/page.tsx
  - sources/frontend/src/app/profile/settings/page.module.css
autonomous: true
requirements:
  - ACCT-03
deferred_requirements:
  - ACCT-04  # avatar upload — defer per CONTEXT D-08; section là static placeholder "Coming soon"
tags:
  - frontend
  - profile
  - rhf-zod-foundation
  - ui

must_haves:
  truths:
    - "User logged-in vào /profile/settings thấy section 'Profile Info' với 2 input editable (fullName, phone) + 1 input read-only (email)"
    - "Submit form với fullName/phone hợp lệ → POST PATCH /api/users/me, toast 'Đã cập nhật' xuất hiện 3-3.5s"
    - "Sau save thành công, navbar (Header) hiển thị fullName mới ngay (không cần reload)"
    - "Phone không khớp regex `^\\+?[0-9\\s-]{7,20}$` → field-level error 'Số điện thoại không hợp lệ', form không submit"
    - "fullName trống (empty string) → field-level error, form không submit"
    - "Backend trả 400 fieldErrors → mapped vào setError per field qua react-hook-form"
    - "Section 'Avatar' hiển thị initials circle + chữ 'Coming soon' (KHÔNG có upload form — defer per D-08)"
    - "Section 'Security' (password form Phase 9) vẫn hoạt động intact"
  artifacts:
    - path: "sources/frontend/src/services/users.ts"
      provides: "getMe() + patchMe() service functions + UpdateMeBody type"
      contains: "export function getMe"
    - path: "sources/frontend/src/types/index.ts"
      provides: "User type extended với hasAvatar?: boolean"
      contains: "hasAvatar"
    - path: "sources/frontend/src/app/profile/settings/page.tsx"
      provides: "Profile Info section dùng rhf+zod + Avatar placeholder section + existing Security section"
      contains: "useForm"
  key_links:
    - from: "Profile Info form onSubmit"
      to: "patchMe()"
      via: "react-hook-form handleSubmit"
      pattern: "patchMe\\("
    - from: "patchMe success handler"
      to: "useAuth().login(updatedUser)"
      via: "AuthProvider context update + localStorage 'userProfile' write"
      pattern: "login\\(\\{\\s*\\.\\.\\.user"
    - from: "form errors"
      to: "Toast"
      via: "useToast().showToast"
      pattern: "showToast\\("
    - from: "useEffect mount"
      to: "getMe()"
      via: "fetch hydrate form defaults"
      pattern: "getMe\\(\\)\\.then"
---

<objective>
Ship Profile Info form (rhf+zod) tại `/profile/settings` (extend existing page Phase 9 — KHÔNG rewrite). Plan này gộp 2 layer: (a) HTTP services `getMe`/`patchMe`, (b) UI section + navbar refresh.

Lý do gộp: services layer chỉ ~10% context (1 file, ~15 lines), không đứng độc lập. Gộp với UI giữ context efficient.

Purpose: Hoàn tất ACCT-03 visible-end-user. Set rhf+zod pattern cho v1.2 (CONTEXT §code_context — lần đầu trong codebase).
Output: 4 file modified. Form hoạt động end-to-end qua gateway → backend Plan 10-01.

**Refinement của CONTEXT D-07:** D-07 nói "manual localStorage + router.refresh()". Pattern mapper phát hiện codebase ĐÃ có `AuthProvider` với `useAuth().login(user)` viết sẵn vào localStorage 'userProfile' + cross-tab storage event listener. Header.tsx auto re-render qua context. Plan dùng `useAuth().login(updatedUser)` thay manual localStorage — KHÔNG vi phạm spirit D-07 (vẫn không tạo AuthContext mới — đã có sẵn). Đơn giản hơn, không cần soft reload.

ACCT-04 (avatar upload) DEFERRED per D-08 — Avatar section là static placeholder, KHÔNG có file input / upload logic.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-user-svc-schema-profile-editing/10-CONTEXT.md
@.planning/phases/10-user-svc-schema-profile-editing/10-PATTERNS.md
@.planning/phases/10-user-svc-schema-profile-editing/10-01-PLAN.md
@.planning/codebase/CONVENTIONS.md
@sources/frontend/src/services/users.ts
@sources/frontend/src/services/http.ts
@sources/frontend/src/services/errors.ts
@sources/frontend/src/types/index.ts
@sources/frontend/src/app/profile/settings/page.tsx
@sources/frontend/src/app/profile/settings/page.module.css
@sources/frontend/src/providers/AuthProvider.tsx
@sources/frontend/src/components/ui/Toast/Toast.tsx
@sources/frontend/src/components/layout/Header/Header.tsx

<interfaces>
<!-- Contracts để executor không phải scavenger hunt. Trích từ codebase + Plan 10-01. -->

Backend wire format (sau Plan 10-01):
- `GET /api/users/me` → 200 ApiResponse<UserDto> (data unwrapped tự động bởi http.ts)
- `PATCH /api/users/me` body `{fullName?: string, phone?: string}` → 200 ApiResponse<UserDto>
- UserDto shape: `{id, username, email, roles, fullName, phone, hasAvatar:boolean, createdAt, updatedAt}`
- 400 VALIDATION_ERROR có `fieldErrors: [{field, message}]`

Frontend HTTP wrapper (services/http.ts):
```typescript
export function httpGet<T>(path: string): Promise<T>;
export function httpPatch<T>(path: string, body: unknown): Promise<T>;
// auto-attach Bearer, auto-unwrap ApiResponse.data, throw ApiError với code/status/message/fieldErrors
```

Existing service pattern (services/users.ts ~lines 38-51):
```typescript
export interface ChangePasswordBody { oldPassword: string; newPassword: string; }
export function changeMyPassword(body: ChangePasswordBody): Promise<{ changed: true }> {
  return httpPost<{ changed: true }>('/api/users/me/password', body);
}
```

AuthProvider (providers/AuthProvider.tsx):
```typescript
const { user, login } = useAuth();
// login(updatedUser) → updates context + writes localStorage 'userProfile' + propagates cross-tab
```

ToastProvider (components/ui/Toast/Toast.tsx):
```typescript
const { showToast } = useToast();
showToast('Đã cập nhật', 'success');  // auto-dismiss ~3.5s
```

ApiError check (services/errors.ts):
```typescript
export function isApiError(err: unknown): err is ApiError;
// ApiError có: code, status, message, fieldErrors?: Array<{field: string, message: string}>
```

Existing page structure (app/profile/settings/page.tsx — Phase 9):
- 'use client' directive
- useState for password form fields
- handleSubmit POST /api/users/me/password
- styles import từ ./page.module.css (.section, .sectionTitle, .form, .field, .label, .input, .fieldError, .submit)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Thêm getMe/patchMe service + extend User type với hasAvatar</name>
  <read_first>
    - sources/frontend/src/services/users.ts (file đang sửa — pattern analog `changeMyPassword` lines 38-51)
    - sources/frontend/src/services/http.ts (httpGet, httpPatch signatures)
    - sources/frontend/src/types/index.ts (User type — file đang sửa)
    - 10-PATTERNS.md §5 "frontend/src/services/users.ts"
  </read_first>
  <files>
    sources/frontend/src/services/users.ts
    sources/frontend/src/types/index.ts
  </files>
  <action>
    **A. Extend `User` type trong `src/types/index.ts`:**
    Thêm `hasAvatar?: boolean` vào interface/type `User` (optional để tương thích các caller hiện tại). Ví dụ:
    ```typescript
    export interface User {
      id: string;
      username: string;
      email: string;
      roles: string;
      fullName?: string | null;
      phone?: string | null;
      hasAvatar?: boolean;  // Phase 10 — luôn false ở Phase 10, true khi avatar wire-up Phase 12+
      createdAt?: string;
      updatedAt?: string;
    }
    ```
    Giữ field shape khớp với backend UserDto (Plan 10-01). KHÔNG đổi type của field hiện có nếu codebase đã định nghĩa khác — chỉ thêm `hasAvatar?: boolean`.

    **B. Thêm getMe/patchMe vào `src/services/users.ts`:**
    Thêm vào CUỐI file (sau changeMyPassword), copy comment block style của analog:
    ```typescript
    // ============================================================
    // Phase 10 / ACCT-03. Self-service profile read & update.
    // Endpoints backend: GET /api/users/me, PATCH /api/users/me.
    // ============================================================

    export interface UpdateMeBody {
      fullName?: string;
      phone?: string;
    }

    export function getMe(): Promise<User> {
      return httpGet<User>('/api/users/me');
    }

    export function patchMe(body: UpdateMeBody): Promise<User> {
      return httpPatch<User>('/api/users/me', body);
    }
    ```

    Đảm bảo `httpGet`, `httpPatch`, `User` đã có trong import statements ở đầu file. Nếu thiếu → thêm vào import.
  </action>
  <verify>
    <automated>cd sources/frontend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - File `src/types/index.ts` grep `hasAvatar` → ≥ 1 match (trong type User)
    - File `src/services/users.ts` grep `export function getMe` → 1 match
    - File `src/services/users.ts` grep `export function patchMe` → 1 match
    - File `src/services/users.ts` grep `export interface UpdateMeBody` → 1 match
    - File `src/services/users.ts` grep `httpGet<User>\('/api/users/me'\)` → 1 match
    - File `src/services/users.ts` grep `httpPatch<User>\('/api/users/me'` → 1 match
    - `npx tsc --noEmit` exit 0 (no TS errors)
  </acceptance_criteria>
  <done>
    Type User có hasAvatar?, services exports getMe/patchMe/UpdateMeBody, TypeScript compile clean.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Build Profile Info section (rhf+zod) + Avatar placeholder + navbar sync</name>
  <read_first>
    - sources/frontend/src/app/profile/settings/page.tsx (file đang sửa — Phase 9 password section, KHÔNG rewrite)
    - sources/frontend/src/app/profile/settings/page.module.css (file đang sửa)
    - sources/frontend/src/providers/AuthProvider.tsx (useAuth, login signature)
    - sources/frontend/src/components/ui/Toast/Toast.tsx (useToast, showToast signature)
    - sources/frontend/src/services/errors.ts (isApiError, ApiError fields)
    - 10-PATTERNS.md §6 (page.tsx) + §7 (page.module.css) + §"rhf + zod — Pattern Mới"
  </read_first>
  <files>
    sources/frontend/src/app/profile/settings/page.tsx
    sources/frontend/src/app/profile/settings/page.module.css
  </files>
  <action>
    **Strategy (per D-01):** Layout dọc, single-page scroll, KHÔNG tab. 3 sections theo thứ tự render:
    1. **Profile Info** (NEW — Phase 10) — fullName + phone editable, email read-only
    2. **Avatar** (NEW — placeholder) — initials circle + "Coming soon" (no upload)
    3. **Security** (EXISTING — Phase 9 password form, intact)

    **A. page.tsx — thêm imports:**
    ```typescript
    import { useEffect } from 'react';
    import { useForm } from 'react-hook-form';
    import { zodResolver } from '@hookform/resolvers/zod';
    import { z } from 'zod';
    import { getMe, patchMe } from '@/services/users';
    import { useToast } from '@/components/ui/Toast/Toast';
    import { useAuth } from '@/providers/AuthProvider';
    ```
    (Adjust path nếu actual path khác — verify bằng read trước khi viết.)

    **LUU Y BAO TON IMPORT:** File page.tsx hien tai (Phase 9, line 6) da co `import { isApiError } from '@/services/errors';` -- KHONG duoc xoa hay rename import nay; usage trong catch block cua onSubmitProfile (Step C) tai dung NGUYEN VEN identifier `isApiError`.

    **B. Định nghĩa zod schema + types ở module-level (ngoài component):**
    ```typescript
    const profileSchema = z.object({
      fullName: z.string()
        .trim()
        .min(1, 'Vui lòng nhập họ tên')
        .max(120, 'Họ tên quá dài (tối đa 120 ký tự)'),
      phone: z.string()
        .regex(/^\+?[0-9\s-]{7,20}$/, 'Số điện thoại không hợp lệ')
        .or(z.literal('')),  // empty string OK (nullable optional)
    });
    type ProfileFormData = z.infer<typeof profileSchema>;
    ```

    **C. Trong component, TRƯỚC password form state existing, thêm rhf hooks:**
    ```typescript
    const { user, login } = useAuth();
    const { showToast } = useToast();

    const {
      register, handleSubmit, formState: { errors, isSubmitting, isDirty },
      reset, setError,
    } = useForm<ProfileFormData>({
      resolver: zodResolver(profileSchema),
      defaultValues: { fullName: '', phone: '' },
    });

    const [profileEmail, setProfileEmail] = useState<string>('');

    useEffect(() => {
      let alive = true;
      getMe()
        .then(me => {
          if (!alive) return;
          reset({ fullName: me.fullName ?? '', phone: me.phone ?? '' });
          setProfileEmail(me.email ?? '');
        })
        .catch(() => { if (alive) showToast('Không tải được thông tin', 'error'); });
      return () => { alive = false; };
    }, [reset, showToast]);

    const onSubmitProfile = handleSubmit(async (data) => {
      try {
        const updated = await patchMe({ fullName: data.fullName, phone: data.phone || undefined });
        if (user) login({ ...user, name: updated.fullName ?? user.name });
        showToast('Đã cập nhật', 'success');
        reset({ fullName: updated.fullName ?? '', phone: updated.phone ?? '' });
      } catch (err) {
        if (isApiError(err) && err.fieldErrors?.length) {
          err.fieldErrors.forEach(fe => {
            if (fe.field === 'fullName' || fe.field === 'phone') {
              setError(fe.field, { message: fe.message });
            }
          });
        } else if (isApiError(err)) {
          showToast(err.message ?? 'Có lỗi xảy ra', 'error');
        } else {
          showToast('Có lỗi xảy ra, vui lòng thử lại', 'error');
        }
      }
    });
    ```
    Lưu ý field `name` của AuthProvider User type: nếu codebase dùng `fullName` thay vì `name`, adjust `login({ ...user, fullName: updated.fullName ?? user.fullName })`. Verify bằng read AuthProvider.tsx trước khi viết.

    **D. JSX render order (thay phần JSX hiện tại — giữ password section intact ở cuối):**
    ```tsx
    <div className={styles.container}>
      <h1>Cài đặt tài khoản</h1>

      {/* Section 1: Profile Info — NEW Phase 10 */}
      <section className={styles.section}>
        <h2 className={styles.sectionTitle}>Thông tin cá nhân</h2>
        <form onSubmit={onSubmitProfile} className={styles.form} noValidate>
          <div className={styles.field}>
            <label htmlFor="fullName" className={styles.label}>Họ và tên</label>
            <input id="fullName" className={styles.input} {...register('fullName')} />
            {errors.fullName && <p className={styles.fieldError}>{errors.fullName.message}</p>}
          </div>
          <div className={styles.field}>
            <label htmlFor="phone" className={styles.label}>Số điện thoại</label>
            <input id="phone" className={styles.input} {...register('phone')} />
            {errors.phone && <p className={styles.fieldError}>{errors.phone.message}</p>}
          </div>
          <div className={styles.field}>
            <label htmlFor="email" className={styles.label}>Email</label>
            <input id="email" className={`${styles.input} ${styles.readonly}`}
                   value={profileEmail} disabled readOnly />
          </div>
          <button type="submit" className={styles.submit} disabled={isSubmitting || !isDirty}>
            {isSubmitting ? 'Đang lưu...' : 'Lưu thay đổi'}
          </button>
        </form>
      </section>

      {/* Section 2: Avatar — placeholder, ACCT-04 defer per D-08 */}
      <section className={styles.section}>
        <h2 className={styles.sectionTitle}>Ảnh đại diện</h2>
        <div className={styles.avatarPlaceholder} aria-label="Ảnh đại diện (chữ cái đầu)">
          {(profileEmail || 'U').charAt(0).toUpperCase()}
        </div>
        <p className={styles.comingSoon}>Tính năng tải ảnh đại diện sẽ có trong bản cập nhật sau.</p>
      </section>

      {/* Section 3: Security — EXISTING Phase 9 password form (giữ nguyên JSX cũ) */}
      <section className={styles.section}>
        <h2 className={styles.sectionTitle}>Bảo mật</h2>
        {/* ... existing password form JSX ... */}
      </section>
    </div>
    ```
    Initials lấy từ `profileEmail.charAt(0)` đơn giản. Có thể nâng cấp dùng `fullName` watch từ rhf nếu muốn — Claude's discretion.

    **E. page.module.css — thêm classes mới:**
    ```css
    .avatarPlaceholder {
      width: 80px; height: 80px; border-radius: 50%;
      background: var(--primary, #2563eb); color: white;
      display: flex; align-items: center; justify-content: center;
      font-size: 1.75rem; font-weight: 600;
      margin-bottom: var(--space-2, 0.5rem);
    }
    .comingSoon {
      font-size: 0.875rem;
      color: var(--on-surface-variant, #6b7280);
      margin-top: var(--space-2, 0.5rem);
    }
    .readonly {
      background: var(--surface-variant, #f3f4f6);
      cursor: not-allowed;
    }
    ```
    Tái dụng `.section`, `.sectionTitle`, `.form`, `.field`, `.label`, `.input`, `.fieldError`, `.submit` đã có (Phase 9).

    **F. Giữ nguyên password form section + handler hiện tại — KHÔNG sửa logic.**
  </action>
  <verify>
    <automated>cd sources/frontend && npx tsc --noEmit && npm run lint && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - File page.tsx grep `useForm<ProfileFormData>` → 1 match
    - File page.tsx grep `zodResolver(profileSchema)` → 1 match
    - File page.tsx grep `patchMe\(\{` → 1 match
    - File page.tsx grep `data.phone || undefined` -> 1 match (locks phone-clear contract: empty string -> undefined -> server skip)
    - File page.tsx grep -c `isApiError` -> >= 2 (1 import line 6 + >= 1 usage trong onSubmitProfile catch); import line 6 KHONG duoc xoa/rename
    - File page.tsx phone regex (zod) literal `/^\+?[0-9\s-]{7,20}$/` -> 1 match (canonical, dong bo voi backend Plan 10-01)
    - File page.tsx grep `login\(\{ \.\.\.user` → 1 match (navbar refresh path)
    - File page.tsx grep `getMe\(\)` → 1 match
    - File page.tsx grep `showToast\('Đã cập nhật'` → 1 match
    - File page.tsx grep `Coming soon|cập nhật sau|sẽ có` → 1 match (avatar placeholder copy)
    - File page.tsx KHÔNG có `<input type="file"` (avatar defer per D-08) → grep returns 0
    - File page.tsx KHÔNG có `router.refresh()` (refinement của D-07 dùng useAuth().login() thay)
    - File page.tsx grep existing password form symbols (e.g., `oldPassword`, `newPassword`, `changeMyPassword`) → ≥ 1 match mỗi (Phase 9 intact)
    - File page.module.css grep `\.avatarPlaceholder`, `\.comingSoon`, `\.readonly` → mỗi 1 match
    - `npx tsc --noEmit` exit 0
    - `npm run lint` exit 0
    - `npm run build` exit 0
  </acceptance_criteria>
  <done>
    /profile/settings render 3 sections theo đúng thứ tự, Profile Info form dùng rhf+zod, save success update navbar qua useAuth().login(), toast hiển thị, password section intact, avatar là static placeholder, build production pass.
  </done>
</task>

</tasks>

<verification>
**Manual smoke (UAT — defer end of phase):**

```
1. docker compose up (full stack)
2. Login với user demo
3. Visit /profile/settings → thấy 3 sections (Profile Info / Avatar / Security)
4. Profile Info form: edit fullName "Nguyễn Văn Test" → click "Lưu thay đổi"
   → toast "Đã cập nhật" xuất hiện ~3s
   → navbar (top-right user name) hiển thị "Nguyễn Văn Test" ngay
5. Refresh page → fullName mới persist (load từ getMe)
6. Edit phone "abc" → submit
   → field-level error "Số điện thoại không hợp lệ"
   → form không submit (button disabled hoặc client validation block)
7. Edit fullName trống → field-level error
8. Avatar section: thấy initials circle (chữ cái đầu email) + "Coming soon" copy
9. Security section: thử đổi password (Phase 9) → vẫn hoạt động
```

UAT items này log vào VERIFICATION.md / UAT.md tại checker step — KHÔNG gate plan completion.
</verification>

<success_criteria>
- [ ] services/users.ts có getMe/patchMe/UpdateMeBody exports
- [ ] User type có hasAvatar?: boolean
- [ ] /profile/settings render 3 sections theo D-01 order (Profile / Avatar / Security)
- [ ] Profile Info form dùng rhf + zod (lần đầu codebase) — schema có fullName 1-120 trim + phone regex `^\+?[0-9\s-]{7,20}$` or empty
- [ ] Submit success → patchMe + useAuth().login(updatedUser) + showToast('Đã cập nhật')
- [ ] Submit fail (400 fieldErrors) → setError per field
- [ ] Avatar section là STATIC placeholder (KHÔNG có file input — ACCT-04 defer per D-08)
- [ ] Security password section (Phase 9) intact
- [ ] tsc + lint + build pass
- [ ] Refinement D-07 documented trong code comment (1 dòng): dùng useAuth().login() thay manual localStorage
</success_criteria>

<output>
Sau khi completion, tạo `.planning/phases/10-user-svc-schema-profile-editing/10-03-SUMMARY.md`.
</output>
