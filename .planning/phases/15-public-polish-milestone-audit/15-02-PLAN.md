---
phase: 15
plan: 02
type: execute
wave: 1
depends_on: [15-00]
files_modified:
  - sources/frontend/src/app/products/[slug]/page.tsx
  - sources/frontend/src/app/products/[slug]/page.module.css
autonomous: true
requirements: [PUB-03, PUB-04]
tags: [phase-15, wave-1, pdp, breadcrumb, stock-badge, thumbnail, add-to-cart]

must_haves:
  truths:
    - "PDP breadcrumb format 'Trang chủ / {Brand} / {Name}' với brand link /products?brand={brand}"
    - "PDP breadcrumb fallback 'Trang chủ / Sản phẩm / {Name}' khi product.brand == null"
    - "PDP stock badge 3-tier: success (>=10), warning (1-9 hiện count), danger (=0)"
    - "Add-to-cart + quantity selector HIDE hoàn toàn (return null) khi stock=0, thay bằng inline message"
    - "Xem giỏ hàng' secondary button vẫn render khi stock=0"
    - "Thumbnail strip có aria-label='Xem ảnh {i+1}' + aria-current khi active + border 2px var(--primary)"
  artifacts:
    - path: "sources/frontend/src/app/products/[slug]/page.tsx"
      provides: "PDP refactored với breadcrumb brand + 3-tier stock badge + conditional add-to-cart + thumbnail a11y"
      contains: "variant=\"success\""
    - path: "sources/frontend/src/app/products/[slug]/page.module.css"
      provides: ".outOfStockMessage class + thumbnail active border 2px var(--primary)"
      contains: "outOfStockMessage"
  key_links:
    - from: "[slug]/page.tsx breadcrumb"
      to: "Phase 14 SEARCH-01 /products?brand= contract"
      via: "<Link href={`/products?brand=${encodeURIComponent(product.brand)}`}>"
      pattern: "products\\?brand=\\$\\{encodeURIComponent\\(product\\.brand\\)\\}"
    - from: "[slug]/page.tsx stock badge"
      to: "components/ui/Badge/Badge.tsx (Wave 0 — variants success/warning/danger)"
      via: "<Badge variant='success|warning|danger'>"
      pattern: "Badge variant=\"(success|warning|danger)\""
    - from: "[slug]/page.tsx add-to-cart"
      to: "stock-aware conditional render"
      via: "{(product.stock ?? 0) > 0 ? <actions/> : <outOfStockMessage/>}"
      pattern: "product\\.stock.*\\?\\?\\s*0\\)\\s*[>=]"
---

<objective>
Refactor PDP `/products/[slug]` để align ROADMAP Phase 15 SC-2 + SC-3:
1. Breadcrumb: `Home > Sản phẩm > {Cat} > {Name}` → `Home > {Brand} > {Name}` + fallback (D-14)
2. Thumbnail polish: aria-label + aria-current + active border 2px primary (D-11)
3. Stock badge 2-tier → 3-tier với Badge component variants từ Wave 0 (D-15)
4. Add-to-cart hide hoàn toàn khi stock=0 + inline message thay thế (D-16)

Purpose: PDP visible polish quality, breadcrumb continuity với Phase 14 brand filter, stock UX rõ ràng theo industry pattern 3-tier.

Output: Modified `[slug]/page.tsx` + `[slug]/page.module.css`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/15-public-polish-milestone-audit/15-CONTEXT.md
@.planning/phases/15-public-polish-milestone-audit/15-RESEARCH.md
@.planning/phases/15-public-polish-milestone-audit/15-PATTERNS.md
@.planning/phases/15-public-polish-milestone-audit/15-UI-SPEC.md
@.planning/phases/15-public-polish-milestone-audit/15-00-SUMMARY.md
@sources/frontend/src/app/products/[slug]/page.tsx
@sources/frontend/src/app/products/[slug]/page.module.css
@sources/frontend/src/components/ui/Badge/Badge.tsx

<interfaces>
<!-- Product type (verified types/index.ts:113-135) -->
```typescript
interface Product {
  id, name, slug, brand?, category, stock, images: string[], specifications: Specification[], ...
}
```

<!-- Badge usage (verified pattern lines 138-156 [slug]/page.tsx) -->
```tsx
<Badge variant="sale|new|hot|default|out-of-stock|success|warning|danger">{label}</Badge>
```

<!-- Existing breadcrumb CSS classes (REUSE — không thay) -->
.breadcrumb, .breadcrumbLink, .breadcrumbSep, .breadcrumbCurrent

<!-- Existing thumbnail CSS classes -->
.thumbnail, .thumbnailActive (line 124-126 — hiện border-color: var(--primary-container))
.thumbnailStrip
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Refactor breadcrumb (brand-based) + thumbnail a11y polish</name>
  <files>sources/frontend/src/app/products/[slug]/page.tsx, sources/frontend/src/app/products/[slug]/page.module.css</files>

  <read_first>
    - sources/frontend/src/app/products/[slug]/page.tsx (lines 105-121 breadcrumb hiện tại; lines 159-171 thumbnail strip)
    - sources/frontend/src/app/products/[slug]/page.module.css (lines 124-126 .thumbnailActive)
    - .planning/phases/15-public-polish-milestone-audit/15-PATTERNS.md (lines 205-245 breadcrumb refactor concrete patch + lines 305-316 thumbnail a11y)
    - .planning/phases/15-public-polish-milestone-audit/15-UI-SPEC.md (lines 159-176 breadcrumb + thumbnail copy contract)
  </read_first>

  <action>
**STEP A: `[slug]/page.tsx` — Breadcrumb refactor (D-14)**

Tìm block `<div className={styles.breadcrumb}>` (khoảng lines 105-121). Hiện tại:
```tsx
<div className={styles.breadcrumb}>
  <div className={styles.container}>
    <Link href="/" className={styles.breadcrumbLink}>Trang chủ</Link>
    <span className={styles.breadcrumbSep}>/</span>
    <Link href="/products" className={styles.breadcrumbLink}>Sản phẩm</Link>
    {product.category && (
      <>
        <span className={styles.breadcrumbSep}>/</span>
        <Link href={`/products?category=${product.category.slug}`} className={styles.breadcrumbLink}>
          {product.category.name}
        </Link>
      </>
    )}
    <span className={styles.breadcrumbSep}>/</span>
    <span className={styles.breadcrumbCurrent}>{product.name}</span>
  </div>
</div>
```

Thay toàn bộ inner block bằng (D-14 brand-based + fallback):
```tsx
<div className={styles.breadcrumb}>
  <div className={styles.container}>
    <Link href="/" className={styles.breadcrumbLink}>Trang chủ</Link>
    <span className={styles.breadcrumbSep}>/</span>
    {product.brand ? (
      <Link
        href={`/products?brand=${encodeURIComponent(product.brand)}`}
        className={styles.breadcrumbLink}
      >
        {product.brand}
      </Link>
    ) : (
      <Link href="/products" className={styles.breadcrumbLink}>Sản phẩm</Link>
    )}
    <span className={styles.breadcrumbSep}>/</span>
    <span className={styles.breadcrumbCurrent}>{product.name}</span>
  </div>
</div>
```

Reuse existing CSS classes (`.breadcrumb*`) — KHÔNG cần thêm CSS class mới cho breadcrumb.

**STEP B: `[slug]/page.tsx` — Thumbnail strip a11y polish (D-11)**

Tìm block thumbnail render (khoảng lines 159-171). Hiện tại:
```tsx
{product.images.map((img, i) => (
  <button
    key={i}
    className={`${styles.thumbnail} ${i === selectedImage ? styles.thumbnailActive : ''}`}
    onClick={() => setSelectedImage(i)}
  >
    <Image src={img} alt={`${product.name} - ${i + 1}`} fill sizes="80px" className={styles.thumbImg} />
  </button>
))}
```

Thay bằng (add `type="button"` + `aria-label` + `aria-current`):
```tsx
{product.images.map((img, i) => (
  <button
    key={i}
    type="button"
    aria-label={`Xem ảnh ${i + 1}`}
    aria-current={i === selectedImage ? 'true' : undefined}
    className={`${styles.thumbnail} ${i === selectedImage ? styles.thumbnailActive : ''}`}
    onClick={() => setSelectedImage(i)}
  >
    <Image src={img} alt={`${product.name} - ${i + 1}`} fill sizes="80px" className={styles.thumbImg} />
  </button>
))}
```

**Optional keyboard arrow nav (D-11 Claude's discretion):** SKIP nếu > 30 LOC. Thay vì implement keyboard handler complex, có thể accept browser tab + Enter/Space (default `<button>` behavior — đã đủ). Nếu executor muốn implement, dùng `onKeyDown` trên parent `<div>` thumbnail strip với arrow Left/Right cycle `selectedImage`. KHÔNG block phase nếu skip.

**STEP C: `[slug]/page.module.css` — Thumbnail active border emphasis (D-11)**

Tìm `.thumbnailActive` block (khoảng lines 124-126):
```css
.thumbnailActive {
  border-color: var(--primary-container) !important;
}
```

Thay bằng (border-width 2px + color đậm hơn):
```css
.thumbnailActive {
  border-color: var(--primary) !important;
  border-width: 2px;
}
```

(Lý do: `--primary-container` quá subtle, `--primary` rõ rệt hơn cho visual cue active state — match UI-SPEC color contract line 96.)
  </action>

  <acceptance_criteria>
    - `grep "products?brand=\${encodeURIComponent(product.brand)}" sources/frontend/src/app/products/[slug]/page.tsx` → matches (brand link với encode)
    - `grep "product.brand ?" sources/frontend/src/app/products/[slug]/page.tsx` → matches (conditional brand check)
    - `grep -c "product.category.slug" sources/frontend/src/app/products/[slug]/page.tsx` → output `0` (KHÔNG còn category breadcrumb segment)
    - `grep 'aria-label={`Xem ảnh' sources/frontend/src/app/products/[slug]/page.tsx` → matches (thumbnail a11y)
    - `grep "aria-current={i === selectedImage" sources/frontend/src/app/products/[slug]/page.tsx` → matches
    - `grep "border-color: var(--primary)" sources/frontend/src/app/products/[slug]/page.module.css` → matches (thumbnail active emphasis)
    - `cd sources/frontend && npm run build` → exit 0
  </acceptance_criteria>

  <verify>
    <automated>cd sources/frontend && npm run build 2>&1 | tail -15</automated>
  </verify>

  <done>Breadcrumb refactor sang brand-based với fallback, thumbnail a11y attrs added, active border emphasis tăng từ container sang primary, build PASS.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Stock badge 3-tier + hide add-to-cart conditional</name>
  <files>sources/frontend/src/app/products/[slug]/page.tsx, sources/frontend/src/app/products/[slug]/page.module.css</files>

  <read_first>
    - sources/frontend/src/app/products/[slug]/page.tsx (lines 209-216 stock display hiện tại; lines 218-263 actions/add-to-cart block; line 265-267 "Xem giỏ hàng" button — phải giữ)
    - sources/frontend/src/app/products/[slug]/page.module.css (lines 217-229 .inStock + .outOfStock — sẽ replace)
    - sources/frontend/src/components/ui/Badge/Badge.tsx (variants `success | warning | danger` từ Wave 0)
    - .planning/phases/15-public-polish-milestone-audit/15-PATTERNS.md (lines 263-291 stock badge + add-to-cart concrete patch)
    - .planning/phases/15-public-polish-milestone-audit/15-UI-SPEC.md (lines 178-187 stock badge copywriting + lines 270-280 stock interaction)
  </read_first>

  <action>
**STEP A: `[slug]/page.tsx` — Stock badge 3-tier (D-15)**

Tìm block hiển thị stock hiện tại (khoảng lines 209-216) — đang là 2-tier `.inStock` / `.outOfStock` inline span. Replace toàn bộ block bằng:

```tsx
<div className={styles.stockInfo}>
  {(product.stock ?? 0) >= 10 && (
    <Badge variant="success">✓ Còn hàng</Badge>
  )}
  {(product.stock ?? 0) > 0 && (product.stock ?? 0) < 10 && (
    <Badge variant="warning">⚠ Sắp hết hàng (còn {product.stock})</Badge>
  )}
  {(product.stock ?? 0) === 0 && (
    <Badge variant="danger">✗ Hết hàng</Badge>
  )}
</div>
```

Notes:
- Dùng `?? 0` defensive nullish coalescing (Product.stock có thể null/undefined trong edge case).
- 3 conditionals mutually exclusive (chỉ 1 render mỗi lúc).
- Icon prefix `✓ ⚠ ✗` đảm bảo color KHÔNG là kênh thông tin duy nhất (WCAG 1.4.1).
- Reuse `<Badge>` component đã extend Wave 0 — KHÔNG inline span.

**STEP B: `[slug]/page.tsx` — Hide add-to-cart hoàn toàn khi stock=0 (D-16)**

Tìm block `<div className={styles.actions}>` (khoảng lines 218-263) — chứa quantity selector + "Thêm vào giỏ hàng" Button. WRAP toàn bộ block trong stock-aware conditional:

```tsx
{(product.stock ?? 0) > 0 ? (
  <div className={styles.actions}>
    <div className={styles.quantitySelector}>
      <button
        type="button"
        className={styles.qtyBtn}
        onClick={() => setQuantity((q) => Math.max(1, q - 1))}
        disabled={quantity <= 1}
      >
        −
      </button>
      <span className={styles.qtyValue}>{quantity}</span>
      <button
        type="button"
        className={styles.qtyBtn}
        onClick={() => setQuantity((q) => Math.min(product.stock ?? 1, q + 1))}
        disabled={quantity >= (product.stock ?? 1)}
      >
        +
      </button>
    </div>
    <Button
      size="lg"
      fullWidth
      loading={addingToCart}
      onClick={async () => {
        // giữ nguyên handler addToCart hiện tại — copy từ block cũ
      }}
    >
      Thêm vào giỏ hàng
    </Button>
  </div>
) : (
  <p className={styles.outOfStockMessage}>
    Sản phẩm tạm hết — vui lòng quay lại sau.
  </p>
)}
```

**Lưu ý quan trọng:**
- Preserve handler logic `addToCart` từ block cũ — KHÔNG viết lại logic, copy nguyên signature/body.
- BỎ `disabled={product.stock === 0}` + label `'Hết hàng'` ternary trên Button cũ — branch stock=0 KHÔNG bao giờ render trong nhánh `> 0` true.
- "♡ Xem giỏ hàng" secondary button (line 265-267 hiện tại — NẰM NGOÀI `.actions`) PHẢI giữ unconditionally — render bất kể stock value (D-16: user vẫn vào giỏ xem item khác).

**STEP C: `[slug]/page.module.css` — Add `.outOfStockMessage` + cleanup**

Append vào cuối file (hoặc đặt gần `.actions` cho organization):

```css
/* === Out-of-stock inline message — Phase 15 PUB-04 (D-16) === */
.outOfStockMessage {
  font-size: var(--text-body-md);
  font-weight: var(--weight-medium);
  color: var(--on-surface-variant);
  padding: var(--space-3) var(--space-4);
  background: var(--surface-container-low);
  border-radius: var(--radius-lg);
  margin: var(--space-3) 0;
}
```

**Optional cleanup:** Xóa `.inStock` + `.outOfStock` classes (lines 217-229) nếu KHÔNG còn reference nào — Badge component thay thế. Verify bằng `grep "inStock\|outOfStock" sources/frontend/src/app/products/[slug]/page.tsx` — output `0` lines (chỉ `outOfStockMessage` nếu có).
  </action>

  <acceptance_criteria>
    - `grep 'variant="success"' sources/frontend/src/app/products/[slug]/page.tsx` → matches
    - `grep 'variant="warning"' sources/frontend/src/app/products/[slug]/page.tsx` → matches
    - `grep 'variant="danger"' sources/frontend/src/app/products/[slug]/page.tsx` → matches
    - `grep "✓ Còn hàng\|⚠ Sắp hết hàng\|✗ Hết hàng" sources/frontend/src/app/products/[slug]/page.tsx` → matches all 3
    - `grep "Sản phẩm tạm hết" sources/frontend/src/app/products/[slug]/page.tsx` → matches (inline message)
    - `grep "(product.stock ?? 0) > 0 ?" sources/frontend/src/app/products/[slug]/page.tsx` → matches (stock-aware conditional)
    - `grep "outOfStockMessage" sources/frontend/src/app/products/[slug]/page.module.css` → matches
    - `grep "var(--surface-container-low)" sources/frontend/src/app/products/[slug]/page.module.css` → matches (token reuse)
    - `cd sources/frontend && npm run build` → exit 0
  </acceptance_criteria>

  <verify>
    <automated>cd sources/frontend && npm run build 2>&1 | tail -15</automated>
  </verify>

  <done>Stock badge 3-tier render đúng theo stock value, add-to-cart + quantity hide hoàn toàn khi stock=0, inline message replace, "Xem giỏ hàng" vẫn render bất kể stock, build PASS.</done>
</task>

</tasks>

<verification>
- `cd sources/frontend && npm run build` exit 0
- Manual visual (M3, M4, M5 trong 15-MANUAL-CHECKLIST.md): thumbnail swap + stock badge 3-tier color + breadcrumb brand link
- Smoke E2E (Wave 3 SMOKE-3): mở PDP DELIVERED product (nếu có)
</verification>

<success_criteria>
- PDP align ROADMAP SC-2 (breadcrumb format) + SC-3 (3-tier badge + hide cart)
- Continuity navigation breadcrumb → Phase 14 FilterSidebar pre-check brand
- WCAG AA: badge có icon + text (color không là kênh duy nhất), thumbnail có aria-label + aria-current
</success_criteria>

<output>
Tạo `.planning/phases/15-public-polish-milestone-audit/15-02-SUMMARY.md` ghi:
- Lines changed (breadcrumb, thumbnail, stock, actions blocks)
- Confirm `.inStock`/`.outOfStock` cleanup status (deleted vs kept dead code)
- Keyboard arrow nav cho thumbnail (D-11): implemented hay skipped + LOC justification
- Verify "Xem giỏ hàng" button vẫn render khi stock=0 (visual confirm)
</output>
