---
phase: 15
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - sources/frontend/public/hero/hero-primary.webp
  - sources/frontend/public/hero/hero-secondary.webp
  - sources/frontend/src/components/ui/Badge/Badge.tsx
  - sources/frontend/src/components/ui/Badge/Badge.module.css
  - .planning/phases/15-public-polish-milestone-audit/15-MANUAL-CHECKLIST.md
  - .planning/phases/15-public-polish-milestone-audit/15-SELECTOR-AUDIT.md
autonomous: true
requirements: [PUB-01, PUB-04, TEST-02]
tags: [phase-15, wave-0, prep, assets, badge, selectors]

must_haves:
  truths:
    - "Thư mục public/hero/ tồn tại với 2 file WebP hợp lệ (primary + secondary), tổng size < 400KB"
    - "Badge component support 3 variants mới: success / warning / danger với CSS class tương ứng"
    - "15-SELECTOR-AUDIT.md document hóa selectors ổn định cho AddressPicker + ProfileSettings + ReviewSection + có quyết định DELIVERED order strategy cho smoke test #3"
    - "15-MANUAL-CHECKLIST.md liệt kê 5 manual visual checks cho LCP / Featured-NewArrivals dedupe / thumbnail swap / stock badge tiers / breadcrumb brand"
  artifacts:
    - path: "sources/frontend/public/hero/hero-primary.webp"
      provides: "Hero LCP image primary (~600×800, WebP)"
    - path: "sources/frontend/public/hero/hero-secondary.webp"
      provides: "Hero secondary visual asset"
    - path: "sources/frontend/src/components/ui/Badge/Badge.tsx"
      provides: "BadgeVariant union mở rộng 'success' | 'warning' | 'danger'"
      contains: "success | warning | danger"
    - path: "sources/frontend/src/components/ui/Badge/Badge.module.css"
      provides: "3 CSS classes mới với fallback hex tokens"
      contains: ".success"
    - path: ".planning/phases/15-public-polish-milestone-audit/15-SELECTOR-AUDIT.md"
      provides: "Audit kết quả AddressPicker selector + DELIVERED seed status + test #3 strategy"
    - path: ".planning/phases/15-public-polish-milestone-audit/15-MANUAL-CHECKLIST.md"
      provides: "Manual visual smoke checklist cho phase gate"
  key_links:
    - from: "Plan 15-02 (PDP) [slug]/page.tsx"
      to: "components/ui/Badge/Badge.tsx"
      via: "<Badge variant='success|warning|danger'>"
      pattern: "variant=\"(success|warning|danger)\""
    - from: "Plan 15-01 (homepage) app/page.tsx"
      to: "public/hero/hero-primary.webp"
      via: "<Image src='/hero/hero-primary.webp' priority>"
      pattern: "/hero/hero-primary\\.webp"
    - from: "Plan 15-03 (smoke E2E) smoke.spec.ts"
      to: "15-SELECTOR-AUDIT.md (DELIVERED strategy)"
      via: "test ordering decision (serial vs skip-if-no-data)"
      pattern: "test\\.describe\\.serial|test\\.skip"
---

<objective>
Wave 0 prep cho Phase 15: tạo hero WebP assets, mở rộng Badge component (3 variants mới với fallback hex vì `--success`/`--warning` tokens KHÔNG tồn tại trong globals.css đã verified), audit selectors cần dùng cho smoke E2E (AddressPicker + ProfileSettings + ReviewSection) và quyết định DELIVERED order strategy cho test #3, viết manual visual checklist cho phase gate.

Purpose: Unblock các Wave sau (Wave 1 cần hero assets + dedupe logic; Wave 2 cần Badge variants; Wave 3 cần selectors locked + strategy locked).

Output: 2 file WebP binary + Badge.tsx + Badge.module.css mở rộng + 2 doc markdown trong phase folder.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/15-public-polish-milestone-audit/15-CONTEXT.md
@.planning/phases/15-public-polish-milestone-audit/15-RESEARCH.md
@.planning/phases/15-public-polish-milestone-audit/15-PATTERNS.md
@.planning/phases/15-public-polish-milestone-audit/15-UI-SPEC.md
@sources/frontend/src/components/ui/Badge/Badge.tsx
@sources/frontend/src/components/ui/Badge/Badge.module.css

<interfaces>
<!-- Badge contract hiện tại (Badge.tsx line 4) -->
```typescript
export type BadgeVariant = 'default' | 'sale' | 'new' | 'hot' | 'out-of-stock';
```
<!-- CSS class lookup tự động qua styles[variant] — không cần đụng component body -->

<!-- AddressPicker selector pattern (verified) — line 109-127 AddressPicker.tsx -->
```tsx
<div role="option" aria-selected={isSelected} className={styles.item} onClick={...} tabIndex={0}>
```
<!-- → Smoke test #2 dùng `page.locator('[role="option"]').first()` -->

<!-- ProfileSettings selectors (verified) — settings/page.tsx -->
input id="fullName"  → page.locator('#fullName')
input id="phone"     → page.locator('#phone')
data-testid="successMsg" → page.locator('[data-testid="successMsg"]')

<!-- Globals.css verified (Bash check 2026-05-02): --success / --warning KHÔNG tồn tại -->
<!-- → MUST hard-code hex theo UI-SPEC line 102-110 fallback contract -->
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Tạo hero WebP assets (mkdir + 2 file binary)</name>
  <files>sources/frontend/public/hero/hero-primary.webp, sources/frontend/public/hero/hero-secondary.webp</files>

  <read_first>
    - sources/frontend/src/app/page.tsx (lines 80-97 — xác nhận Unsplash URL hiện tại để source download nếu cần)
    - .planning/phases/15-public-polish-milestone-audit/15-RESEARCH.md (Pitfall 5 — WebP target 80-150KB / 60-100KB)
    - .planning/phases/15-public-polish-milestone-audit/15-UI-SPEC.md (line 53-54 — aspect ratio bám parent 75%×85% / 45%×50%)
  </read_first>

  <action>
Tạo thư mục `sources/frontend/public/hero/` (mkdir -p) — KHÔNG tồn tại hiện tại (verified bằng Bash ls public/ chỉ có file.svg/globe.svg/next.svg/placeholder.png/vercel.svg/window.svg).

Tạo 2 file WebP cho hero per D-01 + UI-SPEC color/dimension contract:

1. **hero-primary.webp** — dimension target ~600×800 (portrait-ish 3:4), file size 80-150KB, quality 80. Subject: sản phẩm consumer/electronics/fashion phù hợp tone shop tmđt VN. Source options (Claude's discretion):
   - **Option A (preferred)**: Download URL hiện tại trong `app/page.tsx:84` (`https://images.unsplash.com/photo-1558769132-cb1aea458c5e?w=600&h=800&fit=crop`) → lưu local → convert WebP qua `cwebp -q 80 input.jpg -o sources/frontend/public/hero/hero-primary.webp`. Nếu cwebp KHÔNG cài, dùng squoosh.app web upload.
   - **Option B (fallback)**: Sinh placeholder solid-color WebP (~600×800) bằng ImageMagick: `magick -size 600x800 gradient:'#0040a1-#3366cc' -quality 80 sources/frontend/public/hero/hero-primary.webp` — đảm bảo file tồn tại + render OK + file size < 50KB. Acceptable cho v1.2 demo (visual fidelity polish defer v1.3 nếu cần).

2. **hero-secondary.webp** — dimension target ~400×500, file size 60-100KB, quality 80. Source URL hiện tại `app/page.tsx:92` (`https://images.unsplash.com/photo-1606760227091-3dd870d97f1d?w=400&h=500&fit=crop`) — same conversion process. Fallback: ImageMagick gradient `#3366cc-#0040a1` 400×500.

Sau khi tạo:
- `ls -la sources/frontend/public/hero/` xác nhận 2 file
- Verify size: `du -k sources/frontend/public/hero/*.webp` mỗi file < 200KB (Pitfall 5)
- File extension PHẢI `.webp` (KHÔNG .jpg/.png — D-01)

Lý do KHÔNG dùng remote loader Unsplash CDN: tránh phụ thuộc external khi demo offline (D-01 lock).
  </action>

  <acceptance_criteria>
    - `test -f sources/frontend/public/hero/hero-primary.webp` → file exists
    - `test -f sources/frontend/public/hero/hero-secondary.webp` → file exists
    - `file sources/frontend/public/hero/hero-primary.webp` → output chứa "Web/P" hoặc "RIFF" (WebP magic byte)
    - `du -k sources/frontend/public/hero/hero-primary.webp` → ≤ 200 KB
    - `du -k sources/frontend/public/hero/hero-secondary.webp` → ≤ 200 KB
  </acceptance_criteria>

  <verify>
    <automated>cd sources/frontend && test -f public/hero/hero-primary.webp && test -f public/hero/hero-secondary.webp && file public/hero/*.webp | grep -i "webp\|RIFF"</automated>
  </verify>

  <done>2 file WebP tồn tại trong `sources/frontend/public/hero/`, file size hợp lý (< 200KB mỗi file), magic byte WebP valid.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Mở rộng Badge component với 3 variants mới (success/warning/danger)</name>
  <files>sources/frontend/src/components/ui/Badge/Badge.tsx, sources/frontend/src/components/ui/Badge/Badge.module.css</files>

  <read_first>
    - sources/frontend/src/components/ui/Badge/Badge.tsx (line 4 — BadgeVariant union hiện tại)
    - sources/frontend/src/components/ui/Badge/Badge.module.css (lines 21-44 — pattern variant class hiện có: .sale, .new, .hot, .out-of-stock)
    - sources/frontend/src/app/globals.css (verified — KHÔNG có --success / --warning tokens; CÓ --error-container + --on-error-container)
    - .planning/phases/15-public-polish-milestone-audit/15-UI-SPEC.md (lines 102-110 — color contract chi tiết với fallback hex)
    - .planning/phases/15-public-polish-milestone-audit/15-PATTERNS.md (lines 401-419 — Badge.module.css extend pattern)
  </read_first>

  <action>
**File 1: `sources/frontend/src/components/ui/Badge/Badge.tsx`**

Thay line 4 (hiện tại: `export type BadgeVariant = 'default' | 'sale' | 'new' | 'hot' | 'out-of-stock';`) bằng:

```typescript
export type BadgeVariant =
  | 'default'
  | 'sale'
  | 'new'
  | 'hot'
  | 'out-of-stock'
  | 'success'
  | 'warning'
  | 'danger';
```

KHÔNG đụng component body. CSS class lookup `styles[variant]` đã generic — chỉ cần thêm class tương ứng vào module CSS.

**File 2: `sources/frontend/src/components/ui/Badge/Badge.module.css`**

Thêm 3 CSS classes mới ở cuối file (sau `.out-of-stock` block existing). Note quan trọng: CSS module class names PHẢI match exactly với variant string (case-sensitive — `success`, `warning`, `danger`). KHÔNG dùng kebab-case.

Append:

```css
/* Stock badge variants — Phase 15 PUB-04 */
/* Note: --success / --warning design tokens chưa tồn tại trong globals.css (verified 2026-05-02). */
/* Dùng fallback hex CSS variable pattern var(--token, #fallback) để future-proof khi token được add v1.3. */

.success {
  background-color: rgba(22, 163, 74, 0.15); /* fallback for color-mix(in srgb, var(--success, #16a34a) 15%, transparent) */
  color: #15803d; /* green-700 — WCAG AA contrast trên light tint */
}

.warning {
  background-color: rgba(245, 158, 11, 0.18); /* fallback for amber-500 18% tint */
  color: #b45309; /* amber-700 — WCAG AA contrast */
}

.danger {
  background-color: var(--error-container);
  color: var(--on-error-container);
}
```

Lý do `.danger` reuse `--error-container` tokens existing (M3 palette đã có) — KHÔNG cần token mới + đảm bảo WCAG AA. `.success` + `.warning` hard-code hex per UI-SPEC line 110 (D-15) — acceptable vì tokens không tồn tại.
  </action>

  <acceptance_criteria>
    - `grep -E "success.*warning.*danger" sources/frontend/src/components/ui/Badge/Badge.tsx` → matches BadgeVariant union mới
    - `grep "^\\.success" sources/frontend/src/components/ui/Badge/Badge.module.css` → matches
    - `grep "^\\.warning" sources/frontend/src/components/ui/Badge/Badge.module.css` → matches
    - `grep "^\\.danger" sources/frontend/src/components/ui/Badge/Badge.module.css` → matches
    - `grep "var(--error-container)" sources/frontend/src/components/ui/Badge/Badge.module.css` → matches (danger reuse M3 token)
    - `cd sources/frontend && npm run build` → exit 0 (TypeScript compile success)
  </acceptance_criteria>

  <verify>
    <automated>cd sources/frontend && npm run build 2>&1 | tail -20</automated>
  </verify>

  <done>BadgeVariant union mở rộng 3 variants mới, 3 CSS classes existed với colors đúng UI-SPEC contract, npm run build PASS không TS error.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Selector audit + DELIVERED order strategy + manual checklist (2 docs)</name>
  <files>.planning/phases/15-public-polish-milestone-audit/15-SELECTOR-AUDIT.md, .planning/phases/15-public-polish-milestone-audit/15-MANUAL-CHECKLIST.md</files>

  <read_first>
    - sources/frontend/src/components/ui/AddressPicker/AddressPicker.tsx (lines 100-140 — confirm `role="option"` selector)
    - sources/frontend/src/app/profile/settings/page.tsx (lines 155-170 — confirm `id="fullName"` `id="phone"` selectors)
    - sources/frontend/src/app/products/[slug]/ReviewSection/* (grep cho form/textarea/submit selectors — chưa verified)
    - sources/frontend/e2e/order-detail.spec.ts (precedent skip-if-no-data pattern lines 50-53)
    - db/init/01-schemas.sql (verified: chỉ chứa CREATE SCHEMA — KHÔNG có DELIVERED order seed)
    - .planning/phases/15-public-polish-milestone-audit/15-VALIDATION.md (Wave 0 Requirements section)
  </read_first>

  <action>
Tạo 2 file markdown trong phase folder.

**File 1: `.planning/phases/15-public-polish-milestone-audit/15-SELECTOR-AUDIT.md`**

Document hóa selectors đã verified + quyết định DELIVERED strategy. Format:

```markdown
# Phase 15 — Selector Audit + DELIVERED Strategy

**Audited:** 2026-05-02
**Purpose:** Lock selectors stable cho smoke E2E tests + decide test #3 DELIVERED order data strategy.

## Verified Selectors

### AddressPicker (Smoke #2 — checkout)
- File: `sources/frontend/src/components/ui/AddressPicker/AddressPicker.tsx:109-127`
- Selector: `[role="option"]` (verified line 111: `<div role="option" aria-selected={isSelected}>`)
- Playwright usage: `page.locator('[role="option"]').first()`
- Trigger button: `[class*="trigger"]` (line 68 — fallback nếu cần expand dropdown trước)

### ProfileSettings (Smoke #4 — fullName/phone edit)
- File: `sources/frontend/src/app/profile/settings/page.tsx:158,163`
- Selectors:
  - Full name input: `#fullName` (line 158)
  - Phone input: `#phone` (line 163)
  - Submit button: cần grep `name="submit"` hoặc `data-testid` thực tế. Fallback: `getByRole('button', { name: /lưu|cập nhật|save/i })`
  - Success toast: `[data-testid="successMsg"]` (line 239)

### ReviewSection (Smoke #3 — review submission)
- File: `sources/frontend/src/app/products/[slug]/ReviewSection/*` (TODO: executor grep trước khi viết test)
- Expected selectors (predict — verify trong Plan 15-03):
  - Star rating button: `[role="radio"][aria-label*="sao"]` hoặc `[data-testid="rating-5"]`
  - Content textarea: `textarea[name="content"]` hoặc `#reviewContent`
  - Submit button: `getByRole('button', { name: /gửi đánh giá|submit/i })`
- KHÔNG locked → smoke spec executor phải grep `ReviewSection/*` trước khi viết SMOKE-3 (note trong Plan 15-03 task action).

## DELIVERED Order Strategy (Smoke #3 dependency)

**Status seed:** `db/init/` chỉ có `01-schemas.sql` (CREATE SCHEMA only). KHÔNG có DELIVERED order pre-seeded cho user demo (verified bằng find db/ + grep DELIVERED 2026-05-02).

**Decision:** Strategy A (skip-if-no-data) — Smoke #3 dùng pattern precedent `order-detail.spec.ts:50-53`:

```typescript
const productLink = page.locator('a[href^="/products/"]').first();
const hasProduct = await productLink.isVisible({ timeout: 5000 }).catch(() => false);
if (!hasProduct) {
  test.skip(true, 'Không có product nào — seed empty');
  return;
}
// Mở PDP → check eligibility → nếu không eligible (verified-buyer fail), test.skip với message rõ ràng
```

Lý do chọn Strategy A:
- D-18 cho phép skip-if-no-data degradation (precedent order-detail).
- Strategy B (test ordering serial place-order → admin update DELIVERED) yêu cầu admin status update endpoint — chưa verified tồn tại + tăng test fragility.
- Strategy C (seed DELIVERED order) trái D-18 ("KHÔNG seed mới").

**Trade-off:** Smoke #3 có thể skip trên fresh stack nếu seed không có DELIVERED → vẫn PASS criteria D-19 (4/4 pass hoặc skip với reason rõ ràng — KHÔNG fail). Phase gate accept skip với evidence trong test report.

**Tương lai (defer v1.3):** Add 1 DELIVERED order seed (`V101__seed_delivered_orders.sql` cho dev only, KHÔNG production migration) — unblock smoke #3 deterministic.

## Open Items cho Plan 15-03 executor
- [ ] Grep `app/products/[slug]/ReviewSection/` xác định exact rating button + textarea + submit selector trước khi viết SMOKE-3.
- [ ] Verify `e2e/storageState/user.json` tồn tại từ global-setup (Phase 9). Nếu không → smoke spec note "TEST-02 phụ thuộc storageState user fixture từ global-setup.ts".
```

**File 2: `.planning/phases/15-public-polish-milestone-audit/15-MANUAL-CHECKLIST.md`**

Manual visual smoke list cho phase gate (5 items theo VALIDATION.md "Manual-Only Verifications" table). Format:

```markdown
# Phase 15 — Manual Visual Smoke Checklist

**Created:** 2026-05-02
**Trigger:** Chạy SAU khi Wave 1 + Wave 2 + Wave 3 complete, TRƯỚC khi `/gsd-verify-work` Phase 15.

## Setup
1. `cd sources/frontend && npm run build` → exit 0
2. `cd sources/frontend && npm start` → app running ở http://localhost:3000
3. (Smoke E2E) Đảm bảo docker compose stack BE running: `docker compose up -d --build` (root)

## Checklist (5 items)

### M1: Hero LCP < 2.5s (PUB-01) — Lighthouse
- Mở Chrome DevTools → Lighthouse tab → Mobile preset → Performance category only → Generate report
- ✅ PASS: LCP metric < 2500ms (xanh hoặc cam, KHÔNG đỏ)
- ❌ FAIL: LCP > 2500ms → check WebP file size, kiểm tra `priority` prop, rebuild với `rm -rf .next && npm run build`

### M2: Featured + New Arrivals dedupe (PUB-02) — Visual
- Mở `/`
- Scroll qua Featured carousel (8 cards) — note 8 product names/images
- Scroll xuống New Arrivals grid (8 cards)
- ✅ PASS: KHÔNG product nào trùng giữa 2 sections (single-fetch slice(0,8) vs slice(8,16) đảm bảo không overlap)
- ⚠️ Catalog < 16 products: New Arrivals có thể empty/nhỏ hơn 8 — acceptable per D-09 trade-off

### M3: PDP Thumbnail click → main swap (PUB-03) — Functional
- Mở PDP của product có > 1 ảnh (e.g. `/products/{slug}`)
- Click thumbnail thứ 2, 3, 4 lần lượt
- ✅ PASS: Main image đổi đúng + active thumbnail có border 2px primary color rõ rệt
- ✅ PASS a11y: Inspect HTML — thumbnail active có `aria-current="true"`, inactive KHÔNG có aria-current

### M4: Stock badge 3-tier color match (PUB-04) — Visual
- Setup: cần 3 products với `stock` ∈ {0, 5, 100}. Nếu seed không có → admin tạo qua `/admin/products/new` hoặc SQL direct.
- Mở 3 PDP riêng:
  - stock=100: ✅ Badge xanh "✓ Còn hàng" + add-to-cart visible
  - stock=5: ✅ Badge vàng/amber "⚠ Sắp hết hàng (còn 5)" + add-to-cart visible
  - stock=0: ✅ Badge đỏ "✗ Hết hàng" + add-to-cart **HIDDEN hoàn toàn** (return null) + inline message "Sản phẩm tạm hết — vui lòng quay lại sau." + "Xem giỏ hàng" button vẫn show
- ❌ FAIL nếu add-to-cart chỉ disabled (chưa hide): trái D-16

### M5: PDP Breadcrumb brand link (PUB-03) — Functional
- Mở PDP product có brand
- ✅ PASS: Breadcrumb format `Trang chủ / {Brand} / {Name}` (3 segments, KHÔNG còn category)
- Click brand segment → ✅ Navigate sang `/products?brand={brand}`
- Verify FilterSidebar (Phase 14) pre-check brand đó (continuity navigation)
- Mở PDP product KHÔNG có brand → ✅ Fallback: `Trang chủ / Sản phẩm / {Name}` (link "Sản phẩm" → /products)

## Sign-off

- [ ] M1 LCP PASS
- [ ] M2 Dedupe PASS
- [ ] M3 Thumbnail PASS
- [ ] M4 Stock badge PASS
- [ ] M5 Breadcrumb PASS

**Tester:** _____________
**Date:** _____________
**Phase 15 verify status:** ⬜ pending → ✅ ready cho `/gsd-verify-work`
```
  </action>

  <acceptance_criteria>
    - `test -f .planning/phases/15-public-polish-milestone-audit/15-SELECTOR-AUDIT.md` → exists
    - `test -f .planning/phases/15-public-polish-milestone-audit/15-MANUAL-CHECKLIST.md` → exists
    - `grep "role=\"option\"" .planning/phases/15-public-polish-milestone-audit/15-SELECTOR-AUDIT.md` → matches (AddressPicker selector)
    - `grep "Strategy A" .planning/phases/15-public-polish-milestone-audit/15-SELECTOR-AUDIT.md` → matches (DELIVERED decision)
    - `grep -c "^### M[1-5]" .planning/phases/15-public-polish-milestone-audit/15-MANUAL-CHECKLIST.md` → output `5` (5 checklist items)
  </acceptance_criteria>

  <verify>
    <automated>test -f .planning/phases/15-public-polish-milestone-audit/15-SELECTOR-AUDIT.md && test -f .planning/phases/15-public-polish-milestone-audit/15-MANUAL-CHECKLIST.md && grep -c "^### M" .planning/phases/15-public-polish-milestone-audit/15-MANUAL-CHECKLIST.md</automated>
  </verify>

  <done>2 doc markdown tồn tại với content đầy đủ; selector audit lock các selectors verified + DELIVERED strategy A documented; checklist 5 items M1-M5 cover toàn bộ manual verifications từ VALIDATION.md.</done>
</task>

</tasks>

<verification>
- `cd sources/frontend && npm run build` exit 0 (Badge change không break build)
- 2 file WebP tồn tại với valid magic byte
- 2 doc markdown trong phase folder với required content
- Wave 1 + Wave 2 + Wave 3 có thể start ngay sau Wave 0 commit
</verification>

<success_criteria>
- BadgeVariant union mở rộng support 3 variants mới — đảm bảo Plan 15-02 import + dùng được ngay
- Hero assets sẵn sàng cho Plan 15-01 reference path `/hero/hero-primary.webp`
- Smoke test selectors locked — Plan 15-03 executor không phải re-grep nhiều lần
- Manual checklist sẵn sàng cho phase gate sau Wave 3
</success_criteria>

<output>
After completion, create `.planning/phases/15-public-polish-milestone-audit/15-00-SUMMARY.md` ghi lại:
- Hero WebP source/method (Unsplash download + cwebp vs ImageMagick fallback)
- Badge variants final colors (verify hex values match UI-SPEC line 102-110)
- DELIVERED strategy decision (A: skip-if-no-data) + rationale
- Open items chuyển cho Plan 15-03 executor (ReviewSection selector grep)
</output>
