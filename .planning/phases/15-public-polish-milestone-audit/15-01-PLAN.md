---
phase: 15
plan: 01
type: execute
wave: 1
depends_on: [15-00]
files_modified:
  - sources/frontend/src/app/page.tsx
  - sources/frontend/src/app/page.module.css
autonomous: true
requirements: [PUB-01, PUB-02]
tags: [phase-15, wave-1, homepage, hero, featured, carousel, dedupe]

must_haves:
  truths:
    - "Hero section dùng <Image priority src='/hero/hero-primary.webp'> + secondary <Image> (KHÔNG còn <img> Unsplash)"
    - "Featured carousel render 8 sản phẩm sort theo createdAt DESC trong scroll-snap container có role='region' + tabIndex=0"
    - "New Arrivals grid render slice(8,16) cùng dataset với Featured (KHÔNG product nào trùng)"
    - "Categories grid giữ link /products?category={slug} (KHÔNG đổi sang brand per D-07)"
    - "Hero CTA secondary đổi từ /collections (404) sang /products label 'Xem tất cả sản phẩm'"
    - "Single-fetch pattern: 1 listProducts({ size: 16, sort: 'createdAt,desc' }) thay vì 2 separate fetch"
  artifacts:
    - path: "sources/frontend/src/app/page.tsx"
      provides: "Homepage refactored với hero next/image + carousel + dedupe + CTA fix"
      contains: "/hero/hero-primary.webp"
    - path: "sources/frontend/src/app/page.module.css"
      provides: ".featuredScroll + .featuredCard classes cho scroll-snap carousel"
      contains: "scroll-snap-type: x mandatory"
  key_links:
    - from: "app/page.tsx hero section"
      to: "public/hero/hero-primary.webp (Wave 0)"
      via: "<Image src='/hero/hero-primary.webp' priority>"
      pattern: "src=\"/hero/hero-primary\\.webp\""
    - from: "app/page.tsx Featured + New Arrivals"
      to: "services/products.ts listProducts({ size, sort })"
      via: "single Promise resp.content + slice(0,8) + slice(8,16)"
      pattern: "slice\\(8, ?16\\)|slice\\(0, ?8\\)"
    - from: "app/page.tsx Featured carousel container"
      to: "page.module.css .featuredScroll"
      via: "className={styles.featuredScroll}"
      pattern: "featuredScroll.*role=\"region\".*tabIndex"
---

<objective>
Refactor homepage `/` để align với ROADMAP Phase 15 SC-1 + SC-2:
1. Hero `<img>` Unsplash → `<Image priority>` local WebP (PUB-01 LCP)
2. Featured products: sort `createdAt,desc` + render dạng CSS scroll-snap horizontal carousel (PUB-01)
3. New Arrivals dedupe via single-fetch `slice(8,16)` cùng dataset (PUB-02 — Pitfall 2 tránh race)
4. Categories grid + link giữ nguyên `?category=` (D-07)
5. Hero CTA secondary copy + link fix (D-02 — tránh 404)

Purpose: Homepage trở thành landing visible polish-quality, LCP measurable, KHÔNG external CDN dependency, dedupe deterministic.

Output: Modified `app/page.tsx` + `app/page.module.css`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/15-public-polish-milestone-audit/15-CONTEXT.md
@.planning/phases/15-public-polish-milestone-audit/15-RESEARCH.md
@.planning/phases/15-public-polish-milestone-audit/15-PATTERNS.md
@.planning/phases/15-public-polish-milestone-audit/15-UI-SPEC.md
@.planning/phases/15-public-polish-milestone-audit/15-00-SUMMARY.md
@sources/frontend/src/app/page.tsx
@sources/frontend/src/app/page.module.css

<interfaces>
<!-- listProducts signature (services/products.ts:36 — verified) -->
```typescript
listProducts({ size?: number; sort?: string; page?: number }): Promise<PageResponse<Product>>
// PageResponse: { content: Product[]; ... }
```

<!-- next/image priority pattern (precedent: [slug]/page.tsx:130-137) -->
```tsx
import Image from 'next/image';
<Image src="..." alt="..." fill sizes="..." priority className={...} />
```

<!-- Existing CSS tokens dùng trong page.module.css -->
var(--space-1..7), var(--radius-lg/xl/2xl), var(--surface-container-low), var(--primary), var(--on-surface-variant)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Refactor hero section sang next/image + fix CTA secondary</name>
  <files>sources/frontend/src/app/page.tsx</files>

  <read_first>
    - sources/frontend/src/app/page.tsx (full file — đặc biệt lines 1-19 imports, 73-78 CTA, 80-97 hero img blocks)
    - sources/frontend/src/app/products/[slug]/page.tsx (lines 130-137 — precedent next/image priority sizes)
    - .planning/phases/15-public-polish-milestone-audit/15-UI-SPEC.md (Hero section copywriting + interaction contract lines 118-129, 207-211)
    - .planning/phases/15-public-polish-milestone-audit/15-PATTERNS.md (lines 100-145 — next/image refactor + CTA fix concrete patches)
  </read_first>

  <action>
**Step 1: Add `Image` import**

Tại line 2-3 (sau `import React, { useCallback, useEffect, useState } from 'react';`), thêm:

```tsx
import Image from 'next/image';
```

**Step 2: Refactor 2 CTA buttons (D-02 — tránh /collections 404)**

Tìm block `.heroActions` (khoảng lines 73-78). Hiện tại có 2 Button:
```tsx
<Button href="/products" size="lg">Khám phá ngay</Button>
<Button href="/collections" variant="secondary" size="lg">Xem bộ sưu tập</Button>
```

Thay button thứ 2:
```tsx
<Button href="/products" variant="secondary" size="lg">Xem tất cả sản phẩm</Button>
```

**Step 3: Refactor 2 hero `<img>` blocks → `<Image>` (D-01)**

Tìm block `.heroVisual` (khoảng lines 80-97) — hiện tại có 2 `<img>` Unsplash:
```tsx
<div className={styles.heroVisual}>
  <div className={styles.heroImagePrimary}>
    <img src="https://images.unsplash.com/photo-1558769132-cb1aea458c5e?..." alt="..." />
  </div>
  <div className={styles.heroImageSecondary}>
    <img src="https://images.unsplash.com/photo-1606760227091-3dd870d97f1d?..." alt="..." />
  </div>
</div>
```

Thay bằng:

```tsx
<div className={styles.heroVisual}>
  <div className={styles.heroImagePrimary}>
    <Image
      src="/hero/hero-primary.webp"
      alt="Sản phẩm nổi bật"
      fill
      sizes="(max-width: 1024px) 80vw, 45vw"
      priority
      className={styles.heroImg}
    />
  </div>
  <div className={styles.heroImageSecondary}>
    <Image
      src="/hero/hero-secondary.webp"
      alt="Phụ kiện cao cấp"
      fill
      sizes="(max-width: 1024px) 50vw, 25vw"
      className={styles.heroImg}
    />
  </div>
</div>
```

Notes:
- `priority` CHỈ set trên hero-primary (Pitfall — chỉ 1-2 LCP candidates, KHÔNG cả 2).
- `fill` requires parent `position: relative` (đã có trong `.heroImagePrimary` + `.heroImageSecondary` precedent).
- `sizes` literal stable strings (Pitfall 1 — tránh hydration mismatch).
- KHÔNG cần `width/height` khi `fill`.
- Ensure `.heroImg` class tồn tại trong CSS module (nếu chưa, để Task 2 thêm — hiện tại có thể KHÔNG có nếu trước đó dùng `<img>` thuần). Nếu CSS module chưa có `.heroImg`, có thể OMIT className và để next/image styling default — KHÔNG block chức năng.
  </action>

  <acceptance_criteria>
    - `grep "import Image from 'next/image'" sources/frontend/src/app/page.tsx` → matches
    - `grep "/hero/hero-primary.webp" sources/frontend/src/app/page.tsx` → matches
    - `grep "/hero/hero-secondary.webp" sources/frontend/src/app/page.tsx` → matches
    - `grep -c "priority" sources/frontend/src/app/page.tsx` → output `1` (CHỈ 1 priority — chỉ trên primary)
    - `grep "Xem tất cả sản phẩm" sources/frontend/src/app/page.tsx` → matches (CTA secondary mới)
    - `grep -c "/collections" sources/frontend/src/app/page.tsx` → output `0` (KHÔNG còn link broken)
    - `grep -c "images.unsplash.com" sources/frontend/src/app/page.tsx` → output `0` (KHÔNG còn external img URL)
    - `cd sources/frontend && npm run build` → exit 0
  </acceptance_criteria>

  <verify>
    <automated>cd sources/frontend && npm run build 2>&1 | tail -15 && grep -c "/collections\|images.unsplash" src/app/page.tsx</automated>
  </verify>

  <done>Hero render với next/image priority + secondary, CTA secondary copy + link fix, KHÔNG còn Unsplash URL hay /collections link, build PASS.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Single-fetch dedupe + Featured carousel (page.tsx + page.module.css)</name>
  <files>sources/frontend/src/app/page.tsx, sources/frontend/src/app/page.module.css</files>

  <read_first>
    - sources/frontend/src/app/page.tsx (lines 21-53 load pattern; lines 138-170 featured render; lines 220-252 latest render)
    - sources/frontend/src/app/page.module.css (full file để biết existing class pattern + responsive breakpoints; đặc biệt lines 188-192 .productsGrid + lines 278-303 @media)
    - .planning/phases/15-public-polish-milestone-audit/15-PATTERNS.md (lines 53-99 single-fetch pattern + lines 175-197 CSS scroll-snap)
    - .planning/phases/15-public-polish-milestone-audit/15-UI-SPEC.md (Featured Carousel interaction lines 213-225 + Visual Layout sketch)
    - .planning/phases/15-public-polish-milestone-audit/15-RESEARCH.md (Pitfall 2 single-fetch + Pitfall 3 carousel a11y tabindex)
  </read_first>

  <action>
**STEP A: `sources/frontend/src/app/page.tsx` — Single-fetch refactor (D-04 + D-09 + Pitfall 2)**

Tìm khối load function (khoảng lines 21-53). Hiện tại có 2 fetch song song:
```tsx
const [featuredResp, latestResp] = await Promise.all([
  listProducts({ size: 8, sort: 'reviewCount,desc' }).catch(() =>
    listProducts({ size: 8 }),
  ),
  listProducts({ size: 8, sort: 'createdAt,desc' }).catch(() =>
    listProducts({ size: 8 }),
  ),
]);
setFeatured(featuredResp?.content ?? []);
setLatest(latestResp?.content ?? []);
```

Thay bằng (giữ Promise.all cho song song với listCategories nếu có — verify trong file):

```tsx
const [productsResp, categoriesResp] = await Promise.all([
  listProducts({ size: 16, sort: 'createdAt,desc' }).catch(() =>
    listProducts({ size: 16 }),
  ),
  listCategories().catch(() => null),
]);
const all = productsResp?.content ?? [];
setFeatured(all.slice(0, 8));
setLatest(all.slice(8, 16));
setCategories(categoriesResp ?? []);
```

(Adjust shape nếu file hiện tại có pattern listCategories khác — preserve existing categories load logic, chỉ thay 2 listProducts thành 1.)

**Lý do:**
- D-04: sort `createdAt,desc` thay `reviewCount,desc` — match ROADMAP SC-1.
- D-09: dedupe deterministic — Featured = slice(0,8), New Arrivals = slice(8,16) cùng dataset → KHÔNG overlap (Pitfall 2 fixed).
- Trade-off catalog < 16: New Arrivals < 8 hoặc empty — acceptable per D-09 → hiển thị empty state existing.

**STEP B: `sources/frontend/src/app/page.tsx` — Featured render thành carousel**

Tìm Featured render block (khoảng lines 152-168 / 138-170 — block có `featured.map`):
```tsx
{loading ? (
  <div className={styles.productsGrid}>
    {[...Array(4)].map((_, i) => (
      <div key={i} className="skeleton" style={{ height: 360, borderRadius: 'var(--radius-lg)' }} />
    ))}
  </div>
) : failed ? (
  <RetrySection onRetry={() => load()} loading={loading} />
) : featured.length > 0 ? (
  <div className={styles.productsGrid}>
    {featured.map((product) => (
      <ProductCard key={product.id} product={product} />
    ))}
  </div>
) : (
  <p style={{ color: 'var(--on-surface-variant)' }}>Chưa có sản phẩm nổi bật.</p>
)}
```

Thay branch `featured.length > 0` (giữ nguyên skeleton + RetrySection + empty per D-06):

```tsx
) : featured.length > 0 ? (
  <div
    className={styles.featuredScroll}
    role="region"
    aria-label="Sản phẩm nổi bật — vuốt ngang để xem thêm"
    tabIndex={0}
  >
    {featured.map((product) => (
      <div key={product.id} className={styles.featuredCard}>
        <ProductCard product={product} />
      </div>
    ))}
  </div>
) : (
```

**KHÔNG đụng** New Arrivals render block — giữ nguyên `.productsGrid` (D-10: chỉ Featured dùng carousel).

**STEP C: `sources/frontend/src/app/page.module.css` — Add carousel CSS**

Append vào cuối file (sau existing media queries):

```css
/* === Featured Carousel — Phase 15 PUB-01 (D-05) === */
.featuredScroll {
  display: flex;
  gap: var(--space-4);
  overflow-x: auto;
  scroll-snap-type: x mandatory;
  scroll-padding: var(--space-4);
  -webkit-overflow-scrolling: touch; /* iOS momentum */
  scrollbar-width: none; /* Firefox */
  padding-bottom: var(--space-2); /* breathing room */
}

.featuredScroll::-webkit-scrollbar {
  display: none; /* Chrome/Safari/Edge */
}

.featuredScroll:focus-visible {
  outline: 2px solid var(--primary);
  outline-offset: 2px;
  border-radius: var(--radius-lg);
}

.featuredCard {
  flex: 0 0 280px;
  scroll-snap-align: start;
}

@media (max-width: 768px) {
  .featuredCard {
    flex-basis: 240px;
  }
}
```

**Lưu ý:** strict reuse design tokens (`var(--space-4)`, `var(--space-2)`, `var(--primary)`, `var(--radius-lg)`). KHÔNG hard-code px ngoài `flex-basis` (justified per UI-SPEC line 53 — hiển thị 3.5 cards peek).
  </action>

  <acceptance_criteria>
    - `grep "size: 16, sort: 'createdAt,desc'" sources/frontend/src/app/page.tsx` → matches (single-fetch params)
    - `grep "all.slice(0, 8)" sources/frontend/src/app/page.tsx` → matches (Featured slice)
    - `grep "all.slice(8, 16)" sources/frontend/src/app/page.tsx` → matches (New Arrivals dedupe slice)
    - `grep -c "reviewCount,desc" sources/frontend/src/app/page.tsx` → output `0` (KHÔNG còn sort cũ)
    - `grep "featuredScroll" sources/frontend/src/app/page.tsx` → matches (carousel container)
    - `grep "role=\"region\"" sources/frontend/src/app/page.tsx` → matches (a11y Pitfall 3)
    - `grep "tabIndex={0}" sources/frontend/src/app/page.tsx` → matches (keyboard scroll Pitfall 3)
    - `grep "scroll-snap-type: x mandatory" sources/frontend/src/app/page.module.css` → matches
    - `grep "^.featuredScroll" sources/frontend/src/app/page.module.css` → matches
    - `grep "^.featuredCard" sources/frontend/src/app/page.module.css` → matches
    - `grep "flex-basis: 240px" sources/frontend/src/app/page.module.css` → matches (mobile responsive)
    - `cd sources/frontend && npm run build` → exit 0
  </acceptance_criteria>

  <verify>
    <automated>cd sources/frontend && npm run build 2>&1 | tail -15</automated>
  </verify>

  <done>Single-fetch dedupe pattern wired (Featured + New Arrivals từ cùng response slice), Featured render trong scroll-snap carousel có a11y attrs, CSS classes mới với design tokens, build PASS.</done>
</task>

</tasks>

<verification>
- `cd sources/frontend && npm run build` exit 0
- Manual: mở `/` xem hero + carousel scroll-snap (M1, M2 trong 15-MANUAL-CHECKLIST.md sau Wave 3)
- Smoke E2E (Wave 3): SMOKE-1 test hero render + CTA navigate `/products`
</verification>

<success_criteria>
- Homepage match ROADMAP SC-1 + SC-2 acceptance criteria
- LCP ready cho Lighthouse measurement (`next build && next start` mobile preset)
- Featured carousel mobile swipe-friendly (CSS scroll-snap native)
- New Arrivals KHÔNG bao giờ overlap Featured (deterministic dedupe via slice cùng dataset)
</success_criteria>

<output>
Tạo `.planning/phases/15-public-polish-milestone-audit/15-01-SUMMARY.md` ghi:
- Lines actually changed (số dòng add/remove)
- Confirm `<img>` Unsplash count = 0 sau refactor
- Catalog size hiện tại (nếu < 16, note New Arrivals empty acceptable)
- Lighthouse LCP measurement nếu đo được (defer cho M1 manual checklist nếu chưa)
</output>
