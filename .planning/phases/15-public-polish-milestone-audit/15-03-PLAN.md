---
phase: 15
plan: 03
type: execute
wave: 2
depends_on: [15-01, 15-02]
files_modified:
  - sources/frontend/e2e/smoke.spec.ts
autonomous: true
requirements: [TEST-02]
tags: [phase-15, wave-2, e2e, playwright, smoke]

must_haves:
  truths:
    - "File sources/frontend/e2e/smoke.spec.ts tồn tại với 4 test cases (SMOKE-1..4)"
    - "SMOKE-1 test anonymous: load /, hero render, click 'Khám phá ngay' → /products có ít nhất 1 ProductCard"
    - "SMOKE-2 test user: cart → /checkout → AddressPicker [role=option] click → submit → success"
    - "SMOKE-3 test user: PDP review submit (skip-if-no-DELIVERED per Wave 0 strategy A)"
    - "SMOKE-4 test user: /profile/settings sửa fullName + phone → success toast → reload persist"
    - "Tất cả authenticated tests dùng test.use({ storageState: 'e2e/storageState/user.json' }) — KHÔNG re-login"
  artifacts:
    - path: "sources/frontend/e2e/smoke.spec.ts"
      provides: "4 Playwright smoke tests covering critical v1.2 paths"
      min_lines: 120
      contains: "SMOKE-1"
  key_links:
    - from: "smoke.spec.ts SMOKE-2"
      to: "AddressPicker [role='option'] selector (verified Wave 0)"
      via: "page.locator('[role=\"option\"]').first().click()"
      pattern: "\\[role=\"option\"\\]"
    - from: "smoke.spec.ts SMOKE-3"
      to: "Wave 0 DELIVERED Strategy A (skip-if-no-data)"
      via: "test.skip(true, 'No DELIVERED order...') khi eligibility fail"
      pattern: "test\\.skip\\("
    - from: "smoke.spec.ts SMOKE-4"
      to: "/profile/settings #fullName + #phone (verified Wave 0)"
      via: "page.fill('#fullName', ...) + page.fill('#phone', ...)"
      pattern: "#fullName|#phone"
---

<objective>
Tạo file `sources/frontend/e2e/smoke.spec.ts` với 4 Playwright smoke tests (TEST-02 D-17/18/19/20):
1. SMOKE-1 (anon): Homepage hero + CTA navigation
2. SMOKE-2 (user): Cart → checkout → AddressPicker → submit
3. SMOKE-3 (user): PDP review submission (skip-if-no-DELIVERED degradation)
4. SMOKE-4 (user): Profile settings edit + persist

Purpose: Critical-path smoke coverage cho v1.2 milestone closure. PASS criteria: 4/4 PASS HOẶC PASS-with-skip có reason rõ ràng (skip ≠ fail).

Output: 1 file mới `e2e/smoke.spec.ts` (~150-200 LOC).

KHÔNG trong scope: search filter tests (D-20), visual regression, full E2E suite (defer v1.3).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/15-public-polish-milestone-audit/15-CONTEXT.md
@.planning/phases/15-public-polish-milestone-audit/15-RESEARCH.md
@.planning/phases/15-public-polish-milestone-audit/15-PATTERNS.md
@.planning/phases/15-public-polish-milestone-audit/15-SELECTOR-AUDIT.md
@.planning/phases/15-public-polish-milestone-audit/15-00-SUMMARY.md
@.planning/phases/15-public-polish-milestone-audit/15-01-SUMMARY.md
@.planning/phases/15-public-polish-milestone-audit/15-02-SUMMARY.md
@sources/frontend/e2e/global-setup.ts
@sources/frontend/e2e/auth.spec.ts
@sources/frontend/e2e/order-detail.spec.ts
@sources/frontend/e2e/password-change.spec.ts
@sources/frontend/playwright.config.ts

<interfaces>
<!-- Verified selectors từ 15-SELECTOR-AUDIT.md (Wave 0) -->

AddressPicker:
  - Item: [role="option"] (line 111 AddressPicker.tsx)
  - Trigger: [class*="trigger"]

ProfileSettings (/profile/settings):
  - Full name: #fullName
  - Phone: #phone
  - Success toast: [data-testid="successMsg"]
  - Submit form: getByRole('button', { name: /lưu|cập nhật/i })

Hero (after Plan 15-01):
  - Heading: getByRole('heading', { name: /chế tác thủ công/i })
  - CTA primary: getByRole('link', { name: 'Khám phá ngay' })

ReviewSection (TODO: executor grep [slug]/ReviewSection/ trước khi viết SMOKE-3):
  - Tab "Đánh giá": predict — getByRole('tab', { name: /đánh giá/i })
  - Star rating: predict — getByRole('radio', { name: /5 sao/i }) hoặc data-testid
  - Content textarea: predict — page.locator('textarea[name="content"]') hoặc #reviewContent
  - Submit: predict — getByRole('button', { name: /gửi đánh giá/i })

storageState:
  - User: 'e2e/storageState/user.json' (Phase 9 global-setup)

<!-- Existing playwright.config.ts has globalSetup registered → KHÔNG cần thay config -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Write smoke.spec.ts với SMOKE-1 (anon homepage) + SMOKE-4 (profile editing)</name>
  <files>sources/frontend/e2e/smoke.spec.ts</files>

  <read_first>
    - sources/frontend/e2e/auth.spec.ts (lines 1-43 — anonymous flow + cleared storageState pattern)
    - sources/frontend/e2e/password-change.spec.ts (lines 1-50 — form fill + submit + toast assertion pattern; storageState user)
    - sources/frontend/e2e/order-detail.spec.ts (lines 17-55 — storageState + skip-if-no-data degradation pattern)
    - sources/frontend/e2e/global-setup.ts (lines 30-53 — confirm storageState path output)
    - sources/frontend/playwright.config.ts (confirm baseURL + globalSetup registered)
    - .planning/phases/15-public-polish-milestone-audit/15-SELECTOR-AUDIT.md (verified selectors lock)
  </read_first>

  <action>
Tạo file mới `sources/frontend/e2e/smoke.spec.ts` với header docstring + SMOKE-1 + SMOKE-4 (2 tests độc lập, đơn giản nhất, viết trước để gate sớm).

Content:

```typescript
/**
 * Phase 15 / Plan 15-03 (TEST-02) — Smoke E2E (4 critical paths v1.2).
 *
 * D-17/D-18/D-19/D-20:
 *   SMOKE-1 (anon):  Homepage hero render + CTA "Khám phá ngay" → /products có ProductCard
 *   SMOKE-2 (user):  Cart → /checkout → AddressPicker [role=option] → submit order → success
 *   SMOKE-3 (user):  PDP review submission (skip-if-no-DELIVERED — Strategy A từ 15-SELECTOR-AUDIT.md)
 *   SMOKE-4 (user):  /profile/settings sửa fullName + phone → success toast → reload persist
 *
 * Selectors source: .planning/phases/15-public-polish-milestone-audit/15-SELECTOR-AUDIT.md (Wave 0 verified).
 * User storageState từ global-setup.ts (Phase 9 D-13) — KHÔNG re-login trong smoke spec.
 *
 * PASS criteria (D-19): 4/4 PASS hoặc PASS-with-skip (test.skip với reason rõ ràng) trên fresh
 *   docker stack `docker compose up -d --build && npx playwright test e2e/smoke.spec.ts`.
 *   Toàn bộ baseline (auth, admin-products/orders/users, order-detail, password-change) vẫn 100% PASS.
 */

import { test, expect } from '@playwright/test';

// ─────────────────────────────────────────────────────────────────
// SMOKE-1 (anonymous): Homepage hero + CTA navigation
// ─────────────────────────────────────────────────────────────────
test.describe('SMOKE-1: Homepage navigation (anonymous)', () => {
  test.use({ storageState: { cookies: [], origins: [] } });

  test('hero render + CTA "Khám phá ngay" → /products có ProductCard', async ({ page }) => {
    await page.goto('/');
    await page.waitForLoadState('domcontentloaded');

    // Hero heading visible (UI-SPEC line 124: "chế tác thủ công" gradient accent)
    await expect(
      page.getByRole('heading', { name: /chế tác thủ công/i })
    ).toBeVisible({ timeout: 10000 });

    // CTA primary "Khám phá ngay" (Plan 15-01 D-02 — link /products)
    const ctaPrimary = page.getByRole('link', { name: 'Khám phá ngay' });
    await expect(ctaPrimary).toBeVisible();
    await ctaPrimary.click();

    // Navigate /products
    await page.waitForURL(/\/products(\?|$)/, { timeout: 10000 });

    // Có ít nhất 1 ProductCard render (selector tolerant)
    const firstCard = page.locator('a[href^="/products/"]').first();
    await expect(firstCard).toBeVisible({ timeout: 10000 });
  });
});

// ─────────────────────────────────────────────────────────────────
// SMOKE-4 (user): Profile settings edit fullName + phone
// ─────────────────────────────────────────────────────────────────
test.describe('SMOKE-4: Profile editing persist', () => {
  test.use({ storageState: 'e2e/storageState/user.json' });

  test('sửa fullName + phone → success toast → reload persist', async ({ page }) => {
    await page.goto('/profile/settings');
    await page.waitForLoadState('domcontentloaded');

    // Verified selectors từ Wave 0 audit
    const fullNameInput = page.locator('#fullName');
    const phoneInput = page.locator('#phone');

    await expect(fullNameInput).toBeVisible({ timeout: 10000 });
    await expect(phoneInput).toBeVisible();

    // Generate unique value để verify persist (timestamp-based)
    const newName = `Smoke Test ${Date.now()}`;
    const newPhone = `0900${String(Date.now()).slice(-7)}`;

    await fullNameInput.fill(newName);
    await phoneInput.fill(newPhone);

    // Submit form — try data-testid first, fallback button name
    const submitBtn = page
      .locator('[data-testid="submitProfile"]')
      .or(page.getByRole('button', { name: /lưu|cập nhật|save/i }))
      .first();
    await submitBtn.click();

    // Success toast/message (verified [data-testid="successMsg"] tồn tại — line 239 settings/page.tsx)
    // Allow either testid hoặc text content fallback
    const successMsg = page
      .locator('[data-testid="successMsg"]')
      .or(page.getByText(/thành công|đã cập nhật|đã lưu/i))
      .first();
    await expect(successMsg).toBeVisible({ timeout: 10000 });

    // Reload + verify persist
    await page.reload();
    await page.waitForLoadState('domcontentloaded');
    await expect(fullNameInput).toHaveValue(newName, { timeout: 10000 });
    await expect(phoneInput).toHaveValue(newPhone);
  });
});

// SMOKE-2 + SMOKE-3 → Task 2
```

**Lưu ý:**
- Header docstring theo precedent `password-change.spec.ts` + `auth.spec.ts` convention.
- SMOKE-1 dùng `cleared storageState` (anon flow precedent auth.spec.ts:17-20).
- SMOKE-4 dùng `'e2e/storageState/user.json'` (precedent order-detail.spec.ts:17).
- Selectors fallback `.or()` cho robustness — submit button có thể chưa có data-testid (Wave 0 audit để open).
- `Date.now()` cho unique value đảm bảo persist test deterministic (không phụ thuộc previous state).
- KHÔNG re-implement login — global-setup đã handle (Phase 9 D-13).
  </action>

  <acceptance_criteria>
    - `test -f sources/frontend/e2e/smoke.spec.ts` → exists
    - `grep "SMOKE-1" sources/frontend/e2e/smoke.spec.ts` → matches
    - `grep "SMOKE-4" sources/frontend/e2e/smoke.spec.ts` → matches
    - `grep 'storageState: { cookies: \[\]' sources/frontend/e2e/smoke.spec.ts` → matches (anon SMOKE-1)
    - `grep "e2e/storageState/user.json" sources/frontend/e2e/smoke.spec.ts` → matches (user SMOKE-4)
    - `grep "#fullName" sources/frontend/e2e/smoke.spec.ts` → matches
    - `grep "#phone" sources/frontend/e2e/smoke.spec.ts` → matches
    - `cd sources/frontend && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -c "smoke.spec.ts"` → output `0` (KHÔNG có TS error trong smoke spec)
    - `cd sources/frontend && npx playwright test e2e/smoke.spec.ts --list 2>&1 | grep -c "SMOKE"` → ≥ 2 (2 test ở Task 1)
  </acceptance_criteria>

  <verify>
    <automated>cd sources/frontend && npx playwright test e2e/smoke.spec.ts --list 2>&1 | tail -20</automated>
  </verify>

  <done>smoke.spec.ts tồn tại với SMOKE-1 + SMOKE-4 tests, Playwright list parse OK (no syntax error), no TS error.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Append SMOKE-2 (checkout) + SMOKE-3 (review skip-if-no-data) vào smoke.spec.ts</name>
  <files>sources/frontend/e2e/smoke.spec.ts</files>

  <read_first>
    - sources/frontend/e2e/smoke.spec.ts (file hiện tại từ Task 1 — append tests)
    - sources/frontend/src/components/ui/AddressPicker/AddressPicker.tsx (lines 100-140 — confirm role=option pattern)
    - sources/frontend/src/app/checkout/page.tsx (verify URL path + structure — exists hay /checkout/page.tsx?)
    - sources/frontend/src/app/products/[slug]/ReviewSection/ (grep cho exact rating button + textarea + submit — EXECUTOR PHẢI GREP TRƯỚC KHI VIẾT)
    - sources/frontend/e2e/order-detail.spec.ts (lines 50-53 — skip-if-no-data pattern)
    - .planning/phases/15-public-polish-milestone-audit/15-SELECTOR-AUDIT.md (DELIVERED Strategy A documented)
  </read_first>

  <action>
**STEP A: Grep ReviewSection selectors trước khi viết SMOKE-3**

Mandatory pre-step (per 15-SELECTOR-AUDIT.md "Open Items"):
```bash
grep -rn "data-testid\|aria-label\|name=\"" sources/frontend/src/app/products/[slug]/ReviewSection/
```

Document selectors found vào docstring SMOKE-3 cho future maintenance.

**STEP B: Append vào cuối `sources/frontend/e2e/smoke.spec.ts`**

Append (sau khối SMOKE-4 từ Task 1):

```typescript
// ─────────────────────────────────────────────────────────────────
// SMOKE-2 + SMOKE-3 (user): authenticated cart/review flows
// ─────────────────────────────────────────────────────────────────
test.describe('SMOKE-2..3: authenticated commerce flows', () => {
  test.use({ storageState: 'e2e/storageState/user.json' });

  // ─── SMOKE-2: Address-at-checkout ───
  test('SMOKE-2: cart → /checkout → AddressPicker → submit', async ({ page }) => {
    // 1. Mở /products + pick first product
    await page.goto('/products');
    await page.waitForLoadState('domcontentloaded');

    const firstProductLink = page.locator('a[href^="/products/"]').first();
    const hasProduct = await firstProductLink.isVisible({ timeout: 5000 }).catch(() => false);
    if (!hasProduct) {
      test.skip(true, 'Không có product trong catalog — seed empty (Wave 0 strategy A degradation)');
      return;
    }
    await firstProductLink.click();
    await page.waitForURL(/\/products\/[^?]+/, { timeout: 10000 });

    // 2. Add to cart — chỉ click nếu add-to-cart visible (stock > 0 per Plan 15-02 D-16)
    const addToCartBtn = page.getByRole('button', { name: /thêm vào giỏ/i });
    const cartBtnVisible = await addToCartBtn.isVisible({ timeout: 5000 }).catch(() => false);
    if (!cartBtnVisible) {
      test.skip(true, 'Product hết hàng (stock=0) — add-to-cart hidden per D-16. Cần product stock > 0.');
      return;
    }
    await addToCartBtn.click();

    // 3. Goto /checkout
    await page.goto('/checkout');
    await page.waitForLoadState('networkidle');

    // 4. AddressPicker hiện list — verified selector [role="option"]
    const addressOption = page.locator('[role="option"]').first();
    const hasAddress = await addressOption.isVisible({ timeout: 10000 }).catch(() => false);
    if (!hasAddress) {
      // AddressPicker có thể cần expand trigger trước
      const trigger = page.locator('[class*="trigger"]').first();
      const hasTrigger = await trigger.isVisible({ timeout: 3000 }).catch(() => false);
      if (hasTrigger) {
        await trigger.click();
        await page.waitForTimeout(500);
      }
    }
    const addressOptionRetry = page.locator('[role="option"]').first();
    const finalHas = await addressOptionRetry.isVisible({ timeout: 5000 }).catch(() => false);
    if (!finalHas) {
      test.skip(true, 'User không có saved address — cần seed address trước khi chạy SMOKE-2');
      return;
    }
    await addressOptionRetry.click();

    // 5. Submit order
    const submitOrderBtn = page.getByRole('button', { name: /đặt hàng|thanh toán|submit/i });
    await expect(submitOrderBtn).toBeVisible({ timeout: 5000 });
    await submitOrderBtn.click();

    // 6. Assert success: redirect /orders|/profile|success page hoặc toast
    await Promise.race([
      page.waitForURL(/\/orders|\/profile|success/, { timeout: 15000 }),
      page.getByText(/đã đặt hàng|đặt hàng thành công|thành công/i).first().waitFor({ timeout: 15000 }),
    ]).catch(() => {
      throw new Error('Order submit không có URL navigate hoặc toast success — SMOKE-2 fail');
    });
  });

  // ─── SMOKE-3: Review submission ───
  // Selectors confirmed từ grep ReviewSection/ — TODO update sau khi grep
  test('SMOKE-3: PDP review submit (skip nếu no DELIVERED order — Strategy A)', async ({ page }) => {
    // Strategy A (15-SELECTOR-AUDIT.md): skip-if-no-data degradation.
    // Logic: mở first product PDP → check eligibility hint → nếu form không hiện thì skip với reason.

    await page.goto('/products');
    await page.waitForLoadState('domcontentloaded');

    const firstProductLink = page.locator('a[href^="/products/"]').first();
    const hasProduct = await firstProductLink.isVisible({ timeout: 5000 }).catch(() => false);
    if (!hasProduct) {
      test.skip(true, 'Không có product nào — seed empty');
      return;
    }
    await firstProductLink.click();
    await page.waitForURL(/\/products\/[^?]+/, { timeout: 10000 });

    // Mở tab "Đánh giá" — tolerant selector
    const reviewsTab = page
      .getByRole('tab', { name: /đánh giá/i })
      .or(page.getByRole('button', { name: /đánh giá/i }))
      .first();
    const hasReviewsTab = await reviewsTab.isVisible({ timeout: 5000 }).catch(() => false);
    if (hasReviewsTab) {
      await reviewsTab.click();
      await page.waitForTimeout(300);
    }

    // Check eligibility: nếu form KHÔNG hiện (REVIEW_NOT_ELIGIBLE — user không có DELIVERED order chứa product)
    // → Strategy A: skip với reason. Đây là behavior đúng per Phase 13 REV-01 + Wave 0 decision.
    const reviewForm = page
      .locator('form[data-testid="reviewForm"]')
      .or(page.locator('textarea[name="content"]'))
      .or(page.locator('#reviewContent'))
      .first();
    const hasForm = await reviewForm.isVisible({ timeout: 5000 }).catch(() => false);
    if (!hasForm) {
      test.skip(
        true,
        'Review form không hiện cho user (REVIEW_NOT_ELIGIBLE hoặc selectors khác) — ' +
          'cần DELIVERED order chứa product. Strategy A degradation per 15-SELECTOR-AUDIT.md.'
      );
      return;
    }

    // Submit rating 5 sao + content
    // Tolerant rating selectors (executor verify thực tế qua grep ReviewSection/)
    const star5 = page
      .getByRole('radio', { name: /5 sao|rating-5|^5$/i })
      .or(page.locator('[data-testid="rating-5"]'))
      .or(page.locator('[aria-label*="5 sao"]'))
      .first();
    if (await star5.isVisible({ timeout: 3000 }).catch(() => false)) {
      await star5.click();
    }

    const contentTextarea = page
      .locator('textarea[name="content"]')
      .or(page.locator('#reviewContent'))
      .or(page.locator('textarea').first())
      .first();
    await contentTextarea.fill(`Smoke test review ${Date.now()} — chất lượng tốt`);

    const submitBtn = page
      .locator('[data-testid="submitReview"]')
      .or(page.getByRole('button', { name: /gửi đánh giá|submit review/i }))
      .first();
    await submitBtn.click();

    // Assert review xuất hiện (toast hoặc list update)
    await Promise.race([
      page.getByText(/đánh giá đã gửi|cảm ơn|review submitted/i).first().waitFor({ timeout: 10000 }),
      page.getByText(/Smoke test review/).first().waitFor({ timeout: 10000 }),
    ]).catch(() => {
      // Một số implementation chỉ refresh list — tolerant accept nếu KHÔNG có error toast
      // Last resort: assert no error visible
    });

    // No error toast
    const errorToast = page.getByText(/lỗi|error|REVIEW_NOT_ELIGIBLE/i).first();
    expect(await errorToast.isVisible({ timeout: 2000 }).catch(() => false)).toBe(false);
  });
});
```

**Lưu ý quan trọng:**
- SMOKE-2 + SMOKE-3 dùng tolerant selectors (`.or()` chains) — robust hơn cho variation giữa implementation versions.
- Skip-if-no-data pattern (Strategy A) ở 3 chỗ trong SMOKE-2 (no product / hidden cart / no address) + 2 chỗ trong SMOKE-3 (no product / no review form) — đảm bảo PASS-with-skip thay vì hard fail.
- `Date.now()` cho unique content đảm bảo deterministic identity.
- Executor PHẢI grep ReviewSection/ trước khi commit — nếu selectors thực tế khác predictions, update tests + document trong SUMMARY.
  </action>

  <acceptance_criteria>
    - `grep "SMOKE-2" sources/frontend/e2e/smoke.spec.ts` → matches
    - `grep "SMOKE-3" sources/frontend/e2e/smoke.spec.ts` → matches
    - `grep "\\[role=\"option\"\\]" sources/frontend/e2e/smoke.spec.ts` → matches (AddressPicker selector)
    - `grep -c "test.skip" sources/frontend/e2e/smoke.spec.ts` → ≥ 3 (multiple skip-if-no-data points)
    - `grep "Strategy A" sources/frontend/e2e/smoke.spec.ts` → matches (reference to Wave 0 doc)
    - `cd sources/frontend && npx playwright test e2e/smoke.spec.ts --list 2>&1 | grep -c "SMOKE"` → ≥ 4 (4 tests total)
    - `cd sources/frontend && npm run build 2>&1 | tail -5 | grep -c "error\|Error"` → output `0` (no TS error from spec)
  </acceptance_criteria>

  <verify>
    <automated>cd sources/frontend && npx playwright test e2e/smoke.spec.ts --list 2>&1 | tail -20</automated>
  </verify>

  <done>4 tests SMOKE-1..4 list được bằng `--list`, no syntax error, skip-if-no-data degradation wired theo Strategy A.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human checkpoint — Run smoke E2E trên fresh docker stack</name>

  <what-built>
    - 4 Playwright smoke tests (SMOKE-1 anon homepage, SMOKE-2 checkout, SMOKE-3 review, SMOKE-4 profile)
    - Skip-if-no-data degradation cho SMOKE-2 + SMOKE-3 (Strategy A)
    - Reuse storageState từ global-setup (KHÔNG re-login)
  </what-built>

  <how-to-verify>
1. **Start fresh docker stack** từ project root:
   ```bash
   docker compose down -v  # clean previous data
   docker compose up -d --build  # fresh stack
   # Wait ~60s cho services healthy
   docker compose ps  # verify tất cả services up + healthy
   ```

2. **Run baseline tests** (đảm bảo regression KHÔNG break):
   ```bash
   cd sources/frontend
   npx playwright test --reporter=list
   ```
   - ✅ EXPECT: Toàn bộ existing tests (auth, admin-products/orders/users, order-detail, password-change) 100% PASS.
   - ❌ Nếu fail: investigate trước khi proceed (có thể Plan 15-01/15-02 break gì đó).

3. **Run smoke tests riêng**:
   ```bash
   cd sources/frontend
   npx playwright test e2e/smoke.spec.ts --reporter=list
   ```
   - ✅ EXPECT: 4/4 PASS hoặc PASS-with-skip (skip có reason rõ ràng).
   - Acceptable skip cases:
     - SMOKE-2: skip nếu user không có saved address trong seed
     - SMOKE-3: skip nếu user không có DELIVERED order (verified-buyer fail) — Strategy A intentional
   - ❌ Hard fail: SMOKE-1 (homepage anon) hoặc SMOKE-4 (profile editing) — 2 tests này KHÔNG có lý do skip hợp lệ.

4. **Report results** trong resume signal:
   - Số tests PASS / SKIP / FAIL
   - Nếu skip: paste skip reason text từ output
   - Nếu fail: paste error trace + selector/screenshot

5. **Tag annotated nhanh check** (KHÔNG run yet, chỉ verify command):
   ```bash
   git tag --list  # verify v1.2 chưa tồn tại (Plan 15-04 sẽ tag)
   ```
  </how-to-verify>

  <resume-signal>Type "approved" nếu 4/4 PASS-or-acceptable-skip + baseline 100% PASS. Hoặc paste failure details nếu cần fix.</resume-signal>
</task>

</tasks>

<verification>
- `cd sources/frontend && npm run build` exit 0
- `npx playwright test e2e/smoke.spec.ts --list` parse OK (4 tests)
- Human checkpoint Task 3: 4/4 PASS-or-acceptable-skip trên fresh docker stack
- Baseline regression 100% PASS
</verification>

<success_criteria>
- TEST-02 acceptance criteria met (4 critical paths covered)
- Smoke spec PASS criteria D-19 satisfied (4/4 PASS hoặc PASS-with-skip có reason)
- Baseline regression KHÔNG break
- Selectors verified bền vững cho re-run
</success_criteria>

<output>
Tạo `.planning/phases/15-public-polish-milestone-audit/15-03-SUMMARY.md` ghi:
- 4 test results (PASS/SKIP/FAIL với reason)
- ReviewSection selectors thực tế tìm được vs predictions (update SELECTOR-AUDIT nếu khác)
- Baseline test count + status
- Decision unblock cho Wave 4 milestone audit (cần Wave 3 PASS hoặc acceptable-skip-documented)
</output>
