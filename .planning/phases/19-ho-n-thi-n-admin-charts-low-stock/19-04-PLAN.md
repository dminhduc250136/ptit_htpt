---
phase: 19
plan: 04
type: execute
wave: 2
depends_on: [19-01, 19-02, 19-03]
files_modified:
  - sources/frontend/package.json
  - sources/frontend/package-lock.json
  - sources/frontend/src/services/charts.ts
  - sources/frontend/src/lib/chartFormat.ts
  - sources/frontend/src/components/admin/ChartCard.tsx
  - sources/frontend/src/components/admin/ChartCard.module.css
  - sources/frontend/src/components/admin/RevenueChart.tsx
  - sources/frontend/src/components/admin/TopProductsChart.tsx
  - sources/frontend/src/components/admin/StatusDistributionChart.tsx
  - sources/frontend/src/components/admin/UserSignupsChart.tsx
  - sources/frontend/src/components/admin/LowStockSection.tsx
  - sources/frontend/src/app/admin/page.tsx
  - sources/frontend/src/app/admin/page.module.css
  - sources/frontend/e2e/admin-charts.spec.ts
  - sources/frontend/e2e/admin-low-stock.spec.ts
autonomous: true
requirements: [ADMIN-01, ADMIN-02, ADMIN-03, ADMIN-04, ADMIN-05]
tags: [admin, charts, frontend, recharts, dashboard, vietnamese]

must_haves:
  truths:
    - "Admin dashboard render 4 charts (Revenue/TopProducts/StatusPie/UserSignups) trong grid 2x2 sau KPI cards"
    - "Time-window dropdown (Tất cả / 7 ngày / 30 ngày / 90 ngày) ở giữa KPI và charts grid; default = 30 ngày; thay đổi → refetch 3 charts (revenue/top-products/signups), pie giữ snapshot"
    - "Low-stock section full-width ở cuối dashboard, mỗi row có thumbnail + name + brand + stock badge (đỏ <5 / cam 5–9) + nút Sửa; click row → /admin/products?highlight={id}"
    - "Mỗi chart card có 3 state: loading skeleton 250px / success render Recharts SVG / error '--' + retry ⟳"
    - "Tất cả label/tooltip tiếng Việt; số định dạng Intl.NumberFormat('vi-VN'), revenue thêm '₫'; date axis định dạng DD/MM"
    - "Status pie semantic colors: PENDING=#f59e0b CONFIRMED=#3b82f6 SHIPPED=#06b6d4 DELIVERED=#10b981 CANCELLED=#dc2626"
    - "Mobile breakpoint <768px: charts grid → 1 cột"
    - "Empty states tiếng Việt theo D-15: revenue/signups 'Chưa có dữ liệu', top-products 'Chưa có sản phẩm bán ra', status 'Chưa có đơn hàng nào', low-stock 'Tất cả sản phẩm đủ hàng ✓'"
  artifacts:
    - path: "sources/frontend/package.json"
      provides: "+recharts@3.8.1 dep"
      contains: "recharts"
    - path: "sources/frontend/src/services/charts.ts"
      provides: "5 typed chart fetchers (mirror stats.ts)"
      contains: "fetchRevenueChart"
    - path: "sources/frontend/src/lib/chartFormat.ts"
      provides: "vnNumber / vnDate / statusLabel / STATUS_COLORS helpers"
      contains: "STATUS_COLORS"
    - path: "sources/frontend/src/components/admin/ChartCard.tsx"
      provides: "Generic 3-state wrapper component"
      contains: "ChartCard"
    - path: "sources/frontend/src/components/admin/RevenueChart.tsx"
      provides: "Recharts LineChart cho revenue"
      contains: "LineChart"
    - path: "sources/frontend/src/components/admin/TopProductsChart.tsx"
      provides: "Recharts BarChart horizontal cho top-10"
      contains: "BarChart"
    - path: "sources/frontend/src/components/admin/StatusDistributionChart.tsx"
      provides: "Recharts PieChart với per-slice Cell colors"
      contains: "PieChart"
    - path: "sources/frontend/src/components/admin/UserSignupsChart.tsx"
      provides: "Recharts LineChart cho signups"
      contains: "LineChart"
    - path: "sources/frontend/src/components/admin/LowStockSection.tsx"
      provides: "Card list với thumbnail + badge + nav"
      contains: "LowStockSection"
    - path: "sources/frontend/src/app/admin/page.tsx"
      provides: "Extended AdminDashboard: dropdown + 5 fetches + 2x2 grid + low-stock"
      contains: "Promise.allSettled"
    - path: "sources/frontend/e2e/admin-charts.spec.ts"
      provides: "Playwright smoke: dropdown change + chart SVG render"
      contains: "admin charts"
    - path: "sources/frontend/e2e/admin-low-stock.spec.ts"
      provides: "Playwright smoke: low-stock list render + click nav"
      contains: "low-stock"
  key_links:
    - from: "AdminDashboard /admin/page.tsx"
      to: "services/charts.ts fetchers"
      via: "useCallback loaders + Promise.allSettled trong useEffect"
      pattern: "Promise\\.allSettled"
    - from: "RevenueChart / UserSignupsChart / TopProductsChart"
      to: "lib/chartFormat.ts {vnNumber, vnDate}"
      via: "import + tickFormatter / tooltip formatter"
      pattern: "vnNumber|vnDate"
    - from: "StatusDistributionChart"
      to: "lib/chartFormat.ts {STATUS_COLORS, statusLabel}"
      via: "Cell fill = STATUS_COLORS[status]"
      pattern: "STATUS_COLORS"
    - from: "LowStockSection row click"
      to: "/admin/products?highlight={id}"
      via: "router.push (Next navigation)"
      pattern: "/admin/products\\?highlight"
    - from: "Time-window dropdown onChange"
      to: "5 chart loaders"
      via: "setRange → useCallback deps include range → useEffect refire"
      pattern: "setRange"
---

<objective>
Hoàn tất FE Phase 19: install recharts, tạo chart fetchers + chartFormat helpers, build 4 chart components + ChartCard wrapper + LowStockSection, extend admin/page.tsx với time-window dropdown + 2x2 grid + low-stock full-width, viết 2 Playwright smoke specs.

Purpose: 5 endpoints BE Plans 01–03 đã sẵn sàng → FE consume qua gateway routes hiện có. Plan này là Wave 2 vì cần BE endpoints functional để Playwright smoke đi xuyên suốt. Dropdown UX duy nhất (D-06) điều khiển 3 charts có time-window; pie giữ snapshot. Visible-first priority: end-user (admin) thấy ngay 4 charts thực + low-stock alert.

Output: 1 npm dep + 2 lib/service files + 6 components + 1 page extension + 2 Playwright specs. Sau plan này phase 19 hoàn tất.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-ho-n-thi-n-admin-charts-low-stock/19-CONTEXT.md
@.planning/phases/19-ho-n-thi-n-admin-charts-low-stock/19-RESEARCH.md
@.planning/phases/19-ho-n-thi-n-admin-charts-low-stock/19-PATTERNS.md

# Wave 1 prior plan summaries — chart endpoint contracts
@.planning/phases/19-ho-n-thi-n-admin-charts-low-stock/19-01-SUMMARY.md
@.planning/phases/19-ho-n-thi-n-admin-charts-low-stock/19-02-SUMMARY.md
@.planning/phases/19-ho-n-thi-n-admin-charts-low-stock/19-03-SUMMARY.md

# Analog files (FE)
@sources/frontend/src/app/admin/page.tsx
@sources/frontend/src/app/admin/page.module.css
@sources/frontend/src/services/stats.ts
@sources/frontend/src/services/http.ts

<interfaces>
<!-- Wave 1 BE endpoints (contracts FE will consume) -->

GET /api/orders/admin/charts/revenue?range=7d|30d|90d|all
  → 200 ApiResponse<RevenuePoint[]>
  → RevenuePoint: { date: string (ISO yyyy-MM-dd), value: number (BigDecimal serialized) }

GET /api/orders/admin/charts/top-products?range=7d|30d|90d|all
  → 200 ApiResponse<TopProductPoint[]> (length ≤ 10, sorted DESC qtySold)
  → TopProductPoint: { productId, name, brand: string|null, thumbnailUrl: string|null, qtySold: number }

GET /api/orders/admin/charts/status-distribution
  → 200 ApiResponse<StatusPoint[]>
  → StatusPoint: { status: 'PENDING'|'CONFIRMED'|'SHIPPED'|'DELIVERED'|'CANCELLED', count: number }

GET /api/users/admin/charts/signups?range=7d|30d|90d|all
  → 200 ApiResponse<SignupPoint[]>
  → SignupPoint: { date: string (ISO), count: number }

GET /api/products/admin/charts/low-stock
  → 200 ApiResponse<LowStockItem[]> (sorted ASC stock, cap 50)
  → LowStockItem: { id, name, brand: string|null, thumbnailUrl: string|null, stock: number }

<!-- Existing FE primitives -->

services/http.ts httpGet<T>(path):
  - Auto Bearer attach từ localStorage token
  - Auto unwrap ApiResponse envelope (returns parsed.data trực tiếp)
  - Throw ApiError on non-2xx

app/admin/page.tsx existing pattern (lines 22–60, 103–139):
  - 'use client' component
  - useState<CardState<T>> per card
  - useCallback loader per card
  - Promise.allSettled([loaders]) trong useEffect
  - KpiCard<T> generic 3-state component
  - type CardState<T> = { status: 'loading'|'success'|'error'; data?: T; error?: string }

CSS vars sẵn có: --primary, --secondary, --on-surface, --on-surface-variant, --surface, --surface-container-lowest, --surface-variant, --outline, --radius-xl, --radius-md, --radius-sm, --space-3, --space-4, --text-title-md, --text-body-sm, --weight-bold, --weight-medium
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install recharts + create services/charts.ts + lib/chartFormat.ts + ChartCard wrapper component</name>
  <files>
    sources/frontend/package.json,
    sources/frontend/package-lock.json,
    sources/frontend/src/services/charts.ts,
    sources/frontend/src/lib/chartFormat.ts,
    sources/frontend/src/components/admin/ChartCard.tsx,
    sources/frontend/src/components/admin/ChartCard.module.css
  </files>

  <read_first>
    - sources/frontend/src/services/stats.ts (toàn bộ — analog exact cho 5 chart fetchers; mirror import + signature pattern)
    - sources/frontend/src/services/http.ts (line 144 `httpGet` + line 108 envelope unwrap — verify behavior)
    - sources/frontend/src/app/admin/page.tsx (lines 103–139 `KpiCard<T>` generic — analog cho ChartCard)
    - sources/frontend/src/app/admin/page.module.css (lines 13–32 skeleton/error styles + animation `shimmer` — REUSE concepts trong ChartCard.module.css)
    - 19-RESEARCH.md §FE Pattern 5 (lines 597–653) — ChartCard.tsx + ChartCard.module.css verbatim với min-height 250px (Pitfall #1)
    - 19-RESEARCH.md §FE Pattern 6 (lines 657–673) — services/charts.ts verbatim
    - 19-RESEARCH.md §FE Pattern 3 (lines 532–554) — chartFormat.ts STATUS_COLORS + statusLabel + vnNumber + vnDate verbatim
    - 19-CONTEXT.md D-11 (recharts 3.8.1), D-12 (color palette), D-13 (Vietnamese formatters), D-14 (3-state ChartCard wrapper)
  </read_first>

  <action>
    1. **Install recharts:** `cd sources/frontend && npm install recharts@3.8.1` (D-11). Verify `package.json` `dependencies.recharts === "3.8.1"` (KHÔNG semver caret để khớp lock; nếu npm tự thêm `^`, edit thủ công về `"3.8.1"` để match D-11 lock chính xác). Run `npm install` lại để regenerate `package-lock.json`.

    2. **Tạo `sources/frontend/src/services/charts.ts`** copy verbatim RESEARCH §FE Pattern 6 lines 657–673:

       ```ts
       import { httpGet } from './http';

       export interface RevenuePoint    { date: string; value: number }
       export interface TopProductPoint { productId: string; name: string; brand: string|null; thumbnailUrl: string|null; qtySold: number }
       export interface StatusPoint     { status: string; count: number }
       export interface SignupPoint     { date: string; count: number }
       export interface LowStockItem    { id: string; name: string; brand: string|null; thumbnailUrl: string|null; stock: number }

       export type Range = '7d' | '30d' | '90d' | 'all';

       export const fetchRevenueChart  = (range: Range) => httpGet<RevenuePoint[]>(`/api/orders/admin/charts/revenue?range=${range}`);
       export const fetchTopProducts   = (range: Range) => httpGet<TopProductPoint[]>(`/api/orders/admin/charts/top-products?range=${range}`);
       export const fetchStatusDistrib = ()             => httpGet<StatusPoint[]>(`/api/orders/admin/charts/status-distribution`);
       export const fetchUserSignups   = (range: Range) => httpGet<SignupPoint[]>(`/api/users/admin/charts/signups?range=${range}`);
       export const fetchLowStock      = ()             => httpGet<LowStockItem[]>(`/api/products/admin/charts/low-stock`);
       ```

       `httpGet` tự attach Bearer + unwrap ApiResponse envelope.

    3. **Tạo `sources/frontend/src/lib/chartFormat.ts`** copy verbatim RESEARCH §FE Pattern 3 lines 532–554:

       ```ts
       export const STATUS_COLORS: Record<string, string> = {
         PENDING:   '#f59e0b',  // D-12
         CONFIRMED: '#3b82f6',
         SHIPPED:   '#06b6d4',
         DELIVERED: '#10b981',
         CANCELLED: '#dc2626',
       };

       export function statusLabel(s: string): string {
         return ({
           PENDING:   'Chờ xử lý',     // D-13
           CONFIRMED: 'Đã xác nhận',
           SHIPPED:   'Đang giao',
           DELIVERED: 'Đã giao',
           CANCELLED: 'Đã huỷ',
         } as Record<string, string>)[s] ?? s;
       }

       export const vnNumber = (n: number) => new Intl.NumberFormat('vi-VN').format(n);

       export const vnDate = (iso: string) => {
         const d = new Date(iso);
         return new Intl.DateTimeFormat('vi-VN', { day: '2-digit', month: '2-digit' }).format(d);
       };
       ```

    4. **Tạo `sources/frontend/src/components/admin/ChartCard.tsx`** copy verbatim RESEARCH §FE Pattern 5 lines 599–631:

       ```tsx
       'use client';
       import { ReactNode } from 'react';
       import styles from './ChartCard.module.css';

       export type CardState<T> = { status: 'loading' | 'success' | 'error'; data?: T; error?: string };

       interface Props<T> {
         title: string;
         state: CardState<T>;
         renderChart: (data: T) => ReactNode;
         onRetry: () => void;
       }

       export function ChartCard<T>({ title, state, renderChart, onRetry }: Props<T>) {
         return (
           <div className={styles.chartCard}>
             <h3 className={styles.chartTitle}>{title}</h3>
             <div className={styles.chartBody}>
               {state.status === 'loading' && <div className={styles.skeleton} aria-label="Đang tải biểu đồ" />}
               {state.status === 'success' && state.data !== undefined && renderChart(state.data)}
               {state.status === 'error' && (
                 <div className={styles.errorRow}>
                   <span style={{ color: 'var(--on-surface-variant)' }}>--</span>
                   <button type="button" onClick={onRetry} aria-label={`Tải lại ${title}`} title={state.error}>⟳</button>
                 </div>
               )}
             </div>
           </div>
         );
       }
       ```

       **Export `CardState<T>` named export** để page.tsx + chart components reuse cùng type.

    5. **Tạo `sources/frontend/src/components/admin/ChartCard.module.css`** copy verbatim RESEARCH §FE Pattern 5 lines 633–653 — CRITICAL `chartBody { min-height: 250px }` (Pitfall #1: ResponsiveContainer collapse 0px nếu parent không có height).

       ```css
       .chartCard {
         background: var(--surface-container-lowest);
         border-radius: var(--radius-xl);
         padding: var(--space-4);
         display: flex;
         flex-direction: column;
         gap: var(--space-3);
       }
       .chartTitle { font-size: var(--text-title-md); font-weight: var(--weight-bold); }
       .chartBody { min-height: 250px; }
       .skeleton {
         height: 250px;
         background: linear-gradient(90deg, var(--surface-variant) 0%, var(--surface) 50%, var(--surface-variant) 100%);
         background-size: 200% 100%;
         animation: shimmer 1.4s infinite;
         border-radius: var(--radius-md);
       }
       .errorRow { display: flex; align-items: center; gap: var(--space-3); }
       @keyframes shimmer { 0% { background-position: 200% 0; } 100% { background-position: -200% 0; } }
       ```
  </action>

  <verify>
    <automated>cd sources/frontend && npx tsc --noEmit && npm run lint</automated>
  </verify>

  <acceptance_criteria>
    - File tồn tại: `sources/frontend/src/services/charts.ts`
    - File tồn tại: `sources/frontend/src/lib/chartFormat.ts`
    - File tồn tại: `sources/frontend/src/components/admin/ChartCard.tsx`
    - File tồn tại: `sources/frontend/src/components/admin/ChartCard.module.css`
    - Grep `"recharts": "3.8.1"` trong `package.json` returns 1 match
    - Grep `fetchRevenueChart|fetchTopProducts|fetchStatusDistrib|fetchUserSignups|fetchLowStock` trong charts.ts returns 5 matches
    - Grep `STATUS_COLORS` trong chartFormat.ts returns 1 match (export)
    - Grep `Intl\.NumberFormat\('vi-VN'\)` trong chartFormat.ts returns 1 match
    - Grep `Intl\.DateTimeFormat\('vi-VN'` trong chartFormat.ts returns 1 match
    - Grep `min-height: 250px` trong ChartCard.module.css returns 1 match
    - Grep `'Chờ xử lý'|'Đã xác nhận'|'Đang giao'|'Đã giao'|'Đã huỷ'` trong chartFormat.ts returns 5 matches
    - tsc + lint exit 0
  </acceptance_criteria>

  <done>
    Recharts installed (lock 3.8.1), 5 typed fetchers + 4 helper exports + 1 generic ChartCard component sẵn sàng cho 4 chart components consume ở Task 2. Tsc + lint clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: 4 chart components (Revenue/TopProducts/StatusDistribution/UserSignups) + LowStockSection</name>
  <files>
    sources/frontend/src/components/admin/RevenueChart.tsx,
    sources/frontend/src/components/admin/TopProductsChart.tsx,
    sources/frontend/src/components/admin/StatusDistributionChart.tsx,
    sources/frontend/src/components/admin/UserSignupsChart.tsx,
    sources/frontend/src/components/admin/LowStockSection.tsx
  </files>

  <read_first>
    - 19-RESEARCH.md §FE Pattern 1 (lines 442–474) — RevenueChart LineChart verbatim (cũng dùng cho UserSignupsChart adaptation)
    - 19-RESEARCH.md §FE Pattern 2 (lines 478–502) — TopProductsChart BarChart vertical layout verbatim
    - 19-RESEARCH.md §FE Pattern 3 (lines 506–530) — StatusDistributionChart PieChart + Cell verbatim
    - 19-RESEARCH.md §FE Pattern 4 (lines 562–595) — LowStockSection verbatim
    - 19-RESEARCH.md Pitfall #1 (RechartsResponsiveContainer cần parent height — đã xử lý ở ChartCard.module.css)
    - 19-RESEARCH.md Pitfall #2 (Cell deprecated 3.7+ nhưng vẫn hoạt động 3.8.1 — chấp nhận warning)
    - 19-CONTEXT.md D-10 (low-stock row format), D-12 (colors), D-13 (Vietnamese), D-15 (empty states per chart)
    - sources/frontend/src/services/charts.ts (vừa tạo Task 1 — import types từ đây)
    - sources/frontend/src/lib/chartFormat.ts (vừa tạo Task 1 — import helpers từ đây)
  </read_first>

  <action>
    1. **`RevenueChart.tsx`** copy verbatim RESEARCH §FE Pattern 1 lines 444–473. Imports: `recharts` + `vnNumber, vnDate` từ `@/lib/chartFormat`. Type import `RevenuePoint` từ `@/services/charts`. Empty state (D-15): `data.length === 0` → `<p>Chưa có dữ liệu trong khoảng này</p>`. Line stroke `var(--primary)` (D-12). XAxis `tickFormatter={(iso) => vnDate(iso)}`. YAxis `tickFormatter={(n) => vnNumber(n)}`. Tooltip formatter `(value: number) => \`${vnNumber(value)} ₫\`` + labelFormatter `(iso: string) => vnDate(iso)`.

    2. **`TopProductsChart.tsx`** copy verbatim RESEARCH §FE Pattern 2 lines 480–501. Layout `vertical`, Bar fill `var(--secondary)` (D-12). Truncate name >20 chars với '…'. Empty state (D-15): `<p>Chưa có sản phẩm bán ra trong khoảng này</p>`. Tooltip `(v: number) => \`${vnNumber(v)} sản phẩm\``. Type import `TopProductPoint` từ `@/services/charts`.

    3. **`StatusDistributionChart.tsx`** copy verbatim RESEARCH §FE Pattern 3 lines 508–530. Imports: `PieChart, Pie, Tooltip, Legend, ResponsiveContainer, Cell` từ `recharts`; `statusLabel, STATUS_COLORS` từ `@/lib/chartFormat`. Display map: `{ name: statusLabel(d.status), value: d.count, status: d.status }`. Cell `fill={STATUS_COLORS[entry.status] ?? 'var(--outline)'}` per slice (D-12 semantic colors). Empty state (D-15): `<p>Chưa có đơn hàng nào</p>`. Tooltip `(v: number) => \`${v} đơn\``. Legend `wrapperStyle={{ fontSize: 12 }}`. Type import `StatusPoint` từ `@/services/charts`.

       **Note Pitfall #2:** Cell deprecated từ 3.7 nhưng 3.8.1 vẫn hoạt động đúng (chỉ console warning). Accept warning, KHÔNG migrate sang `shape` callback ở phase này (Claude's Discretion từ CONTEXT).

    4. **`UserSignupsChart.tsx`** mirror RevenueChart structure nhưng:
       - Import `SignupPoint` từ `@/services/charts`.
       - Data key: `value` thay bằng `count` (BE trả `{date, count}` shape).
       - Line stroke: `#f59e0b` (D-12 cam, khớp KPI customer card).
       - Tooltip formatter: `(value: number) => \`${vnNumber(value)} người\`` + labelFormatter `vnDate(iso)`.
       - YAxis `tickFormatter={(n) => vnNumber(n)}`.
       - Empty state (D-15): `<p>Chưa có dữ liệu trong khoảng này</p>` (cùng wording revenue).

    5. **`LowStockSection.tsx`** copy verbatim RESEARCH §FE Pattern 4 lines 564–595. Empty state (D-15): `<p>Tất cả sản phẩm đủ hàng ✓</p>`. Mỗi row: thumbnail 40x40 + name + brand subtitle (nếu có) + stock badge (đỏ `#dc2626` nếu `<5`, cam `#f59e0b` nếu 5–9, format "Còn N") + nút "Sửa". Click row hoặc nút → `router.push(\`/admin/products?highlight=${item.id}\`)` (D-10). Stop propagation cho nút Sửa để tránh double-fire. Type import `LowStockItem` từ `@/services/charts`. Use `next/navigation` `useRouter`. Component phải `'use client'`.
  </action>

  <verify>
    <automated>cd sources/frontend && npx tsc --noEmit && npm run lint</automated>
  </verify>

  <acceptance_criteria>
    - 5 files tồn tại trong `sources/frontend/src/components/admin/`: RevenueChart.tsx, TopProductsChart.tsx, StatusDistributionChart.tsx, UserSignupsChart.tsx, LowStockSection.tsx
    - Grep `from 'recharts'` trong components/admin/ trả ≥4 matches
    - Grep `'use client'` trong cả 5 components trả 5 matches
    - Grep `var\(--primary\)` trong RevenueChart.tsx returns 1 match (Line stroke)
    - Grep `var\(--secondary\)` trong TopProductsChart.tsx returns 1 match (Bar fill)
    - Grep `#f59e0b` trong UserSignupsChart.tsx returns 1 match (Line stroke)
    - Grep `STATUS_COLORS\[` trong StatusDistributionChart.tsx returns 1 match
    - Grep `'/admin/products\?highlight='` trong LowStockSection.tsx returns ≥1 match (router.push)
    - Grep `Còn ` trong LowStockSection.tsx returns 1 match (badge label)
    - Grep `'Chưa có dữ liệu trong khoảng này'` returns 2 matches (Revenue + UserSignups)
    - Grep `'Tất cả sản phẩm đủ hàng ✓'` trong LowStockSection.tsx returns 1 match
    - tsc + lint exit 0
  </acceptance_criteria>

  <done>
    4 Recharts components + LowStockSection render đúng theo D-12/13/15. Tsc + lint clean. Components ready cho page.tsx wire ở Task 3.
  </done>
</task>

<task type="auto">
  <name>Task 3: Extend admin/page.tsx với time-window dropdown + 5 fetchers + 2x2 charts grid + low-stock + CSS extension + 2 Playwright smoke specs</name>
  <files>
    sources/frontend/src/app/admin/page.tsx,
    sources/frontend/src/app/admin/page.module.css,
    sources/frontend/e2e/admin-charts.spec.ts,
    sources/frontend/e2e/admin-low-stock.spec.ts
  </files>

  <read_first>
    - sources/frontend/src/app/admin/page.tsx (toàn bộ — extend, KHÔNG rewrite. Lines 22–60 component + loaders + Promise.allSettled, lines 103–139 KpiCard analog. Đọc kỹ existing structure để insert đúng vị trí D-07: KPI cards row → dropdown → charts grid → low-stock)
    - sources/frontend/src/app/admin/page.module.css (toàn bộ — extend với `.timeWindowRow`, `.chartsGrid`, `.lowStockCard`, mobile breakpoint)
    - sources/frontend/e2e/ (xem existing specs để mirror Playwright pattern: admin login + navigation + assertions). Tìm spec login admin sẵn có nhất để copy auth setup.
    - 19-RESEARCH.md §FE Pattern (lines 374–419) — extension pattern + Pitfall #5 (useCallback deps include `range`, KHÔNG include other loader functions)
    - 19-PATTERNS.md §`frontend/src/app/admin/page.tsx` (lines 374–404) — extension blueprint
    - 19-CONTEXT.md D-06 (1 dropdown global điều khiển 3 charts có range, pie KHÔNG bị ảnh hưởng), D-07 (layout dọc), D-13 (Vietnamese label "Khoảng thời gian:"), D-14 (Promise.allSettled + per-chart state)
  </read_first>

  <action>
    1. **Extend `sources/frontend/src/app/admin/page.tsx`** — thêm vào AdminDashboard component (KHÔNG break existing KPI logic):

       a. Imports thêm:
       ```tsx
       import { useState, useCallback, useEffect } from 'react';  // bổ sung useState/useCallback nếu chưa có
       import { ChartCard, type CardState } from '@/components/admin/ChartCard';
       import { RevenueChart } from '@/components/admin/RevenueChart';
       import { TopProductsChart } from '@/components/admin/TopProductsChart';
       import { StatusDistributionChart } from '@/components/admin/StatusDistributionChart';
       import { UserSignupsChart } from '@/components/admin/UserSignupsChart';
       import { LowStockSection } from '@/components/admin/LowStockSection';
       import {
         fetchRevenueChart, fetchTopProducts, fetchStatusDistrib, fetchUserSignups, fetchLowStock,
         type Range, type RevenuePoint, type TopProductPoint, type StatusPoint, type SignupPoint, type LowStockItem,
       } from '@/services/charts';
       ```

       b. State + loaders (mirror existing KPI pattern; 5 cards mới):
       ```tsx
       const [range, setRange] = useState<Range>('30d');  // D-06 default
       const [revenueCard, setRevenueCard]     = useState<CardState<RevenuePoint[]>>({ status: 'loading' });
       const [topProductsCard, setTopProducts] = useState<CardState<TopProductPoint[]>>({ status: 'loading' });
       const [statusCard, setStatusCard]       = useState<CardState<StatusPoint[]>>({ status: 'loading' });
       const [signupsCard, setSignupsCard]     = useState<CardState<SignupPoint[]>>({ status: 'loading' });
       const [lowStockCard, setLowStockCard]   = useState<CardState<LowStockItem[]>>({ status: 'loading' });

       const loadRevenue = useCallback(async () => {
         setRevenueCard({ status: 'loading' });
         try { setRevenueCard({ status: 'success', data: await fetchRevenueChart(range) }); }
         catch (e) { setRevenueCard({ status: 'error', error: (e as Error).message ?? 'Không tải được' }); }
       }, [range]);  // D-06 + Pitfall #5: KHÔNG include loaders khác
       ```
       Lặp tương tự cho `loadTopProducts` (deps `[range]`), `loadStatus` (deps `[]` — pie KHÔNG bị range D-06), `loadSignups` (deps `[range]`), `loadLowStock` (deps `[]`).

       c. Orchestration useEffect:
       ```tsx
       useEffect(() => {
         Promise.allSettled([loadRevenue(), loadTopProducts(), loadStatus(), loadSignups(), loadLowStock()]);
       }, [loadRevenue, loadTopProducts, loadStatus, loadSignups, loadLowStock]);
       ```

       d. JSX extension (D-07 layout) — chèn SAU KPI cards row, TRƯỚC bất kỳ closing element nào:
       ```tsx
       {/* D-06 + D-07: time-window dropdown */}
       <div className={styles.timeWindowRow}>
         <label htmlFor="time-window">Khoảng thời gian:</label>
         <select
           id="time-window"
           value={range}
           onChange={(e) => setRange(e.target.value as Range)}
         >
           <option value="7d">7 ngày</option>
           <option value="30d">30 ngày</option>
           <option value="90d">90 ngày</option>
           <option value="all">Tất cả</option>
         </select>
       </div>

       {/* D-07: 2x2 charts grid */}
       <div className={styles.chartsGrid}>
         <ChartCard title="Doanh thu" state={revenueCard} renderChart={(d) => <RevenueChart data={d} />} onRetry={loadRevenue} />
         <ChartCard title="Sản phẩm bán chạy" state={topProductsCard} renderChart={(d) => <TopProductsChart data={d} />} onRetry={loadTopProducts} />
         <ChartCard title="Phân phối trạng thái" state={statusCard} renderChart={(d) => <StatusDistributionChart data={d} />} onRetry={loadStatus} />
         <ChartCard title="Khách hàng đăng ký" state={signupsCard} renderChart={(d) => <UserSignupsChart data={d} />} onRetry={loadSignups} />
       </div>

       {/* D-07: low-stock full-width cuối */}
       <ChartCard title="Sản phẩm sắp hết hàng" state={lowStockCard} renderChart={(d) => <LowStockSection data={d} />} onRetry={loadLowStock} />
       ```

    2. **Extend `sources/frontend/src/app/admin/page.module.css`**: thêm cuối file (analog `.statsGrid` line 5 + `@media` line 34):
       ```css
       .timeWindowRow {
         display: flex;
         align-items: center;
         gap: var(--space-3);
         margin: var(--space-4) 0;
       }
       .timeWindowRow label {
         font-weight: var(--weight-medium);
       }
       .timeWindowRow select {
         padding: var(--space-2) var(--space-3);
         border: 1px solid var(--outline);
         border-radius: var(--radius-md);
         background: var(--surface);
       }
       .chartsGrid {
         display: grid;
         grid-template-columns: repeat(2, 1fr);
         gap: var(--space-4);
         margin-bottom: var(--space-4);
       }
       @media (max-width: 768px) {
         .chartsGrid { grid-template-columns: 1fr; }
       }
       ```

       (Nếu `--space-2` chưa tồn tại trong CSS vars, dùng `8px` literal.)

    3. **Tạo `sources/frontend/e2e/admin-charts.spec.ts`** — Playwright smoke. Pattern:
       - Login admin (mirror existing admin spec auth setup)
       - Navigate `/admin`
       - Assert KPI cards row vẫn render (không bị break)
       - Assert dropdown `[id="time-window"]` visible với 4 options
       - Wait cho 4 charts render: `expect(page.locator('.chartCard')).toHaveCount(5)` (4 charts + 1 low-stock card cũng dùng ChartCard wrapper) — nếu count khác do markup variation, dùng text-based: `await expect(page.getByText('Doanh thu')).toBeVisible()`, `await expect(page.getByText('Sản phẩm bán chạy')).toBeVisible()`, `await expect(page.getByText('Phân phối trạng thái')).toBeVisible()`, `await expect(page.getByText('Khách hàng đăng ký')).toBeVisible()`.
       - Wait cho ít nhất 1 chart `<svg>` xuất hiện trong charts grid (Recharts render SVG): `await expect(page.locator('.chartsGrid svg').first()).toBeVisible({ timeout: 10000 })`
       - Đổi dropdown sang `7d`: `await page.locator('#time-window').selectOption('7d')` → wait short timeout 2s → assert `<svg>` vẫn render (refetch không crash)

    4. **Tạo `sources/frontend/e2e/admin-low-stock.spec.ts`** — Playwright smoke:
       - Login admin + navigate `/admin`
       - Assert "Sản phẩm sắp hết hàng" title visible
       - Assert hoặc list rows `>0` HOẶC empty placeholder "Tất cả sản phẩm đủ hàng ✓" — accept either tùy seed data:
         ```ts
         const lowStockSection = page.locator('text=Sản phẩm sắp hết hàng').locator('..');
         const hasItems = await lowStockSection.locator('li').count() > 0;
         const hasEmpty = await page.getByText('Tất cả sản phẩm đủ hàng').isVisible().catch(() => false);
         expect(hasItems || hasEmpty).toBeTruthy();
         ```
       - Nếu có items, click row đầu tiên → assert URL contains `/admin/products?highlight=`
       - Nếu empty, skip click test (note trong comment).

       Both specs follow existing E2E auth pattern (đọc 1 spec hiện có để copy login setup).
  </action>

  <verify>
    <automated>cd sources/frontend && npx tsc --noEmit && npm run lint && npx playwright test e2e/admin-charts.spec.ts e2e/admin-low-stock.spec.ts</automated>
  </verify>

  <acceptance_criteria>
    - File `sources/frontend/e2e/admin-charts.spec.ts` tồn tại
    - File `sources/frontend/e2e/admin-low-stock.spec.ts` tồn tại
    - Grep `useState<Range>\('30d'\)` trong page.tsx returns 1 match (D-06 default)
    - Grep `Promise\.allSettled` trong page.tsx returns ≥1 match
    - Grep mỗi loader call riêng lẻ `loadRevenue\(\)`, `loadTopProducts\(\)`, `loadStatus\(\)`, `loadSignups\(\)`, `loadLowStock\(\)` đều returns ≥1 match (thứ tự không bắt buộc)
    - Grep `'Khoảng thời gian:'` trong page.tsx returns 1 match (D-13 dropdown label)
    - Grep `'7 ngày'` + `'30 ngày'` + `'90 ngày'` + `'Tất cả'` trong page.tsx returns 4 matches (dropdown options)
    - Grep `<ChartCard ` trong page.tsx returns 5 matches (4 charts + 1 low-stock)
    - Grep `'Doanh thu'|'Sản phẩm bán chạy'|'Phân phối trạng thái'|'Khách hàng đăng ký'|'Sản phẩm sắp hết hàng'` trong page.tsx returns 5 matches
    - Grep `\.chartsGrid` trong page.module.css returns 1 match
    - Grep `@media \(max-width: 768px\)` + `\.chartsGrid \{ grid-template-columns: 1fr` trong page.module.css returns mobile breakpoint
    - Grep `loadStatus = useCallback.*\[\]` (deps trống vì pie KHÔNG range D-06) — verify manual trong page.tsx
    - tsc + lint exit 0
    - Playwright 2 specs pass (cần backend Plans 01–03 đã chạy + seed data)
  </acceptance_criteria>

  <done>
    Admin dashboard hoàn chỉnh: KPI row → dropdown → 2x2 charts → low-stock. Dropdown đổi range refetch 3 charts (revenue/top-products/signups), pie + low-stock giữ nguyên (D-06). Mobile responsive. 2 Playwright smoke pass. Phase 19 ready cho /gsd-verify-work.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → admin page | Admin user trusted (gated bởi existing admin route guard) |
| admin/page.tsx → BE chart endpoints | JWT Bearer attached qua httpGet (services/http.ts) |
| LowStockSection click → /admin/products?highlight= | Open redirect risk nếu URL không sanitize |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-04-01 | Tampering | Range type cast `e.target.value as Range` | mitigate | Select chỉ có 4 hardcoded options (`7d|30d|90d|all`) — DOM tampering có thể inject other value, nhưng BE Plan 01/02 `Range.parse()` throw 400. FE error state catch + display "--" (D-14). |
| T-19-04-02 | Cross-Site Scripting | thumbnailUrl từ BE render trong `<img src>` | accept | Unsplash CDN URLs (Phase 16 SEED-03 verified). httpGet chỉ trả JSON, React tự escape khi set src. KHÔNG `dangerouslySetInnerHTML`. |
| T-19-04-03 | Cross-Site Scripting | name + brand text trong charts/low-stock | mitigate | React tự escape text content. KHÔNG dùng `dangerouslySetInnerHTML` ở bất kỳ component nào. |
| T-19-04-04 | Open Redirect | router.push(`/admin/products?highlight=${id}`) | mitigate | URL hardcoded prefix `/admin/products?highlight=`, chỉ append `id` từ BE-validated ProductEntity (UUID). KHÔNG accept user input vào URL. |
| T-19-04-05 | Information Disclosure | Bearer token trong localStorage | accept | Existing pattern (memory `feedback_priority.md` defer STORE-04 httpOnly cookie). XSS risk mitigated bởi React escaping + no eval. |
| T-19-04-06 | Information Disclosure | Error messages display | mitigate | ChartCard error state chỉ hiện "--" + retry icon; raw error message ở `title` attribute (tooltip on hover) — KHÔNG leak stack trace. |
| T-19-04-07 | Denial of Service | Recharts SVG render với dataset siêu lớn | accept | BE caps: top-products 10 rows, low-stock 50 rows, revenue/signups ≤ ~365 days (range=all). Recharts SVG handle <1k data points trivially. |
</threat_model>

<verification>
- Quick run (sau mỗi task): `cd sources/frontend && npx tsc --noEmit && npm run lint`
- Plan verification: `cd sources/frontend && npx tsc --noEmit && npm run lint && npx playwright test e2e/admin-charts.spec.ts e2e/admin-low-stock.spec.ts`
- Manual UAT (defer per phase policy nhưng smoke khi possible): start docker-compose, login admin, navigate `/admin`, verify visual:
  1. 4 KPI cards (existing)
  2. Dropdown "Khoảng thời gian:" với default "30 ngày"
  3. 4 chart cards trong 2x2 grid với SVG render
  4. Low-stock section full-width
  5. Đổi dropdown → 3 charts refetch (skeleton brief), pie KHÔNG reload
  6. Click row low-stock → navigate `/admin/products?highlight=...`
  7. Resize browser <768px → charts grid → 1 cột
- Update `19-VALIDATION.md` per-task table với 3 task IDs ở plan này (Wave 2, ADMIN-01..05, FE quick + Playwright smoke)
</verification>

<success_criteria>
- ROADMAP SC #1 (revenue chart 7d/30d/90d/all default 30d): satisfied via RevenueChart + dropdown
- ROADMAP SC #2 (top-10 bar chart): TopProductsChart
- ROADMAP SC #3 (status pie/donut): StatusDistributionChart
- ROADMAP SC #4 (signups line chart): UserSignupsChart
- ROADMAP SC #5 (low-stock list, click → edit): LowStockSection + router.push
- 2 Playwright smoke specs pass
- tsc + lint clean
- Mobile responsive (chartsGrid 1 cột <768px)
- All Vietnamese labels per D-13
</success_criteria>

<output>
Sau khi hoàn tất, tạo `.planning/phases/19-ho-n-thi-n-admin-charts-low-stock/19-04-SUMMARY.md` per template với: artifacts, key decisions ref (D-06..D-15), recharts version locked, Playwright results, screenshot path nếu có, ghi chú deferred (D-10 highlight query param chưa implement ở /admin/products — chỉ navigation).
</output>
