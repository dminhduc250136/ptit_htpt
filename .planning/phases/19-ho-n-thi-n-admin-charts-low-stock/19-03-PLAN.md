---
phase: 19
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - sources/backend/product-service/src/main/java/com/ptit/htpt/productservice/web/AdminChartsController.java
  - sources/backend/product-service/src/main/java/com/ptit/htpt/productservice/web/AdminProductBatchController.java
  - sources/backend/product-service/src/main/java/com/ptit/htpt/productservice/service/LowStockService.java
  - sources/backend/product-service/src/main/java/com/ptit/htpt/productservice/service/ProductBatchService.java
  - sources/backend/product-service/src/main/java/com/ptit/htpt/productservice/repository/ProductRepository.java
  - sources/backend/product-service/src/test/java/com/ptit/htpt/productservice/web/AdminChartsControllerIT.java
  - sources/backend/product-service/src/test/java/com/ptit/htpt/productservice/web/AdminProductBatchControllerIT.java
autonomous: true
requirements: [ADMIN-05]
tags: [admin, charts, product-svc, low-stock, batch, enrichment]

must_haves:
  truths:
    - "GET /admin/products/charts/low-stock trả về list SP có stock<10, sort ASC by stock, cap 50 rows"
    - "Empty stock<10 → trả [] (FE handle empty state)"
    - "POST /admin/products/batch nhận {ids:[uuid,...]} → trả [{id,name,brand,thumbnailUrl}] cho top-products enrichment"
    - "Cả 2 endpoint gate JwtRoleGuard.requireAdmin: missing/invalid Bearer → 401, non-admin → 403"
    - "LOW_STOCK_THRESHOLD = 10 hardcoded constant trong LowStockService (D-08, KHÔNG env var)"
  artifacts:
    - path: "sources/backend/product-service/src/main/java/com/ptit/htpt/productservice/web/AdminChartsController.java"
      provides: "1 endpoint GET /admin/products/charts/low-stock"
      contains: "@RequestMapping(\"/admin/products/charts\")"
    - path: "sources/backend/product-service/src/main/java/com/ptit/htpt/productservice/web/AdminProductBatchController.java"
      provides: "1 endpoint POST /admin/products/batch"
      contains: "@PostMapping(\"/batch\")"
    - path: "sources/backend/product-service/src/main/java/com/ptit/htpt/productservice/service/LowStockService.java"
      provides: "list() method + LOW_STOCK_THRESHOLD constant"
      contains: "LOW_STOCK_THRESHOLD = 10"
    - path: "sources/backend/product-service/src/main/java/com/ptit/htpt/productservice/service/ProductBatchService.java"
      provides: "findByIds(List<String>) method cho enrichment"
      contains: "findByIds"
    - path: "sources/backend/product-service/src/main/java/com/ptit/htpt/productservice/repository/ProductRepository.java"
      provides: "+findLowStock(threshold, Pageable cap)"
      contains: "findLowStock"
  key_links:
    - from: "AdminChartsController /low-stock"
      to: "LowStockService.list"
      via: "constructor inject + @Transactional readOnly"
      pattern: "lowStockService\\.list"
    - from: "AdminProductBatchController POST /batch"
      to: "ProductBatchService.findByIds"
      via: "constructor inject"
      pattern: "productBatchService\\.findByIds"
    - from: "AdminProductBatchController"
      to: "JwtRoleGuard.requireAdmin"
      via: "manual check trước business logic"
      pattern: "jwtRoleGuard\\.requireAdmin"
---

<objective>
Triển khai 2 endpoints product-svc: `/admin/products/charts/low-stock` (ADMIN-05) + `/admin/products/batch` (cross-svc enrichment helper cho Plan 01 top-products D-03).

Purpose:
- ADMIN-05 satisfied: low-stock alert list cho FE dashboard render.
- Batch endpoint là dependency của Plan 01 ProductBatchClient — nhưng vì batch endpoint nằm ở product-svc khác (independent module), Plan 01 và Plan 03 vẫn chạy parallel Wave 1. Plan 01 ProductBatchClient có fallback Map.of() khi product-svc fail → KHÔNG cần wave dependency. Khi cả 2 plan ship, cross-svc happy path hoạt động đầy đủ.

Output: 2 controllers + 2 services + 1 repository extension + integration tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-ho-n-thi-n-admin-charts-low-stock/19-CONTEXT.md
@.planning/phases/19-ho-n-thi-n-admin-charts-low-stock/19-RESEARCH.md
@.planning/phases/19-ho-n-thi-n-admin-charts-low-stock/19-PATTERNS.md

@sources/backend/product-service/src/main/java/com/ptit/htpt/productservice/web/AdminStatsController.java
@sources/backend/product-service/src/main/java/com/ptit/htpt/productservice/repository/ProductRepository.java
@sources/backend/product-service/src/main/java/com/ptit/htpt/productservice/web/JwtRoleGuard.java
@sources/backend/product-service/src/main/java/com/ptit/htpt/productservice/domain/ProductEntity.java

<interfaces>
ApiResponse envelope (existing per-svc): ApiResponse.of(int, String, T)
JwtRoleGuard (existing per-svc): requireAdmin(authHeader)
ProductEntity fields (relevant): id (String UUID), name, brand, thumbnailUrl, stock (int)
ProductRepository (existing): có @SQLRestriction("deleted=false") trên ProductEntity → tự loại deleted (analog comment line 22 ProductRepository); JpaRepository default findAllById(Iterable<String>) đã có sẵn — KHÔNG cần custom batch method.

Existing ProductRepository pattern (lines 27–37 — `findWithFilters`):
  Pageable + nullable params, Page<ProductEntity> return type.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ProductRepository.findLowStock + LowStockService + ProductBatchService với records</name>
  <files>
    sources/backend/product-service/src/main/java/com/ptit/htpt/productservice/repository/ProductRepository.java,
    sources/backend/product-service/src/main/java/com/ptit/htpt/productservice/service/LowStockService.java,
    sources/backend/product-service/src/main/java/com/ptit/htpt/productservice/service/ProductBatchService.java
  </files>

  <read_first>
    - sources/backend/product-service/src/main/java/com/ptit/htpt/productservice/repository/ProductRepository.java (toàn bộ — analog line 22 `@SQLRestriction("deleted=false")` tự apply soft-delete filter; lines 27–37 `findWithFilters` Pageable usage)
    - sources/backend/product-service/src/main/java/com/ptit/htpt/productservice/domain/ProductEntity.java (verify field names: id String, name, brand, thumbnailUrl, stock int)
    - 19-RESEARCH.md §Backend Pattern 5 (lines 391–414) — LowStockService verbatim với LOW_STOCK_THRESHOLD=10, CAP=50
    - 19-CONTEXT.md D-08 (threshold hardcoded), D-09 (sort ASC + cap 50 + empty []), D-10 (response shape `{id, name, brand, thumbnailUrl, stock}`)
  </read_first>

  <behavior>
    - Test 1 (findLowStock): seed 5 SP với stock = [3, 5, 8, 12, 20] → `findLowStock(10, PageRequest.of(0,50))` trả 3 SP (stock<10) sorted ASC `[3, 5, 8]`.
    - Test 2 (cap 50): seed 60 SP stock=1 → `findLowStock(10, PageRequest.of(0,50))` trả đúng 50 rows.
    - Test 3 (soft-delete excluded): seed 1 SP `deleted=true` stock=1 → KHÔNG xuất hiện trong kết quả (`@SQLRestriction` auto-apply).
    - Test 4 (LowStockService.list empty): zero SP có stock<10 → returns empty list.
    - Test 5 (LowStockService.list sort): seed 3 SP stock=[2,5,8] → returns LowStockItem list sorted ASC.
    - Test 6 (ProductBatchService.findByIds happy): seed 5 SP, gọi `findByIds([id1,id3,id5])` → 3 ProductSummary với name/brand/thumbnailUrl đúng.
    - Test 7 (ProductBatchService empty input): `findByIds([])` → empty list (KHÔNG query DB).
    - Test 8 (ProductBatchService missing IDs): seed 2 SP, gọi `findByIds([existing1, fakeUuid])` → 1 result (chỉ existing).
  </behavior>

  <action>
    1. **Extend ProductRepository.java**: thêm @Query method, import nếu thiếu: `org.springframework.data.domain.Pageable`, `org.springframework.data.repository.query.Param`. Copy verbatim RESEARCH §Backend Pattern 5 lines 393–394:

       ```java
       @Query("SELECT p FROM ProductEntity p WHERE p.stock < :threshold ORDER BY p.stock ASC")
       List<ProductEntity> findLowStock(@Param("threshold") int threshold, Pageable cap);
       ```

       **JpaRepository** đã có `findAllById(Iterable<String> ids)` default — KHÔNG cần custom batch method, ProductBatchService dùng method default này.

    2. **Tạo LowStockService.java** tại `sources/backend/product-service/src/main/java/com/ptit/htpt/productservice/service/LowStockService.java`. Copy verbatim RESEARCH §Backend Pattern 5 lines 397–414:

       ```java
       @Service
       public class LowStockService {
         public static final int LOW_STOCK_THRESHOLD = 10;  // D-08
         public static final int CAP = 50;                   // D-09

         private final ProductRepository productRepo;
         public LowStockService(ProductRepository productRepo) { this.productRepo = productRepo; }

         @Transactional(readOnly = true)
         public List<LowStockItem> list() {
           return productRepo.findLowStock(LOW_STOCK_THRESHOLD, PageRequest.of(0, CAP)).stream()
               .map(p -> new LowStockItem(p.getId(), p.getName(), p.getBrand(), p.getThumbnailUrl(), p.getStock()))
               .toList();
         }

         public record LowStockItem(String id, String name, String brand, String thumbnailUrl, int stock) {}
       }
       ```

       **Note:** Adjust `p.getId()` vs `p.id()` syntax theo ProductEntity actual (lombok @Getter vs Java record). Read ProductEntity.java first để xác định.

    3. **Tạo ProductBatchService.java** tại `sources/backend/product-service/src/main/java/com/ptit/htpt/productservice/service/ProductBatchService.java`:

       ```java
       @Service
       public class ProductBatchService {
         private final ProductRepository productRepo;
         public ProductBatchService(ProductRepository productRepo) { this.productRepo = productRepo; }

         @Transactional(readOnly = true)
         public List<ProductSummary> findByIds(List<String> ids) {
           if (ids == null || ids.isEmpty()) return List.of();
           return productRepo.findAllById(ids).stream()
               .map(p -> new ProductSummary(p.getId(), p.getName(), p.getBrand(), p.getThumbnailUrl()))
               .toList();
         }

         public record ProductSummary(String id, String name, String brand, String thumbnailUrl) {}
       }
       ```

       **Why same shape ProductSummary record:** match wire format mà Plan 01 ProductBatchClient deserialize qua `ParameterizedTypeReference<ApiResponse<List<ProductSummary>>>`. Field names ID/name/brand/thumbnailUrl phải khớp JSON.

    4. **Tạo unit/integration tests** `ProductRepositoryLowStockIT.java` (`@DataJpaTest` + Testcontainers Postgres) cover Test 1–3 + `LowStockServiceTest.java` + `ProductBatchServiceTest.java` với Mockito mock repository cho Test 6–8 (hoặc `@DataJpaTest` integration nếu prefer). Cover Test 1–8.
  </action>

  <verify>
    <automated>cd sources/backend/product-service && mvn -q test -Dtest='ProductRepositoryLowStockIT,LowStockServiceTest,ProductBatchServiceTest'</automated>
  </verify>

  <acceptance_criteria>
    - File tồn tại: `sources/backend/product-service/src/main/java/com/ptit/htpt/productservice/service/LowStockService.java`
    - File tồn tại: `sources/backend/product-service/src/main/java/com/ptit/htpt/productservice/service/ProductBatchService.java`
    - Grep `findLowStock` trong ProductRepository.java returns 1 match
    - Grep `LOW_STOCK_THRESHOLD = 10` trong LowStockService.java returns 1 match
    - Grep `CAP = 50` trong LowStockService.java returns 1 match
    - Grep `record LowStockItem\(String id, String name, String brand, String thumbnailUrl, int stock\)` returns 1 match
    - Grep `record ProductSummary\(String id, String name, String brand, String thumbnailUrl\)` returns 1 match
    - mvn test exit 0 với 8/8 tests passing
  </acceptance_criteria>

  <done>
    findLowStock query stock<10 sort ASC + cap 50 + soft-delete excluded; LowStockService + ProductBatchService working trên Testcontainers; records match Plan 01 wire format.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AdminChartsController (low-stock GET) + AdminProductBatchController (POST batch) + integration tests</name>
  <files>
    sources/backend/product-service/src/main/java/com/ptit/htpt/productservice/web/AdminChartsController.java,
    sources/backend/product-service/src/main/java/com/ptit/htpt/productservice/web/AdminProductBatchController.java,
    sources/backend/product-service/src/test/java/com/ptit/htpt/productservice/web/AdminChartsControllerIT.java,
    sources/backend/product-service/src/test/java/com/ptit/htpt/productservice/web/AdminProductBatchControllerIT.java
  </files>

  <read_first>
    - sources/backend/product-service/src/main/java/com/ptit/htpt/productservice/web/AdminStatsController.java (toàn bộ — analog: cùng svc, cùng `@RequestMapping("/admin/products/...")` + `JwtRoleGuard.requireAdmin(authHeader)` + `ApiResponse.of(...)`)
    - sources/backend/product-service/src/main/java/com/ptit/htpt/productservice/web/JwtRoleGuard.java (verify per-svc semantics)
    - 19-PATTERNS.md §Pattern Assignments product-service AdminChartsController (lines 195–231) — controller skeleton verbatim cho cả 2 controllers
    - 19-CONTEXT.md D-01 (batch endpoint shape POST `{ids:[...]}` → response), D-02 (path + JwtRoleGuard reuse), D-09 (low-stock empty → [])
    - **Path note:** `/admin/products/batch` (KHÔNG `/admin/products/charts/batch`) — gateway `/api/products/admin/batch` rewrite `/admin/products/batch` đi qua existing `product-service-admin` route. Verified PATTERNS.md line 231.
  </read_first>

  <behavior>
    - Test 1 (low-stock, admin): seed 3 SP stock=[3,5,8] → GET `/admin/products/charts/low-stock` với admin Bearer → 200, body.data length=3 sorted ASC.
    - Test 2 (low-stock empty): no low-stock SP → 200 với data=[].
    - Test 3 (low-stock no Bearer): → 401.
    - Test 4 (low-stock USER role): → 403.
    - Test 5 (batch happy): seed 5 SP, POST `/admin/products/batch` với body `{"ids":[id1,id3]}` + admin Bearer → 200, length=2 với name/brand/thumbnailUrl.
    - Test 6 (batch empty input): POST với `{"ids":[]}` → 200 với data=[].
    - Test 7 (batch no Bearer): → 401.
    - Test 8 (batch missing IDs): POST với mix existing + fake UUID → 200, chỉ existing trả về.
  </behavior>

  <action>
    1. **Tạo AdminChartsController.java** tại `sources/backend/product-service/src/main/java/com/ptit/htpt/productservice/web/AdminChartsController.java`:

       ```java
       @RestController
       @RequestMapping("/admin/products/charts")
       public class AdminChartsController {
         private final LowStockService lowStockService;
         private final JwtRoleGuard jwtRoleGuard;

         public AdminChartsController(LowStockService lowStockService, JwtRoleGuard jwtRoleGuard) {
           this.lowStockService = lowStockService;
           this.jwtRoleGuard = jwtRoleGuard;
         }

         @GetMapping("/low-stock")
         public ApiResponse<List<LowStockItem>> lowStock(
             @RequestHeader(value = HttpHeaders.AUTHORIZATION, required = false) String authHeader) {
           jwtRoleGuard.requireAdmin(authHeader);
           return ApiResponse.of(200, "Low stock products", lowStockService.list());
         }
       }
       ```

       Imports: package `com.ptit.htpt.productservice.web`, `org.springframework.http.HttpHeaders`, `org.springframework.web.bind.annotation.*`, `com.ptit.htpt.productservice.api.ApiResponse`, `com.ptit.htpt.productservice.service.LowStockService`, `com.ptit.htpt.productservice.service.LowStockService.LowStockItem`.

    2. **Tạo AdminProductBatchController.java** tại `sources/backend/product-service/src/main/java/com/ptit/htpt/productservice/web/AdminProductBatchController.java`:

       ```java
       @RestController
       @RequestMapping("/admin/products")
       public class AdminProductBatchController {
         private final ProductBatchService productBatchService;
         private final JwtRoleGuard jwtRoleGuard;

         public AdminProductBatchController(ProductBatchService productBatchService, JwtRoleGuard jwtRoleGuard) {
           this.productBatchService = productBatchService;
           this.jwtRoleGuard = jwtRoleGuard;
         }

         @PostMapping("/batch")
         public ApiResponse<List<ProductSummary>> batch(
             @RequestBody Map<String, List<String>> body,
             @RequestHeader(value = HttpHeaders.AUTHORIZATION, required = false) String authHeader) {
           jwtRoleGuard.requireAdmin(authHeader);
           List<String> ids = body == null ? List.of() : body.getOrDefault("ids", List.of());
           return ApiResponse.of(200, "Product batch", productBatchService.findByIds(ids));
         }
       }
       ```

       **Path:** `/admin/products` + `@PostMapping("/batch")` → final `/admin/products/batch`. Gateway `/api/products/admin/batch` → rewrite → `/admin/products/batch` qua existing `product-service-admin` catch-all route.

    3. **Tạo integration tests** `AdminChartsControllerIT.java` + `AdminProductBatchControllerIT.java` (`@SpringBootTest(webEnvironment = RANDOM_PORT)` + Testcontainers Postgres). Cover Test 1–8. Reuse JWT generation utility (find via grep `AdminStatsControllerIT` analog).
  </action>

  <verify>
    <automated>cd sources/backend/product-service && mvn -q test -Dtest='AdminChartsControllerIT,AdminProductBatchControllerIT'</automated>
  </verify>

  <acceptance_criteria>
    - File tồn tại: `sources/backend/product-service/src/main/java/com/ptit/htpt/productservice/web/AdminChartsController.java`
    - File tồn tại: `sources/backend/product-service/src/main/java/com/ptit/htpt/productservice/web/AdminProductBatchController.java`
    - Grep `@RequestMapping\("/admin/products/charts"\)` returns 1 match
    - Grep `@RequestMapping\("/admin/products"\)` (KHÔNG /charts) returns ≥1 match (cho batch controller)
    - Grep `@GetMapping\("/low-stock"\)` returns 1 match
    - Grep `@PostMapping\("/batch"\)` returns 1 match
    - Grep `jwtRoleGuard\.requireAdmin\(authHeader\)` trong cả 2 controllers returns 2 matches
    - mvn test exit 0 với 8/8 integration tests passing
  </acceptance_criteria>

  <done>
    `/admin/products/charts/low-stock` GET trả low-stock list (sorted ASC, cap 50, empty=[]). `/admin/products/batch` POST trả enrichment summaries cho top-products cross-svc. Cả 2 endpoints gate JwtRoleGuard. All tests green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → api-gateway | Untrusted admin JWT |
| api-gateway → product-svc | Internal HTTP, JWT relay |
| order-svc → api-gateway → product-svc | Cross-svc batch enrichment, JWT forwarded từ Plan 01 ProductBatchClient |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-03-01 | Spoofing | AdminChartsController + AdminProductBatchController | mitigate | `JwtRoleGuard.requireAdmin(authHeader)` ở cả 2 endpoints. Test 3+4+7. |
| T-19-03-02 | Tampering | POST /batch body `{ids:[...]}` | mitigate | `Map.getOrDefault("ids", List.of())` + null guard ở service (`if (ids.isEmpty()) return List.of()`). Malformed body → Spring deserialization 400 tự động. |
| T-19-03-03 | Information Disclosure | Low-stock SP info | accept | Admin-only context (gated). name/brand/thumbnailUrl đã public qua product list trang user — KHÔNG nhạy cảm. |
| T-19-03-04 | Denial of Service | Unbounded batch IDs | mitigate | `findAllById` dùng IN clause Postgres — limit thực tế bị cap bởi Plan 01 caller `PageRequest.of(0,10)` → tối đa 10 IDs/request. Theoretical risk nếu admin gọi trực tiếp với IDs siêu lớn → accept (admin role trusted). |
| T-19-03-05 | Denial of Service | findLowStock unbounded | mitigate | Pageable cap = `PageRequest.of(0, 50)` ở LowStockService.CAP (D-09). |
| T-19-03-06 | Tampering (SQL Injection) | JPQL findLowStock + JpaRepository.findAllById | mitigate | Parameterized binding. KHÔNG string concat. JpaRepository default method dùng IN(:ids) parameterized. |
| T-19-03-07 | Information Disclosure | Soft-deleted products leak | mitigate | `@SQLRestriction("deleted=false")` trên ProductEntity tự apply cho mọi query (analog ProductRepository line 22 comment). Verified Test 3. |
</threat_model>

<verification>
- Quick run: `cd sources/frontend && npx tsc --noEmit && npm run lint` (sanity)
- Plan verification: `cd sources/backend/product-service && mvn -q test -Dtest='ProductRepositoryLowStockIT,LowStockServiceTest,ProductBatchServiceTest,AdminChartsControllerIT,AdminProductBatchControllerIT'`
- Manual smoke: `curl -H "Authorization: Bearer <admin-jwt>" http://localhost:8080/api/products/admin/charts/low-stock` → 200; `curl -X POST -H "Authorization: Bearer ..." -H "Content-Type: application/json" -d '{"ids":["uuid1","uuid2"]}' http://localhost:8080/api/products/admin/batch` → 200
- Update `19-VALIDATION.md` per-task table với 2 task IDs ở trên (Wave 1, ADMIN-05, integration test, mvn command)
</verification>

<success_criteria>
- 2 endpoints functional: low-stock GET + batch POST
- Auth gate: 401/403 enforced
- Empty inputs trả [] (không 5xx)
- Cap 50 cho low-stock; soft-deleted SP tự loại
- 8 integration tests + 8 unit tests green
</success_criteria>

<output>
Sau khi hoàn tất, tạo `.planning/phases/19-ho-n-thi-n-admin-charts-low-stock/19-03-SUMMARY.md` per template với: artifacts, key decisions ref (D-01/02/08/09/10), test results, gateway verification (existing `product-service-admin` route đã cover cả `/admin/products/charts/**` và `/admin/products/batch`).
</output>
