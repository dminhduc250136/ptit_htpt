---
phase: 19
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/web/AdminChartsController.java
  - sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/service/UserChartsService.java
  - sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/service/Range.java
  - sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/repository/UserRepository.java
  - sources/backend/user-service/src/test/java/com/ptit/htpt/userservice/web/AdminChartsControllerIT.java
autonomous: true
requirements: [ADMIN-04]
tags: [admin, charts, user-svc, signups, aggregation]

must_haves:
  truths:
    - "GET /admin/users/charts/signups?range=30d trả về list daily user-signup counts, gap-filled với count=0 cho ngày không có user mới"
    - "Endpoint accept range ∈ {7d,30d,90d,all}; invalid range → 400"
    - "Missing/invalid Bearer → 401; non-admin role → 403"
    - "Daily granularity (D-05): mọi range trả về continuous days giữa from..today"
  artifacts:
    - path: "sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/web/AdminChartsController.java"
      provides: "1 endpoint /admin/users/charts/signups"
      contains: "@RequestMapping(\"/admin/users/charts\")"
    - path: "sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/service/UserChartsService.java"
      provides: "signupsByDay method với Java gap-fill"
      contains: "SignupPoint"
    - path: "sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/service/Range.java"
      provides: "Range enum (D7/D30/D90/ALL) per-svc copy"
      contains: "enum Range"
    - path: "sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/repository/UserRepository.java"
      provides: "+1 @Query: aggregateSignupsByDay"
      contains: "aggregateSignupsByDay"
    - path: "sources/backend/user-service/src/test/java/com/ptit/htpt/userservice/web/AdminChartsControllerIT.java"
      provides: "Integration test: signups endpoint + auth + invalid range + gap-fill"
      contains: "AdminChartsControllerIT"
  key_links:
    - from: "AdminChartsController /signups"
      to: "JwtRoleGuard.requireAdmin(authHeader)"
      via: "manual check (D-02 reuse Phase 9)"
      pattern: "jwtRoleGuard\\.requireAdmin"
    - from: "UserChartsService.signupsByDay"
      to: "UserRepository.aggregateSignupsByDay"
      via: "constructor inject + @Transactional readOnly"
      pattern: "userRepo\\.aggregateSignupsByDay"
---

<objective>
Triển khai 1 chart endpoint cho user-svc satisfying ADMIN-04 (user signups daily line chart).

Purpose: Mirror Plan 01 backend pattern nhưng ở user-svc — daily signup counts từ `users.created_at` aggregation với Java gap-fill (D-05). Plan này independent với Plan 01 (different svc, different repository) → chạy parallel cùng Wave 1.

Output: 1 controller + 1 service + 1 enum (per-svc copy) + 1 repository extension + integration tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-ho-n-thi-n-admin-charts-low-stock/19-CONTEXT.md
@.planning/phases/19-ho-n-thi-n-admin-charts-low-stock/19-RESEARCH.md
@.planning/phases/19-ho-n-thi-n-admin-charts-low-stock/19-PATTERNS.md

# Analog files
@sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/web/AdminStatsController.java
@sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/repository/UserRepository.java
@sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/web/JwtRoleGuard.java

<interfaces>
ApiResponse envelope (existing per-svc): ApiResponse.of(int, String, T)
JwtRoleGuard (existing per-svc — verified analog AdminStatsController inject): requireAdmin(authHeader)
UserEntity fields (relevant): id, createdAt (Instant)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Range enum (per-svc copy) + UserRepository.aggregateSignupsByDay</name>
  <files>
    sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/service/Range.java,
    sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/repository/UserRepository.java
  </files>

  <read_first>
    - sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/repository/UserRepository.java (toàn bộ — verify imports + class structure trước khi extend)
    - 19-RESEARCH.md §Backend Pattern 4 (lines 376–387) — aggregateSignupsByDay JPQL verbatim
    - 19-RESEARCH.md §Backend Pattern 6 (lines 416–440) — Range enum verbatim (cùng nội dung Plan 01 nhưng package khác)
    - 19-CONTEXT.md D-04 (range param shape) + D-05 (daily gap-fill)
    - **WHY per-svc copy:** monorepo microservice pattern — KHÔNG share code giữa svc; mỗi svc tự copy enum để tránh module dependency cross-svc. Đây là decision đã established trong codebase (mỗi svc có `JwtRoleGuard` riêng).
  </read_first>

  <behavior>
    - Test 1 (Range parse): identical Plan 01 Task 1 Test 1–3 nhưng package `com.ptit.htpt.userservice.service`.
    - Test 2 (UserRepository.aggregateSignupsByDay): seed 5 users với `createdAt` trên 3 ngày khác nhau → trả 3 rows `[Date, Long]` sorted ASC by day. Pass `from=null` → bao gồm tất cả.
    - Test 3 (range filter): seed 1 user 10 ngày trước + 1 user hôm nay; pass `from = now().minus(7 days)` → chỉ trả 1 row (hôm nay).
  </behavior>

  <action>
    1. **Tạo Range.java** tại `sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/service/Range.java`. Copy NGUYÊN XI nội dung Plan 01 Task 1 (RESEARCH §Backend Pattern 6) nhưng đổi package thành `com.ptit.htpt.userservice.service`. Lý do per-svc copy: mỗi microservice là independent Maven module, KHÔNG có shared module trong codebase này (verified: JwtRoleGuard cũng được copy riêng per-svc).

    2. **Extend UserRepository.java**: thêm `@Query` method, import nếu thiếu: `org.springframework.data.jpa.repository.Query`, `org.springframework.data.repository.query.Param`, `java.time.Instant`. Copy verbatim RESEARCH §Backend Pattern 4 lines 377–384:

       ```java
       @Query("""
           SELECT FUNCTION('DATE', u.createdAt) AS day, COUNT(u) AS cnt
           FROM UserEntity u
           WHERE (cast(:from as timestamp) IS NULL OR u.createdAt >= :from)
           GROUP BY FUNCTION('DATE', u.createdAt)
           ORDER BY day ASC
           """)
       List<Object[]> aggregateSignupsByDay(@Param("from") Instant from);
       ```

       **Pitfall #3:** dùng `FUNCTION('DATE', col)` (verified Hibernate 6 + Postgres). Nullable param idiom giống order-svc.

    3. **Tạo unit/integration tests** `RangeTest.java` (pure JUnit) + `UserRepositorySignupsIT.java` (`@DataJpaTest` + Testcontainers Postgres). Cover Test 1–3.
  </action>

  <verify>
    <automated>cd sources/backend/user-service && mvn -q test -Dtest='RangeTest,UserRepositorySignupsIT'</automated>
  </verify>

  <acceptance_criteria>
    - File tồn tại: `sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/service/Range.java`
    - Grep `aggregateSignupsByDay` trong UserRepository.java returns 1 match
    - Grep `FUNCTION\('DATE', u\.createdAt\)` trong UserRepository.java returns 1 match
    - Grep `cast\(:from as timestamp\) IS NULL` trong UserRepository.java returns 1 match
    - mvn test exit 0 với 4/4 tests passing (3 Range + 1+ repository tests)
  </acceptance_criteria>

  <done>
    Range enum + UserRepository @Query method working trên Testcontainers Postgres, sorted ASC by day, filter by `from` correct.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: UserChartsService với gap-fill + AdminChartsController với /signups endpoint + integration tests</name>
  <files>
    sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/service/UserChartsService.java,
    sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/web/AdminChartsController.java,
    sources/backend/user-service/src/test/java/com/ptit/htpt/userservice/web/AdminChartsControllerIT.java
  </files>

  <read_first>
    - sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/web/AdminStatsController.java (toàn bộ — analog exact: cùng svc, cùng pattern `@RequestMapping("/admin/users/...")` + JwtRoleGuard inject)
    - 19-RESEARCH.md §Backend Pattern 1 (lines 264–285) — gap-fill template (revenue), adapt cho count thay BigDecimal
    - 19-RESEARCH.md §Backend Pattern 4 (lines 386–387) — UserChartsService comment: "gap fill identical to revenue, count thay value"
    - 19-PATTERNS.md §Pattern Assignments user-service AdminChartsController (lines 179–189) — controller skeleton
    - 19-CONTEXT.md D-01 (signups endpoint shape `[{date, count}, ...]`), D-02 (path + JwtRoleGuard reuse)
  </read_first>

  <behavior>
    - Test 1 (signups endpoint, admin): GET `/admin/users/charts/signups?range=7d` với admin Bearer → 200, body.data là List<SignupPoint> length=8, gap-filled days có count=0.
    - Test 2 (range=all): seed users từ 30 ngày trước + hôm nay; GET `?range=all` → list bắt đầu từ earliest createdAt date.
    - Test 3 (no Bearer): → 401.
    - Test 4 (USER role): → 403.
    - Test 5 (invalid range): `?range=invalid` → 400.
    - Test 6 (default range): GET `/signups` (không query) → 200 với data span 30 ngày (default `30d` per D-04).
  </behavior>

  <action>
    1. **Tạo UserChartsService.java** tại `sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/service/UserChartsService.java`. Annotate `@Service`. Constructor inject `UserRepository`. Method `@Transactional(readOnly = true) public List<SignupPoint> signupsByDay(Range range)`:

       Copy template từ RESEARCH §Backend Pattern 1 (revenue gap-fill) nhưng:
       - Map `Object[]` row: `row[0]` cast `java.sql.Date.toLocalDate()`, `row[1]` cast `Long` qua `((Number) row[1]).longValue()`.
       - Default value khi gap fill: `0L` (KHÔNG `BigDecimal.ZERO`).
       - Trả `List<SignupPoint>`.

       Nested record cuối file:
       ```java
       public record SignupPoint(String date, long count) {}
       ```
       (Date là ISO `yyyy-MM-dd` từ `LocalDate.toString()`.)

    2. **Tạo AdminChartsController.java** tại `sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/web/AdminChartsController.java`. Path `@RequestMapping("/admin/users/charts")` (D-02). Constructor inject `UserChartsService` + `JwtRoleGuard` (existing per-svc). 1 endpoint:

       ```java
       @GetMapping("/signups")
       public ApiResponse<List<SignupPoint>> signups(
           @RequestParam(value = "range", required = false, defaultValue = "30d") String range,
           @RequestHeader(value = HttpHeaders.AUTHORIZATION, required = false) String authHeader) {
         jwtRoleGuard.requireAdmin(authHeader);
         return ApiResponse.of(200, "User signups", chartsService.signupsByDay(Range.parse(range)));
       }
       ```

       Imports: `org.springframework.http.HttpHeaders`, `org.springframework.web.bind.annotation.*`, `com.ptit.htpt.userservice.api.ApiResponse`, `com.ptit.htpt.userservice.service.{UserChartsService, Range}`, `com.ptit.htpt.userservice.service.UserChartsService.SignupPoint`.

    3. **Tạo integration test** `AdminChartsControllerIT.java` (`@SpringBootTest(webEnvironment = RANDOM_PORT)` + Testcontainers Postgres + `TestRestTemplate`). Generate admin/user JWT bằng existing test utility (mirror analog AdminStatsControllerIT pattern). Cover Test 1–6.
  </action>

  <verify>
    <automated>cd sources/backend/user-service && mvn -q test -Dtest='AdminChartsControllerIT'</automated>
  </verify>

  <acceptance_criteria>
    - File tồn tại: `sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/service/UserChartsService.java`
    - File tồn tại: `sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/web/AdminChartsController.java`
    - Grep `@RequestMapping\("/admin/users/charts"\)` returns 1 match
    - Grep `@GetMapping\("/signups"\)` returns 1 match
    - Grep `jwtRoleGuard\.requireAdmin\(authHeader\)` trong AdminChartsController returns 1 match
    - Grep `@Transactional\(readOnly = true\)` trong UserChartsService returns 1 match
    - Grep `record SignupPoint\(String date, long count\)` returns 1 match
    - mvn test exit 0 với 6/6 integration tests passing
  </acceptance_criteria>

  <done>
    `/admin/users/charts/signups` endpoint trả 200 với daily gap-filled list cho admin token. Auth gate enforced. Invalid range → 400. All tests green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → api-gateway | Untrusted admin JWT |
| api-gateway → user-svc | Internal HTTP, JWT relay; user-svc verify lại role |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-02-01 | Spoofing | AdminChartsController /signups | mitigate | `JwtRoleGuard.requireAdmin(authHeader)` (D-02). 401 missing, 403 non-ADMIN. Test 3+4. |
| T-19-02-02 | Tampering | range param | mitigate | `Range.parse(s)` exhaustive switch + ResponseStatusException 400 cho invalid. Test 5. |
| T-19-02-03 | Information Disclosure | Daily user signup counts | accept | Admin-only context (gated). Aggregate count, KHÔNG email/PII per user. |
| T-19-02-04 | Tampering (SQL Injection) | JPQL @Query với :from | mitigate | Spring Data JPA parameterized binding. KHÔNG string concat. |
| T-19-02-05 | Denial of Service | Unbounded GROUP BY day | accept | Dataset nhỏ (~hundreds users), `range=all` worst-case GROUP BY <500 days. KHÔNG cần Pageable. |
</threat_model>

<verification>
- Quick run: `cd sources/frontend && npx tsc --noEmit && npm run lint` (sanity)
- Plan verification: `cd sources/backend/user-service && mvn -q test -Dtest='RangeTest,UserRepositorySignupsIT,AdminChartsControllerIT'`
- Manual smoke: `curl -H "Authorization: Bearer <admin-jwt>" http://localhost:8080/api/users/admin/charts/signups?range=7d` → 200
- Update `19-VALIDATION.md` per-task table với 2 task IDs ở trên (Wave 1, ADMIN-04, integration test, mvn command)
</verification>

<success_criteria>
- 1 endpoint `/admin/users/charts/signups` trả 200 với admin token, gap-filled daily list
- Auth gate functional (401/403)
- Invalid range → 400
- 6 integration tests + 4 unit tests green
</success_criteria>

<output>
Sau khi hoàn tất, tạo `.planning/phases/19-ho-n-thi-n-admin-charts-low-stock/19-02-SUMMARY.md` per template với: artifacts, key decisions ref (D-01/02/04/05), test results, gateway verification (existing `user-service-admin` route đã cover).
</output>
