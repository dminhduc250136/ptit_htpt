---
phase: 19
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/web/AdminChartsController.java
  - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/service/OrderChartsService.java
  - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/service/ProductBatchClient.java
  - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/service/Range.java
  - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/repository/OrderRepository.java
  - sources/backend/order-service/src/test/java/com/ptit/htpt/orderservice/web/AdminChartsControllerIT.java
autonomous: true
requirements: [ADMIN-01, ADMIN-02, ADMIN-03]
tags: [admin, charts, order-svc, aggregation, jpql, cross-svc]

must_haves:
  truths:
    - "GET /admin/orders/charts/revenue?range=30d trả về list daily revenue points (DELIVERED orders), gap-filled với value=0"
    - "GET /admin/orders/charts/top-products?range=30d trả về top-10 sản phẩm có name+brand+thumbnailUrl từ enrichment cross-svc"
    - "GET /admin/orders/charts/status-distribution trả về counts theo 5 status (snapshot, không range param)"
    - "Top-products endpoint vẫn trả 200 với name='Product {id[:8]}' khi product-svc batch fail (D-03 fallback)"
    - "Invalid range param → 400; missing/invalid Bearer → 401; non-admin role → 403"
  artifacts:
    - path: "sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/web/AdminChartsController.java"
      provides: "3 endpoints /admin/orders/charts/{revenue,top-products,status-distribution}"
      contains: "@RequestMapping(\"/admin/orders/charts\")"
    - path: "sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/service/OrderChartsService.java"
      provides: "revenueByDay + topProducts + statusDistribution methods (@Transactional readOnly)"
      contains: "RevenuePoint"
    - path: "sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/service/ProductBatchClient.java"
      provides: "RestTemplate cross-svc batch fetch + try/catch fallback"
      contains: "fetchBatch"
    - path: "sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/service/Range.java"
      provides: "Range enum (D7/D30/D90/ALL) + parse + toFromInstant"
      contains: "enum Range"
    - path: "sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/repository/OrderRepository.java"
      provides: "+3 @Query methods: aggregateRevenueByDay, aggregateTopProducts, aggregateStatusDistribution"
      contains: "aggregateRevenueByDay"
    - path: "sources/backend/order-service/src/test/java/com/ptit/htpt/orderservice/web/AdminChartsControllerIT.java"
      provides: "Integration tests cover 3 endpoints + auth + invalid range + cross-svc fallback"
      contains: "AdminChartsControllerIT"
  key_links:
    - from: "AdminChartsController endpoints"
      to: "JwtRoleGuard.requireAdmin(authHeader)"
      via: "manual check trong mỗi @GetMapping"
      pattern: "jwtRoleGuard\\.requireAdmin"
    - from: "OrderChartsService.topProducts"
      to: "ProductBatchClient.fetchBatch"
      via: "constructor inject + try/catch fallback"
      pattern: "productBatchClient\\.fetchBatch"
    - from: "ProductBatchClient.fetchBatch"
      to: "http://api-gateway:8080/api/products/admin/batch"
      via: "RestTemplate.exchange POST với Authorization header forwarded (D-03 + Pitfall #4)"
      pattern: "headers\\.set\\(HttpHeaders\\.AUTHORIZATION"
---

<objective>
Triển khai 3 chart endpoints + Range enum + cross-svc batch client cho order-svc, satisfying ADMIN-01 (revenue), ADMIN-02 (top-products) và ADMIN-03 (status distribution).

Purpose: BE foundation cho 3 trong 4 charts admin dashboard. Aggregation từ DELIVERED orders + order_items với gap-fill ở Java cho daily granularity (D-05). Cross-svc enrichment top-products để FE chỉ cần 1 round-trip per chart (D-03).

Output: 1 controller + 1 service + 1 client + 1 enum + 1 repository extension + integration tests sẵn sàng cho FE Plan 04 consume qua gateway routes hiện có (`/api/orders/admin/**`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-ho-n-thi-n-admin-charts-low-stock/19-CONTEXT.md
@.planning/phases/19-ho-n-thi-n-admin-charts-low-stock/19-RESEARCH.md
@.planning/phases/19-ho-n-thi-n-admin-charts-low-stock/19-PATTERNS.md

# Analog files (read for pattern extraction)
@sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/web/AdminStatsController.java
@sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/service/OrderStatsService.java
@sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/service/OrderCrudService.java
@sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/repository/OrderRepository.java
@sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/web/JwtRoleGuard.java
@sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/AppConfig.java
@sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/api/ApiResponse.java

<interfaces>
<!-- Existing types/contracts the executor MUST reuse — no codebase exploration needed -->

ApiResponse envelope (existing):
  ApiResponse.of(int status, String message, T data) → returns ApiResponse<T>

JwtRoleGuard (existing per-svc, inject via constructor):
  void requireAdmin(String authHeader) // throws ResponseStatusException 401/403

OrderRepository existing nullable param idiom (lines 39–51):
  AND (cast(:from as timestamp) IS NULL OR o.createdAt >= :from)

RestTemplate bean (AppConfig.java lines 8–13 — already wired):
  @Bean public RestTemplate restTemplate()

OrderEntity fields (relevant): id, status (enum string PENDING/CONFIRMED/SHIPPED/DELIVERED/CANCELLED), total (BigDecimal), createdAt (Instant), items (List<OrderItemEntity>)
OrderItemEntity fields (relevant): productId (String UUID), quantity (int)

Cross-svc URL convention (from OrderCrudService.java lines 365):
  http://api-gateway:8080/api/products/...
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Range enum + 3 OrderRepository @Query methods + ProductBatchClient với auth-forwarding fallback</name>
  <files>
    sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/service/Range.java,
    sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/repository/OrderRepository.java,
    sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/service/ProductBatchClient.java
  </files>

  <read_first>
    - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/repository/OrderRepository.java (lines 39–51 — nullable param idiom `cast(:from as timestamp) IS NULL OR ...`) — analog cho 3 @Query mới
    - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/service/OrderCrudService.java (lines 355–427 — `validateStockOrThrow` + `deductStockAfterPersist`) — RestTemplate try/catch fallback pattern cho ProductBatchClient
    - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/AppConfig.java (lines 8–13 — `@Bean RestTemplate`) — inject sẵn, KHÔNG add bean mới
    - 19-RESEARCH.md §Backend Pattern 1 (lines 250–285) cho aggregateRevenueByDay JPQL + gap-fill blueprint (gap-fill nằm trong Task 2 — task này chỉ tạo @Query)
    - 19-RESEARCH.md §Backend Pattern 2 (lines 289–351) cho aggregateTopProducts + ProductBatchClient code mẫu hoàn chỉnh
    - 19-RESEARCH.md §Backend Pattern 3 (lines 355–371) cho aggregateStatusDistribution
    - 19-RESEARCH.md §Backend Pattern 6 (lines 416–440) cho Range enum hoàn chỉnh
    - 19-CONTEXT.md D-03 (Top-products enrichment + fallback), D-04 (range param shape + invalid → 400), D-05 (daily granularity + gap fill)
  </read_first>

  <behavior>
    - Test 1 (Range): `Range.parse("7d")` → D7; `parse("30d")` → D30; `parse("90d")` → D90; `parse("all")` → ALL; `parse(null)` → D30 default.
    - Test 2 (Range invalid): `Range.parse("invalid")` throws ResponseStatusException với HttpStatus.BAD_REQUEST.
    - Test 3 (Range toFromInstant): `D7.toFromInstant()` ≈ now().minus(7 days); `ALL.toFromInstant()` returns null.
    - Test 4 (OrderRepository.aggregateRevenueByDay): với Instant from = null + 3 DELIVERED orders trên 3 ngày khác nhau → trả 3 rows `[Date, BigDecimal]` sorted ASC by day. Non-DELIVERED orders bị loại.
    - Test 5 (OrderRepository.aggregateTopProducts): `PageRequest.of(0,10)` + 15 productIds với qty khác nhau → trả top-10 sorted by qtySold DESC.
    - Test 6 (OrderRepository.aggregateStatusDistribution): seed orders đa status → trả counts đúng per status (status string + count long).
    - Test 7 (ProductBatchClient.fetchBatch empty input): `fetchBatch(List.of(), null)` → returns Map.of() (không gọi HTTP).
    - Test 8 (ProductBatchClient fallback): mock RestTemplate ném RuntimeException → returns Map.of() + log.warn (KHÔNG throw lên caller).
  </behavior>

  <action>
    1. **Tạo Range enum** tại `sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/service/Range.java`. Copy nguyên xi RESEARCH §Backend Pattern 6 lines 419–440 (D-04). Package `com.ptit.htpt.orderservice.service`. Imports: `java.time.Instant`, `java.time.temporal.ChronoUnit`, `org.springframework.http.HttpStatus`, `org.springframework.web.server.ResponseStatusException`. Default khi `s == null` là `"30d"` (D-04 explicit).

    2. **Extend OrderRepository.java**: thêm 3 `@Query` methods cuối class (trước `}` đóng), import nếu thiếu: `org.springframework.data.domain.Pageable`, `java.time.Instant`, `org.springframework.data.repository.query.Param`. Copy verbatim từ RESEARCH §Backend Pattern 1/2/3:

       ```java
       @Query("""
           SELECT FUNCTION('DATE', o.createdAt) AS day, SUM(o.total) AS total
           FROM OrderEntity o
           WHERE o.status = 'DELIVERED'
             AND (cast(:from as timestamp) IS NULL OR o.createdAt >= :from)
           GROUP BY FUNCTION('DATE', o.createdAt)
           ORDER BY day ASC
           """)
       List<Object[]> aggregateRevenueByDay(@Param("from") Instant from);

       @Query("""
           SELECT i.productId, SUM(i.quantity) AS qtySold
           FROM OrderEntity o JOIN o.items i
           WHERE o.status = 'DELIVERED'
             AND (cast(:from as timestamp) IS NULL OR o.createdAt >= :from)
           GROUP BY i.productId
           ORDER BY qtySold DESC
           """)
       List<Object[]> aggregateTopProducts(@Param("from") Instant from, Pageable limit);

       @Query("SELECT o.status, COUNT(o) FROM OrderEntity o GROUP BY o.status")
       List<Object[]> aggregateStatusDistribution();
       ```

       **Pitfall #3 (RESEARCH):** dùng `FUNCTION('DATE', col)` KHÔNG raw `DATE(col)`. Pitfall: nullable Instant param phải dùng `cast(:from as timestamp) IS NULL` idiom — ĐÃ verified trong existing line 41 của file này.

    3. **Tạo ProductBatchClient.java** tại `sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/service/ProductBatchClient.java`. Copy verbatim RESEARCH §Backend Pattern 2 lines 304–334 (record `ProductSummary` nested) NHƯNG sửa signature `fetchBatch(List<String> ids, String authHeader)` (KHÔNG bỏ authHeader — D-03 + Pitfall #4 yêu cầu forward Bearer). Inside try block, set `headers.set(HttpHeaders.AUTHORIZATION, authHeader)` trước `new HttpEntity<>(...)`. Nested record: `public record ProductSummary(String id, String name, String brand, String thumbnailUrl) {}`. Annotate `@Component`, inject `RestTemplate` qua constructor (đã có bean trong AppConfig). URL hardcoded: `http://api-gateway:8080/api/products/admin/batch`. Catch `Exception` → `log.warn("[D-03] product-svc batch enrichment failed: {}", ex.getMessage())` → return `Map.of()`.

    4. **Tạo unit tests** trong `sources/backend/order-service/src/test/java/com/ptit/htpt/orderservice/service/RangeTest.java` (pure JUnit) + `OrderRepositoryChartsIT.java` (`@DataJpaTest` + Testcontainers Postgres — kiểm tra repository methods trên DB thật). Cover Test 1–6. ProductBatchClient unit test trong Task 1 sub-test với `Mockito` mock RestTemplate cho Test 7–8.
  </action>

  <verify>
    <automated>cd sources/backend/order-service && mvn -q test -Dtest='RangeTest,OrderRepositoryChartsIT,ProductBatchClientTest'</automated>
  </verify>

  <acceptance_criteria>
    - File tồn tại: `sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/service/Range.java`
    - File tồn tại: `sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/service/ProductBatchClient.java`
    - Grep `aggregateRevenueByDay` trong `OrderRepository.java` returns 1 match
    - Grep `aggregateTopProducts` trong `OrderRepository.java` returns 1 match
    - Grep `aggregateStatusDistribution` trong `OrderRepository.java` returns 1 match
    - Grep `FUNCTION\('DATE'` trong `OrderRepository.java` returns ≥2 matches (revenue + top-products dùng cast idiom)
    - Grep `cast\(:from as timestamp\) IS NULL` trong `OrderRepository.java` returns ≥3 matches (1 existing + 2 new)
    - Grep `headers\.set\(HttpHeaders\.AUTHORIZATION` trong `ProductBatchClient.java` returns 1 match (auth forwarding D-03)
    - Grep `log\.warn.*D-03.*product-svc batch` trong `ProductBatchClient.java` returns 1 match
    - mvn test command exit 0 với 8/8 tests passing
  </acceptance_criteria>

  <done>
    Range enum parse correctly cho 4 inputs + invalid throws 400; 3 repository @Query methods aggregate đúng từ seed data Testcontainers; ProductBatchClient empty input returns empty map (no HTTP) + RestTemplate exception swallowed → empty map. All BE tests green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: OrderChartsService với gap-fill + AdminChartsController với 3 endpoints + integration tests</name>
  <files>
    sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/service/OrderChartsService.java,
    sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/web/AdminChartsController.java,
    sources/backend/order-service/src/test/java/com/ptit/htpt/orderservice/web/AdminChartsControllerIT.java
  </files>

  <read_first>
    - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/service/OrderStatsService.java (toàn bộ — analog exact: cùng svc, cùng `@Transactional(readOnly=true)`, cùng OrderRepository inject)
    - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/web/AdminStatsController.java (toàn bộ — analog exact: `@RequestMapping("/admin/orders/stats")` + `JwtRoleGuard.requireAdmin(authHeader)` + `ApiResponse.of(...)` envelope)
    - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/web/JwtRoleGuard.java (lines 34–63 — `requireAdmin` semantics: missing/invalid → 401, non-ADMIN → 403)
    - 19-RESEARCH.md §Backend Pattern 1 (lines 264–285) — gap-fill code Java verbatim
    - 19-RESEARCH.md §Backend Pattern 2 (lines 336–351) — topProducts combine + fallback code verbatim
    - 19-RESEARCH.md §Backend Pattern 3 (lines 362–371) — statusDistribution code verbatim
    - 19-PATTERNS.md §Pattern Assignments AdminChartsController (lines 65–108) — controller skeleton + auth-forwarding cho top-products
    - 19-CONTEXT.md D-01 (3 endpoints), D-02 (path + JwtRoleGuard reuse), D-03 (cross-svc enrichment), D-05 (gap fill)
  </read_first>

  <behavior>
    - Test 1 (revenue endpoint, admin): GET `/admin/orders/charts/revenue?range=7d` với admin Bearer → 200, body.data là List<RevenuePoint> length=8 (7 days + today inclusive), gap-filled days có value=0.
    - Test 2 (revenue endpoint, range=all): GET `/admin/orders/charts/revenue?range=all` → 200, list bắt đầu từ earliest DELIVERED createdAt date.
    - Test 3 (revenue endpoint, no Bearer): GET `/admin/orders/charts/revenue` (không header) → 401.
    - Test 4 (revenue endpoint, USER role): GET với non-admin token → 403.
    - Test 5 (revenue endpoint, invalid range): GET `?range=invalid` với admin → 400.
    - Test 6 (top-products, admin happy path): seed 12 orders DELIVERED với 12 productIds + mock ProductBatchClient trả enrichment → 200, list length=10 sorted DESC by qtySold, mỗi entry có name+brand+thumbnailUrl.
    - Test 7 (top-products fallback): mock ProductBatchClient trả `Map.of()` (simulate product-svc fail) → 200, mỗi entry name=`"Product {id[:8]}"` brand=null thumbnailUrl=null (D-03 fallback).
    - Test 8 (status-distribution, admin): seed orders đa status → 200, list 5 status entries với counts đúng.
  </behavior>

  <action>
    1. **Tạo OrderChartsService.java** tại `sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/service/OrderChartsService.java`. Annotate `@Service`. Constructor inject `OrderRepository` + `ProductBatchClient`. 3 methods, mỗi `@Transactional(readOnly = true)` (analog OrderStatsService line 20):

       - `revenueByDay(Range range)` → copy verbatim RESEARCH §Backend Pattern 1 lines 264–282 (gap-fill from `from` đến `LocalDate.now()` với `BigDecimal.ZERO`). Convert raw `Object[]` rows: `row[0]` cast `java.sql.Date` → `.toLocalDate()`, `row[1]` cast `BigDecimal`. Trả `List<RevenuePoint>`.
       - `topProducts(Range range, String authHeader)` → copy verbatim RESEARCH §Backend Pattern 2 lines 336–351. `PageRequest.of(0, 10)`. Pass `authHeader` xuống `productBatchClient.fetchBatch(ids, authHeader)`. Fallback: `name = "Product " + id.substring(0, Math.min(8, id.length()))`, brand=null, thumbnailUrl=null.
       - `statusDistribution()` → copy verbatim RESEARCH §Backend Pattern 3 lines 363–368.

       Records nested cuối file (D-01 response shapes):
       ```java
       public record RevenuePoint(String date, BigDecimal value) {}
       public record TopProductPoint(String productId, String name, String brand, String thumbnailUrl, long qtySold) {}
       public record StatusPoint(String status, long count) {}
       ```
       (Date là ISO `yyyy-MM-dd` từ `LocalDate.toString()`.)

    2. **Tạo AdminChartsController.java** tại `sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/web/AdminChartsController.java`. Copy structure analog AdminStatsController. Path: `@RequestMapping("/admin/orders/charts")` (D-02). Constructor inject `OrderChartsService` + `JwtRoleGuard`. 3 endpoints:

       ```java
       @GetMapping("/revenue")
       public ApiResponse<List<RevenuePoint>> revenue(
           @RequestParam(value = "range", required = false, defaultValue = "30d") String range,
           @RequestHeader(value = HttpHeaders.AUTHORIZATION, required = false) String authHeader) {
         jwtRoleGuard.requireAdmin(authHeader);
         return ApiResponse.of(200, "Revenue chart", chartsService.revenueByDay(Range.parse(range)));
       }

       @GetMapping("/top-products")
       public ApiResponse<List<TopProductPoint>> topProducts(
           @RequestParam(value = "range", required = false, defaultValue = "30d") String range,
           @RequestHeader(value = HttpHeaders.AUTHORIZATION, required = false) String authHeader) {
         jwtRoleGuard.requireAdmin(authHeader);
         return ApiResponse.of(200, "Top products", chartsService.topProducts(Range.parse(range), authHeader));
       }

       @GetMapping("/status-distribution")
       public ApiResponse<List<StatusPoint>> statusDistribution(
           @RequestHeader(value = HttpHeaders.AUTHORIZATION, required = false) String authHeader) {
         jwtRoleGuard.requireAdmin(authHeader);
         return ApiResponse.of(200, "Order status distribution", chartsService.statusDistribution());
       }
       ```

       **CRITICAL (Pitfall #4):** top-products forward `authHeader` xuống service → service xuống ProductBatchClient → batch endpoint cũng cần admin token.

       Imports: `org.springframework.http.HttpHeaders`, `org.springframework.web.bind.annotation.*`, `com.ptit.htpt.orderservice.api.ApiResponse`, `com.ptit.htpt.orderservice.service.{OrderChartsService, Range}`, `com.ptit.htpt.orderservice.service.OrderChartsService.{RevenuePoint, TopProductPoint, StatusPoint}`.

    3. **Tạo integration test** `AdminChartsControllerIT.java` (`@SpringBootTest(webEnvironment = RANDOM_PORT)` + Testcontainers Postgres + `TestRestTemplate`). Generate admin/user JWT bằng existing test utility (find via grep `AdminStatsControllerIT` analog hoặc `JwtTestUtil`). Mock ProductBatchClient bean (`@MockBean`) cho Test 6/7. Cover Test 1–8. Để gap-fill stable: seed orders với `createdAt = Instant.now().minus(N, ChronoUnit.DAYS)`.

       **Per-svc auth forwarding sanity:** Test 6 verify `verify(productBatchClient).fetchBatch(anyList(), eq("Bearer <admin-jwt>"))` để chứng minh authHeader propagate.
  </action>

  <verify>
    <automated>cd sources/backend/order-service && mvn -q test -Dtest='AdminChartsControllerIT'</automated>
  </verify>

  <acceptance_criteria>
    - File tồn tại: `sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/service/OrderChartsService.java`
    - File tồn tại: `sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/web/AdminChartsController.java`
    - Grep `@RequestMapping\("/admin/orders/charts"\)` trong AdminChartsController returns 1 match
    - Grep `@GetMapping\("/revenue"\)` + `@GetMapping\("/top-products"\)` + `@GetMapping\("/status-distribution"\)` mỗi cái 1 match
    - Grep `jwtRoleGuard\.requireAdmin\(authHeader\)` returns 3 matches (1 mỗi endpoint)
    - Grep `chartsService\.topProducts\(.*authHeader\)` returns 1 match (auth forwarding D-03)
    - Grep `BigDecimal\.ZERO` trong OrderChartsService returns ≥1 match (gap-fill)
    - Grep `Math\.min\(8, id\.length\(\)\)` trong OrderChartsService returns 1 match (D-03 fallback name truncate)
    - Grep `@Transactional\(readOnly = true\)` trong OrderChartsService returns 3 matches
    - mvn test exit 0 với 8/8 integration tests passing
  </acceptance_criteria>

  <done>
    AdminChartsController exposes 3 endpoints; OrderChartsService gap-fills daily revenue + topProducts với cross-svc enrichment fallback + statusDistribution snapshot. Auth gate enforced (401/403). Invalid range → 400. All integration tests green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → api-gateway | Untrusted admin JWT trong Authorization header |
| api-gateway → order-svc | Internal HTTP, JWT relay; order-svc verify lại role |
| order-svc → api-gateway → product-svc | Cross-svc batch enrichment, JWT forward |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01-01 | Spoofing | AdminChartsController endpoints | mitigate | `JwtRoleGuard.requireAdmin(authHeader)` ở mỗi @GetMapping (D-02). Missing/invalid Bearer → 401; non-ADMIN role → 403. Verified bằng AdminChartsControllerIT Test 3+4. |
| T-19-01-02 | Tampering | range query param | mitigate | `Range.parse(s)` switch-exhaustive, default chỉ accept `7d|30d|90d|all`. Khác → ResponseStatusException 400. Test 5. |
| T-19-01-03 | Information Disclosure | Revenue + status counts | accept | Đây là admin-only context (đã gate qua JwtRoleGuard ADMIN role). Không có PII trong response — chỉ aggregate numbers. |
| T-19-01-04 | Information Disclosure | top-products productId fallback | accept | Khi product-svc fail, productId UUID truncated 8 ký tự (D-03 fallback name="Product {id[:8]}"). Admin-only context, ID không nhạy cảm. |
| T-19-01-05 | Denial of Service | aggregateTopProducts unbounded | mitigate | `Pageable` cap = `PageRequest.of(0, 10)` ở service layer (D-01 explicit "top-10"). JPQL không trả >10 rows. |
| T-19-01-06 | Tampering (SQL Injection) | JPQL `@Query` với `:from` param | mitigate | Spring Data JPA parameterized binding (`@Param("from")`); KHÔNG string concat. Verified analog OrderRepository.findByUserIdWithFilters. |
| T-19-01-07 | Elevation of Privilege | ProductBatchClient cross-svc | mitigate | Auth forwarding (`headers.set(HttpHeaders.AUTHORIZATION, authHeader)` — Pitfall #4). product-svc batch endpoint gate riêng bằng JwtRoleGuard. KHÔNG dùng service-account static token. |
| T-19-01-08 | Repudiation | Cross-svc batch failure swallowed | accept | `log.warn("[D-03] ...")` ghi nhận failure cho debug. KHÔNG silent — chỉ best-effort fallback theo D-03. Admin sẽ thấy fallback name "Product {id[:8]}" làm cue rằng enrichment fail. |
</threat_model>

<verification>
- Quick run: `cd sources/frontend && npx tsc --noEmit && npm run lint` (PHẢI green dù plan này là BE — sanity FE không bị side effect)
- Plan verification: `cd sources/backend/order-service && mvn -q test -Dtest='RangeTest,OrderRepositoryChartsIT,ProductBatchClientTest,AdminChartsControllerIT'`
- Manual smoke (sau khi service start qua docker-compose): `curl -H "Authorization: Bearer <admin-jwt>" http://localhost:8080/api/orders/admin/charts/revenue?range=7d` → 200 với data array
- Update `19-VALIDATION.md` per-task table với 2 task IDs ở trên (Wave 1, ADMIN-01/02/03, integration test type, mvn command)
</verification>

<success_criteria>
- 3 endpoints `/admin/orders/charts/{revenue,top-products,status-distribution}` trả 200 với admin token
- Auth gate functional: 401 missing Bearer, 403 non-admin
- Invalid range → 400
- Top-products fallback: product-svc down → 200 với productId-only entries (KHÔNG fail toàn endpoint)
- Daily gap-fill: revenue chart trả continuous days với value=0 cho empty days
- 8 integration tests + 8 unit tests green (mvn exit 0)
</success_criteria>

<output>
Sau khi hoàn tất, tạo `.planning/phases/19-ho-n-thi-n-admin-charts-low-stock/19-01-SUMMARY.md` per template với: artifacts created, key decisions ref (D-01..D-05), test results, gateway verification (route đã cover qua existing `order-service-admin` catch-all).
</output>
