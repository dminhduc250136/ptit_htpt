---
phase: 26-vnpay-payment-integration
plan: 04
type: execute
wave: 3
depends_on: [26-03]
files_modified:
  - sources/frontend/src/types/index.ts
  - sources/frontend/src/lib/orderLabels.ts
  - sources/frontend/src/services/payments.ts
  - sources/frontend/src/app/checkout/page.tsx
  - sources/frontend/src/app/checkout/result/page.tsx
  - sources/frontend/src/app/profile/orders/[id]/page.tsx
  - sources/frontend/src/app/admin/orders/[id]/page.tsx
  - sources/frontend/tests/vnpay-payment.spec.ts
autonomous: true
requirements: [PAY-01, PAY-02, PAY-04]
must_haves:
  truths:
    - "Khách chọn 'Thanh toán qua VNPay' tại /checkout, đặt hàng → redirect sang paymentUrl"
    - "Trang /checkout/result hiển thị 3 trạng thái + poll payment_status 3s × 5"
    - "/profile/orders/[id] + /admin/orders/[id] hiển thị payment method VNPAY + payment status + mã giao dịch VNPay"
  artifacts:
    - path: "sources/frontend/src/app/checkout/result/page.tsx"
      provides: "Trang kết quả thanh toán + polling"
      contains: "vnp_ResponseCode"
    - path: "sources/frontend/src/lib/orderLabels.ts"
      provides: "Nhãn VNPAY"
      contains: "VNPAY"
  key_links:
    - from: "checkout/page.tsx submitOrder"
      to: "window.location paymentUrl"
      via: "redirect khi paymentMethod=VNPAY"
      pattern: "paymentUrl"
    - from: "checkout/result/page.tsx"
      to: "GET /api/payments/vnpay/return"
      via: "resolve orderId từ vnp_TxnRef trước khi poll"
      pattern: "getVNPayReturn"
    - from: "checkout/result/page.tsx"
      to: "GET /api/orders/{id}"
      via: "poll payment_status mỗi 3s tối đa 5 lần"
      pattern: "getOrderById"
---

<objective>
Hoàn thiện frontend VNPay: thêm option "Thanh toán qua VNPay" vào selector checkout + redirect sang `paymentUrl`; tạo trang kết quả `/checkout/result` poll `payment_status`; hiển thị payment method/status/mã giao dịch tại order display.

Purpose: PAY-01 (selector + redirect), PAY-02 (return page + polling), PAY-04 (order display) — phần khách hàng nhìn thấy.
Output: checkout selector mở rộng + route `checkout/result` + order display + Playwright smoke spec.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-vnpay-payment-integration/26-CONTEXT.md
@.planning/phases/26-vnpay-payment-integration/26-PATTERNS.md
@.planning/phases/26-vnpay-payment-integration/26-UI-SPEC.md
@.planning/phases/26-vnpay-payment-integration/26-03-SUMMARY.md

<interfaces>
<!-- Từ Plan 02/03: OrderDto có thêm field -->
Order (FE type @/types): cần thêm paymentStatus: string, vnpTransactionNo?: string, paymentUrl?: string
createOrder (services/orders.ts) trả Order chứa paymentUrl khi paymentMethod=VNPAY
getOrderById(id) (services/orders.ts) — dùng cho polling payment_status

<!-- Analog files -->
checkout/page.tsx — mảng payment options (~lines 332-350), submitOrder (~lines 183-274), type union paymentMethod (~line 76)
profile/orders/[id]/page.tsx — pattern load + state machine (loading/failed/empty/data) + RetrySection; render paymentMethodMap/paymentStatusMap (~lines 234-240)
lib/orderLabels.ts — paymentMethodMap (thêm VNPAY), paymentStatusMap (đã có PENDING/PAID/FAILED)
components/ui: Button, Banner, RetrySection
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Type Order + orderLabels + checkout selector VNPay + redirect</name>
  <read_first>
    - sources/frontend/src/types/index.ts (interface Order — file sửa)
    - sources/frontend/src/lib/orderLabels.ts (paymentMethodMap — file sửa)
    - sources/frontend/src/app/checkout/page.tsx (mảng payment options + submitOrder — file sửa)
    - .planning/phases/26-vnpay-payment-integration/26-UI-SPEC.md §Checkout selector + §Copywriting Contract
    - .planning/phases/26-vnpay-payment-integration/26-PATTERNS.md §checkout/page.tsx + §orderLabels.ts
  </read_first>
  <action>
    Sửa `src/types/index.ts` — interface `Order`: thêm `paymentStatus: string;` `vnpTransactionNo?: string;` `paymentUrl?: string;`.

    Sửa `src/lib/orderLabels.ts` — thêm 1 entry vào `paymentMethodMap`: `VNPAY: 'Thanh toán qua VNPay'`. `paymentStatusMap` giữ nguyên (đã có PENDING/PAID/FAILED).

    Sửa `src/app/checkout/page.tsx`:
    - Mở rộng type union `paymentMethod` (~line 76): thêm `| 'VNPAY'`.
    - Thêm 1 entry vào mảng payment options (~lines 333-337): `{ value: 'VNPAY', label: 'Thanh toán qua VNPay', icon: '💳' }` — tái dụng class `styles.paymentOption`/`styles.paymentActive` (UI-SPEC §Component reuse, KHÔNG tạo style mới).
    - Trong `submitOrder` (~lines 188-220), sau khi nhận `order` từ `createOrder`: nếu `form.paymentMethod === 'VNPAY'` && `order.paymentUrl` có giá trị → `showToast('Đang chuyển tới cổng thanh toán VNPay...', 'success')` rồi `window.location.assign(order.paymentUrl)` THAY VÌ `router.push('/profile/orders/'+order.id)`. Giữ nguyên cart cleanup logic (~lines 209-219). non-VNPAY giữ hành vi cũ.
  </action>
  <verify>
    <automated>cd sources/frontend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "paymentStatus|vnpTransactionNo|paymentUrl" types/index.ts` — cả 3 field xuất hiện trong interface Order
    - `grep "VNPAY: 'Thanh toán qua VNPay'" lib/orderLabels.ts` khớp
    - `grep "VNPAY" checkout/page.tsx` khớp (mảng option + type union)
    - `grep "window.location" checkout/page.tsx` khớp
    - `grep "Đang chuyển tới cổng thanh toán VNPay" checkout/page.tsx` khớp (copy đúng UI-SPEC)
    - `npx tsc --noEmit` clean
  </acceptance_criteria>
  <done>Type Order mở rộng; selector có option VNPay; đặt hàng VNPAY redirect sang paymentUrl.</done>
</task>

<task type="auto">
  <name>Task 2: Trang kết quả /checkout/result + polling + order display</name>
  <read_first>
    - sources/frontend/src/app/profile/orders/[id]/page.tsx (analog state machine + load + render paymentMethodMap/paymentStatusMap)
    - sources/frontend/src/app/admin/orders/[id]/page.tsx (order display admin — file sửa thêm payment fields)
    - sources/frontend/src/services/orders.ts (getOrderById dùng cho polling)
    - sources/frontend/src/components/ui/Banner.tsx + Button.tsx + RetrySection.tsx
    - .planning/phases/26-vnpay-payment-integration/26-UI-SPEC.md §Interaction Contract + §Copywriting Contract + §Order display
    - .planning/phases/26-vnpay-payment-integration/26-PATTERNS.md §checkout/result/page.tsx
  </read_first>
  <action>
    Tạo route mới `src/app/checkout/result/page.tsx` — `'use client'`:
    - Bọc nội dung trong `<Suspense>` (vì dùng `useSearchParams` — pattern checkout/page.tsx ~lines 530-537).
    - Đọc query string: `vnp_ResponseCode`, `vnp_TxnRef` (= paymentSessionId, KHÔNG phải orderId).
    - **Resolve orderId (cơ chế đã chốt — KHÔNG để implementer tự quyết):** gọi `GET /api/payments/vnpay/return` của payment-service với TOÀN BỘ query string VNPay redirect về (forward nguyên `window.location.search`). Endpoint này (Plan 01 Task 2 `buildReturnView`) trả `{ valid, orderId, responseCode, vnpTransactionNo }`. Lấy `orderId` từ response đó làm input cho polling. Thêm hàm `getVNPayReturn(searchParams)` vào `src/services/payments.ts` gọi endpoint return (endpoint này public — không cần JWT, đã whitelist Plan 01 Task 3).
    - Nếu response `valid=false` hoặc `orderId` null → render empty state (xem dưới).
    - Thiếu tham số bắt buộc → render empty state: heading `Không tìm thấy thông tin thanh toán` + body theo UI-SPEC §CTA empty.
    - Render sơ bộ từ `vnp_ResponseCode`: `00` → giả định chờ IPN (state "đang xác nhận"), `24` → huỷ, khác → thất bại.
    - Polling: `useEffect` + `setInterval` gọi `getOrderById(orderId)` mỗi **3000ms, tối đa 5 lần** (~15s). Dừng sớm khi `order.paymentStatus !== 'PENDING'`. Cleanup interval trong `useEffect` return.
    - 5 trạng thái render theo `26-UI-SPEC.md` §Copywriting Contract → §Trang kết quả thanh toán (copy VERBATIM 5 cặp heading/body — UI-SPEC đã liệt kê đủ 5: "Đang xác nhận thanh toán" / "Thanh toán thành công" / "Thanh toán thất bại" / "Đã huỷ thanh toán" / "Chưa nhận được xác nhận"). Lưu ý: D-12/ROADMAP nêu 3 trạng thái cơ bản (thành công/thất bại/huỷ); mở rộng thành 5 là HỆ QUẢ tất yếu của polling D-13 (thêm "đang xác nhận" trong lúc poll + "poll hết hạn vẫn PENDING"). UI-SPEC đã được checker duyệt với đủ 5 copy string — KHÔNG tự chế copy mới, đọc đúng từ UI-SPEC.
    - Components tái dụng: `Banner` (thông báo), `Button` (`variant="primary"` CTA "Xem đơn hàng" hoặc "Thử thanh toán lại", `variant="secondary"` "Về trang chủ"), `RetrySection` khi poll lỗi mạng. KHÔNG component/style mới.
    - Spinner + text trung tính trong lúc poll (màu `--on-surface-variant`).

    Sửa `src/app/profile/orders/[id]/page.tsx` — order display ĐÃ render `paymentMethodMap`/`paymentStatusMap` (~lines 234-240); thêm 1 dòng hiển thị `Mã giao dịch VNPay:` + `order.vnpTransactionNo` — ẩn dòng nếu rỗng (UI-SPEC §Order display).

    Sửa `src/app/admin/orders/[id]/page.tsx` tương tự — hiển thị payment method (`paymentMethodMap`), payment status (`paymentStatusMap` + `Badge` variant theo UI-SPEC §Color), mã giao dịch VNPay (ẩn nếu rỗng).

    Tạo Playwright smoke spec `tests/vnpay-payment.spec.ts` — stub: (1) checkout có option "Thanh toán qua VNPay"; (2) `/checkout/result?vnp_ResponseCode=00&vnp_TxnRef=...` render heading "Đang xác nhận thanh toán" (mock `getVNPayReturn` trả `orderId` hợp lệ). Reuse storageState pattern. Runtime defer cho /gsd-verify-work (precedent Phase 19/23 — ghi rõ SUMMARY).
  </action>
  <verify>
    <automated>cd sources/frontend && npx tsc --noEmit && npx playwright test vnpay-payment --list</automated>
  </verify>
  <acceptance_criteria>
    - File `src/app/checkout/result/page.tsx` tồn tại; `grep "vnp_ResponseCode" checkout/result/page.tsx` khớp
    - `grep "Suspense" checkout/result/page.tsx` khớp
    - `grep -E "3000|3 \* 1000" checkout/result/page.tsx` khớp (poll 3s); `grep -E "5" checkout/result/page.tsx` cho max attempts
    - `grep "Đang xác nhận thanh toán" checkout/result/page.tsx` khớp (copy UI-SPEC)
    - `grep "getOrderById" checkout/result/page.tsx` khớp
    - `grep "getVNPayReturn" checkout/result/page.tsx` khớp; `grep "vnpay/return" services/payments.ts` khớp (resolve orderId)
    - `grep "vnpTransactionNo" profile/orders/[id]/page.tsx` + `admin/orders/[id]/page.tsx` khớp
    - File `tests/vnpay-payment.spec.ts` tồn tại; `npx tsc --noEmit` clean
  </acceptance_criteria>
  <done>Trang /checkout/result poll payment_status 3s×5 với 5 trạng thái; order display hiển thị payment fields; Playwright spec stub sẵn sàng.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → return URL query string | vnp_* params từ redirect VNPay — KHÔNG tin để cập nhật DB |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-13 | Spoofing/Tampering | checkout/result/page.tsx vnp_ResponseCode | mitigate | vnp_ResponseCode CHỈ render sơ bộ; nguồn sự thật là payment_status poll qua GET /api/orders — user giả query string không đổi được trạng thái thật (D-06, Pitfall 5) |
| T-26-14 | Information Disclosure | order display vnpTransactionNo | accept | Mã giao dịch VNPay hiển thị cho chủ đơn (đã có ownership check getOrderForUser) + admin — không phải secret nhạy cảm |
</threat_model>

<verification>
- `npx tsc --noEmit` clean
- `npx playwright test vnpay-payment --list` liệt kê được spec
- Trang /checkout/result poll đúng 3s × 5
- Order display hiển thị payment fields ở cả profile + admin
</verification>

<success_criteria>
- Selector checkout có option VNPay → redirect paymentUrl
- /checkout/result hiển thị 5 trạng thái + poll payment_status
- /profile/orders/[id] + /admin/orders/[id] hiển thị payment method/status/mã giao dịch
</success_criteria>

<output>
After completion, create `.planning/phases/26-vnpay-payment-integration/26-04-SUMMARY.md`
</output>
