---
phase: 26-vnpay-payment-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sources/backend/payment-service/pom.xml
  - sources/backend/payment-service/src/main/java/com/ptit/htpt/paymentservice/vnpay/VNPayConfig.java
  - sources/backend/payment-service/src/main/java/com/ptit/htpt/paymentservice/vnpay/VNPaySignature.java
  - sources/backend/payment-service/src/main/java/com/ptit/htpt/paymentservice/vnpay/VNPayService.java
  - sources/backend/payment-service/src/main/java/com/ptit/htpt/paymentservice/vnpay/VNPayController.java
  - sources/backend/payment-service/src/main/java/com/ptit/htpt/paymentservice/messaging/config/RabbitMQConfig.java
  - sources/backend/payment-service/src/main/java/com/ptit/htpt/paymentservice/messaging/event/PaymentEventEnvelope.java
  - sources/backend/payment-service/src/main/java/com/ptit/htpt/paymentservice/messaging/publisher/PaymentEventPublisher.java
  - sources/backend/payment-service/src/main/java/com/ptit/htpt/paymentservice/messaging/tracing/TraceIdMessagePostProcessor.java
  - sources/backend/payment-service/src/main/java/com/ptit/htpt/paymentservice/service/PaymentCrudService.java
  - sources/backend/payment-service/src/main/resources/application.yml
  - sources/backend/payment-service/src/test/resources/application-test.yml
  - sources/backend/payment-service/src/test/java/com/ptit/htpt/paymentservice/vnpay/VNPaySignatureTest.java
  - sources/backend/payment-service/src/test/java/com/ptit/htpt/paymentservice/vnpay/VNPayIpnControllerIT.java
  - sources/backend/api-gateway/src/main/resources/application.yml
  - docker-compose.yml
autonomous: true
requirements: [PAY-01, PAY-03]
user_setup:
  - service: vnpay-sandbox
    why: "Tích hợp cổng thanh toán VNPay sandbox — cần merchant credentials"
    env_vars:
      - name: VNP_TMN_CODE
        source: "Đăng ký tài khoản sandbox tại https://sandbox.vnpayment.vn → mục Quản lý → Thông tin merchant"
      - name: VNP_HASH_SECRET
        source: "Cùng trang merchant sandbox.vnpayment.vn — chuỗi Secret Key"
      - name: VNP_PAY_URL
        source: "Cố định: https://sandbox.vnpayment.vn/paymentv2/vpcpay.html"
      - name: VNP_RETURN_URL
        source: "URL FE result page, vd http://localhost:3000/checkout/result"
      - name: VNP_IPN_URL
        source: "URL public gateway, vd https://<tunnel>/api/payments/vnpay/ipn"
    dashboard_config:
      - task: "Cấu hình IPN URL trỏ về gateway public của môi trường demo"
        location: "sandbox.vnpayment.vn → mục cấu hình merchant → IPN URL"

must_haves:
  truths:
    - "payment-service ký được payment URL VNPay với HMAC SHA512 đúng spec v2.1.0"
    - "payment-service verify được vnp_SecureHash của callback và từ chối chữ ký sai"
    - "POST /payments/sessions với provider=VNPAY trả về paymentUrl trỏ cổng sandbox"
    - "GET /payments/vnpay/ipn verify chữ ký, idempotent, trả JSON thuần {RspCode,Message}"
    - "VNPay IPN/return endpoint truy cập được qua gateway không cần JWT"
  artifacts:
    - path: "sources/backend/payment-service/src/main/java/com/ptit/htpt/paymentservice/vnpay/VNPaySignature.java"
      provides: "HMAC SHA512 ký + verify"
      contains: "HmacSHA512"
    - path: "sources/backend/payment-service/src/main/java/com/ptit/htpt/paymentservice/vnpay/VNPayController.java"
      provides: "GET /payments/vnpay/ipn + /return"
      contains: "vnpay/ipn"
    - path: "sources/backend/payment-service/src/main/java/com/ptit/htpt/paymentservice/messaging/config/RabbitMQConfig.java"
      provides: "Exchange payment.events + DLX + DLQ"
      contains: "payment.events"
  key_links:
    - from: "VNPayController.ipn"
      to: "PaymentEventPublisher.publishPaymentEvent"
      via: "phát PaymentSucceeded/PaymentFailed sau verify"
      pattern: "publishPaymentEvent"
    - from: "api-gateway application.yml"
      to: "/api/payments/vnpay/ipn"
      via: "public-endpoints whitelist"
      pattern: "vnpay/ipn"
---

<objective>
Xây toàn bộ logic VNPay trong `payment-service`: ký/verify HMAC SHA512, build payment URL, xử lý IPN/return callback idempotent, phát event RabbitMQ `PaymentSucceeded`/`PaymentFailed`. Whitelist 2 endpoint VNPay qua api-gateway để VNPay (bên ngoài, không gửi JWT) gọi được.

Purpose: `payment-service` sở hữu TOÀN BỘ logic VNPay (D-01) — đây là nền tảng để order-service (Plan 03) tạo session và consume event.
Output: payment-service có module `vnpay/` + `messaging/`, endpoint `/payments/vnpay/ipn` + `/return`, exchange `payment.events`; gateway whitelist 2 endpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-vnpay-payment-integration/26-CONTEXT.md
@.planning/phases/26-vnpay-payment-integration/26-RESEARCH.md
@.planning/phases/26-vnpay-payment-integration/26-PATTERNS.md

<interfaces>
<!-- Analog files — copy pattern verbatim, đổi namespace sang payment.* -->
Messaging analog (order-service — copy nguyên cấu trúc):
- messaging/config/RabbitMQConfig.java — TopicExchange + DLX + DLQ + RabbitTemplate setMandatory(true) + Jackson2JsonMessageConverter
- messaging/event/OrderEventEnvelope.java — record(eventId, eventType, occurredAt, traceId, payload) + factory of()
- messaging/publisher/OrderEventPublisher.java — afterCommit defer + CorrelationData 5s confirm + capture MDC traceId
- messaging/tracing/TraceIdMessagePostProcessor.java — MessagePostProcessor set header X-Trace-Id

VNPay HMAC code (RESEARCH §Code Examples lines 282-351) — copy buildPaymentUrl + verifySignature + hmacSHA512 verbatim.

ApiResponseAdvice bypass: payment-service `api/ApiResponseAdvice.java` có `SKIP_PREFIXES` set + `supports()` loại `CharSequence`. VNPay IPN cần JSON thuần {RspCode,Message}.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: pom amqp + VNPayConfig + VNPaySignature + Wave 0 test ký/verify HMAC</name>
  <read_first>
    - sources/backend/payment-service/pom.xml (file sửa)
    - sources/backend/order-service/pom.xml (analog — block spring-boot-starter-amqp + spring-rabbit-test + testcontainers:rabbitmq đã có)
    - sources/backend/payment-service/src/main/resources/application.yml (đọc style ${ENV:default} placeholder)
    - .planning/phases/26-vnpay-payment-integration/26-RESEARCH.md §Code Examples lines 282-351 (buildPaymentUrl + verifySignature + hmacSHA512)
  </read_first>
  <behavior>
    - Test: ký một map params cố định → verifySignature(params + computed hash) trả true
    - Test: verifySignature với hash bị sửa 1 ký tự → trả false
    - Test: verifySignature loại vnp_SecureHash + vnp_SecureHashType khỏi tập ký trước khi tính lại
    - Test: buildPaymentUrl đặt vnp_Amount = amountVnd × 100 (không phải amountVnd)
    - Test: buildPaymentUrl chuỗi hash dùng URLEncoder.encode(v, StandardCharsets.US_ASCII) cho cả key và value
  </behavior>
  <action>
    Thêm vào `payment-service/pom.xml` 3 dependency (KHÔNG pin version — parent BOM 3.3.2 manage): `org.springframework.boot:spring-boot-starter-amqp`; test scope `org.springframework.amqp:spring-rabbit-test` + `org.testcontainers:rabbitmq`.

    Tạo `vnpay/VNPayConfig.java` — `@ConfigurationProperties(prefix="vnpay")` record với fields `tmnCode`, `hashSecret`, `payUrl`, `returnUrl`, `ipnUrl` (tất cả String). Đăng ký qua `@EnableConfigurationProperties(VNPayConfig.class)` trên `PaymentServiceApplication`. Thêm block vào `application.yml`:
    ```yaml
    vnpay:
      tmn-code: ${VNP_TMN_CODE:}
      hash-secret: ${VNP_HASH_SECRET:}
      pay-url: ${VNP_PAY_URL:https://sandbox.vnpayment.vn/paymentv2/vpcpay.html}
      return-url: ${VNP_RETURN_URL:http://localhost:3000/checkout/result}
      ipn-url: ${VNP_IPN_URL:}
    ```
    KHÔNG hardcode secret, KHÔNG log `hashSecret`.

    Tạo `vnpay/VNPaySignature.java` — copy `buildPaymentUrl(String txnRef, long amountVnd, String orderInfo, String clientIp)` + `verifySignature(Map<String,String> params)` + `hmacSHA512(String key, String data)` verbatim từ RESEARCH §Code Examples. Dùng `TreeMap` để sort key tăng dần; `URLEncoder.encode(v, StandardCharsets.US_ASCII)` cho CẢ ký lẫn verify; `vnp_Amount = amountVnd * 100`; verify loại `vnp_SecureHash` + `vnp_SecureHashType`; so sánh `equalsIgnoreCase`. Nhận `VNPayConfig` injected qua constructor (KHÔNG Lombok — project convention).

    Tạo Wave 0 test `src/test/java/com/ptit/htpt/paymentservice/vnpay/VNPaySignatureTest.java` — `@SpringBootTest` HOẶC plain unit test với `VNPayConfig` tạo thủ công (hash-secret cố định vd "SANDBOXSECRETKEY"). Implement 5 test trong `<behavior>`.
  </action>
  <verify>
    <automated>mvn -q test -pl payment-service -Dtest=VNPaySignatureTest</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "spring-boot-starter-amqp" payment-service/pom.xml` ≥ 1
    - `grep "HmacSHA512" VNPaySignature.java` khớp
    - `grep "amountVnd \* 100" VNPaySignature.java` khớp (vnp_Amount × 100)
    - `grep "US_ASCII" VNPaySignature.java` xuất hiện trong cả buildPaymentUrl và verifySignature
    - `grep -E "vnp_SecureHashType" VNPaySignature.java` khớp (loại khỏi verify)
    - VNPaySignatureTest có ≥ 5 `@Test`, `mvn test -Dtest=VNPaySignatureTest` xanh (hoặc compile-clean nếu Maven defer Windows — ghi rõ trong SUMMARY)
  </acceptance_criteria>
  <done>VNPaySignature ký + verify HMAC SHA512 đúng spec; pom có amqp; VNPaySignatureTest 5 test pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: messaging module payment.events + VNPayService + VNPayController IPN/return</name>
  <read_first>
    - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/messaging/config/RabbitMQConfig.java (analog exact)
    - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/messaging/event/OrderEventEnvelope.java (analog exact)
    - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/messaging/publisher/OrderEventPublisher.java (analog exact)
    - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/messaging/tracing/TraceIdMessagePostProcessor.java (analog)
    - sources/backend/payment-service/src/main/java/com/ptit/htpt/paymentservice/web/PaymentController.java (analog controller shape)
    - sources/backend/payment-service/src/main/java/com/ptit/htpt/paymentservice/api/ApiResponseAdvice.java (đọc SKIP_PREFIXES + supports())
    - sources/backend/payment-service/src/main/java/com/ptit/htpt/paymentservice/domain/PaymentTransactionEntity.java + PaymentSessionEntity.java
    - .planning/phases/26-vnpay-payment-integration/26-PATTERNS.md §VNPayController + §PaymentEventEnvelope
  </read_first>
  <behavior>
    - Test (IT): GET /payments/vnpay/ipn với chữ ký hợp lệ + vnp_ResponseCode=00 + amount khớp → trả {"RspCode":"00",...}, transaction chuyển PAID, publish PaymentSucceeded
    - Test (IT): IPN gọi lại lần 2 cùng giao dịch (transaction đã PAID) → trả {"RspCode":"02",...}, KHÔNG update lại, KHÔNG publish lại
    - Test (IT): IPN với chữ ký sai → trả {"RspCode":"97",...}, KHÔNG update transaction
    - Test (IT): IPN với vnp_Amount lệch số tiền đơn → trả {"RspCode":"04",...}, log audit, KHÔNG PAID
    - Test (IT): response body là JSON thuần {"RspCode","Message"} — KHÔNG bị ApiResponse envelope bọc
  </behavior>
  <action>
    Tạo `messaging/event/PaymentEventEnvelope.java` — record `(String eventId, String eventType, String occurredAt, String traceId, PaymentPayload payload)` + factory `of(eventType, traceId, payload)` sinh `UUID.randomUUID()` + `Instant.now().toString()`. Nested `record PaymentPayload(String orderId, String paymentSessionId, String vnpTransactionNo, BigDecimal amount, String currency)`. `eventType` ∈ {"PaymentSucceeded","PaymentFailed"}.

    Tạo `messaging/config/RabbitMQConfig.java` — copy analog order-service. Constants: `EXCHANGE="payment.events"`, `DLX="payment.dlx"`, `DLQ="payment-events.dlq"`, `ROUTING_KEY_SUCCEEDED="payment.succeeded"`, `ROUTING_KEY_FAILED="payment.failed"`. Beans: `TopicExchange paymentEventsExchange` durable; `DirectExchange paymentDlx`; `Queue paymentDlq`; binding DLQ↔DLX; `RabbitTemplate` với `setMandatory(true)` + `Jackson2JsonMessageConverter`. KHÔNG reuse `order.events` (anti-pattern RESEARCH §Pattern 3).

    Tạo `messaging/tracing/TraceIdMessagePostProcessor.java` — copy analog (set header `X-Trace-Id`).

    Tạo `messaging/publisher/PaymentEventPublisher.java` — copy `OrderEventPublisher` verbatim, đổi tên method `publishPaymentEvent(String eventType, PaymentEventEnvelope.PaymentPayload payload)`: capture `MDC.get("traceId")` NGAY tại caller thread; `TransactionSynchronizationManager` afterCommit defer; `doPublish` với `CorrelationData` + `getFuture().get(5000, MILLISECONDS)`; routing key chọn theo eventType (`payment.succeeded`/`payment.failed`); log contract `[MQ-PUB]`/`[MQ-PUB-NACK]`/`[MQ-PUB-TIMEOUT]`. Constructor injection (KHÔNG Lombok).

    Tạo `vnpay/VNPayService.java` — `@Service`, constructor inject `VNPaySignature`, `PaymentSessionRepository`, `PaymentTransactionRepository`, `PaymentEventPublisher`. Method `processIpn(Map<String,String> params)`:
    1. `verifySignature` false → return `Map.of("RspCode","97","Message","Invalid signature")` + log audit.
    2. Load `PaymentTransactionEntity`/`PaymentSessionEntity` theo `vnp_TxnRef` (= paymentSessionId). Không tìm thấy → `Map.of("RspCode","01","Message","Order not found")`.
    3. So khớp `Long.parseLong(vnp_Amount)` với `session.amount × 100` — lệch → `Map.of("RspCode","04","Message","Invalid amount")` + log audit (D-08), KHÔNG PAID.
    4. Idempotent: transaction status đã terminal (PAID/FAILED) → `Map.of("RspCode","02","Message","Order already confirmed")`, KHÔNG update, KHÔNG publish (Pitfall 3).
    5. `vnp_ResponseCode=="00"` → set transaction PAID + lưu `vnp_TransactionNo` → `@Transactional` save → publish `PaymentSucceeded` (afterCommit) → `Map.of("RspCode","00","Message","Confirm Success")`. Khác "00" → set FAILED → publish `PaymentFailed` → vẫn `RspCode 00` (đã ghi nhận).
    Method `buildReturnView(Map<String,String> params)`: CHỈ `verifySignature` để FE hiển thị — KHÔNG update DB, KHÔNG publish (D-06, Pitfall 5).

    Tạo `vnpay/VNPayController.java` — `@RestController @RequestMapping("/payments")`, constructor inject `VNPayService`. `@GetMapping("/vnpay/ipn")` nhận `@RequestParam Map<String,String> params` → `vnPayService.processIpn(params)` trả `Map<String,String>`. `@GetMapping("/vnpay/return")` → trả thông tin verify cho FE. BYPASS `ApiResponseAdvice`: thêm prefix `/payments/vnpay` vào `SKIP_PREFIXES` set của `ApiResponseAdvice.java` (cách verified trong PATTERNS.md) — sửa file `ApiResponseAdvice.java`.
  </action>
  <verify>
    <automated>mvn -q test -pl payment-service -Dtest=VNPayIpnControllerIT</automated>
  </verify>
  <acceptance_criteria>
    - `grep "payment.events" messaging/config/RabbitMQConfig.java` khớp; KHÔNG có chuỗi `order.events` trong file
    - `grep -E "RspCode.*02" VNPayService.java` khớp (idempotent terminal)
    - `grep -E "RspCode.*97" VNPayService.java` khớp (chữ ký sai)
    - `grep -E "RspCode.*04" VNPayService.java` khớp (amount lệch)
    - `grep "publishPaymentEvent" VNPayService.java` khớp
    - `grep "/payments/vnpay" ApiResponseAdvice.java` khớp (SKIP_PREFIXES)
    - VNPayIpnControllerIT có ≥ 5 `@Test`; chạy xanh hoặc compile-clean (Maven defer Windows ghi rõ SUMMARY)
  </acceptance_criteria>
  <done>IPN verify chữ ký + idempotent + amount-match; phát PaymentSucceeded/Failed; response JSON thuần; VNPayIpnControllerIT 5 test pass.</done>
</task>

<task type="auto">
  <name>Task 3: PaymentCrudService VNPAY session + gateway whitelist + docker-compose env + test config</name>
  <read_first>
    - sources/backend/payment-service/src/main/java/com/ptit/htpt/paymentservice/service/PaymentCrudService.java (file sửa — mở rộng createSession)
    - sources/backend/payment-service/src/main/java/com/ptit/htpt/paymentservice/web/PaymentController.java (POST /payments/sessions)
    - sources/backend/payment-service/src/main/java/com/ptit/htpt/paymentservice/domain/PaymentSessionEntity.java
    - sources/backend/api-gateway/src/main/resources/application.yml (app.auth.public-endpoints — format { method, pattern })
    - docker-compose.yml (block env của payment-service)
    - sources/backend/order-service/src/test/resources/application-test.yml (analog Testcontainers config)
  </read_first>
  <action>
    Mở rộng `PaymentCrudService.createSession` (hoặc method tạo session): khi request có `provider=="VNPAY"` → sau khi tạo `PaymentSessionEntity` (status PENDING, lưu orderId + amount), gọi `VNPaySignature.buildPaymentUrl(sessionId, amountVnd, orderInfo, clientIp)` với `vnp_TxnRef = sessionId` (UUID session — cho phép retry, Pitfall 6), trả `paymentUrl` trong response DTO. `orderInfo` chứa mã đơn. Provider khác → giữ nguyên hành vi cũ. Đảm bảo `POST /payments/sessions` response chứa field `paymentUrl` (nullable — chỉ có với VNPAY). Inject `VNPaySignature` qua constructor.

    Sửa `api-gateway/src/main/resources/application.yml` — thêm 2 entry vào `app.auth.public-endpoints` (đúng format hiện có):
    ```yaml
    - { method: GET, pattern: "/api/payments/vnpay/ipn" }
    - { method: GET, pattern: "/api/payments/vnpay/return" }
    ```

    Sửa `docker-compose.yml` — thêm vào block `environment` của service `payment-service`: `VNP_TMN_CODE`, `VNP_HASH_SECRET`, `VNP_PAY_URL`, `VNP_RETURN_URL`, `VNP_IPN_URL` (dạng `${VNP_TMN_CODE}` đọc từ .env) + `SPRING_RABBITMQ_HOST/USER/PASS` (nếu chưa có — payment-service nay là producer). Thêm `depends_on: rabbitmq: condition: service_healthy`.

    Tạo `payment-service/src/test/resources/application-test.yml` — copy analog order-service: datasource Testcontainers Postgres + `spring.rabbitmq` block với `publisher-confirm-type: correlated` + `publisher-returns: true`; `ddl-auto: validate`; flyway enabled. Thêm block `vnpay` với hash-secret cố định để test.
  </action>
  <verify>
    <automated>mvn -q test -pl payment-service</automated>
  </verify>
  <acceptance_criteria>
    - `grep "vnpay/ipn" api-gateway/src/main/resources/application.yml` khớp
    - `grep "vnpay/return" api-gateway/src/main/resources/application.yml` khớp
    - `grep "VNP_HASH_SECRET" docker-compose.yml` khớp
    - `grep -E "provider.*VNPAY|VNPAY" PaymentCrudService.java` khớp
    - `grep "buildPaymentUrl" PaymentCrudService.java` khớp
    - File `payment-service/src/test/resources/application-test.yml` tồn tại, chứa `publisher-confirm-type`
  </acceptance_criteria>
  <done>POST /payments/sessions provider=VNPAY trả paymentUrl; gateway whitelist 2 endpoint; docker-compose có env VNPay; test config sẵn sàng.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| VNPay → payment-service (IPN/return) | Input không tin cậy từ internet, không có JWT — chỉ HMAC verify |
| api-gateway → payment-service | Gateway forward request VNPay tới payment-service không qua JWT filter (whitelist) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01 | Tampering | VNPaySignature.verifySignature | mitigate | Verify HMAC SHA512, loại vnp_SecureHash + vnp_SecureHashType trước khi tính lại, so sánh equalsIgnoreCase; chữ ký sai → RspCode 97, KHÔNG update |
| T-26-02 | Tampering | VNPayService.processIpn amount check | mitigate | So khớp Long.parseLong(vnp_Amount) với session.amount × 100; lệch → RspCode 04 + log audit, KHÔNG PAID (D-08) |
| T-26-03 | Tampering (replay) | VNPayService.processIpn idempotency | mitigate | Transaction status terminal → RspCode 02, KHÔNG update + KHÔNG publish lại (D-07, Pitfall 3) |
| T-26-04 | Spoofing | VNPayController.buildReturnView | mitigate | Return URL CHỈ verify để hiển thị, KHÔNG update DB — IPN là nguồn sự thật duy nhất (D-06, Pitfall 5) |
| T-26-05 | Information Disclosure | VNPayConfig hashSecret | mitigate | Đọc từ env var, KHÔNG hardcode, KHÔNG log hashSecret |
| T-26-06 | DoS/Spoofing | /payments/vnpay/ipn public endpoint | accept | HMAC verify reject sớm chữ ký sai; demo scope không có rate-limit — chấp nhận, log audit chữ ký sai |
</threat_model>

<verification>
- `mvn -q test -pl payment-service` xanh (hoặc compile-clean nếu Maven defer Windows env — ghi rõ SUMMARY)
- VNPaySignatureTest ≥ 5 test, VNPayIpnControllerIT ≥ 5 test
- payment-service không còn chuỗi `order.events` trong messaging/config
- gateway application.yml whitelist 2 endpoint VNPay
</verification>

<success_criteria>
- payment-service ký/verify HMAC SHA512 đúng spec VNPay v2.1.0
- POST /payments/sessions provider=VNPAY trả paymentUrl
- GET /payments/vnpay/ipn idempotent, verify chữ ký, phát event, trả JSON thuần {RspCode,Message}
- VNPay IPN/return truy cập được qua gateway không cần JWT
</success_criteria>

<output>
After completion, create `.planning/phases/26-vnpay-payment-integration/26-01-SUMMARY.md`
</output>
