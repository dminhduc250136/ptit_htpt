---
phase: 26-vnpay-payment-integration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - sources/backend/order-service/src/main/resources/db/migration/V6__add_payment_status.sql
  - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/domain/OrderEntity.java
  - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/domain/OrderDto.java
  - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/domain/OrderMapper.java
  - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/domain/ProcessedEventEntity.java
  - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/repository/ProcessedEventRepository.java
  - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/messaging/tracing/TraceIdConsumerInterceptor.java
autonomous: true
requirements: [PAY-04]
must_haves:
  truths:
    - "OrderEntity có cột payment_status (default PENDING) + vnp_transaction_no (nullable)"
    - "OrderDto expose paymentStatus, vnpTransactionNo, paymentUrl (paymentUrl nullable transient)"
    - "order-service có hạ tầng idempotency processed_events sẵn cho consumer (Plan 03)"
  artifacts:
    - path: "sources/backend/order-service/src/main/resources/db/migration/V6__add_payment_status.sql"
      provides: "Cột payment_status + vnp_transaction_no + bảng processed_events"
      contains: "payment_status"
    - path: "sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/repository/ProcessedEventRepository.java"
      provides: "insertIfAbsent idempotency"
      contains: "ON CONFLICT"
  key_links:
    - from: "OrderMapper"
      to: "OrderDto.paymentStatus"
      via: "map entity → dto"
      pattern: "paymentStatus"
---

<objective>
Đặt nền tảng dữ liệu order-service cho VNPay: Flyway V6 thêm cột `payment_status` + `vnp_transaction_no` vào bảng `orders` + tạo bảng `processed_events`; mở rộng `OrderEntity`/`OrderDto`/`OrderMapper`; port hạ tầng idempotency `ProcessedEventEntity`/`Repository` + `TraceIdConsumerInterceptor` từ inventory-service.

Purpose: order-service sở hữu cột `payment_status` (D-02) và cần hạ tầng consumer (order-service chưa từng có consumer). Plan 03 dùng các artifact này để consume payment event.
Output: V6 migration + entity/dto/mapper mở rộng + processed_events infra.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-vnpay-payment-integration/26-CONTEXT.md
@.planning/phases/26-vnpay-payment-integration/26-RESEARCH.md
@.planning/phases/26-vnpay-payment-integration/26-PATTERNS.md

<interfaces>
<!-- Analog files để copy pattern -->
order-service migrations hiện có: V1, V2, V4, V5 → migration mới là V6 (verified ls db/migration).
Analog migration: V5__add_coupons.sql — pattern `ALTER TABLE orders ADD COLUMN IF NOT EXISTS`.
OrderEntity pattern cột `couponCode` (nullable) + `discountAmount` (default) — copy style cho cột mới.
Idempotency infra (copy verbatim, đổi package com.ptit.htpt.inventoryservice → com.ptit.htpt.orderservice):
- inventory-service domain/ProcessedEventEntity.java
- inventory-service repository/ProcessedEventRepository.java (insertIfAbsent ON CONFLICT DO NOTHING)
- inventory-service messaging/tracing/TraceIdConsumerInterceptor.java
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flyway V6 migration — cột payment + bảng processed_events</name>
  <read_first>
    - sources/backend/order-service/src/main/resources/db/migration/V5__add_coupons.sql (analog ALTER TABLE)
    - sources/backend/inventory-service/src/main/java/com/ptit/htpt/inventoryservice/domain/ProcessedEventEntity.java (đọc schema cột processed_events)
    - .planning/phases/26-vnpay-payment-integration/26-PATTERNS.md §V6 migration
  </read_first>
  <action>
    Tạo `sources/backend/order-service/src/main/resources/db/migration/V6__add_payment_status.sql`:
    ```sql
    -- order-svc V6 / PAY-04 (D-02): payment_status + vnp_transaction_no cho VNPay
    ALTER TABLE orders ADD COLUMN IF NOT EXISTS payment_status VARCHAR(20) NOT NULL DEFAULT 'PENDING';
    ALTER TABLE orders ADD COLUMN IF NOT EXISTS vnp_transaction_no VARCHAR(50);

    -- processed_events cho PaymentEventListener idempotency (pattern inventory-service)
    CREATE TABLE IF NOT EXISTS processed_events (
      event_id     VARCHAR(36) PRIMARY KEY,
      event_type   VARCHAR(64) NOT NULL,
      processed_at TIMESTAMPTZ NOT NULL DEFAULT now()
    );
    ```
    Lưu ý: order COD cũ nhận `payment_status='PENDING'` mặc định (RESEARCH Open Q #4 — planner chốt giữ default PENDING, không backfill theo order.status; không ảnh hưởng scope VNPay).
  </action>
  <verify>
    <automated>mvn -q test -pl order-service -Dtest=*MigrationIT,*FlywayIT</automated>
  </verify>
  <acceptance_criteria>
    - File `V6__add_payment_status.sql` tồn tại (đúng tên — V6, không phải V3/V7)
    - `grep "payment_status VARCHAR(20) NOT NULL DEFAULT 'PENDING'" V6__add_payment_status.sql` khớp
    - `grep "vnp_transaction_no VARCHAR(50)" V6__add_payment_status.sql` khớp
    - `grep "CREATE TABLE IF NOT EXISTS processed_events" V6__add_payment_status.sql` khớp
    - `grep -c "event_id     VARCHAR(36) PRIMARY KEY" V6__add_payment_status.sql` = 1
  </acceptance_criteria>
  <done>V6 migration thêm 2 cột vào orders + tạo processed_events; Flyway validate pass.</done>
</task>

<task type="auto">
  <name>Task 2: OrderEntity + OrderDto + OrderMapper mở rộng + port idempotency infra</name>
  <read_first>
    - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/domain/OrderEntity.java (file sửa — pattern cột couponCode/discountAmount)
    - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/domain/OrderDto.java (file sửa)
    - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/domain/OrderMapper.java (file sửa)
    - sources/backend/inventory-service/src/main/java/com/ptit/htpt/inventoryservice/domain/ProcessedEventEntity.java (copy verbatim)
    - sources/backend/inventory-service/src/main/java/com/ptit/htpt/inventoryservice/repository/ProcessedEventRepository.java (copy verbatim)
    - sources/backend/inventory-service/src/main/java/com/ptit/htpt/inventoryservice/messaging/tracing/TraceIdConsumerInterceptor.java (copy verbatim)
  </read_first>
  <action>
    Sửa `OrderEntity.java` — thêm 2 field theo pattern cột `couponCode`/`discountAmount`:
    ```java
    @Column(name = "payment_status", nullable = false, length = 20)
    private String paymentStatus = "PENDING";

    @Column(name = "vnp_transaction_no", length = 50)
    private String vnpTransactionNo;
    ```
    Thêm getter record-style (`paymentStatus()`, `vnpTransactionNo()`) + setter (pattern setter `couponCode`) — `setPaymentStatus(String)` + `setVnpTransactionNo(String)` (bump `updatedAt` như pattern `setTotal` Phase 20). KHÔNG Lombok.

    Sửa `OrderDto.java` — thêm 3 field: `paymentStatus` (String), `vnpTransactionNo` (String nullable), `paymentUrl` (String nullable — transient, KHÔNG map từ entity, set thủ công trong OrderCrudService nhánh VNPAY ở Plan 03).

    Sửa `OrderMapper.java` — map `entity.paymentStatus()` → `dto.paymentStatus`, `entity.vnpTransactionNo()` → `dto.vnpTransactionNo`. `paymentUrl` KHÔNG map (để null mặc định — Plan 03 set sau).

    Port idempotency infra từ inventory-service (copy verbatim, đổi package `com.ptit.htpt.inventoryservice` → `com.ptit.htpt.orderservice`, sửa import):
    - `domain/ProcessedEventEntity.java`
    - `repository/ProcessedEventRepository.java` (giữ nguyên `insertIfAbsent` native `ON CONFLICT (event_id) DO NOTHING`)
    - `messaging/tracing/TraceIdConsumerInterceptor.java`
  </action>
  <verify>
    <automated>mvn -q test -pl order-service -Dtest=OrderMapper*</automated>
  </verify>
  <acceptance_criteria>
    - `grep "payment_status" OrderEntity.java` + `grep "vnp_transaction_no" OrderEntity.java` khớp
    - `grep -E "paymentStatus|vnpTransactionNo|paymentUrl" OrderDto.java` — cả 3 field xuất hiện
    - `grep "paymentStatus" OrderMapper.java` khớp
    - File `order-service/.../repository/ProcessedEventRepository.java` tồn tại, `grep "ON CONFLICT" ProcessedEventRepository.java` khớp
    - File `order-service/.../domain/ProcessedEventEntity.java` + `messaging/tracing/TraceIdConsumerInterceptor.java` tồn tại với package `com.ptit.htpt.orderservice`
    - `mvn compile -pl order-service` clean (hoặc compile-clean ghi rõ SUMMARY nếu Maven defer)
  </acceptance_criteria>
  <done>OrderEntity/OrderDto/OrderMapper expose payment fields; processed_events infra port xong.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Flyway migration → DB schema | Migration chạy 1 lần, validate ddl |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-07 | Tampering | V6 migration ddl-auto | mitigate | order-service dùng `ddl-auto=validate` (verified Phase 23 pattern) — chỉ Flyway tạo schema, JPA validate match |
| T-26-08 | Tampering | OrderDto.paymentStatus exposure | accept | paymentStatus chỉ đọc — client KHÔNG set được payment_status (chỉ IPN consumer cập nhật, Plan 03); accept vì DTO read-only field |
</threat_model>

<verification>
- `mvn compile -pl order-service` clean
- V6 migration đúng tên + nội dung
- processed_events infra port đủ 3 file với package order-service
</verification>

<success_criteria>
- OrderEntity có payment_status + vnp_transaction_no
- OrderDto expose paymentStatus + vnpTransactionNo + paymentUrl
- order-service có ProcessedEventRepository sẵn cho consumer Plan 03
</success_criteria>

<output>
After completion, create `.planning/phases/26-vnpay-payment-integration/26-02-SUMMARY.md`
</output>
