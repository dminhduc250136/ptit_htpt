---
phase: 20
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - sources/backend/api-gateway/src/main/resources/application.yml
  - .planning/ROADMAP.md
autonomous: true
requirements: [COUP-02, COUP-03]
tags: [gateway, routes, roadmap, infra]

must_haves:
  truths:
    - "Gateway có 2 route mới: `/api/orders/coupons/**` (user preview) và `/api/orders/admin/coupons/**` (admin CRUD), KHÔNG đụng route `/api/orders/admin/**` đã tồn tại"
    - "Route admin coupons match TRƯỚC route admin orders catch-all để rewrite chính xác `/admin/coupons/{...}`"
    - "Route user coupons match TRƯỚC route order-service catch-all để rewrite chính xác `/orders/coupons/validate`"
    - "ROADMAP.md Pre-Phase Setup table cập nhật: order-svc V3 → V5 cho coupons (D-01); KHÔNG còn nhắc V3 reservation"
    - "Phase 20 trong ROADMAP.md có mục Plans liệt kê 6 plans (20-01..20-06) thay vì placeholder cũ (17-01..17-04)"
  artifacts:
    - path: "sources/backend/api-gateway/src/main/resources/application.yml"
      provides: "+2 gateway routes (user coupons + admin coupons)"
      contains: "order-service-coupons"
    - path: ".planning/ROADMAP.md"
      provides: "Patched Pre-Phase Setup (V5) + Phase 20 Plans list"
      contains: "20-01-PLAN.md"
  key_links:
    - from: "Browser FE checkout `POST /api/orders/coupons/validate`"
      to: "order-svc `POST /orders/coupons/validate` CouponPreviewController"
      via: "gateway route `order-service-coupons-user` rewrite"
      pattern: "RewritePath=/api/orders/coupons"
    - from: "Browser admin `GET/POST/PUT/PATCH/DELETE /api/orders/admin/coupons[/{id}[/active]]`"
      to: "order-svc `/admin/coupons/...` AdminCouponController"
      via: "gateway route `order-service-admin-coupons` rewrite"
      pattern: "RewritePath=/api/orders/admin/coupons"
---

<objective>
Wire gateway routing cho 2 endpoint surface mới (user preview + admin CRUD) và patch ROADMAP.md để phản ánh đúng V5 reservation + 6 plans Phase 20.

Purpose: Plan 20-05 (FE checkout) + Plan 20-06 (FE admin) sẽ gọi qua `/api/orders/coupons/**` và `/api/orders/admin/coupons/**` — routes phải sẵn sàng. Plan này INDEPENDENT với 20-01/20-02/20-03 (chỉ touch gateway YAML + roadmap markdown), chạy parallel Wave 1.

Output: 2 gateway routes thêm vào YAML + ROADMAP.md cập nhật.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-coupons/20-CONTEXT.md

@sources/backend/api-gateway/src/main/resources/application.yml

<interfaces>
<!-- Gateway route ordering convention từ application.yml hiện tại -->

Specific routes BEFORE catch-all:
  user-service-auth (specific) BEFORE user-service-base (catch-all /api/users)
  product-service-admin BEFORE product-service-base
  order-service-admin BEFORE order-service-base

Path matching strategy: Spring Cloud Gateway predicate `Path=` evaluate routes theo thứ tự khai báo. Routes specific phải đứng TRƯỚC routes catch-all.

Rewrite filter syntax:
  RewritePath=<incoming-regex>, <target>
  Regex named group: (?<seg>.*)
  Pattern existing cho admin: `RewritePath=/api/orders/admin/(?<seg>.*), /admin/orders/${seg}`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Thêm 2 gateway routes user coupons + admin coupons vào application.yml</name>
  <files>
    sources/backend/api-gateway/src/main/resources/application.yml
  </files>

  <read_first>
    - sources/backend/api-gateway/src/main/resources/application.yml (toàn bộ — đặc biệt section order-service routes lines 112–138)
    - 20-CONTEXT.md D-13 (path user `/api/orders/coupons/validate`), D-14 (path admin `/api/orders/admin/coupons[/...]`), D-15 (gateway routes spec)
  </read_first>

  <action>
    Mở `sources/backend/api-gateway/src/main/resources/application.yml`. Tìm section `# order-service admin: /api/orders/admin → /admin/orders` (khoảng line 112–124). 

    **CRITICAL ordering:** Cả 2 route MỚI phải đứng TRƯỚC route catch-all `order-service-admin` (id `order-service-admin`, line 119–124) và TRƯỚC `order-service` catch-all (line 133–138). Vì:
    - `order-service-admin-coupons` cần match `/api/orders/admin/coupons/**` TRƯỚC khi `order-service-admin` (Path=/api/orders/admin/**) match.
    - `order-service-coupons-user` cần match `/api/orders/coupons/**` TRƯỚC khi `order-service` (Path=/api/orders/**) match.

    Cấu trúc thêm vào (chèn TRƯỚC route `order-service-admin-base` ở khoảng line 113):

    ```yaml
            # order-service coupons admin: /api/orders/admin/coupons → /admin/coupons (D-15)
            - id: order-service-admin-coupons-base
              uri: http://order-service:8080
              predicates:
                - Path=/api/orders/admin/coupons
              filters:
                - RewritePath=/api/orders/admin/coupons, /admin/coupons
            - id: order-service-admin-coupons
              uri: http://order-service:8080
              predicates:
                - Path=/api/orders/admin/coupons/**
              filters:
                - RewritePath=/api/orders/admin/coupons/(?<seg>.*), /admin/coupons/${seg}

            # order-service coupons user preview: /api/orders/coupons → /orders/coupons (D-15)
            - id: order-service-coupons-user-base
              uri: http://order-service:8080
              predicates:
                - Path=/api/orders/coupons
              filters:
                - RewritePath=/api/orders/coupons, /orders/coupons
            - id: order-service-coupons-user
              uri: http://order-service:8080
              predicates:
                - Path=/api/orders/coupons/**
              filters:
                - RewritePath=/api/orders/coupons/(?<seg>.*), /orders/coupons/${seg}

            # order-service admin: /api/orders/admin → /admin/orders (existing — KHÔNG SỬA, chỉ chèn 4 route ở trên TRƯỚC nó)
    ```

    **Verification chính xác thứ tự:**
    1. `order-service-admin-coupons-base` (specific path)
    2. `order-service-admin-coupons` (specific path catch-all)
    3. `order-service-coupons-user-base` (specific path)
    4. `order-service-coupons-user` (specific path catch-all)
    5. `order-service-admin-base` (existing)
    6. `order-service-admin` (existing)
    7. `order-service-base` (existing)
    8. `order-service` (existing)

    Indentation: 8 spaces (vì lồng trong `routes:` ở 6 spaces). Match đúng existing routes.
  </action>

  <verify>
    <automated>cd sources/backend/api-gateway && mvn -q -DskipTests package</automated>
  </verify>

  <acceptance_criteria>
    - Grep `id: order-service-admin-coupons-base` trong application.yml → 1 match
    - Grep `id: order-service-admin-coupons$` (end of line) trong application.yml → 1 match
    - Grep `id: order-service-coupons-user-base` → 1 match
    - Grep `id: order-service-coupons-user$` → 1 match
    - Grep `RewritePath=/api/orders/admin/coupons/\(\?<seg>\.\*\), /admin/coupons/\$\{seg\}` → 1 match
    - Grep `RewritePath=/api/orders/coupons/\(\?<seg>\.\*\), /orders/coupons/\$\{seg\}` → 1 match
    - Order trong YAML: 4 route mới phải đứng TRƯỚC `id: order-service-admin-base` (verify bằng grep `-n` line numbers; 4 IDs mới có line number nhỏ hơn line number của `order-service-admin-base`)
    - mvn package exit 0 (gateway YAML parse hợp lệ)
  </acceptance_criteria>

  <done>
    Gateway YAML có 4 route entries mới (2 base + 2 catch-all) đặt đúng vị trí TRƯỚC catch-all admin/order, mvn package thành công.
  </done>
</task>

<task type="auto">
  <name>Task 2: Patch ROADMAP.md — Pre-Phase Setup V3 → V5 + Phase 20 Plans list</name>
  <files>
    .planning/ROADMAP.md
  </files>

  <read_first>
    - .planning/ROADMAP.md (toàn bộ — đặc biệt lines 19–30 Pre-Phase Setup V-number table, lines 141–158 Phase 20 details)
    - 20-CONTEXT.md D-01 ("planner phải patch ROADMAP.md Pre-Phase Setup table — đổi V3 → V5")
  </read_first>

  <action>
    1. **Patch Pre-Phase Setup table** ở `.planning/ROADMAP.md` line ~25–30:
       - Tìm row `| order-svc | V3 | Coupons + coupon_redemptions tables | Phase 20 |`
       - Đổi thành: `| order-svc | V5 | Coupons + coupon_redemptions tables (V5 vì V4 đã shipped Phase 18) | Phase 20 |`

       Bảng sau patch nên có 4 rows:
       ```markdown
       | Service | Version | Purpose | Phase |
       |---------|---------|---------|-------|
       | product-svc | V101 | Seed ~100 sản phẩm trong db/seed-dev/ (Spring profile `dev` only) | Phase 16 |
       | order-svc | V5 | Coupons + coupon_redemptions tables (V5 vì V4 đã shipped Phase 18) | Phase 20 |
       | order-svc | V4 | Carts + cart_items tables | Phase 18 |
       | chat_svc | — | Schema init qua Next.js API route (raw pg driver, không Flyway) | Phase 22 |
       ```

    2. **Patch Phase 20 Plans list** lines ~151–158:
       - Tìm `**Plans:** 4 plans` (line ~151) → đổi thành `**Plans:** 6 plans`
       - Tìm placeholder list copy nhầm từ Phase 17 (lines 153–157):
         ```
         Plans:
         - [ ] 17-01-PLAN.md — Tạo lib helpers (orderLabels + useEnrichedItems hook)
         - [ ] 17-02-PLAN.md — Rewrite admin order detail page (xóa placeholder + render items + shipping/payment)
         - [ ] 17-03-PLAN.md — Extend user order detail page (thumbnail + brand subtitle) + CSS
         - [ ] 17-04-PLAN.md — Extend Playwright E2E specs (regression-guard ADM-ORD-3 + ORD-DTL-2)
         ```
       - Thay bằng:
         ```
         Plans:
         - [ ] 20-01-PLAN.md — BE foundation: Flyway V5 migration + JPA entities + repositories (CouponEntity, CouponRedemptionEntity, OrderEntity extension)
         - [ ] 20-02-PLAN.md — BE service + admin controller: CouponService CRUD + CouponPreviewService.validate + CouponRedemptionService.atomicRedeem + AdminCouponController + error code enum + DTOs
         - [ ] 20-03-PLAN.md — BE integration: extend OrderCrudService.create với atomic coupon + extend OrderEntity discountAmount/couponCode + extend OrderDto + POST /orders/coupons/validate + race condition IT (D-25)
         - [ ] 20-04-PLAN.md — Gateway routes (user coupons + admin coupons) + ROADMAP patch (V3 → V5)
         - [ ] 20-05-PLAN.md — FE checkout coupon section: useApplyCoupon mutation + Input/Áp dụng button + chip + auto-revalidate + extend createOrder body với couponCode + error toast mapping
         - [ ] 20-06-PLAN.md — FE admin /admin/coupons page (rhf+zod+modal CRUD) + sidebar nav link + extend /profile/orders/[id] và /admin/orders/[id] hiển thị couponCode + discountAmount
         ```

       Giữ nguyên `**UI hint**: yes` ở line cuối Phase 20 block.

    3. **Lưu file** — đảm bảo encoding UTF-8 + Vietnamese diacritics giữ nguyên (file hiện tại là UTF-8 với `Hệ Thống Coupon` etc.).
  </action>

  <verify>
    <automated>powershell -Command "if ((Get-Content .planning/ROADMAP.md -Raw) -match 'order-svc \| V5 \| Coupons') { exit 0 } else { exit 1 }"</automated>
  </verify>

  <acceptance_criteria>
    - Grep `\| order-svc \| V5 \| Coupons` trong .planning/ROADMAP.md → 1 match
    - Grep `\| order-svc \| V3 \|` trong .planning/ROADMAP.md → 0 match (V3 đã được thay bằng V5)
    - Grep `\*\*Plans:\*\* 6 plans` trong Phase 20 section → 1 match
    - Grep `20-01-PLAN\.md` trong .planning/ROADMAP.md → 1 match
    - Grep `20-02-PLAN\.md` → 1 match
    - Grep `20-03-PLAN\.md` → 1 match
    - Grep `20-04-PLAN\.md` → 1 match
    - Grep `20-05-PLAN\.md` → 1 match
    - Grep `20-06-PLAN\.md` → 1 match
    - Grep `17-04-PLAN\.md` xuất hiện trong Phase 17 section ONLY (NOT trong Phase 20 / 21 / 22 placeholders)
  </acceptance_criteria>

  <done>
    ROADMAP V-number table phản ánh V5 cho coupons; Phase 20 có Plans list đúng 6 plans với mô tả chính xác.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → api-gateway | Untrusted requests; gateway routing không gate auth (auth check ở downstream svc) |
| api-gateway → order-svc | Internal HTTP; order-svc verify role qua JwtRoleGuard cho admin endpoints |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-04-01 | Spoofing | Gateway routes pass-through KHÔNG có auth gate | accept | Pattern existing cho mọi route — auth gate ở downstream svc (order-svc JwtRoleGuard cho admin endpoints, X-User-Id check cho user endpoints). |
| T-20-04-02 | Tampering | Gateway path injection via crafted URL | mitigate | Spring Cloud Gateway parse Path predicate strict regex (`/**` nested only); `RewritePath` regex named group capture chỉ accept path segment characters. KHÔNG inject vào downstream URL. |
| T-20-04-03 | Information Disclosure | Route ordering reveal nội bộ paths | accept | application.yml KHÔNG public; route IDs internal. |
| T-20-04-04 | Tampering | Routing misorder cause wrong rewrite | mitigate | Route ordering verified bằng acceptance criteria grep line-number — specific routes TRƯỚC catch-all. |
</threat_model>

<verification>
- Plan verification: `cd sources/backend/api-gateway && mvn -q -DskipTests package` (YAML parse OK)
- Manual smoke (sau docker-compose up): `curl -X POST http://localhost:8080/api/orders/coupons/validate -H "Content-Type: application/json" -d '{"code":"TEST","cartTotal":100000}'` → expect 404 COUPON_NOT_FOUND (route hoạt động, downstream trả lỗi nghiệp vụ — chứng tỏ rewrite đúng).
- ROADMAP sanity: `grep -c "20-0[1-6]-PLAN.md" .planning/ROADMAP.md` → output 6.
</verification>

<success_criteria>
- 4 gateway route entries mới (2 user + 2 admin, base + catch-all) ở đúng thứ tự
- mvn package gateway green
- ROADMAP V-number table hiển thị V5 (KHÔNG còn V3) cho coupons
- ROADMAP Phase 20 Plans list đúng 6 plans với mô tả ngắn
</success_criteria>

<output>
Sau khi hoàn tất, tạo `.planning/phases/20-coupons/20-04-SUMMARY.md` với artifacts (2 file modified), decision refs (D-01, D-15), test results (mvn package + grep ROADMAP).
</output>
</content>
</invoke>