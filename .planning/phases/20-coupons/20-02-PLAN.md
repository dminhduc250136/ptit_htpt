---
phase: 20
plan: 02
type: execute
wave: 2
depends_on: [20-01]
files_modified:
  - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/exception/CouponException.java
  - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/exception/CouponErrorCode.java
  - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/web/CouponExceptionHandler.java
  - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/service/CouponService.java
  - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/service/CouponPreviewService.java
  - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/service/CouponRedemptionService.java
  - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/web/AdminCouponController.java
  - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/web/CouponDtos.java
  - sources/backend/order-service/src/test/java/com/ptit/htpt/orderservice/service/CouponPreviewServiceTest.java
  - sources/backend/order-service/src/test/java/com/ptit/htpt/orderservice/web/AdminCouponControllerIT.java
autonomous: true
requirements: [COUP-02]
tags: [coupon, service, admin, controller, validation, dto]

must_haves:
  truths:
    - "Admin gọi GET/POST/PUT/PATCH/DELETE trên /admin/coupons với admin Bearer token thực hiện đầy đủ vòng đời CRUD coupon"
    - "Admin tạo coupon với type/value/min_order/expiry/max_total_uses qua POST → 201 + DTO đầy đủ"
    - "Admin DELETE coupon đã có redemption → 409 COUPON_HAS_REDEMPTIONS (D-14); DELETE coupon used_count=0 → 204"
    - "PATCH /admin/coupons/{id}/active toggle active=false (soft-disable) — coupon vẫn tồn tại nhưng preview sẽ trả COUPON_INACTIVE"
    - "CouponPreviewService.validate phân biệt 6 case lỗi (NOT_FOUND/INACTIVE/EXPIRED/MIN_ORDER_NOT_MET/ALREADY_REDEEMED/MAX_USES_REACHED) và happy path (trả discountAmount + finalTotal)"
    - "CouponRedemptionService.atomicRedeem trả CouponEntity reloaded khi redeemAtomic=1, throw CouponException(CONFLICT_OR_EXHAUSTED) khi redeemAtomic=0, throw CouponException(ALREADY_REDEEMED) khi DataIntegrityViolation insert redemption"
    - "Validation jakarta annotation reject code không match `^[A-Z0-9_-]{3,32}$`, value <= 0, type không in enum, minOrderAmount < 0 → 400 VALIDATION_ERROR"
    - "ApiErrorResponse trả `code` field = 1 trong 8 CouponErrorCode constants với message Vietnamese theo D-11"
  artifacts:
    - path: "sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/exception/CouponErrorCode.java"
      provides: "Enum 8 error codes với HTTP status + Vietnamese message"
      contains: "COUPON_NOT_FOUND"
    - path: "sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/exception/CouponException.java"
      provides: "RuntimeException + CouponErrorCode + optional details map"
      contains: "extends RuntimeException"
    - path: "sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/web/CouponExceptionHandler.java"
      provides: "@RestControllerAdvice translate CouponException → ApiErrorResponse với HTTP status đúng"
      contains: "@ExceptionHandler(CouponException.class)"
    - path: "sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/service/CouponService.java"
      provides: "Admin CRUD: list (page/size/sort/q/active filter), getById, create, update, toggleActive, delete (with countByCouponId guard)"
      contains: "@Transactional"
    - path: "sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/service/CouponPreviewService.java"
      provides: "validate(code, cartTotal, userId) → CouponPreviewResponse hoặc CouponException"
      contains: "validate"
    - path: "sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/service/CouponRedemptionService.java"
      provides: "atomicRedeem(code, userId, orderId) → CouponEntity (reloaded sau update) hoặc CouponException; computeDiscount(coupon, cartTotal) → BigDecimal"
      contains: "atomicRedeem"
    - path: "sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/web/AdminCouponController.java"
      provides: "5 admin endpoints theo D-14 dưới @RequestMapping(\"/admin/coupons\")"
      contains: "@RequestMapping(\"/admin/coupons\")"
    - path: "sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/web/CouponDtos.java"
      provides: "CouponDto, CreateCouponRequest, UpdateCouponRequest, CouponPreviewRequest, CouponPreviewResponse, ActiveToggleRequest records với jakarta validation"
      contains: "record CouponDto"
  key_links:
    - from: "AdminCouponController endpoints"
      to: "JwtRoleGuard.requireAdmin(authHeader)"
      via: "manual check trong mỗi handler trước business logic"
      pattern: "jwtRoleGuard\\.requireAdmin"
    - from: "CouponPreviewService.validate"
      to: "CouponRepository.findByCode + CouponRedemptionRepository.existsByCouponIdAndUserId"
      via: "constructor inject; KHÔNG mutate state (read-only D-08 step 1)"
      pattern: "couponRepository\\.findByCode"
    - from: "CouponRedemptionService.atomicRedeem"
      to: "CouponRepository.redeemAtomic + CouponRedemptionRepository.save"
      via: "atomic SQL UPDATE rowsAffected check + INSERT trong cùng transaction Plan 20-03"
      pattern: "redeemAtomic"
    - from: "CouponException"
      to: "CouponExceptionHandler"
      via: "@RestControllerAdvice → ApiErrorResponse envelope"
      pattern: "@ExceptionHandler\\(CouponException\\.class\\)"
---

<objective>
Triển khai BE service + admin controller layer cho coupon system: CRUD admin (D-14), preview validation (D-08 step 1), atomic redemption helper (D-08 step 2 — gọi từ Plan 20-03), error code enum + handler (D-11), validation DTO (D-16).

Purpose: Plan 20-03 sẽ inject `CouponRedemptionService` vào `OrderCrudService.create` để áp coupon trong transaction tạo order. Plan 20-03 + Plan 20-05 (FE) consume `POST /orders/coupons/validate` (preview).

Output: 8 Java file mới + tests cover happy path + 6 error cases preview + 5 admin endpoints + race-lose từ atomic redeem.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-coupons/20-CONTEXT.md
@.planning/phases/20-coupons/20-01-PLAN.md

# Analog files (read for pattern extraction)
@sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/web/AdminStatsController.java
@sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/web/JwtRoleGuard.java
@sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/api/ApiResponse.java
@sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/exception/StockShortageException.java

<interfaces>
<!-- Foundation từ Plan 20-01 - sẽ exist khi Plan 20-02 chạy (Wave 2 phụ thuộc Wave 1) -->

CouponEntity (từ Plan 20-01):
  String id(), String code(), CouponType type(), BigDecimal value(), BigDecimal minOrderAmount(),
  Integer maxTotalUses() // nullable, int usedCount(), Instant expiresAt() // nullable, boolean active()
  static CouponEntity create(String code, CouponType type, BigDecimal value, BigDecimal minOrderAmount,
                             Integer maxTotalUses, Instant expiresAt, boolean active)
  void update(...) // mirror create signature
  void setActive(boolean)

CouponType: enum { PERCENT, FIXED }

CouponRedemptionEntity (từ Plan 20-01):
  static create(CouponEntity coupon, String userId, String orderId)

CouponRepository:
  Optional<CouponEntity> findByCode(String code)
  int redeemAtomic(@Param("code") String code) // returns rowsAffected

CouponRedemptionRepository:
  boolean existsByCouponIdAndUserId(String couponId, String userId)
  long countByCouponId(String couponId)

ApiResponse envelope (existing): ApiResponse.of(int status, String message, T data)
JwtRoleGuard (existing): void requireAdmin(String authHeader)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CouponErrorCode enum + CouponException + CouponExceptionHandler + CouponDtos records</name>
  <files>
    sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/exception/CouponErrorCode.java,
    sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/exception/CouponException.java,
    sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/web/CouponExceptionHandler.java,
    sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/web/CouponDtos.java
  </files>

  <read_first>
    - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/exception/StockShortageException.java (toàn bộ — analog: domain exception với detail items + ExceptionHandler @RestControllerAdvice translate sang ApiErrorResponse)
    - Find existing @RestControllerAdvice trong order-service (grep `@RestControllerAdvice` trong sources/backend/order-service/src/main/java) — analog ApiErrorResponse construction
    - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/api/ApiResponse.java (envelope cho DTO trả)
    - 20-CONTEXT.md D-11 (8 error codes với HTTP status + Vietnamese message), D-16 (DTO shapes + jakarta validation)
  </read_first>

  <behavior>
    - Test 1 (CouponErrorCode shape): mỗi entry có (httpStatus int, code String, defaultMessageVi String). 8 entries: NOT_FOUND/INACTIVE/EXPIRED/MIN_ORDER_NOT_MET/ALREADY_REDEEMED/MAX_USES_REACHED/CONFLICT_OR_EXHAUSTED/HAS_REDEMPTIONS.
    - Test 2 (CouponException carries details map): `new CouponException(MIN_ORDER_NOT_MET, Map.of("minOrderAmount", BigDecimal.valueOf(100000)))` → `getDetails()` returns map.
    - Test 3 (CouponExceptionHandler 422): handler nhận CouponException(EXPIRED) → trả ResponseEntity status=422, body có code="COUPON_EXPIRED", message="Mã giảm giá đã hết hạn".
    - Test 4 (CouponExceptionHandler 409 với details): CouponException(MIN_ORDER_NOT_MET, {minOrderAmount:100000}) → status=422, body.details.minOrderAmount=100000.
    - Test 5 (CreateCouponRequest validation OK): code="SUMMER2026" type="PERCENT" value=10 → @Valid pass.
    - Test 6 (CreateCouponRequest validation fail code regex): code="abc" → 400 VALIDATION_ERROR field "code".
    - Test 7 (CreateCouponRequest validation fail value): value=0 → 400 VALIDATION_ERROR field "value".
    - Test 8 (CreateCouponRequest validation fail type): type="UNKNOWN" → 400 VALIDATION_ERROR field "type" (enum binding).
  </behavior>

  <action>
    1. **Tạo `CouponErrorCode.java`** package `com.ptit.htpt.orderservice.exception`:
       ```java
       public enum CouponErrorCode {
         COUPON_NOT_FOUND(404, "Mã giảm giá không tồn tại"),
         COUPON_INACTIVE(422, "Mã giảm giá đã bị vô hiệu hoá"),
         COUPON_EXPIRED(422, "Mã giảm giá đã hết hạn"),
         COUPON_MIN_ORDER_NOT_MET(422, "Đơn hàng không đạt giá trị tối thiểu để dùng mã này"),
         COUPON_ALREADY_REDEEMED(409, "Bạn đã sử dụng mã giảm giá này"),
         COUPON_MAX_USES_REACHED(409, "Mã giảm giá đã hết lượt sử dụng"),
         COUPON_CONFLICT_OR_EXHAUSTED(409, "Mã giảm giá không còn khả dụng"),
         COUPON_HAS_REDEMPTIONS(409, "Mã giảm giá đã có người dùng — vui lòng tắt thay vì xoá");

         public final int httpStatus;
         public final String defaultMessageVi;
         CouponErrorCode(int httpStatus, String defaultMessageVi) {
           this.httpStatus = httpStatus;
           this.defaultMessageVi = defaultMessageVi;
         }
       }
       ```

       **CRITICAL D-11:** code names + status + message verbatim CONTEXT.md D-11. KHÔNG sửa.

    2. **Tạo `CouponException.java`** package `com.ptit.htpt.orderservice.exception`:
       ```java
       public class CouponException extends RuntimeException {
         private final CouponErrorCode errorCode;
         private final Map<String, Object> details;

         public CouponException(CouponErrorCode errorCode) {
           this(errorCode, Map.of());
         }
         public CouponException(CouponErrorCode errorCode, Map<String, Object> details) {
           super(errorCode.defaultMessageVi);
           this.errorCode = errorCode;
           this.details = details == null ? Map.of() : Map.copyOf(details);
         }
         public CouponErrorCode errorCode() { return errorCode; }
         public Map<String, Object> details() { return details; }
       }
       ```

    3. **Tạo `CouponExceptionHandler.java`** package `com.ptit.htpt.orderservice.web`. Mirror StockShortageException handler analog. Đọc analog file trước rồi copy structure cho ApiErrorResponse construction:
       ```java
       @RestControllerAdvice
       public class CouponExceptionHandler {
         @ExceptionHandler(CouponException.class)
         public ResponseEntity<ApiErrorResponse> handle(CouponException ex, HttpServletRequest req) {
           CouponErrorCode ec = ex.errorCode();
           ApiErrorResponse body = new ApiErrorResponse(
               Instant.now().toString(),
               ec.httpStatus,
               HttpStatus.valueOf(ec.httpStatus).getReasonPhrase(),
               ec.name(),                  // code field = "COUPON_EXPIRED"...
               ec.defaultMessageVi,
               req.getRequestURI(),
               UUID.randomUUID().toString(),  // traceId
               null,                          // fieldErrors
               ex.details().isEmpty() ? null : ex.details()
           );
           return ResponseEntity.status(ec.httpStatus).body(body);
         }
       }
       ```

       **Note:** Adjust `ApiErrorResponse` constructor signature theo analog đọc được trước. Nếu khác → match exact existing constructor.

    4. **Tạo `CouponDtos.java`** package `com.ptit.htpt.orderservice.web` chứa records:
       ```java
       public final class CouponDtos {
         private CouponDtos() {}

         public record CouponDto(
             String id, String code, String type, BigDecimal value,
             BigDecimal minOrderAmount, Integer maxTotalUses, int usedCount,
             Instant expiresAt, boolean active,
             Instant createdAt, Instant updatedAt
         ) {}

         public record CreateCouponRequest(
             @Pattern(regexp = "^[A-Z0-9_-]{3,32}$",
                 message = "Mã coupon phải gồm 3-32 ký tự A-Z, 0-9, _, -")
             @NotBlank String code,
             @NotNull CouponType type,
             @NotNull @DecimalMin(value = "0.01", message = "Giá trị phải lớn hơn 0") BigDecimal value,
             @NotNull @DecimalMin("0.0") BigDecimal minOrderAmount,
             @Min(1) Integer maxTotalUses,                  // nullable
             Instant expiresAt,                             // nullable
             Boolean active
         ) {}

         public record UpdateCouponRequest(
             @Pattern(regexp = "^[A-Z0-9_-]{3,32}$") @NotBlank String code,
             @NotNull CouponType type,
             @NotNull @DecimalMin("0.01") BigDecimal value,
             @NotNull @DecimalMin("0.0") BigDecimal minOrderAmount,
             @Min(1) Integer maxTotalUses,
             Instant expiresAt,
             Boolean active
         ) {}

         public record ActiveToggleRequest(@NotNull Boolean active) {}

         public record CouponPreviewRequest(
             @NotBlank String code,
             @NotNull @DecimalMin("0.0") BigDecimal cartTotal
         ) {}

         public record CouponPreviewResponse(
             String code, String type, BigDecimal value,
             BigDecimal discountAmount, BigDecimal finalTotal, String message
         ) {}
       }
       ```

       Imports: `jakarta.validation.constraints.*`, `java.math.BigDecimal`, `java.time.Instant`, `com.ptit.htpt.orderservice.domain.CouponType`.

    5. **Tạo unit test** `CouponDtosValidationTest.java` dùng `Validator validator = Validation.buildDefaultValidatorFactory().getValidator()` cho Test 5–8. Tạo `CouponExceptionHandlerTest.java` với MockMvc / pure unit test cho Test 1–4.
  </action>

  <verify>
    <automated>cd sources/backend/order-service && mvn -q test -Dtest='CouponDtosValidationTest,CouponExceptionHandlerTest'</automated>
  </verify>

  <acceptance_criteria>
    - File tồn tại: 4 file ở `<files>`
    - Grep `COUPON_NOT_FOUND\(404` trong CouponErrorCode.java → 1 match
    - Grep `COUPON_HAS_REDEMPTIONS\(409` trong CouponErrorCode.java → 1 match
    - Grep tất cả 8 enum constants → 8 match (đếm bằng `wc -l` trên grep output)
    - Grep `@ExceptionHandler\(CouponException\.class\)` trong CouponExceptionHandler.java → 1 match
    - Grep `@Pattern\(regexp = "\^\[A-Z0-9_-\]\{3,32\}\$"` trong CouponDtos.java → ≥2 match (Create + Update)
    - Grep `record CouponDto\(` trong CouponDtos.java → 1 match
    - Grep `record CouponPreviewResponse\(` trong CouponDtos.java → 1 match
    - mvn test exit 0 với 8/8 tests passing
  </acceptance_criteria>

  <done>
    8 error code constants với HTTP status đúng, exception handler trả ApiErrorResponse với `code` + `message` + `details` theo D-11 + D-16. Validation reject mọi input invalid theo regex/range. Tests green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CouponService (admin CRUD) + CouponPreviewService + CouponRedemptionService + AdminCouponController + integration tests</name>
  <files>
    sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/service/CouponService.java,
    sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/service/CouponPreviewService.java,
    sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/service/CouponRedemptionService.java,
    sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/web/AdminCouponController.java,
    sources/backend/order-service/src/test/java/com/ptit/htpt/orderservice/service/CouponPreviewServiceTest.java,
    sources/backend/order-service/src/test/java/com/ptit/htpt/orderservice/web/AdminCouponControllerIT.java
  </files>

  <read_first>
    - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/web/AdminStatsController.java (toàn bộ — analog: cùng svc, cùng `@RequestMapping("/admin/...")` + `JwtRoleGuard.requireAdmin(authHeader)` + `ApiResponse.of(...)` envelope)
    - 20-CONTEXT.md D-04 (PERCENT/FIXED discount math + RoundingMode.FLOOR scale 0 + cap), D-08 (preview vs atomic split), D-09 (re-fetch by code), D-13 (preview endpoint sẽ implement ở Plan 20-03 OrderController), D-14 (5 admin endpoints chính xác), D-16 (DTO shapes)
    - Plan 20-01 frontmatter `<interfaces>` cho repository contracts
  </read_first>

  <behavior>
    **CouponPreviewService:**
    - Test P1 (NOT_FOUND): code không tồn tại → throws CouponException(COUPON_NOT_FOUND).
    - Test P2 (INACTIVE): coupon active=false → throws COUPON_INACTIVE.
    - Test P3 (EXPIRED): coupon expiresAt = past → throws COUPON_EXPIRED.
    - Test P4 (MIN_ORDER_NOT_MET): coupon minOrderAmount=200000 + cartTotal=100000 → throws COUPON_MIN_ORDER_NOT_MET với details.minOrderAmount=200000.
    - Test P5 (MAX_USES_REACHED): coupon maxTotalUses=10 + usedCount=10 → throws COUPON_MAX_USES_REACHED.
    - Test P6 (ALREADY_REDEEMED): user đã redeem → throws COUPON_ALREADY_REDEEMED.
    - Test P7 (PERCENT happy): coupon PERCENT 10% + cartTotal=1500000 → discount=150000 (FLOOR scale=0), finalTotal=1350000.
    - Test P8 (PERCENT cap): coupon PERCENT 200% (admin lỡ tạo, nhưng giả sử pass validation hypothetically) — task này KHÔNG cần test; validation đã reject value > thực tế hợp lý. Tuy nhiên test cap chung: PERCENT 100% cartTotal=1000 → discount=1000 finalTotal=0 (cap đảm bảo finalTotal ≥ 0).
    - Test P9 (FIXED happy): coupon FIXED 50000 + cartTotal=1500000 → discount=50000, finalTotal=1450000.
    - Test P10 (FIXED cap): coupon FIXED 999999 + cartTotal=100000 → discount=100000 (cap = cartTotal), finalTotal=0.
    - Test P11 (FLOOR rounding): coupon PERCENT 33% + cartTotal=10000 → discount=3300 (chính xác); cartTotal=999 → discount=329.67 → FLOOR scale=0 → 329, finalTotal=670.

    **CouponService (admin CRUD) — integration tests qua AdminCouponController:**
    - Test C1 (POST happy admin): POST /admin/coupons body Valid + admin Bearer → 201, body.data CouponDto đầy đủ với usedCount=0, active=true (nếu Boolean.TRUE.equals(req.active()) hoặc null=default true).
    - Test C2 (POST no Bearer): → 401.
    - Test C3 (POST USER role): → 403.
    - Test C4 (POST invalid code regex): code="abc" → 400 VALIDATION_ERROR.
    - Test C5 (GET list filter active): seed 3 active + 2 inactive → GET ?active=true → 3 results.
    - Test C6 (GET detail): GET /admin/coupons/{id} → 200 với DTO.
    - Test C7 (GET detail not found): random UUID → 404 COUPON_NOT_FOUND.
    - Test C8 (PUT happy): seed coupon → PUT thay đổi value → 200 + DTO updated.
    - Test C9 (PATCH /active): seed active=true → PATCH {active:false} → 200, reload coupon → active=false.
    - Test C10 (DELETE no redemption): seed coupon used_count=0 → DELETE → 204.
    - Test C11 (DELETE has redemption): seed coupon + 1 redemption → DELETE → 409 COUPON_HAS_REDEMPTIONS.

    **CouponRedemptionService — unit/integration:**
    - Test R1 (atomicRedeem happy): seed coupon usedCount=0 → atomicRedeem(code, userId, orderId) → returns reloaded CouponEntity với usedCount=1; coupon_redemptions có 1 row mới với (couponId, userId, orderId).
    - Test R2 (atomicRedeem race-lose): seed coupon maxTotalUses=1 usedCount=1 → throws CouponException(CONFLICT_OR_EXHAUSTED).
    - Test R3 (atomicRedeem already redeemed): seed coupon + redemption (couponId, userId) đã tồn tại + simulate concurrent flush → DataIntegrityViolationException catch → throws CouponException(ALREADY_REDEEMED). (Có thể test bằng cách gọi atomicRedeem 2 lần liên tiếp cùng userId trong cùng test.)
    - Test R4 (computeDiscount PERCENT): xem Test P7.
    - Test R5 (computeDiscount FIXED): xem Test P9.
  </behavior>

  <action>
    1. **Tạo `CouponService.java`** package `com.ptit.htpt.orderservice.service` — admin CRUD theo D-14:
       ```java
       @Service
       public class CouponService {
         private final CouponRepository couponRepository;
         private final CouponRedemptionRepository redemptionRepository;

         public CouponService(CouponRepository couponRepository, CouponRedemptionRepository redemptionRepository) {
           this.couponRepository = couponRepository;
           this.redemptionRepository = redemptionRepository;
         }

         @Transactional(readOnly = true)
         public Page<CouponDto> list(int page, int size, String sort, String q, Boolean active) {
           Pageable pageable = PageRequest.of(Math.max(page, 0), Math.max(size, 20), parseSort(sort));
           Page<CouponEntity> all = couponRepository.findAll(pageable);  // baseline; advanced filter sẽ extend qua Specification nếu cần
           // In-memory filter cho q (code contains, case-insensitive) + active — simple đủ MVP với volume nhỏ
           List<CouponDto> filtered = all.stream()
               .filter(c -> q == null || q.isBlank() || c.code().toLowerCase().contains(q.toLowerCase()))
               .filter(c -> active == null || c.active() == active)
               .map(this::toDto).toList();
           return new PageImpl<>(filtered, pageable, filtered.size());
         }

         @Transactional(readOnly = true)
         public CouponDto getById(String id) {
           return couponRepository.findById(id)
               .map(this::toDto)
               .orElseThrow(() -> new CouponException(CouponErrorCode.COUPON_NOT_FOUND));
         }

         @Transactional
         public CouponDto create(CreateCouponRequest req) {
           // KHÔNG check duplicate code — DB UNIQUE constraint sẽ reject
           CouponEntity c = CouponEntity.create(
               req.code(), req.type(), req.value(),
               req.minOrderAmount(), req.maxTotalUses(), req.expiresAt(),
               req.active() == null ? true : req.active()
           );
           return toDto(couponRepository.save(c));
         }

         @Transactional
         public CouponDto update(String id, UpdateCouponRequest req) {
           CouponEntity c = couponRepository.findById(id)
               .orElseThrow(() -> new CouponException(CouponErrorCode.COUPON_NOT_FOUND));
           c.update(req.code(), req.type(), req.value(), req.minOrderAmount(),
                    req.maxTotalUses(), req.expiresAt(),
                    req.active() == null ? c.active() : req.active());
           return toDto(couponRepository.save(c));
         }

         @Transactional
         public CouponDto setActive(String id, boolean active) {
           CouponEntity c = couponRepository.findById(id)
               .orElseThrow(() -> new CouponException(CouponErrorCode.COUPON_NOT_FOUND));
           c.setActive(active);
           return toDto(couponRepository.save(c));
         }

         @Transactional
         public void delete(String id) {
           CouponEntity c = couponRepository.findById(id)
               .orElseThrow(() -> new CouponException(CouponErrorCode.COUPON_NOT_FOUND));
           // D-14: hard-delete chỉ khi used_count = 0 (KHÔNG có redemption)
           if (redemptionRepository.countByCouponId(id) > 0 || c.usedCount() > 0) {
             throw new CouponException(CouponErrorCode.COUPON_HAS_REDEMPTIONS);
           }
           couponRepository.delete(c);
         }

         private CouponDto toDto(CouponEntity c) {
           return new CouponDto(c.id(), c.code(), c.type().name(), c.value(),
               c.minOrderAmount(), c.maxTotalUses(), c.usedCount(),
               c.expiresAt(), c.active(), c.createdAt(), c.updatedAt());
         }

         private Sort parseSort(String sort) {
           if (sort == null || sort.isBlank()) return Sort.by(Sort.Direction.DESC, "createdAt");
           String[] parts = sort.split(",");
           Sort.Direction dir = parts.length > 1 && "desc".equalsIgnoreCase(parts[1])
               ? Sort.Direction.DESC : Sort.Direction.ASC;
           return Sort.by(dir, parts[0]);
         }
       }
       ```

    2. **Tạo `CouponPreviewService.java`** package `com.ptit.htpt.orderservice.service` — D-08 step 1 (read-only validation):
       ```java
       @Service
       public class CouponPreviewService {
         private final CouponRepository couponRepository;
         private final CouponRedemptionRepository redemptionRepository;

         public CouponPreviewService(CouponRepository couponRepository, CouponRedemptionRepository redemptionRepository) {
           this.couponRepository = couponRepository;
           this.redemptionRepository = redemptionRepository;
         }

         @Transactional(readOnly = true)
         public CouponPreviewResponse validate(String code, BigDecimal cartTotal, String userId) {
           CouponEntity c = couponRepository.findByCode(code)
               .orElseThrow(() -> new CouponException(CouponErrorCode.COUPON_NOT_FOUND));

           if (!c.active()) {
             throw new CouponException(CouponErrorCode.COUPON_INACTIVE);
           }
           if (c.expiresAt() != null && c.expiresAt().isBefore(Instant.now())) {
             throw new CouponException(CouponErrorCode.COUPON_EXPIRED);
           }
           if (c.minOrderAmount() != null && cartTotal.compareTo(c.minOrderAmount()) < 0) {
             throw new CouponException(CouponErrorCode.COUPON_MIN_ORDER_NOT_MET,
                 Map.of("minOrderAmount", c.minOrderAmount()));
           }
           if (c.maxTotalUses() != null && c.usedCount() >= c.maxTotalUses()) {
             throw new CouponException(CouponErrorCode.COUPON_MAX_USES_REACHED);
           }
           if (userId != null && redemptionRepository.existsByCouponIdAndUserId(c.id(), userId)) {
             throw new CouponException(CouponErrorCode.COUPON_ALREADY_REDEEMED);
           }

           BigDecimal discount = computeDiscount(c, cartTotal);
           BigDecimal finalTotal = cartTotal.subtract(discount);
           return new CouponPreviewResponse(c.code(), c.type().name(), c.value(),
               discount, finalTotal, "Áp dụng mã thành công");
         }

         /** D-04 discount math. Cap: discount ≤ cartTotal. */
         public static BigDecimal computeDiscount(CouponEntity c, BigDecimal cartTotal) {
           BigDecimal raw;
           if (c.type() == CouponType.PERCENT) {
             raw = cartTotal.multiply(c.value())
                 .divide(BigDecimal.valueOf(100), 0, RoundingMode.FLOOR);
           } else { // FIXED
             raw = c.value();
           }
           return raw.min(cartTotal);  // cap
         }
       }
       ```

    3. **Tạo `CouponRedemptionService.java`** package `com.ptit.htpt.orderservice.service` — D-08 step 2 (atomic mutation, gọi từ Plan 20-03 OrderCrudService.create trong cùng @Transactional):
       ```java
       @Service
       public class CouponRedemptionService {
         private static final Logger log = LoggerFactory.getLogger(CouponRedemptionService.class);
         private final CouponRepository couponRepository;
         private final CouponRedemptionRepository redemptionRepository;

         public CouponRedemptionService(CouponRepository couponRepository,
                                        CouponRedemptionRepository redemptionRepository) {
           this.couponRepository = couponRepository;
           this.redemptionRepository = redemptionRepository;
         }

         /**
          * D-08 step 2 atomic redemption. PHẢI gọi trong @Transactional cha (Plan 20-03 OrderCrudService.create).
          * Trả CouponEntity reloaded sau UPDATE thành công (caller cần code + computed discount + type + value).
          *
          * @throws CouponException CONFLICT_OR_EXHAUSTED khi redeemAtomic returns 0 (race-lose / expired / inactive / hết slot)
          * @throws CouponException ALREADY_REDEEMED khi insert vi phạm UNIQUE(coupon_id, user_id)
          */
         public CouponEntity atomicRedeem(String code, String userId, String orderId) {
           int rows = couponRepository.redeemAtomic(code);
           if (rows == 0) {
             throw new CouponException(CouponErrorCode.COUPON_CONFLICT_OR_EXHAUSTED);
           }
           CouponEntity c = couponRepository.findByCode(code)
               .orElseThrow(() -> new CouponException(CouponErrorCode.COUPON_CONFLICT_OR_EXHAUSTED));
           try {
             redemptionRepository.save(CouponRedemptionEntity.create(c, userId, orderId));
           } catch (DataIntegrityViolationException ex) {
             log.info("[D-08] coupon already redeemed by user (UNIQUE violation): couponId={} userId={}",
                 c.id(), userId);
             throw new CouponException(CouponErrorCode.COUPON_ALREADY_REDEEMED);
           }
           return c;
         }
       }
       ```

    4. **Tạo `AdminCouponController.java`** package `com.ptit.htpt.orderservice.web`:
       ```java
       @RestController
       @RequestMapping("/admin/coupons")
       public class AdminCouponController {
         private final CouponService couponService;
         private final JwtRoleGuard jwtRoleGuard;

         public AdminCouponController(CouponService couponService, JwtRoleGuard jwtRoleGuard) {
           this.couponService = couponService;
           this.jwtRoleGuard = jwtRoleGuard;
         }

         @GetMapping
         public ApiResponse<Page<CouponDto>> list(
             @RequestParam(defaultValue = "0") int page,
             @RequestParam(defaultValue = "20") int size,
             @RequestParam(defaultValue = "createdAt,desc") String sort,
             @RequestParam(required = false) String q,
             @RequestParam(required = false) Boolean active,
             @RequestHeader(value = HttpHeaders.AUTHORIZATION, required = false) String authHeader) {
           jwtRoleGuard.requireAdmin(authHeader);
           return ApiResponse.of(200, "Coupons listed", couponService.list(page, size, sort, q, active));
         }

         @GetMapping("/{id}")
         public ApiResponse<CouponDto> get(@PathVariable String id,
             @RequestHeader(value = HttpHeaders.AUTHORIZATION, required = false) String authHeader) {
           jwtRoleGuard.requireAdmin(authHeader);
           return ApiResponse.of(200, "Coupon loaded", couponService.getById(id));
         }

         @PostMapping
         @ResponseStatus(HttpStatus.CREATED)
         public ApiResponse<CouponDto> create(@Valid @RequestBody CreateCouponRequest req,
             @RequestHeader(value = HttpHeaders.AUTHORIZATION, required = false) String authHeader) {
           jwtRoleGuard.requireAdmin(authHeader);
           return ApiResponse.of(201, "Coupon created", couponService.create(req));
         }

         @PutMapping("/{id}")
         public ApiResponse<CouponDto> update(@PathVariable String id, @Valid @RequestBody UpdateCouponRequest req,
             @RequestHeader(value = HttpHeaders.AUTHORIZATION, required = false) String authHeader) {
           jwtRoleGuard.requireAdmin(authHeader);
           return ApiResponse.of(200, "Coupon updated", couponService.update(id, req));
         }

         @PatchMapping("/{id}/active")
         public ApiResponse<CouponDto> toggleActive(@PathVariable String id, @Valid @RequestBody ActiveToggleRequest req,
             @RequestHeader(value = HttpHeaders.AUTHORIZATION, required = false) String authHeader) {
           jwtRoleGuard.requireAdmin(authHeader);
           return ApiResponse.of(200, "Coupon active toggled", couponService.setActive(id, req.active()));
         }

         @DeleteMapping("/{id}")
         @ResponseStatus(HttpStatus.NO_CONTENT)
         public void delete(@PathVariable String id,
             @RequestHeader(value = HttpHeaders.AUTHORIZATION, required = false) String authHeader) {
           jwtRoleGuard.requireAdmin(authHeader);
           couponService.delete(id);
         }
       }
       ```

       **CRITICAL D-14:** 5 endpoints ở đúng path + HTTP method + HTTP status. KHÔNG sửa.

    5. **Tạo tests:**
       - `CouponPreviewServiceTest.java` (`@DataJpaTest` + `@Import(CouponPreviewService.class)` để inject vào test ApplicationContext) — cover Test P1–P11.
       - `CouponRedemptionServiceIT.java` (`@DataJpaTest` + Testcontainers Postgres + `@Import(CouponRedemptionService.class)`) — cover Test R1–R3.
       - `AdminCouponControllerIT.java` (`@SpringBootTest` + Testcontainers Postgres + admin/user JWT util) — cover Test C1–C11.
  </action>

  <verify>
    <automated>cd sources/backend/order-service && mvn -q test -Dtest='CouponPreviewServiceTest,CouponRedemptionServiceIT,AdminCouponControllerIT'</automated>
  </verify>

  <acceptance_criteria>
    - File tồn tại: 6 file ở `<files>` (4 source + 2 test)
    - Grep `@RequestMapping\("/admin/coupons"\)` trong AdminCouponController.java → 1 match
    - Grep `@GetMapping`, `@PostMapping`, `@PutMapping`, `@PatchMapping\("/\{id\}/active"\)`, `@DeleteMapping` trong AdminCouponController.java → 5 method-level mappings
    - Grep `jwtRoleGuard\.requireAdmin\(authHeader\)` trong AdminCouponController.java → 6 match (1 mỗi endpoint, list/get/create/update/patch/delete)
    - Grep `RoundingMode\.FLOOR` trong CouponPreviewService.java → 1 match (D-04)
    - Grep `\.divide\(BigDecimal\.valueOf\(100\), 0, RoundingMode\.FLOOR\)` → 1 match (PERCENT discount math)
    - Grep `raw\.min\(cartTotal\)` trong CouponPreviewService.java → 1 match (cap D-04)
    - Grep `couponRepository\.redeemAtomic\(code\)` trong CouponRedemptionService.java → 1 match
    - Grep `DataIntegrityViolationException` trong CouponRedemptionService.java → 1 match
    - Grep `COUPON_HAS_REDEMPTIONS` trong CouponService.java → 1 match (D-14 delete guard)
    - mvn test exit 0 với (11 + 3 + 11) = 25/25 tests passing
  </acceptance_criteria>

  <done>
    Admin CRUD 5 endpoints functional + auth gated; preview validate phân biệt 6 lỗi + happy với discount math chính xác (FLOOR scale 0 + cap); atomic redeem helper sẵn sàng cho Plan 20-03 inject. Tests green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser admin → api-gateway | Untrusted admin JWT |
| api-gateway → order-svc | Internal HTTP, JWT relay; order-svc verify role |
| Anonymous user → preview endpoint (Plan 20-03) | KHÔNG nhạy cảm read-only — defer rate-limit (D-deferred) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-02-01 | Spoofing | AdminCouponController 5 endpoints | mitigate | `JwtRoleGuard.requireAdmin(authHeader)` ở mỗi handler. Test C2+C3. |
| T-20-02-02 | Tampering | CreateCouponRequest input | mitigate | `@Valid` + jakarta annotations (@Pattern code regex, @DecimalMin value, @NotNull type) → 400 VALIDATION_ERROR. Test C4. |
| T-20-02-03 | Tampering | discountAmount client-side | mitigate (Plan 20-03) | CouponPreviewService.computeDiscount tính server-side từ cartTotal client gửi (preview chỉ là gợi ý UX). Plan 20-03 atomic redemption RE-tính từ server-side cart total — KHÔNG tin client (D-10). |
| T-20-02-04 | Information Disclosure | Coupon details qua /admin/coupons | accept | Admin-only context (gated). Coupon code + value KHÔNG nhạy cảm — admin tạo. |
| T-20-02-05 | Repudiation | Coupon CRUD audit | accept | createdAt/updatedAt timestamp ghi nhận thay đổi. Detailed audit log defer. |
| T-20-02-06 | Denial of Service | Brute force /coupons/validate | accept (defer) | D-deferred: rate-limit defer khi đo được abuse. Preview là read-only KHÔNG mutate. |
| T-20-02-07 | Tampering (SQL Injection) | CouponService.list q filter | mitigate | `q` dùng in-memory filter (`String.toLowerCase().contains`) — KHÔNG concat vào SQL. JPQL/native KHÔNG dùng q. |
| T-20-02-08 | Information Disclosure | DELETE coupon đã có redemption | mitigate | D-14 guard `countByCouponId > 0` → 409 COUPON_HAS_REDEMPTIONS thay vì xoá silent → bảo toàn audit redemptions. |
</threat_model>

<verification>
- Quick run: `cd sources/backend/order-service && mvn -q compile`
- Plan verification: `cd sources/backend/order-service && mvn -q test -Dtest='CouponDtosValidationTest,CouponExceptionHandlerTest,CouponPreviewServiceTest,CouponRedemptionServiceIT,AdminCouponControllerIT'`
- Manual smoke (sau docker-compose up): admin Bearer → `curl -X POST http://localhost:8080/api/orders/admin/coupons -H "Authorization: Bearer <admin>" -H "Content-Type: application/json" -d '{"code":"SUMMER2026","type":"PERCENT","value":10,"minOrderAmount":0,"active":true}'` → 201. (Gateway route Plan 20-04 sẽ map `/api/orders/admin/coupons` → `/admin/coupons`.)
</verification>

<success_criteria>
- 8 file Java mới (4 service/handler + 4 test)
- 5 admin endpoints functional + auth gated
- Preview validate đúng 6 lỗi + happy
- Atomic redemption helper sẵn sàng inject vào order create
- 33 test pass (8 + 25)
</success_criteria>

<output>
Sau khi hoàn tất, tạo `.planning/phases/20-coupons/20-02-SUMMARY.md` với artifacts, decision refs (D-04/D-08/D-09/D-11/D-14/D-16), test results, manual smoke verification.
</output>
</content>
</invoke>