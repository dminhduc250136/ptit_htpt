---
phase: 20
plan: 06
type: execute
wave: 4
depends_on: [20-03, 20-04]
files_modified:
  - sources/frontend/src/services/admin-coupons.ts
  - sources/frontend/src/app/admin/coupons/page.tsx
  - sources/frontend/src/app/admin/coupons/page.module.css
  - sources/frontend/src/app/admin/layout.tsx
  - sources/frontend/src/app/profile/orders/[id]/page.tsx
  - sources/frontend/src/app/admin/orders/[id]/page.tsx
  - sources/frontend/src/types/index.ts
autonomous: true
requirements: [COUP-02, COUP-05]
tags: [frontend, admin, coupon-crud, order-detail-display, vietnamese-ui, rhf, zod]

must_haves:
  truths:
    - "Admin sidebar có link 'Coupon' đặt sau 'Đơn hàng' (D-22), click → /admin/coupons"
    - "Trang /admin/coupons hiển thị table coupons với columns: Mã / Loại / Giá trị / Đơn tối thiểu / Đã dùng / Hết hạn / Trạng thái / Hành động (D-21)"
    - "Trang có search input theo code + select active filter (Tất cả / Đang bật / Đã tắt)"
    - "Click '+ Thêm coupon' mở modal form với rhf+zod validation: code regex auto-uppercase, type radio PERCENT/FIXED, value với suffix % hoặc đ, min order, max uses (checkbox 'Không giới hạn'), expiry datetime (checkbox 'Không hết hạn'), active checkbox (D-21)"
    - "Form submit → POST /api/orders/admin/coupons; success → toast + reload list"
    - "Click row 'Sửa' mở modal pre-filled → PUT /api/orders/admin/coupons/{id}; click 'Tắt'/'Bật' → PATCH /active toggle; click 'Xoá' → DELETE; nếu BE trả COUPON_HAS_REDEMPTIONS → modal lỗi 'Coupon đã có người dùng — vui lòng tắt thay vì xoá' (D-21 message)"
    - "Order detail user (/profile/orders/[id]) + admin (/admin/orders/[id]) hiển thị block 'Mã giảm giá: CODE_X' + 'Giảm giá: -X đ' phía trên 'Tổng tiền' KHI order.couponCode non-null (D-23)"
  artifacts:
    - path: "sources/frontend/src/services/admin-coupons.ts"
      provides: "5 typed fetchers: listAdminCoupons, createCoupon, updateCoupon, toggleCouponActive, deleteCoupon"
      contains: "listAdminCoupons"
    - path: "sources/frontend/src/app/admin/coupons/page.tsx"
      provides: "Admin coupons CRUD page với list + filter + create/edit modal (rhf+zod) + delete confirm"
      contains: "useForm"
    - path: "sources/frontend/src/app/admin/coupons/page.module.css"
      provides: "CSS classes cho coupon admin (mirror /admin/products/page.module.css style)"
      contains: "tableWrapper"
    - path: "sources/frontend/src/app/admin/layout.tsx"
      provides: "+navItems entry 'Coupon' đặt sau 'Đơn hàng'"
      contains: "/admin/coupons"
    - path: "sources/frontend/src/app/profile/orders/[id]/page.tsx"
      provides: "+Coupon block hiển thị trên Tổng tiền khi order.couponCode non-null"
      contains: "order.couponCode"
    - path: "sources/frontend/src/app/admin/orders/[id]/page.tsx"
      provides: "+Coupon block hiển thị trên Tổng tiền khi order.couponCode non-null"
      contains: "order.couponCode"
    - path: "sources/frontend/src/types/index.ts"
      provides: "+AdminCoupon type + extend Order với optional discountAmount + couponCode"
      contains: "AdminCoupon"
  key_links:
    - from: "Admin layout sidebar item 'Coupon'"
      to: "/admin/coupons route"
      via: "Next.js Link href"
      pattern: "href=\"/admin/coupons\""
    - from: "Form submit"
      to: "createCoupon / updateCoupon fetcher"
      via: "rhf handleSubmit → onSubmit handler → service call → BE POST/PUT"
      pattern: "createCoupon|updateCoupon"
    - from: "Order detail render"
      to: "order.couponCode + order.discountAmount conditional block"
      via: "early return null nếu couponCode falsy"
      pattern: "order\\.couponCode"
---

<objective>
Triển khai FE admin coupon CRUD page (D-21, D-22) + extend 2 order detail pages để hiển thị coupon snapshot (D-23, COUP-05).

Purpose: COUP-02 (admin CRUD UI) + COUP-05 (order detail display) hoàn chỉnh — closing 2/4 SC.

Output: 7 file (1 service + 1 page + 1 CSS + 3 page extensions + types extension). rhf+zod form mirror admin/products pattern.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-coupons/20-CONTEXT.md
@.planning/phases/20-coupons/20-02-PLAN.md
@.planning/phases/20-coupons/20-03-PLAN.md

@sources/frontend/src/app/admin/products/page.tsx
@sources/frontend/src/app/admin/layout.tsx
@sources/frontend/src/app/profile/orders/[id]/page.tsx
@sources/frontend/src/app/admin/orders/[id]/page.tsx
@sources/frontend/src/services/orders.ts
@sources/frontend/src/services/http.ts

<interfaces>
<!-- BE contract từ Plan 20-02 + 20-03 -->

GET /api/orders/admin/coupons?page=&size=&sort=&q=&active=
  → ApiResponse<Page<CouponDto>>  // Spring Page shape: { content, totalElements, totalPages, ... }
GET /api/orders/admin/coupons/{id}
  → ApiResponse<CouponDto>
POST /api/orders/admin/coupons
  body: { code, type: 'PERCENT'|'FIXED', value, minOrderAmount, maxTotalUses?, expiresAt? (ISO8601), active? }
  → 201 ApiResponse<CouponDto>
PUT /api/orders/admin/coupons/{id}
  body: same as POST
  → ApiResponse<CouponDto>
PATCH /api/orders/admin/coupons/{id}/active
  body: { active: boolean }
  → ApiResponse<CouponDto>
DELETE /api/orders/admin/coupons/{id}
  → 204 (or 409 COUPON_HAS_REDEMPTIONS nếu đã có redemption)

CouponDto shape:
  { id, code, type: 'PERCENT'|'FIXED', value: number, minOrderAmount: number,
    maxTotalUses: number|null, usedCount: number, expiresAt: string|null (ISO),
    active: boolean, createdAt: string, updatedAt: string }

OrderDto extension Plan 20-03 (existing /api/orders/{id} response):
  + discountAmount: number   // 0 if no coupon
  + couponCode: string|null  // null if no coupon

Existing Order interface FE @/types: cần extend với 2 field optional.

react-hook-form + zod (existing in package.json — Phase 11 address form pattern):
  useForm({resolver: zodResolver(schema)})
  Controller cho controlled inputs

Existing patterns (admin/products/page.tsx):
  Modal overlay với onClick outside close + onClick stopPropagation inside
  Confirm modal cho delete
  Toast pattern showToast(msg, 'success'|'error')
  Skeleton loading state
  RetrySection on fetch failure
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: services/admin-coupons.ts + types extension + admin layout sidebar nav update</name>
  <files>
    sources/frontend/src/services/admin-coupons.ts,
    sources/frontend/src/types/index.ts,
    sources/frontend/src/app/admin/layout.tsx
  </files>

  <read_first>
    - sources/frontend/src/services/orders.ts (analog: listAdminOrders + getAdminOrderById + updateOrderState — admin endpoint pattern qua gateway `/api/orders/admin/...`)
    - sources/frontend/src/types/index.ts (xem Order interface hiện có để biết field nào đã có)
    - sources/frontend/src/app/admin/layout.tsx (lines 11–16 navItems array + icon SVG inline pattern)
    - 20-CONTEXT.md D-14 (admin endpoints), D-21 (page columns + form fields), D-22 (sidebar nav location)
  </read_first>

  <action>
    1. **Tạo `sources/frontend/src/services/admin-coupons.ts`:**
       ```typescript
       /**
        * Admin coupon service — D-14 endpoints qua gateway /api/orders/admin/coupons.
        */
       import { httpGet, httpPost, httpPut, httpPatch, httpDelete } from './http';
       import type { AdminCoupon, PaginatedResponse } from '@/types';

       export interface CouponUpsertBody {
         code: string;
         type: 'PERCENT' | 'FIXED';
         value: number;
         minOrderAmount: number;
         maxTotalUses?: number | null;
         expiresAt?: string | null;  // ISO8601
         active: boolean;
       }

       export interface ListAdminCouponsParams {
         page?: number;
         size?: number;
         sort?: string;
         q?: string;
         active?: boolean;
       }

       export function listAdminCoupons(
         params?: ListAdminCouponsParams,
       ): Promise<PaginatedResponse<AdminCoupon>> {
         const qs = new URLSearchParams();
         if (params?.page !== undefined) qs.set('page', String(params.page));
         if (params?.size !== undefined) qs.set('size', String(params.size));
         if (params?.sort) qs.set('sort', params.sort);
         if (params?.q) qs.set('q', params.q);
         if (params?.active !== undefined) qs.set('active', String(params.active));
         const suffix = qs.toString() ? `?${qs}` : '';
         return httpGet<PaginatedResponse<AdminCoupon>>(`/api/orders/admin/coupons${suffix}`);
       }

       export function getAdminCoupon(id: string): Promise<AdminCoupon> {
         return httpGet<AdminCoupon>(`/api/orders/admin/coupons/${encodeURIComponent(id)}`);
       }

       export function createCoupon(body: CouponUpsertBody): Promise<AdminCoupon> {
         return httpPost<AdminCoupon>('/api/orders/admin/coupons', body);
       }

       export function updateCoupon(id: string, body: CouponUpsertBody): Promise<AdminCoupon> {
         return httpPut<AdminCoupon>(`/api/orders/admin/coupons/${encodeURIComponent(id)}`, body);
       }

       export function toggleCouponActive(id: string, active: boolean): Promise<AdminCoupon> {
         return httpPatch<AdminCoupon>(
           `/api/orders/admin/coupons/${encodeURIComponent(id)}/active`,
           { active },
         );
       }

       export function deleteCoupon(id: string): Promise<void> {
         return httpDelete<void>(`/api/orders/admin/coupons/${encodeURIComponent(id)}`);
       }
       ```

    2. **Extend `sources/frontend/src/types/index.ts`:**
       ```typescript
       export interface AdminCoupon {
         id: string;
         code: string;
         type: 'PERCENT' | 'FIXED';
         value: number;
         minOrderAmount: number;
         maxTotalUses: number | null;
         usedCount: number;
         expiresAt: string | null;  // ISO8601
         active: boolean;
         createdAt: string;
         updatedAt: string;
       }
       ```

       Tìm `Order` interface hiện có và thêm 2 field optional:
       ```typescript
       // export interface Order { ... existing fields ... }
       // Thêm:
       //   discountAmount?: number;
       //   couponCode?: string | null;
       ```
       Đọc `types/index.ts` trước để lấy exact Order signature, thêm 2 field optional vào cuối.

    3. **Extend `sources/frontend/src/app/admin/layout.tsx` navItems** (line ~11–16). Thêm entry mới SAU 'Đơn hàng' (D-22):
       ```tsx
       const navItems = [
         { href: '/admin', label: 'Dashboard', icon: <svg ... /> },
         { href: '/admin/products', label: 'Sản phẩm', icon: <svg ... /> },
         { href: '/admin/orders', label: 'Đơn hàng', icon: <svg ... /> },
         { href: '/admin/coupons', label: 'Coupon', icon: (
           <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2">
             <path d="M21 12V7H5a2 2 0 0 1 0-4h14v4"/>
             <path d="M3 5v14a2 2 0 0 0 2 2h16v-5"/>
             <path d="M18 12a2 2 0 0 0 0 4h4v-4z"/>
           </svg>
         )},
         { href: '/admin/users', label: 'Tài khoản', icon: <svg ... /> },
       ];
       ```
       (Coupon tag/wallet icon — Lucide-style stroke). Giữ nguyên các entries khác.
  </action>

  <verify>
    <automated>cd sources/frontend && npx tsc --noEmit && npm run lint</automated>
  </verify>

  <acceptance_criteria>
    - File tồn tại: services/admin-coupons.ts
    - Grep `listAdminCoupons`, `createCoupon`, `updateCoupon`, `toggleCouponActive`, `deleteCoupon` trong admin-coupons.ts → 5 export match
    - Grep `'/api/orders/admin/coupons'` trong admin-coupons.ts → ≥1 match
    - Grep `interface AdminCoupon` trong types/index.ts → 1 match
    - Grep `discountAmount\?:` trong types/index.ts → 1 match (Order extension)
    - Grep `couponCode\?:` trong types/index.ts → 1 match (Order extension)
    - Grep `href: '/admin/coupons'` trong admin/layout.tsx → 1 match
    - Grep `label: 'Coupon'` trong admin/layout.tsx → 1 match
    - tsc + lint exit 0
  </acceptance_criteria>

  <done>
    Service + types + sidebar nav sẵn sàng cho Task 2 page implementation.
  </done>
</task>

<task type="auto">
  <name>Task 2: Tạo /admin/coupons/page.tsx + page.module.css với rhf+zod CRUD form</name>
  <files>
    sources/frontend/src/app/admin/coupons/page.tsx,
    sources/frontend/src/app/admin/coupons/page.module.css
  </files>

  <read_first>
    - sources/frontend/src/app/admin/products/page.tsx (toàn bộ — analog: list + search + Modal Add/Edit + Confirm Delete + handleSubmit + skeleton loading + RetrySection)
    - sources/frontend/src/app/admin/products/page.module.css (analog: tableWrapper, table, modalForm, formRow, modalActions, overlay, modal, confirmModal styles)
    - 20-CONTEXT.md D-21 (columns + form fields verbatim), D-14 (DELETE COUPON_HAS_REDEMPTIONS message)
    - 20-PLAN-05 couponErrorMessages.ts (reuse error map cho admin DELETE error)
  </read_first>

  <action>
    Tạo file `/admin/coupons/page.tsx` mirror pattern admin/products/page.tsx nhưng dùng rhf+zod cho form (admin/products dùng plain useState — admin/coupons cần stricter validation theo D-21 → rhf+zod đắt hơn nhưng cleaner):

    ```tsx
    'use client';

    import React, { useCallback, useEffect, useState } from 'react';
    import { useForm, Controller } from 'react-hook-form';
    import { zodResolver } from '@hookform/resolvers/zod';
    import { z } from 'zod';
    import styles from './page.module.css';
    import Button from '@/components/ui/Button/Button';
    import Input from '@/components/ui/Input/Input';
    import Badge from '@/components/ui/Badge/Badge';
    import RetrySection from '@/components/ui/RetrySection/RetrySection';
    import { useToast } from '@/components/ui/Toast/Toast';
    import {
      listAdminCoupons, createCoupon, updateCoupon, toggleCouponActive, deleteCoupon,
      type CouponUpsertBody,
    } from '@/services/admin-coupons';
    import { isApiError } from '@/services/errors';
    import { formatCouponError } from '@/lib/couponErrorMessages';
    import type { AdminCoupon } from '@/types';

    const couponSchema = z.object({
      code: z.string()
        .regex(/^[A-Z0-9_-]{3,32}$/, 'Mã phải gồm 3-32 ký tự A-Z, 0-9, _, -'),
      type: z.enum(['PERCENT', 'FIXED']),
      value: z.coerce.number().positive('Giá trị phải lớn hơn 0'),
      minOrderAmount: z.coerce.number().min(0, 'Đơn tối thiểu không âm'),
      noLimit: z.boolean(),
      maxTotalUses: z.coerce.number().int().positive().optional().nullable(),
      noExpiry: z.boolean(),
      expiresAt: z.string().optional().nullable(),
      active: z.boolean(),
    }).refine((d) => {
      if (d.type === 'PERCENT' && d.value > 100) return false;
      return true;
    }, { message: 'Phần trăm tối đa 100%', path: ['value'] });

    type CouponFormData = z.infer<typeof couponSchema>;

    const emptyForm: CouponFormData = {
      code: '', type: 'PERCENT', value: 10, minOrderAmount: 0,
      noLimit: true, maxTotalUses: null,
      noExpiry: true, expiresAt: null,
      active: true,
    };

    export default function AdminCouponsPage() {
      const [coupons, setCoupons] = useState<AdminCoupon[]>([]);
      const [loading, setLoading] = useState(false);
      const [failed, setFailed] = useState(false);
      const [showModal, setShowModal] = useState(false);
      const [editTarget, setEditTarget] = useState<AdminCoupon | null>(null);
      const [deleteTarget, setDeleteTarget] = useState<AdminCoupon | null>(null);
      const [deleteError, setDeleteError] = useState<string | null>(null);
      const [saving, setSaving] = useState(false);
      const [search, setSearch] = useState('');
      const [activeFilter, setActiveFilter] = useState<'ALL' | 'true' | 'false'>('ALL');
      const { showToast } = useToast();

      const {
        control, register, handleSubmit, reset, watch, setValue,
        formState: { errors },
      } = useForm<CouponFormData>({
        resolver: zodResolver(couponSchema),
        defaultValues: emptyForm,
      });

      const noLimit = watch('noLimit');
      const noExpiry = watch('noExpiry');
      const couponType = watch('type');

      const load = useCallback(async () => {
        setLoading(true); setFailed(false);
        try {
          const params: { q?: string; active?: boolean } = {};
          if (search.trim()) params.q = search.trim();
          if (activeFilter !== 'ALL') params.active = activeFilter === 'true';
          const resp = await listAdminCoupons(params);
          setCoupons(resp?.content ?? []);
        } catch {
          setFailed(true);
        } finally {
          setLoading(false);
        }
      }, [search, activeFilter]);

      useEffect(() => { load(); }, [load]);

      const openAddModal = () => {
        setEditTarget(null);
        reset(emptyForm);
        setShowModal(true);
      };

      const openEditModal = (c: AdminCoupon) => {
        setEditTarget(c);
        reset({
          code: c.code,
          type: c.type,
          value: c.value,
          minOrderAmount: c.minOrderAmount,
          noLimit: c.maxTotalUses === null,
          maxTotalUses: c.maxTotalUses,
          noExpiry: c.expiresAt === null,
          expiresAt: c.expiresAt ? c.expiresAt.slice(0, 16) : null,  // datetime-local format
          active: c.active,
        });
        setShowModal(true);
      };

      const closeModal = () => {
        setShowModal(false);
        setEditTarget(null);
      };

      const onSubmit = async (data: CouponFormData) => {
        setSaving(true);
        try {
          const body: CouponUpsertBody = {
            code: data.code.toUpperCase(),
            type: data.type,
            value: data.value,
            minOrderAmount: data.minOrderAmount,
            maxTotalUses: data.noLimit ? null : (data.maxTotalUses ?? null),
            expiresAt: data.noExpiry ? null : (data.expiresAt ? new Date(data.expiresAt).toISOString() : null),
            active: data.active,
          };
          if (editTarget) {
            await updateCoupon(editTarget.id, body);
            showToast('Coupon đã được cập nhật', 'success');
          } else {
            await createCoupon(body);
            showToast('Coupon đã được tạo', 'success');
          }
          closeModal();
          await load();
        } catch (err) {
          const fallback = editTarget ? 'Không thể cập nhật coupon' : 'Không thể tạo coupon';
          if (isApiError(err)) {
            showToast(formatCouponError(err.code, err.details) ?? err.message ?? fallback, 'error');
          } else {
            showToast(fallback, 'error');
          }
        } finally {
          setSaving(false);
        }
      };

      const handleToggleActive = async (c: AdminCoupon) => {
        try {
          await toggleCouponActive(c.id, !c.active);
          showToast(c.active ? 'Đã tắt coupon' : 'Đã bật coupon', 'success');
          await load();
        } catch {
          showToast('Không thể thay đổi trạng thái coupon', 'error');
        }
      };

      const handleConfirmDelete = async () => {
        if (!deleteTarget) return;
        setDeleteError(null);
        try {
          await deleteCoupon(deleteTarget.id);
          showToast('Coupon đã được xoá', 'success');
          setDeleteTarget(null);
          await load();
        } catch (err) {
          if (isApiError(err) && err.code === 'COUPON_HAS_REDEMPTIONS') {
            setDeleteError('Coupon đã có người dùng — vui lòng tắt thay vì xoá');
          } else {
            setDeleteError('Không thể xoá coupon. Vui lòng thử lại');
          }
        }
      };

      const formatExpiry = (iso: string | null) => {
        if (!iso) return 'Không hết hạn';
        return new Date(iso).toLocaleDateString('vi-VN');
      };

      const formatValue = (c: AdminCoupon) =>
        c.type === 'PERCENT'
          ? `${c.value}%`
          : `${new Intl.NumberFormat('vi-VN').format(c.value)} đ`;

      return (
        <div className={styles.page}>
          <div className={styles.header}>
            <h1 className={styles.title}>Quản lý coupon</h1>
            <Button onClick={openAddModal}>+ Thêm coupon</Button>
          </div>

          <div className={styles.toolbar}>
            <Input
              placeholder="Tìm theo mã..."
              value={search}
              onChange={(e) => setSearch(e.target.value)}
            />
            <select
              value={activeFilter}
              onChange={(e) => setActiveFilter(e.target.value as 'ALL' | 'true' | 'false')}
              className={styles.select}
            >
              <option value="ALL">Tất cả</option>
              <option value="true">Đang bật</option>
              <option value="false">Đã tắt</option>
            </select>
            <span className={styles.count}>{coupons.length} coupon</span>
          </div>

          <div className={styles.tableWrapper}>
            <table className={styles.table}>
              <thead>
                <tr>
                  <th>Mã</th>
                  <th>Loại</th>
                  <th>Giá trị</th>
                  <th>Đơn tối thiểu</th>
                  <th>Đã dùng / Tối đa</th>
                  <th>Hết hạn</th>
                  <th>Trạng thái</th>
                  <th>Hành động</th>
                </tr>
              </thead>
              <tbody>
                {loading && [...Array(5)].map((_, i) => (
                  <tr key={i}><td colSpan={8}><div className="skeleton" style={{ height: 40 }} /></td></tr>
                ))}
                {!loading && failed && (
                  <tr><td colSpan={8}><RetrySection onRetry={load} loading={loading} /></td></tr>
                )}
                {!loading && !failed && coupons.length === 0 && (
                  <tr><td colSpan={8} style={{ textAlign: 'center', padding: 'var(--space-7) 0' }}>Chưa có coupon nào</td></tr>
                )}
                {!loading && !failed && coupons.map((c) => (
                  <tr key={c.id}>
                    <td><strong>{c.code}</strong></td>
                    <td>{c.type === 'PERCENT' ? 'Phần trăm' : 'Số tiền cố định'}</td>
                    <td>{formatValue(c)}</td>
                    <td>{new Intl.NumberFormat('vi-VN').format(c.minOrderAmount)} đ</td>
                    <td>{c.usedCount} / {c.maxTotalUses ?? '∞'}</td>
                    <td>{formatExpiry(c.expiresAt)}</td>
                    <td>
                      <Badge variant={c.active ? 'new' : 'default'}>
                        {c.active ? 'Đang bật' : 'Đã tắt'}
                      </Badge>
                    </td>
                    <td>
                      <div className={styles.actions}>
                        <button className={styles.actionBtn} onClick={() => openEditModal(c)} aria-label="Sửa">✏️</button>
                        <button className={styles.actionBtn} onClick={() => handleToggleActive(c)} aria-label={c.active ? 'Tắt' : 'Bật'}>
                          {c.active ? '⏸' : '▶️'}
                        </button>
                        <button className={`${styles.actionBtn} ${styles.deleteBtn}`} onClick={() => setDeleteTarget(c)} aria-label="Xoá">🗑️</button>
                      </div>
                    </td>
                  </tr>
                ))}
              </tbody>
            </table>
          </div>

          {/* Modal: Add / Edit */}
          {showModal && (
            <div className={styles.overlay} onClick={closeModal}>
              <div className={styles.modal} onClick={(e) => e.stopPropagation()}>
                <div className={styles.modalHeader}>
                  <h3>{editTarget ? 'Chỉnh sửa coupon' : 'Thêm coupon mới'}</h3>
                  <button className={styles.closeBtn} onClick={closeModal}>✕</button>
                </div>
                <form className={styles.modalForm} onSubmit={handleSubmit(onSubmit)}>
                  <Input
                    label="Mã coupon"
                    {...register('code', { onChange: (e) => setValue('code', e.target.value.toUpperCase()) })}
                    error={errors.code?.message}
                    fullWidth
                    placeholder="VD: SUMMER2026"
                  />

                  <div className={styles.formRow}>
                    <label className={styles.radioGroup}>
                      <span style={{ marginBottom: 'var(--space-2)' }}>Loại</span>
                      <Controller
                        name="type"
                        control={control}
                        render={({ field }) => (
                          <div style={{ display: 'flex', gap: 'var(--space-3)' }}>
                            <label><input type="radio" value="PERCENT" checked={field.value === 'PERCENT'} onChange={() => field.onChange('PERCENT')} /> Phần trăm</label>
                            <label><input type="radio" value="FIXED" checked={field.value === 'FIXED'} onChange={() => field.onChange('FIXED')} /> Số tiền cố định</label>
                          </div>
                        )}
                      />
                    </label>
                    <Input
                      label={`Giá trị ${couponType === 'PERCENT' ? '(%)' : '(đ)'}`}
                      type="number"
                      {...register('value')}
                      error={errors.value?.message}
                      fullWidth
                    />
                  </div>

                  <Input
                    label="Đơn tối thiểu (đ)"
                    type="number"
                    {...register('minOrderAmount')}
                    error={errors.minOrderAmount?.message}
                    fullWidth
                  />

                  <div className={styles.formRow}>
                    <label className={styles.checkboxLine}>
                      <input type="checkbox" {...register('noLimit')} /> Không giới hạn lượt dùng
                    </label>
                    {!noLimit && (
                      <Input
                        label="Tối đa lượt dùng"
                        type="number"
                        {...register('maxTotalUses')}
                        error={errors.maxTotalUses?.message}
                        fullWidth
                      />
                    )}
                  </div>

                  <div className={styles.formRow}>
                    <label className={styles.checkboxLine}>
                      <input type="checkbox" {...register('noExpiry')} /> Không hết hạn
                    </label>
                    {!noExpiry && (
                      <Input
                        label="Ngày hết hạn"
                        type="datetime-local"
                        {...register('expiresAt')}
                        fullWidth
                      />
                    )}
                  </div>

                  <label className={styles.checkboxLine}>
                    <input type="checkbox" {...register('active')} /> Đang bật
                  </label>

                  <div className={styles.modalActions}>
                    <Button variant="secondary" type="button" onClick={closeModal}>Hủy</Button>
                    <Button type="submit" loading={saving}>{editTarget ? 'Lưu thay đổi' : 'Thêm coupon'}</Button>
                  </div>
                </form>
              </div>
            </div>
          )}

          {/* Confirm Delete */}
          {deleteTarget && (
            <div className={styles.overlay} onClick={() => { setDeleteTarget(null); setDeleteError(null); }}>
              <div className={styles.confirmModal} onClick={(e) => e.stopPropagation()}>
                <h3>Xác nhận xoá</h3>
                <p>Bạn có chắc muốn xoá coupon <strong>{deleteTarget.code}</strong>?</p>
                {deleteError && <p style={{ color: 'var(--error)', marginTop: 'var(--space-2)' }}>{deleteError}</p>}
                <div className={styles.confirmActions}>
                  <Button variant="secondary" onClick={() => { setDeleteTarget(null); setDeleteError(null); }}>Hủy</Button>
                  <Button variant="danger" onClick={handleConfirmDelete}>Xoá coupon</Button>
                </div>
              </div>
            </div>
          )}
        </div>
      );
    }
    ```

    **Tạo `page.module.css`** mirror admin/products/page.module.css. Đọc file đó trước, copy structure rồi adapt:
    ```css
    .page { padding: var(--space-5); }
    .header { display: flex; justify-content: space-between; align-items: center; margin-bottom: var(--space-4); }
    .title { font-size: var(--text-headline-md); font-weight: 700; }
    .toolbar { display: flex; gap: var(--space-3); align-items: center; margin-bottom: var(--space-4); }
    .toolbar > :first-child { flex: 1; }
    .select { padding: var(--space-2) var(--space-3); border-radius: var(--radius-md); border: 1.5px solid rgba(195,198,214,0.2); background: var(--surface-container-lowest); }
    .count { color: var(--on-surface-variant); }
    .tableWrapper { background: var(--surface-container-lowest); border-radius: var(--radius-xl); padding: var(--space-3); overflow: auto; }
    .table { width: 100%; border-collapse: collapse; }
    .table th, .table td { padding: var(--space-3); text-align: left; border-bottom: 1px solid rgba(195,198,214,0.15); }
    .actions { display: flex; gap: var(--space-2); }
    .actionBtn { background: none; border: none; cursor: pointer; font-size: 18px; padding: var(--space-1); }
    .deleteBtn { color: var(--error); }
    .overlay { position: fixed; inset: 0; background: rgba(0,0,0,0.5); display: flex; align-items: center; justify-content: center; z-index: 1000; }
    .modal { background: var(--surface-container-lowest); border-radius: var(--radius-xl); padding: var(--space-5); max-width: 600px; width: 90%; max-height: 90vh; overflow: auto; }
    .modalHeader { display: flex; justify-content: space-between; align-items: center; margin-bottom: var(--space-4); }
    .closeBtn { background: none; border: none; font-size: 20px; cursor: pointer; }
    .modalForm { display: flex; flex-direction: column; gap: var(--space-3); }
    .formRow { display: grid; grid-template-columns: 1fr 1fr; gap: var(--space-3); }
    .radioGroup { display: flex; flex-direction: column; }
    .checkboxLine { display: flex; align-items: center; gap: var(--space-2); }
    .modalActions { display: flex; justify-content: flex-end; gap: var(--space-3); margin-top: var(--space-3); }
    .confirmModal { background: var(--surface-container-lowest); border-radius: var(--radius-xl); padding: var(--space-5); max-width: 400px; }
    .confirmActions { display: flex; justify-content: flex-end; gap: var(--space-3); margin-top: var(--space-4); }
    ```
  </action>

  <verify>
    <automated>cd sources/frontend && npx tsc --noEmit && npm run lint</automated>
  </verify>

  <acceptance_criteria>
    - File tồn tại: page.tsx + page.module.css
    - Grep `useForm` trong page.tsx → 1 match
    - Grep `zodResolver` trong page.tsx → 1 match
    - Grep `couponSchema` trong page.tsx → ≥1 match (definition + use)
    - Grep `listAdminCoupons|createCoupon|updateCoupon|toggleCouponActive|deleteCoupon` trong page.tsx → ≥5 (all 5 services used)
    - Grep `COUPON_HAS_REDEMPTIONS` trong page.tsx → 1 match (delete error mapping)
    - Grep `'Coupon đã có người dùng — vui lòng tắt thay vì xoá'` trong page.tsx → 1 match (D-21 verbatim message)
    - Grep `'Phần trăm'` + `'Số tiền cố định'` trong page.tsx → 1 each
    - Grep `'Không giới hạn lượt dùng'` + `'Không hết hạn'` trong page.tsx → 1 each (D-21)
    - Grep `\.tableWrapper \{` trong page.module.css → 1 match
    - Grep `\.modalForm \{` trong page.module.css → 1 match
    - tsc + lint exit 0
  </acceptance_criteria>

  <done>
    Admin /admin/coupons hoàn chỉnh: list + filter + CRUD modal + toggle active + delete với error mapping. Sidebar nav có link mới. tsc + lint green.
  </done>
</task>

<task type="auto">
  <name>Task 3: Extend /profile/orders/[id]/page.tsx + /admin/orders/[id]/page.tsx hiển thị coupon block (D-23)</name>
  <files>
    sources/frontend/src/app/profile/orders/[id]/page.tsx,
    sources/frontend/src/app/admin/orders/[id]/page.tsx
  </files>

  <read_first>
    - sources/frontend/src/app/profile/orders/[id]/page.tsx (toàn bộ — tìm summary section / total row để chèn coupon block phía trên)
    - sources/frontend/src/app/admin/orders/[id]/page.tsx (toàn bộ — đặc biệt block "Tổng cộng" lines 196–201)
    - 20-CONTEXT.md D-23 (block layout: "Mã giảm giá: CODE_X" + "Giảm giá: -X đ" trên Tổng tiền)
  </read_first>

  <action>
    1. **Extend `/profile/orders/[id]/page.tsx`:**

       Đọc toàn bộ file trước. Tìm block render order summary / "Tổng tiền" / "Tổng cộng". Chèn block coupon NGAY TRƯỚC dòng total:

       ```tsx
       {order.couponCode && (
         <>
           <div className={styles.summaryRow}>
             <span>Mã giảm giá</span>
             <span><strong>{order.couponCode}</strong></span>
           </div>
           <div className={styles.summaryRow} style={{ color: 'var(--success, #10b981)' }}>
             <span>Giảm giá</span>
             <span>-{(order.discountAmount ?? 0).toLocaleString('vi-VN')}₫</span>
           </div>
         </>
       )}
       ```

       (Nếu file dùng inline styles thay vì CSS module, mirror pattern; nếu dùng formatPrice utility, dùng `formatPrice(order.discountAmount)`.)

       **Quan trọng:** chỉ hiển thị block khi `order.couponCode` truthy. Order cũ (chưa có coupon) → KHÔNG render gì thêm.

    2. **Extend `/admin/orders/[id]/page.tsx`:**

       Tìm block "Tổng cộng" (line ~196–201):
       ```tsx
       <div style={{ borderTop: '1px solid rgba(195,198,214,0.15)', marginTop: 'var(--space-3)', paddingTop: 'var(--space-3)', textAlign: 'right' }}>
         <span style={{ fontWeight: 700, fontSize: 'var(--text-title-sm)' }}>Tổng cộng: </span>
         <span style={{ color: 'var(--primary)', fontWeight: 700 }}>
           {(order.totalAmount ?? order.total ?? 0).toLocaleString('vi-VN')}₫
         </span>
       </div>
       ```

       Chèn block coupon NGAY TRƯỚC div này:
       ```tsx
       {order.couponCode && (
         <div style={{ borderTop: '1px solid rgba(195,198,214,0.15)', marginTop: 'var(--space-3)', paddingTop: 'var(--space-3)', textAlign: 'right' }}>
           <div style={{ marginBottom: 'var(--space-1)' }}>
             <span style={{ color: 'var(--on-surface-variant)' }}>Mã giảm giá: </span>
             <strong>{order.couponCode}</strong>
           </div>
           <div style={{ color: 'var(--success, #10b981)' }}>
             <span>Giảm giá: </span>
             <strong>-{(order.discountAmount ?? 0).toLocaleString('vi-VN')}₫</strong>
           </div>
         </div>
       )}
       ```

       Để giữ "Tổng cộng" hiển thị final total (đã trừ discount nếu có), giá trị `totalAmount` từ BE đã = subtotal - discountAmount (Plan 20-03 set `placeholder.update(userId, totalWithDiscount, ...)` trước save). Vì vậy KHÔNG cần subtract discount lần nữa ở FE — chỉ hiển thị 2 dòng coupon info bổ sung.
  </action>

  <verify>
    <automated>cd sources/frontend && npx tsc --noEmit && npm run lint</automated>
  </verify>

  <acceptance_criteria>
    - Grep `order\.couponCode` trong profile/orders/[id]/page.tsx → ≥1 match
    - Grep `order\.couponCode` trong admin/orders/[id]/page.tsx → ≥1 match
    - Grep `order\.discountAmount` trong profile/orders/[id]/page.tsx → ≥1 match
    - Grep `order\.discountAmount` trong admin/orders/[id]/page.tsx → ≥1 match
    - Grep `'Mã giảm giá'` trong cả 2 file → 1 each
    - Grep `'Giảm giá'` trong cả 2 file → 1 each
    - tsc + lint exit 0
  </acceptance_criteria>

  <done>
    Cả 2 order detail page hiển thị coupon block khi `order.couponCode` non-null. Backward compat: order cũ KHÔNG render block thêm. tsc + lint green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser admin → api-gateway | Untrusted admin JWT — gateway pass-through, BE JwtRoleGuard verify role |
| Admin form input → BE validation | rhf+zod client-side; BE jakarta annotations server-side (defense-in-depth) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-06-01 | Spoofing | /admin/coupons accessible by non-admin | mitigate | Admin layout gate (existing pattern Phase 9 / Phase 19); BE JwtRoleGuard.requireAdmin trên mọi /admin/coupons endpoint reject 401/403. |
| T-20-06-02 | Tampering | Form bypass client validation | mitigate | rhf+zod is UX-only; BE @Pattern + @DecimalMin reject invalid (Plan 20-02 Test C4). |
| T-20-06-03 | Information Disclosure | Order detail leak coupon for other users | mitigate | `/api/orders/{id}` BE check ownership (Phase 11 ACCT-02 — getOrder verify userId match X-User-Id). FE chỉ render từ trả về của BE. |
| T-20-06-04 | Tampering (XSS) | Coupon code chứa script injected vào order detail | mitigate | BE @Pattern regex `^[A-Z0-9_-]{3,32}$` reject special chars. React JSX auto-escape. |
| T-20-06-05 | Information Disclosure | discountAmount của order khác user | mitigate | (Same as 03 — ownership check ở BE). |
| T-20-06-06 | Repudiation | Admin xoá nhầm coupon hậu quả | mitigate | D-14 hard-delete chỉ khi used_count=0; nếu có redemption → 409 + UI gợi ý "tắt thay vì xoá". Soft-disable preferred. |
</threat_model>

<verification>
- Quick run: `cd sources/frontend && npx tsc --noEmit && npm run lint`
- Manual UAT (sau backend Plan 20-01..04 deploy):
  1. Login admin → /admin → click sidebar 'Coupon' → /admin/coupons hiển thị
  2. Click '+ Thêm coupon' → fill form: code=DEMO123, type=PERCENT, value=15, minOrder=100000, noLimit, noExpiry, active → Submit → toast success + row mới xuất hiện
  3. Click ✏️ row → modal pre-filled → đổi value=20 → Lưu → row update
  4. Click ⏸ → toast "Đã tắt" + Badge đổi thành "Đã tắt"
  5. Click ▶️ → toast "Đã bật" + Badge đổi thành "Đang bật"
  6. Click 🗑 trên coupon used_count=0 → confirm → toast success + row biến mất
  7. (Cần seed redemption thủ công) Click 🗑 trên coupon đã có redemption → modal lỗi "Coupon đã có người dùng — vui lòng tắt thay vì xoá"
  8. User đặt order với coupon DEMO123 → vào /profile/orders/{id} → thấy "Mã giảm giá: DEMO123" + "Giảm giá: -X đ" trên Tổng cộng
  9. Admin vào /admin/orders/{id} của order trên → cũng thấy block coupon
</verification>

<success_criteria>
- 7 file modified/tạo (3 Task 1 + 2 Task 2 + 2 Task 3)
- tsc + lint green
- Admin sidebar có link Coupon
- /admin/coupons CRUD đầy đủ + delete error map đúng
- /profile/orders/[id] và /admin/orders/[id] hiển thị coupon block khi non-null
</success_criteria>

<output>
Sau khi hoàn tất, tạo `.planning/phases/20-coupons/20-06-SUMMARY.md` với artifacts, decision refs (D-21/D-22/D-23/D-24), tsc+lint output, manual UAT transcript 9 bước.
</output>
</content>
</invoke>