---
phase: 20
plan: 05
type: execute
wave: 4
depends_on: [20-03, 20-04]
files_modified:
  - sources/frontend/src/services/coupons.ts
  - sources/frontend/src/services/orders.ts
  - sources/frontend/src/lib/couponErrorMessages.ts
  - sources/frontend/src/hooks/useApplyCoupon.ts
  - sources/frontend/src/app/checkout/page.tsx
  - sources/frontend/src/app/checkout/page.module.css
  - sources/frontend/src/types/index.ts
autonomous: true
requirements: [COUP-03]
tags: [frontend, checkout, coupon, react-query, vietnamese-ui]

must_haves:
  truths:
    - "User mở /checkout thấy section 'Mã giảm giá' giữa form và order summary với input + nút 'Áp dụng' (D-17)"
    - "User nhập mã hợp lệ + click 'Áp dụng' → BE preview validate trả discount → UI hiển thị chip 'CODE_X (-150.000 đ) [Bỏ]' + cập nhật order summary với 3 dòng (Tạm tính / Giảm giá / Tổng tiền) (D-17, D-20)"
    - "User nhập mã sai/expired/đã dùng → toast Vietnamese cụ thể theo mã lỗi BE (COUPON_NOT_FOUND/EXPIRED/INACTIVE/MIN_ORDER_NOT_MET/ALREADY_REDEEMED/MAX_USES_REACHED) (D-11, D-18)"
    - "User thay đổi cart items (add/remove/update qty) sau khi áp coupon → auto re-validate; nếu fail (ví dụ subtotal mới không đạt minOrder) → clear coupon + toast 'Mã giảm giá không còn áp dụng được' (D-18)"
    - "User click 'Đặt hàng' → POST /api/orders body có couponCode (null nếu chưa apply) → BE atomic redeem; nếu fail → toast cụ thể, KHÔNG clear cart (D-19)"
    - "Khi BE atomic fail (ví dụ coupon vừa bị disable/race-lose) → user vẫn ở /checkout, có thể bỏ mã + retry (D-19)"
  artifacts:
    - path: "sources/frontend/src/services/coupons.ts"
      provides: "validateCoupon(code, cartTotal, userId?) → CouponPreviewResponse"
      contains: "validateCoupon"
    - path: "sources/frontend/src/services/orders.ts"
      provides: "Extended createOrder body với couponCode optional"
      contains: "couponCode"
    - path: "sources/frontend/src/lib/couponErrorMessages.ts"
      provides: "Map 8 CouponErrorCode → Vietnamese message + extractMinOrder helper"
      contains: "COUPON_NOT_FOUND"
    - path: "sources/frontend/src/hooks/useApplyCoupon.ts"
      provides: "React Query useMutation hook + helper hook quản lý applied coupon state"
      contains: "useMutation"
    - path: "sources/frontend/src/app/checkout/page.tsx"
      provides: "+Coupon section với Input/Button/chip + auto re-validate effect + submit body extend"
      contains: "applyCouponMutation"
    - path: "sources/frontend/src/app/checkout/page.module.css"
      provides: "+CSS classes cho coupon section (couponSection, couponRow, couponChip, discountRow)"
      contains: "couponChip"
    - path: "sources/frontend/src/types/index.ts"
      provides: "+CouponPreview type + extend CreateOrderRequest với optional couponCode"
      contains: "CouponPreview"
  key_links:
    - from: "Input + Áp dụng button"
      to: "useApplyCoupon mutation"
      via: "click handler gọi mutation.mutate({code: input, cartTotal: subtotal})"
      pattern: "applyCouponMutation\\.mutate"
    - from: "useApplyCoupon onError"
      to: "couponErrorMessages map"
      via: "switch err.code (ApiError) → showToast(message, 'error')"
      pattern: "couponErrorMessages\\["
    - from: "useEffect dependency [cartItems]"
      to: "applyCouponMutation auto re-call"
      via: "khi cartItems thay đổi và đã có applied coupon → mutation.mutate lại với cartTotal mới"
      pattern: "useEffect.*\\[cartItems"
    - from: "submitOrder createOrder body"
      to: "appliedCoupon.code (or null)"
      via: "include couponCode field in CreateOrderRequest"
      pattern: "couponCode:"
---

<objective>
Triển khai FE checkout coupon UX theo D-17/D-18/D-19/D-20: input mã + nút Áp dụng + chip display + auto re-validate khi cart đổi + extend createOrder body + error mapping Vietnamese.

Purpose: COUP-03 (FE checkout input) hoàn chỉnh. User có thể nhập mã → preview → submit order với coupon → BE atomic redeem.

Output: 5 file mới + 2 file modified (checkout page + types). React Query mutation hook + Vietnamese error map + UI integration với existing checkout layout.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-coupons/20-CONTEXT.md
@.planning/phases/20-coupons/20-03-PLAN.md

@sources/frontend/src/app/checkout/page.tsx
@sources/frontend/src/services/orders.ts
@sources/frontend/src/services/http.ts
@sources/frontend/src/services/errors.ts
@sources/frontend/src/types/index.ts

<interfaces>
<!-- BE contract từ Plan 20-03 -->

POST /api/orders/coupons/validate
  Request body: { code: string, cartTotal: number }
  Header: X-User-Id (optional but PHẢI gửi cho user logged-in để check already-redeemed)
  Response 200: ApiResponse<CouponPreviewResponse>
    CouponPreviewResponse: { code, type: "PERCENT"|"FIXED", value, discountAmount, finalTotal, message }
  Response 4xx: ApiErrorResponse với code = 1 trong:
    "COUPON_NOT_FOUND" (404)
    "COUPON_INACTIVE" (422)
    "COUPON_EXPIRED" (422)
    "COUPON_MIN_ORDER_NOT_MET" (422, details.minOrderAmount = number)
    "COUPON_ALREADY_REDEEMED" (409)
    "COUPON_MAX_USES_REACHED" (409)

POST /api/orders (existing extended Plan 20-03):
  Body shape: existing + couponCode?: string  (null/undefined → no coupon applied)
  4xx errors: existing stock conflict + thêm CouponException codes nếu BE atomic fail tại submit time

ApiError class (existing services/errors.ts):
  code: string, status: number, message: string, details?: Record<string, unknown>
  isApiError(e): typeguard

httpPost (existing services/http.ts):
  httpPost<T>(path, body?, extraHeaders?) — auto Bearer + envelope unwrap

useCart hook (existing hooks/useCart):
  data: CartItem[] với { productId, name, price, quantity, thumbnailUrl }

useToast hook (existing components/ui/Toast):
  showToast(message, 'success' | 'error')

formatPrice utility (existing services/api):
  formatPrice(n: number) → "150.000 đ"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: services/coupons.ts + couponErrorMessages.ts + useApplyCoupon.ts hook + types extension</name>
  <files>
    sources/frontend/src/services/coupons.ts,
    sources/frontend/src/lib/couponErrorMessages.ts,
    sources/frontend/src/hooks/useApplyCoupon.ts,
    sources/frontend/src/services/orders.ts,
    sources/frontend/src/types/index.ts
  </files>

  <read_first>
    - sources/frontend/src/services/orders.ts (toàn bộ — analog: createOrder với userId/X-User-Id pattern, httpPost usage)
    - sources/frontend/src/services/http.ts (extraHeaders X-User-Id pattern)
    - sources/frontend/src/services/errors.ts (ApiError shape + isApiError)
    - 20-CONTEXT.md D-11 (8 error codes Vietnamese verbatim), D-13 (preview path), D-18 (mutation pattern), D-19 (submit body extend)
  </read_first>

  <action>
    1. **Tạo `sources/frontend/src/services/coupons.ts`:**
       ```typescript
       /**
        * Coupon service API — preview validate (D-13).
        * BE: POST /api/orders/coupons/validate body {code, cartTotal} header X-User-Id.
        * Read-only: KHÔNG mutate state, atomic redemption diễn ra ở POST /api/orders.
        */
       import { httpPost } from './http';
       import type { CouponPreview } from '@/types';

       export interface CouponValidateBody {
         code: string;
         cartTotal: number;
       }

       export function validateCoupon(body: CouponValidateBody, userId?: string): Promise<CouponPreview> {
         const headers: Record<string, string> = {};
         if (userId) headers['X-User-Id'] = userId;
         return httpPost<CouponPreview>('/api/orders/coupons/validate', body, headers);
       }
       ```

    2. **Extend `sources/frontend/src/types/index.ts`** — thêm types:
       ```typescript
       export interface CouponPreview {
         code: string;
         type: 'PERCENT' | 'FIXED';
         value: number;
         discountAmount: number;
         finalTotal: number;
         message: string;
       }
       ```
       
       Tìm `CreateOrderRequest` interface hiện có và extend:
       ```typescript
       // Trước:
       // export interface CreateOrderRequest { items, shippingAddress, paymentMethod, note?: string }
       // Sau (thêm couponCode optional cuối):
       // ... existing fields
       // couponCode?: string;
       ```
       Đảm bảo KHÔNG break existing usages — couponCode là optional.

       Read `types/index.ts` trước để biết exact CreateOrderRequest signature, sau đó thêm `couponCode?: string;` vào cuối interface.

    3. **Extend `sources/frontend/src/services/orders.ts` `createOrder` JSDoc** — note rằng body có thể có couponCode (KHÔNG cần thay đổi function signature vì TypeScript interface đã extend ở step 2). Chỉ thêm comment:
       ```typescript
       /**
        * Create an order. ... existing JSDoc ...
        * Phase 20 / COUP-03: body có thể có optional couponCode — BE atomic redeem trong cùng transaction.
        * Nếu BE atomic fail → throw ApiError với code = COUPON_* (xem couponErrorMessages.ts).
        */
       ```

    4. **Tạo `sources/frontend/src/lib/couponErrorMessages.ts`:**
       ```typescript
       /**
        * D-11: Map BE CouponErrorCode → Vietnamese message cho user toast.
        * Match verbatim CONTEXT.md D-11 message text.
        */
       export const couponErrorMessages: Record<string, string> = {
         COUPON_NOT_FOUND: 'Mã giảm giá không tồn tại',
         COUPON_INACTIVE: 'Mã giảm giá đã bị vô hiệu hoá',
         COUPON_EXPIRED: 'Mã giảm giá đã hết hạn',
         COUPON_MIN_ORDER_NOT_MET: 'Đơn hàng không đạt giá trị tối thiểu để dùng mã này',
         COUPON_ALREADY_REDEEMED: 'Bạn đã sử dụng mã giảm giá này',
         COUPON_MAX_USES_REACHED: 'Mã giảm giá đã hết lượt sử dụng',
         COUPON_CONFLICT_OR_EXHAUSTED: 'Mã giảm giá không còn khả dụng',
         COUPON_HAS_REDEMPTIONS: 'Mã giảm giá đã có người dùng — vui lòng tắt thay vì xoá',
       };

       /**
        * Format error message với details nếu có (D-11 minOrderAmount).
        * Trả null nếu code không phải COUPON_* — caller fallback toast generic.
        */
       export function formatCouponError(
         code: string,
         details?: Record<string, unknown>,
       ): string | null {
         const base = couponErrorMessages[code];
         if (!base) return null;
         if (code === 'COUPON_MIN_ORDER_NOT_MET' && details && typeof details.minOrderAmount === 'number') {
           const formatted = new Intl.NumberFormat('vi-VN').format(details.minOrderAmount);
           return `Đơn hàng tối thiểu ${formatted} đ để dùng mã này`;
         }
         return base;
       }

       export function isCouponError(code: string): boolean {
         return code.startsWith('COUPON_');
       }
       ```

    5. **Tạo `sources/frontend/src/hooks/useApplyCoupon.ts`:**
       ```typescript
       /**
        * D-18: React Query mutation cho preview validation.
        * Caller (checkout/page.tsx) quản lý applied coupon state local + auto re-validate khi cart đổi.
        */
       import { useMutation } from '@tanstack/react-query';
       import { validateCoupon, type CouponValidateBody } from '@/services/coupons';
       import type { CouponPreview } from '@/types';

       export function useApplyCoupon(userId?: string) {
         return useMutation<CouponPreview, Error, CouponValidateBody>({
           mutationFn: (body: CouponValidateBody) => validateCoupon(body, userId),
         });
       }
       ```

       (KHÔNG dùng `useQueryClient` invalidate vì preview là client-side state local component, không phải global cache.)
  </action>

  <verify>
    <automated>cd sources/frontend && npx tsc --noEmit && npm run lint</automated>
  </verify>

  <acceptance_criteria>
    - File tồn tại: 4 file source + 1 file types extension
    - Grep `validateCoupon` trong services/coupons.ts → 1 export match
    - Grep `'/api/orders/coupons/validate'` trong services/coupons.ts → 1 match
    - Grep `'X-User-Id'` trong services/coupons.ts → 1 match
    - Grep tất cả 8 keys `COUPON_NOT_FOUND`, `COUPON_INACTIVE`, ..., `COUPON_HAS_REDEMPTIONS` trong couponErrorMessages.ts → 8 match
    - Grep `useApplyCoupon` trong hooks/useApplyCoupon.ts → 1 export match
    - Grep `useMutation` trong hooks/useApplyCoupon.ts → 1 match
    - Grep `couponCode\?:` trong types/index.ts → 1 match (extension CreateOrderRequest)
    - Grep `interface CouponPreview` trong types/index.ts → 1 match
    - tsc + lint exit 0
  </acceptance_criteria>

  <done>
    Service + hook + error map + types đầy đủ; tsc + lint green; sẵn sàng cho Task 2 wire vào checkout page.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend /checkout/page.tsx với coupon section + auto re-validate + submit body extend + CSS</name>
  <files>
    sources/frontend/src/app/checkout/page.tsx,
    sources/frontend/src/app/checkout/page.module.css
  </files>

  <read_first>
    - sources/frontend/src/app/checkout/page.tsx (toàn bộ — đặc biệt summary block lines 269–313 và submitOrder lines 111–183)
    - sources/frontend/src/app/checkout/page.module.css (xem các CSS class hiện có cho summary/totalRow để mirror style)
    - 20-CONTEXT.md D-17 (vị trí UI giữa cart và order summary), D-18 (mutation + auto re-validate trigger), D-19 (submit body extend), D-20 (3 dòng tổng tiền layout)
  </read_first>

  <action>
    Hai thay đổi chính trong `checkout/page.tsx`:

    **A. Thêm coupon state + mutation + auto re-validate effect ở top of component:**

    Thêm imports đầu file:
    ```typescript
    import { useApplyCoupon } from '@/hooks/useApplyCoupon';
    import { formatCouponError, isCouponError } from '@/lib/couponErrorMessages';
    import type { CouponPreview } from '@/types';
    ```

    Sau dòng `const [paymentModal, setPaymentModal] = useState(false);` thêm:
    ```typescript
    // === COUPON STATE (D-17, D-18) ===
    const [couponInput, setCouponInput] = useState('');
    const [appliedCoupon, setAppliedCoupon] = useState<CouponPreview | null>(null);
    const applyCouponMutation = useApplyCoupon(user?.id);

    const subtotalForCoupon = subtotal;  // alias để clear intent

    const handleApplyCoupon = async () => {
      const code = couponInput.trim().toUpperCase();
      if (!code) {
        showToast('Vui lòng nhập mã giảm giá', 'error');
        return;
      }
      try {
        const preview = await applyCouponMutation.mutateAsync({ code, cartTotal: subtotalForCoupon });
        setAppliedCoupon(preview);
        setCouponInput('');
        showToast('Áp dụng mã thành công', 'success');
      } catch (err) {
        if (isApiError(err) && isCouponError(err.code)) {
          const msg = formatCouponError(err.code, err.details) ?? 'Không thể áp dụng mã giảm giá';
          showToast(msg, 'error');
        } else {
          showToast('Không thể áp dụng mã giảm giá, vui lòng thử lại', 'error');
        }
      }
    };

    const handleRemoveCoupon = () => {
      setAppliedCoupon(null);
      setCouponInput('');
    };

    // D-18: Auto re-validate khi cart đổi sau khi đã apply coupon
    useEffect(() => {
      if (!appliedCoupon || cartItems.length === 0) return;
      let alive = true;
      validateCoupon({ code: appliedCoupon.code, cartTotal: subtotal }, user?.id)
        .then((preview) => {
          if (!alive) return;
          setAppliedCoupon(preview);
        })
        .catch((err) => {
          if (!alive) return;
          // Cart thay đổi làm coupon không còn valid (ví dụ subtotal mới < minOrder)
          setAppliedCoupon(null);
          if (isApiError(err) && isCouponError(err.code)) {
            showToast('Mã giảm giá không còn áp dụng được', 'error');
          }
        });
      return () => { alive = false; };
      // eslint-disable-next-line react-hooks/exhaustive-deps
    }, [subtotal]);  // dependency on subtotal — đổi khi cart đổi
    ```

    Thêm import `validateCoupon` từ `@/services/coupons` ở đầu file.

    **B. Extend submitOrder để gửi couponCode:**

    Trong `submitOrder` (line ~111), tìm body của `createOrder({...})` và thêm field `couponCode`:
    ```typescript
    const order = await createOrder({
      items: cartItems.map(...),
      shippingAddress: {...},
      paymentMethod: form.paymentMethod,
      note: form.note || undefined,
      couponCode: appliedCoupon?.code,   // D-19: null/undefined nếu chưa apply
    }, user?.id);
    ```

    Trong catch block, thêm CouponException case TRƯỚC `default`:
    ```typescript
    default: {
      if (isCouponError(err.code)) {
        const msg = formatCouponError(err.code, err.details) ?? 'Mã giảm giá không khả dụng';
        showToast(msg, 'error');
        // D-19: KHÔNG clear cart — user có thể bỏ mã + retry
        setAppliedCoupon(null);
      } else {
        showToast('Đã có lỗi, vui lòng thử lại', 'error');
      }
      break;
    }
    ```

    **C. Thêm coupon section UI** TRƯỚC `<Right Column — Summary>` aside (D-17 — giữa form và summary). Vì layout 2-column hiện có, đặt section này ngay đầu summary block (line ~271 sau `<aside className={styles.summaryColumn}>`):

    ```tsx
    <aside className={styles.summaryColumn}>
      <div className={styles.summary}>
        <h3 className={styles.summaryTitle}>Đơn hàng của bạn</h3>

        {/* Items list (existing) */}
        <div className={styles.summaryItems}>
          {/* ... existing ... */}
        </div>

        {/* === COUPON SECTION (D-17) === */}
        <div className={styles.couponSection}>
          <h4 className={styles.couponTitle}>Mã giảm giá</h4>
          {!appliedCoupon ? (
            <div className={styles.couponRow}>
              <Input
                placeholder="Nhập mã giảm giá"
                value={couponInput}
                onChange={(e) => setCouponInput(e.target.value.toUpperCase())}
                fullWidth
              />
              <Button
                type="button"
                variant="secondary"
                onClick={handleApplyCoupon}
                loading={applyCouponMutation.isPending}
              >
                Áp dụng
              </Button>
            </div>
          ) : (
            <div className={styles.couponChip}>
              <span className={styles.couponChipCode}>{appliedCoupon.code}</span>
              <span className={styles.couponChipDiscount}>
                -{formatPrice(appliedCoupon.discountAmount)}
              </span>
              <button
                type="button"
                className={styles.couponChipRemove}
                onClick={handleRemoveCoupon}
                aria-label="Bỏ mã giảm giá"
              >
                Bỏ
              </button>
            </div>
          )}
        </div>

        {/* === SUMMARY ROWS (D-20) === */}
        <div className={styles.summaryRows}>
          <div className={styles.summaryRow}>
            <span>Tạm tính</span>
            <span>{formatPrice(subtotal)}</span>
          </div>
          <div className={styles.summaryRow}>
            <span>Phí vận chuyển</span>
            <span className={shippingFee === 0 ? styles.free : ''}>
              {shippingFee === 0 ? 'Miễn phí' : formatPrice(shippingFee)}
            </span>
          </div>
          {appliedCoupon && (
            <div className={`${styles.summaryRow} ${styles.discountRow}`}>
              <span>Giảm giá ({appliedCoupon.code})</span>
              <span>-{formatPrice(appliedCoupon.discountAmount)}</span>
            </div>
          )}
        </div>
        <div className={styles.totalRow}>
          <span>Tổng cộng</span>
          <span className={styles.totalPrice}>
            {formatPrice(Math.max(0, total - (appliedCoupon?.discountAmount ?? 0)))}
          </span>
        </div>
        <Button type="submit" size="lg" fullWidth loading={loading} disabled={cartItems.length === 0}>
          Đặt hàng
        </Button>
        <Link href="/cart" className={styles.backLink}>← Quay lại giỏ hàng</Link>
      </div>
    </aside>
    ```

    **CRITICAL:** `total` hiện có = `subtotal + shippingFee`. Khi có coupon, hiển thị `total - discountAmount` (cap min 0).

    **D. Thêm CSS classes vào `checkout/page.module.css`:**
    ```css
    .couponSection {
      padding: var(--space-3) 0;
      border-top: 1px solid var(--outline-variant, rgba(195,198,214,0.15));
      border-bottom: 1px solid var(--outline-variant, rgba(195,198,214,0.15));
      margin: var(--space-3) 0;
    }
    .couponTitle {
      font-size: var(--text-body-md);
      font-weight: 600;
      margin-bottom: var(--space-2);
    }
    .couponRow {
      display: flex;
      gap: var(--space-2);
      align-items: flex-end;
    }
    .couponRow > :global(.input-wrapper),
    .couponRow > :first-child {
      flex: 1;
    }
    .couponChip {
      display: inline-flex;
      align-items: center;
      gap: var(--space-2);
      padding: var(--space-2) var(--space-3);
      background: var(--primary-container, rgba(94,124,255,0.12));
      border-radius: var(--radius-md);
      font-size: var(--text-body-md);
    }
    .couponChipCode {
      font-weight: 600;
      color: var(--primary);
    }
    .couponChipDiscount {
      color: var(--success, #10b981);
      font-weight: 500;
    }
    .couponChipRemove {
      background: none;
      border: none;
      color: var(--on-surface-variant);
      cursor: pointer;
      text-decoration: underline;
      font-size: var(--text-body-sm);
      padding: 0 var(--space-1);
    }
    .couponChipRemove:hover {
      color: var(--error, #ef4444);
    }
    .discountRow {
      color: var(--success, #10b981);
      font-weight: 500;
    }
    ```

    Match exact selector names mà JSX trên dùng. Nếu CSS variables `--primary-container`, `--success`, `--outline-variant` không tồn tại global, dùng fallback hex như trên.
  </action>

  <verify>
    <automated>cd sources/frontend && npx tsc --noEmit && npm run lint</automated>
  </verify>

  <acceptance_criteria>
    - Grep `appliedCoupon` trong checkout/page.tsx → ≥6 match (state + setState + conditional render + auto-revalidate dep + summary row + total)
    - Grep `useApplyCoupon` trong checkout/page.tsx → 1 import + 1 call
    - Grep `applyCouponMutation\.mutateAsync` → 1 match
    - Grep `formatCouponError` trong checkout/page.tsx → ≥2 match (apply error + submit error)
    - Grep `couponCode: appliedCoupon\?\.code` trong checkout/page.tsx → 1 match (D-19 submit body)
    - Grep `Mã giảm giá` trong checkout/page.tsx → ≥2 match (section title + label/placeholder)
    - Grep `\.couponSection \{` trong checkout/page.module.css → 1 match
    - Grep `\.couponChip \{` trong checkout/page.module.css → 1 match
    - Grep `\.discountRow \{` trong checkout/page.module.css → 1 match
    - Grep `Áp dụng` trong checkout/page.tsx → 1 match (button label)
    - tsc + lint exit 0
  </acceptance_criteria>

  <done>
    Coupon UI hoạt động end-to-end với BE: input → áp dụng → chip → re-validate khi cart đổi → submit với couponCode. Toast error cụ thể theo mã. tsc + lint green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser checkout page → api-gateway | Untrusted input (mã coupon) — BE validate đầy đủ |
| Local component state → submitOrder | appliedCoupon state có thể bị tampered qua devtools — BE atomic step KHÔNG TIN client (D-10) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-05-01 | Tampering | User edit appliedCoupon trong devtools để discount tăng | mitigate (BE) | BE atomic step compute discount server-side từ subtotal (D-10). Client appliedCoupon chỉ là UX hint — KHÔNG affect actual order total. |
| T-20-05-02 | Tampering | Input coupon code chứa script | mitigate | `value.toUpperCase()` client filter; BE @Pattern regex `^[A-Z0-9_-]{3,32}$` reject input không match. React JSX auto-escape → KHÔNG XSS. |
| T-20-05-03 | Information Disclosure | Coupon code visible trên Network tab | accept | Coupon code public (admin tạo cho user dùng). KHÔNG nhạy cảm. |
| T-20-05-04 | Spoofing | User chưa login spam preview endpoint | accept (defer) | D-deferred rate-limit. Endpoint read-only, không mutate. X-User-Id header optional cho preview. |
| T-20-05-05 | Repudiation | User claim "đã apply mã nhưng order không có discount" | mitigate | OrderDto BE trả discountAmount + couponCode cho FE — Plan 20-06 hiển thị ở /profile/orders/[id]. User verify được. |
</threat_model>

<verification>
- Quick run: `cd sources/frontend && npx tsc --noEmit && npm run lint`
- Manual UAT (sau docker-compose up backend đã có Plan 20-01..04 deploy + admin tạo coupon SUMMER2026 PERCENT 10%):
  1. Login user → /checkout → cart có items
  2. Nhập "SUMMER2026" → Áp dụng → expect chip + 3 dòng summary có "Giảm giá: -X đ"
  3. Đổi qty cart → expect auto re-validate + chip cập nhật
  4. Nhập "INVALID" → Áp dụng → expect toast "Mã giảm giá không tồn tại"
  5. Click Đặt hàng → expect order tạo thành công với discount; navigate `/profile/orders/{id}` (Plan 20-06 sẽ display)
</verification>

<success_criteria>
- 7 file modified/tạo (5 Task 1 + 2 Task 2)
- tsc + lint green
- Manual UAT happy path: input → áp dụng → chip → submit có discount
- 6 error cases hiển thị toast Vietnamese đúng theo D-11
- Auto re-validate khi cart đổi
</success_criteria>

<output>
Sau khi hoàn tất, tạo `.planning/phases/20-coupons/20-05-SUMMARY.md` với artifacts, decision refs (D-11/D-17/D-18/D-19/D-20), tsc+lint output, manual UAT screenshot/transcript.
</output>
</content>
</invoke>