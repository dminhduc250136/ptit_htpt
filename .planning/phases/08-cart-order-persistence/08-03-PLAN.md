---
phase: 08-cart-order-persistence
plan: 03
type: execute
wave: 2
depends_on:
  - 08-02
files_modified:
  - sources/frontend/src/app/profile/orders/[id]/page.tsx
  - sources/frontend/src/app/profile/orders/[id]/page.module.css
  - sources/frontend/src/app/checkout/page.tsx
  - sources/frontend/src/types/index.ts
autonomous: true
requirements:
  - PERSIST-03

must_haves:
  truths:
    - "Sau checkout thành công, browser redirect sang /account/orders/{id} (không còn modal inline)"
    - "/account/orders/{id} gọi getOrderById(id) thật và render full breakdown"
    - "Order detail hiện items table (product name, quantity, unit price, line total)"
    - "Order detail hiện shipping address và payment method"
    - "Order detail hiện subtotal / shipping fee / total"
    - "Loading state: skeleton 3 rows trong khi fetch"
    - "Error state: RetrySection nếu fetch fail; empty state nếu 404"
  artifacts:
    - path: "sources/frontend/src/app/profile/orders/[id]/page.tsx"
      provides: "Order detail page wire real API với async fetch + full breakdown UI"
      contains: "getOrderById"
    - path: "sources/frontend/src/app/profile/orders/[id]/page.module.css"
      provides: "CSS classes cho items table theo UI-SPEC"
      contains: "itemsTable"
    - path: "sources/frontend/src/app/checkout/page.tsx"
      provides: "Redirect sang /account/orders/{id} sau createOrder() thành công"
      contains: "router.push"
    - path: "sources/frontend/src/types/index.ts"
      provides: "Order type update với items + shippingAddress + paymentMethod + orderCode field"
      contains: "items: OrderItem[]"
  key_links:
    - from: "checkout/page.tsx submitOrder()"
      to: "/account/orders/{order.id}"
      via: "router.push sau createOrder() success"
      pattern: "router\\.push.*account/orders"
    - from: "orders/[id]/page.tsx"
      to: "getOrderById(id)"
      via: "useEffect fetch → setState"
      pattern: "getOrderById"
---

<objective>
Wire FE order detail page để render full breakdown thật từ backend payload (Plan 08-02). Bỏ modal success inline ở checkout, thay bằng redirect sang `/account/orders/{id}`. Update `Order` type trong types/index.ts cho phù hợp với backend payload mới.

Purpose: End-to-end visible flow: checkout → persist → redirect → order detail hiển thị breakdown thật.
Output: checkout redirect + order detail page full UI theo UI-SPEC §CSS Modules Contract.
</objective>

<execution_context>
@D:/SYP_PROJECT/gsd-learning/tmdt-use-gsd/.claude/get-shit-done/workflows/execute-plan.md
@D:/SYP_PROJECT/gsd-learning/tmdt-use-gsd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:/SYP_PROJECT/gsd-learning/tmdt-use-gsd/.planning/ROADMAP.md
@D:/SYP_PROJECT/gsd-learning/tmdt-use-gsd/.planning/phases/08-cart-order-persistence/08-CONTEXT.md
@D:/SYP_PROJECT/gsd-learning/tmdt-use-gsd/.planning/phases/08-cart-order-persistence/08-UI-SPEC.md

<interfaces>
<!-- Từ orders.ts — service functions đã có -->
<!-- getOrderById(id: string): Promise<Order> → GET /api/orders/orders/{id} -->
<!-- createOrder(body: CreateOrderRequest, userId?: string): Promise<Order> -->

<!-- Từ page.tsx hiện tại (profile/orders/[id]/page.tsx) -->
<!-- - Hiện: loadOrder() trả undefined → render "Đơn hàng không tồn tại" với placeholder text -->
<!-- - Cần: async fetch getOrderById(id) → setState → render full layout đã có sẵn -->
<!-- - Layout đã có: tracker, grid, section (items + priceBreakdown), infoColumn (address + payment) -->
<!-- - statusMap, paymentMethodMap, paymentStatusMap ĐÃ CÓ SẴN — giữ nguyên -->
<!-- - order.items.map() ĐÃ CÓ trong layout — chỉ cần wire real data -->
<!-- - order.shippingAddress, order.paymentMethod, order.paymentStatus ĐÃ DÙNG trong layout -->

<!-- Từ checkout/page.tsx -->
<!-- - submitOrder(): hiện gọi setShowSuccess({orderCode}) sau createOrder() thành công -->
<!-- - if (showSuccess) { return <successOverlay>...</successOverlay> } block cần XÓA -->
<!-- - Thay bằng: import useRouter from next/navigation; const router = useRouter(); -->
<!--   sau createOrder() success: clearCart(); router.push('/account/orders/' + order.id); -->
<!-- - Giữ nguyên toàn bộ error handling: stock modal, payment modal, banner, field errors -->

<!-- Từ types/index.ts -->
<!-- Order interface hiện có: id, orderCode, userId, items, shippingAddress, paymentMethod, paymentStatus, orderStatus, subtotal, shippingFee, discount, totalAmount, note, createdAt, updatedAt -->
<!-- OrderItem interface hiện có: id, productId, productName, productImage, price, quantity, subtotal -->
<!-- Backend sẽ trả: id, userId, total, status, note, items (OrderItemDto), shippingAddress (Map), paymentMethod, createdAt, updatedAt -->
<!-- Cần thêm alias/map: orderCode ← order.id (backend không có orderCode field riêng) -->
<!-- orderStatus ← order.status; totalAmount ← order.total; paymentStatus không có trong backend MVP -->
<!-- items[].lineTotal ← item.lineTotal; items[].unitPrice ← item.unitPrice -->

<!-- UI-SPEC CSS classes cần thêm vào page.module.css: -->
<!-- .itemsTable, .tableHeader, .tableHeaderCell, .tableRow, .tableCell, .lineTotalCell -->
<!-- .skeletonRow, .skeletonCard -->
<!-- Typography: var(--text-body-sm) + var(--weight-semibold) cho table header -->
<!--             var(--text-body-md) + var(--weight-regular) cho data rows -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cập nhật Order/OrderItem types + checkout redirect flow</name>
  <files>
    sources/frontend/src/types/index.ts
    sources/frontend/src/app/checkout/page.tsx
  </files>

  <read_first>
    - sources/frontend/src/types/index.ts (PHẢI đọc — xem Order và OrderItem interfaces hiện tại)
    - sources/frontend/src/app/checkout/page.tsx (PHẢI đọc — xem showSuccess state, submitOrder(), if(showSuccess) block)
    - sources/frontend/src/services/orders.ts (xem createOrder() return type Promise<Order>)
  </read_first>

  <action>
**Cập nhật `types/index.ts` — OrderItem type thêm lineTotal + unitPrice alias:**

Tìm `OrderItem` interface và cập nhật để align với backend `OrderItemDto`:

```typescript
export interface OrderItem {
  id: string;
  productId: string;
  productName: string;
  productImage?: string;   // optional — backend không trả, FE có thể bỏ trống
  price: number;           // alias cho unitPrice — giữ cho compat
  unitPrice?: number;      // D-10: backend OrderItemDto trả unitPrice
  quantity: number;
  subtotal: number;        // alias cho lineTotal — giữ cho compat
  lineTotal?: number;      // D-10: backend OrderItemDto trả lineTotal
}
```

Tìm `Order` interface. Backend MVP trả: `id, userId, total, status, note, items, shippingAddress, paymentMethod, createdAt, updatedAt`. Cập nhật Order type để chấp nhận cả backend fields lẫn legacy FE fields:

```typescript
export interface Order {
  id: string;
  orderCode?: string;      // FE legacy — backend dùng id làm orderCode
  userId: string;
  items: OrderItem[];
  shippingAddress: Address;
  paymentMethod: 'COD' | 'BANK_TRANSFER' | 'E_WALLET' | string;
  paymentStatus?: 'PENDING' | 'PAID' | 'FAILED' | 'REFUNDED';
  orderStatus?: string;    // optional — backend trả 'status' không phải 'orderStatus'
  status?: string;         // D-10: backend field name
  subtotal?: number;
  shippingFee?: number;
  discount?: number;
  totalAmount?: number;    // FE legacy alias cho total
  total?: number;          // D-10: backend field name
  note?: string;
  createdAt: string;
  updatedAt: string;
}
```

**Cập nhật `checkout/page.tsx`** — bỏ modal success, thêm redirect:

1. Thêm import useRouter (đã có useParams có thể không — kiểm tra):
```typescript
import { useRouter } from 'next/navigation';
```

2. Trong component body, thêm `const router = useRouter();` cạnh `const { showToast }`.

3. Xóa state `showSuccess`: tìm dòng `const [showSuccess, setShowSuccess] = useState<{ orderCode: string } | null>(null);` và XÓA.

4. Trong `submitOrder()`, thay block:
```typescript
clearCart();
setShowSuccess({
  orderCode: order?.orderCode ?? (order as { code?: string } | undefined)?.code ?? '—',
});
```
Thành:
```typescript
clearCart();
router.push('/account/orders/' + order.id);
```

5. Xóa toàn bộ block `if (showSuccess)` (lines ~156-180 — từ `if (showSuccess) {` đến closing `}`). Block này return JSX với `successOverlay`, `successModal`, `successIcon`, `successTitle`, `successDesc`, `successActions`.

QUAN TRỌNG: Giữ nguyên toàn bộ error handling (stockModal, paymentModal, fieldErrors, banner), giữ nguyên form JSX, giữ nguyên 2 Modal components cuối file.
  </action>

  <verify>
    <automated>grep -n "router\.push.*account/orders" sources/frontend/src/app/checkout/page.tsx && grep -n "showSuccess" sources/frontend/src/app/checkout/page.tsx | wc -l</automated>
  </verify>

  <acceptance_criteria>
    - `checkout/page.tsx` chứa `router.push('/account/orders/' + order.id)` trong submitOrder()
    - `checkout/page.tsx` KHÔNG còn `showSuccess` state (grep showSuccess trả 0 kết quả)
    - `checkout/page.tsx` KHÔNG còn `successOverlay`, `successModal`, `successTitle`
    - `checkout/page.tsx` vẫn có `stockModal` state và Modal component cho stock conflict
    - `types/index.ts` Order interface chứa `items: OrderItem[]` (không optional)
    - `types/index.ts` OrderItem interface chứa `lineTotal?: number` và `unitPrice?: number`
    - `types/index.ts` Order interface chứa `status?: string` và `total?: number` (backend field names)
  </acceptance_criteria>

  <done>Checkout redirect sang /account/orders/{id} sau success. types/index.ts Order type align với backend payload.</done>
</task>

<task type="auto">
  <name>Task 2: Order detail page — async fetch + full breakdown UI + CSS classes</name>
  <files>
    sources/frontend/src/app/profile/orders/[id]/page.tsx
    sources/frontend/src/app/profile/orders/[id]/page.module.css
  </files>

  <read_first>
    - sources/frontend/src/app/profile/orders/[id]/page.tsx (PHẢI đọc — xem layout hiện có, statusMap, paymentMethodMap, paymentStatusMap, styles đã dùng)
    - sources/frontend/src/app/profile/orders/[id]/page.module.css (xem classes đã có để biết cần thêm gì)
    - sources/frontend/src/services/orders.ts (xem getOrderById signature)
    - sources/frontend/src/services/api.ts (xem formatPrice export)
    - sources/frontend/src/components/ui/RetrySection/RetrySection.tsx (xem props interface)
    - sources/frontend/src/types/index.ts (sau Task 1 — xem Order type đã update)
  </read_first>

  <action>
**Cập nhật `orders/[id]/page.tsx`** — chuyển từ sync placeholder sang async real API:

File hiện tại là server component (không có 'use client'). Cần chuyển thành client component để dùng useState + useEffect vì fetch bằng `getOrderById`.

Viết lại hoàn toàn file với cấu trúc:

```typescript
'use client';

import React, { useEffect, useState, useCallback } from 'react';
import Link from 'next/link';
import styles from './page.module.css';
import Button from '@/components/ui/Button/Button';
import RetrySection from '@/components/ui/RetrySection/RetrySection';
import { formatPrice } from '@/services/api';
import { getOrderById } from '@/services/orders';
import { isApiError } from '@/services/errors';
import type { Order } from '@/types';

// [Giữ nguyên statusMap, paymentMethodMap, paymentStatusMap từ file cũ]

export default function OrderDetailPage({ params }: { params: { id: string } }) {
  const { id } = params;
  const [order, setOrder] = useState<Order | null>(null);
  const [loading, setLoading] = useState(true);
  const [failed, setFailed] = useState(false);

  const load = useCallback(async () => {
    setLoading(true);
    setFailed(false);
    try {
      const data = await getOrderById(id);
      setOrder(data);
    } catch (err) {
      if (isApiError(err) && err.code === 'NOT_FOUND') {
        setOrder(null);
        setFailed(false); // 404 → empty state, không phải error
      } else {
        setFailed(true);
      }
    } finally {
      setLoading(false);
    }
  }, [id]);

  useEffect(() => { load(); }, [load]);

  // Loading state: skeleton theo UI-SPEC §Loading States
  if (loading) {
    return (
      <div className={styles.page}>
        <div className={styles.container} style={{ padding: 'var(--space-5) 0' }}>
          <div className={styles.skeletonRow} style={{ width: '200px', marginBottom: 'var(--space-4)' }} />
          <div className={styles.skeletonRow} />
          <div className={styles.skeletonRow} />
          <div className={styles.skeletonRow} />
          <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: 'var(--space-4)', marginTop: 'var(--space-4)' }}>
            <div className={styles.skeletonCard} />
            <div className={styles.skeletonCard} />
          </div>
        </div>
      </div>
    );
  }

  // Error state: RetrySection component
  if (failed) {
    return (
      <div className={styles.page}>
        <div className={styles.container} style={{ padding: 'var(--space-5) 0' }}>
          <RetrySection onRetry={load} loading={loading} />
        </div>
      </div>
    );
  }

  // Empty / Not found state (UI-SPEC §Empty States §Order detail)
  if (!order) {
    return (
      <div className={styles.notFound}>
        <h2>Không tìm thấy đơn hàng</h2>
        <p style={{ color: 'var(--on-surface-variant)', marginBottom: 'var(--space-4)' }}>
          Mã đơn #{id} không tồn tại hoặc bạn không có quyền xem.
        </p>
        <Button href="/profile">Xem danh sách đơn hàng</Button>
      </div>
    );
  }

  // Normalize backend fields → UI fields
  const orderStatus = order.orderStatus ?? order.status ?? 'PENDING';
  const totalAmount = order.totalAmount ?? order.total ?? 0;
  const shippingFee = order.shippingFee ?? 0;
  const subtotal = order.subtotal ?? (totalAmount - shippingFee);
  const orderCode = order.orderCode ?? order.id;
  const addr = order.shippingAddress;

  const steps = ['PENDING', 'CONFIRMED', 'SHIPPING', 'DELIVERED'];
  const currentStep = steps.indexOf(orderStatus);

  return (
    <div className={styles.page}>
      {/* Page Header */}
      <div className={styles.pageHeader}>
        <div className={styles.container}>
          <Link href="/profile" className={styles.backBtn}>← Quay lại</Link>
          <div className={styles.headerRow}>
            <div>
              <h1 className={styles.pageTitle}>Đơn hàng #{orderCode}</h1>
              <p className={styles.pageSubtitle}>
                Đặt ngày {new Date(order.createdAt).toLocaleDateString('vi-VN')}
              </p>
            </div>
          </div>
        </div>
      </div>

      <div className={`${styles.container} ${styles.content}`}>
        {/* Order Status Tracker */}
        {!['CANCELLED', 'RETURNED'].includes(orderStatus) && (
          <div className={styles.tracker}>
            {steps.map((step, i) => (
              <div key={step} className={`${styles.trackerStep} ${i <= currentStep ? styles.trackerActive : ''}`}>
                <div className={styles.trackerDot}>{i <= currentStep ? '✓' : i + 1}</div>
                <span className={styles.trackerLabel}>{statusMap[step]?.label}</span>
                {i < steps.length - 1 && (
                  <div className={`${styles.trackerLine} ${i < currentStep ? styles.trackerLineActive : ''}`} />
                )}
              </div>
            ))}
          </div>
        )}

        <div className={styles.grid}>
          {/* Left: Items + Price Breakdown */}
          <div className={styles.section}>
            <h3 className={styles.sectionTitle}>Sản phẩm</h3>

            {/* Items Table — UI-SPEC §Order Items Table Layout */}
            <table className={styles.itemsTable}>
              <thead>
                <tr className={styles.tableHeader}>
                  <th className={styles.tableHeaderCell}>Sản phẩm</th>
                  <th className={styles.tableHeaderCell}>Số lượng</th>
                  <th className={styles.tableHeaderCell}>Đơn giá</th>
                  <th className={styles.tableHeaderCell}>Thành tiền</th>
                </tr>
              </thead>
              <tbody>
                {(order.items ?? []).map((item) => {
                  const lineTotal = item.lineTotal ?? item.subtotal ?? 0;
                  const unitPrice = item.unitPrice ?? item.price ?? 0;
                  return (
                    <tr key={item.id} className={styles.tableRow}>
                      <td className={styles.tableCell}>{item.productName}</td>
                      <td className={styles.tableCell}>{item.quantity}</td>
                      <td className={styles.tableCell}>{formatPrice(unitPrice)}</td>
                      <td className={`${styles.tableCell} ${styles.lineTotalCell}`}>
                        {formatPrice(lineTotal)}
                      </td>
                    </tr>
                  );
                })}
              </tbody>
            </table>

            {/* Price Breakdown */}
            <div className={styles.priceBreakdown}>
              <div className={styles.priceRow}>
                <span>Tạm tính</span>
                <span>{formatPrice(subtotal)}</span>
              </div>
              <div className={styles.priceRow}>
                <span>Phí vận chuyển</span>
                <span>{shippingFee === 0 ? 'Miễn phí' : formatPrice(shippingFee)}</span>
              </div>
              {(order.discount ?? 0) > 0 && (
                <div className={styles.priceRow}>
                  <span>Giảm giá</span>
                  <span>-{formatPrice(order.discount!)}</span>
                </div>
              )}
              <div className={styles.totalRow}>
                <span>Tổng cộng</span>
                <span className={styles.totalPrice}>{formatPrice(totalAmount)}</span>
              </div>
            </div>
          </div>

          {/* Right: Info Cards */}
          <div className={styles.infoColumn}>
            {addr && (
              <div className={styles.infoCard}>
                <h4 className={styles.infoCardTitle}>Địa chỉ giao hàng</h4>
                <p>{[addr.street, addr.ward, addr.district, addr.city].filter(Boolean).join(', ')}</p>
              </div>
            )}
            <div className={styles.infoCard}>
              <h4 className={styles.infoCardTitle}>Thanh toán</h4>
              <p>{paymentMethodMap[order.paymentMethod] ?? order.paymentMethod}</p>
              {order.paymentStatus && (
                <p className={styles.paymentStatus}>{paymentStatusMap[order.paymentStatus]}</p>
              )}
            </div>
            {order.note && (
              <div className={styles.infoCard}>
                <h4 className={styles.infoCardTitle}>Ghi chú</h4>
                <p>{order.note}</p>
              </div>
            )}
          </div>
        </div>
      </div>
    </div>
  );
}
```

**Cập nhật `page.module.css`** — thêm classes theo UI-SPEC §CSS Modules Contract:

Đọc file CSS hiện tại trước, sau đó append các classes mới nếu chưa có:

```css
/* ===== Phase 8 / Plan 03: Order Items Table ===== */
.itemsTable {
  width: 100%;
  border-collapse: collapse;
  margin-bottom: var(--space-3);
}

.tableHeader {
  /* header row styling */
}

.tableHeaderCell {
  padding: var(--space-2) var(--space-3);
  text-align: left;
  font-size: var(--text-body-sm);
  font-weight: var(--weight-semibold);
  color: var(--on-surface-variant);
  border-bottom: 1px solid var(--outline-variant);
}

.tableHeaderCell:nth-child(2) {
  text-align: center;
}

.tableHeaderCell:nth-child(3),
.tableHeaderCell:nth-child(4) {
  text-align: right;
}

.tableRow {
  border-bottom: 1px solid var(--outline-variant);
}

.tableCell {
  padding: var(--space-2) var(--space-3);
  font-size: var(--text-body-md);
  color: var(--on-surface);
  vertical-align: middle;
}

.tableCell:nth-child(2) {
  text-align: center;
}

.tableCell:nth-child(3),
.tableCell:nth-child(4) {
  text-align: right;
}

.lineTotalCell {
  font-weight: var(--weight-semibold);
}

/* ===== Loading Skeletons ===== */
.skeletonRow {
  height: 40px;
  width: 100%;
  margin-bottom: var(--space-2);
  border-radius: var(--radius-md, 8px);
  background: var(--surface-container);
  animation: shimmer 1.5s infinite;
}

.skeletonCard {
  height: 120px;
  border-radius: var(--radius-xl, 16px);
  background: var(--surface-container);
  animation: shimmer 1.5s infinite;
}

@keyframes shimmer {
  0% { opacity: 0.6; }
  50% { opacity: 1; }
  100% { opacity: 0.6; }
}
```

QUAN TRỌNG: Nếu `.skeletonRow`, `.tableHeaderCell`, etc. đã tồn tại trong CSS file, KHÔNG duplicate — chỉ thêm các class chưa có.
  </action>

  <verify>
    <automated>grep -n "getOrderById" sources/frontend/src/app/profile/orders/[id]/page.tsx && grep -n "itemsTable" sources/frontend/src/app/profile/orders/[id]/page.module.css && grep -n "use client" sources/frontend/src/app/profile/orders/[id]/page.tsx</automated>
  </verify>

  <acceptance_criteria>
    - `orders/[id]/page.tsx` bắt đầu bằng `'use client';`
    - `orders/[id]/page.tsx` gọi `getOrderById(id)` trong useEffect
    - `orders/[id]/page.tsx` có 3 states: loading (skeleton), failed (RetrySection), not-found (empty state)
    - `orders/[id]/page.tsx` render `<table className={styles.itemsTable}>` với 4 columns
    - Empty state text là "Mã đơn #{id} không tồn tại hoặc bạn không có quyền xem." (UI-SPEC §Empty States)
    - `orders/[id]/page.tsx` KHÔNG còn text "Chi tiết đơn hàng sẽ khả dụng ở Phase 8"
    - `page.module.css` chứa `.itemsTable`, `.tableHeaderCell`, `.tableCell`, `.lineTotalCell`
    - `page.module.css` chứa `.skeletonRow`, `.skeletonCard`
    - CSS classes dùng `var(--space-*)`, `var(--text-body-*)`, `var(--weight-*)` tokens (không hardcode px)
  </acceptance_criteria>

  <done>Order detail page render full breakdown thật từ getOrderById(). CSS table classes theo UI-SPEC. Loading/error/empty states hoạt động. Checkout redirect sang trang này sau success.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → GET /api/orders/orders/{id} | User có thể thay đổi {id} trong URL để xem order của người khác |
| FE render shippingAddress | shippingAddress là object từ backend — có thể có malformed fields |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03-01 | Information Disclosure | GET /account/orders/{id} | accept | Backend Phase 6 JWT: X-User-Id header từ token; FE không có thêm authorization check tại page level — acceptable MVP (order detail visible nếu biết ID). Phase sau: backend filter by userId |
| T-08-03-02 | Tampering | checkout redirect với order.id | accept | order.id từ API response — không user-controlled; redirect target không thể hijack |
| T-08-03-03 | Information Disclosure | shippingAddress render | mitigate | Dùng `[addr.street, addr.ward, addr.district, addr.city].filter(Boolean).join(', ')` — null/undefined fields bị lọc, không crash |
| T-08-03-04 | Denial of Service | items array render | mitigate | `(order.items ?? []).map()` — null items không crash; empty array renders empty table |
</threat_model>

<verification>
```bash
# Verify checkout redirect
grep "router.push.*account/orders" sources/frontend/src/app/checkout/page.tsx

# Verify no showSuccess
grep -c "showSuccess" sources/frontend/src/app/checkout/page.tsx
# Expected: 0

# Verify order detail is client component with real fetch
grep "use client" sources/frontend/src/app/profile/orders/[id]/page.tsx
grep "getOrderById" sources/frontend/src/app/profile/orders/[id]/page.tsx

# Verify CSS table classes
grep "itemsTable\|tableHeaderCell\|lineTotalCell" "sources/frontend/src/app/profile/orders/[id]/page.module.css"
```
</verification>

<success_criteria>
- checkout/page.tsx redirect sang /account/orders/{id} sau createOrder() success
- checkout/page.tsx không còn showSuccess state + modal block
- orders/[id]/page.tsx là 'use client' component với useEffect fetch
- getOrderById(id) được gọi và order data render full breakdown
- Items table hiện đúng 4 columns theo UI-SPEC
- CSS classes itemsTable, tableHeaderCell, tableCell, lineTotalCell tồn tại với tokens đúng
- Loading skeleton, error RetrySection, 404 empty state đều implemented
</success_criteria>

<output>
Sau khi hoàn thành, tạo `.planning/phases/08-cart-order-persistence/08-03-SUMMARY.md`
</output>
