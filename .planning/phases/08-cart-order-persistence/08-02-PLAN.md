---
phase: 08-cart-order-persistence
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - sources/backend/order-service/src/main/resources/db/migration/V2__add_order_items.sql
  - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/domain/OrderItemEntity.java
  - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/domain/OrderEntity.java
  - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/domain/OrderItemDto.java
  - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/domain/OrderDto.java
  - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/domain/OrderMapper.java
  - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/service/OrderCrudService.java
  - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/repository/OrderItemRepository.java
  - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/AppConfig.java
autonomous: true
requirements:
  - PERSIST-01
  - PERSIST-02
  - PERSIST-03

must_haves:
  truths:
    - "POST /api/orders tạo OrderEntity kèm per-item OrderItemEntity rows trong DB"
    - "POST /api/orders persist shippingAddress (JSONB) và paymentMethod (VARCHAR)"
    - "POST /api/orders persist productName snapshot (VARCHAR 300) trong mỗi OrderItemEntity row"
    - "GET /api/orders/{id} trả full payload với items array + shippingAddress + paymentMethod"
    - "GET /api/orders/me trả list orders với items array cho từng order"
    - "409 STOCK_SHORTAGE được trả khi quantity > stock (cần Plan 08-01 stock field)"
    - "Stock deduct gọi PATCH product-service sau order persist thành công"
  artifacts:
    - path: "sources/backend/order-service/src/main/resources/db/migration/V2__add_order_items.sql"
      provides: "Tạo bảng order_items + thêm columns shippingAddress + paymentMethod vào orders"
      contains: "CREATE TABLE order_svc.order_items"
    - path: "sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/domain/OrderItemEntity.java"
      provides: "JPA Entity cho order_items table"
      contains: "@Entity"
    - path: "sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/domain/OrderEntity.java"
      provides: "Thêm @OneToMany items + shippingAddress + paymentMethod fields"
      contains: "List<OrderItemEntity> items"
    - path: "sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/domain/OrderDto.java"
      provides: "Wire format với items array + shippingAddress + paymentMethod"
      contains: "List<OrderItemDto> items"
    - path: "sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/domain/OrderMapper.java"
      provides: "Map entity + items → dto"
      contains: "toDto"
    - path: "sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/service/OrderCrudService.java"
      provides: "createOrderFromCommand() persist items + stock validate (D-04) + stock deduct (D-05)"
      contains: "STOCK_SHORTAGE"
    - path: "sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/AppConfig.java"
      provides: "@Bean RestTemplate để gọi product-service"
      contains: "@Configuration"
  key_links:
    - from: "OrderController.createOrder()"
      to: "OrderCrudService.createOrderFromCommand()"
      via: "CreateOrderCommand → persist OrderItemEntity cascade"
      pattern: "createOrderFromCommand"
    - from: "OrderEntity.items"
      to: "OrderItemEntity"
      via: "@OneToMany cascade ALL + orphanRemoval"
      pattern: "@OneToMany.*cascade.*ALL"
    - from: "OrderCrudService.createOrderFromCommand()"
      to: "GET http://api-gateway:8080/api/products/{productId}"
      via: "RestTemplate trước khi persist — D-04 stock validate"
      pattern: "restTemplate.*getForObject.*products"
    - from: "OrderCrudService.createOrderFromCommand()"
      to: "PATCH http://api-gateway:8080/api/products/admin/{productId}"
      via: "RestTemplate.exchange() sau orderRepository.save() — D-05 stock deduct"
      pattern: "restTemplate.*exchange.*PATCH.*products"
---

<objective>
Mở rộng order-service để persist per-item breakdown: tạo bảng `order_svc.order_items`, `OrderItemEntity`, thêm `shippingAddress` (JSONB) + `paymentMethod` vào `orders` table, update `OrderDto`/`OrderMapper`, wire `createOrderFromCommand()` để save items (kèm productName snapshot) + validate stock (D-04: 409 STOCK_SHORTAGE) + deduct stock sau persist (D-05).

Purpose: FE checkout → real per-item data visible tại order detail page. Cần Plan 08-03 (FE) consume.
Output: V2 migration + OrderItemEntity + updated OrderEntity/Dto/Mapper/Service + AppConfig.
</objective>

<execution_context>
@D:/SYP_PROJECT/gsd-learning/tmdt-use-gsd/.claude/get-shit-done/workflows/execute-plan.md
@D:/SYP_PROJECT/gsd-learning/tmdt-use-gsd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:/SYP_PROJECT/gsd-learning/tmdt-use-gsd/.planning/ROADMAP.md
@D:/SYP_PROJECT/gsd-learning/tmdt-use-gsd/.planning/phases/08-cart-order-persistence/08-CONTEXT.md

<interfaces>
<!-- Từ OrderEntity.java (hiện tại) — cần mở rộng -->
<!-- @Entity @Table(name="orders", schema="order_svc") -->
<!-- Fields hiện có: id, userId, total, status, note, deleted, createdAt, updatedAt -->
<!-- CẦN THÊM: List<OrderItemEntity> items, String shippingAddress, String paymentMethod -->

<!-- Từ OrderDto.java (hiện tại) -->
<!-- record OrderDto(id, userId, total, status, note, createdAt, updatedAt) -->
<!-- CẦN THÊM: List<OrderItemDto> items, Map<String,Object> shippingAddress, String paymentMethod -->

<!-- Từ OrderMapper.java (hiện tại) -->
<!-- public static OrderDto toDto(OrderEntity e) { return new OrderDto(e.id(), e.userId(), e.total(), e.status(), e.note(), e.createdAt(), e.updatedAt()); } -->
<!-- CẦN CẬP NHẬT: map items + shippingAddress + paymentMethod -->

<!-- Từ OrderCrudService.java — CreateOrderCommand record (đã có, chưa persist): -->
<!-- record CreateOrderCommand(@NotEmpty List<OrderItemRequest> items, @NotNull ShippingAddressRequest shippingAddress, @NotBlank String paymentMethod, String note) {} -->
<!-- record OrderItemRequest(@NotBlank String productId, @Min(1) int quantity, @NotNull @DecimalMin("0.0") BigDecimal unitPrice) {} -->
<!-- PHẢI THÊM: String productName vào OrderItemRequest (D-06 productName snapshot, D-04/D-05 fix) -->
<!-- createOrderFromCommand() hiện chỉ save OrderEntity basic, bỏ qua items/shippingAddress/paymentMethod -->

<!-- Từ V1__init_schema.sql — orders table hiện có: -->
<!-- CREATE TABLE order_svc.orders (id, user_id, total, status, note, deleted, created_at, updated_at) -->

<!-- Pattern GlobalExceptionHandler — để throw 409 STOCK_SHORTAGE: -->
<!-- Tạo StockShortageException extends RuntimeException -->
<!-- @ExceptionHandler(StockShortageException.class) → return 409 CONFLICT + ApiErrorResponse{domainCode="STOCK_SHORTAGE", ...} -->
<!-- FE discriminate bởi: err.code === 'CONFLICT' && d?.domainCode === 'STOCK_SHORTAGE' -->

<!-- Product-service API response (đã có từ Plan 08-01): -->
<!-- GET /api/products/{id} trả JSON có field "stock": int -->
<!-- PATCH /api/products/admin/{id} nhận body {"stock": newValue} hoặc fields khác ProductUpsertRequest -->
<!-- Plan 08-01 thêm stock field → nếu 08-01 chưa deploy, GET product trả stock=0 → validate bỏ qua items có stock=0 -->

<!-- D-04 stock validate: trước khi persist OrderEntity, lặp từng item: -->
<!--   GET http://api-gateway:8080/api/products/{productId} → parse field "stock" -->
<!--   Nếu item.quantity() > stock → throw StockShortageException với productId + available stock -->
<!--   Dùng RestTemplate.getForObject() -->

<!-- D-05 stock deduct: sau orderRepository.save() thành công, lặp từng item: -->
<!--   PATCH http://api-gateway:8080/api/products/admin/{productId} body {"stock": currentStock - quantity} -->
<!--   Cần GET stock trước → tính newStock → PATCH -->
<!--   Nếu PATCH fail → log.error, KHÔNG rollback order (MVP acceptable — user confirmed) -->
<!--   Dùng RestTemplate.exchange(url, HttpMethod.PATCH, entity, Void.class) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flyway V2 migration — tạo bảng order_items + mở rộng orders table</name>
  <files>sources/backend/order-service/src/main/resources/db/migration/V2__add_order_items.sql</files>

  <read_first>
    - sources/backend/order-service/src/main/resources/db/migration/V1__init_schema.sql (xem schema hiện tại của orders table)
  </read_first>

  <action>
Tạo file mới `V2__add_order_items.sql`. Order-service hiện chỉ có V1 → dùng V2 (không phải V3).

```sql
-- Phase 8 / Plan 02 (D-06, D-08, D-09): Mở rộng order-service schema.
-- Tạo bảng order_items, thêm shipping_address + payment_method vào orders.

-- Bảng order_items: per-item breakdown (D-06)
CREATE TABLE IF NOT EXISTS order_svc.order_items (
  id            VARCHAR(36)    PRIMARY KEY,
  order_id      VARCHAR(36)    NOT NULL,
  product_id    VARCHAR(36)    NOT NULL,
  product_name  VARCHAR(300)   NOT NULL,
  quantity      INT            NOT NULL,
  unit_price    DECIMAL(12,2)  NOT NULL,
  line_total    DECIMAL(12,2)  NOT NULL
);

CREATE INDEX IF NOT EXISTS idx_order_items_order_id ON order_svc.order_items(order_id);

-- Mở rộng orders table: shipping_address JSONB (D-08) + payment_method VARCHAR (D-09)
ALTER TABLE order_svc.orders ADD COLUMN IF NOT EXISTS shipping_address JSONB;
ALTER TABLE order_svc.orders ADD COLUMN IF NOT EXISTS payment_method VARCHAR(30);
```

Lưu ý: shipping_address nullable (orders cũ từ Phase 5 seed không có địa chỉ).
  </action>

  <verify>
    <automated>grep -n "CREATE TABLE IF NOT EXISTS order_svc.order_items" sources/backend/order-service/src/main/resources/db/migration/V2__add_order_items.sql && grep -n "ADD COLUMN IF NOT EXISTS shipping_address JSONB" sources/backend/order-service/src/main/resources/db/migration/V2__add_order_items.sql</automated>
  </verify>

  <done>Migration V2 tạo bảng order_items và mở rộng orders table, sẵn sàng cho JPA entities.</done>
</task>

<task type="auto">
  <name>Task 2: Tạo OrderItemEntity + OrderItemRepository + mở rộng OrderEntity</name>
  <files>
    sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/domain/OrderItemEntity.java
    sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/repository/OrderItemRepository.java
    sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/domain/OrderEntity.java
  </files>

  <read_first>
    - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/domain/OrderEntity.java (PHẢI đọc trước — xem cấu trúc hiện tại để thêm đúng chỗ)
    - sources/backend/product-service/src/main/java/com/ptit/htpt/productservice/domain/ProductEntity.java (pattern @Entity, @Column, accessor style)
  </read_first>

  <action>
**Tạo `OrderItemEntity.java`** (file mới):

```java
package com.ptit.htpt.orderservice.domain;

import jakarta.persistence.Column;
import jakarta.persistence.Entity;
import jakarta.persistence.FetchType;
import jakarta.persistence.Id;
import jakarta.persistence.JoinColumn;
import jakarta.persistence.ManyToOne;
import jakarta.persistence.Table;
import java.math.BigDecimal;
import java.util.UUID;

@Entity
@Table(name = "order_items", schema = "order_svc")
public class OrderItemEntity {

  @Id
  @Column(length = 36, nullable = false, updatable = false)
  private String id;

  @ManyToOne(fetch = FetchType.LAZY)
  @JoinColumn(name = "order_id", nullable = false)
  private OrderEntity order;

  @Column(name = "product_id", nullable = false, length = 36)
  private String productId;

  @Column(name = "product_name", nullable = false, length = 300)
  private String productName;

  @Column(nullable = false)
  private int quantity;

  @Column(name = "unit_price", nullable = false, precision = 12, scale = 2)
  private BigDecimal unitPrice;

  @Column(name = "line_total", nullable = false, precision = 12, scale = 2)
  private BigDecimal lineTotal;

  protected OrderItemEntity() {}

  public static OrderItemEntity create(OrderEntity order, String productId,
                                       String productName, int quantity,
                                       BigDecimal unitPrice) {
    OrderItemEntity item = new OrderItemEntity();
    item.id = UUID.randomUUID().toString();
    item.order = order;
    item.productId = productId;
    item.productName = productName;
    item.quantity = quantity;
    item.unitPrice = unitPrice;
    item.lineTotal = unitPrice.multiply(BigDecimal.valueOf(quantity));
    return item;
  }

  public String id() { return id; }
  public String productId() { return productId; }
  public String productName() { return productName; }
  public int quantity() { return quantity; }
  public BigDecimal unitPrice() { return unitPrice; }
  public BigDecimal lineTotal() { return lineTotal; }
}
```

**Tạo `OrderItemRepository.java`** (file mới):

```java
package com.ptit.htpt.orderservice.repository;

import com.ptit.htpt.orderservice.domain.OrderItemEntity;
import org.springframework.data.jpa.repository.JpaRepository;

public interface OrderItemRepository extends JpaRepository<OrderItemEntity, String> {}
```

**Mở rộng `OrderEntity.java`** — thêm 3 fields mới:

1. Thêm imports cần thiết ở đầu file:
```java
import jakarta.persistence.CascadeType;
import jakarta.persistence.FetchType;
import jakarta.persistence.OneToMany;
import java.util.ArrayList;
import java.util.List;
import org.hibernate.annotations.JdbcTypeCode;
import org.hibernate.type.SqlTypes;
```

2. Thêm fields sau `private String note;`:
```java
@OneToMany(mappedBy = "order", cascade = CascadeType.ALL, orphanRemoval = true, fetch = FetchType.LAZY)
private List<OrderItemEntity> items = new ArrayList<>();

@JdbcTypeCode(SqlTypes.JSON)
@Column(name = "shipping_address", columnDefinition = "jsonb")
private String shippingAddress;

@Column(name = "payment_method", length = 30)
private String paymentMethod;
```

3. Thêm getters và setters sau `note()` getter:
```java
public List<OrderItemEntity> items() { return items; }
public String shippingAddress() { return shippingAddress; }
public String paymentMethod() { return paymentMethod; }

public void addItem(OrderItemEntity item) {
  this.items.add(item);
}
public void setShippingAddress(String shippingAddress) {
  this.shippingAddress = shippingAddress;
}
public void setPaymentMethod(String paymentMethod) {
  this.paymentMethod = paymentMethod;
}
```

QUAN TRỌNG: `@JdbcTypeCode(SqlTypes.JSON)` yêu cầu import `org.hibernate.annotations.JdbcTypeCode` và `org.hibernate.type.SqlTypes`. Đây là Hibernate 6 API (Spring Boot 3.x).
  </action>

  <verify>
    <automated>grep -n "@Entity" sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/domain/OrderItemEntity.java && grep -n "List<OrderItemEntity> items" sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/domain/OrderEntity.java && grep -n "shippingAddress" sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/domain/OrderEntity.java && grep -n "paymentMethod" sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/domain/OrderEntity.java</automated>
  </verify>

  <done>OrderItemEntity và OrderItemRepository tồn tại. OrderEntity có đầy đủ fields mới với JPA mappings đúng.</done>
</task>

<task type="auto">
  <name>Task 3: Cập nhật OrderDto + OrderItemDto + OrderMapper + basic createOrderFromCommand() với productName snapshot</name>
  <files>
    sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/domain/OrderItemDto.java
    sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/domain/OrderDto.java
    sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/domain/OrderMapper.java
    sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/service/OrderCrudService.java
    sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/AppConfig.java
  </files>

  <read_first>
    - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/domain/OrderDto.java (xem record hiện tại trước khi sửa)
    - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/domain/OrderMapper.java (xem toDto() method hiện tại)
    - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/service/OrderCrudService.java (xem createOrderFromCommand() hiện tại và records CreateOrderCommand, OrderItemRequest, ShippingAddressRequest)
    - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/api/ApiErrorResponse.java (xem structure để throw 409 đúng format)
  </read_first>

  <action>
**Tạo `OrderItemDto.java`** (file mới):

```java
package com.ptit.htpt.orderservice.domain;

import java.math.BigDecimal;

public record OrderItemDto(
    String id,
    String productId,
    String productName,
    int quantity,
    BigDecimal unitPrice,
    BigDecimal lineTotal
) {}
```

**Cập nhật `OrderDto.java`** — thêm items, shippingAddress, paymentMethod:

```java
package com.ptit.htpt.orderservice.domain;

import com.fasterxml.jackson.annotation.JsonProperty;
import java.math.BigDecimal;
import java.time.Instant;
import java.util.List;
import java.util.Map;

public record OrderDto(
    String id,
    String userId,
    BigDecimal total,
    String status,
    String note,
    List<OrderItemDto> items,
    Map<String, Object> shippingAddress,
    String paymentMethod,
    Instant createdAt,
    Instant updatedAt
) {
  @JsonProperty("totalAmount")
  public BigDecimal totalAmount() {
    return total;
  }
}
```

**Cập nhật `OrderMapper.java`** — map items + shippingAddress + paymentMethod:

```java
package com.ptit.htpt.orderservice.domain;

import com.fasterxml.jackson.core.type.TypeReference;
import com.fasterxml.jackson.databind.ObjectMapper;
import java.util.Collections;
import java.util.List;
import java.util.Map;

public final class OrderMapper {
  private OrderMapper() {}

  private static final ObjectMapper MAPPER = new ObjectMapper();

  public static OrderDto toDto(OrderEntity e) {
    List<OrderItemDto> items = e.items() == null ? Collections.emptyList() :
        e.items().stream()
            .map(i -> new OrderItemDto(
                i.id(), i.productId(), i.productName(),
                i.quantity(), i.unitPrice(), i.lineTotal()))
            .toList();

    Map<String, Object> shippingAddress = null;
    if (e.shippingAddress() != null) {
      try {
        shippingAddress = MAPPER.readValue(e.shippingAddress(),
            new TypeReference<Map<String, Object>>() {});
      } catch (Exception ex) {
        // malformed JSON — trả empty map thay vì crash
        shippingAddress = Collections.emptyMap();
      }
    }

    return new OrderDto(
        e.id(), e.userId(), e.total(), e.status(), e.note(),
        items, shippingAddress, e.paymentMethod(),
        e.createdAt(), e.updatedAt()
    );
  }
}
```

**Tạo `AppConfig.java`** (file mới, tại package root `com.ptit.htpt.orderservice`):

```java
package com.ptit.htpt.orderservice;

import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;
import org.springframework.web.client.RestTemplate;

@Configuration
public class AppConfig {
  @Bean
  public RestTemplate restTemplate() {
    return new RestTemplate();
  }
}
```

**Cập nhật `OrderCrudService.java`** — thêm productName vào OrderItemRequest + basic createOrderFromCommand (persist only, không có stock logic):

Phần 1 — Thêm imports ở đầu class:
```java
import com.fasterxml.jackson.databind.ObjectMapper;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
```

Phần 2 — Thêm fields và constructor mới:
```java
private static final Logger log = LoggerFactory.getLogger(OrderCrudService.class);
private final ObjectMapper objectMapper;
private final OrderItemRepository orderItemRepository;

// Constructor update — thêm 2 deps (RestTemplate được thêm ở Task 4):
public OrderCrudService(InMemoryCartRepository cartRepository,
                        OrderRepository orderRepository,
                        OrderItemRepository orderItemRepository,
                        ObjectMapper objectMapper) {
  this.cartRepository = cartRepository;
  this.orderRepository = orderRepository;
  this.orderItemRepository = orderItemRepository;
  this.objectMapper = objectMapper;
}
```

Phần 3 — Thêm `String productName` vào `OrderItemRequest` record (D-06 productName snapshot):

Tìm dòng:
```java
public record OrderItemRequest(
    @NotBlank String productId,
    @Min(1) int quantity,
    @NotNull @DecimalMin("0.0") BigDecimal unitPrice
) {}
```

Thay bằng:
```java
public record OrderItemRequest(
    @NotBlank String productId,
    @NotBlank String productName,
    @Min(1) int quantity,
    @NotNull @DecimalMin("0.0") BigDecimal unitPrice
) {}
```

Phần 4 — Thay thế `createOrderFromCommand()` bằng implementation mới (persist items + shippingAddress + paymentMethod, productName snapshot):

```java
public OrderDto createOrderFromCommand(String userId, CreateOrderCommand command) {
  if (userId == null || userId.isBlank()) {
    throw new ResponseStatusException(HttpStatus.BAD_REQUEST, "Missing X-User-Id session header");
  }

  // D-04: Stock validation — xem Task 4 (validateStock được gọi ở đây sau khi Task 4 thêm)
  // Task 4 sẽ inject RestTemplate và thêm gọi validateStockOrThrow() trước block persist bên dưới.

  BigDecimal totalAmount = command.items().stream()
      .map(item -> item.unitPrice().multiply(BigDecimal.valueOf(item.quantity())))
      .reduce(BigDecimal.ZERO, BigDecimal::add);

  // Serialize shippingAddress thành JSON string để lưu JSONB (D-08)
  String shippingAddressJson;
  try {
    shippingAddressJson = objectMapper.writeValueAsString(command.shippingAddress());
  } catch (Exception e) {
    throw new ResponseStatusException(HttpStatus.BAD_REQUEST, "Invalid shippingAddress format");
  }

  // Tạo và persist OrderEntity
  OrderEntity order = OrderEntity.create(userId, totalAmount, "PENDING", command.note());
  order.setShippingAddress(shippingAddressJson);
  order.setPaymentMethod(command.paymentMethod());

  // Tạo OrderItemEntity cho từng item — cascade ALL sẽ persist cùng order
  // D-06: productName snapshot lấy trực tiếp từ command (FE truyền item.name từ cart state)
  for (OrderItemRequest itemReq : command.items()) {
    OrderItemEntity item = OrderItemEntity.create(
        order,
        itemReq.productId(),
        itemReq.productName(),   // D-06: productName snapshot — KHÔNG dùng productId làm placeholder
        itemReq.quantity(),
        itemReq.unitPrice()
    );
    order.addItem(item);
  }

  OrderEntity saved = orderRepository.save(order);

  // D-05: Stock deduct — xem Task 4 (deductStock được gọi ở đây sau khi Task 4 thêm)
  // Task 4 sẽ thêm gọi deductStockAfterPersist(command.items()) sau dòng save ở trên.

  return OrderMapper.toDto(saved);
}
```

QUAN TRỌNG: Trong Task 3 này chưa có D-04 validate và D-05 deduct. Task 4 sẽ inject RestTemplate và thêm 2 private methods `validateStockOrThrow()` + `deductStockAfterPersist()`, đồng thời chèn gọi chúng vào `createOrderFromCommand()`.
  </action>

  <verify>
    <automated>grep -n "List<OrderItemDto> items" sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/domain/OrderDto.java && grep -n "productName" sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/service/OrderCrudService.java && grep -n "@Configuration" sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/AppConfig.java</automated>
  </verify>

  <done>OrderItemDto tồn tại. OrderDto có full payload (items, shippingAddress, paymentMethod). OrderMapper.toDto() map đầy đủ. OrderItemRequest có productName field (D-06 snapshot). createOrderFromCommand() persist items với productName thật, shippingAddress JSON, paymentMethod. AppConfig.java có @Bean RestTemplate.</done>
</task>

<task type="auto">
  <name>Task 4: Stock validate D-04 (409 STOCK_SHORTAGE) + Stock deduct D-05 (PATCH product-service) + StockShortageException + GlobalExceptionHandler case</name>
  <files>
    sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/service/OrderCrudService.java
    sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/exception/StockShortageException.java
    sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/api/GlobalExceptionHandler.java
  </files>

  <read_first>
    - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/api/GlobalExceptionHandler.java (xem handlers hiện tại, pattern @ExceptionHandler, cách dùng ApiErrorResponse)
    - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/api/ApiErrorResponse.java (xem record structure — fields: code, message, details?)
    - sources/backend/api-gateway/src/main/resources/application.yml (confirm route cho /api/products/{id} và /api/products/admin/{id})
  </read_first>

  <action>
**Tạo `StockShortageException.java`** (file mới):

```java
package com.ptit.htpt.orderservice.exception;

import java.util.List;

/**
 * Thrown when one or more order items exceed available stock.
 * Mapped to 409 CONFLICT by GlobalExceptionHandler with domainCode=STOCK_SHORTAGE.
 */
public class StockShortageException extends RuntimeException {

  private final List<StockShortageItem> shortageItems;

  public StockShortageException(List<StockShortageItem> shortageItems) {
    super("One or more items exceed available stock");
    this.shortageItems = shortageItems;
  }

  public List<StockShortageItem> shortageItems() {
    return shortageItems;
  }

  /** Per-item shortage detail (productId, requested quantity, available stock). */
  public record StockShortageItem(
      String productId,
      String productName,
      int requested,
      int available
  ) {}
}
```

**Cập nhật `GlobalExceptionHandler.java`** — thêm handler cho StockShortageException:

Đọc file hiện tại để biết package và import pattern. Thêm method sau handler cuối cùng trong class:

```java
import com.ptit.htpt.orderservice.exception.StockShortageException;
import org.springframework.http.HttpStatus;
import org.springframework.web.bind.annotation.ExceptionHandler;

@ExceptionHandler(StockShortageException.class)
@org.springframework.web.bind.annotation.ResponseStatus(HttpStatus.CONFLICT)
public org.springframework.http.ResponseEntity<Object> handleStockShortage(
    StockShortageException ex) {
  // ApiErrorResponse shape: {code, message, details} — xem record structure từ read_first
  // Nếu ApiErrorResponse có constructor (String code, String message, Object details):
  var body = java.util.Map.of(
      "code", "CONFLICT",
      "domainCode", "STOCK_SHORTAGE",
      "message", "Một số sản phẩm không đủ số lượng trong kho",
      "items", ex.shortageItems().stream().map(i -> java.util.Map.of(
          "productId", i.productId(),
          "productName", i.productName(),
          "requested", i.requested(),
          "available", i.available()
      )).toList()
  );
  return org.springframework.http.ResponseEntity.status(HttpStatus.CONFLICT).body(body);
}
```

QUAN TRỌNG: Sau khi đọc `ApiErrorResponse.java`, nếu nó là record với specific constructor, dùng constructor đó thay vì `Map.of`. Đảm bảo response body có `domainCode: "STOCK_SHORTAGE"` để FE discriminate.

**Cập nhật `OrderCrudService.java`** — thêm RestTemplate inject + 2 private methods + wire vào createOrderFromCommand():

Phần 1 — Thêm imports:
```java
import com.ptit.htpt.orderservice.exception.StockShortageException;
import com.ptit.htpt.orderservice.exception.StockShortageException.StockShortageItem;
import org.springframework.http.HttpEntity;
import org.springframework.http.HttpHeaders;
import org.springframework.http.HttpMethod;
import org.springframework.http.MediaType;
import org.springframework.web.client.RestTemplate;
import java.util.ArrayList;
import java.util.Map;
```

Phần 2 — Thêm field và update constructor (inject RestTemplate):
```java
private final RestTemplate restTemplate;

// Constructor mới (thay constructor từ Task 3 — thêm RestTemplate):
public OrderCrudService(InMemoryCartRepository cartRepository,
                        OrderRepository orderRepository,
                        OrderItemRepository orderItemRepository,
                        ObjectMapper objectMapper,
                        RestTemplate restTemplate) {
  this.cartRepository = cartRepository;
  this.orderRepository = orderRepository;
  this.orderItemRepository = orderItemRepository;
  this.objectMapper = objectMapper;
  this.restTemplate = restTemplate;
}
```

Phần 3 — Thêm 2 private methods D-04 và D-05 vào cuối class (trước closing `}`):

```java
/**
 * D-04: Validate stock cho từng item trước khi persist order.
 * Gọi GET /api/products/{productId} qua gateway để lấy stock thật.
 * Nếu bất kỳ item nào có quantity > stock → throw StockShortageException (→ 409 CONFLICT).
 * Nếu product-service không trả được (timeout, 404) → log.warn và bỏ qua item đó (best-effort MVP).
 */
private void validateStockOrThrow(List<OrderItemRequest> items) {
  List<StockShortageItem> shortages = new ArrayList<>();
  for (OrderItemRequest item : items) {
    try {
      String url = "http://api-gateway:8080/api/products/" + item.productId();
      @SuppressWarnings("unchecked")
      Map<String, Object> product = restTemplate.getForObject(url, Map.class);
      if (product == null) {
        log.warn("[D-04] product-service returned null for productId={}", item.productId());
        continue;
      }
      // product-service response: {id, name, stock, ...} — stock field từ Plan 08-01
      Object stockObj = product.get("stock");
      int stock = stockObj == null ? 0 : ((Number) stockObj).intValue();
      if (item.quantity() > stock) {
        shortages.add(new StockShortageItem(
            item.productId(),
            item.productName(),
            item.quantity(),
            stock
        ));
      }
    } catch (Exception ex) {
      // product-service không available — log và bỏ qua validate cho item này (MVP best-effort)
      log.warn("[D-04] Could not fetch stock for productId={}: {}", item.productId(), ex.getMessage());
    }
  }
  if (!shortages.isEmpty()) {
    throw new StockShortageException(shortages);
  }
}

/**
 * D-05: Decrement stock sau khi order đã persist thành công.
 * Gọi GET /api/products/{id} để lấy currentStock → tính newStock → PATCH /api/products/admin/{id}.
 * Nếu fail → log.error, KHÔNG rollback order (user confirmed MVP acceptable).
 */
private void deductStockAfterPersist(List<OrderItemRequest> items) {
  for (OrderItemRequest item : items) {
    try {
      // Fetch current stock
      String getUrl = "http://api-gateway:8080/api/products/" + item.productId();
      @SuppressWarnings("unchecked")
      Map<String, Object> product = restTemplate.getForObject(getUrl, Map.class);
      if (product == null) {
        log.error("[D-05] Cannot deduct stock for productId={}: product not found", item.productId());
        continue;
      }
      Object stockObj = product.get("stock");
      int currentStock = stockObj == null ? 0 : ((Number) stockObj).intValue();
      int newStock = Math.max(0, currentStock - item.quantity());

      // PATCH /api/products/admin/{id} với stock mới
      String patchUrl = "http://api-gateway:8080/api/products/admin/" + item.productId();
      HttpHeaders headers = new HttpHeaders();
      headers.setContentType(MediaType.APPLICATION_JSON);
      // Gửi partial update body — product-service admin endpoint nhận toàn bộ ProductUpsertRequest
      // Chỉ update stock field; các field khác (name, price, ...) cần lấy từ product response.
      // Lấy các fields cần thiết từ product response để không null-out các fields khác:
      @SuppressWarnings("unchecked")
      Map<String, Object> patchBody = buildStockUpdateBody(product, newStock);
      HttpEntity<Map<String, Object>> entity = new HttpEntity<>(patchBody, headers);
      restTemplate.exchange(patchUrl, HttpMethod.PATCH, entity, Void.class);
      log.info("[D-05] Stock deducted for productId={}: {} → {}", item.productId(), currentStock, newStock);
    } catch (Exception ex) {
      log.error("[D-05] Stock deduct failed for productId={} qty={}: {}",
          item.productId(), item.quantity(), ex.getMessage());
      // KHÔNG throw — order đã saved, deduct failure là acceptable (MVP)
    }
  }
}

/**
 * Builds PATCH body reusing existing product fields from GET response + overriding stock.
 * Prevents null-out of name/price/description when PATCH requires full ProductUpsertRequest.
 */
@SuppressWarnings("unchecked")
private Map<String, Object> buildStockUpdateBody(Map<String, Object> existingProduct, int newStock) {
  Map<String, Object> body = new java.util.LinkedHashMap<>();
  // Copy fields that ProductUpsertRequest expects — read sources/backend/product-service/...
  // ProductUpsertRequest likely has: name, description, price, category, stock, etc.
  // Copy existing non-null fields to preserve them; override stock.
  for (Map.Entry<String, Object> entry : existingProduct.entrySet()) {
    if (entry.getValue() != null) {
      body.put(entry.getKey(), entry.getValue());
    }
  }
  body.put("stock", newStock);
  // Remove read-only fields that would cause validation errors:
  body.remove("id");
  body.remove("createdAt");
  body.remove("updatedAt");
  body.remove("deleted");
  return body;
}
```

Phần 4 — Cập nhật `createOrderFromCommand()` để gọi 2 methods mới:

Tìm comment `// D-04: Stock validation — xem Task 4` (đặt ở Task 3) và thay bằng gọi thật:
```java
// D-04: Validate stock trước khi persist — throw 409 STOCK_SHORTAGE nếu quantity > stock
validateStockOrThrow(command.items());
```

Tìm comment `// D-05: Stock deduct — xem Task 4` và thay bằng:
```java
// D-05: Deduct stock sau persist — best-effort, không rollback order nếu fail
deductStockAfterPersist(command.items());
```

Kết quả `createOrderFromCommand()` sau Task 4 phải có đúng thứ tự:
1. userId check
2. `validateStockOrThrow(command.items())` — D-04
3. Tính totalAmount
4. Serialize shippingAddress
5. Tạo OrderEntity + addItem (với productName thật)
6. `orderRepository.save(order)`
7. `deductStockAfterPersist(command.items())` — D-05
8. Return `OrderMapper.toDto(saved)`
  </action>

  <verify>
    <automated>grep -n "validateStockOrThrow\|deductStockAfterPersist\|STOCK_SHORTAGE" sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/service/OrderCrudService.java && grep -n "StockShortageException" sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/exception/StockShortageException.java && grep -n "StockShortageException\|STOCK_SHORTAGE" sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/api/GlobalExceptionHandler.java</automated>
  </verify>

  <done>StockShortageException tồn tại. GlobalExceptionHandler có case 409 STOCK_SHORTAGE với domainCode. OrderCrudService.createOrderFromCommand() gọi validateStockOrThrow() trước persist và deductStockAfterPersist() sau persist. D-04 và D-05 implemented đầy đủ per locked decisions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FE checkout → POST /api/orders | CreateOrderCommand chứa items[] với unitPrice và productName — FE có thể gửi giá fake hoặc productName sai |
| order-service → product-service (validate) | GET stock qua internal gateway — không auth; product-service có thể không available |
| order-service → product-service (deduct) | PATCH stock qua internal gateway — không auth; có race condition nếu 2 orders cùng lúc |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-02-01 | Tampering | CreateOrderCommand.items[].unitPrice | accept | MVP: unitPrice từ FE cart snapshot — không verify với product-service (acceptable v1.1, backend price verification defer v1.2) |
| T-08-02-02 | Tampering | CreateOrderCommand.items[].productName | accept | productName snapshot từ FE cart state — không verify với product-service (MVP: lưu as-is, đủ cho order history display) |
| T-08-02-03 | Repudiation | OrderEntity.shippingAddress JSONB | accept | shippingAddress là customer-supplied data, lưu as-is; không cần audit log ở MVP |
| T-08-02-04 | Information Disclosure | GET /orders/{id} trả shippingAddress | mitigate | OrderController chỉ trả order của userId từ X-User-Id header (Phase 6 JWT verify tại gateway); endpoint không trả deleted=true orders qua @SQLRestriction |
| T-08-02-05 | Denial of Service | POST /orders với items[] lớn | mitigate | @NotEmpty + @Valid annotation trên items — malformed request trả 400 VALIDATION_ERROR; không có item count limit tại MVP (acceptable) |
| T-08-02-06 | Elevation of Privilege | stock deduct PATCH gọi admin endpoint | accept | Internal call từ order-service → gateway → product-service; không expose ra ngoài; MVP acceptable |
| T-08-02-07 | Denial of Service | D-04 RestTemplate GET stock cho mỗi item | mitigate | Exception catch per-item — product-service timeout không block order; log.warn và bỏ qua validate (best-effort MVP) |
</threat_model>

<verification>
```bash
# Verify migration
grep "CREATE TABLE IF NOT EXISTS order_svc.order_items" sources/backend/order-service/src/main/resources/db/migration/V2__add_order_items.sql

# Verify OrderItemEntity exists
grep "@Entity" sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/domain/OrderItemEntity.java

# Verify OrderDto has items
grep "List<OrderItemDto>" sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/domain/OrderDto.java

# Verify productName in OrderItemRequest (D-06 fix)
grep "productName" sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/service/OrderCrudService.java

# Verify D-04 stock validate implemented
grep "validateStockOrThrow\|STOCK_SHORTAGE" sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/service/OrderCrudService.java

# Verify D-05 stock deduct implemented
grep "deductStockAfterPersist\|PATCH.*products" sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/service/OrderCrudService.java

# Verify StockShortageException exists
grep "StockShortageException" sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/exception/StockShortageException.java

# Verify GlobalExceptionHandler has 409 STOCK_SHORTAGE case
grep "STOCK_SHORTAGE" sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/api/GlobalExceptionHandler.java

# Verify AppConfig in correct location
grep "@Configuration" sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/AppConfig.java
```
</verification>

<success_criteria>
- V2 migration tạo order_items table + mở rộng orders (shipping_address JSONB, payment_method)
- OrderItemEntity + OrderItemRepository tồn tại
- OrderEntity có items/@OneToMany, shippingAddress, paymentMethod
- OrderDto/OrderItemDto có full payload shape
- OrderMapper.toDto() map đầy đủ
- OrderItemRequest có String productName field (D-06 snapshot — KHÔNG dùng productId làm placeholder)
- createOrderFromCommand() gọi validateStockOrThrow() trước persist (D-04)
- StockShortageException tồn tại với shortageItems List
- GlobalExceptionHandler map StockShortageException → 409 CONFLICT với domainCode=STOCK_SHORTAGE
- createOrderFromCommand() gọi deductStockAfterPersist() sau orderRepository.save() (D-05)
- PATCH /api/products/admin/{id} được gọi với newStock = currentStock - quantity
- Nếu PATCH fail → log.error, KHÔNG rollback order
- AppConfig.java tồn tại tại sources/backend/order-service/.../AppConfig.java với @Bean RestTemplate
</success_criteria>

<output>
Sau khi hoàn thành, tạo `.planning/phases/08-cart-order-persistence/08-02-SUMMARY.md`
</output>
