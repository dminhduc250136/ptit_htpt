---
phase: 08-cart-order-persistence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sources/backend/product-service/src/main/resources/db/migration/V3__add_product_stock.sql
  - sources/backend/product-service/src/main/java/com/ptit/htpt/productservice/domain/ProductEntity.java
  - sources/backend/product-service/src/main/java/com/ptit/htpt/productservice/service/ProductCrudService.java
autonomous: true
requirements:
  - PERSIST-01

must_haves:
  truths:
    - "GET /api/products/{id} trả field `stock` thật từ DB (không phải hardcode 0)"
    - "GET /api/products/slug/{slug} trả `stock` thật"
    - "Admin có thể set/update stock qua ProductUpsertRequest"
    - "10 products trong seed đều có stock = 50 (default seed value)"
  artifacts:
    - path: "sources/backend/product-service/src/main/resources/db/migration/V3__add_product_stock.sql"
      provides: "Flyway migration thêm column stock vào bảng products + UPDATE seed values"
      contains: "ALTER TABLE product_svc.products ADD COLUMN IF NOT EXISTS stock INT"
    - path: "sources/backend/product-service/src/main/java/com/ptit/htpt/productservice/domain/ProductEntity.java"
      provides: "Entity field stock + getter"
      contains: "private int stock"
    - path: "sources/backend/product-service/src/main/java/com/ptit/htpt/productservice/service/ProductCrudService.java"
      provides: "toResponse() đọc entity.stock(), ProductUpsertRequest có stock field"
      exports: ["ProductUpsertRequest", "ProductResponse", "toResponse"]
  key_links:
    - from: "V3__add_product_stock.sql"
      to: "ProductEntity.stock"
      via: "Flyway migration → JPA column mapping"
      pattern: "\\@Column.*stock|private int stock"
    - from: "ProductCrudService.toResponse()"
      to: "product.stock()"
      via: "getter call thay vì hardcode 0"
      pattern: "product\\.stock\\(\\)"
---

<objective>
Thêm field `stock` vào ProductEntity và persist trong DB qua Flyway migration. Fix `ProductCrudService.toResponse()` đang hardcode `stock: 0` để đọc từ entity. Thêm `int stock` vào `ProductUpsertRequest` để admin có thể set stock.

Purpose: Unblock FE product detail (Plan 08-04) và order stock validation (Plan 08-02). Đây là foundation của PERSIST-01.
Output: Migration V3 + ProductEntity.stock field + service wire.
</objective>

<execution_context>
@D:/SYP_PROJECT/gsd-learning/tmdt-use-gsd/.claude/get-shit-done/workflows/execute-plan.md
@D:/SYP_PROJECT/gsd-learning/tmdt-use-gsd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:/SYP_PROJECT/gsd-learning/tmdt-use-gsd/.planning/ROADMAP.md
@D:/SYP_PROJECT/gsd-learning/tmdt-use-gsd/.planning/phases/08-cart-order-persistence/08-CONTEXT.md

<interfaces>
<!-- Từ ProductEntity.java (hiện tại) — thêm vào file này -->
<!-- Entity dùng accessor style `name()` không phải getter getXxx() -->
<!-- @Entity @Table(name="products", schema="product_svc") -->
<!-- Accessor hiện có: id(), name(), slug(), categoryId(), price(), status(), brand(), thumbnailUrl(), shortDescription(), originalPrice(), deleted(), createdAt(), updatedAt() -->
<!-- CẦN THÊM: private int stock field + stock() getter + setStock(int) method -->

<!-- Từ ProductCrudService.java line 178 (hiện tại) -->
<!-- toResponse() tạo ProductResponse với: 0, // stock default — read from inventory-service -->
<!-- CẦN ĐỔI THÀNH: product.stock() -->
<!-- ProductUpsertRequest record hiện tại: name, slug, categoryId, price, status, brand, thumbnailUrl, shortDescription, originalPrice -->
<!-- CẦN THÊM: int stock (default 0 nếu null) -->
<!-- createProduct() gọi ProductEntity.create() — CẦN truyền stock sau khi create -->
<!-- updateProduct() gọi current.update() — CẦN thêm stock param -->

<!-- Pattern V2 migration (tham khảo): -->
<!-- ALTER TABLE product_svc.products ADD COLUMN IF NOT EXISTS brand VARCHAR(200); -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flyway V3 migration — thêm column stock + seed defaults</name>
  <files>sources/backend/product-service/src/main/resources/db/migration/V3__add_product_stock.sql</files>

  <read_first>
    - sources/backend/product-service/src/main/resources/db/migration/V2__add_product_extended_fields.sql (pattern ADD COLUMN IF NOT EXISTS)
    - sources/backend/product-service/src/main/resources/db/migration/V1__init_schema.sql (nếu tồn tại — xem cấu trúc bảng gốc + seed products)
  </read_first>

  <action>
Tạo file mới `V3__add_product_stock.sql` với nội dung sau:

```sql
-- Phase 8 / Plan 01 (D-01): Thêm column stock vào product_svc.products.
-- IF NOT EXISTS đảm bảo idempotent khi chạy lại trong test environments.
ALTER TABLE product_svc.products ADD COLUMN IF NOT EXISTS stock INT NOT NULL DEFAULT 0;

-- Seed stock = 50 cho tất cả products hiện có (10 products từ Phase 5 seed).
-- Products chưa có stock sẽ được cập nhật từ DEFAULT 0 lên 50.
UPDATE product_svc.products SET stock = 50 WHERE deleted = false;
```

Không thêm gì khác — migration phải ngắn gọn và idempotent.
  </action>

  <verify>
    <automated>grep -n "ADD COLUMN IF NOT EXISTS stock INT" sources/backend/product-service/src/main/resources/db/migration/V3__add_product_stock.sql && grep -n "UPDATE product_svc.products SET stock = 50" sources/backend/product-service/src/main/resources/db/migration/V3__add_product_stock.sql</automated>
  </verify>

  <acceptance_criteria>
    - File tồn tại tại `sources/backend/product-service/src/main/resources/db/migration/V3__add_product_stock.sql`
    - Chứa `ALTER TABLE product_svc.products ADD COLUMN IF NOT EXISTS stock INT NOT NULL DEFAULT 0`
    - Chứa `UPDATE product_svc.products SET stock = 50 WHERE deleted = false`
    - Không có migration V3 nào khác trong cùng thư mục (tên file là `V3__add_product_stock.sql`)
  </acceptance_criteria>

  <done>Migration file tồn tại với đúng SQL statements, sẵn sàng cho Flyway chạy khi service start.</done>
</task>

<task type="auto">
  <name>Task 2: ProductEntity — thêm field stock + getter + setStock method</name>
  <files>sources/backend/product-service/src/main/java/com/ptit/htpt/productservice/domain/ProductEntity.java</files>

  <read_first>
    - sources/backend/product-service/src/main/java/com/ptit/htpt/productservice/domain/ProductEntity.java (PHẢI đọc trước — xem cấu trúc hiện tại, vị trí insert)
  </read_first>

  <action>
Thêm vào `ProductEntity.java`:

1. **Field declaration** — thêm sau `private BigDecimal originalPrice;` (line ~59):
```java
@Column(nullable = false)
private int stock = 0;
```

2. **Factory method `create()`** — giữ nguyên signature hiện tại (không thêm stock param vào `create()` để không phá các call sites hiện có). Stock set sau khi create qua setStock.

3. **Method `update()`** — thêm `int stock` param cuối, thêm `this.stock = stock;` trong body:
```java
public void update(String name, String slug, String categoryId, BigDecimal price, String status,
                   String brand, String thumbnailUrl,
                   String shortDescription, BigDecimal originalPrice, int stock) {
  // ... existing fields ...
  this.stock = stock;
  this.updatedAt = Instant.now();
}
```

4. **setStock method** — thêm sau `setStatus()`:
```java
public void setStock(int stock) {
  this.stock = Math.max(0, stock);
  this.updatedAt = Instant.now();
}
```

5. **Getter** — thêm sau `originalPrice()`:
```java
public int stock() { return stock; }
```

QUAN TRỌNG: Đọc file trước, chèn đúng vị trí, giữ nguyên toàn bộ code còn lại.
  </action>

  <verify>
    <automated>grep -n "private int stock" sources/backend/product-service/src/main/java/com/ptit/htpt/productservice/domain/ProductEntity.java && grep -n "public int stock()" sources/backend/product-service/src/main/java/com/ptit/htpt/productservice/domain/ProductEntity.java && grep -n "public void setStock" sources/backend/product-service/src/main/java/com/ptit/htpt/productservice/domain/ProductEntity.java</automated>
  </verify>

  <acceptance_criteria>
    - `ProductEntity.java` chứa `private int stock = 0;` với `@Column(nullable = false)`
    - `ProductEntity.java` chứa `public int stock() { return stock; }`
    - `ProductEntity.java` chứa `public void setStock(int stock)`
    - `ProductEntity.java` chứa `public void update(... int stock)` với `int stock` là param cuối
    - File vẫn compile được (không thay đổi imports, không xóa fields hiện có)
  </acceptance_criteria>

  <done>ProductEntity có field stock persist, getter trả về đúng value, setStock dùng được từ service.</done>
</task>

<task type="auto">
  <name>Task 3: ProductCrudService — wire stock thật + ProductUpsertRequest thêm stock field</name>
  <files>sources/backend/product-service/src/main/java/com/ptit/htpt/productservice/service/ProductCrudService.java</files>

  <read_first>
    - sources/backend/product-service/src/main/java/com/ptit/htpt/productservice/service/ProductCrudService.java (PHẢI đọc trước — thấy rõ dòng 178 hardcode + record ProductUpsertRequest)
    - sources/backend/product-service/src/main/java/com/ptit/htpt/productservice/domain/ProductEntity.java (sau khi Task 2 hoàn tất — xem signature update() mới)
  </read_first>

  <action>
Thực hiện 4 thay đổi trong `ProductCrudService.java`:

**1. Fix `toResponse()` line 178 — đọc stock từ entity thay vì hardcode 0:**
Tìm dòng:
```java
0,                                             // stock default — read from inventory-service
```
Thay bằng:
```java
product.stock(),                               // D-02: đọc từ ProductEntity.stock (Phase 8 PERSIST-01)
```

**2. Update `ProductUpsertRequest` record — thêm `int stock` field:**
Tìm record:
```java
public record ProductUpsertRequest(
    @NotBlank String name,
    ...
    BigDecimal originalPrice    // nullable — D-03
) {}
```
Thêm field cuối:
```java
public record ProductUpsertRequest(
    @NotBlank String name,
    @NotBlank String slug,
    @NotBlank String categoryId,
    @DecimalMin("0.0") BigDecimal price,
    @NotBlank String status,
    String brand,
    String thumbnailUrl,
    String shortDescription,
    BigDecimal originalPrice,
    @Min(0) int stock           // D-01: stock field cho admin set/update (Phase 8)
) {}
```
Cần thêm import `import jakarta.validation.constraints.Min;` nếu chưa có.

**3. Fix `createProduct()` — set stock sau khi tạo entity:**
Tìm:
```java
ProductEntity product = ProductEntity.create(...);
return productRepo.save(product);
```
Thêm `product.setStock(request.stock());` giữa create và save:
```java
ProductEntity product = ProductEntity.create(
    request.name(), request.slug(), request.categoryId(),
    request.price(), request.status(), request.brand(),
    request.thumbnailUrl(), request.shortDescription(), request.originalPrice()
);
product.setStock(request.stock());
return productRepo.save(product);
```

**4. Fix `updateProduct()` — truyền stock vào update():**
Tìm `current.update(request.name(), ..., request.originalPrice());`
Thêm `request.stock()` làm param cuối:
```java
current.update(
    request.name(), request.slug(), request.categoryId(), request.price(), request.status(),
    request.brand(), request.thumbnailUrl(), request.shortDescription(), request.originalPrice(),
    request.stock()
);
```

QUAN TRỌNG: Check imports — nếu `@Min` chưa có thì thêm `import jakarta.validation.constraints.Min;`.
  </action>

  <verify>
    <automated>grep -n "product\.stock()" sources/backend/product-service/src/main/java/com/ptit/htpt/productservice/service/ProductCrudService.java && grep -n "@Min(0) int stock" sources/backend/product-service/src/main/java/com/ptit/htpt/productservice/service/ProductCrudService.java && grep -n "product\.setStock(request\.stock())" sources/backend/product-service/src/main/java/com/ptit/htpt/productservice/service/ProductCrudService.java</automated>
  </verify>

  <acceptance_criteria>
    - `toResponse()` chứa `product.stock()` thay vì literal `0` trong vị trí stock
    - `ProductUpsertRequest` record chứa `int stock` field (có `@Min(0)` annotation)
    - `createProduct()` gọi `product.setStock(request.stock())` trước `productRepo.save(product)`
    - `updateProduct()` gọi `current.update(... request.stock())` với stock là param cuối
    - File không còn comment `// stock default — read from inventory-service` ở vị trí hardcode
  </acceptance_criteria>

  <done>ProductCrudService trả stock thật từ entity. Admin có thể set stock qua ProductUpsertRequest. createProduct và updateProduct đều persist stock.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin API → product-service | PATCH /api/products/admin/{id} nhận stock value — phải validate >= 0 |
| Flyway migration | Chạy as DB user — SQL injection không áp dụng (không có user input) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01-01 | Tampering | ProductUpsertRequest.stock | mitigate | `@Min(0)` annotation đảm bảo stock không âm; GlobalExceptionHandler trả VALIDATION_ERROR 400 |
| T-08-01-02 | Information Disclosure | ProductResponse.stock | accept | stock là public product info — không sensitive; trả trong public GET endpoints |
| T-08-01-03 | Elevation of Privilege | PATCH /admin/products/{id} | accept | Gateway route `/api/products/admin/**` → admin endpoint; Phase 6 đã có role check tại FE middleware; backend không có auth (MVP acceptable per v1.1 scope) |
</threat_model>

<verification>
```bash
# Verify migration file
grep "ADD COLUMN IF NOT EXISTS stock INT NOT NULL DEFAULT 0" sources/backend/product-service/src/main/resources/db/migration/V3__add_product_stock.sql

# Verify entity field
grep "private int stock" sources/backend/product-service/src/main/java/com/ptit/htpt/productservice/domain/ProductEntity.java

# Verify service wire
grep "product\.stock()" sources/backend/product-service/src/main/java/com/ptit/htpt/productservice/service/ProductCrudService.java

# Verify no hardcode 0 remains in stock position of toResponse
# (Count 0 occurrences at the right position — check manually if grep returns results)
```
</verification>

<success_criteria>
- V3 migration file tồn tại với đúng SQL
- ProductEntity có `private int stock`, `stock()`, `setStock()`, `update(..., int stock)`
- ProductCrudService.toResponse() gọi `product.stock()` (không phải literal 0)
- ProductUpsertRequest record có `@Min(0) int stock` field
- createProduct() và updateProduct() đều persist stock từ request
</success_criteria>

<output>
Sau khi hoàn thành, tạo `.planning/phases/08-cart-order-persistence/08-01-SUMMARY.md`
</output>
