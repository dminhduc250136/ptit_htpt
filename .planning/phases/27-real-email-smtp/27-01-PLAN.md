---
phase: 27-real-email-smtp
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/messaging/event/OrderEventEnvelope.java
  - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/messaging/config/RabbitMQConfig.java
  - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/messaging/publisher/OrderEventPublisher.java
  - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/service/OrderCrudService.java
autonomous: true
requirements: [MAIL-03, MAIL-04]
must_haves:
  truths:
    - "OrderPlaced event mang customerEmail + productName cho từng item"
    - "Khi admin đổi trạng thái đơn sang shipped/delivered/cancelled, order-service publish event OrderStatusChanged afterCommit"
  artifacts:
    - path: "sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/messaging/event/OrderEventEnvelope.java"
      provides: "OrderPlacedPayload có customerEmail; Item có productName; OrderStatusChangedPayload mới + factory createOrderStatusChanged"
      contains: "OrderStatusChangedPayload"
    - path: "sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/messaging/publisher/OrderEventPublisher.java"
      provides: "publishOrderStatusChanged() afterCommit"
      contains: "publishOrderStatusChanged"
  key_links:
    - from: "OrderCrudService.updateOrderState"
      to: "OrderEventPublisher.publishOrderStatusChanged"
      via: "method call sau orderRepository.save"
      pattern: "publishOrderStatusChanged"
---

<objective>
Mở rộng producer phía order-service để cung cấp đủ dữ liệu cho email và phát thêm event đổi trạng thái đơn.

Mục đích: notification-service (D-11) KHÔNG gọi REST cross-service — mọi dữ liệu render email phải nằm sẵn trong event payload. Hiện `OrderPlacedPayload` thiếu `customerEmail` và `Item` thiếu `productName`; đồng thời chưa có event nào phát khi đổi trạng thái đơn.

Output: `OrderEventEnvelope` mở rộng (customerEmail + productName + OrderStatusChangedPayload), `OrderEventPublisher.publishOrderStatusChanged()`, và `OrderCrudService.updateOrderState()` publish event afterCommit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-real-email-smtp/27-CONTEXT.md
@.planning/phases/27-real-email-smtp/27-RESEARCH.md
@.planning/phases/27-real-email-smtp/27-PATTERNS.md

<interfaces>
<!-- OrderEventEnvelope hiện tại (verified) — payload field là typed OrderPlacedPayload, KHÔNG phải Object -->
record OrderEventEnvelope(String eventId, String eventType, String occurredAt, String traceId, OrderPlacedPayload payload)
  static createOrderPlaced(String traceId, OrderPlacedPayload payload)

<!-- QUYẾT ĐỊNH PLANNER (Pitfall 3 RESEARCH): GIỮ field payload typed nhưng đổi sang Object để chứa cả 2 loại payload.
     Lý do: tách 2 envelope class sẽ buộc refactor OrderEventPublisher generic + listener 2 phía — rủi ro lan rộng hơn.
     Dùng Object + Jackson @JsonTypeInfo/@JsonSubTypes để deserialize đúng loại theo eventType. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Mở rộng OrderEventEnvelope với customerEmail, productName, OrderStatusChangedPayload</name>
  <read_first>
    - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/messaging/event/OrderEventEnvelope.java (file đang sửa — đọc TOÀN BỘ để biết OrderPlacedPayload + Item record hiện tại)
    - .planning/phases/27-real-email-smtp/27-PATTERNS.md (section OrderEventEnvelope — pattern mở rộng + Pitfall 3)
  </read_first>
  <behavior>
    - Test 1: ObjectMapper serialize rồi deserialize một OrderEventEnvelope chứa OrderPlacedPayload → payload đọc lại đúng kiểu OrderPlacedPayload, có customerEmail không null
    - Test 2: ObjectMapper serialize rồi deserialize một OrderEventEnvelope chứa OrderStatusChangedPayload → payload đọc lại đúng kiểu OrderStatusChangedPayload, đọc newStatus đúng
    - Test 3: createOrderPlaced() set eventType="OrderPlaced"; createOrderStatusChanged() set eventType="OrderStatusChanged"
  </behavior>
  <action>
Sửa `OrderEventEnvelope.java`:

1. Đổi field `payload` từ kiểu `OrderPlacedPayload` sang `Object` để chứa cả 2 loại payload. Annotate field bằng Jackson polymorphic deserialization:
```java
@JsonTypeInfo(use = JsonTypeInfo.Id.NAME, include = JsonTypeInfo.As.EXTERNAL_PROPERTY, property = "eventType")
@JsonSubTypes({
    @JsonSubTypes.Type(value = OrderPlacedPayload.class, name = "OrderPlaced"),
    @JsonSubTypes.Type(value = OrderStatusChangedPayload.class, name = "OrderStatusChanged")
})
```
Đặt annotation này lên component `payload` của record (dạng `@field:` hoặc trên record component param). Import `com.fasterxml.jackson.annotation.JsonTypeInfo` + `com.fasterxml.jackson.annotation.JsonSubTypes`.

2. Mở rộng `OrderPlacedPayload` record — thêm `String customerEmail` (chèn sau `userId`) và thêm `String productName` vào record `Item` (chèn sau `productId`). Giữ nguyên thứ tự các field còn lại (`items`, `totalAmount`, `currency` / `quantity`, `priceAtPurchase`).

3. Thêm record mới lồng trong envelope:
```java
public record OrderStatusChangedPayload(
    String orderId, String userId, String customerEmail,
    String newStatus, String customerName) {}
```

4. Thêm factory:
```java
public static OrderEventEnvelope createOrderStatusChanged(String traceId, OrderStatusChangedPayload payload) {
  return new OrderEventEnvelope(UUID.randomUUID().toString(), "OrderStatusChanged",
      Instant.now().toString(), traceId, payload);
}
```

Giữ `createOrderPlaced` signature cũ nhưng param `payload` vẫn nhận `OrderPlacedPayload` (autoboxing lên Object OK).

Lưu ý: callers hiện build `OrderPlacedPayload` sẽ vỡ compile vì thiếu `customerEmail`/`productName` — Task 3 sẽ sửa caller. KHÔNG dùng Lombok.
  </action>
  <verify>
    <automated>cd sources/backend/order-service && mvn -q compile 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "OrderStatusChangedPayload" OrderEventEnvelope.java` >= 3 (record def + factory param + import/usage)
    - `grep "customerEmail" OrderEventEnvelope.java` xuất hiện trong cả OrderPlacedPayload và OrderStatusChangedPayload
    - `grep "productName" OrderEventEnvelope.java` xuất hiện trong record Item
    - `grep "@JsonSubTypes" OrderEventEnvelope.java` xuất hiện
    - `grep "createOrderStatusChanged" OrderEventEnvelope.java` xuất hiện
  </acceptance_criteria>
  <done>OrderEventEnvelope chứa cả 2 payload type với Jackson polymorphic config; factory createOrderStatusChanged tồn tại.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Thêm publishOrderStatusChanged vào OrderEventPublisher + routing key constant</name>
  <read_first>
    - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/messaging/publisher/OrderEventPublisher.java (file đang sửa — đọc TOÀN BỘ, đặc biệt publishOrderPlaced + doPublish)
    - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/messaging/config/RabbitMQConfig.java (file đang sửa — xem nơi khai báo routing key constant)
    - .planning/phases/27-real-email-smtp/27-PATTERNS.md (section OrderEventPublisher — pattern method mới)
  </read_first>
  <behavior>
    - Test 1: Gọi publishOrderStatusChanged trong một transaction active → message KHÔNG publish ngay, chỉ publish sau afterCommit (verify qua TransactionSynchronizationManager registered)
    - Test 2: Gọi publishOrderStatusChanged ngoài transaction → publish ngay (fallback path)
  </behavior>
  <action>
1. Trong `RabbitMQConfig.java`: thêm constant `public static final String ROUTING_KEY_ORDER_STATUS_CHANGED = "order.status-changed";` cạnh constant routing key OrderPlaced hiện có. KHÔNG thêm exchange/queue mới — queue `notification.order-events` đã bind `order.#` nên `order.status-changed` tự route (D-03).

2. Trong `OrderEventPublisher.java`: thêm method `publishOrderStatusChanged(OrderEventEnvelope.OrderStatusChangedPayload payload)`, copy y cấu trúc `publishOrderPlaced()`:
```java
public void publishOrderStatusChanged(OrderEventEnvelope.OrderStatusChangedPayload payload) {
    String traceId = MDC.get("traceId");
    final String safeTraceId = traceId != null ? traceId : "no-trace";
    final OrderEventEnvelope envelope =
        OrderEventEnvelope.createOrderStatusChanged(safeTraceId, payload);
    if (TransactionSynchronizationManager.isSynchronizationActive()) {
        TransactionSynchronizationManager.registerSynchronization(new TransactionSynchronization() {
            @Override public void afterCommit() {
                doPublish(envelope, RabbitMQConfig.ROUTING_KEY_ORDER_STATUS_CHANGED, safeTraceId);
            }
        });
    } else {
        doPublish(envelope, RabbitMQConfig.ROUTING_KEY_ORDER_STATUS_CHANGED, safeTraceId);
    }
}
```
Nếu `doPublish` hiện tại hardcode routing key thay vì nhận tham số, refactor `doPublish` để nhận `String routingKey` làm tham số và cập nhật caller `publishOrderPlaced` tương ứng. Giữ nguyên CorrelationData + Publisher Confirms + TraceIdMessagePostProcessor trong doPublish. KHÔNG Lombok.
  </action>
  <verify>
    <automated>cd sources/backend/order-service && mvn -q compile 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep "ROUTING_KEY_ORDER_STATUS_CHANGED" RabbitMQConfig.java` = `order.status-changed`
    - `grep "publishOrderStatusChanged" OrderEventPublisher.java` xuất hiện
    - `grep "registerSynchronization" OrderEventPublisher.java` xuất hiện trong method mới (afterCommit pattern)
    - `mvn -q compile` exit 0
  </acceptance_criteria>
  <done>publishOrderStatusChanged tồn tại, dùng afterCommit pattern, publish với routing key order.status-changed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire OrderCrudService — publish OrderStatusChanged + cung cấp customerEmail/productName</name>
  <read_first>
    - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/service/OrderCrudService.java (file đang sửa — đọc TOÀN BỘ: updateOrderState + createOrderFromCommand nơi build OrderPlacedPayload)
    - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/messaging/event/OrderEventEnvelope.java (đã sửa ở Task 1 — biết signature payload mới)
    - .planning/phases/27-real-email-smtp/27-PATTERNS.md (section OrderCrudService — pattern updateOrderState)
  </read_first>
  <behavior>
    - Test 1: updateOrderState("shipped") → orderEventPublisher.publishOrderStatusChanged được gọi với payload.newStatus="shipped"
    - Test 2: createOrderFromCommand → OrderPlacedPayload build có customerEmail không blank và mỗi Item có productName
  </behavior>
  <action>
1. `updateOrderState(String id, OrderStateRequest request)`: sau `OrderEntity saved = orderRepository.save(current);`, thêm:
```java
OrderEventEnvelope.OrderStatusChangedPayload statusPayload =
    new OrderEventEnvelope.OrderStatusChangedPayload(
        saved.id(), saved.userId(), resolveCustomerEmail(saved),
        request.state(), null);
orderEventPublisher.publishOrderStatusChanged(statusPayload);
```
(Nếu `updateOrderState` chưa trả `OrderDto` từ biến `saved`, refactor để dùng `saved`.)

2. `createOrderFromCommand`: tại chỗ build `OrderPlacedPayload` (hiện chỉ có orderId/userId/items/totalAmount/currency), thêm `customerEmail` qua `resolveCustomerEmail(saved)` và thêm `productName` cho từng `Item`.

3. Cách lấy `customerEmail` và `productName` (D-11 — payload mang sẵn dữ liệu, KHÔNG gọi REST từ notification-service; nhưng order-service CÓ thể đọc dữ liệu sẵn có của chính nó):
   - `customerEmail`: viết helper `private String resolveCustomerEmail(OrderEntity order)`. Ưu tiên đọc từ field email đã có trên `OrderEntity`/`CreateOrderCommand` nếu tồn tại (đọc OrderEntity để xác nhận). Nếu OrderEntity KHÔNG có email: order-service đã có `RestTemplate` (dùng cho stock validate Phase 23) — gọi user-service `GET /users/{userId}` lấy email. Đọc OrderCrudService imports để xác nhận RestTemplate có sẵn; nếu cần base URL, dùng `@Value` env theo pattern stock validate hiện có.
   - `productName`: order-service khi build item từ cart đã có productId — nếu `OrderItemEntity` lưu sẵn `productName` (snapshot lúc đặt) thì dùng trực tiếp; nếu KHÔNG, dùng RestTemplate batch tới product-service như cách enrichment đã có. Đọc OrderItemEntity để xác nhận field nào tồn tại.
   - Nếu không thể lấy email/productName từ nguồn sẵn có, set giá trị fallback an toàn (`customerEmail` = "" → notification-service sẽ ghi SKIPPED; `productName` = productId) và ghi log WARN. KHÔNG để NPE.
  </action>
  <verify>
    <automated>cd sources/backend/order-service && mvn -q compile 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep "publishOrderStatusChanged" OrderCrudService.java` xuất hiện trong updateOrderState
    - `grep "customerEmail" OrderCrudService.java` xuất hiện trong build OrderPlacedPayload
    - `grep "productName" OrderCrudService.java` xuất hiện trong build Item
    - `mvn -q compile` exit 0
  </acceptance_criteria>
  <done>updateOrderState publish OrderStatusChanged; OrderPlacedPayload build có customerEmail + productName.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| order-service → RabbitMQ | Event payload mang customerEmail (PII) đi qua broker nội bộ |
| order-service → user-service (nếu fetch email) | Internal REST call lấy email khách |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-01 | Information Disclosure | OrderEventEnvelope chứa customerEmail trên broker | accept | Broker nội bộ docker network, không expose ra ngoài (precedent Phase 23 guest/guest accept). PII email cần thiết cho luồng gửi mail. |
| T-27-02 | Tampering | Event giả OrderStatusChanged | accept | Chỉ order-service publish vào exchange order.events trong mạng nội bộ; không có path từ client. |
</threat_model>

<verification>
- `mvn -q compile` order-service exit 0 sau cả 3 task.
- OrderEventEnvelope deserialize đúng polymorphic theo eventType (unit test Task 1).
</verification>

<success_criteria>
- OrderPlacedPayload có customerEmail + Item có productName.
- OrderStatusChangedPayload + factory + publishOrderStatusChanged tồn tại.
- updateOrderState publish event afterCommit khi đổi trạng thái.
</success_criteria>

<output>
After completion, create `.planning/phases/27-real-email-smtp/27-01-SUMMARY.md`
</output>
