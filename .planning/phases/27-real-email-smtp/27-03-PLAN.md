---
phase: 27-real-email-smtp
plan: 03
type: execute
wave: 2
depends_on: ["27-01"]
files_modified:
  - sources/backend/notification-service/pom.xml
  - sources/backend/notification-service/src/main/resources/application.yml
  - sources/backend/notification-service/src/main/java/com/ptit/htpt/notificationservice/service/email/EmailSender.java
  - sources/backend/notification-service/src/main/java/com/ptit/htpt/notificationservice/service/email/MailTemplate.java
  - sources/backend/notification-service/src/main/java/com/ptit/htpt/notificationservice/service/NotificationDispatchService.java
  - sources/backend/notification-service/src/main/java/com/ptit/htpt/notificationservice/messaging/event/OrderEventEnvelope.java
autonomous: true
requirements: [MAIL-01, MAIL-03, MAIL-04]
must_haves:
  truths:
    - "notification-service khởi động bình thường dù thiếu env SMTP, log WARN và mọi email ghi dispatch_log status=SKIPPED"
    - "Khi SMTP cấu hình đủ, EmailSender gửi MimeMessage HTML qua JavaMailSender"
    - "6 template HTML tiếng Việt render đủ dữ liệu yêu cầu"
  artifacts:
    - path: "sources/backend/notification-service/src/main/java/com/ptit/htpt/notificationservice/service/email/EmailSender.java"
      provides: "gửi SMTP với graceful degradation"
      contains: "EmailResult"
    - path: "sources/backend/notification-service/src/main/java/com/ptit/htpt/notificationservice/service/email/MailTemplate.java"
      provides: "enum 6 template HTML"
      contains: "ACCOUNT_VERIFICATION"
    - path: "sources/backend/notification-service/src/main/java/com/ptit/htpt/notificationservice/service/NotificationDispatchService.java"
      provides: "sendOrderConfirmation + sendOrderStatusChanged + sendUserEmail gọi EmailSender + ghi dispatch_log"
      contains: "sendOrderStatusChanged"
  key_links:
    - from: "NotificationDispatchService"
      to: "EmailSender.send"
      via: "method call sau template.render"
      pattern: "emailSender.send"
    - from: "EmailSender"
      to: "JavaMailSender"
      via: "MimeMessageHelper setText HTML"
      pattern: "createMimeMessage"
---

<objective>
Thêm tầng gửi SMTP thật vào notification-service với graceful degradation, 6 template HTML tiếng Việt, và nâng cấp NotificationDispatchService gọi email thật.

Mục đích: MAIL-01 (config SMTP từ env, không crash khi thiếu), MAIL-03 (email xác nhận đơn), MAIL-04 (email cập nhật trạng thái). Phase 23 chỉ ghi dispatch_log — phase này gửi mail thật và ghi status SENT/FAILED/SKIPPED.

Output: dependency `spring-boot-starter-mail`, `spring.mail.*` config, `EmailSender`, `MailTemplate` (6 template), `NotificationDispatchService` nâng cấp, `OrderEventEnvelope` phía notification-service đồng bộ với order-service.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-real-email-smtp/27-CONTEXT.md
@.planning/phases/27-real-email-smtp/27-RESEARCH.md
@.planning/phases/27-real-email-smtp/27-PATTERNS.md
@.planning/phases/27-real-email-smtp/27-01-SUMMARY.md

<interfaces>
<!-- spring-boot-starter-mail CHƯA có trong notification-service pom.xml (verified) -->
<!-- TransientMessageException / PermanentMessageException đã có tại
     notification-service/.../messaging/exception/ (Phase 23) — import trực tiếp -->
<!-- DispatchLogEntity.create(eventId, recipientUserId, channel, subject, body, status) đã có -->

<!-- OrderEventEnvelope notification-service hiện hardcoded OrderPlacedPayload — Task 1 đồng bộ
     với order-service (Plan 27-01): payload thành Object + @JsonSubTypes, thêm OrderStatusChangedPayload -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: spring-boot-starter-mail + spring.mail config + EmailSender graceful degradation</name>
  <read_first>
    - sources/backend/notification-service/pom.xml (file đang sửa — pattern khai báo dependency)
    - sources/backend/notification-service/src/main/resources/application.yml (file đang sửa — thêm spring.mail block)
    - sources/backend/notification-service/src/main/java/com/ptit/htpt/notificationservice/messaging/exception/ (đọc TransientMessageException + PermanentMessageException — constructor signature)
    - .planning/phases/27-real-email-smtp/27-PATTERNS.md (section EmailSender — pattern constructor + send)
    - .planning/phases/27-real-email-smtp/27-RESEARCH.md (Pattern 1 + Pitfall 1)
  </read_first>
  <behavior>
    - Test 1 (testSkipsWhenNotConfigured): EmailSender khởi tạo với mailSender=null → send() trả EmailResult.SKIPPED, KHÔNG throw
    - Test 2 (testSkipsWhenFromBlank): mailSender không null nhưng fromAddress blank → configured=false → send() trả SKIPPED
    - Test 3 (testWarnOnInit): khởi tạo khi chưa configured → log WARN một lần (verify qua không throw + configured=false)
    - Test 4: mailSender mock OK → send() build MimeMessage, gọi mailSender.send(), trả SENT
  </behavior>
  <action>
1. `notification-service/pom.xml`: thêm dependency (BOM 3.3.2 tự quản version):
```xml
<dependency>
  <groupId>org.springframework.boot</groupId>
  <artifactId>spring-boot-starter-mail</artifactId>
</dependency>
```

2. `notification-service/application.yml`: thêm dưới `spring:`:
```yaml
  mail:
    host: ${MAIL_SMTP_HOST:}
    port: ${MAIL_SMTP_PORT:587}
    username: ${MAIL_SMTP_USERNAME:}
    password: ${MAIL_SMTP_PASSWORD:}
    default-encoding: UTF-8
    properties:
      "[mail.smtp.auth]": true
      "[mail.smtp.starttls.enable]": true
      "[mail.smtp.connectiontimeout]": 5000
      "[mail.smtp.timeout]": 3000
      "[mail.smtp.writetimeout]": 5000
```

3. Tạo `service/email/EmailSender.java` — `@Service`:
   - Constructor: `@Autowired(required=false) JavaMailSender mailSender`, `@Value("${MAIL_FROM_ADDRESS:}") String fromAddress`. Set `configured = mailSender != null && !fromAddress.isBlank()` (Pitfall 1: check cả fromAddress vì blank host vẫn có thể tạo bean). Nếu `!configured` → `log.warn("[EMAIL-INIT] SMTP chưa cấu hình. Mọi email sẽ ghi SKIPPED vào dispatch_log.")`.
   - Nested `public enum EmailResult { SENT, FAILED, SKIPPED }`.
   - `public EmailResult send(String to, String subject, String htmlBody)`:
     - Nếu `!configured` HOẶC `to == null || to.isBlank()` → `log.warn("[EMAIL-SKIP] ...")` → return `SKIPPED`.
     - Try: `MimeMessage msg = mailSender.createMimeMessage()`; `MimeMessageHelper helper = new MimeMessageHelper(msg, false, "UTF-8")`; setFrom/setTo/setSubject/`setText(htmlBody, true)`; `mailSender.send(msg)`; return `SENT`.
     - catch `MailException` → `log.error("[EMAIL-FAIL] ...")` → throw `new TransientMessageException(...)` (D-17: SMTP lỗi tạm thời → retry → DLQ).
     - catch `MessagingException` → `log.error("[EMAIL-PERM-FAIL] ...")` → throw `new PermanentMessageException(...)`.
   Import `jakarta.mail.MessagingException`, `jakarta.mail.internet.MimeMessage`, `org.springframework.mail.MailException`, `org.springframework.mail.javamail.*`. KHÔNG Lombok.
  </action>
  <verify>
    <automated>cd sources/backend/notification-service && mvn -q test -Dtest=EmailSenderTest 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `grep "spring-boot-starter-mail" sources/backend/notification-service/pom.xml` xuất hiện
    - `grep "mail:" sources/backend/notification-service/src/main/resources/application.yml` xuất hiện với `starttls`
    - EmailSender.java có `@Autowired(required = false)`, enum `EmailResult`, throw `TransientMessageException` và `PermanentMessageException`
    - `EmailSenderTest` xanh (4 test)
  </acceptance_criteria>
  <done>EmailSender gửi HTML qua JavaMailSender, graceful degradation khi thiếu env (SKIPPED, không crash).</done>
</task>

<task type="auto">
  <name>Task 2: MailTemplate enum — 6 template HTML tiếng Việt</name>
  <read_first>
    - sources/backend/notification-service/src/main/java/com/ptit/htpt/notificationservice/service/NotificationDispatchService.java (analog — string-concat hiện tại để nâng cấp thành text block)
    - .planning/phases/27-real-email-smtp/27-PATTERNS.md (section MailTemplate — vars map keys cho mỗi template)
    - .planning/phases/27-real-email-smtp/27-RESEARCH.md (Pattern 3 — Java text block HTML)
  </read_first>
  <action>
Tạo `service/email/MailTemplate.java` — `public enum MailTemplate` với 6 hằng số, mỗi hằng override 2 abstract method `public abstract String subject()` và `public abstract String render(Map<String,String> vars)`.

6 template (nội dung tiếng Việt, HTML inline-CSS, text block `"""..."""` + `.formatted(...)`):

- `ACCOUNT_VERIFICATION` — subject "Xác minh địa chỉ email của bạn". Vars: `fullName`, `verifyUrl`. Body: lời chào, nút "Xác minh email" href verifyUrl, note "Link có hiệu lực trong 24 giờ".
- `PASSWORD_RESET` — subject "Đặt lại mật khẩu". Vars: `fullName`, `resetUrl`. Body: lời chào, nút "Đặt lại mật khẩu" href resetUrl, note "Link có hiệu lực trong 1 giờ. Nếu bạn không yêu cầu, bỏ qua email này."
- `ORDER_CONFIRMATION` — subject "Xác nhận đơn hàng". Vars: `orderId`, `totalAmount`, `currency`, `itemCount`. Body: cảm ơn đặt hàng, mã đơn, số sản phẩm, tổng tiền.
- `ORDER_SHIPPED` — subject "Đơn hàng đang được giao". Vars: `orderId`. Body: thông báo đơn đã chuyển sang trạng thái đang giao.
- `ORDER_DELIVERED` — subject "Đơn hàng đã giao thành công". Vars: `orderId`. Body: thông báo giao thành công, cảm ơn.
- `ORDER_CANCELLED` — subject "Đơn hàng đã bị huỷ". Vars: `orderId`. Body: thông báo đơn bị huỷ.

Mỗi template HTML: `<!DOCTYPE html><html lang="vi">` + `<head><meta charset="UTF-8">` + body inline-CSS (font Arial, max-width 600px, màu accent `#2563eb`). Dùng `%s` placeholder + `.formatted(vars.get("key"), ...)`. Đảm bảo mọi var key dùng trong `.formatted` đều có trong vars map mà NotificationDispatchService truyền (Task 3). KHÔNG dùng template engine ngoài (D-09).
  </action>
  <verify>
    <automated>cd sources/backend/notification-service && mvn -q compile 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - MailTemplate.java có đủ 6 hằng: `ACCOUNT_VERIFICATION`, `PASSWORD_RESET`, `ORDER_CONFIRMATION`, `ORDER_SHIPPED`, `ORDER_DELIVERED`, `ORDER_CANCELLED`
    - `grep -c "abstract" MailTemplate.java` >= 2 (subject + render)
    - `grep "lang=\"vi\"" MailTemplate.java` xuất hiện (HTML tiếng Việt)
    - `mvn -q compile` exit 0
  </acceptance_criteria>
  <done>Enum MailTemplate đủ 6 template HTML tiếng Việt với subject + render.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Đồng bộ OrderEventEnvelope + nâng cấp NotificationDispatchService gọi EmailSender</name>
  <read_first>
    - sources/backend/notification-service/src/main/java/com/ptit/htpt/notificationservice/messaging/event/OrderEventEnvelope.java (file đang sửa — đồng bộ với phiên bản order-service đã sửa ở 27-01)
    - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/messaging/event/OrderEventEnvelope.java (đã sửa ở 27-01 — copy y cấu trúc payload + @JsonSubTypes)
    - sources/backend/notification-service/src/main/java/com/ptit/htpt/notificationservice/service/NotificationDispatchService.java (file đang sửa — đọc TOÀN BỘ: recordOrderConfirmation hiện tại)
    - sources/backend/notification-service/src/main/java/com/ptit/htpt/notificationservice/domain/DispatchLogEntity.java (factory create signature)
    - .planning/phases/27-real-email-smtp/27-PATTERNS.md (section NotificationDispatchService)
  </read_first>
  <behavior>
    - Test 1: sendOrderConfirmation với customerEmail hợp lệ + EmailSender mock SENT → DispatchLogEntity lưu status="SENT"
    - Test 2: sendOrderConfirmation khi EmailSender trả SKIPPED → dispatch_log status="SKIPPED"
    - Test 3: sendOrderStatusChanged(ORDER_SHIPPED) → dispatch_log lưu subject của ORDER_SHIPPED
    - Test 4: sendUserEmail(ACCOUNT_VERIFICATION) → render với fullName + actionUrl → dispatch_log SENT
  </behavior>
  <action>
1. `OrderEventEnvelope.java` (notification-service): đồng bộ CHÍNH XÁC với phiên bản order-service đã sửa ở Plan 27-01 — đổi field `payload` sang `Object` + `@JsonTypeInfo`/`@JsonSubTypes` (OrderPlaced → OrderPlacedPayload, OrderStatusChanged → OrderStatusChangedPayload); thêm `customerEmail` vào OrderPlacedPayload, `productName` vào Item, thêm record `OrderStatusChangedPayload`. KHÔNG cần factory create (notification-service chỉ deserialize). Thêm accessor helper nếu cần để cast payload theo eventType.

2. Nâng cấp `NotificationDispatchService.java`:
   - Inject thêm `EmailSender emailSender` (constructor injection — cùng với `DispatchLogRepository` đã có).
   - `sendOrderConfirmation(String eventId, OrderEventEnvelope.OrderPlacedPayload payload)` `@Transactional`: vars Map(`orderId`, `totalAmount`=payload.totalAmount().toPlainString(), `currency`, `itemCount`=String.valueOf(items.size())); `subject = MailTemplate.ORDER_CONFIRMATION.subject()`; `body = render(vars)`; `result = emailSender.send(payload.customerEmail(), subject, body)`; `dispatchLogRepository.save(DispatchLogEntity.create(eventId, payload.userId(), "email", subject, body, result.name()))`.
   - `sendOrderStatusChanged(String eventId, OrderEventEnvelope.OrderStatusChangedPayload payload, MailTemplate template)` `@Transactional`: vars Map(`orderId`); subject/body/send/save tương tự, recipient = `payload.customerEmail()`, recipientUserId = `payload.userId()`.
   - `sendUserEmail(String eventId, MailTemplate template, UserPayloadView payload)` `@Transactional`: vars Map tùy template — ACCOUNT_VERIFICATION/PASSWORD_RESET cần `fullName` + `verifyUrl`/`resetUrl` (cả hai map từ `payload.actionUrl()`); send tới `payload.email()`; save dispatch_log.
     - `UserEventListener` (Plan 27-05) sẽ gọi method này. Để 27-05 không phụ thuộc class user của user-service, định nghĩa method nhận tham số kiểu của notification-service. Khuyến nghị: tham số là `String to, String fullName, String actionUrl` (3 String) để 27-05 truyền trực tiếp từ UserEventEnvelope.UserPayload của notification-service. Ghi rõ signature đã chọn trong SUMMARY để 27-05 dùng đúng.
   - Giữ method cũ `recordOrderConfirmation` nếu listener cũ còn gọi — nhưng 27-05 sẽ chuyển listener sang gọi `sendOrderConfirmation`. Có thể đổi tên `recordOrderConfirmation` → `sendOrderConfirmation` và cập nhật signature; ghi quyết định trong SUMMARY.
KHÔNG Lombok.
  </action>
  <verify>
    <automated>cd sources/backend/notification-service && mvn -q test -Dtest=NotificationDispatchServiceTest 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - OrderEventEnvelope.java (notification) có `OrderStatusChangedPayload`, `customerEmail`, `productName`, `@JsonSubTypes`
    - NotificationDispatchService.java có `emailSender` field + `sendOrderConfirmation` + `sendOrderStatusChanged` + `sendUserEmail`
    - `grep "emailSender.send" NotificationDispatchService.java` xuất hiện
    - `grep "result.name()" NotificationDispatchService.java` xuất hiện (status từ EmailResult)
    - `mvn -q compile` exit 0
  </acceptance_criteria>
  <done>OrderEventEnvelope đồng bộ; NotificationDispatchService gọi EmailSender thật + ghi dispatch_log SENT/FAILED/SKIPPED.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RabbitMQ → notification-service | Event payload (untrusted-ish) chứa customerEmail, dữ liệu render template |
| notification-service → Gmail SMTP | Kết nối ra ngoài qua STARTTLS với App Password |
| env → notification-service | SMTP credential nạp qua biến môi trường |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-07 | Information Disclosure | SMTP password hardcode trong source | mitigate | Toàn bộ credential đọc từ env `MAIL_SMTP_*` (D-14); không giá trị mặc định chứa secret trong application.yml. |
| T-27-08 | Denial of Service | SMTP server treo → consumer block | mitigate | `mail.smtp.connectiontimeout=5000` + `timeout=3000` + `writetimeout=5000` giới hạn thời gian chờ. |
| T-27-09 | Tampering | HTML injection qua dữ liệu payload vào template | accept | Dữ liệu (orderId, fullName) là internal-generated, không phải free-text từ attacker; visible-first priority — escape đầy đủ defer. |
</threat_model>

<verification>
- `mvn -q compile` notification-service exit 0.
- `EmailSenderTest` + `NotificationDispatchServiceTest` xanh.
- application.yml có spring.mail block, không chứa credential literal.
</verification>

<success_criteria>
- notification-service không crash khi thiếu env SMTP (graceful degradation).
- 6 template HTML tiếng Việt render đủ dữ liệu.
- NotificationDispatchService gửi mail thật + ghi dispatch_log SENT/FAILED/SKIPPED.
</success_criteria>

<output>
After completion, create `.planning/phases/27-real-email-smtp/27-03-SUMMARY.md`
</output>
