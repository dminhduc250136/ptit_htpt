---
phase: 27-real-email-smtp
plan: 05
type: execute
wave: 3
depends_on: ["27-03", "27-04"]
files_modified:
  - sources/backend/notification-service/src/main/java/com/ptit/htpt/notificationservice/messaging/config/RabbitMQConfig.java
  - sources/backend/notification-service/src/main/java/com/ptit/htpt/notificationservice/messaging/event/UserEventEnvelope.java
  - sources/backend/notification-service/src/main/java/com/ptit/htpt/notificationservice/messaging/consumer/UserEventListener.java
  - sources/backend/notification-service/src/main/java/com/ptit/htpt/notificationservice/messaging/consumer/OrderPlacedNotifyListener.java
  - docker-compose.yml
  - sources/frontend/src/app/forgot-password/page.tsx
  - sources/frontend/src/app/verify-email/page.tsx
  - sources/frontend/src/app/verify-email/page.module.css
  - sources/frontend/src/app/reset-password/page.tsx
  - sources/frontend/src/services/auth.ts
autonomous: true
requirements: [MAIL-01, MAIL-02, MAIL-03, MAIL-04]
must_haves:
  truths:
    - "notification-service consume queue notification.user-events → gửi email xác minh / reset"
    - "OrderPlacedNotifyListener branch theo eventType: OrderPlaced → email xác nhận, OrderStatusChanged shipped/delivered/cancelled → email cập nhật"
    - "Người dùng mở /verify-email?token=... thấy trạng thái thành công / hết hạn / không hợp lệ"
    - "Người dùng dùng /forgot-password và /reset-password để đặt lại mật khẩu"
  artifacts:
    - path: "sources/backend/notification-service/src/main/java/com/ptit/htpt/notificationservice/messaging/consumer/UserEventListener.java"
      provides: "consume notification.user-events idempotent + gửi email tài khoản"
      contains: "notification.user-events"
    - path: "sources/frontend/src/app/verify-email/page.tsx"
      provides: "status card đọc ?token gọi verify-email API"
      contains: "useSearchParams"
    - path: "sources/frontend/src/app/forgot-password/page.tsx"
      provides: "form nhập email gọi password/forgot"
      contains: "forgot-password"
    - path: "sources/frontend/src/app/reset-password/page.tsx"
      provides: "form mật khẩu mới gọi password/reset"
      contains: "reset-password"
  key_links:
    - from: "UserEventListener"
      to: "NotificationDispatchService.sendUserEmail"
      via: "branch theo eventType"
      pattern: "sendUserEmail"
    - from: "OrderPlacedNotifyListener"
      to: "NotificationDispatchService.sendOrderStatusChanged"
      via: "switch eventType OrderStatusChanged"
      pattern: "sendOrderStatusChanged"
    - from: "verify-email/page.tsx"
      to: "/api/users/auth/verify-email"
      via: "fetch trong useEffect"
      pattern: "verify-email"
---

<objective>
Hoàn tất luồng email end-to-end: consumer notification-service cho event tài khoản + nhánh status đơn, env docker-compose, và 3 trang FE auth.

Mục đích: đóng cả 4 requirement — UserEventListener gửi email xác minh/reset (MAIL-02), OrderPlacedNotifyListener branch gửi email xác nhận + cập nhật đơn (MAIL-03/04), docker-compose nạp env SMTP + RabbitMQ (MAIL-01), 3 trang FE để người dùng tương tác link email (MAIL-02).

Output: topology `user.events` phía notification-service, `UserEventListener`, `UserEventEnvelope`, `OrderPlacedNotifyListener` mở rộng, env docker-compose, 3 trang Next.js + service call.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-real-email-smtp/27-CONTEXT.md
@.planning/phases/27-real-email-smtp/27-RESEARCH.md
@.planning/phases/27-real-email-smtp/27-PATTERNS.md
@.planning/phases/27-real-email-smtp/27-UI-SPEC.md
@.planning/phases/27-real-email-smtp/27-03-SUMMARY.md
@.planning/phases/27-real-email-smtp/27-04-SUMMARY.md

<interfaces>
<!-- Từ Plan 27-03: signature chính xác xem 27-03-SUMMARY.md -->
NotificationDispatchService.sendOrderConfirmation(eventId, OrderEventEnvelope.OrderPlacedPayload)
NotificationDispatchService.sendOrderStatusChanged(eventId, OrderEventEnvelope.OrderStatusChangedPayload, MailTemplate)
NotificationDispatchService.sendUserEmail(eventId, MailTemplate, ...)  <!-- signature String to/fullName/actionUrl per 27-03-SUMMARY -->
ProcessedEventRepository.insertIfAbsent(eventId, eventType) -> boolean   <!-- Phase 23, đã có -->

<!-- FE: login page ĐÃ có <Link href="/forgot-password"> (verified line 132) — KHÔNG cần thêm -->
<!-- FE pattern: tái dụng login/page.module.css cho forgot + reset; verify-email có CSS riêng -->
<!-- API gateway prefix cho user-service: /api/users (FE gọi /api/users/auth/...) -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Topology user.events + UserEventListener + nhánh OrderStatusChanged</name>
  <read_first>
    - sources/backend/notification-service/src/main/java/com/ptit/htpt/notificationservice/messaging/config/RabbitMQConfig.java (file đang sửa — đọc TOÀN BỘ topology order.events hiện có)
    - sources/backend/notification-service/src/main/java/com/ptit/htpt/notificationservice/messaging/consumer/OrderPlacedNotifyListener.java (file đang sửa — đọc TOÀN BỘ: idempotency, traceId, error handling)
    - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/messaging/event/OrderEventEnvelope.java (analog cho UserEventEnvelope record)
    - .planning/phases/27-real-email-smtp/27-PATTERNS.md (sections RabbitMQConfig, UserEventListener, UserEventEnvelope, OrderPlacedNotifyListener)
    - .planning/phases/27-real-email-smtp/27-03-SUMMARY.md (signature NotificationDispatchService chính xác)
  </read_first>
  <behavior>
    - Test 1 (OrderStatusChangedListenerIT): publish OrderStatusChanged newStatus="shipped" → dispatch_log có 1 bản ghi subject ORDER_SHIPPED
    - Test 2: OrderStatusChanged newStatus="confirmed" → KHÔNG gửi email, KHÔNG ghi dispatch_log mới (template null → bỏ qua)
    - Test 3: cùng eventId xử lý 2 lần → chỉ 1 dispatch_log (idempotent qua processed_events)
    - Test 4 (UserEventListener): publish UserRegistered → dispatch_log subject ACCOUNT_VERIFICATION
  </behavior>
  <action>
1. `RabbitMQConfig.java` (notification-service): thêm topology `user.events` (KHÔNG khai báo lại `jacksonMessageConverter`/`rabbitTemplate` đã có). Thêm constants + `@Bean`: `USER_EXCHANGE="user.events"`, `USER_DLX="user.dlx"`, `USER_DLQ="user-events.dlq"`, `USER_DLQ_ROUTING="user-events"`, `USER_NOTIFICATION_QUEUE="notification.user-events"`, `USER_BINDING_KEY="user.#"`. Beans: `userEventsExchange` (TopicExchange durable), `userDeadLetterExchange` (DirectExchange durable), `userDeadLetterQueue`, `userDlqBinding`, `userNotificationQueue` (durable + x-dead-letter-exchange/routing-key args), `userNotificationBinding`. Copy y pattern topology order.events.

2. Tạo `messaging/event/UserEventEnvelope.java` — record `(String eventId, String eventType, String occurredAt, String traceId, UserPayload payload)` với nested `record UserPayload(String userId, String email, String fullName, String actionUrl)`. KHÔNG cần factory (chỉ deserialize).

3. Tạo `messaging/consumer/UserEventListener.java` — `@Component`, `@RabbitListener(queues="notification.user-events")` method `@Transactional`. Copy cấu trúc OrderPlacedNotifyListener:
   - `TraceIdConsumerInterceptor.enter(traceIdHeader)` đầu, `.exit()` finally.
   - Idempotency: `processedEventRepository.insertIfAbsent(eventId, eventType)` → nếu false log skipped-duplicate + return.
   - Branch eventType: `"UserRegistered"` → `sendUserEmail(eventId, MailTemplate.ACCOUNT_VERIFICATION, payload.email(), payload.fullName(), payload.actionUrl())`; `"PasswordResetRequested"` → `MailTemplate.PASSWORD_RESET` tương tự; default → throw `PermanentMessageException`.
   - 5 log path `[MQ-CONSUME]` received/done/skipped-duplicate + `[MQ-DLQ]` + `[MQ-RETRY]`; catch PermanentMessageException re-throw, TransientDataAccess → wrap TransientMessageException, RuntimeException → AmqpRejectAndDontRequeueException.
   - Dùng đúng signature `sendUserEmail` theo 27-03-SUMMARY.

4. `OrderPlacedNotifyListener.java`: thay lời gọi trực tiếp confirmation bằng branch theo `envelope.eventType()` (sau idempotency check):
   - `"OrderPlaced"` → `notificationDispatchService.sendOrderConfirmation(eventId, <cast payload OrderPlacedPayload>)`.
   - `"OrderStatusChanged"` → cast payload `OrderStatusChangedPayload`; map `newStatus` → MailTemplate: `shipped`→ORDER_SHIPPED, `delivered`→ORDER_DELIVERED, `cancelled`→ORDER_CANCELLED, default→`null`; nếu template != null → `sendOrderStatusChanged(eventId, payload, template)`; null → log info bỏ qua, KHÔNG ghi dispatch_log.
   - default eventType → throw `PermanentMessageException`.
   Vì OrderEventEnvelope dùng `Object` payload + `@JsonSubTypes` (Plan 27-03), cast theo eventType là an toàn sau Jackson polymorphic deserialize. Constructor injection KHÔNG đổi nếu NotificationDispatchService đã inject sẵn.
KHÔNG Lombok.
  </action>
  <verify>
    <automated>cd sources/backend/notification-service && mvn -q test -Dtest=OrderStatusChangedListenerIT,UserEventListenerIT 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep "USER_EXCHANGE" RabbitMQConfig.java` = `user.events`; `grep -c "@Bean" RabbitMQConfig.java` tăng so với trước (>= 6 bean user mới)
    - File UserEventEnvelope.java có record `UserPayload`
    - File UserEventListener.java có `@RabbitListener(queues = ` `notification.user-events` + `insertIfAbsent`
    - `grep "OrderStatusChanged" OrderPlacedNotifyListener.java` xuất hiện trong switch/branch
    - `grep "ORDER_SHIPPED\|ORDER_DELIVERED\|ORDER_CANCELLED" OrderPlacedNotifyListener.java` xuất hiện
    - Tests xanh
  </acceptance_criteria>
  <done>topology user.events + UserEventListener + OrderPlacedNotifyListener branch eventType hoạt động idempotent.</done>
</task>

<task type="auto">
  <name>Task 2: docker-compose env SMTP + RabbitMQ cho user-service + notification-service</name>
  <read_first>
    - docker-compose.yml (file đang sửa — đọc block user-service + notification-service + rabbitmq service)
    - .planning/phases/27-real-email-smtp/27-RESEARCH.md (section Docker-compose additions)
  </read_first>
  <action>
Sửa `docker-compose.yml`:

1. Service `user-service`: thêm `depends_on` `rabbitmq: { condition: service_healthy }` (giữ depends_on postgres hiện có). Thêm vào `environment`:
```yaml
  SPRING_RABBITMQ_HOST: rabbitmq
  SPRING_RABBITMQ_USER: guest
  SPRING_RABBITMQ_PASS: guest
  APP_BASE_URL: http://localhost:3000
```
(Dùng đúng tên env-var key mà user-service application.yml đọc — đối chiếu application.yml đã sửa ở 27-02; nếu key là `SPRING_RABBITMQ_USERNAME/PASSWORD` thì dùng key đó.)

2. Service `notification-service`: thêm vào `environment`:
```yaml
  MAIL_SMTP_HOST: ${MAIL_SMTP_HOST:-smtp.gmail.com}
  MAIL_SMTP_PORT: ${MAIL_SMTP_PORT:-587}
  MAIL_SMTP_USERNAME: ${MAIL_SMTP_USERNAME:-}
  MAIL_SMTP_PASSWORD: ${MAIL_SMTP_PASSWORD:-}
  MAIL_FROM_ADDRESS: ${MAIL_FROM_ADDRESS:-}
```
KHÔNG hardcode credential — dùng `${VAR:-default}` để giá trị thật nạp từ shell env / file .env của user. Nếu user không cấp → MAIL_SMTP_USERNAME/PASSWORD/FROM rỗng → graceful degradation (D-15).
  </action>
  <verify>
    <automated>docker compose -f docker-compose.yml config 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep "APP_BASE_URL" docker-compose.yml` xuất hiện trong block user-service
    - `grep "MAIL_SMTP_PASSWORD" docker-compose.yml` xuất hiện và dùng `${...}` (KHÔNG literal password)
    - `grep "SPRING_RABBITMQ_HOST" docker-compose.yml` xuất hiện trong block user-service
    - `docker compose config` exit 0 (YAML hợp lệ)
  </acceptance_criteria>
  <done>docker-compose nạp env SMTP + RabbitMQ + APP_BASE_URL; không có credential literal.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 3 trang FE auth — forgot-password / verify-email / reset-password</name>
  <read_first>
    - sources/frontend/src/app/login/page.tsx (analog cho forgot-password — đọc TOÀN BỘ: Suspense, handleSubmit, Banner, layout)
    - sources/frontend/src/app/register/page.tsx (analog cho reset-password — 2 password field + validation match)
    - sources/frontend/src/app/login/page.module.css (CSS tái dụng cho forgot + reset)
    - sources/frontend/src/services/auth.ts (file đang sửa — pattern API call hiện có)
    - .planning/phases/27-real-email-smtp/27-UI-SPEC.md (Copywriting Contract + Interaction Contracts + Page Layouts — đọc TOÀN BỘ)
    - .planning/phases/27-real-email-smtp/27-PATTERNS.md (sections forgot-password/verify-email/reset-password page.tsx)
  </read_first>
  <what-built>
3 trang Next.js App Router + service call, theo UI-SPEC:

1. `services/auth.ts`: thêm 3 hàm — `forgotPassword(email)` POST `/api/users/auth/password/forgot`; `resetPassword(token, newPassword)` POST `/api/users/auth/password/reset`; verify-email gọi trực tiếp fetch trong page (GET). Theo pattern API call + ApiError hiện có.

2. `forgot-password/page.tsx` — form 1 Input email + Button submit. Tái dụng `import styles from '../login/page.module.css'`. Suspense wrapper. States: idle/loading/success/error. Success → ẩn form, hiện panel "Kiểm tra hộp thư của bạn" với email. Copy theo UI-SPEC Copywriting Contract /forgot-password. switchAuth link "/login".

3. `verify-email/page.tsx` + `verify-email/page.module.css` — status card (KHÔNG form). Suspense + `useSearchParams` đọc `?token`. useEffect gọi `GET /api/users/auth/verify-email?token=...`. State machine `loading|success|expired|invalid`: 200→success, 410→expired, khác→invalid, không token→invalid ngay. Icon circle 64px (success `#16a34a` one-off, error `var(--error)`). CTA theo state. CSS riêng: `.page .statusCard .iconCircle .iconCircleSuccess .iconCircleError .heading .body .cta`.

4. `reset-password/page.tsx` — form 2 password field (mật khẩu mới + xác nhận). Tái dụng login/page.module.css. Suspense + `useSearchParams` đọc `?token`. Không token → status card lỗi. Validate độ dài >=6 + khớp 2 field. Submit → `resetPassword(token, newPassword)`. Success → panel "Mật khẩu đã được đặt lại!" + CTA "/login". Token 400/410 → status card lỗi. Copy theo UI-SPEC /reset-password.

Login page: ĐÃ có link "Quên mật khẩu?" (verified) — KHÔNG sửa.
  </what-built>
  <how-to-verify>
1. `cd sources/frontend && npm run build` — phải build thành công (Suspense wrap đúng, không lỗi useSearchParams).
2. Chạy `docker compose up` (hoặc `npm run dev`), mở trình duyệt:
   - `http://localhost:3000/forgot-password` — form email hiển thị, layout giống login, submit email bất kỳ → panel "Kiểm tra hộp thư".
   - `http://localhost:3000/verify-email` (không token) → status card "Link không hợp lệ" màu đỏ.
   - `http://localhost:3000/verify-email?token=abc` → spinner rồi status lỗi (token giả).
   - `http://localhost:3000/reset-password` (không token) → status card lỗi.
   - `http://localhost:3000/reset-password?token=abc` → form 2 password; nhập lệch → lỗi "Mật khẩu không khớp".
3. Kiểm tra tiếng Việt đúng theo Copywriting Contract, không vỡ layout.
  </how-to-verify>
  <resume-signal>Type "approved" hoặc mô tả vấn đề UI cần sửa</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RabbitMQ → notification-service | Event tài khoản (chứa actionUrl + token) qua queue user-events |
| browser → /verify-email?token | Token đi qua query param trong URL bookmark/history |
| FE → API gateway | Request forgot/reset từ trình duyệt |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-13 | Tampering | Open-redirect qua link verify/reset | mitigate | actionUrl dựng server-side từ `APP_BASE_URL` cố định + token (Plan 27-04) — client KHÔNG cung cấp URL; FE chỉ đọc `?token`, không redirect theo param ngoài. |
| T-27-14 | Information Disclosure | Token lộ trong browser history /verify-email?token | accept | Token single-use + hết hạn (T-27-04/05); harmless sau khi consume. Visible-first — hardening (one-time hash) defer. |
| T-27-15 | Denial of Service | Consumer email lỗi → loop | mitigate | Tái dụng retry 3× + DLQ Phase 23; PermanentMessageException → DLQ ngay (D-17). |
</threat_model>

<verification>
- `mvn -q test` notification-service: `OrderStatusChangedListenerIT` + `UserEventListenerIT` xanh.
- `docker compose config` hợp lệ.
- `npm run build` frontend thành công.
- Checkpoint human-verify 3 trang FE approved.
</verification>

<success_criteria>
- UserEventListener gửi email xác minh/reset; OrderPlacedNotifyListener gửi email xác nhận + cập nhật trạng thái.
- docker-compose nạp env đầy đủ.
- 3 trang FE hoạt động đúng UI-SPEC.
</success_criteria>

<output>
After completion, create `.planning/phases/27-real-email-smtp/27-05-SUMMARY.md`
</output>
