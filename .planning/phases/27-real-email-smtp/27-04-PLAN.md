---
phase: 27-real-email-smtp
plan: 04
type: execute
wave: 2
depends_on: ["27-02"]
files_modified:
  - sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/service/AuthService.java
  - sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/web/AuthController.java
  - sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/web/dto/ForgotPasswordRequest.java
  - sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/web/dto/ResetPasswordRequest.java
autonomous: true
requirements: [MAIL-02]
must_haves:
  truths:
    - "Đăng ký tài khoản mới → user-service sinh token EMAIL_VERIFY + publish event user.registered"
    - "GET /auth/verify-email?token=... lật email_verified=true và đánh dấu token used"
    - "POST /auth/password/forgot luôn trả 200 dù email không tồn tại (chống enumeration)"
    - "POST /auth/password/reset đổi mật khẩu khi token hợp lệ"
  artifacts:
    - path: "sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/web/AuthController.java"
      provides: "3 endpoint verify-email + password/forgot + password/reset"
      contains: "verify-email"
    - path: "sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/service/AuthService.java"
      provides: "register hook publish event + verifyEmail + forgotPassword + resetPassword"
      contains: "forgotPassword"
  key_links:
    - from: "AuthService.register"
      to: "AccountEventPublisher.publishUserRegistered"
      via: "method call sau userRepo.save"
      pattern: "publishUserRegistered"
    - from: "AuthController POST /password/reset"
      to: "VerificationTokenService.verifyAndConsume"
      via: "AuthService.resetPassword"
      pattern: "verifyAndConsume"
---

<objective>
Gắn luồng token email vào auth của user-service: register phát event xác minh, và 3 endpoint mới cho verify-email / forgot-password / reset-password.

Mục đích: hoàn tất MAIL-02 phía user-service. Login KHÔNG bị hard-gate (D-07) — register vẫn phát JWT ngay, link xác minh chỉ lật cờ. `POST /password/forgot` luôn trả 200 để chống email enumeration (D-08).

Output: `AuthService` (register hook + verifyEmail + forgotPassword + resetPassword), 3 endpoint trong `AuthController`, 2 request DTO.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-real-email-smtp/27-CONTEXT.md
@.planning/phases/27-real-email-smtp/27-RESEARCH.md
@.planning/phases/27-real-email-smtp/27-PATTERNS.md
@.planning/phases/27-real-email-smtp/27-02-SUMMARY.md

<interfaces>
<!-- Từ Plan 27-02 (Wave 1) — dùng trực tiếp: -->
VerificationTokenService.createToken(String userId, String type, Duration ttl) -> VerificationTokenEntity
VerificationTokenService.verifyAndConsume(String token, String expectedType) -> VerificationTokenEntity
  <!-- type ∈ {"EMAIL_VERIFY", "PASSWORD_RESET"} -->
AccountEventPublisher.publishUserRegistered(UserEventEnvelope.UserPayload payload)
AccountEventPublisher.publishPasswordReset(UserEventEnvelope.UserPayload payload)
UserEventEnvelope.UserPayload(String userId, String email, String fullName, String actionUrl)
UserEntity.setEmailVerified(boolean) ; UserEntity.emailVerified()
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Request DTO + nâng cấp AuthService với verifyEmail/forgotPassword/resetPassword + register hook</name>
  <read_first>
    - sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/service/AuthService.java (file đang sửa — đọc TOÀN BỘ: register(), error pattern ResponseStatusException, JWT issue, password encoder)
    - sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/web/dto/ (đọc 1 request DTO có sẵn — pattern record + jakarta validation annotations)
    - .planning/phases/27-real-email-smtp/27-PATTERNS.md (section AuthService — pattern chèn register + forgotPassword)
    - .planning/phases/27-real-email-smtp/27-02-SUMMARY.md (signature chính xác VerificationTokenService + AccountEventPublisher)
  </read_first>
  <action>
1. Tạo `web/dto/ForgotPasswordRequest.java` — record `(@Email @NotBlank String email)`. Import `jakarta.validation.constraints.*`.

2. Tạo `web/dto/ResetPasswordRequest.java` — record `(@NotBlank String token, @NotBlank @Size(min=6) String newPassword)`.

3. `AuthService.java` — inject thêm `VerificationTokenService verificationTokenService`, `AccountEventPublisher accountEventPublisher`, và `@Value("${APP_BASE_URL:http://localhost:3000}") String appBaseUrl` (constructor injection — thêm vào constructor hiện có).

4. `register()`: sau `userRepo.save(entity)` (giữ nguyên luồng phát JWT — D-07), chèn:
```java
String verifyToken = verificationTokenService.createToken(
    entity.id(), "EMAIL_VERIFY", Duration.ofHours(24)).token();
String verifyUrl = appBaseUrl + "/verify-email?token=" + verifyToken;
accountEventPublisher.publishUserRegistered(new UserEventEnvelope.UserPayload(
    entity.id(), entity.email(),
    entity.fullName() != null ? entity.fullName() : entity.username(),
    verifyUrl));
```
Giữ phần issue JWT + return AuthResponseDto KHÔNG đổi.

5. Thêm method `verifyEmail(String token)` `@Transactional`:
```java
VerificationTokenEntity vt = verificationTokenService.verifyAndConsume(token, "EMAIL_VERIFY");
UserEntity user = userRepo.findById(vt.userId())
    .orElseThrow(() -> new ResponseStatusException(HttpStatus.BAD_REQUEST, "Tài khoản không tồn tại"));
user.setEmailVerified(true);
userRepo.save(user);
```

6. Thêm method `forgotPassword(String email)` `@Transactional` — anti-enumeration, KHÔNG throw dù email không tồn tại:
```java
userRepo.findByEmail(email).ifPresent(user -> {
    String resetToken = verificationTokenService.createToken(
        user.id(), "PASSWORD_RESET", Duration.ofHours(1)).token();
    String resetUrl = appBaseUrl + "/reset-password?token=" + resetToken;
    accountEventPublisher.publishPasswordReset(new UserEventEnvelope.UserPayload(
        user.id(), user.email(),
        user.fullName() != null ? user.fullName() : user.username(),
        resetUrl));
});
```

7. Thêm method `resetPassword(String token, String newPassword)` `@Transactional`:
```java
VerificationTokenEntity vt = verificationTokenService.verifyAndConsume(token, "PASSWORD_RESET");
UserEntity user = userRepo.findById(vt.userId())
    .orElseThrow(() -> new ResponseStatusException(HttpStatus.BAD_REQUEST, "Tài khoản không tồn tại"));
user.setPasswordHash(passwordEncoder.encode(newPassword));  // dùng đúng tên setter/encoder hiện có
userRepo.save(user);
```
Đọc UserEntity + AuthService để dùng đúng tên setter password hash và bean encoder hiện có. KHÔNG Lombok.
  </action>
  <verify>
    <automated>cd sources/backend/user-service && mvn -q compile 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File ForgotPasswordRequest.java + ResetPasswordRequest.java tồn tại với jakarta validation annotations
    - `grep "publishUserRegistered" AuthService.java` xuất hiện trong register
    - AuthService.java có method `verifyEmail`, `forgotPassword`, `resetPassword`
    - `grep "findByEmail" AuthService.java` trong forgotPassword dùng `.ifPresent` (không throw)
    - `mvn -q compile` exit 0
  </acceptance_criteria>
  <done>AuthService phát event khi register + 3 method verifyEmail/forgotPassword/resetPassword; register vẫn phát JWT ngay.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 3 endpoint mới trong AuthController</name>
  <read_first>
    - sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/web/AuthController.java (file đang sửa — đọc TOÀN BỘ: pattern @PostMapping + ApiResponse.of + endpoint logout)
    - sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/web/dto/ForgotPasswordRequest.java (DTO Task 1)
    - sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/web/dto/ResetPasswordRequest.java (DTO Task 1)
    - .planning/phases/27-real-email-smtp/27-PATTERNS.md (section AuthController — pattern 3 endpoint + Pattern 4 ApiResponse)
  </read_first>
  <behavior>
    - Test 1 (testVerifyEmail): GET /auth/verify-email?token={valid} → 200; user.email_verified=true sau call
    - Test 2 (testForgotReturns200Always): POST /auth/password/forgot với email KHÔNG tồn tại → vẫn 200
    - Test 3: POST /auth/password/forgot với email tồn tại → 200 + event publish
    - Test 4: POST /auth/password/reset với token hợp lệ → 200; login bằng mật khẩu mới thành công
    - Test 5: GET /auth/verify-email?token={token đã used} → 410
  </behavior>
  <action>
Thêm 3 endpoint vào `AuthController.java` (sau endpoint cuối cùng hiện có). Dùng pattern `ApiResponse.of(statusCode, message, data)` — KHÔNG return plain DTO (tránh double-wrap):

```java
@GetMapping("/verify-email")
public ApiResponse<Void> verifyEmail(@RequestParam String token) {
    authService.verifyEmail(token);
    return ApiResponse.of(200, "Email xác minh thành công", null);
}

@PostMapping("/password/forgot")
public ApiResponse<Void> forgotPassword(@Valid @RequestBody ForgotPasswordRequest request) {
    authService.forgotPassword(request.email());
    return ApiResponse.of(200, "Nếu email tồn tại, link đặt lại đã được gửi", null);
}

@PostMapping("/password/reset")
public ApiResponse<Void> resetPassword(@Valid @RequestBody ResetPasswordRequest request) {
    authService.resetPassword(request.token(), request.newPassword());
    return ApiResponse.of(200, "Mật khẩu đã được đặt lại", null);
}
```
Import thêm `GetMapping`, `RequestParam` nếu chưa có. Nếu generic `ApiResponse` không nhận `Void` đúng cách trong codebase, dùng đúng kiểu wrapper hiện có (đọc ApiResponse). Đảm bảo 3 endpoint này nằm trong nhóm route public (không yêu cầu JWT) — kiểm tra security config nếu user-service có whitelist path; nếu có, thêm `/auth/verify-email`, `/auth/password/forgot`, `/auth/password/reset` vào whitelist (verify-email + forgot + reset KHÔNG cần đăng nhập).
  </action>
  <verify>
    <automated>cd sources/backend/user-service && mvn -q test -Dtest=AuthControllerIT 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `grep "verify-email" AuthController.java` xuất hiện với `@GetMapping`
    - `grep "password/forgot" AuthController.java` xuất hiện với `@PostMapping`
    - `grep "password/reset" AuthController.java` xuất hiện với `@PostMapping`
    - `grep "ApiResponse.of" AuthController.java` xuất hiện trong 3 endpoint mới
    - `AuthControllerIT` xanh (5 test gồm anti-enumeration + token replay)
  </acceptance_criteria>
  <done>3 endpoint verify-email/forgot/reset hoạt động, public, trả ApiResponse; forgot luôn 200.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /auth/* | Endpoint public — verify/forgot/reset không yêu cầu JWT |
| /auth/password/forgot | Input email từ kẻ tấn công tiềm năng (probe enumeration) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-10 | Information Disclosure | Email enumeration qua /password/forgot | mitigate | Endpoint luôn trả 200 + message trung tính bất kể email tồn tại hay không (D-08); không tiết lộ qua status/timing rõ rệt. |
| T-27-11 | Spoofing | verify-email link bị mở bởi người khác | accept | Lật email_verified là vô hại; reset token mới là rủi ro thực — đã mitigate ở T-27-04 (single-use). |
| T-27-12 | Denial of Service | Spam request /password/forgot | accept | Rate-limit reset request defer (Deferred Idea #4, visible-first priority). |
</threat_model>

<verification>
- `mvn -q compile` user-service exit 0.
- `AuthControllerIT` xanh — verify-email flip cờ, forgot luôn 200, reset đổi mật khẩu, token replay → 410.
</verification>

<success_criteria>
- register phát event xác minh, vẫn trả JWT ngay (D-07).
- 3 endpoint hoạt động; forgot chống enumeration.
</success_criteria>

<output>
After completion, create `.planning/phases/27-real-email-smtp/27-04-SUMMARY.md`
</output>
