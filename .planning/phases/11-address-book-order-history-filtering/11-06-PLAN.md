---
phase: 11-address-book-order-history-filtering
plan: "06"
type: execute
wave: 4
depends_on:
  - "11-04"
  - "11-05"
files_modified:
  - sources/frontend/src/app/profile/addresses/page.tsx
  - sources/frontend/src/app/profile/addresses/page.module.css
  - sources/frontend/src/app/profile/orders/page.tsx
  - sources/frontend/src/app/profile/orders/page.module.css
  - sources/frontend/src/app/profile/page.tsx
  - sources/frontend/src/app/checkout/page.tsx
autonomous: true
requirements:
  - ACCT-02
  - ACCT-05
  - ACCT-06

must_haves:
  truths:
    - "User vào /profile/addresses thấy danh sách addresses + nút Thêm địa chỉ mới (primary CTA)"
    - "Create/edit address mở Modal với AddressForm; submit gọi createAddress/updateAddress; toast success; list refresh"
    - "Xóa address: Modal confirm → deleteAddress → toast 'Đã xóa địa chỉ'; Đặt mặc định → setDefaultAddress → toast"
    - "Khi đủ 10 địa chỉ: nút Thêm bị disabled + tooltip 'Đã đạt giới hạn 10 địa chỉ'"
    - "User vào /profile/orders thấy OrderFilterBar (sticky) + order list; thay đổi filter → URL encode → fetch mới"
    - "Profile page: click tab orders → router.push('/profile/orders'); click tab addresses → router.push('/profile/addresses')"
    - "Checkout page: AddressPicker hiển thị phía trên form; chọn address → snap-fill 6 fields form; fetch fail → ẩn picker (silent)"
    - "tsc --noEmit + npm run build exit 0"
  artifacts:
    - path: "sources/frontend/src/app/profile/addresses/page.tsx"
      provides: "Address book CRUD page"
      contains: "AddressCard\|AddressForm\|createAddress\|deleteAddress"
    - path: "sources/frontend/src/app/profile/orders/page.tsx"
      provides: "Order history filter page"
      contains: "OrderFilterBar\|useSearchParams\|listMyOrders"
    - path: "sources/frontend/src/app/checkout/page.tsx"
      provides: "Checkout with AddressPicker"
      contains: "AddressPicker\|listAddresses"
    - path: "sources/frontend/src/app/profile/page.tsx"
      provides: "Profile hub with tab redirects"
      contains: "router.push.*profile/orders\|router.push.*profile/addresses"
  key_links:
    - from: "profile/orders/page.tsx — filter change"
      to: "URL state (?status=X&from=Y&to=Z&q=W)"
      via: "router.push với URLSearchParams"
      pattern: "router.push.*profile/orders"
    - from: "profile/orders/page.tsx — URL read"
      to: "listMyOrders(params)"
      via: "useSearchParams() read initial + re-fetch on URL change"
      pattern: "useSearchParams.*status\|searchParams.get"
    - from: "checkout/page.tsx — AddressPicker"
      to: "form fields (fullName, phone, street, ward, district, city)"
      via: "onSelect callback → setForm() merge"
      pattern: "onSelect.*setForm\|address.fullName"
---

<objective>
Tạo /profile/addresses page + /profile/orders standalone page + cập nhật profile hub redirects + tích hợp AddressPicker vào checkout.

Purpose: Đây là plan cuối của Phase 11 — wire tất cả components vào pages thực tế. Deliver toàn bộ 3 Success Criteria còn lại (ACCT-02, ACCT-05, ACCT-06 user-visible flows).

Output: 4 pages/files hoạt động end-to-end với API thật.
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-address-book-order-history-filtering/11-CONTEXT.md
@.planning/phases/11-address-book-order-history-filtering/11-UI-SPEC.md

<interfaces>
<!-- Components tạo ở Plan 11-05 -->
```typescript
// AddressCard props:
// address: SavedAddress; onEdit: (a) => void; onDelete: (a) => void;
// onSetDefault: (a) => void; settingDefault?: boolean

// AddressForm props:
// initialValues?: Partial<AddressBody>; onSubmit: (data: AddressBody) => Promise<void>;
// onCancel: () => void; loading?: boolean

// AddressPicker props:
// addresses: SavedAddress[]; loading?: boolean; onSelect: (a: SavedAddress) => void

// OrderFilterBar props:
// initialStatus?: string; initialFrom?: string; initialTo?: string; initialQ?: string;
// onChange: (filters: { status; from; to; q }) => void
```

<!-- Services từ Plan 11-04 -->
```typescript
// services/users.ts:
// listAddresses(): Promise<SavedAddress[]>
// createAddress(body: AddressBody): Promise<SavedAddress>
// updateAddress(id: string, body: AddressBody): Promise<SavedAddress>
// deleteAddress(id: string): Promise<void>
// setDefaultAddress(id: string): Promise<SavedAddress>

// services/orders.ts:
// listMyOrders(params?: ListOrdersParams): Promise<PaginatedResponse<Order>>
// ListOrdersParams: { page?, size?, sort?, status?, from?, to?, q? }
```

<!-- Checkout form fields hiện tại -->
```typescript
// const [form, setForm] = useState({
//   fullName: '', phone: '', email: '', street: '',
//   ward: '', district: '', city: '', note: '', paymentMethod: 'COD'
// });
// const update = (k: string, v: string) => setForm((p) => ({ ...p, [k]: v }));
// Snap-fill: setForm(prev => ({ ...prev, fullName: addr.fullName, phone: addr.phone,
//   street: addr.street, ward: addr.ward, district: addr.district, city: addr.city }))
```

<!-- profile/page.tsx: tabs hiện tại -->
```typescript
// type Tab = 'profile' | 'orders' | 'addresses';
// const [activeTab, setActiveTab] = useState<Tab>('profile');
// tab 'orders': setActiveTab('orders') → cần replace bằng router.push('/profile/orders') (D-09)
// tab 'addresses': setActiveTab('addresses') → cần replace bằng router.push('/profile/addresses') (D-08)
```

<!-- Order list từ profile/page.tsx (reuse pattern) -->
```typescript
// statusMap: Record<string, { label: string; variant: ... }> — reuse nguyên
// Order card: Link href={`/profile/orders/${order.id}`} — giữ nguyên
// formatPrice từ services/api
// RetrySection component
```

<!-- useAuth + useToast pattern -->
```typescript
// const { user } = useAuth();
// const { showToast } = useToast();
// showToast('Đã thêm địa chỉ', 'success');
// showToast('Đã xảy ra lỗi. Vui lòng thử lại.', 'error');
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: /profile/addresses page + /profile/orders standalone page</name>
  <read_first>
    - sources/frontend/src/app/profile/page.tsx (TOÀN BỘ — sidebar nav pattern, statusMap, order card JSX, CSS class names để reuse)
    - sources/frontend/src/app/profile/page.module.css (sidebar, main, section, orderCard classes pattern)
    - sources/frontend/src/app/profile/orders/[id]/page.tsx (xem layout pattern cho /profile/orders/ sibling — đừng đụng file này)
    - sources/frontend/src/components/ui/Modal/Modal.tsx (props: open, onClose, title, primaryAction, secondaryAction)
    - sources/frontend/src/components/ui/RetrySection/RetrySection.tsx (props: onRetry, loading)
  </read_first>
  <files>
    sources/frontend/src/app/profile/addresses/page.tsx,
    sources/frontend/src/app/profile/addresses/page.module.css,
    sources/frontend/src/app/profile/orders/page.tsx,
    sources/frontend/src/app/profile/orders/page.module.css
  </files>
  <action>
**1. /profile/addresses/page.tsx:**

'use client'; — cần hooks

State:
- `addresses: SavedAddress[]` + `loading: boolean` + `failed: boolean`
- `showCreateModal: boolean`
- `editTarget: SavedAddress | null` (null = closed, non-null = editing)
- `deleteTarget: SavedAddress | null` (null = closed, non-null = confirming delete)
- `settingDefaultId: string | null` (ID đang trong quá trình set-default)
- `submitting: boolean` (create/edit API call loading)

Load: `useCallback loadAddresses()` → listAddresses() → setAddresses; useEffect gọi loadAddresses() on mount.

Layout (per UI-SPEC):
- Page header: `<h1>Sổ địa chỉ</h1>` + Button "Thêm địa chỉ mới" (primary, disabled khi addresses.length >= 10, title="Đã đạt giới hạn 10 địa chỉ" khi disabled, onClick → setShowCreateModal(true))
- Sidebar (260px) + main layout (reuse profile/page.module.css sidebar pattern)
- Sidebar: profile nav (Thông tin → /profile/settings, Đơn hàng → /profile/orders, Địa chỉ → /profile/addresses với navActive, Cài đặt bảo mật → /profile/settings#security — dùng Link)
- Main: danh sách addresses với AddressCard map; loading skeleton; failed RetrySection; empty state khi length === 0
- Empty state: `<h3>Chưa có địa chỉ nào</h3><p>Thêm địa chỉ giao hàng để thanh toán nhanh hơn.</p><Button onClick...>Thêm địa chỉ đầu tiên</Button>`

Modals:
- Create modal: Modal open={showCreateModal} title="Thêm địa chỉ" → AddressForm onSubmit={async (data) => { await createAddress(data); showToast('Đã thêm địa chỉ', 'success'); setShowCreateModal(false); loadAddresses(); }} onCancel={() => setShowCreateModal(false)} loading={submitting}
- Edit modal: Modal open={!!editTarget} title="Sửa địa chỉ" → AddressForm initialValues={editTarget} onSubmit={async (data) => { await updateAddress(editTarget.id, data); showToast('Đã cập nhật địa chỉ', 'success'); setEditTarget(null); loadAddresses(); }} onCancel={() => setEditTarget(null)}
- Delete modal: Modal open={!!deleteTarget} title="Xác nhận xóa địa chỉ?" primaryAction={{ label: "Xóa địa chỉ", variant: "danger", onClick: handleDelete }} secondaryAction={{ label: "Hủy", onClick: () => setDeleteTarget(null) }}: handleDelete: deleteAddress(deleteTarget.id) → showToast('Đã xóa địa chỉ', 'success'); setDeleteTarget(null); loadAddresses()
- Error handling: catch ApiError ADDRESS_LIMIT_EXCEEDED → showToast('Đã đạt giới hạn 10 địa chỉ...', 'error'); else showToast('Đã xảy ra lỗi...', 'error')

Set-default: onSetDefault handler → setSettingDefaultId(id) → setDefaultAddress(id) → showToast('Đã đặt làm địa chỉ mặc định', 'success') → loadAddresses() → setSettingDefaultId(null)

**2. /profile/addresses/page.module.css:**
Reuse CSS vars từ profile/page.module.css. Thêm: `.pageHeader`, `.headerRow`, `.addressList`, `.emptyState`, `.emptyBody`, `.emptyActions`

**3. /profile/orders/page.tsx (standalone page — D-08):**

'use client'; + 'use router'

State: `orders: Order[]` + `ordersLoading: boolean` + `ordersFailed: boolean` + `totalElements: number`

URL state (D-11):
```typescript
const searchParams = useSearchParams();
const router = useRouter();

const statusParam = searchParams.get('status') ?? 'ALL';
const fromParam   = searchParams.get('from')   ?? '';
const toParam     = searchParams.get('to')     ?? '';
const qParam      = searchParams.get('q')      ?? '';
```

Fetch khi URL params change:
```typescript
const { user } = useAuth();
useEffect(() => {
  if (!user?.id) return;
  loadOrders();
}, [searchParams, user?.id]);

async function loadOrders() {
  setOrdersLoading(true);
  setOrdersFailed(false);
  try {
    const resp = await listMyOrders({
      page: 0, size: 20, sort: 'createdAt,desc',
      status: statusParam !== 'ALL' ? statusParam : undefined,
      from: fromParam || undefined,
      to: toParam || undefined,
      q: qParam || undefined,
    });
    setOrders(resp?.content ?? []);
    setTotalElements(resp?.totalElements ?? 0);
  } catch {
    setOrdersFailed(true);
  } finally {
    setOrdersLoading(false);
  }
}
```

OrderFilterBar onChange callback (useCallback để stable ref):
```typescript
const handleFilterChange = useCallback((filters: { status: string; from: string; to: string; q: string }) => {
  const qs = new URLSearchParams();
  if (filters.status && filters.status !== 'ALL') qs.set('status', filters.status);
  if (filters.from) qs.set('from', filters.from);
  if (filters.to)   qs.set('to', filters.to);
  if (filters.q)    qs.set('q', filters.q);
  router.push(`/profile/orders${qs.toString() ? '?' + qs.toString() : ''}`);
}, [router]);
```

Layout (per UI-SPEC):
- `<h1>Lịch sử đơn hàng</h1>`
- OrderFilterBar (sticky sau header) với initialStatus/From/To/Q từ URL params, onChange={handleFilterChange}
- Order list (reuse order card pattern từ profile/page.tsx — Link href, Badge status, formatPrice, image thumbnails)
- Empty state: "Không tìm thấy đơn hàng nào" + "Thử thay đổi bộ lọc hoặc xóa bộ lọc để xem tất cả đơn hàng."
- Loading: skeleton div
- Failed: RetrySection

**4. /profile/orders/page.module.css:**
`.page`, `.pageHeader`, `.stickyFilter`, `.orderList`, `.orderCard`, `.orderHeader`, `.orderCode`, `.orderItems`, `.orderItemThumb`, `.orderItemCount`, `.orderFooter`, `.orderDate`, `.orderTotal`, `.emptyState`

Note: `/profile/orders/[id]/page.tsx` KHÔNG được đụng vào — chỉ tạo `/profile/orders/page.tsx`.
  </action>
  <verify>
    <automated>
      grep -n "AddressCard\|createAddress\|setDefaultAddress" sources/frontend/src/app/profile/addresses/page.tsx
      grep -n "OrderFilterBar\|useSearchParams\|handleFilterChange" sources/frontend/src/app/profile/orders/page.tsx
      grep -n "router.push.*profile/orders\|searchParams.get" sources/frontend/src/app/profile/orders/page.tsx
      ls sources/frontend/src/app/profile/addresses/ sources/frontend/src/app/profile/orders/
    </automated>
  </verify>
  <acceptance_criteria>
    - /profile/addresses/page.tsx: 'use client', import AddressCard + AddressForm + Modal + RetrySection, 3 modal states (create/edit/delete), toast calls với exact copy strings từ UI-SPEC
    - Khi addresses.length >= 10: Button "Thêm địa chỉ mới" có disabled=true + title="Đã đạt giới hạn 10 địa chỉ"
    - /profile/orders/page.tsx: 'use client', useSearchParams để read params, useEffect dep on searchParams, handleFilterChange với useCallback, router.push URL encode
    - Orders page KHÔNG phải /profile/orders/[id]/page.tsx (dynamic route) — là /profile/orders/page.tsx (index)
    - CSS files tồn tại
  </acceptance_criteria>
  <done>/profile/addresses CRUD page + /profile/orders standalone filter page hoàn chỉnh với URL state.</done>
</task>

<task type="auto">
  <name>Task 2: Cập nhật profile/page.tsx tab redirects + checkout/page.tsx AddressPicker integration</name>
  <read_first>
    - sources/frontend/src/app/profile/page.tsx (TOÀN BỘ — xem tab onClick handlers cho 'orders' và 'addresses', useRouter import nếu chưa có)
    - sources/frontend/src/app/checkout/page.tsx (TOÀN BỘ — xem form state, update() function, form layout "Thông tin giao hàng" section, imports)
    - sources/frontend/src/components/ui/AddressPicker/AddressPicker.tsx (vừa tạo Plan 11-05 — xem onSelect callback type)
  </read_first>
  <files>
    sources/frontend/src/app/profile/page.tsx,
    sources/frontend/src/app/checkout/page.tsx
  </files>
  <action>
**1. Cập nhật profile/page.tsx — tab redirects (D-09):**

Thêm import useRouter nếu chưa có:
```typescript
import { useRouter } from 'next/navigation';
// Thêm vào trong component:
const router = useRouter();
```

Thay thế onClick cho tab 'orders':
```typescript
// TRƯỚC: onClick={() => setActiveTab('orders')}
// SAU:
onClick={() => router.push('/profile/orders')}
```

Thay thế onClick cho tab 'addresses':
```typescript
// TRƯỚC: onClick={() => setActiveTab('addresses')}
// SAU:
onClick={() => router.push('/profile/addresses')}
```

Giữ nguyên phần render `{activeTab === 'orders' && ...}` và `{activeTab === 'addresses' && ...}` — chúng sẽ không còn được trigger nữa vì tab đã redirect, nhưng giữ lại không làm hỏng gì và tránh rework lớn. Nếu muốn clean: có thể xóa các block render đó sau khi verify redirects hoạt động.

**2. Cập nhật checkout/page.tsx — thêm AddressPicker (D-01, D-02, D-03, ACCT-06):**

Thêm imports:
```typescript
import AddressPicker from '@/components/ui/AddressPicker/AddressPicker';
import { listAddresses } from '@/services/users';
import type { SavedAddress } from '@/types';
```

Thêm state:
```typescript
const [savedAddresses, setSavedAddresses] = useState<SavedAddress[]>([]);
const [addressesLoading, setAddressesLoading] = useState(false);
// pickerVisible: hiển thị AddressPicker chỉ khi fetch thành công (silent fail per D-Discretion)
const [pickerVisible, setPickerVisible] = useState(false);
```

Thêm fetch trong useEffect (sau cart listener useEffect, hoặc trong useEffect mount):
```typescript
// Fetch saved addresses khi user logged-in
useEffect(() => {
  if (!user) return; // không fetch nếu chưa login
  let alive = true;
  setAddressesLoading(true);
  listAddresses()
    .then((addresses) => {
      if (!alive) return;
      setSavedAddresses(addresses);
      setPickerVisible(true); // chỉ show khi fetch thành công
    })
    .catch(() => {
      // Silent fail per D-Discretion (Claude's Discretion: ẩn picker, không toast)
      if (!alive) return;
      setPickerVisible(false);
    })
    .finally(() => {
      if (alive) setAddressesLoading(false);
    });
  return () => { alive = false; };
}, [user]);
```

Thêm onSelect handler:
```typescript
const handleAddressSelect = (address: SavedAddress) => {
  setForm((prev) => ({
    ...prev,
    fullName: address.fullName,
    phone: address.phone,
    street: address.street,
    ward: address.ward,
    district: address.district,
    city: address.city,
  }));
};
```

Cập nhật JSX trong form column, trong section "Thông tin giao hàng", TRƯỚC `<div className={styles.formGrid}>`:
```tsx
{/* D-01, D-02, D-03: AddressPicker snap-fill — chỉ show khi fetch thành công (silent fail) */}
{pickerVisible && (
  <>
    <AddressPicker
      addresses={savedAddresses}
      loading={addressesLoading}
      onSelect={handleAddressSelect}
    />
    <div className={styles.pickerDivider}>— hoặc điền thủ công —</div>
  </>
)}
```

Thêm `.pickerDivider` vào checkout/page.module.css (text-center, color --on-surface-variant, margin --space-2 0, font-size --text-body-md).
  </action>
  <verify>
    <automated>
      grep -n "router.push.*profile/orders\|router.push.*profile/addresses" sources/frontend/src/app/profile/page.tsx
      grep -n "AddressPicker\|listAddresses\|handleAddressSelect" sources/frontend/src/app/checkout/page.tsx
      grep -n "pickerVisible\|silent\|alive" sources/frontend/src/app/checkout/page.tsx
      grep -n "address.fullName\|address.street" sources/frontend/src/app/checkout/page.tsx
    </automated>
  </verify>
  <acceptance_criteria>
    - profile/page.tsx: tab 'orders' onClick = `router.push('/profile/orders')`, tab 'addresses' onClick = `router.push('/profile/addresses')`
    - checkout/page.tsx: import AddressPicker + listAddresses + SavedAddress
    - checkout/page.tsx: useEffect fetch listAddresses khi user != null, alive flag cleanup, catch → setPickerVisible(false) (silent fail)
    - handleAddressSelect: setForm merge với 6 fields: fullName, phone, street, ward, district, city
    - AddressPicker render chỉ khi pickerVisible=true
    - Divider "— hoặc điền thủ công —" sau AddressPicker
  </acceptance_criteria>
  <done>Profile hub tab redirects đến dedicated pages. Checkout có AddressPicker snap-fill với silent-fail.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Build verify + visual spot-check</name>
  <what-built>
    Toàn bộ Phase 11 frontend: /profile/addresses CRUD, /profile/orders filter, profile tab redirects, checkout AddressPicker.
  </what-built>
  <how-to-verify>
```bash
# Bước 1: TypeScript check
cd sources/frontend && npx tsc --noEmit

# Bước 2: Build check
cd sources/frontend && npm run build

# Nếu cả 2 pass — visual check (với docker stack running):
# Bước 3: Visit http://localhost:3000/profile → click tab "Đơn hàng" → redirect /profile/orders
# Bước 4: Visit http://localhost:3000/profile → click tab "Địa chỉ" → redirect /profile/addresses
# Bước 5: /profile/addresses → list empty state "Chưa có địa chỉ nào"
# Bước 6: /profile/addresses → click "Thêm địa chỉ mới" → modal với form 6 fields
# Bước 7: /profile/orders → filter bar hiển thị (status dropdown + date range + keyword)
# Bước 8: /checkout → AddressPicker button "Địa chỉ đã lưu ▼" hiển thị phía trên form (nếu login)
```
  </how-to-verify>
  <resume-signal>Gõ "approved" nếu tsc + build pass và visual flows OK. Gõ "failed: [mô tả]" nếu có lỗi.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| /profile/addresses → API | User-authenticated CRUD operations |
| /checkout → addresses API | Fetch saved addresses khi user logged-in |
| URL filter params → API | User-controlled filter state encode vào URL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-06-01 | Information Disclosure | /profile/addresses — list addresses của user khác | mitigate | listAddresses() gọi /api/users/me/addresses — Bearer token xác định owner ở backend. FE không có userId param. |
| T-11-06-02 | Tampering | Checkout snap-fill — override form với malicious address data | accept | Snap-fill chỉ fill form; user review và edit trước khi submit. Data validated bởi backend CreateOrderCommand. |
| T-11-06-03 | Spoofing | URL filter params giả mạo (status=ADMIN, q=malicious) | accept | Status enum validated ở backend (không match → JPQL WHERE a.status = :status trả empty). q dùng ILIKE bound param. |
| T-11-06-04 | Elevation of Privilege | /profile/* pages — chưa login access | mitigate | middleware.ts matcher đã bảo vệ /profile/* routes (Phase 9 AUTH-06). Middleware redirect /login nếu không có token. |
</threat_model>

<verification>
```bash
# TypeScript compile
cd sources/frontend && npx tsc --noEmit

# Build
cd sources/frontend && npm run build

# Verify key files
ls sources/frontend/src/app/profile/addresses/page.tsx
ls sources/frontend/src/app/profile/orders/page.tsx

# Verify tab redirects
grep -n "router.push.*profile/orders" sources/frontend/src/app/profile/page.tsx
grep -n "router.push.*profile/addresses" sources/frontend/src/app/profile/page.tsx

# Verify AddressPicker integration
grep -n "AddressPicker\|pickerVisible" sources/frontend/src/app/checkout/page.tsx
```
</verification>

<success_criteria>
- tsc --noEmit exit 0 (no TypeScript errors)
- npm run build thành công (tất cả pages compile)
- /profile/addresses/page.tsx tồn tại với AddressCard + Modal CRUD flows
- /profile/orders/page.tsx tồn tại với OrderFilterBar + useSearchParams URL state
- profile/page.tsx: 2 tab onClick gọi router.push (KHÔNG setActiveTab)
- checkout/page.tsx: AddressPicker render với silent-fail (alive flag cleanup)
- Phase 11 Success Criteria SC-1..SC-5 phủ được qua 6 plans này
</success_criteria>

<output>
Sau khi hoàn thành, tạo `.planning/phases/11-address-book-order-history-filtering/11-06-SUMMARY.md`
</output>
