---
phase: 11-address-book-order-history-filtering
plan: "03"
type: execute
wave: 2
depends_on:
  - "11-01"
  - "11-02"
files_modified:
  - sources/backend/api-gateway/src/main/resources/application.yml
autonomous: false
requirements:
  - ACCT-05
  - ACCT-06
  - ACCT-02

must_haves:
  truths:
    - "Gateway forward /api/users/me/addresses/** → user-service:8080 /users/me/addresses/**"
    - "V4 Flyway migration chạy thành công khi user-service khởi động"
    - "user-service health endpoint trả UP sau khi Flyway apply V4"
    - "Bảng user_svc.addresses tồn tại trong database"
  artifacts:
    - path: "sources/backend/api-gateway/src/main/resources/application.yml"
      provides: "Gateway route user-service-me-addresses TRƯỚC user-service-me"
      contains: "user-service-me-addresses"
  key_links:
    - from: "frontend /api/users/me/addresses/**"
      to: "user-service /users/me/addresses/**"
      via: "api-gateway RewritePath"
      pattern: "user-service-me-addresses"
---

<objective>
[BLOCKING] Thêm gateway route cho addresses + restart user-service để Flyway V4 migration apply.

Purpose: Hai backend plans (11-01, 11-02) tạo code nhưng chưa chạy được. Plan này: (1) thêm gateway route addresses, (2) restart user-service để Flyway V4 apply tự động, (3) verify migration thành công trước khi frontend plans bắt đầu.

Output: Gateway route hoạt động + bảng user_svc.addresses tồn tại trong DB.
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-address-book-order-history-filtering/11-CONTEXT.md

<interfaces>
<!-- Gateway route pattern hiện tại từ application.yml -->
```yaml
# Pattern: user-service-me ĐỨNG TRƯỚC user-service-base (xem comment hiện tại)
# Phase 11: user-service-me-addresses PHẢI đứng TRƯỚC user-service-me
# Vì /api/users/me/addresses/** ⊂ /api/users/me/**
# Nếu user-service-me match trước → RewritePath strip sang /users/me → sai path

# Route hiện tại user-service-me:
- id: user-service-me-base
  uri: http://user-service:8080
  predicates:
    - Path=/api/users/me
  filters:
    - RewritePath=/api/users/me, /users/me
- id: user-service-me
  uri: http://user-service:8080
  predicates:
    - Path=/api/users/me/**
  filters:
    - RewritePath=/api/users/me/(?<seg>.*), /users/me/${seg}
# → /api/users/me/addresses/123 → /users/me/addresses/123 ✓ (đã đúng!)
# Không cần route riêng nếu user-service-me/** đã capture đúng
# Verify lại: /api/users/me/addresses/123 với seg=addresses/123 → /users/me/addresses/123
# AddressController @RequestMapping("/users/me/addresses") → match ✓
```

IMPORTANT: Sau khi đọc application.yml thực tế, executor cần verify xem route user-service-me/** đã đủ để forward /api/users/me/addresses/** chưa. Nếu đã đủ, chỉ cần thêm comment. Nếu chưa đủ, thêm route riêng user-service-me-addresses TRƯỚC user-service-me.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify + update gateway route cho /api/users/me/addresses/**</name>
  <read_first>
    - sources/backend/api-gateway/src/main/resources/application.yml (TOÀN BỘ — đặc biệt user-service-me route group, thứ tự các routes)
  </read_first>
  <files>
    sources/backend/api-gateway/src/main/resources/application.yml
  </files>
  <action>
**Verify logic:**

Route hiện tại `user-service-me`:
```yaml
- id: user-service-me
  predicates:
    - Path=/api/users/me/**
  filters:
    - RewritePath=/api/users/me/(?<seg>.*), /users/me/${seg}
```

Request: `GET /api/users/me/addresses/123`
- seg = `addresses/123`
- Forward: `/users/me/addresses/123` ✓

AddressController: `@RequestMapping("/users/me/addresses")` + `@GetMapping("/{id}")` → match `/users/me/addresses/123` ✓

**Kết luận:** Route hiện tại đã đủ. KHÔNG cần route riêng `user-service-me-addresses`.

**Action:** Chỉ cần thêm comment vào application.yml để document rằng addresses endpoints cũng được forward qua route user-service-me:

```yaml
# user-service me: /api/users/me/** → /users/me/** (PHẢI đứng TRƯỚC user-service-base
# để /api/users/me/password KHÔNG match catch-all /api/users/{id} với id="me/password".
# Phase 10 sẽ extend với GET/PATCH /api/users/me cho profile editing.
# Phase 11: /api/users/me/addresses/** → /users/me/addresses/** qua route này (seg capture đúng)
```

Update comment trên route `user-service-me-base` và `user-service-me` để phản ánh Phase 11 addresses routing.
  </action>
  <verify>
    <automated>
      grep -n "user-service-me" sources/backend/api-gateway/src/main/resources/application.yml
    </automated>
  </verify>
  <acceptance_criteria>
    - application.yml có route `user-service-me` với Path=/api/users/me/** (đã tồn tại hoặc được verify)
    - RewritePath pattern có `(?&lt;seg&gt;.*)` capture group → `/users/me/${seg}`
    - Comment document Phase 11 addresses routing
  </acceptance_criteria>
  <done>Gateway application.yml verified — route user-service-me/** đã đủ để forward addresses endpoints. Comment updated.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: [BLOCKING] Rebuild + restart user-service → Flyway V4 apply + verify</name>
  <read_first>
    - sources/backend/user-service/src/main/resources/db/migration/V4__create_addresses.sql (verify file tồn tại trước khi restart)
  </read_first>
  <what-built>
    Backend user-service với V4__create_addresses.sql đã được tạo ở Plan 11-01. Cần rebuild Docker image và restart để Flyway tự động apply migration V4 khi service boot.
  </what-built>
  <how-to-verify>
```bash
# Bước 1: Rebuild user-service image (Flyway migration chạy khi service boot)
docker compose build user-service

# Bước 2: Restart user-service
docker compose restart user-service

# Bước 3: Chờ service khởi động (30-60 giây), check logs
docker compose logs user-service --tail=50

# Bước 4: Verify health endpoint
docker compose exec user-service curl -s http://localhost:8080/actuator/health

# Kết quả mong đợi: {"status":"UP"} — nếu Flyway fail, service sẽ không UP

# Bước 5: Verify bảng tồn tại trong DB (nếu có access psql)
# docker compose exec postgres psql -U postgres -c "\dt user_svc.addresses"
# Hoặc check Flyway history:
# docker compose exec postgres psql -U postgres -c "SELECT version, description, success FROM flyway_schema_history WHERE installed_rank > 3;"

# Mong đợi: V4 với description "create addresses" và success = true
```
  </how-to-verify>
  <resume-signal>Gõ "approved" nếu user-service health = UP và không có Flyway error trong logs. Gõ "failed: [mô tả lỗi]" nếu có vấn đề.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gateway → user-service | Internal Docker network routing |
| Flyway → Postgres | Schema migration quyền |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03-01 | Tampering | Gateway route order — addresses route hijack | mitigate | Route user-service-me/** đã bao phủ addresses/** đúng via seg capture group. Verify trong task 1. |
| T-11-03-02 | Denial of Service | Flyway V4 fail → user-service không boot | accept | Nếu fail: human checkpoint (Task 2) sẽ detect. Fix SQL và restart. Low risk cho demo env. |
</threat_model>

<verification>
```bash
# Verify gateway route
grep -A3 "user-service-me$" sources/backend/api-gateway/src/main/resources/application.yml

# Verify user-service health sau restart
docker compose exec user-service curl -s http://localhost:8080/actuator/health | grep -o '"status":"UP"'

# Verify Flyway migration V4 success (nếu có DB access)
docker compose logs user-service 2>&1 | grep -i "flyway\|V4\|migration"
```
</verification>

<success_criteria>
- application.yml có route user-service-me/** forward đúng sang /users/me/${seg}
- user-service restart thành công (health = UP)
- Flyway logs không có error cho V4__create_addresses.sql
- Bảng user_svc.addresses tồn tại (verify qua logs hoặc psql)
</success_criteria>

<output>
Sau khi hoàn thành, tạo `.planning/phases/11-address-book-order-history-filtering/11-03-SUMMARY.md`
</output>
