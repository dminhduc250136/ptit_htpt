---
phase: 11-address-book-order-history-filtering
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/repository/OrderRepository.java
  - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/service/OrderCrudService.java
  - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/web/OrderController.java
autonomous: true
requirements:
  - ACCT-02

must_haves:
  truths:
    - "GET /orders?userId=X&status=DELIVERED&from=2026-04-01&to=2026-04-30&q=ORD trả đúng orders lọc theo status + date range + keyword"
    - "Date range filter: from=2026-04-30 bao gồm đơn hàng lúc 23:59 GMT+7 ngày 30/4 (full day UTC+7)"
    - "Keyword q tìm kiếm trên order.id ILIKE — không join order items"
    - "Filter params stack thêm vào pagination (page/size/sort giữ nguyên)"
    - "Khi không có filter params, behavior giống cũ (trả all orders)"
  artifacts:
    - path: "sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/repository/OrderRepository.java"
      provides: "JPA query methods cho filter"
      contains: "findByUserIdAndFilters"
    - path: "sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/service/OrderCrudService.java"
      provides: "listMyOrders() với filter logic"
      contains: "ListMyOrdersQuery"
    - path: "sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/web/OrderController.java"
      provides: "GET /orders với filter @RequestParam"
      contains: "status.*from.*to.*q"
  key_links:
    - from: "OrderController.listOrders()"
      to: "OrderCrudService.listMyOrders()"
      via: "ListMyOrdersQuery record"
      pattern: "ListMyOrdersQuery"
    - from: "OrderCrudService"
      to: "OrderRepository"
      via: "JPA @Query với Instant conversion UTC+7"
      pattern: "createdAt.*BETWEEN"
---

<objective>
Extend order-service backend để hỗ trợ filter query params (status, from, to, q) cho endpoint GET /orders.

Purpose: ACCT-02 yêu cầu server-side filtering (D-12) với timezone correct (D-14). Frontend /profile/orders page gọi API này với filter params encode trong URL.

Output: GET /orders nhận thêm status/from/to/q params, filter server-side, trả paginated results đúng timezone.
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/11-address-book-order-history-filtering/11-CONTEXT.md

<interfaces>
<!-- OrderRepository hiện tại -->
```java
// sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/repository/OrderRepository.java
public interface OrderRepository extends JpaRepository<OrderEntity, String> {
    List<OrderEntity> findByUserId(String userId);
    long countByStatus(String status);
}
```

<!-- OrderCrudService.listOrders() hiện tại — cần extend -->
```java
// Hiện tại: listOrders(int page, int size, String sort, boolean includeDeleted)
// Fetch tất cả orders rồi filter/sort in-memory
// Phase 11: Thêm ListMyOrdersQuery record để filter theo userId+status+date+q

// paginate() helper đã có:
private <T> Map<String, Object> paginate(List<T> source, int page, int size) { ... }
// Trả: content, totalElements, totalPages, currentPage, pageSize, isFirst, isLast
```

<!-- OrderEntity fields liên quan -->
```java
// OrderEntity: id (String UUID), userId (String), status (String), createdAt (Instant)
// @Column(name = "created_at", nullable = false, updatable = false)
// private Instant createdAt;
// Getters: id(), userId(), status(), createdAt()
```

<!-- OrderController hiện tại -->
```java
@GetMapping
public ApiResponse<Map<String, Object>> listOrders(
    @RequestParam(defaultValue = "0") int page,
    @RequestParam(defaultValue = "20") int size,
    @RequestParam(defaultValue = "updatedAt,desc") String sort
) { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Thêm ListMyOrdersQuery + filter logic vào OrderCrudService + extend OrderRepository</name>
  <read_first>
    - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/service/OrderCrudService.java (TOÀN BỘ — đặc biệt listOrders(), paginate(), orderComparator())
    - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/repository/OrderRepository.java (methods hiện tại)
    - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/domain/OrderEntity.java (fields: userId, status, createdAt, id)
  </read_first>
  <files>
    sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/repository/OrderRepository.java,
    sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/service/OrderCrudService.java
  </files>
  <action>
**1. Extend OrderRepository.java:**

Thêm JPA query method để filter by userId + optional status + date range. Do Instant comparison cần BETWEEN, dùng @Query JPQL:

```java
import org.springframework.data.jpa.repository.Query;
import org.springframework.data.repository.query.Param;
import java.time.Instant;

// Thêm vào interface OrderRepository:

/**
 * Phase 11 / ACCT-02 (D-12, D-13, D-14): filter orders by userId + optional params.
 * status=null → bỏ qua filter status. from=null / to=null → bỏ qua filter date.
 * q tìm kiếm trên id (LOWER LIKE) — D-13: order ID only, không join items.
 */
@Query("SELECT o FROM OrderEntity o WHERE o.userId = :userId " +
       "AND (:status IS NULL OR o.status = :status) " +
       "AND (:from IS NULL OR o.createdAt >= :from) " +
       "AND (:to IS NULL OR o.createdAt <= :to) " +
       "AND (:q IS NULL OR LOWER(o.id) LIKE LOWER(CONCAT('%', :q, '%'))) " +
       "ORDER BY o.createdAt DESC")
List<OrderEntity> findByUserIdWithFilters(
    @Param("userId") String userId,
    @Param("status") String status,
    @Param("from") Instant from,
    @Param("to") Instant to,
    @Param("q") String q
);
```

**2. Thêm ListMyOrdersQuery record + listMyOrders() method vào OrderCrudService.java:**

Thêm inner record (cuối class, cạnh các records hiện tại như CreateOrderCommand):
```java
/**
 * Phase 11 / ACCT-02 (D-10, D-11, D-12, D-13, D-14, D-15).
 * Filter params cho GET /orders từ user đang đăng nhập.
 * status=null → ALL. from/to = YYYY-MM-DD string → convert sang Instant UTC+7 (D-14).
 * q = order ID keyword → ILIKE (D-13).
 */
public record ListMyOrdersQuery(
    String userId,    // required — từ X-User-Id header (Phase 11 giữ nguyên pattern cũ)
    String status,    // nullable — "PENDING"/"CONFIRMED"/"SHIPPING"/"DELIVERED"/"CANCELLED"
    String from,      // nullable — "YYYY-MM-DD"
    String to,        // nullable — "YYYY-MM-DD"
    String q,         // nullable — keyword search on order.id
    int page,
    int size,
    String sort
) {}
```

Thêm method listMyOrders():
```java
/**
 * D-12: Filter server-side (không filter in-memory).
 * D-14: Date string "2026-04-30" → from = 2026-04-30T00:00:00+07:00, to = 2026-04-30T23:59:59+07:00
 * Timezone offset: ZoneOffset.of("+07:00") — KHÔNG trust client timezone.
 */
public Map<String, Object> listMyOrders(ListMyOrdersQuery query) {
    // Parse status: null hoặc "ALL" → pass null vào repository (bỏ qua filter)
    String statusFilter = (query.status() == null || "ALL".equalsIgnoreCase(query.status()))
        ? null : query.status();

    // D-14: parse date string "YYYY-MM-DD" → Instant UTC+7
    // from → 2026-04-01T00:00:00+07:00 (start of day Saigon)
    // to   → 2026-04-30T23:59:59+07:00 (end of day Saigon — SC-5: không miss đơn 23:59 GMT+7)
    ZoneOffset saigon = ZoneOffset.of("+07:00");
    Instant fromInstant = null;
    Instant toInstant = null;
    if (query.from() != null && !query.from().isBlank()) {
        fromInstant = LocalDate.parse(query.from()).atStartOfDay().toInstant(saigon);
    }
    if (query.to() != null && !query.to().isBlank()) {
        // end of day: 23:59:59 (+07:00)
        toInstant = LocalDate.parse(query.to()).atTime(23, 59, 59).toInstant(saigon);
    }

    // D-13: q = null/blank → pass null (bỏ qua filter)
    String qFilter = (query.q() == null || query.q().isBlank()) ? null : query.q().trim();

    List<OrderEntity> filtered = orderRepository.findByUserIdWithFilters(
        query.userId(), statusFilter, fromInstant, toInstant, qFilter
    );

    // Map sang DTO trước khi paginate
    List<OrderDto> dtos = filtered.stream().map(OrderMapper::toDto).toList();
    return paginate(dtos, query.page(), query.size());
}
```

Thêm imports cần thiết lên đầu file:
```java
import java.time.LocalDate;
import java.time.ZoneOffset;
```
  </action>
  <verify>
    <automated>
      grep -n "findByUserIdWithFilters" sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/repository/OrderRepository.java
      grep -n "ListMyOrdersQuery" sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/service/OrderCrudService.java
      grep -n "ZoneOffset\|LocalDate" sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/service/OrderCrudService.java
      grep -n "listMyOrders" sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/service/OrderCrudService.java
    </automated>
  </verify>
  <acceptance_criteria>
    - OrderRepository có method `findByUserIdWithFilters` với @Query JPQL + 5 @Param
    - OrderCrudService có record `ListMyOrdersQuery` với 8 fields
    - listMyOrders() có logic: "ALL" → null statusFilter, date string → Instant UTC+7 (ZoneOffset +07:00)
    - to date → 23:59:59 end-of-day (KHÔNG 00:00:00) — đảm bảo SC-5 timezone correctness
    - qFilter = null khi q blank (không pass empty string vào LIKE query)
  </acceptance_criteria>
  <done>OrderRepository extend với filter query method, OrderCrudService có listMyOrders(ListMyOrdersQuery) với đúng UTC+7 timezone logic.</done>
</task>

<task type="auto">
  <name>Task 2: Extend OrderController GET /orders nhận filter params + wire listMyOrders</name>
  <read_first>
    - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/web/OrderController.java (TOÀN BỘ — listOrders endpoint hiện tại, X-User-Id header pattern)
    - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/service/OrderCrudService.java (ListMyOrdersQuery signature vừa thêm)
  </read_first>
  <files>
    sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/web/OrderController.java
  </files>
  <action>
Modify `listOrders()` trong OrderController để nhận thêm filter params và route sang `listMyOrders()` khi có X-User-Id header.

**Strategy:** Giữ nguyên `listOrders()` cho admin/generic case (không có userId). Thêm `listMyOrders()` endpoint riêng tại GET /orders/me hoặc MODIFY `listOrders()` để detect X-User-Id header và route sang đúng service method.

**Theo pattern hiện tại:** `createOrder()` đã dùng `@RequestHeader(value = "X-User-Id", required = false) String userId`. Áp dụng cùng pattern cho listOrders.

Modify `listOrders()` method:
```java
@GetMapping
public ApiResponse<Map<String, Object>> listOrders(
    @RequestParam(defaultValue = "0") int page,
    @RequestParam(defaultValue = "20") int size,
    @RequestParam(defaultValue = "updatedAt,desc") String sort,
    // Phase 11 / ACCT-02 (D-10, D-11, D-12): filter params
    @RequestParam(required = false) String status,
    @RequestParam(required = false) String from,
    @RequestParam(required = false) String to,
    @RequestParam(required = false) String q,
    @RequestHeader(value = "X-User-Id", required = false) String userId
) {
    // Nếu có userId → dùng listMyOrders với filter (ACCT-02 path)
    // Nếu không có userId → fallback listOrders cũ (backward compat cho admin/test)
    if (userId != null && !userId.isBlank()) {
        OrderCrudService.ListMyOrdersQuery query = new OrderCrudService.ListMyOrdersQuery(
            userId, status, from, to, q, page, size, sort
        );
        return ApiResponse.of(200, "Orders listed", orderCrudService.listMyOrders(query));
    }
    return ApiResponse.of(200, "Orders listed", orderCrudService.listOrders(page, size, sort, false));
}
```

Không thêm import mới nếu ListMyOrdersQuery đã accessible qua OrderCrudService.ListMyOrdersQuery (inner record).
  </action>
  <verify>
    <automated>
      grep -n "status.*from.*to.*q" sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/web/OrderController.java
      grep -n "listMyOrders" sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/web/OrderController.java
      grep -n "X-User-Id" sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/web/OrderController.java
    </automated>
  </verify>
  <acceptance_criteria>
    - listOrders() nhận 4 filter @RequestParam mới: status, from, to, q (tất cả required=false)
    - Method nhận @RequestHeader X-User-Id (required=false) — consistent với createOrder pattern
    - Khi userId != null → gọi listMyOrders(ListMyOrdersQuery) thay vì listOrders()
    - Khi userId = null → backward compat: gọi listOrders() cũ (không break admin endpoints)
  </acceptance_criteria>
  <done>OrderController.listOrders() extend đầy đủ với filter params, routing đúng sang listMyOrders() khi có userId.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → OrderController | Untrusted query params (status, from, to, q) + X-User-Id header |
| OrderCrudService → DB | JPQL @Query với @Param — không raw string concat |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-02-01 | Information Disclosure | listOrders — user xem orders của user khác | mitigate | findByUserIdWithFilters filter theo userId từ X-User-Id header — chỉ trả orders của đúng user. Admin path (không có userId) giữ nguyên. |
| T-11-02-02 | Tampering | date filter — client inject arbitrary timezone offset | mitigate | D-14: backend luôn dùng ZoneOffset.of("+07:00") — không trust client timezone string. Chỉ nhận date string YYYY-MM-DD. |
| T-11-02-03 | Injection | q param — SQL injection qua keyword search | mitigate | JPQL @Param binding — không raw string concat vào query. LIKE pattern: CONCAT('%', :q, '%') via JPA. |
| T-11-02-04 | Denial of Service | status/from/to với malformed input gây exception | accept | ParseException từ LocalDate.parse() → 500 (acceptable — not PII risk, low-value target). Phase 11 demo scope. |
</threat_model>

<verification>
```bash
# Verify OrderRepository filter method
grep -n "findByUserIdWithFilters" sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/repository/OrderRepository.java

# Verify ListMyOrdersQuery record trong service
grep -n "record ListMyOrdersQuery" sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/service/OrderCrudService.java

# Verify timezone handling
grep -n "ZoneOffset\|23.*59.*59\|atStartOfDay" sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/service/OrderCrudService.java

# Verify controller extend
grep -n "@RequestParam.*from\|@RequestParam.*status" sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/web/OrderController.java
```
</verification>

<success_criteria>
- OrderRepository.java có @Query method findByUserIdWithFilters với 5 params (userId, status, from, to, q)
- OrderCrudService.java có record ListMyOrdersQuery + method listMyOrders(ListMyOrdersQuery)
- Date parsing: from → start-of-day UTC+7, to → 23:59:59 UTC+7 (SC-5 timezone correctness)
- OrderController.listOrders() nhận 4 filter params mới + X-User-Id header, route đúng
- Khi không có userId → behavior cũ không thay đổi (backward compat)
</success_criteria>

<output>
Sau khi hoàn thành, tạo `.planning/phases/11-address-book-order-history-filtering/11-02-SUMMARY.md`
</output>
