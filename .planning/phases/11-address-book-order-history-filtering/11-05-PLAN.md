---
phase: 11-address-book-order-history-filtering
plan: "05"
type: execute
wave: 3
depends_on:
  - "11-03"
files_modified:
  - sources/frontend/src/components/ui/AddressCard/AddressCard.tsx
  - sources/frontend/src/components/ui/AddressCard/AddressCard.module.css
  - sources/frontend/src/components/ui/AddressForm/AddressForm.tsx
  - sources/frontend/src/components/ui/AddressForm/AddressForm.module.css
  - sources/frontend/src/components/ui/AddressPicker/AddressPicker.tsx
  - sources/frontend/src/components/ui/AddressPicker/AddressPicker.module.css
  - sources/frontend/src/components/ui/OrderFilterBar/OrderFilterBar.tsx
  - sources/frontend/src/components/ui/OrderFilterBar/OrderFilterBar.module.css
autonomous: true
requirements:
  - ACCT-02
  - ACCT-05
  - ACCT-06

must_haves:
  truths:
    - "AddressCard render 1 địa chỉ với fullName, phone, street+ward+district+city, badge Mặc định (khi isDefault=true), 3 action buttons (Đặt làm mặc định / Sửa / Xóa)"
    - "AddressForm là rhf+zod form 6 fields (fullName, phone, street, ward, district, city) với VN phone regex"
    - "AddressPicker dropdown hiển thị list saved addresses, default address pre-highlighted, empty state với link"
    - "OrderFilterBar có 4 controls: status select + from date + to date + keyword input; debounce 400ms; 'Xóa bộ lọc' hiện khi có filter active"
    - "tsc --noEmit exit 0 sau khi tạo 4 components"
  artifacts:
    - path: "sources/frontend/src/components/ui/AddressCard/AddressCard.tsx"
      provides: "Address row display + 3 action buttons"
      exports: ["AddressCard"]
    - path: "sources/frontend/src/components/ui/AddressForm/AddressForm.tsx"
      provides: "rhf+zod form 6 fields, reusable cho create và edit"
      exports: ["AddressForm"]
    - path: "sources/frontend/src/components/ui/AddressPicker/AddressPicker.tsx"
      provides: "Dropdown snap-fill, max-height 240px, default pre-highlighted"
      exports: ["AddressPicker"]
    - path: "sources/frontend/src/components/ui/OrderFilterBar/OrderFilterBar.tsx"
      provides: "Filter bar với debounce 400ms + URL state"
      exports: ["OrderFilterBar"]
  key_links:
    - from: "AddressForm"
      to: "rhf + zod (Phase 10 pattern)"
      via: "useForm<AddressFormData> + zodResolver(addressSchema)"
      pattern: "zodResolver.*addressSchema"
    - from: "AddressPicker"
      to: "AddressBody (checkout form)"
      via: "onSelect(address: SavedAddress) callback"
      pattern: "onSelect.*SavedAddress"
    - from: "OrderFilterBar"
      to: "URL state"
      via: "useSearchParams + router.push với debounce 400ms"
      pattern: "debounce.*400\|useSearchParams"
---

<objective>
Tạo 4 UI components mới: AddressCard, AddressForm, AddressPicker, OrderFilterBar.

Purpose: Components này được compose vào pages (Plan 11-06). Tách riêng để giảm context cost mỗi plan và đảm bảo component quality trước khi integrate.

Output: 4 components với CSS Modules, đầy đủ props, accessibility attrs, compile-safe.
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-address-book-order-history-filtering/11-UI-SPEC.md
@.planning/phases/11-address-book-order-history-filtering/11-CONTEXT.md

<interfaces>
<!-- Button component props -->
```typescript
// src/components/ui/Button/Button.tsx
// Props: variant?: 'primary'|'secondary'|'tertiary'|'danger'; size?: 'sm'|'md'|'lg';
// fullWidth?: boolean; loading?: boolean; disabled?: boolean; type?: 'button'|'submit'
// onClick?: () => void; children: React.ReactNode; style?: React.CSSProperties;
// aria-label?: string; title?: string
```

<!-- Badge component props -->
```typescript
// src/components/ui/Badge/Badge.tsx
// Props: variant?: 'default'|'new'|'hot'|'sale'|'out-of-stock'; children: React.ReactNode
// Dùng Badge cho status đơn hàng — Phase 11 badge "Mặc định" dùng custom CSS (không Badge component)
```

<!-- Input component props -->
```typescript
// src/components/ui/Input/Input.tsx
// Props: label?: string; placeholder?: string; value: string; onChange: (e) => void;
// error?: string; fullWidth?: boolean; type?: string; helperText?: string
```

<!-- Modal component props -->
```typescript
// src/components/ui/Modal/Modal.tsx
// Props: open: boolean; onClose: () => void; title: string; children: React.ReactNode;
// primaryAction?: { label: string; onClick: () => void; loading?: boolean; variant?: string }
// secondaryAction?: { label: string; onClick: () => void; variant?: string }
```

<!-- rhf + zod pattern (Phase 10 lần đầu) -->
```typescript
// import { useForm } from 'react-hook-form';
// import { zodResolver } from '@hookform/resolvers/zod';
// import { z } from 'zod';
// const schema = z.object({ ... });
// type FormData = z.infer<typeof schema>;
// const { register, handleSubmit, formState: { errors }, reset } = useForm<FormData>({
//   resolver: zodResolver(schema),
//   defaultValues: { ... }
// });
// Phone regex Phase 10 (VN loose): /^\+?[0-9\s-]{7,20}$/
// Phone regex Phase 11 (VN strict per UI-SPEC): /^(0|\+84)[3-9]\d{8}$/
```

<!-- SavedAddress type (từ Plan 11-04 types/index.ts) -->
```typescript
interface SavedAddress {
  id: string; userId: string; fullName: string; phone: string;
  street: string; ward: string; district: string; city: string;
  isDefault: boolean; createdAt: string;
}
interface AddressBody {
  fullName: string; phone: string; street: string;
  ward: string; district: string; city: string; isDefault?: boolean;
}
```

<!-- CSS custom properties (từ UI-SPEC.md) -->
```css
/* Colors */
--surface-container-low: #f2f4f6;
--surface-container: #e7eaf0;       /* hover bg cho cards */
--surface-container-lowest: #ffffff;
--primary: #0056D2;
--primary-fixed: #dae2ff;           /* highlight default address, badge bg */
--error: #ba1a1a;
/* Spacing */
--space-1: 4px; --space-2: 8px; --space-3: 16px; --space-4: 24px;
/* Text */
--text-body-md: 14px; --text-body-lg: 16px; --text-title-sm: 14px;
--weight-regular: 400; --weight-semibold: 600;
/* Radius */
--radius-lg: ...; --radius-xl: ...; --radius-full: 9999px;
/* Shadow */
--shadow-md: ...;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AddressCard + AddressCard.module.css + AddressForm + AddressForm.module.css</name>
  <read_first>
    - sources/frontend/src/components/ui/Button/Button.tsx (props interface: variant, onClick, disabled, aria-label, title)
    - sources/frontend/src/components/ui/Badge/Badge.tsx (variant options — để NOT use cho badge "Mặc định", thay bằng custom CSS)
    - sources/frontend/src/app/profile/page.module.css (xem .orderCard, .addressCard patterns nếu có để reuse CSS vars)
    - sources/frontend/src/app/profile/settings/page.tsx (rhf+zod pattern lần đầu — useForm, zodResolver, register, errors pattern)
  </read_first>
  <files>
    sources/frontend/src/components/ui/AddressCard/AddressCard.tsx,
    sources/frontend/src/components/ui/AddressCard/AddressCard.module.css,
    sources/frontend/src/components/ui/AddressForm/AddressForm.tsx,
    sources/frontend/src/components/ui/AddressForm/AddressForm.module.css
  </files>
  <action>
**1. AddressCard.tsx** (src/components/ui/AddressCard/):

Props interface:
```typescript
interface AddressCardProps {
  address: SavedAddress;
  onEdit: (address: SavedAddress) => void;
  onDelete: (address: SavedAddress) => void;
  onSetDefault: (address: SavedAddress) => void;
  settingDefault?: boolean; // loading state khi đang set default
}
```

Layout per UI-SPEC:
- Card container: surface-container-low bg, hover → surface-container bg + translateY(-1px)
- Dòng 1: badge "Mặc định" (khi isDefault=true, custom span) + fullName (text-title-sm, weight-semibold) | phone (text-body-md)
- Dòng 2: `{street}, {ward}, {district}, {city}` (text-body-md, --on-surface-variant color)
- Actions row: Button "Đặt làm mặc định" (secondary, ẩn khi isDefault=true), Button "Sửa" (secondary, aria-label="Sửa địa chỉ {fullName}"), Button "Xóa" (danger, aria-label="Xóa địa chỉ {fullName}")
- settingDefault=true → "Đặt làm mặc định" button loading, card opacity 0.7
- Badge "Mặc định": `<span>` với CSS: bg `--primary-fixed`, color `--primary`, radius `--radius-full`, font 11px weight-semibold, aria-label="Địa chỉ mặc định"

**2. AddressCard.module.css:**
Định nghĩa: `.card`, `.card:hover`, `.cardDefault`, `.header`, `.fullName`, `.phone`, `.addressText`, `.actions`, `.badge`, `.loadingCard`

**3. AddressForm.tsx** (src/components/ui/AddressForm/):

Props:
```typescript
interface AddressFormProps {
  initialValues?: Partial<AddressBody>;
  onSubmit: (data: AddressBody) => Promise<void>;
  onCancel: () => void;
  loading?: boolean;
}
```

Implementation:
- `'use client'`
- Import: useForm, zodResolver, z từ zod
- Zod schema (per UI-SPEC AddressForm Field Contract):
  ```typescript
  const addressSchema = z.object({
    fullName: z.string().min(2, 'Tối thiểu 2 ký tự').max(100, 'Tối đa 100 ký tự'),
    phone: z.string().regex(/^(0|\+84)[3-9]\d{8}$/, 'Số điện thoại VN không hợp lệ (VD: 0912345678)'),
    street: z.string().min(5, 'Tối thiểu 5 ký tự').max(200, 'Tối đa 200 ký tự'),
    ward:   z.string().min(2).max(100),
    district: z.string().min(2).max(100),
    city:   z.string().min(2).max(100),
  });
  type AddressFormData = z.infer<typeof addressSchema>;
  ```
- Layout trong Modal (grid 2 cols):
  - Hàng 1: fullName (1fr) + phone (1fr)
  - Hàng 2: street (full width, span 2)
  - Hàng 3: ward + district + city (3 cols hoặc stack mobile)
- Input components cho từng field với label + placeholder từ UI-SPEC
- Buttons row: Button "Hủy" (secondary) + Button "Lưu địa chỉ" (primary, type="submit", loading={loading})
- `onSubmit`: rhf handleSubmit → call props.onSubmit(data) → caller handle API + toast + modal close

**4. AddressForm.module.css:**
Định nghĩa: `.form`, `.grid2`, `.spanFull`, `.grid3`, `.actions`
  </action>
  <verify>
    <automated>
      grep -n "AddressCardProps\|SavedAddress" sources/frontend/src/components/ui/AddressCard/AddressCard.tsx
      grep -n "zodResolver\|addressSchema" sources/frontend/src/components/ui/AddressForm/AddressForm.tsx
      grep -n "(0|\\+84)\[3-9\]" sources/frontend/src/components/ui/AddressForm/AddressForm.tsx
      ls sources/frontend/src/components/ui/AddressCard/ sources/frontend/src/components/ui/AddressForm/
    </automated>
  </verify>
  <acceptance_criteria>
    - AddressCard.tsx: 'use client', props type AddressCardProps với 5 fields, badge "Mặc định" có aria-label, Sửa/Xóa có aria-label với fullName
    - AddressForm.tsx: 'use client', zod schema với phone regex `/^(0|\+84)[3-9]\d{8}$/`, useForm với zodResolver, 6 Input components
    - AddressForm onSubmit gọi props.onSubmit(data) — không tự call API (caller responsibility)
    - CSS files tồn tại với ít nhất .card và .form class definitions
  </acceptance_criteria>
  <done>AddressCard + AddressForm tạo xong với đúng props, zod validation (VN phone strict), CSS Modules.</done>
</task>

<task type="auto">
  <name>Task 2: AddressPicker + AddressPicker.module.css + OrderFilterBar + OrderFilterBar.module.css</name>
  <read_first>
    - sources/frontend/src/app/checkout/page.tsx (xem form.fullName/phone/street/ward/district/city — để biết field names cần fill khi snap)
    - sources/frontend/src/components/ui/AddressCard/AddressCard.tsx (vừa tạo — xem SavedAddress type usage)
    - sources/frontend/src/app/profile/page.tsx (xem statusMap — reuse order status labels)
  </read_first>
  <files>
    sources/frontend/src/components/ui/AddressPicker/AddressPicker.tsx,
    sources/frontend/src/components/ui/AddressPicker/AddressPicker.module.css,
    sources/frontend/src/components/ui/OrderFilterBar/OrderFilterBar.tsx,
    sources/frontend/src/components/ui/OrderFilterBar/OrderFilterBar.module.css
  </files>
  <action>
**1. AddressPicker.tsx** (src/components/ui/AddressPicker/):

Props:
```typescript
interface AddressPickerProps {
  addresses: SavedAddress[];
  loading?: boolean;      // đang fetch addresses
  onSelect: (address: SavedAddress) => void;
}
```

Implementation:
- `'use client'`
- State: `isOpen` (boolean), `selectedId` (string | null — auto-set to default address id khi addresses load)
- Trigger button: `<button>` text "Địa chỉ đã lưu ▼", variant="secondary" CSS, full-width, height 40px
  - `aria-expanded={isOpen}`, `aria-haspopup="listbox"`, onClick toggle isOpen
- Dropdown: `role="listbox"`, max-height 240px, overflow-y auto, `--shadow-md`, `--surface-container-lowest` bg
  - Khi loading: 2 skeleton rows (div với class `.skeleton`)
  - Khi addresses.length === 0: message "Chưa có địa chỉ đã lưu." + Link href="/profile/addresses" text "Thêm tại Sổ địa chỉ →" target="_blank"
  - Khi có addresses: map qua addresses, mỗi item là `<div role="option">`:
    - Text: `{fullName} — {street}, {ward}, {district}, {city}` (per UI-SPEC display format)
    - isDefault → pre-highlighted với `--primary-fixed` bg
    - selectedId === address.id → check mark "✓"
    - onClick: setSelectedId(address.id); onSelect(address); setIsOpen(false)
- useEffect: khi `addresses` load (length > 0), set selectedId = addresses.find(a => a.isDefault)?.id ?? null
- Click-outside close: useEffect với document click listener (check contains)

**2. AddressPicker.module.css:**
`.trigger`, `.dropdown`, `.item`, `.itemDefault`, `.itemSelected`, `.emptyState`, `.skeletonRow`

**3. OrderFilterBar.tsx** (src/components/ui/OrderFilterBar/):

Props:
```typescript
interface OrderFilterBarProps {
  initialStatus?: string;
  initialFrom?: string;
  initialTo?: string;
  initialQ?: string;
  onChange: (filters: { status: string; from: string; to: string; q: string }) => void;
}
```

Implementation:
- `'use client'`
- State: status (string, default 'ALL'), from (string, default ''), to (string, default ''), q (string, default '')
- Khởi tạo từ initialStatus/initialFrom/initialTo/initialQ props
- hasFilter: `status !== 'ALL' || from !== '' || to !== '' || q !== ''`
- Debounce 400ms: dùng useEffect với setTimeout/clearTimeout pattern:
  ```typescript
  useEffect(() => {
    const timer = setTimeout(() => {
      onChange({ status, from, to, q });
    }, 400);
    return () => clearTimeout(timer);
  }, [status, from, to, q]);
  // onChange excluded từ deps — caller phải wrap trong useCallback
  ```
- Status `<select>` với 6 options (Tất cả / Chờ xác nhận / Đã xác nhận / Đang giao / Đã giao / Đã hủy — values: ALL/PENDING/CONFIRMED/SHIPPING/DELIVERED/CANCELLED)
- From `<input type="date">` với label "Từ ngày"
- To `<input type="date">` với label "Đến ngày"
- Keyword `<input type="text">` placeholder "Tìm theo mã đơn hàng..."
- "Xóa bộ lọc" Button (variant="tertiary", aria-label="Xóa tất cả bộ lọc đang áp dụng") — chỉ render khi hasFilter:
  onClick: setStatus('ALL'); setFrom(''); setTo(''); setQ('');

**Layout:** `display: flex, gap: --space-2, flex-wrap: wrap, align-items: flex-end` — 1 hàng desktop, stack mobile
Filter bar bg: `--surface-container-low`, padding `--space-3 --space-4`, border-radius `--radius-xl`

**4. OrderFilterBar.module.css:**
`.filterBar`, `.filterGroup`, `.label`, `.select`, `.input`, `.clearBtn`
  </action>
  <verify>
    <automated>
      grep -n "role=\"listbox\"\|aria-expanded\|onSelect" sources/frontend/src/components/ui/AddressPicker/AddressPicker.tsx
      grep -n "debounce\|setTimeout.*400\|clearTimeout" sources/frontend/src/components/ui/OrderFilterBar/OrderFilterBar.tsx
      grep -n "Xóa bộ lọc\|hasFilter\|clearTimeout" sources/frontend/src/components/ui/OrderFilterBar/OrderFilterBar.tsx
      grep -n "primary-fixed\|Chưa có địa chỉ" sources/frontend/src/components/ui/AddressPicker/AddressPicker.tsx
      ls sources/frontend/src/components/ui/AddressPicker/ sources/frontend/src/components/ui/OrderFilterBar/
    </automated>
  </verify>
  <acceptance_criteria>
    - AddressPicker.tsx: role="listbox" trên dropdown, aria-expanded trên trigger, default address có --primary-fixed highlight, empty state có Link href="/profile/addresses"
    - AddressPicker max-height 240px trong CSS
    - OrderFilterBar.tsx: status select với 6 options (ALL/PENDING/CONFIRMED/SHIPPING/DELIVERED/CANCELLED), debounce setTimeout 400ms trong useEffect, "Xóa bộ lọc" chỉ render khi hasFilter
    - OrderFilterBar onChange gọi sau 400ms delay, KHÔNG gọi ngay khi input thay đổi
    - CSS files tồn tại
  </acceptance_criteria>
  <done>AddressPicker (dropdown snap-fill, 240px max, default pre-highlighted) + OrderFilterBar (debounce 400ms, live filter, Xóa bộ lọc conditional) hoàn chỉnh.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input → OrderFilterBar | Untrusted text input cho keyword q |
| AddressPicker → checkout form | Fill form fields với saved address data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-05-01 | Injection | OrderFilterBar q input → URL → API | accept | q encode qua URLSearchParams (services/orders.ts qs.set). Backend dùng JPQL @Param. No XSS risk — giá trị không render as HTML. |
| T-11-05-02 | Spoofing | AddressPicker snap-fill form | accept | Chỉ fill form fields từ SavedAddress data — user vẫn edit được trước khi submit. Không auto-submit. |
| T-11-05-03 | Denial of Service | AddressPicker click-outside listener memory leak | mitigate | useEffect cleanup: `return () => document.removeEventListener(...)` — prevent stale listener accumulation. |
</threat_model>

<verification>
```bash
# Verify all 4 component directories created
ls sources/frontend/src/components/ui/AddressCard/
ls sources/frontend/src/components/ui/AddressForm/
ls sources/frontend/src/components/ui/AddressPicker/
ls sources/frontend/src/components/ui/OrderFilterBar/

# TypeScript compile
cd sources/frontend && npx tsc --noEmit 2>&1 | tail -10
```
</verification>

<success_criteria>
- 8 files tạo mới (4 .tsx + 4 .module.css)
- AddressCard: badge "Mặc định" với aria-label, action buttons với contextual aria-labels
- AddressForm: VN phone regex `/^(0|\+84)[3-9]\d{8}$/`, 6 fields, rhf+zod
- AddressPicker: max-height 240px, role=listbox, default pre-highlighted, empty state với link
- OrderFilterBar: debounce 400ms, "Xóa bộ lọc" conditional, 6 status options
- tsc --noEmit exit 0
</success_criteria>

<output>
Sau khi hoàn thành, tạo `.planning/phases/11-address-book-order-history-filtering/11-05-SUMMARY.md`
</output>
