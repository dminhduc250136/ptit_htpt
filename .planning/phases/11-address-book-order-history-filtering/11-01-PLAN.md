---
phase: 11-address-book-order-history-filtering
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - sources/backend/user-service/src/main/resources/db/migration/V4__create_addresses.sql
  - sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/domain/AddressEntity.java
  - sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/domain/AddressDto.java
  - sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/repository/AddressRepository.java
  - sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/exception/AddressLimitExceededException.java
  - sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/service/AddressService.java
  - sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/web/AddressController.java
  - sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/exception/GlobalExceptionHandler.java
autonomous: true
requirements:
  - ACCT-05
  - ACCT-06

must_haves:
  truths:
    - "GET /users/me/addresses trả list addresses của user đang đăng nhập (filter by user_id từ JWT)"
    - "POST /users/me/addresses tạo address mới; nếu tổng > 10 → 422 ADDRESS_LIMIT_EXCEEDED"
    - "PUT /users/me/addresses/{id} cập nhật address (chỉ owner); address không tồn tại → 404"
    - "DELETE /users/me/addresses/{id} xóa address (hard-delete, chỉ owner)"
    - "PUT /users/me/addresses/{id}/default set is_default=true cho 1 row, clear is_default trên các row khác của cùng user"
    - "Partial unique index enforces: chỉ 1 row is_default=true per user_id"
  artifacts:
    - path: "sources/backend/user-service/src/main/resources/db/migration/V4__create_addresses.sql"
      provides: "addresses table + partial unique index"
      contains: "CREATE TABLE user_svc.addresses"
    - path: "sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/domain/AddressEntity.java"
      provides: "JPA entity ánh xạ bảng user_svc.addresses"
      exports: ["AddressEntity"]
    - path: "sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/service/AddressService.java"
      provides: "CRUD + set-default business logic"
      exports: ["AddressService"]
    - path: "sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/web/AddressController.java"
      provides: "REST endpoints /users/me/addresses/**"
      exports: ["AddressController"]
  key_links:
    - from: "AddressController"
      to: "UserMeController.extractUserIdFromBearer()"
      via: "JwtUtils.parseToken(token).getSubject()"
      pattern: "extractUserIdFromBearer"
    - from: "AddressService.setDefault()"
      to: "addressRepository"
      via: "clearDefault() + save()"
      pattern: "is_default.*false.*userId"
    - from: "V4__create_addresses.sql"
      to: "user_svc schema"
      via: "Flyway auto-apply on startup"
      pattern: "CREATE UNIQUE INDEX.*WHERE is_default"
---

<objective>
Tạo toàn bộ backend address CRUD cho user-service: V4 Flyway migration, AddressEntity, AddressRepository, AddressService, AddressController với 5 endpoints.

Purpose: Đây là foundation layer cho ACCT-05 (address book) và ACCT-06 (checkout snap-fill). Frontend plans phụ thuộc vào plan này hoàn thành trước.

Output: 5 REST endpoints dưới /users/me/addresses/** hoạt động với JWT auth, cap 10 addresses/user, partial unique index bảo vệ set-default.
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/11-address-book-order-history-filtering/11-CONTEXT.md

<interfaces>
<!-- Patterns từ UserMeController — tái dụng extractUserIdFromBearer() -->
<!-- sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/web/UserMeController.java -->

```java
// JWT extraction pattern — COPY NGUYÊN method này vào AddressController:
private String extractUserIdFromBearer(String authHeader) {
    if (authHeader == null || !authHeader.startsWith("Bearer ")) {
        throw new ResponseStatusException(HttpStatus.UNAUTHORIZED,
                "Missing or invalid Authorization header");
    }
    String token = authHeader.substring("Bearer ".length()).trim();
    try {
        Claims claims = jwtUtils.parseToken(token);
        return claims.getSubject();
    } catch (Exception e) {
        throw new ResponseStatusException(HttpStatus.UNAUTHORIZED, "Invalid token");
    }
}
// JwtUtils inject qua constructor (final field)
// Package: com.ptit.htpt.userservice.jwt.JwtUtils
```

<!-- Pattern từ UserProfileService — @Transactional + ResponseStatusException 404 -->
```java
// @Service @Transactional class pattern
// userRepo.findById(id).orElseThrow(() -> new ResponseStatusException(HttpStatus.NOT_FOUND, "..."))
```

<!-- Pattern từ V2__add_fullname_phone.sql -->
```sql
-- Dùng schema prefix: user_svc.*
-- Ví dụ: ALTER TABLE user_svc.users ...
```

<!-- ApiResponse envelope -->
```java
// return ApiResponse.of(200, "message", data);
// return ApiResponse.of(201, "Address created", dto);
// Package: com.ptit.htpt.userservice.api.ApiResponse
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: V4 Flyway migration + AddressEntity + AddressRepository + AddressDto</name>
  <read_first>
    - sources/backend/user-service/src/main/resources/db/migration/V2__add_fullname_phone.sql (schema prefix pattern: user_svc.*)
    - sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/domain/UserEntity.java (JPA annotation pattern: @Entity, @Table, @Column, @SQLRestriction, Instant fields)
    - sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/domain/UserDto.java (DTO record pattern)
    - sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/repository/UserRepository.java (JpaRepository pattern)
  </read_first>
  <files>
    sources/backend/user-service/src/main/resources/db/migration/V4__create_addresses.sql,
    sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/domain/AddressEntity.java,
    sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/domain/AddressDto.java,
    sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/repository/AddressRepository.java
  </files>
  <action>
**1. Tạo V4__create_addresses.sql:**
```sql
-- Phase 11 / Plan 11-01 (ACCT-05, D-04, D-05)
-- addresses table: id UUID, user_id UUID FK (không enforce FK để tránh cross-schema issue),
-- full_name, phone, street, ward, district, city, is_default, created_at

CREATE TABLE user_svc.addresses (
    id          UUID        NOT NULL DEFAULT gen_random_uuid() PRIMARY KEY,
    user_id     UUID        NOT NULL,
    full_name   VARCHAR(100) NOT NULL,
    phone       VARCHAR(20)  NOT NULL,
    street      VARCHAR(200) NOT NULL,
    ward        VARCHAR(100) NOT NULL,
    district    VARCHAR(100) NOT NULL,
    city        VARCHAR(100) NOT NULL,
    is_default  BOOLEAN      NOT NULL DEFAULT false,
    created_at  TIMESTAMPTZ  NOT NULL DEFAULT NOW()
);

-- D-05: Partial unique index — chỉ 1 row is_default=true per user_id
-- Enforces SC-3 (concurrent set-default)
CREATE UNIQUE INDEX idx_addresses_user_default
    ON user_svc.addresses (user_id)
    WHERE is_default = true;

-- Index cho query by user_id (sort by created_at DESC)
CREATE INDEX idx_addresses_user_created
    ON user_svc.addresses (user_id, created_at DESC);
```

**2. Tạo AddressEntity.java** (package: com.ptit.htpt.userservice.domain):
- @Entity, @Table(name = "addresses", schema = "user_svc")
- Không cần @SQLRestriction / soft-delete — address dùng hard-delete (D-07)
- Fields: id (UUID String), userId (String, @Column name="user_id"), fullName, phone, street, ward, district, city (VARCHAR các độ dài tương ứng), isDefault (boolean), createdAt (Instant, updatable=false)
- Static factory: `AddressEntity.create(userId, fullName, phone, street, ward, district, city)` — sinh UUID, isDefault=false, createdAt=Instant.now()
- Setters: setFullName(), setPhone(), setStreet(), setWard(), setDistrict(), setCity(), setDefault(boolean)
- Getters record-style: id(), userId(), fullName(), phone(), street(), ward(), district(), city(), isDefault(), createdAt()

**3. Tạo AddressDto.java** (record, cùng package domain):
```java
public record AddressDto(
    String id,
    String userId,
    String fullName,
    String phone,
    String street,
    String ward,
    String district,
    String city,
    boolean isDefault,
    String createdAt  // ISO-8601 string
) {
    public static AddressDto from(AddressEntity e) {
        return new AddressDto(e.id(), e.userId(), e.fullName(), e.phone(),
            e.street(), e.ward(), e.district(), e.city(), e.isDefault(),
            e.createdAt().toString());
    }
}
```

**4. Tạo AddressRepository.java** (package: com.ptit.htpt.userservice.repository):
```java
public interface AddressRepository extends JpaRepository<AddressEntity, String> {
    List<AddressEntity> findByUserIdOrderByIsDefaultDescCreatedAtDesc(String userId);
    long countByUserId(String userId);
    // Dùng @Modifying + @Query để clear default trước khi set mới:
    @Modifying
    @Query("UPDATE AddressEntity a SET a.isDefault = false WHERE a.userId = :userId AND a.isDefault = true")
    void clearDefaultByUserId(@Param("userId") String userId);
}
```
  </action>
  <verify>
    <automated>
      grep -n "CREATE TABLE user_svc.addresses" sources/backend/user-service/src/main/resources/db/migration/V4__create_addresses.sql
      grep -n "WHERE is_default = true" sources/backend/user-service/src/main/resources/db/migration/V4__create_addresses.sql
      grep -n "class AddressEntity" sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/domain/AddressEntity.java
      grep -n "clearDefaultByUserId" sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/repository/AddressRepository.java
    </automated>
  </verify>
  <acceptance_criteria>
    - V4__create_addresses.sql tồn tại với `CREATE TABLE user_svc.addresses`
    - File có dòng `WHERE is_default = true` trong partial unique index
    - AddressEntity.java có @Entity + @Table(name = "addresses", schema = "user_svc")
    - AddressRepository.java có method `clearDefaultByUserId` với @Modifying + @Query
    - AddressDto.java là record với static factory method `from(AddressEntity e)`
  </acceptance_criteria>
  <done>V4 migration SQL, AddressEntity JPA, AddressDto record, AddressRepository với clearDefaultByUserId tất cả tồn tại và compile-safe.</done>
</task>

<task type="auto">
  <name>Task 2: AddressService + AddressLimitExceededException + AddressController + GlobalExceptionHandler update</name>
  <read_first>
    - sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/service/UserProfileService.java (service pattern: @Service @Transactional, ResponseStatusException 404)
    - sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/web/UserMeController.java (extractUserIdFromBearer pattern, @RequestHeader Authorization, @RequestMapping "/users/me")
    - sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/exception/ (GlobalExceptionHandler pattern — cần extend với ADDRESS_LIMIT_EXCEEDED)
    - sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/api/ApiResponse.java (ApiResponse.of() signature)
  </read_first>
  <files>
    sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/exception/AddressLimitExceededException.java,
    sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/service/AddressService.java,
    sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/web/AddressController.java,
    sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/exception/GlobalExceptionHandler.java
  </files>
  <action>
**1. Tạo AddressLimitExceededException.java** (package: com.ptit.htpt.userservice.exception):
```java
public class AddressLimitExceededException extends RuntimeException {
    public AddressLimitExceededException() {
        super("ADDRESS_LIMIT_EXCEEDED");
    }
}
```

**2. Tạo AddressService.java** (package: com.ptit.htpt.userservice.service):
- @Service @Transactional
- Inject AddressRepository addressRepo
- `List<AddressDto> listAddresses(String userId)` — gọi findByUserIdOrderByIsDefaultDescCreatedAtDesc(userId), map sang AddressDto
- `AddressDto createAddress(String userId, AddressRequest req)` — kiểm tra countByUserId(userId) >= 10 → throw AddressLimitExceededException; tạo entity, nếu req.isDefault() thì clearDefaultByUserId(userId) trước; save; return dto
- `AddressDto updateAddress(String userId, String addressId, AddressRequest req)` — findById(addressId) + kiểm tra userId match (else 403 FORBIDDEN "Not your address"); cập nhật tất cả fields (fullName, phone, street, ward, district, city); save; return dto. Không tự động clear default khi update thường.
- `void deleteAddress(String userId, String addressId)` — findById + ownership check; deleteById(addressId)
- `AddressDto setDefault(String userId, String addressId)` — findById + ownership check; clearDefaultByUserId(userId); entity.setDefault(true); save; return dto

**AddressRequest** (inner record hoặc tạo trong web package — đặt trong service file như inner record hoặc separate file):
```java
public record AddressRequest(
    @NotBlank @Size(max = 100) String fullName,
    @NotBlank @Size(max = 20)  String phone,
    @NotBlank @Size(max = 200) String street,
    @NotBlank @Size(max = 100) String ward,
    @NotBlank @Size(max = 100) String district,
    @NotBlank @Size(max = 100) String city,
    boolean isDefault
) {}
```

**3. Tạo AddressController.java** (package: com.ptit.htpt.userservice.web):
- @RestController @RequestMapping("/users/me/addresses")
- Inject: AddressService addressService, JwtUtils jwtUtils
- Copy nguyên `extractUserIdFromBearer(String authHeader)` từ UserMeController (private method)
- Endpoints:
  - `GET /` → `@GetMapping` — listAddresses(userId) → ApiResponse.of(200, "OK", list)
  - `POST /` → `@PostMapping` @ResponseStatus(CREATED) — createAddress(userId, body) → ApiResponse.of(201, "Address created", dto)
  - `PUT /{id}` → `@PutMapping("/{id}")` — updateAddress(userId, id, body) → ApiResponse.of(200, "Updated", dto)
  - `DELETE /{id}` → `@DeleteMapping("/{id}")` → deleteAddress(userId, id); ApiResponse.of(200, "Deleted", Map.of("id", id))
  - `PUT /{id}/default` → `@PutMapping("/{id}/default")` — setDefault(userId, id) → ApiResponse.of(200, "Default set", dto)

**4. Cập nhật GlobalExceptionHandler.java:**
- Tìm class GlobalExceptionHandler (đã tồn tại, có handler cho InvalidPasswordException)
- Thêm handler cho AddressLimitExceededException:
```java
@ExceptionHandler(AddressLimitExceededException.class)
@ResponseStatus(HttpStatus.UNPROCESSABLE_ENTITY)
public ApiResponse<Void> handleAddressLimit(AddressLimitExceededException ex) {
    return ApiResponse.error(422, "ADDRESS_LIMIT_EXCEEDED",
        "Đã đạt giới hạn 10 địa chỉ. Vui lòng xóa bớt trước khi thêm mới.");
}
```
(Dùng ApiResponse.error() nếu có, hoặc ApiResponse.of() với error body — xem signature ApiResponse hiện tại)
  </action>
  <verify>
    <automated>
      grep -n "AddressLimitExceededException" sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/exception/AddressLimitExceededException.java
      grep -n "class AddressService" sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/service/AddressService.java
      grep -n "setDefault\|clearDefaultByUserId" sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/service/AddressService.java
      grep -n "@RequestMapping.*addresses" sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/web/AddressController.java
      grep -n "AddressLimitExceededException" sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/exception/GlobalExceptionHandler.java
    </automated>
  </verify>
  <acceptance_criteria>
    - AddressService.java có 5 methods: listAddresses, createAddress, updateAddress, deleteAddress, setDefault
    - createAddress có logic `countByUserId(userId) >= 10` → throw AddressLimitExceededException
    - setDefault gọi clearDefaultByUserId(userId) trước khi set entity.isDefault=true
    - AddressController.java có 5 @Mapping annotations cho đúng HTTP methods + paths
    - GlobalExceptionHandler xử lý AddressLimitExceededException → 422 với code "ADDRESS_LIMIT_EXCEEDED"
    - Tất cả methods có ownership check (userId từ JWT == address.userId() — else 403)
  </acceptance_criteria>
  <done>Backend address CRUD hoàn chỉnh: service layer + controller + exception handler. Tất cả 5 endpoints /users/me/addresses/** có logic đúng, compile-safe.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → AddressController | Untrusted JWT token + request body |
| AddressService → DB | Trusted internal call, SQL injection via JPA params |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01-01 | Spoofing | AddressController — userId extraction | mitigate | userId luôn lấy từ JWT claims.sub (extractUserIdFromBearer) — KHÔNG từ path/body param. Consistent với T-09-03-02 pattern. |
| T-11-01-02 | Elevation of Privilege | PUT/DELETE /{id} — ownership check | mitigate | Service check `address.userId().equals(userId)` — nếu không match → 403 FORBIDDEN "Not your address". Không thể modify/delete address của user khác. |
| T-11-01-03 | Denial of Service | POST /addresses — unlimited creation | mitigate | Cap 10/user: `countByUserId >= 10` → throw AddressLimitExceededException → 422. Precedent từ ROADMAP v1.2 lock. |
| T-11-01-04 | Tampering | PUT /{id}/default — concurrent set-default | mitigate | DB-level partial unique index `WHERE is_default = true` enforces exactly 1 default per user — race condition handled at DB, not application layer. |
| T-11-01-05 | Information Disclosure | GET /addresses — list all users | mitigate | findByUserIdOrderBy... filter by JWT-extracted userId — user chỉ thấy addresses của mình. |
</threat_model>

<verification>
```bash
# Verify V4 migration file tồn tại
ls sources/backend/user-service/src/main/resources/db/migration/V4__create_addresses.sql

# Verify partial unique index trong SQL
grep "WHERE is_default = true" sources/backend/user-service/src/main/resources/db/migration/V4__create_addresses.sql

# Verify 5 files Java tồn tại
ls sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/domain/AddressEntity.java
ls sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/domain/AddressDto.java
ls sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/repository/AddressRepository.java
ls sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/service/AddressService.java
ls sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/web/AddressController.java

# Verify key patterns
grep -n "ADDRESS_LIMIT_EXCEEDED" sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/exception/GlobalExceptionHandler.java
grep -n "extractUserIdFromBearer" sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/web/AddressController.java
```
</verification>

<success_criteria>
- V4__create_addresses.sql tồn tại với bảng user_svc.addresses + partial unique index WHERE is_default = true
- 5 Java files tạo mới: AddressEntity, AddressDto, AddressRepository, AddressService, AddressController
- AddressController xử lý 5 endpoints: GET / POST / PUT /{id} / DELETE /{id} / PUT /{id}/default
- GlobalExceptionHandler extend với ADDRESS_LIMIT_EXCEEDED → 422
- Ownership check tồn tại trong updateAddress, deleteAddress, setDefault (403 FORBIDDEN nếu userId không khớp)
- Code compile-safe (không có syntax error rõ ràng)
</success_criteria>

<output>
Sau khi hoàn thành, tạo `.planning/phases/11-address-book-order-history-filtering/11-01-SUMMARY.md`
</output>
