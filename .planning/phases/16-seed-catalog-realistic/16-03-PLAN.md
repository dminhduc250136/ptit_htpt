---
phase: 16-seed-catalog-realistic
plan: 03
type: execute
wave: 3
depends_on:
  - "16-02"
files_modified:
  - sources/frontend/e2e/seed-catalog.spec.ts
  - .planning/phases/16-seed-catalog-realistic/16-VERIFICATION.md
autonomous: false
requirements:
  - SEED-01
  - SEED-02
  - SEED-03
  - SEED-04
tags:
  - seed
  - e2e
  - playwright
  - verification
must_haves:
  truths:
    - "Test `seed-catalog.spec.ts` chạy pass: `/products` render ≥20 cards/category cho 5 categories tech"
    - "Test verify zero broken images (network response listener không có 4xx/5xx cho domain images.unsplash.com)"
    - "Test verify FilterSidebar brand panel chỉ chứa brand domain-tech (không có MAC/Anessa/Cuckoo/sách)"
    - "Manual D-31 negative test bash command documented trong 16-VERIFICATION.md"
    - "Manual smoke SQL count queries documented trong 16-VERIFICATION.md"
    - "Manual add-to-cart smoke test documented (verify A2 inventory assumption)"
  artifacts:
    - path: "sources/frontend/e2e/seed-catalog.spec.ts"
      provides: "Playwright spec verify catalog page render + image load + brand filter"
      min_lines: 60
    - path: ".planning/phases/16-seed-catalog-realistic/16-VERIFICATION.md"
      provides: "Manual smoke SQL + D-31 prod negative test + add-to-cart smoke commands"
      min_lines: 50
  key_links:
    - from: "seed-catalog.spec.ts"
      to: "BE GET /api/products + /api/products/brands"
      via: "Playwright navigate /products + assert DOM"
      pattern: "page.goto\\('/products'\\)"
    - from: "16-VERIFICATION.md SQL smoke section"
      to: "product_svc.products + product_svc.flyway_schema_history"
      via: "psql commands"
      pattern: "psql.*product_svc"
---

<objective>
Verify Phase 16 deliver đúng 4 success criteria từ ROADMAP qua: (a) E2E Playwright spec mới `seed-catalog.spec.ts` automated cho catalog rendering + brand filter + image load, (b) Manual verification doc với SQL smoke counts + D-31 prod profile negative test + add-to-cart smoke (verify A2 inventory assumption — Open Q #1 từ RESEARCH).

Purpose: Đóng loop Phase 16 — chứng minh seed data chạy đúng cả ở DB layer (SQL counts) và UI layer (Playwright). Document manual commands cần human chạy (profile switch không tự động trong CI).

Output:
- `sources/frontend/e2e/seed-catalog.spec.ts` (NEW)
- `.planning/phases/16-seed-catalog-realistic/16-VERIFICATION.md` (NEW)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-seed-catalog-realistic/16-CONTEXT.md
@.planning/phases/16-seed-catalog-realistic/16-RESEARCH.md
@.planning/phases/16-seed-catalog-realistic/16-VALIDATION.md
@sources/frontend/e2e/smoke.spec.ts
@sources/frontend/playwright.config.ts

<interfaces>
<!-- Playwright config (playwright.config.ts) -->
- testDir: './e2e'
- baseURL: 'http://localhost:3000'
- globalSetup: './e2e/global-setup.ts' (login user + admin storageState)
- workers: 1, fullyParallel: false
- locale: 'vi-VN'

<!-- Existing storageState files -->
- e2e/storageState/user.json (logged-in user)
- e2e/storageState/admin.json (logged-in admin)

<!-- /products page DOM (verified products/page.tsx + smoke.spec.ts) -->
- ProductCard: <a href="/products/{slug}">...</a>
- FilterSidebar brand panel trigger: button có text "Thương hiệu" hoặc tương đương
- FilterSidebar brand options render từ availableBrands prop (dynamic từ /api/products/brands)
- Image: <img src="https://images.unsplash.com/photo-..."> hoặc next/image với srcset
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 3.1: Viết Playwright spec `seed-catalog.spec.ts` verify catalog render + brand filter + image load</name>
  <files>sources/frontend/e2e/seed-catalog.spec.ts</files>
  <read_first>
    - sources/frontend/e2e/smoke.spec.ts (precedent pattern, anonymous storageState usage SMOKE-1)
    - sources/frontend/playwright.config.ts (config baseURL + locale)
    - .planning/phases/16-seed-catalog-realistic/16-CONTEXT.md (D-07 categories, D-09 brand list, D-27 acceptance)
    - .planning/phases/16-seed-catalog-realistic/16-VALIDATION.md (Wave 0 requirements)
    - sources/frontend/src/components/ui/FilterSidebar/FilterSidebar.tsx (verify brand option DOM structure)
  </read_first>
  <action>
Tạo file `sources/frontend/e2e/seed-catalog.spec.ts` (NEW) với 3 test groups verify Phase 16 deliverables.

**File header (bắt buộc):**
```typescript
/**
 * Phase 16 / Plan 16-03 (SEED-02, SEED-03, D-27, D-30):
 * Verify catalog seed deliver đúng — sau khi V101 apply, /products render ≥20 cards/category,
 * Unsplash images load 200 OK, FilterSidebar brand panel chỉ chứa brand domain-tech.
 *
 * Anonymous storageState (cookies empty) — không cần login để xem catalog.
 * Run: cd sources/frontend && npx playwright test e2e/seed-catalog.spec.ts
 */

import { test, expect } from '@playwright/test';
```

**Test 1 — SEED-CAT-1: `/products` render ≥20 cards mỗi category (5 categories tech)**

```typescript
test.describe('SEED-CAT-1: catalog render per-category', () => {
  test.use({ storageState: { cookies: [], origins: [] } });

  // 5 categories tech (D-07 slug list)
  const categories = [
    { slug: 'dien-thoai', label: 'Điện thoại' },
    { slug: 'laptop',     label: 'Laptop' },
    { slug: 'chuot',      label: 'Chuột' },
    { slug: 'ban-phim',   label: 'Bàn phím' },
    { slug: 'tai-nghe',   label: 'Tai nghe' },
  ];

  for (const cat of categories) {
    test(`category "${cat.label}" có ≥20 product cards`, async ({ page }) => {
      // Filter qua URL query param (verify products/page.tsx accept ?category= hoặc tương đương)
      // Strategy: navigate /products?category={slug}, đợi load, đếm <a href="/products/{some-slug}">
      await page.goto(`/products?category=${cat.slug}`);
      await page.waitForLoadState('networkidle');

      // Đếm product card links — selector từ smoke.spec.ts SMOKE-1 verified
      const cards = page.locator('a[href^="/products/"]:not([href="/products"])');
      const count = await cards.count();
      expect(count, `Category ${cat.label} chỉ có ${count} cards (cần ≥20)`).toBeGreaterThanOrEqual(20);
    });
  }
});
```

**Note quan trọng:** Nếu products/page.tsx KHÔNG support `?category=` filter, fall back: navigate `/products` (no filter), đếm tổng cards ≥95, và verify category filter qua FilterSidebar interaction nếu có (kiểm tra tại runtime — adjust selector).

**Test 2 — SEED-CAT-2: Zero broken Unsplash images on /products**

```typescript
test.describe('SEED-CAT-2: no broken Unsplash images', () => {
  test.use({ storageState: { cookies: [], origins: [] } });

  test('/products: tất cả Unsplash image responses status < 400', async ({ page }) => {
    const failedImages: { url: string; status: number }[] = [];

    page.on('response', (resp) => {
      const url = resp.url();
      if (url.includes('images.unsplash.com')) {
        const status = resp.status();
        if (status >= 400) {
          failedImages.push({ url, status });
        }
      }
    });

    await page.goto('/products');
    await page.waitForLoadState('networkidle');
    // Scroll xuống để trigger lazy-load nếu next/image dùng lazy loading
    await page.evaluate(() => window.scrollTo(0, document.body.scrollHeight));
    await page.waitForTimeout(2000);

    expect(failedImages, `Có ${failedImages.length} ảnh Unsplash failed: ${JSON.stringify(failedImages.slice(0, 3))}`).toHaveLength(0);
  });
});
```

**Test 3 — SEED-CAT-3: FilterSidebar brand panel chỉ chứa brand domain-tech**

```typescript
test.describe('SEED-CAT-3: FilterSidebar brand list domain-tech', () => {
  test.use({ storageState: { cookies: [], origins: [] } });

  test('brand panel KHÔNG có brand từ catalog cũ (MAC/Anessa/Cuckoo)', async ({ page }) => {
    await page.goto('/products');
    await page.waitForLoadState('networkidle');

    // Mở FilterSidebar brand panel — selector tolerant (button text "Thương hiệu" hoặc collapsed panel header)
    // Có thể brand panel mở mặc định — không cần click trigger.
    // Strategy: lấy toàn bộ text trong sidebar, search vắng các brand cũ.

    // Brand domain-tech expected (D-09 — phải có ít nhất 7 trong số này)
    const techBrands = ['Apple', 'Samsung', 'Logitech', 'Sony', 'Razer', 'ASUS', 'Dell', 'HP', 'Lenovo', 'Bose', 'Xiaomi', 'Keychron'];
    // Brand DOMAIN CŨ — KHÔNG được hiện
    const oldDomainBrands = ['MAC', 'Anessa', 'Cuckoo']; // cosmetics + household — từ V100 prod-005, prod-009, prod-010

    // Lấy text nội dung filter sidebar — adjust selector nếu sidebar có data-testid hoặc role
    const sidebar = page.locator('aside, [role="complementary"], [data-testid*="filter"], [class*="FilterSidebar"]').first();
    await expect(sidebar).toBeVisible({ timeout: 10000 });
    const sidebarText = await sidebar.textContent() || '';

    // Verify ≥7 tech brands xuất hiện
    const techBrandHits = techBrands.filter(b => sidebarText.includes(b));
    expect(techBrandHits.length, `Chỉ thấy ${techBrandHits.length}/12 tech brand trong sidebar: ${techBrandHits.join(', ')}`).toBeGreaterThanOrEqual(7);

    // Verify brand domain cũ KHÔNG xuất hiện (riêng "MAC" có thể trùng với "MacBook" → check word boundary)
    for (const oldBrand of oldDomainBrands) {
      // Sidebar không nên có brand cũ — SOFT-DELETE đã ẩn products cũ → /api/products/brands DISTINCT trả về tech-only
      // (KHÔNG dùng word-boundary regex chặt với "MAC" vì có thể trùng "MacBook" trong product name của Apple — nhưng sidebar chỉ render brand list, không product names)
      if (oldBrand === 'MAC') {
        // Đặc biệt: brand value đúng là "MAC" (3 chars) cho cosmetics. Nếu product list có Apple MacBook xuất hiện trong sidebar text (không nên — sidebar chỉ list brand), check fail-safe word boundary.
        expect(sidebarText, `Sidebar có chứa brand cosmetics "MAC" (cần soft-delete)`).not.toMatch(/\bMAC\b/);
      } else {
        expect(sidebarText, `Sidebar không được chứa brand "${oldBrand}" (catalog cũ)`).not.toContain(oldBrand);
      }
    }
  });
});
```

**Critical Notes cho executor:**
1. **Selector adjustment:** Trước khi chạy, executor PHẢI inspect FilterSidebar.tsx + ProductCard.tsx render thực tế trên `/products` để verify selector. Sửa selector trong test nếu cần (ví dụ data-testid khác, sidebar wrapper element khác).
2. **Category filter URL param:** `products/page.tsx` line ~27-73 dùng `useState`/`useEffect` cho filter — verify URL pattern thật (có thể là `?category=cat-phone` thay vì `?category=dien-thoai`, hoặc qua sidebar click). Adjust Test 1 nếu cần.
3. **Test 1 fallback:** Nếu URL query param không hỗ trợ category filter, gộp Test 1 thành 1 test duy nhất: navigate `/products`, count tổng cards ≥95, không split per-category. Document trong SUMMARY.md.
4. **Test 3 brand selector:** Nếu FilterSidebar render brand qua checkboxes với label, dùng `page.getByRole('checkbox')` để enumerate, lấy aria-label/text. Adjust selector tolerant.

**Verification command:**
```bash
cd sources/frontend
# Đảm bảo dev stack đang chạy + V101 đã apply (Plan 16-02 done)
npx playwright test e2e/seed-catalog.spec.ts --reporter=list
# Expect: 7 passed (5 per-category tests + image test + brand test)
```
  </action>
  <verify>
    <automated>cd sources/frontend && npx playwright test e2e/seed-catalog.spec.ts --reporter=list</automated>
  </verify>
  <acceptance_criteria>
    - File `sources/frontend/e2e/seed-catalog.spec.ts` tồn tại, ≥60 lines
    - File chứa 3 `test.describe` blocks: `SEED-CAT-1`, `SEED-CAT-2`, `SEED-CAT-3`
    - Sau khi Plan 16-02 đã apply V101 + dev stack chạy: `npx playwright test e2e/seed-catalog.spec.ts` → tất cả pass (7 tests)
    - Test 1 verify ≥20 cards mỗi category (5 categories) — hoặc fallback: ≥95 cards tổng
    - Test 2 verify 0 ảnh Unsplash response 4xx/5xx
    - Test 3 verify ≥7 tech brand trong sidebar + KHÔNG có "MAC"/"Anessa"/"Cuckoo"
    - Full regression: `npx playwright test` không break test cũ (smoke + auth + admin-* + order-detail + password-change)
  </acceptance_criteria>
  <done>seed-catalog.spec.ts pass, full Playwright suite vẫn green.</done>
</task>

<task type="auto">
  <name>Task 3.2: Tạo `16-VERIFICATION.md` với manual SQL smoke + D-31 negative test + add-to-cart smoke</name>
  <files>.planning/phases/16-seed-catalog-realistic/16-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/16-seed-catalog-realistic/16-RESEARCH.md (Example 2 SQL smoke counts, Example 3 prod negative test, Pitfall 5 add-to-cart inventory)
    - .planning/phases/16-seed-catalog-realistic/16-CONTEXT.md (D-29 smoke counts, D-31 negative test, D-06 inventory defer)
  </read_first>
  <action>
Tạo file `.planning/phases/16-seed-catalog-realistic/16-VERIFICATION.md` (NEW) chứa concrete commands cho 3 manual verifications.

**File structure:**

```markdown
# Phase 16 — Verification (Manual + Automated)

**Phase:** 16-seed-catalog-realistic
**Verified by:** _human reviewer_
**Date:** YYYY-MM-DD

---

## 1. Smoke SQL counts (D-29)

**Pre-requisite:** product-svc đã restart với `SPRING_PROFILES_ACTIVE=dev` và V101 đã apply.

```bash
docker compose exec postgres psql -U tmdt -d tmdt -c "
SELECT 'categories_active' AS metric, COUNT(*) AS value FROM product_svc.categories WHERE deleted = FALSE
UNION ALL
SELECT 'products_active',          COUNT(*) FROM product_svc.products   WHERE deleted = FALSE
UNION ALL
SELECT 'distinct_brands',          COUNT(DISTINCT brand) FROM product_svc.products WHERE deleted = FALSE
UNION ALL
SELECT 'unsplash_webp_urls',       COUNT(*) FROM product_svc.products
                                   WHERE deleted = FALSE
                                     AND thumbnail_url LIKE 'https://images.unsplash.com/%'
                                     AND thumbnail_url LIKE '%fm=webp%'
UNION ALL
SELECT 'low_stock_products',       COUNT(*) FROM product_svc.products
                                   WHERE deleted = FALSE AND stock < 10 AND stock > 0
UNION ALL
SELECT 'out_of_stock_products',    COUNT(*) FROM product_svc.products
                                   WHERE deleted = FALSE AND stock = 0
UNION ALL
SELECT 'with_original_price',      COUNT(*) FROM product_svc.products
                                   WHERE deleted = FALSE AND original_price IS NOT NULL;
"
```

**Expected:**
| metric | expected value |
|--------|---------------|
| categories_active | 5 |
| products_active | ∈ [95, 105] |
| distinct_brands | ≥ 15 |
| unsplash_webp_urls | = products_active (100%) |
| low_stock_products | ∈ [8, 12] (~10%) |
| out_of_stock_products | ∈ [3, 7] (~5%) |
| with_original_price | ∈ [65, 75] (~70%) |

**Verify old data soft-deleted:**
```bash
docker compose exec postgres psql -U tmdt -d tmdt -c "
SELECT id, deleted FROM product_svc.categories WHERE id IN (
  'cat-electronics','cat-fashion','cat-household','cat-books','cat-cosmetics'
);
SELECT id, deleted FROM product_svc.products WHERE id LIKE 'prod-0%' AND id < 'prod-pho-001'
ORDER BY id;
"
```
Expected: tất cả 5 categories cũ + 10 products cũ có `deleted = TRUE`.

**Sign-off:** ☐ PASS  ☐ FAIL — note: ___

---

## 2. D-31 Profile=prod negative test (SEED-04)

Verify V101 KHÔNG chạy khi profile=prod.

```bash
# 1. Reset DB hoàn toàn
docker compose down -v
docker compose up postgres -d
sleep 5

# 2. Start product-svc với SPRING_PROFILES_ACTIVE=prod (override profile mặc định dev)
SPRING_PROFILES_ACTIVE=prod docker compose up product-service -d
sleep 20

# 3. Query Flyway history — V101 KHÔNG được có
docker compose exec postgres psql -U tmdt -d tmdt -c \
  "SELECT version, script, success FROM product_svc.flyway_schema_history WHERE version IN ('100','101');"
```

**Expected:** 0 rows (cả V100 và V101 skip trong prod profile vì path `db/seed-dev/` không trong location scan).

**Cleanup sau test:**
```bash
docker compose down -v
docker compose up -d  # về lại profile dev mặc định
```

**Sign-off:** ☐ PASS  ☐ FAIL — note: ___

---

## 3. Add-to-cart smoke (verify A2 inventory assumption — Pitfall 5)

Verify 1 SP mới có thể add-to-cart không cần inventory entries (D-06 defer assumption).

**Steps:**
1. Mở browser http://localhost:3000/products (anonymous OK hoặc login user demo)
2. Click product card đầu tiên (ví dụ `prod-pho-001`)
3. Click button "Thêm vào giỏ"
4. Quan sát:
   - **PASS:** Toast/notification "Đã thêm vào giỏ" hoặc cart icon update count → A2 đúng, KHÔNG cần inventory seed
   - **FAIL:** Error message "Không đủ hàng" / "Inventory not found" / 500 → A2 sai, CẦN tạo sub-plan inventory V3 seed

**Nếu FAIL → tạo Plan 16-04 sub-plan:** thêm `sources/backend/inventory-service/src/main/resources/db/seed-dev/V3__seed_catalog_inventory.sql` với 100 inventory rows tương ứng `prod-{pho|lap|mou|key|hea}-NNN` (mapping product_id từ V101).

**Sign-off:** ☐ PASS (A2 confirmed) ☐ FAIL (cần Plan 16-04) — note: ___

---

## 4. Automated E2E (Playwright)

```bash
cd sources/frontend
npx playwright test e2e/seed-catalog.spec.ts --reporter=list
# Expect: 7 passed
```

**Sign-off:** ☐ PASS  ☐ FAIL — note: ___

---

## Verification Verdict

- [ ] Section 1 (smoke SQL): PASS
- [ ] Section 2 (D-31 prod negative): PASS
- [ ] Section 3 (add-to-cart smoke): PASS — hoặc FAIL với Plan 16-04 created
- [ ] Section 4 (Playwright e2e): PASS

**Overall:** ☐ Phase 16 ACCEPTED  ☐ Phase 16 NEEDS REWORK
```

**Verification command (sau khi tạo file):**
```bash
test -f .planning/phases/16-seed-catalog-realistic/16-VERIFICATION.md && \
  grep -c "## " .planning/phases/16-seed-catalog-realistic/16-VERIFICATION.md
# Expect: ≥4 (4 main sections)
```
  </action>
  <verify>
    <automated>test -f .planning/phases/16-seed-catalog-realistic/16-VERIFICATION.md && grep -q "D-31" .planning/phases/16-seed-catalog-realistic/16-VERIFICATION.md && grep -q "add-to-cart" .planning/phases/16-seed-catalog-realistic/16-VERIFICATION.md && grep -q "smoke SQL" .planning/phases/16-seed-catalog-realistic/16-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/16-seed-catalog-realistic/16-VERIFICATION.md` tồn tại, ≥50 lines
    - Chứa 4 sections: smoke SQL counts, D-31 prod negative test, add-to-cart smoke, automated E2E
    - Section 1 chứa SQL query đếm: categories_active, products_active, distinct_brands, unsplash_webp_urls, low_stock_products, out_of_stock_products, with_original_price
    - Section 2 chứa bash commands `SPRING_PROFILES_ACTIVE=prod` + Flyway history query
    - Section 3 chứa quy trình manual click "Thêm vào giỏ" + branching PASS/FAIL với Plan 16-04 reference
    - Section 4 chứa Playwright command exact: `npx playwright test e2e/seed-catalog.spec.ts`
    - Có "Verification Verdict" checkboxes cho human sign-off
  </acceptance_criteria>
  <done>16-VERIFICATION.md ready cho human runner thực hiện 3 manual + 1 automated check.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint 3.3: Human chạy 3 manual verifications + automated E2E + acceptance sign-off</name>
  <what-built>
    - Plan 16-01: IMAGES.csv với ≥100 curated Unsplash photo IDs
    - Plan 16-02: V101__seed_catalog_realistic.sql + ROADMAP.md V7→V101 patch
    - Plan 16-03 Task 3.1: seed-catalog.spec.ts (Playwright E2E)
    - Plan 16-03 Task 3.2: 16-VERIFICATION.md (manual command doc)
  </what-built>
  <how-to-verify>
    1. **Restart dev stack với V101 fresh apply:**
       ```bash
       docker compose down -v
       docker compose up -d
       # đợi product-svc healthy ~30s
       ```

    2. **Section 1 — Smoke SQL counts:** chạy block SQL trong `16-VERIFICATION.md` Section 1. Verify 7 metrics khớp expected ranges (categories_active=5, products_active∈[95,105], distinct_brands≥15, unsplash_webp_urls=products_active, low_stock∈[8,12], oos∈[3,7], with_original_price∈[65,75]).

    3. **Section 2 — D-31 profile=prod negative test:** chạy bash block. Verify Flyway history KHÔNG có row V100/V101 khi profile=prod. Sau test, cleanup về dev profile.

    4. **Section 3 — Add-to-cart smoke:** mở http://localhost:3000/products, click 1 product mới, click "Thêm vào giỏ". Quan sát PASS hoặc FAIL.
       - PASS → A2 confirmed, KHÔNG cần Plan 16-04
       - FAIL → ghi lại error message, **dừng** và yêu cầu Claude tạo Plan 16-04 inventory seed sub-plan

    5. **Section 4 — Automated E2E:**
       ```bash
       cd sources/frontend
       npx playwright test e2e/seed-catalog.spec.ts --reporter=list
       # Expect: 7 passed
       npx playwright test --reporter=list
       # Expect: full suite green (no regression)
       ```

    6. **UI walkthrough:** mở http://localhost:3000/products, kiểm trực quan:
       - 5 categories tech hiển thị (KHÔNG còn fashion/household/books/cosmetics)
       - Mỗi card có ảnh load, brand thực tế, giá có gạch ngang ở ~70% SP
       - FilterSidebar mở "Thương hiệu" panel → chỉ thấy brand domain-tech (Apple/Samsung/Logitech/Sony/Razer/ASUS/Dell/HP/Lenovo/Bose/Xiaomi/Keychron/...). KHÔNG có "MAC"/"Anessa"/"Cuckoo".

    7. Tick checkboxes sign-off trong `16-VERIFICATION.md` (commit nếu cần audit trail).
  </how-to-verify>
  <resume-signal>
    Type "approved" nếu tất cả 4 sections + UI walkthrough PASS.
    Type "fail: <section>" + mô tả nếu có section fail (vd "fail: section 3 add-to-cart 500 error inventory_items not found" → Claude tạo Plan 16-04).
    Type "fail: regression in <spec>" nếu Playwright suite có test cũ break.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Playwright runner ↔ dev stack | E2E test chạy local, không touch prod |
| Manual prod negative test ↔ DB | Bash script reset DB local — destructive nhưng chỉ trên dev volume |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-06 | Denial of Service | E2E flaky vì Unsplash CDN slow | accept | Test 2 dùng `networkidle` + 2s timeout sau scroll; Unsplash CDN imgix cache global, latency low; nếu flaky → mark test retry trong CI (Playwright retry config) |
| T-16-07 | Tampering | D-31 negative test reset DB volume → mất dev data | accept | Manual command với prompt human; doc cleanup step ngay sau test (`docker compose down -v && docker compose up -d` re-seed) |
</threat_model>

<verification>
- seed-catalog.spec.ts pass (7 tests)
- 16-VERIFICATION.md có 4 sections complete
- Human sign-off 4/4 sections PASS
- Full Playwright regression suite green
</verification>

<success_criteria>
- SEED-01: 5 cat tech active, 5 cat cũ deleted (verify SQL Section 1)
- SEED-02: ~100 SP với brand domain-tech (verify SQL + UI walkthrough)
- SEED-03: 100% URL Unsplash WebP load 200 (verify Playwright Test 2)
- SEED-04: profile=prod KHÔNG run V101 (verify Section 2)
- A2 verified: add-to-cart hoạt động hoặc Plan 16-04 trigger
</success_criteria>

<output>
After completion, create `.planning/phases/16-seed-catalog-realistic/16-03-SUMMARY.md` ghi lại:
- Test results (7 passed/failed counts)
- Section 1-4 sign-off status
- A2 outcome (PASS hoặc Plan 16-04 needed)
- UI walkthrough screenshots (optional)
- Phase 16 final verdict (ACCEPTED hoặc NEEDS REWORK)
</output>
