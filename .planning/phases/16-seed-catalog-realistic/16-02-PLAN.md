---
phase: 16-seed-catalog-realistic
plan: 02
type: execute
wave: 2
depends_on:
  - "16-01"
files_modified:
  - sources/backend/product-service/src/main/resources/db/seed-dev/V101__seed_catalog_realistic.sql
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - SEED-01
  - SEED-02
  - SEED-03
  - SEED-04
tags:
  - seed
  - flyway
  - migration
  - sql
must_haves:
  truths:
    - "File V101__seed_catalog_realistic.sql tồn tại trong db/seed-dev/ (KHÔNG phải db/migration/)"
    - "Sau khi product-svc restart với profile=dev, query `SELECT COUNT(*) FROM product_svc.categories WHERE deleted=FALSE` = 5"
    - "Sau khi product-svc restart với profile=dev, query `SELECT COUNT(*) FROM product_svc.products WHERE deleted=FALSE` ∈ [95, 105]"
    - "Mọi sản phẩm mới có `thumbnail_url` khớp pattern `https://images.unsplash.com/photo-%?fm=webp%`"
    - "5 categories cũ (cat-electronics, cat-fashion, cat-household, cat-books, cat-cosmetics) có `deleted=TRUE`"
    - "10 products cũ (prod-001..prod-010) có `deleted=TRUE`"
    - "Distinct brand count ≥ 15 trong products active"
    - "ROADMAP.md `Pre-Phase Setup (v1.3)` table reference V101 (KHÔNG còn V7) cho product-svc Phase 16"
  artifacts:
    - path: "sources/backend/product-service/src/main/resources/db/seed-dev/V101__seed_catalog_realistic.sql"
      provides: "Reset 5 categories + 10 products cũ + insert 5 cat tech + ~100 products tech"
      contains: "V101 single-file Flyway migration với 4 blocks: 2 UPDATE soft-delete + 2 INSERT"
      min_lines: 130
  key_links:
    - from: "V101 INSERT thumbnail_url values"
      to: ".planning/phases/16-seed-catalog-realistic/IMAGES.csv"
      via: "URL construct: https://images.unsplash.com/photo-{IMAGES.csv:id}?fm=webp&q=80&w=800"
      pattern: "images\\.unsplash\\.com/photo-[^?]+\\?fm=webp&q=80&w=800"
    - from: "V101 INSERT category_id values"
      to: "V101 Block 3 categories"
      via: "FK fk_products_category"
      pattern: "category_id IN \\('cat-phone','cat-laptop','cat-mouse','cat-keyboard','cat-headphone'\\)"
---

<objective>
Tạo Flyway migration `V101__seed_catalog_realistic.sql` trong `db/seed-dev/` thực hiện single-transaction reset (soft-delete cũ + insert ~100 SP tech mới) thoả 4 success criteria từ ROADMAP Phase 16. Đồng thời sửa drift V7→V101 trong ROADMAP.md.

Purpose: Deliver dữ liệu catalog thực tế cho profile=dev. Profile isolation tự nhiên qua path `db/seed-dev/` — KHÔNG sửa application.yml.

Output:
- `sources/backend/product-service/src/main/resources/db/seed-dev/V101__seed_catalog_realistic.sql` (NEW)
- `.planning/ROADMAP.md` patch reference V7→V101
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-seed-catalog-realistic/16-CONTEXT.md
@.planning/phases/16-seed-catalog-realistic/16-RESEARCH.md
@.planning/phases/16-seed-catalog-realistic/IMAGES.csv
@sources/backend/product-service/src/main/resources/db/seed-dev/V100__seed_dev_data.sql
@sources/backend/product-service/src/main/resources/db/migration/V2__add_product_extended_fields.sql
@sources/backend/product-service/src/main/resources/db/migration/V3__add_product_stock.sql

<interfaces>
<!-- product_svc.categories schema (V1__init_schema.sql) -->
categories (
  id VARCHAR(36) PK,
  name VARCHAR(200) NOT NULL,
  slug VARCHAR(220) UNIQUE NOT NULL,
  deleted BOOLEAN NOT NULL DEFAULT FALSE,
  created_at TIMESTAMP WITH TIME ZONE NOT NULL,
  updated_at TIMESTAMP WITH TIME ZONE NOT NULL
)

<!-- product_svc.products schema (V1 + V2 + V3 merged) -->
products (
  id VARCHAR(36) PK,
  name VARCHAR(300) NOT NULL,
  slug VARCHAR(320) UNIQUE NOT NULL,
  category_id VARCHAR(36) NOT NULL FK → categories(id),
  price NUMERIC(12,2) NOT NULL,
  status VARCHAR(20) NOT NULL,            -- 'ACTIVE'
  deleted BOOLEAN NOT NULL DEFAULT FALSE,
  created_at TIMESTAMP WITH TIME ZONE NOT NULL,
  updated_at TIMESTAMP WITH TIME ZONE NOT NULL,
  brand VARCHAR(200),                     -- V2 nullable
  thumbnail_url VARCHAR(500),             -- V2 nullable, full Unsplash URL ~95 char
  short_description VARCHAR(500),         -- V2 nullable, 80-200 chars VN
  original_price NUMERIC(12,2),           -- V2 nullable, ~70% có giá trị
  stock INT NOT NULL DEFAULT 0            -- V3
)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 2.1: Viết V101__seed_catalog_realistic.sql với 4 blocks (soft-delete cũ + insert mới)</name>
  <files>sources/backend/product-service/src/main/resources/db/seed-dev/V101__seed_catalog_realistic.sql</files>
  <read_first>
    - .planning/phases/16-seed-catalog-realistic/16-CONTEXT.md (D-01 đến D-31 — đặc biệt D-04, D-07, D-09, D-12, D-13, D-15, D-18)
    - .planning/phases/16-seed-catalog-realistic/16-RESEARCH.md (Example 1 SQL skeleton, Pitfall 1 slug collision, Pitfall 6 URL length)
    - .planning/phases/16-seed-catalog-realistic/IMAGES.csv (nguồn photo IDs)
    - sources/backend/product-service/src/main/resources/db/seed-dev/V100__seed_dev_data.sql (precedent pattern)
  </read_first>
  <action>
Tạo file `sources/backend/product-service/src/main/resources/db/seed-dev/V101__seed_catalog_realistic.sql` (NEW, không overwrite). KHÔNG đặt trong `db/migration/` (Pitfall 4 — D-02).

**Header comment (bắt buộc, phía trên cùng file):**
```sql
-- V101__seed_catalog_realistic.sql
-- Phase 16 (SEED-01..04): Reset catalog cũ + seed ~100 SP / 5 tech categories.
-- DEV PROFILE ONLY — file này nằm trong db/seed-dev/ KHÔNG chạy khi SPRING_PROFILES_ACTIVE=prod.
-- Idempotent: ON CONFLICT (id) DO NOTHING cho INSERT.
-- Soft-delete: UPDATE deleted=TRUE thay vì DELETE để giữ audit trail + cross-service refs (xem 16-CONTEXT.md D-04, D-05).
```

**Block 1 — Soft-delete 5 categories cũ (D-04):**
```sql
UPDATE product_svc.categories
SET deleted = TRUE, updated_at = NOW()
WHERE id IN ('cat-electronics', 'cat-fashion', 'cat-household', 'cat-books', 'cat-cosmetics');
```

**Block 2 — Soft-delete 10 products cũ (D-04):**
```sql
UPDATE product_svc.products
SET deleted = TRUE, updated_at = NOW()
WHERE id IN ('prod-001','prod-002','prod-003','prod-004','prod-005',
             'prod-006','prod-007','prod-008','prod-009','prod-010');
```

**Block 3 — INSERT 5 categories tech mới (D-07, ON CONFLICT DO NOTHING per D-03):**
```sql
INSERT INTO product_svc.categories (id, name, slug, deleted, created_at, updated_at) VALUES
  ('cat-phone',     'Điện thoại', 'dien-thoai', FALSE, NOW(), NOW()),
  ('cat-laptop',    'Laptop',     'laptop',     FALSE, NOW(), NOW()),
  ('cat-mouse',     'Chuột',      'chuot',      FALSE, NOW(), NOW()),
  ('cat-keyboard',  'Bàn phím',   'ban-phim',   FALSE, NOW(), NOW()),
  ('cat-headphone', 'Tai nghe',   'tai-nghe',   FALSE, NOW(), NOW())
ON CONFLICT (id) DO NOTHING;
```

**Block 4a-4e — INSERT ~20 products mỗi category (5 INSERT statements, group theo category):**

Mỗi statement có cấu trúc:
```sql
INSERT INTO product_svc.products (
  id, name, slug, category_id, brand,
  price, original_price, short_description, thumbnail_url,
  stock, status, deleted, created_at, updated_at
) VALUES
  (...20 rows...)
ON CONFLICT (id) DO NOTHING;
```

**Rules từng row (THỰC HIỆN ĐÚNG concrete values, KHÔNG placeholder):**

- **id**: pattern `prod-{cat3}-{NNN}` (D-09 Claude discretion). cat3 ∈ {pho, lap, mou, key, hea}. NNN từ `001` đến `020` mỗi category. Tổng đạt **100** SP (5 × 20).
- **name**: `"{Brand} {Model/Series}"` per D-10. Vietnamese prefix OK cho generic terms ("Tai nghe", "Bàn phím cơ"), brand+model giữ tiếng Anh gốc. Ví dụ: `'Apple iPhone 15 Pro 256GB'`, `'Logitech MX Master 3S'`, `'Sony WH-1000XM5'`.
- **slug**: kebab-case ASCII unaccent của name (D-11). Bắt buộc UNIQUE — thêm storage/color suffix khi cần để tránh collision (Pitfall 1). Ví dụ `apple-iphone-15-pro-256gb` vs `apple-iphone-15-pro-512gb`. Vietnamese diacritics → unaccent (xem RESEARCH Finding 7 conversion table).
- **category_id**: chính xác 1 trong `cat-phone | cat-laptop | cat-mouse | cat-keyboard | cat-headphone`.
- **brand**: per D-09 brand list:
  - Phone: Apple, Samsung, Xiaomi, OPPO, Vivo, Realme
  - Laptop: Apple, Dell, HP, Lenovo, ASUS, Acer, MSI
  - Mouse: Logitech, Razer, SteelSeries, Microsoft, Apple
  - Keyboard: Keychron, Logitech, Razer, Corsair, Akko, Leopold
  - Headphone: Sony, Bose, Apple, Sennheiser, JBL, Audio-Technica
  - Phải dùng ≥4 brand riêng cho mỗi category để đảm bảo tổng `COUNT(DISTINCT brand) ≥ 15`.
- **price**: NUMERIC(12,2) trong ranges per D-12 (VND):
  - Phone: 3,000,000 — 35,000,000
  - Laptop: 12,000,000 — 60,000,000
  - Mouse: 250,000 — 3,500,000
  - Keyboard: 400,000 — 6,000,000
  - Headphone: 350,000 — 12,000,000
  Format: `28990000.00` (.00 tail bắt buộc).
- **original_price**: per D-13 — **~70% rows** có `original_price > price` (markup 5-25% above price), **~30% rows** = `NULL`. Phân bổ deterministic — viết tay (KHÔNG dùng `random()`). Trong tổng 100 SP, ~70 có giá trị, ~30 NULL. Ví dụ row có discount: `28990000.00, 32990000.00, ...`. Row không discount: `28990000.00, NULL, ...`.
- **short_description**: tiếng Việt 80-200 ký tự per D-23, D-24. Factual specs-driven, KHÔNG marketing fluff. Template: `'{key spec 1}, {key spec 2}, {use case ngắn}'`. Ví dụ: `'Chip A17 Pro, camera 48MP, màn 6.1 inch Super Retina XDR ProMotion'` (66 ký tự — ngắn hơn 80, mở rộng thêm `'... pin 3274mAh, 5G dual SIM'` để đạt 80+).
- **thumbnail_url**: lấy ID từ `IMAGES.csv` — match category đúng. URL pattern (D-18): `https://images.unsplash.com/photo-{ID}?fm=webp&q=80&w=800`. Length ~95 ký tự — dưới VARCHAR(500) nhiều (Pitfall 6 — KHÔNG include `ixid=`/`ixlib=`). Mỗi photo ID dùng đúng 1 lần (KHÔNG reuse — IMAGES.csv có ≥100 unique IDs cho 100 SP).
- **stock**: per D-15, D-16 — distribution viết tay deterministic:
  - **5 SP** có `stock = 0` (out of stock, tổng 5/100 = 5%)
  - **10 SP** có `stock` ∈ [3, 9] (low-stock, 10/100 = 10%)
  - **85 SP** còn lại có `stock` ∈ [15, 150] (chọn variety: 15, 22, 35, 48, 60, 75, 88, 100, 120, 150...)
  - Phân bổ trải đều qua cả 5 categories (mỗi category có ~1 SP stock=0, ~2 SP low-stock, ~17 SP normal).
- **status**: `'ACTIVE'` cho tất cả 100 SP (D-28).
- **deleted**: `FALSE`.
- **created_at**, **updated_at**: `NOW()`.

**Tổng kết file structure (xác nhận trước khi save):**
- Header comment (~5 lines)
- Block 1: 1 UPDATE statement
- Block 2: 1 UPDATE statement
- Block 3: 1 INSERT statement với 5 rows
- Block 4a (phone): 1 INSERT với 20 rows
- Block 4b (laptop): 1 INSERT với 20 rows
- Block 4c (mouse): 1 INSERT với 20 rows
- Block 4d (keyboard): 1 INSERT với 20 rows
- Block 4e (headphone): 1 INSERT với 20 rows

File ước tính ≥130 lines.

**Pre-commit local checks (trước khi save):**
1. Slug uniqueness: tự kiểm grep tất cả slug trong file, sort | uniq -d → empty.
2. URL pattern check: mọi `thumbnail_url` value khớp `https://images.unsplash.com/photo-[^?]+\?fm=webp&q=80&w=800`, length < 500.
3. Stock distribution count: đếm `stock = 0` (=5), `stock < 10 AND stock > 0` (=10), `stock >= 15` (=85).
4. original_price NULL count ≈ 30, NOT NULL count ≈ 70.
5. Distinct brand count ≥ 15 (đếm unique values cột `brand` qua tất cả 100 rows).
  </action>
  <verify>
    <automated>test -f sources/backend/product-service/src/main/resources/db/seed-dev/V101__seed_catalog_realistic.sql && grep -c "INSERT INTO product_svc.products" sources/backend/product-service/src/main/resources/db/seed-dev/V101__seed_catalog_realistic.sql | awk '$1 == 5 {exit 0} {exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - File `sources/backend/product-service/src/main/resources/db/seed-dev/V101__seed_catalog_realistic.sql` tồn tại
    - Đường dẫn KHÔNG phải `db/migration/` (Pitfall 4): `! test -f sources/backend/product-service/src/main/resources/db/migration/V101__seed_catalog_realistic.sql`
    - Header comment chứa dòng `-- DEV PROFILE ONLY` và `-- Phase 16 (SEED-01..04)`
    - Có 2 statement `UPDATE product_svc.categories SET deleted = TRUE` và `UPDATE product_svc.products SET deleted = TRUE`
    - Có 1 statement `INSERT INTO product_svc.categories` với 5 rows (cat-phone, cat-laptop, cat-mouse, cat-keyboard, cat-headphone)
    - Có chính xác 5 statement `INSERT INTO product_svc.products` (1 per category): `grep -c "INSERT INTO product_svc.products" V101*.sql` = 5
    - Tổng số row product values (line bắt đầu với `('prod-`): `grep -c "^\s*('prod-" V101*.sql` = 100
    - Tất cả ID khớp pattern `prod-(pho|lap|mou|key|hea)-\d{3}`: `grep -oE "'prod-(pho|lap|mou|key|hea)-[0-9]{3}'" V101*.sql | wc -l` = 100
    - Mọi thumbnail_url khớp Unsplash WebP pattern: `grep -oE "'https://images\\.unsplash\\.com/photo-[^']+\\?fm=webp&q=80&w=800'" V101*.sql | wc -l` = 100
    - Slug duplicate check: `grep -oE "'[a-z0-9-]+', '(cat-|dien|laptop|chuot|ban|tai)" V101*.sql | sort | uniq -d` empty (no slug collision)
    - File apply thành công khi product-svc restart với SPRING_PROFILES_ACTIVE=dev (kiểm thủ công sau khi save):
      - `docker compose down -v && docker compose up product-service -d` rồi `psql -c "SELECT version FROM product_svc.flyway_schema_history WHERE version='101'"` → 1 row success
      - `psql -c "SELECT COUNT(*) FROM product_svc.categories WHERE deleted=FALSE"` = 5
      - `psql -c "SELECT COUNT(*) FROM product_svc.products WHERE deleted=FALSE"` ∈ [95, 105]
      - `psql -c "SELECT COUNT(DISTINCT brand) FROM product_svc.products WHERE deleted=FALSE"` ≥ 15
      - `psql -c "SELECT COUNT(*) FROM product_svc.products WHERE deleted=FALSE AND thumbnail_url LIKE 'https://images.unsplash.com/%' AND thumbnail_url LIKE '%fm=webp%'"` = COUNT(products active)
  </acceptance_criteria>
  <done>V101 file viết xong với 100 SP đầy đủ thoả mọi acceptance criteria (slug unique, URL WebP, stock distribution, brand domain-tech, status ACTIVE).</done>
</task>

<task type="auto">
  <name>Task 2.2: Patch ROADMAP.md fix V7 → V101 reference cho product-svc Phase 16</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md (sections "Pre-Phase Setup (v1.3)" + "Spring profile `dev` isolation")
    - .planning/phases/16-seed-catalog-realistic/16-CONTEXT.md (D-01 lý do chốt V101)
  </read_first>
  <action>
Sửa `.planning/ROADMAP.md` để dòng reference V7 product-svc khớp với CONTEXT D-01 chốt V101.

**Edit 1** — table "Flyway V-number reservations" (~line 26-30):
- Tìm dòng: `| product-svc | V7 | Seed ~100 sản phẩm (Spring profile `dev` only) | Phase 16 |`
- Thay thành: `| product-svc | V101 | Seed ~100 sản phẩm trong db/seed-dev/ (Spring profile `dev` only) | Phase 16 |`

**Edit 2** — section "Spring profile `dev` isolation" (~line 39-43):
- Tìm dòng: `- Seed migration `V7` phải ở `classpath:db/seed/dev/` (KHÔNG phải `classpath:db/migration/`)`
- Thay thành: `- Seed migration `V101` phải ở `classpath:db/seed-dev/` (KHÔNG phải `classpath:db/migration/`) — tiếp nối V100 đã có`
- Tìm dòng: `- `application-dev.yml` thêm `spring.flyway.locations` include seed path`
- Thay thành: `- `application.yml` profile=dev đã include `classpath:db/seed-dev` trong `spring.flyway.locations` — KHÔNG cần sửa config (verified RESEARCH Finding 1)`

**KHÔNG đụng các phần khác của ROADMAP.md** (giữ nguyên Phase 16 details, success criteria, các phase 17-22).

**Verification:**
```bash
# Verify V7 không còn xuất hiện trong context product-svc Phase 16
grep -n "product-svc.*V7\|V7.*product-svc\|V7.*Seed.*sản phẩm" .planning/ROADMAP.md
# Expect: empty

# Verify V101 reference present
grep -n "V101.*product-svc\|product-svc.*V101\|V101.*Phase 16" .planning/ROADMAP.md
# Expect: ≥1 line

# Verify db/seed-dev path correct
grep -n "db/seed-dev" .planning/ROADMAP.md
# Expect: ≥1 line (nội dung "classpath:db/seed-dev")
```
  </action>
  <verify>
    <automated>grep -q "V101" .planning/ROADMAP.md && ! grep -qE "product-svc \| V7|V7.*Seed.*sản phẩm" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/ROADMAP.md` chứa string `V101` trong context product-svc Phase 16
    - `.planning/ROADMAP.md` KHÔNG còn dòng table với `| product-svc | V7 |`
    - `.planning/ROADMAP.md` KHÔNG còn string `Seed migration `V7``
    - `.planning/ROADMAP.md` chứa `classpath:db/seed-dev` (path đúng theo D-02)
    - Phase 16 details section (Goal + Success Criteria 1-4) GIỮ NGUYÊN không thay đổi
    - Phases 17-22 sections KHÔNG bị edit
  </acceptance_criteria>
  <done>ROADMAP.md drift V7→V101 fix xong, các phần khác giữ nguyên.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Spring profile boundary (dev vs prod) | V101 file nằm `db/seed-dev/` đảm bảo Flyway scan loại trừ trong profile prod |
| Static SQL file ↔ DB | SQL file commit vào source — không có user input runtime |

## STRIDE Threat Register

Phase 16 V101 là Flyway migration với SQL static. Không có user input. Threat surface chủ yếu là profile leak (V101 chạy nhầm trong prod) + slug collision rollback.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-03 | Tampering | V101 SQL apply nhầm trên prod DB | mitigate | File path `db/seed-dev/` (KHÔNG `db/migration/`); header comment "DEV PROFILE ONLY"; Plan 16-03 task 3.3 D-31 negative test verify Flyway history không có V101 ở profile=prod |
| T-16-04 | Denial of Service | Migration fail giữa chừng → Flyway pending state | mitigate | Pre-commit slug uniq check + URL pattern check; Flyway wrap toàn bộ file trong 1 transaction (auto-rollback nếu lỗi); ON CONFLICT DO NOTHING idempotent cho retry |
| T-16-05 | Information Disclosure | thumbnail_url hot-link Unsplash CDN external | accept | Public CDN, Unsplash hot-link policy permitted; FE đã có placeholder fallback nếu URL die (D-22) |
</threat_model>

<verification>
- V101 file path = `db/seed-dev/V101__seed_catalog_realistic.sql`
- Smoke SQL counts (manual sau restart): 5 categories active, 95-105 products active, ≥15 distinct brands, 100% URL WebP, ~10% low-stock, ~5% out-of-stock, ~70% có original_price
- ROADMAP.md V7→V101 drift fixed
- Flyway apply success (Plan 16-03 sẽ verify E2E)
</verification>

<success_criteria>
- SEED-01: 5 cat cũ deleted=TRUE, 5 cat tech mới active
- SEED-02: ~100 SP với name/brand/price/original_price/short_description/stock đầy đủ
- SEED-03: 100% thumbnail_url khớp Unsplash WebP pattern
- SEED-04: V101 nằm db/seed-dev/, idempotent ON CONFLICT
- ROADMAP doc drift fixed
</success_criteria>

<output>
After completion, create `.planning/phases/16-seed-catalog-realistic/16-02-SUMMARY.md` ghi lại: tổng SP seeded, distribution per category, distinct brand count, smoke SQL count results, ROADMAP edit confirm.
</output>
