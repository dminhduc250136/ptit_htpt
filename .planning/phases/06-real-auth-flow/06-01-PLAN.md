---
phase: 06-real-auth-flow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sources/backend/user-service/pom.xml
  - sources/backend/user-service/src/main/resources/application.yml
  - sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/config/PasswordEncoderConfig.java
  - sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/jwt/JwtUtils.java
  - sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/service/AuthService.java
  - sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/web/AuthController.java
  - sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/web/RegisterRequest.java
  - sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/web/LoginRequest.java
  - sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/web/AuthResponseDto.java
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03

must_haves:
  truths:
    - "POST /api/users/auth/register trả 201 + accessToken + UserDto khi credentials hợp lệ"
    - "POST /api/users/auth/register trả 409 khi username hoặc email đã tồn tại"
    - "POST /api/users/auth/login trả 200 + accessToken + UserDto khi credentials đúng"
    - "POST /api/users/auth/login trả 401 khi email không tìm thấy hoặc password sai"
    - "POST /api/users/auth/logout trả 200 OK"
    - "accessToken là JWT HS256 24h, claim: sub=userId, username, roles"
  artifacts:
    - path: "sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/jwt/JwtUtils.java"
      provides: "issueToken(userId, username, roles) → JWT string, JJWT 0.12.7 API"
      exports: ["issueToken", "parseToken"]
    - path: "sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/service/AuthService.java"
      provides: "register() + login() business logic với BCrypt"
    - path: "sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/web/AuthController.java"
      provides: "HTTP layer: POST /auth/register, /auth/login, /auth/logout"
    - path: "sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/config/PasswordEncoderConfig.java"
      provides: "@Bean PasswordEncoder (BCryptPasswordEncoder)"
  key_links:
    - from: "AuthController.register()"
      to: "AuthService.register()"
      via: "Spring @Autowired constructor injection"
    - from: "AuthService.register()"
      to: "JwtUtils.issueToken()"
      via: "gọi trực tiếp sau userRepo.save()"
    - from: "AuthController"
      to: "ApiResponse<AuthResponseDto>"
      via: "ApiResponse.of() manually — bypass ApiResponseAdvice double-wrap"
    - from: "pom.xml spring-security-crypto"
      to: "PasswordEncoderConfig @Bean"
      via: "move scope from test → compile"
---

<objective>
Xây dựng toàn bộ backend auth: thêm JJWT + BCrypt dependencies, tạo JwtUtils, AuthService, AuthController trong user-service. Sau plan này backend có thể nhận register/login/logout requests và trả JWT.

Purpose: AUTH-01, AUTH-02, AUTH-03 — không có backend thật thì FE không thể gỡ mock.
Output: 3 new Java classes (JwtUtils, AuthService, AuthController), 1 config class, 3 record DTOs, updated pom.xml + application.yml.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-real-auth-flow/06-RESEARCH.md
@.planning/phases/06-real-auth-flow/06-CONTEXT.md

<interfaces>
<!-- Extracted từ codebase — executor dùng trực tiếp, không cần explore thêm -->

From sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/domain/UserEntity.java:
```java
// Factory method + accessors (record-style)
public static UserEntity create(String username, String email, String passwordHash, String roles)
public String id()
public String username()
public String email()
public String passwordHash()
public String roles()
```

From sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/domain/UserDto.java:
```java
public record UserDto(String id, String username, String email, String roles, Instant createdAt, Instant updatedAt)
```

From sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/domain/UserMapper.java:
<!-- Cần kiểm tra xem UserMapper đã có toDto() chưa — nếu có dùng luôn, nếu không thì tạo inline -->

From sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/repository/UserRepository.java:
```java
Optional<UserEntity> findByUsername(String username);
Optional<UserEntity> findByEmail(String email);
```

From sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/api/ApiResponse.java:
<!-- Dùng ApiResponse.of(status, message, data) như UserProfileController pattern -->

From sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/api/GlobalExceptionHandler.java:
<!-- ResponseStatusException → ApiErrorResponse (tự động xử lý) -->
<!-- AuthService throw new ResponseStatusException(HttpStatus.CONFLICT, ...) là đủ -->

Existing application.yml structure (user-service):
```yaml
spring:
  application:
    name: user-service
  datasource: ...
  jpa: ...
  flyway: ...
server:
  port: 8080
springdoc: ...
---
spring:
  config:
    activate:
      on-profile: dev
  flyway:
    locations: classpath:db/migration,classpath:db/seed-dev
```
<!-- Thêm app.jwt block vào phần KHÔNG profile-specific (trước ---) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: pom.xml + application.yml — thêm JJWT deps + JWT config</name>
  <files>
    sources/backend/user-service/pom.xml,
    sources/backend/user-service/src/main/resources/application.yml
  </files>
  <action>
**pom.xml — 2 thay đổi:**

1. Tìm block `spring-security-crypto` (hiện có `<scope>test</scope>`) và XÓA dòng `<scope>test</scope>` — để dependency ở compile scope (mặc định). Đây là move scope, không thêm dependency mới. Giữ comment Phase 5.

2. Thêm 3 JJWT dependencies MỚI ngay sau block spring-security-crypto (per D-06, D-11):
```xml
<!-- Phase 6: JJWT 0.12.7 — JWT issue/verify (D-06/D-11) -->
<dependency>
  <groupId>io.jsonwebtoken</groupId>
  <artifactId>jjwt-api</artifactId>
  <version>0.12.7</version>
</dependency>
<dependency>
  <groupId>io.jsonwebtoken</groupId>
  <artifactId>jjwt-impl</artifactId>
  <version>0.12.7</version>
  <scope>runtime</scope>
</dependency>
<dependency>
  <groupId>io.jsonwebtoken</groupId>
  <artifactId>jjwt-jackson</artifactId>
  <version>0.12.7</version>
  <scope>runtime</scope>
</dependency>
```
CRITICAL: `jjwt-impl` và `jjwt-jackson` PHẢI là `runtime` scope — không được để compile scope.

**application.yml — thêm JWT config:**

Thêm block `app:` vào phần không profile-specific (trước dấu `---`), ngay sau phần `springdoc:`:
```yaml
app:
  jwt:
    secret: ${JWT_SECRET:dev-jwt-secret-key-minimum-32-characters-for-hs256-ok}
    expiration-ms: ${JWT_EXPIRATION_MS:86400000}
```
Giá trị fallback dev phải đủ 32+ chars để tránh JJWT WeakKeyException.
  </action>
  <verify>
    <automated>cd sources/backend/user-service && mvn dependency:resolve -q 2>&1 | grep -E "ERROR|WARN|jjwt|spring-security-crypto" || echo "DEPS OK"</automated>
  </verify>
  <done>
    - pom.xml: spring-security-crypto không còn `scope=test`; 3 jjwt deps có mặt với đúng scope
    - application.yml: có `app.jwt.secret` và `app.jwt.expiration-ms`
    - `mvn dependency:resolve` không báo lỗi
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PasswordEncoderConfig + JwtUtils + AuthService + AuthController + DTOs</name>
  <files>
    sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/config/PasswordEncoderConfig.java,
    sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/jwt/JwtUtils.java,
    sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/service/AuthService.java,
    sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/web/AuthController.java,
    sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/web/RegisterRequest.java,
    sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/web/LoginRequest.java,
    sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/web/AuthResponseDto.java
  </files>
  <behavior>
    - register với username/email mới → 201, accessToken non-null, user.username đúng, không có passwordHash trong response
    - register với username đã tồn tại → 409 CONFLICT
    - register với email đã tồn tại → 409 CONFLICT
    - login với email + password đúng → 200, accessToken non-null
    - login với email không tồn tại → 401 UNAUTHORIZED
    - login với password sai → 401 UNAUTHORIZED
    - logout → 200 OK
  </behavior>
  <action>
**1. `config/PasswordEncoderConfig.java`** — package `com.ptit.htpt.userservice.config`:
```java
@Configuration
public class PasswordEncoderConfig {
  @Bean
  public PasswordEncoder passwordEncoder() {
    return new BCryptPasswordEncoder();
  }
}
```
Import: `org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder`, `org.springframework.security.crypto.password.PasswordEncoder`.

**2. `jwt/JwtUtils.java`** — package `com.ptit.htpt.userservice.jwt`:

Dùng JJWT 0.12.x API (KHÔNG dùng `Jwts.parserBuilder()` — đó là 0.11 deprecated):
```java
@Component
public class JwtUtils {

  @Value("${app.jwt.secret}")
  private String jwtSecret;

  @Value("${app.jwt.expiration-ms:86400000}")
  private long expirationMs;

  private SecretKey getSigningKey() {
    return Keys.hmacShaKeyFor(jwtSecret.getBytes(StandardCharsets.UTF_8));
  }

  public String issueToken(String userId, String username, String roles) {
    Instant now = Instant.now();
    return Jwts.builder()
        .subject(userId)
        .claim("username", username)
        .claim("roles", roles)
        .issuedAt(Date.from(now))
        .expiration(Date.from(now.plusMillis(expirationMs)))
        .signWith(getSigningKey(), Jwts.SIG.HS256)
        .compact();
  }

  public Claims parseToken(String token) {
    return Jwts.parser()
        .verifyWith(getSigningKey())
        .build()
        .parseSignedClaims(token)
        .getPayload();
  }
}
```
Imports: `io.jsonwebtoken.{Claims,Jwts}`, `io.jsonwebtoken.security.Keys`, `java.nio.charset.StandardCharsets`, `java.security.Key`, `javax.crypto.SecretKey`.

**3. `web/RegisterRequest.java`** — package `com.ptit.htpt.userservice.web` (record, per D-01):
```java
public record RegisterRequest(
    @NotBlank @Size(min=3, max=80) String username,
    @NotBlank @Email String email,
    @NotBlank @Size(min=6, max=100) String password
) {}
```

**4. `web/LoginRequest.java`** — package `com.ptit.htpt.userservice.web` (record, per D-02):
```java
public record LoginRequest(
    @NotBlank @Email String email,
    @NotBlank String password
) {}
```

**5. `web/AuthResponseDto.java`** — package `com.ptit.htpt.userservice.web` (record, per D-03):
```java
public record AuthResponseDto(
    String accessToken,
    UserDto user
) {}
```
Import `com.ptit.htpt.userservice.domain.UserDto`. KHÔNG có `refreshToken` field (deferred per CONTEXT.md).

**6. `service/AuthService.java`** — package `com.ptit.htpt.userservice.service`:

```java
@Service
@Transactional
public class AuthService {

  private final UserRepository userRepo;
  private final PasswordEncoder passwordEncoder;
  private final JwtUtils jwtUtils;

  public AuthService(UserRepository userRepo, PasswordEncoder passwordEncoder, JwtUtils jwtUtils) {
    this.userRepo = userRepo;
    this.passwordEncoder = passwordEncoder;
    this.jwtUtils = jwtUtils;
  }

  public AuthResponseDto register(RegisterRequest req) {
    if (userRepo.findByUsername(req.username()).isPresent())
      throw new ResponseStatusException(HttpStatus.CONFLICT, "Username already exists");
    if (userRepo.findByEmail(req.email()).isPresent())
      throw new ResponseStatusException(HttpStatus.CONFLICT, "Email already exists");
    String hash = passwordEncoder.encode(req.password());
    UserEntity entity = UserEntity.create(req.username(), req.email(), hash, "USER");
    userRepo.save(entity);
    String token = jwtUtils.issueToken(entity.id(), entity.username(), entity.roles());
    return new AuthResponseDto(token, UserMapper.toDto(entity));
  }

  public AuthResponseDto login(LoginRequest req) {
    UserEntity entity = userRepo.findByEmail(req.email())
        .orElseThrow(() -> new ResponseStatusException(HttpStatus.UNAUTHORIZED, "Invalid credentials"));
    if (!passwordEncoder.matches(req.password(), entity.passwordHash()))
      throw new ResponseStatusException(HttpStatus.UNAUTHORIZED, "Invalid credentials");
    String token = jwtUtils.issueToken(entity.id(), entity.username(), entity.roles());
    return new AuthResponseDto(token, UserMapper.toDto(entity));
  }
}
```
Trước khi dùng `UserMapper.toDto()`, đọc `UserMapper.java` — nếu method chưa có, tạo inline mapping thay thế: `new UserDto(entity.id(), entity.username(), entity.email(), entity.roles(), entity.createdAt(), entity.updatedAt())`.

**7. `web/AuthController.java`** — package `com.ptit.htpt.userservice.web` (per D-12):

CRITICAL: Trả `ApiResponse<AuthResponseDto>` manually — KHÔNG trả plain `AuthResponseDto`. ApiResponseAdvice pass-through khi `instanceof ApiResponse<?>` → không double-wrap:

```java
@RestController
@RequestMapping("/auth")
@Validated
public class AuthController {

  private final AuthService authService;

  public AuthController(AuthService authService) {
    this.authService = authService;
  }

  @PostMapping("/register")
  @ResponseStatus(HttpStatus.CREATED)
  public ApiResponse<AuthResponseDto> register(@Valid @RequestBody RegisterRequest request) {
    return ApiResponse.of(201, "Registered successfully", authService.register(request));
  }

  @PostMapping("/login")
  public ApiResponse<AuthResponseDto> login(@Valid @RequestBody LoginRequest request) {
    return ApiResponse.of(200, "Login successful", authService.login(request));
  }

  @PostMapping("/logout")
  public ApiResponse<Void> logout() {
    return ApiResponse.of(200, "Logged out", null);
  }
}
```

Path `/auth` map qua API gateway như `/api/users/auth/*` per D-12.

Imports cần: `com.ptit.htpt.userservice.api.ApiResponse`, `jakarta.validation.Valid`, `org.springframework.validation.annotation.Validated`, `org.springframework.http.HttpStatus`, `org.springframework.web.bind.annotation.*`.
  </action>
  <verify>
    <automated>cd sources/backend/user-service && mvn compile -q 2>&1 | tail -20</automated>
  </verify>
  <done>
    - `mvn compile` thành công, không có errors
    - 7 files mới tồn tại với đúng package declarations
    - AuthController trả `ApiResponse&lt;AuthResponseDto&gt;` (không plain AuthResponseDto)
    - AuthService dùng `ResponseStatusException` cho 409/401 (không throws checked exceptions)
    - JwtUtils dùng `Jwts.parser()` (0.12.x API), không dùng `Jwts.parserBuilder()` (0.11 deprecated)
    - RegisterRequest/LoginRequest là Jakarta records với `@Valid` annotations
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → AuthController | Untrusted input: username/email/password từ HTTP body |
| AuthController → JwtUtils | JWT secret không expose ra response |
| JWT → Backend claims | Claims trong JWT chưa verify tại gateway (deferred D14) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Spoofing | AuthService.login() | mitigate | Generic "Invalid credentials" message cho cả email-not-found và password-wrong — không tiết lộ field nào sai |
| T-06-02 | Tampering | JwtUtils.issueToken() | mitigate | HS256 signature; secret via `${JWT_SECRET}` env var, không hardcode plain text trong yml |
| T-06-03 | Information Disclosure | AuthResponseDto | mitigate | UserDto không có passwordHash field (VERIFIED: UserDto.java chỉ có id/username/email/roles/timestamps) |
| T-06-04 | Elevation of Privilege | PasswordEncoderConfig | mitigate | BCryptPasswordEncoder standalone — không enable Spring Security filter chain, không intercept /auth/* với SecurityFilterChain |
| T-06-05 | Denial of Service | AuthController.login() | accept | Brute-force protection deferred per CONTEXT.md Deferred section — MVP visible-first priority |
| T-06-06 | Tampering | JWT secret key length | mitigate | Dev fallback 32+ chars (`dev-jwt-secret-key-minimum-32-characters-for-hs256-ok`) — JJWT enforce minimum 256 bits cho HS256, WeakKeyException thrown nếu ngắn hơn |
</threat_model>

<verification>
Sau khi Plan 01 hoàn thành:
1. `cd sources/backend/user-service && mvn compile` — PASS, no errors
2. `cd sources/backend/user-service && mvn test -Dtest=AuthControllerTest` — nếu test file đã có (từ Wave 0 nếu tạo trước)
3. Docker smoke (sau khi Plan 03 merge): `curl -X POST http://localhost:8080/api/users/auth/register -H "Content-Type: application/json" -d '{"username":"testuser","email":"test@test.com","password":"password123"}' | jq .`
   - Expected: `{ "status": 201, "data": { "accessToken": "eyJ...", "user": { "username": "testuser" } } }`
4. Duplicate check: gọi lại request trên → expect 409 CONFLICT
</verification>

<success_criteria>
- `mvn compile` xanh trên user-service
- 3 JJWT artifacts trong pom.xml với đúng scope (api=compile, impl=runtime, jackson=runtime)
- spring-security-crypto không còn test scope
- AuthController trả `ApiResponse<AuthResponseDto>` (không double-wrap)
- POST /auth/register trả 201 + JWT + UserDto
- POST /auth/login trả 200 + JWT + UserDto
- POST /auth/logout trả 200
- 409 khi username/email duplicate
- 401 khi wrong credentials
</success_criteria>

<output>
Sau khi hoàn thành, tạo `.planning/phases/06-real-auth-flow/06-01-SUMMARY.md` theo template.
</output>
