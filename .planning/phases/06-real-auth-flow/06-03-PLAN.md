---
phase: 06-real-auth-flow
plan: 03
type: execute
wave: 2
depends_on:
  - "06-01"
  - "06-02"
files_modified:
  - sources/frontend/src/app/login/page.tsx
  - sources/frontend/src/app/register/page.tsx
  - sources/frontend/src/app/403/page.tsx
  - sources/frontend/src/app/403/page.module.css
  - sources/frontend/middleware.ts
autonomous: true
requirements:
  - AUTH-03
  - AUTH-04
  - AUTH-05
  - AUTH-06

must_haves:
  truths:
    - "Login page gọi real services/auth.login() thay vì mock setTimeout"
    - "Login 401 hiển thị Banner lỗi đúng copy (không highlight field)"
    - "Register page có username field (first), không có fullName/phone"
    - "Register 409 username conflict → error text đúng per UI-SPEC"
    - "Register 409 email conflict → error text đúng per UI-SPEC"
    - "Register thành công → auto-login → redirect / (không qua /login)"
    - "middleware.ts matcher có /account/:path*"
    - "/admin/* với user_role không phải ADMIN → redirect /403"
    - "/403 page render với đúng copy và Button 'Về trang chủ'"
  artifacts:
    - path: "sources/frontend/src/app/login/page.tsx"
      provides: "Login page wired với real auth.login()"
    - path: "sources/frontend/src/app/register/page.tsx"
      provides: "Register page: username field + real auth.register() + auto-login"
    - path: "sources/frontend/src/app/403/page.tsx"
      provides: "/403 page per UI-SPEC"
    - path: "sources/frontend/src/app/403/page.module.css"
      provides: "403 page styles: centered, max-width 440px"
    - path: "sources/frontend/middleware.ts"
      provides: "matcher + /account/:path* + admin role check → /403"
  key_links:
    - from: "login/page.tsx handleSubmit"
      to: "services/auth.login()"
      via: "import { login } from '@/services/auth'"
    - from: "register/page.tsx handleSubmit"
      to: "services/auth.register()"
      via: "import { register } from '@/services/auth'"
    - from: "middleware.ts /admin check"
      to: "user_role cookie"
      via: "req.cookies.get('user_role')?.value"
    - from: "middleware /403 redirect"
      to: "app/403/page.tsx"
      via: "NextResponse.redirect(new URL('/403', req.url))"
---

<objective>
Wire FE pages vào real auth backend và bổ sung route protection: gỡ mock trong login/register pages, refactor register form fields, cập nhật middleware thêm /account/* và admin role check, tạo /403 page.

Purpose: Hoàn tất toàn bộ end-user visible auth flow — sau plan này user có thể register, login, và protected routes hoạt động đúng.
Output: 2 pages wired + 1 new page + middleware updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-real-auth-flow/06-CONTEXT.md
@.planning/phases/06-real-auth-flow/06-RESEARCH.md
@.planning/phases/06-real-auth-flow/06-UI-SPEC.md
@.planning/phases/06-real-auth-flow/06-01-SUMMARY.md
@.planning/phases/06-real-auth-flow/06-02-SUMMARY.md

<interfaces>
<!-- Verified từ codebase reads — executor dùng trực tiếp -->

services/auth.ts exports (sau Plan 02):
```typescript
export async function login(body: LoginRequest): Promise<AuthResponse>
export async function register(body: RegisterRequest): Promise<AuthResponse>
export function logout(): void
```

services/token.ts exports (sau Plan 02):
```typescript
export function setTokens(access: string, refresh?: string | null): void
export function clearTokens(): void
export function setUserRole(role: string): void
export function clearUserRole(): void
```

AuthProvider (providers/AuthProvider.tsx):
```typescript
// useAuth() returns { login: authLogin, logout: authLogout, user }
// authLogin({ id: string, email: string, name: string }) — note: name field (không phải username)
// Mapping: name: data.user.username (display name mapping — KHÔNG refactor AuthProvider)
```

ApiError (services/errors.ts):
```typescript
// import { ApiError } from '@/services/errors'
// err instanceof ApiError → err.status (number), err.fieldErrors (FieldError[])
// FieldError: { field: string, message: string }
```

Button component:
```typescript
// <Button variant="primary" size="lg" loading={loading} disabled={loading} fullWidth>
// loading prop: hiển thị spinner thay text
```

Input component:
```typescript
// <Input label="..." placeholder="..." value={...} onChange={...} error="..." icon={<svg/>} />
// error prop: hiển thị error text bên dưới, đổi border sang --error color
```

Banner component:
```typescript
// <Banner count={N} message="..." /> — hiển thị khi có multiple errors hoặc server error
// count=1 cho server errors, count=N cho field validation errors
```

Existing login page structure (để tham khảo diff):
- import setTokens từ @/services/token — THAY bằng import login từ @/services/auth
- Mock block: await setTimeout, setTokens('mock-...'), authLogin mock data — XÓA VÀ THAY

Existing register page structure:
- form state: { fullName, email, phone, password, confirmPassword } — REFACTOR thành { username, email, password, confirmPassword }
- Validation fullName → validation username
- Mock block: XÓA VÀ THAY

middleware.ts hiện tại:
```typescript
matcher: ['/checkout/:path*', '/profile/:path*', '/admin/:path*']
// Thêm '/account/:path*' per D-07
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire login page + refactor register page</name>
  <files>
    sources/frontend/src/app/login/page.tsx,
    sources/frontend/src/app/register/page.tsx
  </files>
  <action>
**login/page.tsx — gỡ mock, wire real backend:**

Đọc file đầy đủ trước khi edit.

Thay đổi imports: bỏ `import { setTokens } from '@/services/token'`, thêm:
```typescript
import { login } from '@/services/auth';
import { ApiError } from '@/services/errors';
```

Thêm state `apiError` riêng cho server errors (ngoài field errors):
```typescript
const [apiError, setApiError] = useState<string | null>(null);
```

Thay toàn bộ mock block trong `handleSubmit` bằng:
```typescript
setErrors({});
setApiError(null);
setLoading(true);
try {
  const data = await login({ email, password });
  authLogin({ id: data.user.id, email: data.user.email, name: data.user.username ?? data.user.email });
  router.replace(returnTo);
} catch (err: unknown) {
  if (err instanceof ApiError && err.status === 401) {
    // Per UI-SPEC: 401 → Banner form-level (không highlight field)
    setApiError('Email hoặc mật khẩu không chính xác. Vui lòng thử lại');
  } else {
    setApiError('Có lỗi xảy ra, vui lòng thử lại');
  }
} finally {
  setLoading(false);
}
```

Thêm Banner hiển thị `apiError` ở đầu form (trước fields), nếu chưa có:
```tsx
{apiError && <Banner count={1} message={apiError} />}
```

Giữ nguyên: field validation (email trống / password trống), returnTo hardening, CSS modules, tất cả JSX structure.

---

**register/page.tsx — refactor fields + wire real backend:**

Đọc file đầy đủ trước khi edit.

1. Thay đổi imports: bỏ `import { setTokens } from '@/services/token'`, thêm:
```typescript
import { register } from '@/services/auth';
import { ApiError } from '@/services/errors';
```

2. Refactor form state: `{ fullName, email, phone, password, confirmPassword }` → `{ username, email, password, confirmPassword }`:
```typescript
const [form, setForm] = useState({
  username: '',
  email: '',
  password: '',
  confirmPassword: '',
});
```

3. Thêm `apiError` state:
```typescript
const [apiError, setApiError] = useState<string | null>(null);
```

4. Cập nhật validation trong `handleSubmit` (xóa fullName/phone validation, thêm username):
```typescript
const newErrors: Record<string, string> = {};
if (!form.username.trim()) newErrors.username = 'Vui lòng nhập tên đăng nhập';
else if (form.username.trim().length < 3) newErrors.username = 'Tên đăng nhập ít nhất 3 ký tự';
if (!form.email.trim()) newErrors.email = 'Vui lòng nhập email';
else if (!/\S+@\S+\.\S+/.test(form.email)) newErrors.email = 'Email không hợp lệ';
if (!form.password) newErrors.password = 'Vui lòng nhập mật khẩu';
else if (form.password.length < 6) newErrors.password = 'Mật khẩu ít nhất 6 ký tự';
if (form.password !== form.confirmPassword)
  newErrors.confirmPassword = 'Mật khẩu không khớp';
```

5. Thay mock block bằng real register call (per D-04 auto-login):
```typescript
setErrors({});
setApiError(null);
setLoading(true);
try {
  const data = await register({
    username: form.username.trim(),
    email: form.email.trim(),
    password: form.password,
  });
  // D-04: auth.register() đã gọi setTokens + setUserRole nội bộ
  authLogin({ id: data.user.id, email: data.user.email, name: data.user.username ?? data.user.email });
  router.replace('/');  // D-04: redirect về trang chủ (không qua /login)
} catch (err: unknown) {
  if (err instanceof ApiError) {
    if (err.status === 409) {
      // Per UI-SPEC: 409 → highlight field cụ thể
      const msg = err.message ?? '';
      if (msg.toLowerCase().includes('username')) {
        setErrors({ username: 'Tên đăng nhập này đã được sử dụng' });
      } else if (msg.toLowerCase().includes('email')) {
        setErrors({ email: 'Email này đã được đăng ký. Đăng nhập' });
      } else {
        setApiError(err.message ?? 'Có lỗi xảy ra, vui lòng thử lại');
      }
    } else {
      setApiError('Có lỗi xảy ra, vui lòng thử lại');
    }
  } else {
    setApiError('Có lỗi xảy ra, vui lòng thử lại');
  }
} finally {
  setLoading(false);
}
```

6. Cập nhật JSX fields theo UI-SPEC order (username first):
- XÓA: fullName field JSX, phone field JSX
- THÊM: username field ở vị trí đầu tiên:
```tsx
<Input
  label="Tên đăng nhập"
  placeholder="ten_dang_nhap"
  type="text"
  value={form.username}
  onChange={(e) => update('username', e.target.value)}
  error={errors.username}
  icon={/* SVG user-circle 18x18, giống icon hiện tại của fullName */}
/>
```
- GIỮU nguyên: email, password, confirmPassword fields
- THÊM Banner hiển thị `apiError` ở đầu form nếu chưa có

Copy label/placeholder phải khớp chính xác với UI-SPEC:
- username: label="Tên đăng nhập", placeholder="ten_dang_nhap"
- Primary CTA: "Tạo tài khoản" (giữ nguyên)
  </action>
  <verify>
    <automated>cd sources/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - login/page.tsx: không còn import setTokens, không còn setTimeout mock, có try/catch với login()
    - register/page.tsx: form state có username (không có fullName/phone), form JSX có username field đầu tiên
    - register: auto-login sau success redirect về '/' (không '/login')
    - 409 conflict: username → "Tên đăng nhập này đã được sử dụng"; email → "Email này đã được đăng ký. Đăng nhập"
    - `npx tsc --noEmit` không có errors mới
  </done>
</task>

<task type="auto">
  <name>Task 2: middleware.ts update + /403 page tạo mới</name>
  <files>
    sources/frontend/middleware.ts,
    sources/frontend/src/app/403/page.tsx,
    sources/frontend/src/app/403/page.module.css
  </files>
  <action>
**middleware.ts — 2 thay đổi (per D-07, D-08, D-09):**

Đọc file trước khi edit. Thay toàn bộ nội dung:

```typescript
/**
 * Route protection middleware — presence-check + admin role check.
 *
 * Source: 04-RESEARCH.md §Pattern 4 + Phase 6 D-07/D-08/D-09.
 *
 * D-07: matcher thêm /account/:path*
 * D-08: admin role check đọc user_role cookie (non-httpOnly, set khi login)
 * D-09: non-ADMIN truy cập /admin/* → redirect /403
 * D-10: unauthenticated → redirect /login?returnTo=<path>
 *
 * NOTE: Edge Runtime không thể verify JWT signature — dùng user_role cookie
 * approach (D-08). Cookie chỉ cho UX redirect, không phải security enforcement thật.
 * Backend vẫn validate JWT trên mỗi API call qua Authorization: Bearer header.
 */

import { NextRequest, NextResponse } from 'next/server';

export function middleware(req: NextRequest) {
  const authPresent = req.cookies.get('auth_present')?.value;
  if (!authPresent) {
    const returnTo = encodeURIComponent(req.nextUrl.pathname + req.nextUrl.search);
    const loginUrl = new URL(`/login?returnTo=${returnTo}`, req.url);
    return NextResponse.redirect(loginUrl);
  }

  // Admin route check — Edge Runtime cannot verify JWT signature (D-08).
  // user_role cookie set at login by token.ts setUserRole().
  if (req.nextUrl.pathname.startsWith('/admin')) {
    const userRole = req.cookies.get('user_role')?.value;
    // includes() safe cho "ADMIN" single value và "ADMIN,USER" comma-separated
    if (!userRole?.includes('ADMIN')) {
      return NextResponse.redirect(new URL('/403', req.url));
    }
  }

  return NextResponse.next();
}

export const config = {
  // D-07: thêm /account/:path*
  matcher: ['/checkout/:path*', '/profile/:path*', '/admin/:path*', '/account/:path*'],
};
```

---

**app/403/page.tsx — tạo mới (per D-09, UI-SPEC §/403 Page):**

```typescript
'use client';

import Link from 'next/link';
import Button from '@/components/ui/Button/Button';
import styles from './page.module.css';

export default function ForbiddenPage() {
  return (
    <div className={styles.page}>
      <div className={styles.container}>
        <h1 className={styles.title}>Không có quyền truy cập</h1>
        <p className={styles.body}>Bạn không có quyền xem trang này.</p>
        <Link href="/" className={styles.link}>
          <Button variant="primary" size="lg" fullWidth>Về trang chủ</Button>
        </Link>
      </div>
    </div>
  );
}
```

---

**app/403/page.module.css — tạo mới (per UI-SPEC §/403 Page Layout Spec):**

```css
/* /403 Forbidden Page
 * Per UI-SPEC: centered vertically + horizontally, full viewport height.
 * Max-width 440px. Background --surface (không dùng gradient).
 * Không có header/footer navigation.
 */

.page {
  display: flex;
  align-items: center;
  justify-content: center;
  min-height: 100vh;
  background: var(--surface);
  padding: var(--space-4);   /* 24px */
}

.container {
  max-width: 440px;
  width: 100%;
  text-align: center;
}

.title {
  font-size: var(--text-headline-md);   /* 28px */
  font-weight: var(--weight-bold);      /* 700 */
  line-height: var(--leading-tight);    /* 1.2 */
  color: var(--on-surface);
}

.body {
  font-size: var(--text-body-lg);       /* 16px */
  font-weight: var(--weight-regular);   /* 400 */
  line-height: var(--leading-relaxed);  /* 1.6 */
  color: var(--on-surface-variant);
  margin-top: var(--space-3);           /* 16px */
}

.link {
  display: block;
  margin-top: var(--space-5);           /* 32px */
  text-decoration: none;
}
```

CSS tokens phải dùng đúng tên từ globals.css (`--text-headline-md`, `--weight-bold`, `--leading-tight`, `--on-surface`, `--on-surface-variant`, `--text-body-lg`, `--weight-regular`, `--leading-relaxed`, `--space-3`, `--space-4`, `--space-5`, `--surface`). Kiểm tra globals.css nếu cần xác nhận tên token.
  </action>
  <verify>
    <automated>cd sources/frontend && npx tsc --noEmit 2>&1 | head -20 && ls src/app/403/</automated>
  </verify>
  <done>
    - middleware.ts matcher có 4 entries bao gồm '/account/:path*'
    - middleware.ts có admin check: req.nextUrl.pathname.startsWith('/admin') + user_role cookie
    - Non-admin redirect đến '/403' (không '/admin' hay lỗi khác)
    - app/403/page.tsx và page.module.css tồn tại
    - /403 page: h1 "Không có quyền truy cập", p "Bạn không có quyền xem trang này.", Button "Về trang chủ"
    - CSS dùng --surface background (không gradient), max-width 440px, centered
    - `npx tsc --noEmit` xanh
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → middleware (Edge) | user_role cookie có thể bị forge bởi JS/XSS |
| Login returnTo param | Open redirect nếu không validate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-10 | Spoofing | middleware admin check | accept | user_role cookie non-httpOnly — có thể bị forge. Accepted MVP; middleware chỉ làm UX redirect, backend validate JWT trên mỗi API call |
| T-06-11 | Spoofing | login page returnTo | mitigate | returnTo validation đã có: `startsWith('/') && !startsWith('//')` (VERIFIED: login/page.tsx line 27) — giữ nguyên khi refactor |
| T-06-12 | Elevation of Privilege | /admin/* access | mitigate | middleware redirect non-ADMIN → /403; backend API luôn require valid JWT qua Authorization header |
</threat_model>

<verification>
Smoke test toàn bộ Phase 6 (thực hiện sau khi cả 3 plans merge và `docker compose up`):

**AUTH-01 + AUTH-02: Backend endpoints**
```bash
# Register thành công
curl -s -X POST http://localhost:8080/api/users/auth/register \
  -H "Content-Type: application/json" \
  -d '{"username":"smoketest","email":"smoke@test.com","password":"password123"}' | jq .
# Expected: status=201, data.accessToken non-null, data.user.username="smoketest"

# Register duplicate username
curl -s -X POST http://localhost:8080/api/users/auth/register \
  -H "Content-Type: application/json" \
  -d '{"username":"smoketest","email":"other@test.com","password":"password123"}' | jq .
# Expected: 409 CONFLICT

# Login đúng credentials
curl -s -X POST http://localhost:8080/api/users/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"smoke@test.com","password":"password123"}' | jq .
# Expected: status=200, data.accessToken non-null

# Login sai password
curl -s -X POST http://localhost:8080/api/users/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"smoke@test.com","password":"wrongpass"}' | jq .
# Expected: 401 UNAUTHORIZED
```

**AUTH-03 + AUTH-04: FE pages**
- Truy cập http://localhost:3000/login → điền email/password → Submit → redirect về /
- Truy cập http://localhost:3000/register → có 4 fields (Tên đăng nhập, Email, Mật khẩu, Xác nhận mật khẩu) → Register thành công → redirect về /
- Đăng ký username đã tồn tại → hiển thị "Tên đăng nhập này đã được sử dụng"

**AUTH-05: Middleware**
- Logout (clear cookies) → truy cập http://localhost:3000/account/orders → redirect /login?returnTo=%2Faccount%2Forders
- Login với USER role → truy cập http://localhost:3000/admin → redirect /403
- /403 page render đúng: h1 "Không có quyền truy cập", Button "Về trang chủ"

**AUTH-06: Session persist**
- Login → F5 reload → vẫn logged in (không bị kick ra /login)
</verification>

<success_criteria>
- login/page.tsx: mock code đã xóa, real login() call, 401 → Banner lỗi
- register/page.tsx: username field có, fullName/phone fields không còn, real register() call, D-04 auto-login, 409 → field errors đúng copy
- middleware.ts: 4 matcher entries, admin check present
- /403 page: 2 files tồn tại, copy đúng per UI-SPEC
- `npx tsc --noEmit` xanh (toàn bộ FE)
- `docker compose up` + smoke test 7 scenarios trên → tất cả PASS
</success_criteria>

<output>
Sau khi hoàn thành, tạo `.planning/phases/06-real-auth-flow/06-03-SUMMARY.md` theo template.
</output>
