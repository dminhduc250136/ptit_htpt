---
phase: 21-hoan-thien-reviews
plan: 02
type: execute
wave: 2
depends_on: [21-01]
files_modified:
  - sources/backend/product-service/src/main/java/com/ptit/htpt/productservice/service/ReviewService.java
  - sources/backend/product-service/src/main/java/com/ptit/htpt/productservice/service/AdminReviewDTO.java
  - sources/backend/product-service/src/main/java/com/ptit/htpt/productservice/web/ReviewController.java
  - sources/backend/product-service/src/main/java/com/ptit/htpt/productservice/web/AdminReviewController.java
  - sources/backend/product-service/src/test/java/com/ptit/htpt/productservice/service/ReviewServiceTest.java
  - sources/backend/product-service/src/test/java/com/ptit/htpt/productservice/web/ReviewControllerTest.java
  - sources/backend/product-service/src/test/java/com/ptit/htpt/productservice/web/AdminReviewControllerTest.java
autonomous: true
requirements: [REV-04, REV-05, REV-06]
must_haves:
  truths:
    - "PATCH /api/products/{productId}/reviews/{reviewId} cho phép owner sửa review trong 24h; trả 403 cho non-owner; 422 REVIEW_EDIT_WINDOW_EXPIRED khi quá hạn; 422 REVIEW_NOT_FOUND khi review đã soft-delete"
    - "DELETE /api/products/{productId}/reviews/{reviewId} cho phép owner soft-delete (set deleted_at), 403 cho non-owner, 204 success"
    - "GET /api/products/{productId}/reviews?sort=newest|rating_desc|rating_asc trả review list theo đúng sort; invalid value fallback newest (KHÔNG 400)"
    - "GET /api/products/admin/reviews?filter=all|visible|hidden|deleted (Authorization: Bearer admin) trả paginated AdminReviewDTO với productSlug, hidden, deletedAt"
    - "PATCH /api/products/admin/reviews/{reviewId}/visibility body {hidden:true|false} cập nhật + recompute avg_rating"
    - "DELETE /api/products/admin/reviews/{reviewId} hard-delete row + recompute avg_rating"
    - "ReviewService.recomputeProductRating(productId) helper cô lập, được gọi ở: editReview (chỉ khi rating đổi), softDeleteReview, setVisibility, hardDelete (5 path mới + 1 path cũ createReview = 6 calls tối thiểu)"
    - "Public listReviews response embed config.editWindowHours cho FE đọc (D-02)"
  artifacts:
    - path: "sources/backend/product-service/src/main/java/com/ptit/htpt/productservice/web/ReviewController.java"
      provides: "PATCH /{reviewId} (author edit) + DELETE /{reviewId} (author soft-delete) + GET với sort param"
      contains: "@PatchMapping(\"/{reviewId}\")"
    - path: "sources/backend/product-service/src/main/java/com/ptit/htpt/productservice/web/AdminReviewController.java"
      provides: "Admin moderation endpoints (list/visibility/hardDelete) tại /admin/products/reviews"
      contains: "@RequestMapping(\"/admin/products/reviews\")"
    - path: "sources/backend/product-service/src/main/java/com/ptit/htpt/productservice/service/AdminReviewDTO.java"
      provides: "Record DTO cho admin list response (id, productId, productSlug, reviewerName, rating, content, hidden, deletedAt, createdAt)"
      contains: "public record AdminReviewDTO"
    - path: "sources/backend/product-service/src/test/java/com/ptit/htpt/productservice/web/AdminReviewControllerTest.java"
      provides: "MockMvc tests admin role guard + 3 endpoint happy path"
      contains: "401\\|403\\|200"
  key_links:
    - from: "ReviewController.editReview"
      to: "ReviewService.editReview → recomputeProductRating (only if rating changed)"
      via: "PATCH endpoint → service mutation → conditional recompute"
      pattern: "if \\(newRating .* != oldRating\\)"
    - from: "AdminReviewController.list"
      to: "JwtRoleGuard.requireAdmin + ReviewService.listAdminReviews"
      via: "Bearer auth + Specification filter"
      pattern: "jwtRoleGuard\\.requireAdmin"
    - from: "ReviewService.recomputeProductRating"
      to: "ProductRepository.findById + computeStats + updateRatingStats"
      via: "Helper invariant — gọi ở 5 mutation path mới"
      pattern: "recomputeProductRating\\("
---

<objective>
Triển khai 5 endpoint mới + 5 service method + recompute invariant + extensive tests trên BE. Wave 2, blocking cho 21-03 (FE author UX) và 21-04 (FE admin + E2E).

Purpose: Phase 21 BE complete — REV-04 author edit/delete (3 endpoints PATCH/DELETE), REV-05 sort (extend GET), REV-06 admin moderation (3 endpoints). Test coverage cho mọi auth path + window enforcement + recompute scope.

Output: ReviewController extended, AdminReviewController created, ReviewService với 5 method mới + recompute helper isolated, AdminReviewDTO record, 3 test class với coverage cho ownership / window / role-guard / recompute invariants.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-hoan-thien-reviews/21-CONTEXT.md
@.planning/phases/21-hoan-thien-reviews/21-RESEARCH.md
@.planning/phases/21-hoan-thien-reviews/21-PATTERNS.md
@.planning/phases/21-hoan-thien-reviews/21-01-SUMMARY.md
@sources/backend/product-service/src/main/java/com/ptit/htpt/productservice/service/ReviewService.java
@sources/backend/product-service/src/main/java/com/ptit/htpt/productservice/web/ReviewController.java
@sources/backend/product-service/src/main/java/com/ptit/htpt/productservice/web/JwtRoleGuard.java
@sources/backend/product-service/src/main/java/com/ptit/htpt/productservice/web/AdminProductController.java
@sources/backend/product-service/src/main/java/com/ptit/htpt/productservice/web/AdminStatsController.java
@sources/backend/product-service/src/main/java/com/ptit/htpt/productservice/domain/ProductDto.java
@sources/backend/product-service/src/main/java/com/ptit/htpt/productservice/api/ApiResponse.java
@sources/backend/product-service/src/test/java/com/ptit/htpt/productservice/service/ReviewServiceTest.java
@sources/backend/product-service/src/test/java/com/ptit/htpt/productservice/web/ReviewControllerTest.java

<interfaces>
<!-- Contracts EXPOSED bởi 21-01 + Phase 13 baseline mà 21-02 tiêu thụ -->

From ReviewEntity (Phase 21-01 extension):
```java
public void markDeleted();
public void setHidden(boolean hidden);
public void applyEdit(int newRating, String sanitizedContent);
public Instant deletedAt();
public boolean hidden();
public boolean isActive();
// + Phase 13 accessors: id(), productId(), userId(), reviewerName(), rating(), content(), createdAt(), updatedAt()
```

From ReviewRepository (Phase 21-01 extension):
```java
Page<ReviewEntity> findByProductIdAndDeletedAtIsNullAndHiddenFalse(String productId, Pageable pageable);
boolean existsByProductIdAndUserIdAndDeletedAtIsNull(String productId, String userId);
Optional<ReviewEntity> findByIdAndDeletedAtIsNull(String reviewId);
@Query("SELECT COALESCE(AVG(r.rating), 0), COUNT(r) FROM ReviewEntity r WHERE r.productId = :productId AND r.deletedAt IS NULL AND r.hidden = false")
Object[] computeStats(@Param("productId") String productId);
// inherited: findAll(Specification<ReviewEntity>, Pageable), findById, save, delete
```

From AdminReviewSpecifications:
```java
public static Specification<ReviewEntity> withFilter(String filter);
```

From ProductEntity (Phase 13):
```java
public void updateRatingStats(BigDecimal avgRating, int reviewCount);
public String slug();
public String name();
```

From JwtRoleGuard (existing):
```java
public void requireAdmin(String authHeader);  // throws 401/403
// + parseToken pattern via @Value jwt secret
```

From ApiResponse:
```java
public static <T> ApiResponse<T> of(int status, String message, T data);
```

NEW endpoints exposed by this plan (consumed by FE plans 21-03, 21-04):
```
PATCH  /api/products/{productId}/reviews/{reviewId}            → 200 {data: review} | 403 | 422
DELETE /api/products/{productId}/reviews/{reviewId}            → 204
GET    /api/products/{productId}/reviews?sort=...&page=&size=  → 200 {data: {content[], config{editWindowHours}, ...}}
GET    /api/products/admin/reviews?filter=&page=&size=         → 200 {data: {content: AdminReviewDTO[], ...}}
PATCH  /api/products/admin/reviews/{reviewId}/visibility       → 200 (body {hidden: boolean})
DELETE /api/products/admin/reviews/{reviewId}                  → 204 (hard-delete)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Mở rộng ReviewService — recompute helper isolation + 5 mutation methods + listReviews sort + listAdminReviews + AdminReviewDTO + edit-window @Value</name>
  <read_first>
    - sources/backend/product-service/src/main/java/com/ptit/htpt/productservice/service/ReviewService.java (toàn bộ — Phase 13 baseline)
    - sources/backend/product-service/src/main/java/com/ptit/htpt/productservice/domain/ProductDto.java (record DTO style)
    - sources/backend/product-service/src/main/java/com/ptit/htpt/productservice/repository/ReviewRepository.java (sau 21-01 extend)
    - sources/backend/product-service/src/main/java/com/ptit/htpt/productservice/domain/ProductEntity.java (updateRatingStats signature)
    - .planning/phases/21-hoan-thien-reviews/21-PATTERNS.md (§ReviewService verbatim methods + AdminReviewDTO)
    - .planning/phases/21-hoan-thien-reviews/21-RESEARCH.md (Finding 6 — @Value pattern, Approach Summary [1]-[5])
    - .planning/phases/21-hoan-thien-reviews/21-01-SUMMARY.md (interfaces exposed)
  </read_first>
  <behavior>
    - Test: `editReview(reviewId, ownerId, 4, "edited")` trong 24h → return updated review map; rating thay đổi → recomputeProductRating gọi 1 lần.
    - Test: `editReview` non-owner → throw 403 `REVIEW_NOT_OWNER`.
    - Test: `editReview` past 24h (set `app.reviews.edit-window-hours=0`) → throw 422 `REVIEW_EDIT_WINDOW_EXPIRED`.
    - Test: `editReview` review đã soft-delete → throw 422 `REVIEW_NOT_FOUND`.
    - Test: `editReview` content-only (rating giữ nguyên) → KHÔNG gọi recompute (Pitfall 2 optimization).
    - Test: `softDeleteReview` owner → review.deletedAt != null + recompute 1 lần + repo.save called.
    - Test: `softDeleteReview` non-owner → throw 403.
    - Test: `setVisibility(reviewId, true)` → review.hidden=true + recompute. Cùng pattern unhide.
    - Test: `hardDelete(reviewId)` → repo.delete + recompute (capture productId trước khi delete).
    - Test: `listReviews(productId, 0, 10, "rating_desc")` → repo gọi với Pageable có Sort `[rating DESC, createdAt DESC]`.
    - Test: `listReviews(productId, 0, 10, "invalid")` → fallback "newest" Sort `[createdAt DESC]`, KHÔNG throw.
    - Test: `listReviews` response map có key `config.editWindowHours = 24` (D-02).
    - Test: `listAdminReviews(0, 20, "all")` → spec withFilter("all") + pagination + slug resolution; product đã soft-delete → slug = null.
    - Test: `recomputeProductRating` excludes hidden + deleted (verify computeStats call site).
    - Test: `recomputeProductRating` resets to ZERO khi all reviews deleted (count=0).
  </behavior>
  <action>
    **A. Sửa `ReviewService.java` — implement theo PATTERNS verbatim (không paraphrase).**

    **A.1 Constructor + @Value injection (Finding 6 — D-02):**
    ```java
    private final long editWindowHours;

    public ReviewService(
        ReviewRepository reviewRepo,
        ProductRepository productRepo,
        RestTemplate restTemplate,
        @Value("${app.reviews.edit-window-hours:24}") long editWindowHours) {
      this.reviewRepo = reviewRepo;
      this.productRepo = productRepo;
      this.restTemplate = restTemplate;
      this.editWindowHours = editWindowHours;
    }
    ```

    **A.2 Cô lập `recomputeProductRating(String productId)` helper** (per "Recompute invariant MANDATORY" của plan rules):
    - Đổi signature từ method nhận ProductEntity (Phase 13) sang nhận `String productId` để chuẩn hoá. Body: findById, gọi computeStats, build BigDecimal + int, gọi updateRatingStats, save.
    - Method `private` (KHÔNG public — Spring proxy pitfall: gọi từ method `@Transactional` cùng class OK vì same-instance call). Nếu repo của 21-01 SUMMARY báo updateRatingStats cần ZERO sentinel: pass `BigDecimal.ZERO` thay vì null khi count=0.
    - Body verbatim từ PATTERNS §ReviewService Analog 3.

    **A.3 `editReview(reviewId, userId, Integer newRating, String content)`** verbatim từ PATTERNS:
    - findByIdAndDeletedAtIsNull → orElseThrow `UNPROCESSABLE_ENTITY "REVIEW_NOT_FOUND"`
    - Owner check: `if (!review.userId().equals(userId))` → throw `FORBIDDEN "REVIEW_NOT_OWNER"`
    - Window check: `Instant deadline = review.createdAt().plus(editWindowHours, ChronoUnit.HOURS); if (Instant.now().isAfter(deadline))` → throw `UNPROCESSABLE_ENTITY "REVIEW_EDIT_WINDOW_EXPIRED"`
    - Sanitize content nếu present: `Jsoup.clean(content, Safelist.none())` (D-14 Phase 13 reuse)
    - applyEdit(newRating != null ? newRating : oldRating, sanitized) — content nullable
    - save
    - **OPTIMIZATION (Pitfall 2):** chỉ recompute nếu `newRating != null && newRating != oldRating`. Verify bằng test.
    - Return `toResponse(review)` map.
    - Annotate `@Transactional`.

    **A.4 `softDeleteReview(reviewId, userId)`:**
    - findByIdAndDeletedAtIsNull → 422 NOT_FOUND nếu vắng
    - Owner check → 403 NOT_OWNER
    - review.markDeleted() + save
    - **LUÔN** recomputeProductRating(review.productId())
    - Annotate `@Transactional`.

    **A.5 `listReviews(productId, page, size, sortKey)` — extend Phase 13:**
    - Thêm `private static Sort resolveSort(String sortKey)` switch case verbatim (Finding 2):
      - `"rating_desc"` → `Sort.by(desc("rating"), desc("createdAt"))`
      - `"rating_asc"` → `Sort.by(asc("rating"), desc("createdAt"))`
      - default (newest + invalid) → `Sort.by(desc("createdAt"))`
    - safePage / safeSize bounds (max 50)
    - Pageable = PageRequest.of(safePage, safeSize, resolveSort(sortKey))
    - Đổi repo call sang `findByProductIdAndDeletedAtIsNullAndHiddenFalse(productId, pageable)`
    - Build response map giữ key cũ + thêm `result.put("config", Map.of("editWindowHours", editWindowHours))`.

    **A.6 `listAdminReviews(page, size, filter)`:**
    - safePage 0+, safeSize bounds (max 100, default 20)
    - Pageable = PageRequest.of(safePage, safeSize, Sort.by(desc("createdAt")))
    - `Page<ReviewEntity> result = reviewRepo.findAll(AdminReviewSpecifications.withFilter(filter), pageable)`
    - Resolve productSlug batch (Finding 9):
      ```java
      List<String> productIds = result.getContent().stream().map(ReviewEntity::productId).distinct().toList();
      Map<String, String> idToSlug = productRepo.findAllById(productIds).stream()
          .collect(Collectors.toMap(ProductEntity::id, ProductEntity::slug));
      ```
    - Note: `ProductRepository.findAllById` sẽ skip product đã soft-delete (do @SQLRestriction trên ProductEntity) — kết quả là `idToSlug.get(...)` trả null. Map AdminReviewDTO.from(r, idToSlug.get(r.productId())) — DTO field `productSlug` nullable.
    - Build response map identical pattern Phase 13 paginate.

    **A.7 `setVisibility(reviewId, boolean hidden)`:**
    - findById (KHÔNG dùng findByIdAndDeletedAtIsNull — admin có thể hide review đã soft-delete để ẩn khỏi mọi list, edge case nhưng allowed; nếu CONTEXT D-17 không cho phép thì bỏ qua) — fallback hợp lý: dùng `findById` đơn thuần.
    - review.setHidden(hidden) + save
    - recomputeProductRating(review.productId())
    - `@Transactional`.

    **A.8 `hardDelete(reviewId)`:**
    - findById → 404 NOT_FOUND nếu vắng
    - Capture `String productId = review.productId()` TRƯỚC khi delete (Pitfall: sau delete entity detached)
    - reviewRepo.delete(review)
    - recomputeProductRating(productId)
    - `@Transactional`.

    **A.9 KHÔNG quên: cập nhật `createReview` existing (Phase 13) — đổi pre-check từ `existsByProductIdAndUserId` sang `existsByProductIdAndUserIdAndDeletedAtIsNull` (D-06: cho phép re-review sau soft-delete).** Nếu Phase 13 throw `REVIEW_ALREADY_EXISTS`, behavior giữ nguyên — chỉ đổi method name.

    **B. Tạo file `sources/backend/product-service/src/main/java/com/ptit/htpt/productservice/service/AdminReviewDTO.java`** — verbatim từ PATTERNS:
    ```java
    package com.ptit.htpt.productservice.service;

    import com.ptit.htpt.productservice.domain.ReviewEntity;
    import java.time.Instant;

    public record AdminReviewDTO(
        String id, String productId, String productSlug,
        String userId, String reviewerName,
        int rating, String content,
        boolean hidden, Instant deletedAt, Instant createdAt
    ) {
      public static AdminReviewDTO from(ReviewEntity r, String productSlug) {
        return new AdminReviewDTO(
            r.id(), r.productId(), productSlug,
            r.userId(), r.reviewerName(),
            r.rating(), r.content(),
            r.hidden(), r.deletedAt(), r.createdAt()
        );
      }
    }
    ```

    **C. Mở rộng `ReviewServiceTest.java` — thêm test cases liệt kê ở `<behavior>` block:**

    Sử dụng `@SpringBootTest` (existing pattern) + Mockito hoặc real repo nếu test sẵn dùng @DataJpaTest. Cho test edit-window-expired: `@TestPropertySource(properties = "app.reviews.edit-window-hours=0")` ở class riêng, hoặc inject manually qua constructor cho unit test thuần.

    Tối thiểu 11 test methods (đã liệt kê <behavior>) — cộng vào suite hiện có. Sử dụng AssertJ + Mockito.verify để confirm `recomputeProductRating` call count.

    Đặc biệt cho test "recompute invariant":
    ```java
    @Test
    void editReview_contentOnly_skipsRecompute() {
      // ... setup mocks: review with rating=4, call editReview(reviewId, owner, null, "new content")
      reviewService.editReview(reviewId, ownerId, null, "new content");
      verify(productRepo, never()).save(any());     // recompute KHÔNG chạy
    }

    @Test
    void editReview_ratingChanged_triggersRecompute() {
      // call editReview(reviewId, owner, 5, null)
      reviewService.editReview(reviewId, ownerId, 5, null);
      verify(productRepo, atLeastOnce()).save(any());   // recompute chạy
    }
    ```
  </action>
  <verify>
    <automated>cd sources/backend/product-service && mvn -q test -Dtest=ReviewServiceTest</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "recomputeProductRating" sources/backend/product-service/src/main/java/com/ptit/htpt/productservice/service/ReviewService.java` ≥ 6 (5 mutation paths + helper definition; cao hơn nếu createReview cũng gọi)
    - `grep -c "@Value(\"\\${app.reviews.edit-window-hours:24}\")" .../ReviewService.java` = 1
    - `grep -c "REVIEW_EDIT_WINDOW_EXPIRED" .../ReviewService.java` = 1
    - `grep -c "REVIEW_NOT_OWNER" .../ReviewService.java` ≥ 2 (edit + delete paths)
    - `grep -c "REVIEW_NOT_FOUND" .../ReviewService.java` ≥ 2
    - `grep -c "Jsoup.clean" .../ReviewService.java` ≥ 2 (createReview cũ + editReview mới)
    - `grep -c "AdminReviewSpecifications.withFilter" .../ReviewService.java` = 1
    - `grep -c "editWindowHours" .../ReviewService.java` ≥ 4 (field, constructor param, listReviews config map, editReview deadline)
    - `grep -c "config" .../ReviewService.java` ≥ 1 trong context của listReviews response (key `"config"` của Map)
    - File `AdminReviewDTO.java` tồn tại + `grep -c "public record AdminReviewDTO" file` = 1 + 10 fields
    - File `ReviewServiceTest.java` có ≥ 11 method `@Test` mới (grep `@Test` count delta ≥ 11 so với baseline)
    - `mvn -pl product-service test -Dtest=ReviewServiceTest` exit code 0
  </acceptance_criteria>
  <done>5 mutation methods + recompute helper + listReviews(sort) + listAdminReviews + AdminReviewDTO; ReviewServiceTest pass với coverage cho mọi branch.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Mở rộng ReviewController (PATCH/DELETE author + GET sort param) + Tạo AdminReviewController + ReviewControllerTest extend + AdminReviewControllerTest mới</name>
  <read_first>
    - sources/backend/product-service/src/main/java/com/ptit/htpt/productservice/web/ReviewController.java (Phase 13 baseline — parseToken pattern)
    - sources/backend/product-service/src/main/java/com/ptit/htpt/productservice/web/AdminProductController.java (PATCH/DELETE pattern + JwtRoleGuard injection)
    - sources/backend/product-service/src/main/java/com/ptit/htpt/productservice/web/AdminStatsController.java (RequestMapping pattern `/admin/products/...`)
    - sources/backend/product-service/src/main/java/com/ptit/htpt/productservice/web/JwtRoleGuard.java (requireAdmin signature)
    - sources/backend/product-service/src/test/java/com/ptit/htpt/productservice/web/ReviewControllerTest.java (MockMvc setup baseline)
    - .planning/phases/21-hoan-thien-reviews/21-PATTERNS.md (§ReviewController, §AdminReviewController verbatim)
    - .planning/phases/21-hoan-thien-reviews/21-RESEARCH.md (Finding 1 — gateway URL convention; Finding 8 — AdminController pattern)
    - sources/backend/api-gateway/src/main/resources/application.yml (verify route `product-service-admin` — `/api/products/admin/**` rewrite)
  </read_first>
  <behavior>
    - Test (Review): `PATCH /products/{pid}/reviews/{rid}` với owner Bearer + body {rating:4} trong window → 200 + ApiResponse với data có rating=4.
    - Test: `PATCH` non-owner Bearer → 403 + body có code `REVIEW_NOT_OWNER`.
    - Test: `PATCH` window expired → 422 + `REVIEW_EDIT_WINDOW_EXPIRED`.
    - Test: `PATCH` review đã soft-delete → 422 + `REVIEW_NOT_FOUND`.
    - Test: `DELETE /products/{pid}/reviews/{rid}` với owner → 204.
    - Test: `DELETE` non-owner → 403.
    - Test: `GET /products/{pid}/reviews?sort=rating_desc` → 200, content order rating DESC.
    - Test: `GET /products/{pid}/reviews?sort=invalid` → 200 (fallback newest, KHÔNG 400).
    - Test (Admin): `GET /admin/products/reviews` không Authorization → 401.
    - Test: `GET /admin/products/reviews` Bearer non-admin (roles không chứa ADMIN) → 403.
    - Test: `GET /admin/products/reviews?filter=all` admin Bearer → 200, content có hidden + deletedAt + productSlug fields.
    - Test: `PATCH /admin/products/reviews/{rid}/visibility {hidden:true}` admin → 200 + recompute (verify thông qua mock spy).
    - Test: `DELETE /admin/products/reviews/{rid}` admin → 204 + repo.delete called.
  </behavior>
  <action>
    **A. Sửa `ReviewController.java` — thêm 2 endpoint + extend GET (verbatim từ PATTERNS §ReviewController):**

    **A.1 GET extend với sort param** — thêm `@RequestParam(defaultValue = "newest") String sort`, pass xuống `reviewService.listReviews(productId, page, size, sort)`.

    **A.2 PATCH `/{reviewId}` author edit:**
    ```java
    @PatchMapping("/{reviewId}")
    public ApiResponse<Map<String, Object>> editReview(
        @PathVariable String productId,
        @PathVariable String reviewId,
        @RequestHeader("Authorization") String auth,
        @Valid @RequestBody EditReviewRequest body) {
      Claims claims = parseToken(auth);
      String userId = claims.getSubject();
      return ApiResponse.of(200, "Review updated",
          reviewService.editReview(reviewId, userId, body.rating(), body.content()));
    }
    ```

    **A.3 DELETE `/{reviewId}` author soft-delete:**
    ```java
    @DeleteMapping("/{reviewId}")
    @ResponseStatus(HttpStatus.NO_CONTENT)
    public void softDeleteReview(
        @PathVariable String productId,
        @PathVariable String reviewId,
        @RequestHeader("Authorization") String auth) {
      Claims claims = parseToken(auth);
      String userId = claims.getSubject();
      reviewService.softDeleteReview(reviewId, userId);
    }
    ```

    **A.4 Inner record `EditReviewRequest`:**
    ```java
    public record EditReviewRequest(
        @Min(1) @Max(5) Integer rating,         // nullable — sửa content-only được
        @Size(max = 500) String content
    ) {}
    ```

    **B. Tạo `sources/backend/product-service/src/main/java/com/ptit/htpt/productservice/web/AdminReviewController.java`** — verbatim từ PATTERNS §AdminReviewController (Finding 8):

    ```java
    package com.ptit.htpt.productservice.web;

    import com.ptit.htpt.productservice.api.ApiResponse;
    import com.ptit.htpt.productservice.service.ReviewService;
    import jakarta.validation.Valid;
    import jakarta.validation.constraints.NotNull;
    import java.util.Map;
    import org.springframework.http.HttpHeaders;
    import org.springframework.http.HttpStatus;
    import org.springframework.web.bind.annotation.*;

    @RestController
    @RequestMapping("/admin/products/reviews")  // gateway rewrite: /api/products/admin/reviews → /admin/products/reviews
    public class AdminReviewController {

      private final ReviewService reviewService;
      private final JwtRoleGuard jwtRoleGuard;

      public AdminReviewController(ReviewService reviewService, JwtRoleGuard jwtRoleGuard) {
        this.reviewService = reviewService;
        this.jwtRoleGuard = jwtRoleGuard;
      }

      @GetMapping
      public ApiResponse<Map<String, Object>> list(
          @RequestHeader(value = HttpHeaders.AUTHORIZATION, required = false) String auth,
          @RequestParam(defaultValue = "0") int page,
          @RequestParam(defaultValue = "20") int size,
          @RequestParam(defaultValue = "all") String filter) {
        jwtRoleGuard.requireAdmin(auth);
        return ApiResponse.of(200, "Admin reviews listed",
            reviewService.listAdminReviews(page, size, filter));
      }

      @PatchMapping("/{reviewId}/visibility")
      public ApiResponse<Object> setVisibility(
          @RequestHeader(value = HttpHeaders.AUTHORIZATION, required = false) String auth,
          @PathVariable String reviewId,
          @Valid @RequestBody VisibilityRequest body) {
        jwtRoleGuard.requireAdmin(auth);
        reviewService.setVisibility(reviewId, body.hidden());
        return ApiResponse.of(200, "Visibility updated",
            Map.of("id", reviewId, "hidden", body.hidden()));
      }

      @DeleteMapping("/{reviewId}")
      @ResponseStatus(HttpStatus.NO_CONTENT)
      public void hardDelete(
          @RequestHeader(value = HttpHeaders.AUTHORIZATION, required = false) String auth,
          @PathVariable String reviewId) {
        jwtRoleGuard.requireAdmin(auth);
        reviewService.hardDelete(reviewId);
      }

      public record VisibilityRequest(@NotNull Boolean hidden) {}
    }
    ```

    **CHÚ Ý CRITICAL: URL path `/admin/products/reviews` (3 segments). Đây là Finding 1 correction — KHÔNG dùng `/admin/reviews`. Gateway route `product-service-admin` (`/api/products/admin/**`) rewrite từ `/api/products/admin/reviews/...` → `/admin/products/reviews/...`. CONTEXT D-18 viết nhầm — follow Finding 1.**

    **C. Mở rộng `ReviewControllerTest.java`** — thêm 8 test method theo `<behavior>` block (PATCH/DELETE author + sort/invalid). MockMvc setup pattern Phase 13 baseline đã có; thêm `@MockBean ReviewService` nếu test spec yêu cầu, hoặc dùng @SpringBootTest end-to-end.

    Test đặc biệt cho "fallback newest":
    ```java
    @Test
    void getReviews_invalidSort_fallbackNewestNot400() throws Exception {
      mockMvc.perform(get("/products/{pid}/reviews?sort=garbage", PRODUCT_ID))
        .andExpect(status().isOk());   // KHÔNG 400 (D-12)
    }
    ```

    **D. Tạo `sources/backend/product-service/src/test/java/com/ptit/htpt/productservice/web/AdminReviewControllerTest.java`** — copy MockMvc setup từ ReviewControllerTest, thay `ReviewService` mock + `JwtRoleGuard` mock.

    Cases tối thiểu:
    1. `list_missingAuth_returns401` — POST không Authorization header → 401 (JwtRoleGuard.requireAdmin throws ResponseStatusException UNAUTHORIZED).
    2. `list_nonAdminToken_returns403` — Bearer với roles không chứa ADMIN → 403.
    3. `list_admin_returnsPaginated` — Bearer admin, mock service trả Page<AdminReviewDTO> sample → 200 + body verify content[].productSlug field present.
    4. `setVisibility_admin_returns200` — PATCH `/abc/visibility` body `{"hidden":true}` admin → 200 + verify(reviewService).setVisibility("abc", true).
    5. `hardDelete_admin_returns204` — DELETE `/abc` admin → 204 + verify(reviewService).hardDelete("abc").

    Sử dụng `@WebMvcTest(AdminReviewController.class)` để khoanh vùng controller-only test với `@MockBean` cho ReviewService + JwtRoleGuard.
  </action>
  <verify>
    <automated>cd sources/backend/product-service && mvn -q test -Dtest=ReviewControllerTest,AdminReviewControllerTest</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "@PatchMapping(\"/{reviewId}\")" sources/backend/product-service/src/main/java/com/ptit/htpt/productservice/web/ReviewController.java` = 1
    - `grep -c "@DeleteMapping(\"/{reviewId}\")" .../ReviewController.java` = 1
    - `grep -c "EditReviewRequest" .../ReviewController.java` ≥ 2 (record + parameter)
    - `grep -c "@RequestMapping(\"/admin/products/reviews\")" .../AdminReviewController.java` = 1   ← Finding 1 critical
    - `grep -c "/admin/reviews" .../AdminReviewController.java` = 0   ← MUST NOT contain `/admin/reviews` standalone (sai per Finding 1)
    - `grep -c "jwtRoleGuard.requireAdmin(auth)" .../AdminReviewController.java` = 3 (list + visibility + hardDelete)
    - `grep -c "@PatchMapping(\"/{reviewId}/visibility\")" .../AdminReviewController.java` = 1
    - `grep -c "VisibilityRequest" .../AdminReviewController.java` ≥ 2
    - File `AdminReviewControllerTest.java` tồn tại + `grep -c "@Test" file` ≥ 5
    - `grep -c "REVIEW_EDIT_WINDOW_EXPIRED\\|REVIEW_NOT_OWNER\\|REVIEW_NOT_FOUND" .../ReviewControllerTest.java` ≥ 3 (assert error code)
    - `mvn -pl product-service test -Dtest=ReviewControllerTest,AdminReviewControllerTest` exit code 0
  </acceptance_criteria>
  <done>5 endpoint mới (PATCH/DELETE author + admin GET/PATCH/DELETE) sẵn sàng cho FE consume; tests cover auth + window + role-guard + happy path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Gateway → product-service | Bearer JWT cross trust boundary; ownership + role decided server-side |
| Author endpoints ↔ admin endpoints | Author cannot mutate non-owned reviews; admin can but cannot bypass recompute |
| FE editWindowHours state ↔ BE re-check | FE state advisory only; BE re-checks against `Instant.now()` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-02-01 | Spoofing | PATCH/DELETE without Bearer or with forged JWT | mitigate | `parseToken(auth)` throws 401 nếu missing/invalid; signing key `app.jwt.secret` HMAC-SHA256 (Phase 13 baseline). |
| T-21-02-02 | Elevation of Privilege | Non-owner edit/delete via reviewId guess (IDOR) | mitigate | `ReviewService.editReview/softDeleteReview` checks `review.userId().equals(claims.getSubject())` → 403 `REVIEW_NOT_OWNER`. Tests `editReview_nonOwner_returns403` + `softDelete_nonOwner_returns403`. |
| T-21-02-03 | Tampering | Edit window bypass — FE manipulates `editWindowHours` to allow late edit | mitigate | BE re-check `Instant.now().isAfter(review.createdAt().plus(editWindowHours, HOURS))` → 422. Test `editReview_pastWindow_returns422` với `@TestPropertySource(properties = "app.reviews.edit-window-hours=0")`. |
| T-21-02-04 | Information Disclosure | Soft-deleted review leaks via public GET list | mitigate | Repo finder `findByProductIdAndDeletedAtIsNullAndHiddenFalse` enforces filter at SQL level (Phase 21-01). Test `listReviews_excludesDeletedAndHidden`. |
| T-21-02-05 | Tampering | XSS via edit content (Stored XSS) | mitigate | `Jsoup.clean(content, Safelist.none())` strip toàn bộ HTML — strict reuse Phase 13 D-14 ở edit path (PATTERNS Analog 4). |
| T-21-02-06 | Elevation of Privilege | Admin endpoint exposed without role check | mitigate | `AdminReviewController` mọi handler gọi `jwtRoleGuard.requireAdmin(auth)` đầu tiên (3/3 endpoints). Tests verify 401/403 paths. |
| T-21-02-07 | Repudiation | Admin moderation actions không log | accept | Audit log defer per CONTEXT.md "Deferred Ideas" — visible-first project, single-developer demo. |
| T-21-02-08 | Denial of Service | Hard-delete trigger N+1 query qua recompute | accept | `recomputeProductRating` chỉ hit 2 query (computeStats + product save) — bounded. |
| T-21-02-09 | Tampering | Admin hide review của product đã soft-delete → recompute fails | mitigate | `recomputeProductRating` `productRepo.findById(productId).orElseThrow()` — admin hard-delete trên review của sản phẩm đã xoá sẽ throw 404 (acceptable edge case; document trong code comment). |
| T-21-02-10 | Information Disclosure | AdminReviewDTO trả userId raw (PII concern) | accept | userId là internal UUID, không phải email/phone. AdminReviewDTO dùng `reviewerName` (snapshot) cho display; userId chỉ để admin reference (acceptable for moderation). |
</threat_model>

<validation>
## Test Strategy (Nyquist enabled)

**Unit / Service tests** (Task 1):
- File: `sources/backend/product-service/src/test/java/com/ptit/htpt/productservice/service/ReviewServiceTest.java`
- ≥ 11 new `@Test` methods covering recompute scope (hidden + deleted excluded), edit window, ownership, sort fallback, content-only-skip-recompute optimization.
- Acceptance: `mvn -pl product-service test -Dtest=ReviewServiceTest` exit 0.

**MockMvc Controller tests** (Task 2):
- File 1: `sources/backend/product-service/src/test/java/com/ptit/htpt/productservice/web/ReviewControllerTest.java` — extend với 8+ test cases mới (PATCH/DELETE author + sort happy/fallback/expired).
- File 2: `sources/backend/product-service/src/test/java/com/ptit/htpt/productservice/web/AdminReviewControllerTest.java` — NEW, ≥ 5 cases (401/403/200 + happy paths cho 3 endpoints).
- Acceptance: `mvn -pl product-service test -Dtest=ReviewControllerTest,AdminReviewControllerTest` exit 0.

**Migration runtime test (indirect):** Spring Boot context startup trong test sẽ apply V7 (Plan 21-01) qua Flyway. Nếu V7 lỗi → mọi test trong plan này fail tại context init → quay lại 21-01 fix.

**E2E test deferred to Plan 21-04** (Playwright).

**Recompute invariant grep verify** (acceptance criteria Task 1):
- `grep -c "recomputeProductRating(" ReviewService.java` ≥ 6: helper definition + 5 mutation paths + createReview cũ (Phase 13).
</validation>

<verification>
- ReviewService có ≥ 6 occurrence của `recomputeProductRating` (helper + 5 mutation paths; createReview cũ giữ nguyên thêm).
- Edit-content-only path KHÔNG gọi recompute (Pitfall 2 optimization verified by `verify(productRepo, never())` test).
- AdminReviewController URL = `/admin/products/reviews` (Finding 1) — verified by grep.
- `JwtRoleGuard.requireAdmin` gọi ở mọi handler admin (3 occurrences — verified by grep count = 3).
- Jsoup.clean reused ở edit path (KHÔNG chỉ create) — verified grep ≥ 2.
- Compile + unit + MockMvc tests pass.
</verification>

<success_criteria>
- [ ] `mvn -pl product-service test` exit code 0 (full test suite không regress)
- [ ] ReviewService extended với 5 mutation methods + recompute helper isolated
- [ ] AdminReviewController tạo mới với 3 endpoints + role guard ở mỗi handler
- [ ] AdminReviewDTO record có 10 fields đúng spec
- [ ] ReviewControllerTest extended với 8+ tests mới
- [ ] AdminReviewControllerTest mới với 5+ tests (401/403/happy paths)
- [ ] Recompute scope verified: excludes hidden + deleted; reset to ZERO khi count=0
- [ ] Edit window enforcement BE-side re-check verified
- [ ] AdminReviewController URL path = `/admin/products/reviews` (Finding 1 correction)
</success_criteria>

<output>
Sau khi hoàn thành, tạo `.planning/phases/21-hoan-thien-reviews/21-02-SUMMARY.md` ghi:
- 5 endpoint mới với cURL examples
- Recompute call sites (file:line) — proof of invariant
- Test counts (ReviewServiceTest delta, ReviewControllerTest delta, AdminReviewControllerTest total)
- Gateway URL verified: `/api/products/admin/reviews/...` route qua product-service-admin
- Interfaces exposed cho FE plans (21-03, 21-04): URL paths + request/response shapes
</output>
