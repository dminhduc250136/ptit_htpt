---
phase: 21-hoan-thien-reviews
plan: 04
type: execute
wave: 3
depends_on: [21-02]
files_modified:
  - sources/frontend/src/app/admin/reviews/page.tsx
  - sources/frontend/src/app/admin/reviews/page.module.css
  - sources/frontend/src/app/admin/layout.tsx
  - sources/frontend/tests/e2e/reviews-author-edit.spec.ts
  - sources/frontend/tests/e2e/admin-reviews-moderation.spec.ts
autonomous: true
requirements: [REV-06]
must_haves:
  truths:
    - "Admin truy cập /admin/reviews thấy bảng danh sách review với cột: Sản phẩm | Reviewer | Rating | Trích đoạn | Trạng thái | Ngày tạo | Hành động"
    - "Filter dropdown 4-state: Tất cả | Đang hiện | Đã ẩn | Đã xoá (author); default Tất cả"
    - "Pagination server-side ?page=&size=20 với nút Trước/Sau"
    - "Mỗi row có badge trạng thái: Hiện (xanh) / Ẩn (đỏ) / Đã xoá (xám)"
    - "Action 'Ẩn' / 'Bỏ ẩn' (PATCH visibility) toast + refetch row; row đã deleted_at chỉ hiện 'Xoá'"
    - "Action 'Xoá' (đỏ) → window.confirm 'Xoá vĩnh viễn review này? Không thể hoàn tác.' → DELETE → toast + refetch"
    - "Sidebar admin có link 'Đánh giá' giữa 'Đơn hàng' và 'Tài khoản'"
    - "E2E spec reviews-author-edit covers: owner edit happy + window expired"
    - "E2E spec admin-reviews-moderation covers: admin login → /admin/reviews → hide review → review vắng trên PDP → unhide → review hiện lại"
  artifacts:
    - path: "sources/frontend/src/app/admin/reviews/page.tsx"
      provides: "Admin reviews moderation page với filter + pagination + actions"
      contains: "listAdminReviews"
    - path: "sources/frontend/src/app/admin/reviews/page.module.css"
      provides: "Styles cho admin reviews table (clone từ /admin/products)"
      contains: "table"
    - path: "sources/frontend/src/app/admin/layout.tsx"
      provides: "Sidebar nav extended với link /admin/reviews"
      contains: "/admin/reviews"
    - path: "sources/frontend/tests/e2e/reviews-author-edit.spec.ts"
      provides: "Playwright spec cho author edit/delete UX"
      contains: "test"
    - path: "sources/frontend/tests/e2e/admin-reviews-moderation.spec.ts"
      provides: "Playwright spec cho admin hide/unhide flow"
      contains: "test"
  key_links:
    - from: "/admin/reviews/page.tsx"
      to: "BE GET /api/products/admin/reviews + PATCH /admin/products/reviews/{id}/visibility + DELETE /admin/products/reviews/{id}"
      via: "services/reviews.ts (listAdminReviews/setReviewVisibility/hardDeleteReview)"
      pattern: "listAdminReviews\\|setReviewVisibility\\|hardDeleteReview"
    - from: "admin/layout.tsx navItems"
      to: "/admin/reviews route"
      via: "Next.js App Router segment"
      pattern: "href=.*admin/reviews"
    - from: "admin-reviews-moderation E2E spec"
      to: "BE hide endpoint visible filter"
      via: "Playwright assertions on PDP review list before/after"
      pattern: "expect.*toBeVisible\\|toBeHidden"
---

<objective>
FE admin moderation page `/admin/reviews` (REV-06) + Playwright E2E specs cho author edit và admin moderation flow. Wave 3, parallel với 21-03 (FE author UX). Đây là 1/3 success criteria của Phase 21 (admin moderation) + locked-in regression guard cho cả 3 success criteria.

Purpose: Admin có entry point trực tiếp vào `/admin/reviews` để hide/unhide/hard-delete; E2E specs cố định behavior end-to-end qua Playwright (existing test suite v1.2 pattern).

Output: 1 page mới + 1 CSS + 1 sidebar nav update + 2 E2E spec files (author + admin).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-hoan-thien-reviews/21-CONTEXT.md
@.planning/phases/21-hoan-thien-reviews/21-RESEARCH.md
@.planning/phases/21-hoan-thien-reviews/21-PATTERNS.md
@.planning/phases/21-hoan-thien-reviews/21-02-SUMMARY.md
@.planning/phases/21-hoan-thien-reviews/21-03-SUMMARY.md
@sources/frontend/src/app/admin/products/page.tsx
@sources/frontend/src/app/admin/products/page.module.css
@sources/frontend/src/app/admin/layout.tsx
@sources/frontend/src/services/reviews.ts
@sources/frontend/src/types/index.ts
@sources/frontend/src/components/ui/Toast/Toast.tsx
@sources/frontend/src/components/ui/Badge/Badge.tsx
@sources/frontend/src/components/ui/RetrySection/RetrySection.tsx
@sources/frontend/src/components/ui/Button/Button.tsx
@sources/frontend/playwright.config.ts

<interfaces>
<!-- Contracts từ 21-02 (BE) + 21-03 (FE service layer) mà plan này tiêu thụ -->

From services/reviews.ts (Plan 21-03 added):
```typescript
export function listAdminReviews(page = 0, size = 20, filter: 'all'|'visible'|'hidden'|'deleted' = 'all'): Promise<AdminReviewListResponse>;
export function setReviewVisibility(reviewId: string, hidden: boolean): Promise<void>;
export function hardDeleteReview(reviewId: string): Promise<void>;
```

From types/index.ts (Plan 21-03 added):
```typescript
export interface AdminReview extends Review {
  productSlug: string | null;
  productName?: string;
  hidden?: boolean;
  deletedAt?: string | null;
}
```

From admin/products/page.tsx (analog — clone-then-adapt):
- Pagination state pattern (page/size)
- Filter dropdown style
- Table layout + actions inline pattern
- Loading/failed state via RetrySection

From admin/layout.tsx navItems (lines ~11-16):
- Pattern `{ href, label, icon }` array
- Insert giữa Đơn hàng và Tài khoản

BE endpoints (Plan 21-02):
```
GET    /api/products/admin/reviews?page=&size=&filter=    → AdminReviewListResponse
PATCH  /api/products/admin/reviews/{id}/visibility        body { hidden: boolean }
DELETE /api/products/admin/reviews/{id}                   → 204
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tạo /admin/reviews/page.tsx + page.module.css + extend admin/layout.tsx sidebar</name>
  <read_first>
    - sources/frontend/src/app/admin/products/page.tsx (toàn bộ — analog clone, đặc biệt state setup, table render, filter dropdown, pagination)
    - sources/frontend/src/app/admin/products/page.module.css (toàn bộ — CSS clone)
    - sources/frontend/src/app/admin/layout.tsx (navItems array vị trí thêm link)
    - sources/frontend/src/services/reviews.ts (sau Plan 21-03 — listAdminReviews/setReviewVisibility/hardDeleteReview)
    - sources/frontend/src/types/index.ts (AdminReview shape)
    - sources/frontend/src/components/ui/Badge/Badge.tsx (variants available — verify "new", "out-of-stock", "default" exist)
    - .planning/phases/21-hoan-thien-reviews/21-PATTERNS.md (§app/admin/reviews/page.tsx verbatim, §app/admin/layout.tsx)
    - .planning/phases/21-hoan-thien-reviews/21-CONTEXT.md (D-14 columns, D-15 filter, D-17 actions, specifics line 239-240 wording)
  </read_first>
  <action>
    **A. Tạo `sources/frontend/src/app/admin/reviews/page.tsx`** — clone-then-adapt từ admin/products/page.tsx + verbatim pattern PATTERNS §app/admin/reviews/page.tsx.

    **A.1 Imports + setup:**
    ```typescript
    'use client';
    import React, { useCallback, useEffect, useState } from 'react';
    import Link from 'next/link';
    import styles from './page.module.css';
    import Button from '@/components/ui/Button/Button';
    import Badge from '@/components/ui/Badge/Badge';
    import RetrySection from '@/components/ui/RetrySection/RetrySection';
    import { useToast } from '@/components/ui/Toast/Toast';
    import { listAdminReviews, setReviewVisibility, hardDeleteReview } from '@/services/reviews';
    import type { AdminReview } from '@/types';

    type FilterValue = 'all' | 'visible' | 'hidden' | 'deleted';
    ```

    **A.2 State + load callback:**
    ```typescript
    const [page, setPage] = useState(0);
    const [size] = useState(20);
    const [filter, setFilter] = useState<FilterValue>('all');
    const [reviews, setReviews] = useState<AdminReview[]>([]);
    const [meta, setMeta] = useState<{ totalElements: number; totalPages: number; isLast: boolean } | null>(null);
    const [loading, setLoading] = useState(false);
    const [failed, setFailed] = useState(false);
    const { showToast } = useToast();

    const load = useCallback(async () => {
      setLoading(true); setFailed(false);
      try {
        const resp = await listAdminReviews(page, size, filter);
        setReviews(resp.content);
        setMeta({ totalElements: resp.totalElements, totalPages: resp.totalPages, isLast: resp.isLast });
      } catch { setFailed(true); }
      finally { setLoading(false); }
    }, [page, size, filter]);

    useEffect(() => { load(); }, [load]);
    ```

    **A.3 Action handlers (D-17):**
    ```typescript
    async function onToggleHidden(r: AdminReview) {
      try {
        await setReviewVisibility(r.id, !r.hidden);
        showToast(r.hidden ? 'Đã bỏ ẩn review' : 'Đã ẩn review', 'success');
        await load();
      } catch { showToast('Không thể cập nhật trạng thái', 'error'); }
    }
    async function onHardDelete(r: AdminReview) {
      if (!window.confirm('Xoá vĩnh viễn review này? Không thể hoàn tác.')) return;
      try {
        await hardDeleteReview(r.id);
        showToast('Đã xoá vĩnh viễn', 'success');
        await load();
      } catch { showToast('Không thể xoá review', 'error'); }
    }
    ```

    **A.4 Status badge helper (D-14):**
    ```typescript
    function statusBadge(r: AdminReview) {
      if (r.deletedAt) return <Badge variant="default">Đã xoá</Badge>;
      if (r.hidden)    return <Badge variant="out-of-stock">Ẩn</Badge>;
      return <Badge variant="new">Hiện</Badge>;
    }
    ```
    Nếu Badge component KHÔNG có các variants này, fallback dùng inline span với màu via styles class.

    **A.5 Render structure:**
    ```tsx
    return (
      <div className={styles.page}>
        <header className={styles.header}>
          <h1>Quản lý đánh giá</h1>
          <div className={styles.filterRow}>
            <select
              className={styles.filterSelect}
              value={filter}
              onChange={(e) => { setPage(0); setFilter(e.target.value as FilterValue); }}
            >
              <option value="all">Tất cả</option>
              <option value="visible">Đang hiện</option>
              <option value="hidden">Đã ẩn</option>
              <option value="deleted">Đã xoá (author)</option>
            </select>
          </div>
        </header>

        {loading && <p>Đang tải...</p>}
        {failed && <RetrySection onRetry={load} />}

        {!loading && !failed && (
          <>
            <table className={styles.table}>
              <thead>
                <tr>
                  <th>Sản phẩm</th>
                  <th>Reviewer</th>
                  <th>Rating</th>
                  <th>Nội dung</th>
                  <th>Trạng thái</th>
                  <th>Ngày tạo</th>
                  <th>Hành động</th>
                </tr>
              </thead>
              <tbody>
                {reviews.map((r) => (
                  <tr key={r.id}>
                    <td>
                      {r.productSlug ? (
                        <Link href={`/products/${r.productSlug}`}>{r.productName ?? r.productSlug}</Link>
                      ) : <span>—</span>}
                    </td>
                    <td>{r.reviewerName}</td>
                    <td>{'★'.repeat(r.rating)}{'☆'.repeat(5 - r.rating)}</td>
                    <td title={r.content ?? ''} className={styles.contentCell}>
                      {(r.content ?? '').length > 60 ? `${(r.content ?? '').slice(0, 60)}…` : (r.content ?? '')}
                    </td>
                    <td>{statusBadge(r)}</td>
                    <td>{new Date(r.createdAt).toLocaleDateString('vi-VN')}</td>
                    <td className={styles.actions}>
                      {!r.deletedAt && (
                        <button className={styles.actionBtn} onClick={() => onToggleHidden(r)}>
                          {r.hidden ? 'Bỏ ẩn' : 'Ẩn'}
                        </button>
                      )}
                      <button className={`${styles.actionBtn} ${styles.deleteBtn}`} onClick={() => onHardDelete(r)}>
                        Xoá
                      </button>
                    </td>
                  </tr>
                ))}
              </tbody>
            </table>

            {meta && meta.totalPages > 1 && (
              <div className={styles.paginationRow}>
                <Button variant="secondary" disabled={page === 0} onClick={() => setPage((p) => Math.max(0, p - 1))}>Trước</Button>
                <span>Trang {page + 1} / {meta.totalPages}</span>
                <Button variant="secondary" disabled={meta.isLast} onClick={() => setPage((p) => p + 1)}>Sau</Button>
              </div>
            )}

            {meta && meta.totalElements === 0 && (
              <p className={styles.empty}>Không có đánh giá nào trong bộ lọc hiện tại.</p>
            )}
          </>
        )}
      </div>
    );
    ```

    **B. Tạo `sources/frontend/src/app/admin/reviews/page.module.css`** — clone từ `admin/products/page.module.css` (toàn bộ), giữ nguyên class names hoặc đổi tối thiểu để khớp render trên. Tối thiểu phải có:
    - `.page`, `.header`, `.filterRow`, `.filterSelect`, `.table`, `.contentCell`, `.actions`, `.actionBtn`, `.deleteBtn`, `.paginationRow`, `.empty`
    - Style table: full width, border-collapse, padding cells theo design tokens.
    - `.deleteBtn` color = `var(--error)`.
    - `.contentCell` max-width + ellipsis.

    Nếu copy nguyên file `admin/products/page.module.css` thì OK — class dư không hại.

    **C. Sửa `sources/frontend/src/app/admin/layout.tsx`** — chèn nav item:
    Tìm array `navItems = [...]` (lines ~11-16). Insert giữa Đơn hàng và Tài khoản:
    ```typescript
    { href: '/admin/orders', label: 'Đơn hàng', icon: <svg .../> },
    { href: '/admin/reviews', label: 'Đánh giá', icon: (
        <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round">
          <polygon points="12 2 15.09 8.26 22 9.27 17 14.14 18.18 21.02 12 17.77 5.82 21.02 7 14.14 2 9.27 8.91 8.26 12 2"/>
        </svg>
      )
    },
    { href: '/admin/users', label: 'Tài khoản', icon: <svg .../> },
    ```

    KHÔNG đổi href/label của các item khác. Verify icon SVG render được (star polygon).
  </action>
  <verify>
    <automated>cd sources/frontend && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - File `sources/frontend/src/app/admin/reviews/page.tsx` tồn tại
    - `grep -c "listAdminReviews" .../admin/reviews/page.tsx` ≥ 1
    - `grep -c "setReviewVisibility" .../admin/reviews/page.tsx` ≥ 1
    - `grep -c "hardDeleteReview" .../admin/reviews/page.tsx` ≥ 1
    - `grep -c "Tất cả\\|Đang hiện\\|Đã ẩn\\|Đã xoá" .../admin/reviews/page.tsx` ≥ 4 (4 filter options)
    - `grep -c "Xoá vĩnh viễn review này\\? Không thể hoàn tác" .../admin/reviews/page.tsx` = 1
    - `grep -c "Đã ẩn review\\|Đã bỏ ẩn review\\|Đã xoá vĩnh viễn" .../admin/reviews/page.tsx` ≥ 3 (toast wording)
    - File `sources/frontend/src/app/admin/reviews/page.module.css` tồn tại
    - `grep -c "/admin/reviews" sources/frontend/src/app/admin/layout.tsx` ≥ 1
    - `grep -c "Đánh giá" sources/frontend/src/app/admin/layout.tsx` ≥ 1
    - `npx tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>Admin moderation page hoàn chỉnh + sidebar nav extended; tsc passes.</done>
</task>

<task type="auto">
  <name>Task 2: Tạo Playwright E2E specs reviews-author-edit + admin-reviews-moderation</name>
  <read_first>
    - sources/frontend/playwright.config.ts (base URL + global setup nếu có)
    - sources/frontend/tests/e2e/ (toàn bộ list — học pattern login/auth setup, fixture user)
    - sources/frontend/tests/e2e/* (1-2 spec hiện có để học naming + assertions style — vd review-related nếu có từ Phase 13/15, hoặc admin-products spec)
    - .planning/phases/21-hoan-thien-reviews/21-CONTEXT.md (D-21..D-23 author UX, D-14..D-17 admin UX)
    - .planning/phases/21-hoan-thien-reviews/21-RESEARCH.md (validation_architecture — E2E scope)
    - sources/frontend/src/app/products/[slug]/ReviewSection/ReviewList.tsx (locators: label "Sửa", "Xoá", select "Sắp xếp đánh giá")
  </read_first>
  <action>
    **A. Tạo `sources/frontend/tests/e2e/reviews-author-edit.spec.ts`:**

    Test 1 — author edit happy path:
    1. Login as user đã từng review sản phẩm X (seed test data hoặc dùng existing user fixture).
    2. Navigate `/products/{slug}` của X.
    3. Scroll vào ReviewSection. Locate review của user (selector dựa trên reviewerName).
    4. Click button "Sửa" (selector: `button:has-text("Sửa")` trong context của review item của user).
    5. ReviewForm xuất hiện inline: assert textarea visible + content prefilled.
    6. Update content + select rating mới.
    7. Click "Lưu thay đổi".
    8. Assert toast 'Đã cập nhật đánh giá' visible.
    9. Assert review item content text khớp giá trị mới.

    Test 2 — edit window expired button disabled:
    1. Để spec test này deterministic, hoặc:
       (a) Dùng API helper trong test setup tạo review với `created_at` = 25h trước (nếu có direct DB access trong test env), HOẶC
       (b) Override `app.reviews.edit-window-hours=0` qua env var khi start test backend (Spring profile test có sẵn từ Phase 13).
    2. Login + nav PDP.
    3. Locate review của user.
    4. Assert button "Sửa" disabled (`expect(editBtn).toBeDisabled()`).
    5. Hover button → assert tooltip / title attribute = "Đã quá thời hạn chỉnh sửa (24h)".

    Nếu setup (a) hoặc (b) phức tạp → fallback: chỉ test happy path; document trong action note rằng window-expired test deferred.

    Test 3 — author soft-delete:
    1. Login + nav PDP có review của user.
    2. Click "Xoá".
    3. Mock `window.confirm` qua `page.on('dialog', dialog => dialog.accept())`.
    4. Assert toast 'Đã xoá đánh giá'.
    5. Assert review không còn xuất hiện trong list (selector vắng).

    **B. Tạo `sources/frontend/tests/e2e/admin-reviews-moderation.spec.ts`:**

    Test — admin hide → unhide flow:
    1. Login as admin user (existing admin fixture).
    2. Navigate `/admin/reviews`.
    3. Assert table visible với ≥ 1 row (seed data từ Phase 16 + Phase 13 reviews đã có).
    4. Capture row đầu tiên không hidden (filter "Đang hiện") — note `productSlug` + `reviewerName` + content snippet.
    5. Click "Ẩn" trên row đó.
    6. Assert toast 'Đã ẩn review'.
    7. Open new tab/page navigate `/products/{productSlug}`.
    8. Scroll ReviewSection.
    9. Assert review của reviewerName đó KHÔNG xuất hiện (`expect(reviewLocator).toHaveCount(0)`).
    10. Quay lại admin tab. Đổi filter dropdown sang "Đã ẩn".
    11. Click "Bỏ ẩn" trên review đó.
    12. Assert toast 'Đã bỏ ẩn review'.
    13. Refresh PDP tab.
    14. Assert review xuất hiện lại trên PDP.

    Test — admin filter shows correct counts:
    1. Login admin nav `/admin/reviews`.
    2. Test filter "Đang hiện" → mọi row có badge "Hiện".
    3. Test filter "Đã xoá" → mọi row có badge "Đã xoá" + chỉ hiển thị nút "Xoá" (KHÔNG có "Ẩn"/"Bỏ ẩn").

    **C. Verify Playwright config:**
    - Cả 2 spec phải nằm trong `sources/frontend/tests/e2e/` (đường dẫn match `playwright.config.ts` testDir).
    - Sử dụng existing fixtures cho login (xem 1-2 spec hiện có để clone setup).
    - Nếu có `tests/e2e/fixtures.ts` hoặc `tests/e2e/helpers.ts` — reuse, KHÔNG re-implement login.

    **Lưu ý — selectors:** ReviewList ở 21-03 dùng button text "Sửa" / "Xoá" và `<select aria-label="Sắp xếp đánh giá">`. Admin page text: "Ẩn" / "Bỏ ẩn" / "Xoá", filter `<select>` không aria-label cụ thể nhưng có 4 options "Tất cả/Đang hiện/Đã ẩn/Đã xoá (author)". Test dùng `getByRole('button', { name: 'Sửa' })` hoặc text-based locator.
  </action>
  <verify>
    <automated>cd sources/frontend && npx playwright test tests/e2e/reviews-author-edit.spec.ts tests/e2e/admin-reviews-moderation.spec.ts --reporter=list --workers=1</automated>
  </verify>
  <acceptance_criteria>
    - File `sources/frontend/tests/e2e/reviews-author-edit.spec.ts` tồn tại
    - File `sources/frontend/tests/e2e/admin-reviews-moderation.spec.ts` tồn tại
    - `grep -c "test(" sources/frontend/tests/e2e/reviews-author-edit.spec.ts` ≥ 2 (happy + delete; expired có thể defer)
    - `grep -c "test(" sources/frontend/tests/e2e/admin-reviews-moderation.spec.ts` ≥ 1
    - `grep -c "Đã cập nhật đánh giá\\|Đã xoá đánh giá" sources/frontend/tests/e2e/reviews-author-edit.spec.ts` ≥ 1
    - `grep -c "Đã ẩn review\\|Đã bỏ ẩn review" sources/frontend/tests/e2e/admin-reviews-moderation.spec.ts` ≥ 2
    - `grep -c "/admin/reviews" sources/frontend/tests/e2e/admin-reviews-moderation.spec.ts` ≥ 1
    - `grep -c "/products/" sources/frontend/tests/e2e/admin-reviews-moderation.spec.ts` ≥ 1 (cross-tab PDP visibility check)
    - Playwright CLI `npx playwright test ...` exit 0 — IF backend + frontend đang chạy. Nếu test infra không khả dụng trong execute env, ít nhất `npx playwright test --list` phải liệt kê được cả 2 specs (syntactically valid).
  </acceptance_criteria>
  <done>2 E2E specs tồn tại + cover các flow critical; test list parse được; happy paths chạy nếu env có server.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser admin → BE admin endpoints | Bearer JWT mang `roles=ADMIN` claim; BE re-check qua JwtRoleGuard.requireAdmin (Plan 21-02 T-21-02-06) |
| Admin UI ↔ user PDP | Hidden review không được leak qua public list; E2E spec verify cross-tab |
| window.confirm hard-delete | Native confirm là gate cuối; bypass = data loss vĩnh viễn |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-04-01 | Elevation of Privilege | Non-admin user navigate `/admin/reviews` qua URL trực tiếp | mitigate | BE endpoint trả 403 (T-21-02-06). FE page không có middleware guard riêng — admin layout hiện có middleware route-gate (Phase 9 AUTH-06: `/admin/**` trong middleware matcher). Verify middleware matcher cover `/admin/reviews`. |
| T-21-04-02 | Tampering | Hard-delete bị trigger nhầm (mis-click) | mitigate | `window.confirm` với wording "Xoá vĩnh viễn review này? Không thể hoàn tác." (CONTEXT specifics line 238). |
| T-21-04-03 | Information Disclosure | productSlug null leak gì? | accept | productSlug null khi product đã soft-delete; FE render "—" — không leak data sensitive. |
| T-21-04-04 | Denial of Service | Pagination với size=20 trên ngàn reviews → slow | accept | BE Plan 21-02 cap size max 100; default 20. Demo project, dataset ~ trăm reviews tối đa. |
| T-21-04-05 | Repudiation | Admin actions không log | accept | Audit log defer per CONTEXT.md "Deferred Ideas". |
| T-21-04-06 | Spoofing (E2E) | E2E test login as admin với credential hard-coded leak vào git | mitigate | Sử dụng existing admin fixture pattern (Phase 9-15 đã thiết lập). KHÔNG hard-code credential mới — reuse env var hoặc fixture file đã trong gitignore. |
</threat_model>

<validation>
## Test Strategy (Nyquist enabled)

**E2E Playwright (Task 2):**
- File 1: `sources/frontend/tests/e2e/reviews-author-edit.spec.ts` — ≥ 2 tests (happy edit + soft-delete; window-expired defer nếu setup phức tạp).
- File 2: `sources/frontend/tests/e2e/admin-reviews-moderation.spec.ts` — ≥ 1 test (hide → PDP vắng → unhide → PDP hiện lại) + 1 filter test optional.
- Acceptance: `npx playwright test ... --reporter=list --workers=1` exit 0 (yêu cầu backend + frontend chạy).

**Static checks (Task 1):**
- `npx tsc --noEmit` passes — admin page types align.

**Manual smoke (recommended, không phải acceptance):**
- Mở `/admin/reviews` as admin → thấy table + filter + pagination.
- Hide một review → mở `/products/{slug}` → review vắng.
- Unhide → review hiện lại.
- Hard-delete → confirm dialog → xoá xong → review vắng vĩnh viễn.

**Cross-plan validation:**
- E2E specs trong plan này verify HÀNH VI tổng hợp của cả Phase 21:
  - reviews-author-edit.spec → REV-04 (edit/delete UX)
  - admin-reviews-moderation.spec → REV-06 (admin moderation flow + visibility filter ở public list)
  - REV-05 sort: optional thêm test trong admin spec hoặc author spec (assert dropdown đổi reorder list).
</validation>

<verification>
- /admin/reviews page tồn tại + render được không lỗi runtime.
- Sidebar nav có link "Đánh giá" hiển thị giữa Đơn hàng và Tài khoản.
- Cả 2 E2E spec syntactically valid (playwright test --list parse OK).
- E2E specs cover ≥ 1 flow cho REV-04 + ≥ 1 flow cho REV-06 (Plan 21-04 chính là validation chính cho REV-06).
- Toast wording đúng spec CONTEXT specifics 240.
- Hard-delete confirm wording đúng spec CONTEXT specifics 238.
</verification>

<success_criteria>
- [ ] `/admin/reviews/page.tsx` render table + filter + pagination + actions
- [ ] `/admin/reviews/page.module.css` style table sạch
- [ ] Sidebar admin có link "Đánh giá"
- [ ] reviews-author-edit.spec.ts có ≥ 2 tests pass (hoặc syntactically valid + chạy được trên CI)
- [ ] admin-reviews-moderation.spec.ts có ≥ 1 test pass demonstrating hide/unhide visibility filter
- [ ] tsc + Playwright list pass
- [ ] Status badge mapping đúng: Hiện (xanh) / Ẩn (đỏ) / Đã xoá (xám)
- [ ] Filter 4-state đúng wording per CONTEXT D-15
</success_criteria>

<output>
Sau khi hoàn thành, tạo `.planning/phases/21-hoan-thien-reviews/21-04-SUMMARY.md` ghi:
- Files created
- Sidebar nav delta
- E2E spec test counts + status (pass/skip lý do)
- Playwright run output (nếu chạy được)
- Any selector/locator gotchas phát hiện
- Manual smoke verifies done (nếu có)
- Phase 21 OVERALL completion status (3/3 success criteria covered: REV-04 author UX → 21-03; REV-05 sort → 21-03; REV-06 admin moderation + E2E → 21-04)
</output>
