---
phase: 21-hoan-thien-reviews
plan: 03
type: execute
wave: 3
depends_on: [21-02]
files_modified:
  - sources/frontend/src/types/index.ts
  - sources/frontend/src/services/reviews.ts
  - sources/frontend/src/app/products/[slug]/ReviewSection/ReviewSection.tsx
  - sources/frontend/src/app/products/[slug]/ReviewSection/ReviewList.tsx
  - sources/frontend/src/app/products/[slug]/ReviewSection/ReviewForm.tsx
  - sources/frontend/src/app/products/[slug]/ReviewSection/ReviewSection.module.css
autonomous: true
requirements: [REV-04, REV-05]
must_haves:
  truths:
    - "Tác giả review thấy nút 'Sửa' và 'Xoá' trên review của mình; non-owner KHÔNG thấy"
    - "Click 'Sửa' → ReviewForm mode='edit' inline thay thế item content; submit → PATCH → toast 'Đã cập nhật đánh giá' + refetch"
    - "Click 'Xoá' → window.confirm 'Xoá đánh giá này? Hành động không thể hoàn tác.' → DELETE → toast + refetch"
    - "Button 'Sửa' disabled với tooltip 'Đã quá thời hạn chỉnh sửa (24h)' khi vượt editWindowHours kể từ createdAt"
    - "Sort dropdown hiển thị cạnh <h3> 'Đánh giá từ khách hàng (N)' với 3 option Mới nhất / Đánh giá cao nhất / Đánh giá thấp nhất"
    - "Đổi sort → router.replace cập nhật ?sort= (default newest KHÔNG ghi vào URL) + fetch page 0 + reset list"
    - "Error mapping: REVIEW_EDIT_WINDOW_EXPIRED → 'Đã quá thời hạn chỉnh sửa (24h kể từ lúc đăng)'; REVIEW_NOT_OWNER → 'Bạn không có quyền chỉnh sửa review này'; REVIEW_NOT_FOUND → 'Review không tồn tại hoặc đã bị xoá'"
  artifacts:
    - path: "sources/frontend/src/types/index.ts"
      provides: "Review extended với hidden? + deletedAt?, SortKey type, AdminReview extends Review"
      contains: "export type SortKey"
    - path: "sources/frontend/src/services/reviews.ts"
      provides: "editReview, softDeleteReview, listReviews(sort), listAdminReviews, setReviewVisibility, hardDeleteReview"
      contains: "export function editReview"
    - path: "sources/frontend/src/app/products/[slug]/ReviewSection/ReviewSection.tsx"
      provides: "Sort state + URL persist + currentUserId pass-through + edit/delete handlers"
      contains: "router.replace"
    - path: "sources/frontend/src/app/products/[slug]/ReviewSection/ReviewList.tsx"
      provides: "Sort dropdown header + per-item edit/delete actions + inline edit form swap"
      contains: "actionsRow"
    - path: "sources/frontend/src/app/products/[slug]/ReviewSection/ReviewForm.tsx"
      provides: "mode prop ('create' | 'edit') + initialValues + onCancel"
      contains: "mode === 'edit'"
  key_links:
    - from: "ReviewList sort dropdown onChange"
      to: "ReviewSection.onSortChange → router.replace + loadPage(0, false, newSort)"
      via: "callback prop"
      pattern: "onSortChange"
    - from: "ReviewList per-item edit/delete buttons"
      to: "ReviewSection.handleEdit/handleDelete → services/reviews.ts editReview/softDeleteReview"
      via: "PATCH/DELETE /api/products/{pid}/reviews/{rid}"
      pattern: "editReview\\|softDeleteReview"
    - from: "ReviewSection editWindowHours state"
      to: "BE response config.editWindowHours (D-02)"
      via: "listReviews response read at load time"
      pattern: "config\\?\\.editWindowHours"
---

<objective>
FE author UX cho REV-04 (edit/delete inline) + REV-05 (sort dropdown + URL persist) trên PDP. Wave 3, parallel với 21-04 (admin moderation page).

Purpose: User mua hàng + review → có thể sửa trong 24h, xoá bất cứ lúc nào, sắp xếp danh sách review theo 3 chiều. Đây là 2/3 success criteria của Phase 21 từ user perspective.

Output: ReviewSection orchestrator quản lý sort state + edit/delete handlers; ReviewList render sort dropdown + per-item actions; ReviewForm support mode='edit'; services/reviews.ts có 5 hàm mới (3 author + 3 admin — admin dùng cho 21-04); types extend Review + SortKey + AdminReview.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-hoan-thien-reviews/21-CONTEXT.md
@.planning/phases/21-hoan-thien-reviews/21-RESEARCH.md
@.planning/phases/21-hoan-thien-reviews/21-PATTERNS.md
@.planning/phases/21-hoan-thien-reviews/21-02-SUMMARY.md
@sources/frontend/src/types/index.ts
@sources/frontend/src/services/reviews.ts
@sources/frontend/src/services/http.ts
@sources/frontend/src/services/errors.ts
@sources/frontend/src/app/products/[slug]/ReviewSection/ReviewSection.tsx
@sources/frontend/src/app/products/[slug]/ReviewSection/ReviewList.tsx
@sources/frontend/src/app/products/[slug]/ReviewSection/ReviewForm.tsx
@sources/frontend/src/app/products/[slug]/ReviewSection/ReviewSection.module.css
@sources/frontend/src/providers/AuthProvider.tsx

<interfaces>
<!-- Contracts từ BE plan 21-02 mà FE plan này tiêu thụ -->

BE endpoints (from 21-02):
```
GET    /api/products/{productId}/reviews?page=&size=&sort=newest|rating_desc|rating_asc
       → ApiResponse<{ content: Review[], totalElements, totalPages, currentPage, pageSize, isFirst, isLast, config: { editWindowHours: number } }>
PATCH  /api/products/{productId}/reviews/{reviewId}    body { rating?: number, content?: string }
       → ApiResponse<Review> | 403 REVIEW_NOT_OWNER | 422 REVIEW_EDIT_WINDOW_EXPIRED | 422 REVIEW_NOT_FOUND
DELETE /api/products/{productId}/reviews/{reviewId}    → 204
```

Existing FE primitives:
```typescript
// services/http.ts (Phase 13)
export const httpGet:    <T>(path: string) => Promise<T>;
export const httpPost:   <T>(path: string, body?: unknown) => Promise<T>;
export const httpPatch:  <T>(path: string, body?: unknown) => Promise<T>;   // verify exists; if not, add
export const httpDelete: <T>(path: string) => Promise<T>;                   // verify exists; if not, add

// services/errors.ts
export function isApiError(err: unknown): err is { code: string; message: string; status: number };

// providers/AuthProvider.tsx
export function useAuth(): { isAuthenticated: boolean; user: { id: string; email: string; name: string } | null };

// components/ui/Toast/Toast
export function useToast(): { showToast: (msg: string, kind: 'success'|'error') => void };
```

Types extended in this plan (consumed by 21-04 for admin):
```typescript
export interface Review {
  id: string;
  userId: string;
  reviewerName: string;
  productId: string;
  rating: number;
  content?: string;
  createdAt: string;
  hidden?: boolean;            // NEW Phase 21
  deletedAt?: string | null;   // NEW Phase 21
}
export type SortKey = 'newest' | 'rating_desc' | 'rating_asc';
export interface AdminReview extends Review {
  productSlug: string | null;
  productName?: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mở rộng types/index.ts + services/reviews.ts (5 hàm mới: editReview, softDeleteReview, listReviews(sort), listAdminReviews, setReviewVisibility, hardDeleteReview) + xác nhận httpPatch/httpDelete tồn tại</name>
  <read_first>
    - sources/frontend/src/types/index.ts (Review interface line 230-241 baseline)
    - sources/frontend/src/services/reviews.ts (toàn bộ — Phase 13 baseline)
    - sources/frontend/src/services/http.ts (verify httpPatch + httpDelete; nếu vắng thì thêm)
    - sources/frontend/src/services/products.ts (URLSearchParams pattern)
    - .planning/phases/21-hoan-thien-reviews/21-PATTERNS.md (§services/reviews.ts verbatim, §types/index.ts verbatim)
    - .planning/phases/21-hoan-thien-reviews/21-RESEARCH.md (Finding 1 — admin URL convention)
  </read_first>
  <action>
    **A. Sửa `sources/frontend/src/types/index.ts`** — extend Review + thêm SortKey + AdminReview:

    Tìm `export interface Review` và bổ sung 2 field optional (Phase 13 đã có id/userId/reviewerName/productId/rating/content/createdAt):
    ```typescript
    export interface Review {
      // ... existing fields ...
      hidden?: boolean;            // NEW Phase 21 (admin context)
      deletedAt?: string | null;   // NEW Phase 21 (ISO string from BE Instant)
    }
    ```

    Thêm sau Review:
    ```typescript
    export type SortKey = 'newest' | 'rating_desc' | 'rating_asc';

    export interface AdminReview extends Review {
      productSlug: string | null;     // null khi product đã soft-delete (Finding 9 gotcha)
      productName?: string;
    }
    ```

    **B. Verify httpPatch + httpDelete trong services/http.ts:**
    Grep `httpPatch\|httpDelete` trong `sources/frontend/src/services/http.ts`. Nếu KHÔNG có → thêm theo pattern httpGet/httpPost hiện có:
    ```typescript
    export const httpPatch  = <T>(path: string, body?: unknown) => request<T>('PATCH', path, body);
    export const httpDelete = <T>(path: string) => request<T>('DELETE', path);
    ```
    Nếu CÓ rồi → skip.

    **C. Sửa `sources/frontend/src/services/reviews.ts`** — verbatim từ PATTERNS §services/reviews.ts:

    Update import:
    ```typescript
    import type { Review, AdminReview, SortKey } from '@/types';
    import { httpGet, httpPost, httpPatch, httpDelete } from './http';
    ```

    Update `ReviewListResponse` interface (thêm `config?`):
    ```typescript
    export interface ReviewListResponse {
      content: Review[];
      totalElements: number;
      totalPages: number;
      currentPage: number;
      pageSize: number;
      isFirst: boolean;
      isLast: boolean;
      config?: { editWindowHours: number };       // NEW Phase 21 (D-02)
    }
    ```

    Update `listReviews` để nhận `sort: SortKey = 'newest'`:
    ```typescript
    export function listReviews(productId: string, page = 0, size = 10, sort: SortKey = 'newest'): Promise<ReviewListResponse> {
      const qs = new URLSearchParams();
      qs.set('page', String(page));
      qs.set('size', String(size));
      if (sort !== 'newest') qs.set('sort', sort);
      return httpGet<ReviewListResponse>(`/api/products/${encodeURIComponent(productId)}/reviews?${qs}`);
    }
    ```

    Thêm 5 hàm mới (3 author + 3 admin — admin reuse cho Plan 21-04):
    ```typescript
    export function editReview(productId: string, reviewId: string, body: { rating?: number; content?: string }): Promise<Review> {
      return httpPatch<Review>(
        `/api/products/${encodeURIComponent(productId)}/reviews/${encodeURIComponent(reviewId)}`,
        body
      );
    }

    export function softDeleteReview(productId: string, reviewId: string): Promise<void> {
      return httpDelete<void>(
        `/api/products/${encodeURIComponent(productId)}/reviews/${encodeURIComponent(reviewId)}`
      );
    }

    // === Admin endpoints (Finding 1: gateway path /api/products/admin/reviews/**) ===
    export interface AdminReviewListResponse {
      content: AdminReview[];
      totalElements: number;
      totalPages: number;
      currentPage: number;
      pageSize: number;
      isFirst: boolean;
      isLast: boolean;
    }

    export function listAdminReviews(page = 0, size = 20, filter: 'all'|'visible'|'hidden'|'deleted' = 'all'): Promise<AdminReviewListResponse> {
      const qs = new URLSearchParams({ page: String(page), size: String(size), filter });
      return httpGet<AdminReviewListResponse>(`/api/products/admin/reviews?${qs}`);
    }

    export function setReviewVisibility(reviewId: string, hidden: boolean): Promise<void> {
      return httpPatch<void>(
        `/api/products/admin/reviews/${encodeURIComponent(reviewId)}/visibility`,
        { hidden }
      );
    }

    export function hardDeleteReview(reviewId: string): Promise<void> {
      return httpDelete<void>(
        `/api/products/admin/reviews/${encodeURIComponent(reviewId)}`
      );
    }
    ```

    **CRITICAL: Admin URL paths PHẢI là `/api/products/admin/reviews/...` (KHÔNG `/api/admin/reviews/...`) per Finding 1.**
  </action>
  <verify>
    <automated>cd sources/frontend && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export type SortKey" sources/frontend/src/types/index.ts` = 1
    - `grep -c "export interface AdminReview" sources/frontend/src/types/index.ts` = 1
    - `grep -c "hidden\\?:" sources/frontend/src/types/index.ts` ≥ 1 (Review extension)
    - `grep -c "deletedAt\\?:" sources/frontend/src/types/index.ts` ≥ 1
    - `grep -c "export function editReview" sources/frontend/src/services/reviews.ts` = 1
    - `grep -c "export function softDeleteReview" sources/frontend/src/services/reviews.ts` = 1
    - `grep -c "export function listAdminReviews" sources/frontend/src/services/reviews.ts` = 1
    - `grep -c "export function setReviewVisibility" sources/frontend/src/services/reviews.ts` = 1
    - `grep -c "export function hardDeleteReview" sources/frontend/src/services/reviews.ts` = 1
    - `grep -c "/api/products/admin/reviews" sources/frontend/src/services/reviews.ts` ≥ 3 (list + visibility + hard-delete)
    - `grep -c "/api/admin/reviews" sources/frontend/src/services/reviews.ts` = 0 ← MUST be 0 (Finding 1)
    - `grep -c "config\\?:" sources/frontend/src/services/reviews.ts` ≥ 1 (ReviewListResponse field)
    - TypeScript compile clean: `npx tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>Types + service layer ready; httpPatch/httpDelete confirmed; tsc passes.</done>
</task>

<task type="auto">
  <name>Task 2: ReviewSection orchestrator (sort state + URL persist + edit/delete handlers + currentUserId/editWindowHours pass-through) + ReviewList sort dropdown header + per-item actions inline + edit form swap + ReviewForm mode='edit' + CSS</name>
  <read_first>
    - sources/frontend/src/app/products/[slug]/ReviewSection/ReviewSection.tsx (toàn bộ — orchestrator hiện tại)
    - sources/frontend/src/app/products/[slug]/ReviewSection/ReviewList.tsx (toàn bộ)
    - sources/frontend/src/app/products/[slug]/ReviewSection/ReviewForm.tsx (toàn bộ)
    - sources/frontend/src/app/products/[slug]/ReviewSection/ReviewSection.module.css
    - sources/frontend/src/providers/AuthProvider.tsx (verify field `user.id`)
    - sources/frontend/src/services/errors.ts (isApiError signature)
    - .planning/phases/21-hoan-thien-reviews/21-PATTERNS.md (§ReviewSection.tsx, §ReviewList.tsx, §ReviewForm.tsx, §ReviewSection.module.css verbatim)
    - .planning/phases/21-hoan-thien-reviews/21-RESEARCH.md (Finding 7 — URL persistence; Pitfall 5 — useAuth field; Pitfall 9 — ReviewForm reset on initialValues change)
  </read_first>
  <action>
    **A. Sửa `ReviewSection.tsx`** — verbatim từ PATTERNS §ReviewSection.tsx:

    **A.1 Imports + URL hooks:**
    ```typescript
    'use client';
    import { useCallback, useEffect, useState } from 'react';
    import { usePathname, useRouter, useSearchParams } from 'next/navigation';
    import { useAuth } from '@/providers/AuthProvider';
    import { useToast } from '@/components/ui/Toast/Toast';
    import { isApiError } from '@/services/errors';
    import { listReviews, editReview, softDeleteReview } from '@/services/reviews';
    import type { Review, SortKey } from '@/types';
    ```

    **A.2 State + sort init từ URL:**
    ```typescript
    const pathname = usePathname();
    const router = useRouter();
    const searchParams = useSearchParams();
    const initialSort = (searchParams.get('sort') as SortKey) ?? 'newest';
    const [sort, setSort] = useState<SortKey>(initialSort);
    const [editWindowHours, setEditWindowHours] = useState(24);
    const { user } = useAuth();
    const { showToast } = useToast();
    ```

    **A.3 Đổi `loadPage` accept sort param:**
    Tìm `loadPage` hiện tại và đổi signature `loadPage(page, append, sortOverride?: SortKey)`. Trong body: `const sortKey = sortOverride ?? sort; const res = await listReviews(productId, page, size, sortKey);`. Sau success: `if (res.config?.editWindowHours) setEditWindowHours(res.config.editWindowHours);`.

    **A.4 onSortChange handler (Finding 7 — URL persist):**
    ```typescript
    const onSortChange = useCallback((newSort: SortKey) => {
      const params = new URLSearchParams(searchParams.toString());
      if (newSort === 'newest') params.delete('sort');     // default không ghi (D-13)
      else params.set('sort', newSort);
      const qs = params.toString();
      router.replace(qs ? `${pathname}?${qs}` : pathname, { scroll: false });
      setSort(newSort);
      loadPage(0, false, newSort);   // refetch page 0
    }, [pathname, router, searchParams, loadPage]);
    ```

    **A.5 handleEdit + handleDelete handlers** (verbatim từ PATTERNS):
    ```typescript
    const handleEdit = useCallback(async (reviewId: string, body: { rating?: number; content?: string }) => {
      try {
        await editReview(productId, reviewId, body);
        showToast('Đã cập nhật đánh giá', 'success');
        await loadPage(0, false, sort);
      } catch (err) {
        if (isApiError(err)) {
          if (err.code === 'REVIEW_EDIT_WINDOW_EXPIRED') { showToast('Đã quá thời hạn chỉnh sửa (24h kể từ lúc đăng)', 'error'); return; }
          if (err.code === 'REVIEW_NOT_OWNER')          { showToast('Bạn không có quyền chỉnh sửa review này', 'error'); return; }
          if (err.code === 'REVIEW_NOT_FOUND')          { showToast('Review không tồn tại hoặc đã bị xoá', 'error'); return; }
        }
        showToast('Đã xảy ra lỗi. Vui lòng thử lại.', 'error');
        throw err;   // re-throw để ReviewList giữ form open cho user retry
      }
    }, [productId, sort, showToast, loadPage]);

    const handleDelete = useCallback(async (reviewId: string) => {
      if (!window.confirm('Xoá đánh giá này? Hành động không thể hoàn tác.')) return;
      try {
        await softDeleteReview(productId, reviewId);
        showToast('Đã xoá đánh giá', 'success');
        await loadPage(0, false, sort);
      } catch {
        showToast('Không thể xoá đánh giá. Vui lòng thử lại.', 'error');
      }
    }, [productId, sort, showToast, loadPage]);
    ```

    **A.6 Pass-through props xuống ReviewList:**
    ```tsx
    <ReviewList
      reviews={reviews}
      currentUserId={user?.id}              // Pitfall 5: AuthProvider field `id`
      editWindowHours={editWindowHours}
      sort={sort}
      onSortChange={onSortChange}
      onEdit={handleEdit}
      onDelete={handleDelete}
      /* ...existing props (loading, totalElements, etc.) */
    />
    ```

    **B. Sửa `ReviewList.tsx`** — thêm sort dropdown header + per-item actions:

    **B.1 Mở rộng props interface:**
    ```typescript
    interface ReviewListProps {
      /* ... existing ... */
      currentUserId?: string;
      editWindowHours: number;
      sort: SortKey;
      onSortChange: (s: SortKey) => void;
      onEdit: (reviewId: string, body: { rating?: number; content?: string }) => Promise<void>;
      onDelete: (reviewId: string) => Promise<void>;
    }
    ```

    **B.2 Thay `<h3>` standalone bằng list header với sort dropdown** (D-13):
    ```tsx
    <div className={styles.listHeader}>
      <h3 className={styles.listHeading}>Đánh giá từ khách hàng ({totalElements})</h3>
      <select
        className={styles.sortDropdown}
        value={sort}
        onChange={(e) => onSortChange(e.target.value as SortKey)}
        aria-label="Sắp xếp đánh giá"
      >
        <option value="newest">Mới nhất</option>
        <option value="rating_desc">Đánh giá cao nhất</option>
        <option value="rating_asc">Đánh giá thấp nhất</option>
      </select>
    </div>
    ```

    **B.3 Per-item edit/delete actions + inline edit form swap (D-21, D-22):**
    Thêm state `const [editingId, setEditingId] = useState<string | null>(null);` ở đầu component.

    Trong `.map((review) => ...)`, ngay TRƯỚC `</li>` (hoặc cuối review item div):
    ```tsx
    {(() => {
      const isOwner = currentUserId && review.userId === currentUserId;
      const editExpired = (Date.now() - new Date(review.createdAt).getTime()) > editWindowHours * 3600 * 1000;
      if (!isOwner) return null;
      if (editingId === review.id) {
        return (
          <ReviewForm
            mode="edit"
            initialValues={{ rating: review.rating, content: review.content ?? '' }}
            onCancel={() => setEditingId(null)}
            onSubmit={async (data) => {
              try {
                await onEdit(review.id, data);
                setEditingId(null);   // chỉ close khi success
              } catch { /* ReviewSection đã toast — giữ form mở để user thử lại */ }
            }}
          />
        );
      }
      return (
        <div className={styles.actionsRow}>
          <button
            type="button"
            className={styles.actionLink}
            disabled={editExpired}
            title={editExpired ? 'Đã quá thời hạn chỉnh sửa (24h)' : undefined}
            onClick={() => setEditingId(review.id)}
          >Sửa</button>
          <button
            type="button"
            className={`${styles.actionLink} ${styles.actionDanger}`}
            onClick={() => onDelete(review.id)}
          >Xoá</button>
        </div>
      );
    })()}
    ```

    **C. Sửa `ReviewForm.tsx`** — thêm prop `mode` + `initialValues` + `onCancel` + reset on initialValues change (Pitfall 9):

    ```typescript
    interface ReviewFormProps {
      mode?: 'create' | 'edit';
      initialValues?: { rating: number; content?: string };
      onSubmit: (data: { rating: number; content?: string }) => Promise<void>;
      onCancel?: () => void;
    }

    export default function ReviewForm({ mode = 'create', initialValues, onSubmit, onCancel }: ReviewFormProps) {
      const { register, handleSubmit, setValue, watch, reset, formState: { errors }, ... } = useForm<ReviewFormData>({
        resolver: zodResolver(reviewSchema),
        defaultValues: {
          rating: initialValues?.rating ?? 0,
          content: initialValues?.content ?? '',
        },
      });

      // Pitfall 9: reset khi mode/initialValues đổi (component remount-less swap)
      useEffect(() => {
        reset({ rating: initialValues?.rating ?? 0, content: initialValues?.content ?? '' });
      }, [mode, initialValues?.rating, initialValues?.content, reset]);

      // Submit row update:
      <div className={styles.submitRow}>
        {mode === 'edit' && onCancel && (
          <Button type="button" variant="secondary" onClick={onCancel} disabled={submitting}>Huỷ</Button>
        )}
        <Button type="submit" variant="primary" loading={submitting} disabled={submitting}>
          {mode === 'edit' ? 'Lưu thay đổi' : 'Gửi đánh giá'}
        </Button>
      </div>
    ```

    **Lưu ý:** giữ `reset({ rating: 0, content: '' })` cho `mode='create'` sau submit thành công (existing Phase 13 behavior — D-07). Trong `mode='edit'`, parent unmount form qua `setEditingId(null)`, KHÔNG reset.

    **D. Sửa `ReviewSection.module.css`** — thêm classes (verbatim từ PATTERNS):
    ```css
    .listHeader {
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: var(--space-3);
      margin-bottom: var(--space-3);
    }
    .sortDropdown {
      padding: var(--space-2) var(--space-3);
      border-radius: var(--radius-md);
      border: 1.5px solid var(--outline-variant);
      background: var(--surface-container-lowest);
      font-family: var(--font-family-body);
      cursor: pointer;
    }
    .actionsRow {
      display: flex;
      gap: var(--space-3);
      margin-top: var(--space-2);
    }
    .actionLink {
      background: none;
      border: none;
      padding: 0;
      font-size: var(--text-body-sm);
      color: var(--primary);
      cursor: pointer;
    }
    .actionLink:disabled { color: var(--on-surface-variant); cursor: not-allowed; }
    .actionDanger { color: var(--error); }
    ```

    Verify token names (`--outline-variant`, `--primary`, `--error`, `--space-2/3`, `--radius-md`, `--surface-container-lowest`, `--font-family-body`, `--text-body-sm`, `--on-surface-variant`) khớp với design tokens hiện có. Nếu một token nào KHÔNG tồn tại, fallback dùng class similar trong file (giữ visual consistency).
  </action>
  <verify>
    <automated>cd sources/frontend && npx tsc --noEmit -p tsconfig.json && npm run lint --silent</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "router.replace" sources/frontend/src/app/products/[slug]/ReviewSection/ReviewSection.tsx` ≥ 1
    - `grep -c "useSearchParams" sources/frontend/src/app/products/[slug]/ReviewSection/ReviewSection.tsx` = 1
    - `grep -c "REVIEW_EDIT_WINDOW_EXPIRED" sources/frontend/src/app/products/[slug]/ReviewSection/ReviewSection.tsx` = 1
    - `grep -c "REVIEW_NOT_OWNER" sources/frontend/src/app/products/[slug]/ReviewSection/ReviewSection.tsx` = 1
    - `grep -c "REVIEW_NOT_FOUND" sources/frontend/src/app/products/[slug]/ReviewSection/ReviewSection.tsx` = 1
    - `grep -c "currentUserId" sources/frontend/src/app/products/[slug]/ReviewSection/ReviewSection.tsx` ≥ 1
    - `grep -c "editWindowHours" sources/frontend/src/app/products/[slug]/ReviewSection/ReviewSection.tsx` ≥ 2 (state + pass-through)
    - `grep -c "sortDropdown" sources/frontend/src/app/products/[slug]/ReviewSection/ReviewList.tsx` ≥ 1
    - `grep -c "Mới nhất" sources/frontend/src/app/products/[slug]/ReviewSection/ReviewList.tsx` = 1
    - `grep -c "Đánh giá cao nhất" .../ReviewList.tsx` = 1
    - `grep -c "Đánh giá thấp nhất" .../ReviewList.tsx` = 1
    - `grep -c "actionsRow" .../ReviewList.tsx` ≥ 1
    - `grep -c "editingId" .../ReviewList.tsx` ≥ 2
    - `grep -c "Đã quá thời hạn chỉnh sửa (24h)" .../ReviewList.tsx` = 1
    - `grep -c "mode === 'edit'" .../ReviewForm.tsx` ≥ 1
    - `grep -c "initialValues" .../ReviewForm.tsx` ≥ 2
    - `grep -c "onCancel" .../ReviewForm.tsx` ≥ 2
    - `grep -c "Xoá đánh giá này\\? Hành động không thể hoàn tác" .../ReviewSection.tsx` = 1
    - `grep -c "\\.listHeader" sources/frontend/src/app/products/[slug]/ReviewSection/ReviewSection.module.css` = 1
    - `grep -c "\\.actionsRow" sources/frontend/src/app/products/[slug]/ReviewSection/ReviewSection.module.css` = 1
    - `npx tsc --noEmit` exit 0; `npm run lint` exit 0
  </acceptance_criteria>
  <done>FE author edit/delete UX hoàn chỉnh inline; sort dropdown + URL persistence; ReviewForm support edit mode; CSS aligned với design tokens.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → BE PATCH/DELETE | Bearer JWT auto-attached via httpPatch/httpDelete; ownership re-checked server-side |
| FE editWindowHours UI hint ↔ BE re-check | FE chỉ disable button (UX); BE Plan 21-02 re-check authoritative |
| FE state editingId ↔ DOM render | Owner check `review.userId === currentUserId` chỉ ảnh hưởng UI; non-owner không thấy actions row |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-03-01 | Spoofing | currentUserId từ AuthProvider có thể bị tamper trong dev tools | accept | FE chỉ render button; BE PATCH/DELETE re-check JWT (Plan 21-02 T-21-02-02). User self-DOS qua tamper không vấn đề. |
| T-21-03-02 | Tampering | Editing past 24h bằng cách tamper FE state hoặc gọi API trực tiếp | mitigate | BE re-check `Instant.now()` vs `createdAt + editWindowHours` (T-21-02-03). FE button disabled chỉ là UX hint. |
| T-21-03-03 | Information Disclosure | XSS qua editing content | mitigate | BE Jsoup.clean (T-21-02-05) + React text node render `{review.content}` — KHÔNG dangerouslySetInnerHTML (Phase 13 D-14 carry-over). |
| T-21-03-04 | Information Disclosure | Sort `?sort=` injection vào URL → XSS qua URL param | mitigate | `searchParams.get('sort') as SortKey` cast unsafe nhưng giá trị chỉ truyền vào BE qua URLSearchParams.set('sort', value). BE fallback newest cho invalid → no XSS surface (T-21-02 D-12). FE dùng `value` cho `<select>`, não dom inject. |
| T-21-03-05 | Tampering | window.confirm bị browser block hoặc skip → delete xảy ra without consent | accept | window.confirm là native API; rất hiếm bị block. Acceptable cho demo project. |
| T-21-03-06 | Repudiation | User edit/delete không log audit trail | accept | Audit log defer per CONTEXT.md "Deferred Ideas". |
</threat_model>

<validation>
## Test Strategy

**Plan này KHÔNG có FE unit tests** (per project visible-first priority + CONTEXT discretion). Validation = TypeScript + ESLint + manual smoke trong dev (defer chính thức cho Plan 21-04 Playwright E2E).

**Static checks (in tasks above):**
- `npx tsc --noEmit` passes — đảm bảo types align với services + props.
- `npm run lint` passes — code style + import order.

**Manual smoke checks (thực hiện trong dev server, KHÔNG phải acceptance criteria nhưng nên do):**
- Mở `/products/{slug}` đã có review → owner thấy "Sửa | Xoá".
- Click "Sửa" → form inline xuất hiện với rating/content prefilled.
- Submit → toast + list refresh với content mới.
- Đổi sort dropdown → URL update + list reorder.
- Refresh page với `?sort=rating_desc` → dropdown initial value khớp.

**E2E coverage** delegated to Plan 21-04 (`tests/e2e/reviews-author-edit.spec.ts`).
</validation>

<verification>
- TypeScript compile sạch (`tsc --noEmit` exit 0).
- ESLint pass.
- All admin URL paths trong reviews.ts đều bắt đầu `/api/products/admin/reviews` (Finding 1 verified by grep).
- ReviewSection consume `config?.editWindowHours` từ BE response (KHÔNG hard-code 24).
- Sort default newest KHÔNG ghi vào URL (params.delete khi newSort === 'newest').
- ReviewForm reset trên initialValues change (Pitfall 9 — useEffect deps).
</verification>

<success_criteria>
- [ ] types/index.ts có Review extended + SortKey + AdminReview
- [ ] services/reviews.ts có 5 hàm mới + admin URLs `/api/products/admin/reviews/...`
- [ ] ReviewSection có sort state + URL persist + edit/delete handlers + currentUserId pass-through
- [ ] ReviewList render sort dropdown + per-item actions + inline edit form swap
- [ ] ReviewForm support mode='edit' + initialValues + onCancel + reset on change
- [ ] CSS classes added with design tokens
- [ ] tsc + lint pass
- [ ] Error code mapping (3 codes → 3 Vietnamese messages) đúng spec CONTEXT specifics 240-244
</success_criteria>

<output>
Sau khi hoàn thành, tạo `.planning/phases/21-hoan-thien-reviews/21-03-SUMMARY.md` ghi:
- Files modified với delta lines
- 5 service functions exported (3 author + 3 admin reused)
- ReviewSection state shape (sort + editWindowHours + currentUserId source)
- Sort dropdown URL behavior (default suppression)
- Interface contracts cho 21-04 (admin functions + AdminReview type — 21-04 sẽ import)
</output>
