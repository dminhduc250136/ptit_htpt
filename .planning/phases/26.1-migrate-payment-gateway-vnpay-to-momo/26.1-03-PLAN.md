---
phase: 26.1-migrate-payment-gateway-vnpay-to-momo
plan: 03
type: execute
wave: 2
depends_on: [26.1-01, 26.1-02]
files_modified:
  - sources/frontend/src/types/index.ts
  - sources/frontend/src/lib/orderLabels.ts
  - sources/frontend/src/services/payments.ts
  - sources/frontend/src/app/checkout/page.tsx
  - sources/frontend/src/app/checkout/result/page.tsx
  - sources/frontend/src/app/profile/orders/[id]/page.tsx
  - sources/frontend/src/app/admin/orders/[id]/page.tsx
  - sources/frontend/e2e/12-vnpay-payment.spec.ts
  - sources/frontend/e2e/12-momo-payment.spec.ts
autonomous: true
requirements: [PAY-01, PAY-02, PAY-04]

must_haves:
  truths:
    - "Type Order field `vnpTransactionNo` rename thành `paymentTransactionNo` để khớp backend Plan 02"
    - "orderLabels.paymentMethodMap có entry MOMO; entry VNPAY bị xóa (D-08)"
    - "Checkout selector hiển thị 'Thanh toán qua MoMo' (value=MOMO); option VNPay bị xóa (D-15)"
    - "services/payments.ts có hàm getMomoReturn gọi GET /api/payments/momo/return; hàm getVNPayReturn bị xóa"
    - "Trang /checkout/result đọc query string MoMo (orderId/resultCode/transId/extraData) thay vì VNPay (vnp_ResponseCode/vnp_TxnRef) — giữ logic poll 3s×5 và 5 trạng thái UI-SPEC"
    - "Order display (/profile/orders/[id] + /admin/orders/[id]) hiển thị 'Mã giao dịch MoMo' với field paymentTransactionNo"
    - "Playwright spec 12-vnpay-payment.spec.ts xóa, thay bằng 12-momo-payment.spec.ts (cập nhật text + mocked endpoint)"
  artifacts:
    - path: "sources/frontend/src/app/checkout/result/page.tsx"
      provides: "Trang kết quả thanh toán MoMo + polling"
      contains: "getMomoReturn"
    - path: "sources/frontend/src/lib/orderLabels.ts"
      provides: "Nhãn MOMO"
      contains: "MOMO: 'Thanh toán qua MoMo'"
    - path: "sources/frontend/e2e/12-momo-payment.spec.ts"
      provides: "Playwright smoke spec MoMo (selector + result page 3 states)"
      contains: "Thanh toán qua MoMo"
  key_links:
    - from: "checkout/page.tsx submitOrder"
      to: "window.location paymentUrl"
      via: "redirect khi paymentMethod=MOMO"
      pattern: "paymentUrl"
    - from: "checkout/result/page.tsx getMomoReturn"
      to: "GET /api/payments/momo/return"
      via: "resolve orderId từ MoMo query string (orderId = paymentSessionId)"
      pattern: "getMomoReturn"
    - from: "checkout/result/page.tsx"
      to: "GET /api/orders/{id}"
      via: "poll payment_status mỗi 3s tối đa 5 lần (giữ logic Phase 26)"
      pattern: "getOrderById"
---

<objective>
Hoàn thiện frontend migration VNPay → MoMo: rename field type Order; cập nhật orderLabels + checkout selector + trang /checkout/result + order display để dùng MoMo; xóa file Playwright VNPay, tạo spec MoMo tương đương. Trang /checkout/result GIỮ NGUYÊN logic polling 3s×5 + 5 trạng thái UI-SPEC (D-16 — copy strings không đổi), chỉ thay query string parse + service function name + endpoint mocking.

Purpose: PAY-01 (selector MoMo + redirect), PAY-02 (return page tái dụng với endpoint MoMo), PAY-04 (order display hiển thị mã giao dịch MoMo) — phần khách hàng nhìn thấy.
Output: types/lib/services/checkout/result/order-display cập nhật + Playwright spec rename + zero VNPay references trong frontend.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26.1-migrate-payment-gateway-vnpay-to-momo/26.1-CONTEXT.md
@.planning/phases/26-vnpay-payment-integration/26-UI-SPEC.md
@.planning/phases/26-vnpay-payment-integration/26-04-PLAN.md
@.planning/phases/26-vnpay-payment-integration/26-04-SUMMARY.md
@.planning/phases/26.1-migrate-payment-gateway-vnpay-to-momo/26.1-01-PLAN.md
@.planning/phases/26.1-migrate-payment-gateway-vnpay-to-momo/26.1-02-PLAN.md

<interfaces>
<!-- Sau Plan 02: Order type FE đồng bộ với backend rename: -->
interface Order {
  // ... existing fields
  paymentStatus?: string;          // giữ nguyên (đã widened ở Phase 26-04 SUMMARY)
  paymentTransactionNo?: string;   // RENAME từ vnpTransactionNo (D-07/D-17)
  paymentUrl?: string;             // giữ nguyên (transient)
}

<!-- Sau Plan 01: payment-service endpoint mới -->
- POST /api/payments/momo/ipn (server-to-server, FE không gọi)
- GET /api/payments/momo/return → trả Map { valid, orderId, resultCode, paymentTransactionNo } cho FE
- POST /api/payments/sessions provider=MOMO → trả paymentUrl trỏ test-payment.momo.vn

<!-- MoMo redirect URL query string (từ MoMo sandbox redirect về /checkout/result) -->
MoMo gửi GET tới returnUrl với các query param:
- partnerCode, accessKey, requestId (= paymentSessionId), amount, orderId (= paymentSessionId), orderInfo, orderType, transId (= paymentTransactionNo), resultCode, message, payType, responseTime, extraData, signature
→ FE đọc: `resultCode`, `orderId` (= paymentSessionId — KHÔNG phải orderId nội bộ; cần resolve qua getMomoReturn), `transId`.

<!-- UI-SPEC 5 trạng thái (D-16 GIỮ NGUYÊN copy verbatim — UI-SPEC đã duyệt chung cho mọi gateway) -->
- "Đang xác nhận thanh toán" (poll)
- "Thanh toán thành công"
- "Thanh toán thất bại"
- "Đã huỷ thanh toán"
- "Chưa nhận được xác nhận" (poll timeout)

<!-- MoMo result code mapping (D-15/D-16): -->
- resultCode == "0" (string) hoặc "9000" (pending/authorized) → render "đang xác nhận" + poll
- resultCode == "1006" → user huỷ → "Đã huỷ thanh toán"
- resultCode khác 0 → render sơ bộ "Thanh toán thất bại" (vẫn poll vì IPN là nguồn sự thật T-26.1-04)
Lưu ý: Mapping resultCode → label là render SƠ BỘ; nguồn sự thật là payment_status poll qua GET /api/orders/{id} (mitigation T-26.1-04 inherited).

<!-- Phase 26 hiện có (sẽ sửa/xóa) -->
- src/types/index.ts: interface Order có `vnpTransactionNo?: string` → rename
- src/lib/orderLabels.ts: paymentMethodMap có `VNPAY: 'Thanh toán qua VNPay'` → xóa, thêm `MOMO: 'Thanh toán qua MoMo'`
- src/app/checkout/page.tsx: type union `'VNPAY'` + option `{value: 'VNPAY', label: 'Thanh toán qua VNPay', icon: '💳'}` + submitOrder redirect → đổi sang MOMO
- src/services/payments.ts: hàm `getVNPayReturn(searchParams)` → đổi thành `getMomoReturn(searchParams)` với endpoint `/api/payments/momo/return`
- src/app/checkout/result/page.tsx: đọc `vnp_ResponseCode` + `vnp_TxnRef` → đọc `resultCode` + `orderId` (MoMo query string); copy strings GIỮ NGUYÊN
- src/app/profile/orders/[id]/page.tsx + admin/orders/[id]/page.tsx: hiển thị `vnpTransactionNo` + label "Mã giao dịch VNPay" → `paymentTransactionNo` + "Mã giao dịch MoMo"
- e2e/12-vnpay-payment.spec.ts: text "Thanh toán qua VNPay" + mock `/api/payments/vnpay/return` → đổi MoMo
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: types/orderLabels/services/checkout — rename field + đổi enum + redirect MoMo</name>
  <read_first>
    - sources/frontend/src/types/index.ts (interface Order — file sửa)
    - sources/frontend/src/lib/orderLabels.ts (paymentMethodMap — file sửa)
    - sources/frontend/src/services/payments.ts (getVNPayReturn — file sửa)
    - sources/frontend/src/app/checkout/page.tsx (type union + payment options + submitOrder — file sửa)
    - .planning/phases/26.1-migrate-payment-gateway-vnpay-to-momo/26.1-CONTEXT.md §D-15, D-17, D-18
    - .planning/phases/26-vnpay-payment-integration/26-04-SUMMARY.md (tham khảo deviation 1 paymentStatus widened — KHÔNG đụng lại)
  </read_first>
  <action>
    **Bước 1 — Sửa `src/types/index.ts`** (interface `Order`):
    - Rename `vnpTransactionNo?: string` → `paymentTransactionNo?: string`. Giữ `paymentStatus?: string` + `paymentUrl?: string`.
    - `CreateOrderRequest.paymentMethod` (union type): xóa `'VNPAY'`, thêm `'MOMO'`. Giữ các value khác (`'COD'`, `'BANK_TRANSFER'`, `'E_WALLET'`).

    **Bước 2 — Sửa `src/lib/orderLabels.ts`** (`paymentMethodMap`):
    - Xóa entry `VNPAY: 'Thanh toán qua VNPay'`.
    - Thêm `MOMO: 'Thanh toán qua MoMo'` (D-15).
    - `paymentStatusMap` KHÔNG đụng (PENDING/PAID/FAILED chung cho mọi gateway).

    **Bước 3 — Sửa `src/services/payments.ts`** — đổi tên hàm + endpoint:
    ```ts
    // CŨ:
    export async function getVNPayReturn(searchParams: string) {
      return httpGet(`/api/payments/vnpay/return${searchParams}`, /* không cần JWT */);
    }

    // MỚI:
    export async function getMomoReturn(searchParams: string) {
      return httpGet(`/api/payments/momo/return${searchParams}`, /* không cần JWT */);
    }
    ```
    Giữ NGUYÊN logic httpGet + handle response (envelope nếu có). Response shape: `{ valid: boolean, orderId: string | null, resultCode: string, paymentTransactionNo: string | null }` (theo Plan 01 buildReturnView).

    **Bước 4 — Sửa `src/app/checkout/page.tsx`**:
    - Type union `paymentMethod`: xóa `| 'VNPAY'`, thêm `| 'MOMO'`.
    - Mảng payment options (~lines 333-337): xóa entry `{ value: 'VNPAY', label: 'Thanh toán qua VNPay', icon: '💳' }`, thêm `{ value: 'MOMO', label: 'Thanh toán qua MoMo', icon: '💳' }` (D-15 — giữ icon 💳 theo Claude's discretion; emoji 📱 cũng được nhưng 💳 giữ continuity).
    - `submitOrder` (~lines 188-220): đổi điều kiện redirect `form.paymentMethod === 'VNPAY'` → `form.paymentMethod === 'MOMO'`; đổi toast text "Đang chuyển tới cổng thanh toán VNPay..." → "Đang chuyển tới cổng thanh toán MoMo...". Giữ nguyên `window.location.assign(order.paymentUrl)` + cart cleanup logic. non-MOMO giữ hành vi cũ.
  </action>
  <verify>
    <automated>cd sources/frontend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep "paymentTransactionNo" types/index.ts` khớp; `grep "vnpTransactionNo" types/index.ts` KHÔNG match
    - `grep "'MOMO'" types/index.ts` khớp; `grep "'VNPAY'" types/index.ts` KHÔNG match
    - `grep "MOMO: 'Thanh toán qua MoMo'" lib/orderLabels.ts` khớp; `grep "VNPAY:" lib/orderLabels.ts` KHÔNG match
    - `grep "getMomoReturn" services/payments.ts` khớp; `grep "getVNPayReturn" services/payments.ts` KHÔNG match
    - `grep "momo/return" services/payments.ts` khớp; `grep "vnpay/return" services/payments.ts` KHÔNG match
    - `grep "'MOMO'" checkout/page.tsx` khớp; `grep "'VNPAY'" checkout/page.tsx` KHÔNG match
    - `grep "Đang chuyển tới cổng thanh toán MoMo" checkout/page.tsx` khớp
    - `npx tsc --noEmit` clean
  </acceptance_criteria>
  <done>Type Order rename; orderLabels MoMo; services getMomoReturn; checkout selector MOMO + redirect; type-check clean.</done>
</task>

<task type="auto">
  <name>Task 2: /checkout/result MoMo query + order display + Playwright spec rename</name>
  <read_first>
    - sources/frontend/src/app/checkout/result/page.tsx (file sửa — đọc vnp_ResponseCode/vnp_TxnRef + gọi getVNPayReturn)
    - sources/frontend/src/app/profile/orders/[id]/page.tsx (file sửa — label "Mã giao dịch VNPay")
    - sources/frontend/src/app/admin/orders/[id]/page.tsx (file sửa — label + badge)
    - sources/frontend/e2e/12-vnpay-payment.spec.ts (file XÓA — đọc trước để biết 4 test cases)
    - .planning/phases/26-vnpay-payment-integration/26-UI-SPEC.md §Trang kết quả thanh toán (5 copy strings giữ NGUYÊN)
    - .planning/phases/26.1-migrate-payment-gateway-vnpay-to-momo/26.1-CONTEXT.md §D-16, D-17
  </read_first>
  <action>
    **Bước 1 — Sửa `src/app/checkout/result/page.tsx`** — chuyển query string parse từ VNPay sang MoMo (D-16 logic GIỮ NGUYÊN polling + 5 trạng thái + copy strings):
    - Import: đổi `getVNPayReturn` → `getMomoReturn` (từ Plan 01 Task 3 alias).
    - Query string parse: đổi `searchParams.get('vnp_ResponseCode')` → `searchParams.get('resultCode')` (MoMo); `searchParams.get('vnp_TxnRef')` → `searchParams.get('orderId')` (MoMo orderId = paymentSessionId). Thêm `searchParams.get('transId')` nếu cần hiển thị transactionNo sơ bộ (optional).
    - Render sơ bộ từ `resultCode` (T-26.1-04: chỉ hiển thị, KHÔNG update DB — comment giữ nguyên):
      - `resultCode == "0"` → "đang xác nhận" (poll active)
      - `resultCode == "1006"` → "Đã huỷ thanh toán"
      - khác → "Thanh toán thất bại" (sơ bộ — vẫn poll vì IPN async)
    - Resolve orderId: gọi `getMomoReturn(window.location.search)` (forward NGUYÊN query string). Response: `{ valid, orderId, resultCode, paymentTransactionNo }`. Lấy `orderId` làm input cho polling. Nếu `valid=false` hoặc `orderId=null` → render empty state ("Không tìm thấy thông tin thanh toán" — copy UI-SPEC §CTA empty).
    - Polling: GIỮ NGUYÊN — `setInterval` 3000ms × 5 attempts, gọi `getOrderById(orderId)`, dừng sớm khi `paymentStatus !== 'PENDING'`, cleanup trong useEffect return. KHÔNG đụng constants `POLL_INTERVAL_MS = 3000`, `POLL_MAX_ATTEMPTS = 5`.
    - 5 trạng thái copy strings: GIỮ NGUYÊN verbatim từ UI-SPEC (D-16 — UI-SPEC dùng chung cho mọi gateway):
      - "Đang xác nhận thanh toán"
      - "Thanh toán thành công"
      - "Thanh toán thất bại"
      - "Đã huỷ thanh toán"
      - "Chưa nhận được xác nhận"
    - Comment `// T-26-13 mitigate` (từ Phase 26): đổi thành `// T-26.1-04 mitigate` (Spoofing — return URL chỉ hiển thị, nguồn sự thật là poll payment_status).
    - Suspense wrapper + Spinner inline + Button (primary "Xem đơn hàng" / "Thử thanh toán lại", secondary "Về trang chủ") + RetrySection: GIỮ NGUYÊN.

    **Bước 2 — Sửa `src/app/profile/orders/[id]/page.tsx`** — đổi label + field:
    - Dòng hiển thị mã giao dịch: đổi label "Mã giao dịch VNPay:" → "Mã giao dịch MoMo:" (D-17); đổi field `order.vnpTransactionNo` → `order.paymentTransactionNo`. Giữ logic ẩn dòng nếu rỗng.

    **Bước 3 — Sửa `src/app/admin/orders/[id]/page.tsx`** — tương tự:
    - Đổi label "Mã giao dịch VNPay:" → "Mã giao dịch MoMo:"; field `vnpTransactionNo` → `paymentTransactionNo`. Giữ `Badge` payment status (paymentStatusMap không đổi).

    **Bước 4 — XÓA `e2e/12-vnpay-payment.spec.ts` + TẠO `e2e/12-momo-payment.spec.ts`** (D-21 + D-15):
    - Xóa file cũ `sources/frontend/e2e/12-vnpay-payment.spec.ts`.
    - Tạo `sources/frontend/e2e/12-momo-payment.spec.ts` — copy structure từ file cũ (4 smoke tests), thay text + endpoint mock:
      ```ts
      import { test, expect } from '@playwright/test';

      test.describe('MoMo payment flow', () => {
        test('checkout page hiển thị option "Thanh toán qua MoMo"', async ({ page }) => {
          await page.goto('/checkout');
          await expect(page.getByText('Thanh toán qua MoMo')).toBeVisible();
        });

        test('/checkout/result với resultCode=0 render "Đang xác nhận thanh toán"', async ({ page }) => {
          await page.route('**/api/payments/momo/return*', (route) =>
            route.fulfill({ status: 200, contentType: 'application/json',
              body: JSON.stringify({ valid: true, orderId: 'ord-123', resultCode: '0', paymentTransactionNo: '2024xxxx' }) }));
          await page.route('**/api/orders/ord-123', (route) =>
            route.fulfill({ status: 200, contentType: 'application/json',
              body: JSON.stringify({ id: 'ord-123', paymentStatus: 'PENDING' }) }));
          await page.goto('/checkout/result?resultCode=0&orderId=sess-456&transId=2024xxxx');
          await expect(page.getByText('Đang xác nhận thanh toán')).toBeVisible();
        });

        test('/checkout/result với resultCode=1006 render "Đã huỷ thanh toán"', async ({ page }) => {
          // mock getMomoReturn trả valid=true, orderId=null hoặc render sơ bộ từ query
          await page.route('**/api/payments/momo/return*', (route) =>
            route.fulfill({ status: 200, contentType: 'application/json',
              body: JSON.stringify({ valid: true, orderId: 'ord-789', resultCode: '1006', paymentTransactionNo: null }) }));
          await page.route('**/api/orders/ord-789', (route) =>
            route.fulfill({ status: 200, contentType: 'application/json',
              body: JSON.stringify({ id: 'ord-789', paymentStatus: 'FAILED' }) }));
          await page.goto('/checkout/result?resultCode=1006&orderId=sess-789');
          await expect(page.getByText('Đã huỷ thanh toán')).toBeVisible();
        });

        test('/checkout/result với valid=false render empty state', async ({ page }) => {
          await page.route('**/api/payments/momo/return*', (route) =>
            route.fulfill({ status: 200, contentType: 'application/json',
              body: JSON.stringify({ valid: false, orderId: null, resultCode: 'x', paymentTransactionNo: null }) }));
          await page.goto('/checkout/result?resultCode=99&orderId=invalid');
          await expect(page.getByText('Không tìm thấy thông tin thanh toán')).toBeVisible();
        });
      });
      ```
    - LƯU Ý: Playwright spec runtime defer cho /gsd-verify-work (precedent Phase 19/23/26 — spec list-only verify ở task này; ghi rõ trong SUMMARY).
  </action>
  <verify>
    <automated>cd sources/frontend && npx tsc --noEmit && npx playwright test 12-momo-payment --list</automated>
  </verify>
  <acceptance_criteria>
    - `grep "getMomoReturn" checkout/result/page.tsx` khớp; `grep "getVNPayReturn" checkout/result/page.tsx` KHÔNG match
    - `grep "resultCode" checkout/result/page.tsx` khớp; `grep "vnp_ResponseCode\\|vnp_TxnRef" checkout/result/page.tsx` KHÔNG match
    - `grep "Đang xác nhận thanh toán" checkout/result/page.tsx` khớp (UI-SPEC verbatim giữ NGUYÊN)
    - `grep "POLL_INTERVAL_MS = 3000\\|3000" checkout/result/page.tsx` khớp; `grep "POLL_MAX_ATTEMPTS = 5\\|= 5" checkout/result/page.tsx` khớp
    - `grep "Mã giao dịch MoMo" profile/orders/[id]/page.tsx` khớp; `grep "VNPay" profile/orders/[id]/page.tsx` KHÔNG match (cho lines liên quan payment)
    - `grep "paymentTransactionNo" profile/orders/[id]/page.tsx` + `admin/orders/[id]/page.tsx` khớp cả 2 file
    - `grep "vnpTransactionNo" profile/orders/[id]/page.tsx\\|admin/orders/[id]/page.tsx` KHÔNG match
    - File `e2e/12-vnpay-payment.spec.ts` KHÔNG tồn tại
    - File `e2e/12-momo-payment.spec.ts` tồn tại; `grep "Thanh toán qua MoMo" 12-momo-payment.spec.ts` khớp; `grep "momo/return" 12-momo-payment.spec.ts` khớp
    - `npx tsc --noEmit` clean
    - `npx playwright test 12-momo-payment --list` ≥ 3 tests listed
  </acceptance_criteria>
  <done>Trang /checkout/result đọc MoMo query string + giữ polling 3s×5 + 5 trạng thái UI-SPEC; order display label + field MoMo; Playwright spec rename; type-check + Playwright list clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → return URL query string (MoMo redirect) | resultCode/orderId/transId từ MoMo redirect — KHÔNG tin để cập nhật DB (inherited Phase 26 nguyên tắc) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26.1-12 | Spoofing/Tampering | checkout/result/page.tsx resultCode | mitigate | resultCode CHỈ render sơ bộ; nguồn sự thật là payment_status poll qua GET /api/orders/{id} — user fake query string không đổi được trạng thái thật (D-16, nguyên tắc D-06 Phase 26 inherited) |
| T-26.1-13 | Information Disclosure | order display paymentTransactionNo | accept | Mã giao dịch MoMo hiển thị cho chủ đơn (ownership check getOrderForUser từ Phase 17/18) + admin — không phải secret nhạy cảm |
| T-26.1-14 | Tampering | getMomoReturn forward query string | mitigate | getMomoReturn forward NGUYÊN window.location.search; payment-service verifyIpn/return tự verify chữ ký SHA256 — FE không tin query, server-side là quyết định cuối |
</threat_model>

<verification>
- `npx tsc --noEmit` clean
- `npx playwright test 12-momo-payment --list` liệt kê ≥ 3 spec
- Trang /checkout/result giữ polling 3s × 5 với MoMo query string parse
- Order display ở cả profile + admin hiển thị "Mã giao dịch MoMo" + field paymentTransactionNo
- frontend zero VNPay references: `grep -rE "VNPay|vnpay|VNPAY|vnp_|vnpTransactionNo|getVNPayReturn" sources/frontend/src sources/frontend/e2e 2>/dev/null` zero matches
</verification>

<success_criteria>
- Selector checkout có option MoMo → redirect paymentUrl trỏ test-payment.momo.vn
- /checkout/result hiển thị 5 trạng thái + poll payment_status với MoMo query
- /profile/orders/[id] + /admin/orders/[id] hiển thị "Mã giao dịch MoMo" + paymentTransactionNo
- e2e/12-momo-payment.spec.ts thay e2e/12-vnpay-payment.spec.ts
- frontend zero VNPay references
</success_criteria>

<output>
After completion, create `.planning/phases/26.1-migrate-payment-gateway-vnpay-to-momo/26.1-03-SUMMARY.md`
</output>
