---
phase: 26.1-migrate-payment-gateway-vnpay-to-momo
plan: 04
type: execute
wave: 3
depends_on: [26.1-01, 26.1-02, 26.1-03]
files_modified:
  - .planning/phases/26.1-migrate-payment-gateway-vnpay-to-momo/26.1-CLEANUP-VERIFY.md
autonomous: true
requirements: [PAY-01, PAY-02, PAY-03, PAY-04]

must_haves:
  truths:
    - "Toàn bộ source code (backend + frontend) zero VNPay references — SC5 enforced bằng grep audit"
    - "Smoke test mới: Playwright list 12-momo-payment OK + mvn compile cả payment-service + order-service OK"
    - "File audit report 26.1-CLEANUP-VERIFY.md tạo ra ghi log kết quả grep + verify command"
  artifacts:
    - path: ".planning/phases/26.1-migrate-payment-gateway-vnpay-to-momo/26.1-CLEANUP-VERIFY.md"
      provides: "Audit report verify SC5 (zero VNPay references) + smoke tests"
      contains: "VNPay references"
  key_links:
    - from: "SC5 phase 26.1"
      to: "grep audit results"
      via: "verify command zero matches"
      pattern: "VNPay"
---

<objective>
Cleanup verification cuối cùng cho Phase 26.1: chạy grep audit toàn codebase confirm zero VNPay references còn lại (SC5 enforcement); chạy smoke test compile + Playwright list mới; tạo file 26.1-CLEANUP-VERIFY.md ghi log kết quả để làm bằng chứng cho `/gsd-verify-work`. Plan này KHÔNG tạo code mới — chỉ verify cumulative output của Plan 01-03.

Purpose: SC5 Phase 26.1 = "Code VNPay (`paymentservice/vnpay/` + test) bị xóa sạch; không còn reference `VNPAY` / `vnp_*` trong codebase" — Plan 01-03 đã xóa từng phần; plan này verify tổng thể bằng grep + ghi nhận bất kỳ residue nào (acceptable matches: comment lịch sử, git log, `.planning/` artifacts Phase 26 archive).
Output: 26.1-CLEANUP-VERIFY.md với kết quả grep + smoke test status; nếu phát hiện residue ngoài acceptable list → task FAIL + tạo task remediation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26.1-migrate-payment-gateway-vnpay-to-momo/26.1-CONTEXT.md
@.planning/phases/26.1-migrate-payment-gateway-vnpay-to-momo/26.1-01-PLAN.md
@.planning/phases/26.1-migrate-payment-gateway-vnpay-to-momo/26.1-02-PLAN.md
@.planning/phases/26.1-migrate-payment-gateway-vnpay-to-momo/26.1-03-PLAN.md

<acceptable_matches>
**Acceptable VNPay matches (KHÔNG fail SC5):**
- `.planning/phases/26-vnpay-payment-integration/**/*` — Phase 26 archive artifacts (giữ làm history)
- `.planning/phases/26-vnpay-payment-integration/*.md` — PLAN/SUMMARY/RESEARCH/CONTEXT cũ
- `.planning/STATE.md` — phần "Roadmap Evolution" + "Decisions" lưu lịch sử Phase 26
- `.planning/ROADMAP.md` — phần "Phase 26: Tích Hợp Thanh Toán VNPay Sandbox" (header lịch sử) + "Phase 26.1" header
- `.planning/REQUIREMENTS.md` — coverage map Phase 26
- `.planning/milestones/` — milestone archive nếu có
- `.planning/RETROSPECTIVE.md` — milestone retrospective nếu có
- `.planning/phases/26.1-*/26.1-*-PLAN.md` — chính các plan này (mô tả migration mention VNPay)
- `.planning/phases/26.1-*/26.1-*-SUMMARY.md` — summary plan 01-03 mô tả deviation
- `.planning/phases/26.1-*/26.1-CLEANUP-VERIFY.md` — chính file này
- `docker-compose.yml` ở dòng comment lịch sử (nếu Plan 01 Task 3 để comment giải thích migration)
- Git history / commit messages — KHÔNG audit
- `.git/` — KHÔNG audit

**UNACCEPTABLE matches (FAIL SC5):**
- Bất kỳ file dưới `sources/backend/payment-service/src/main/` hoặc `sources/backend/payment-service/src/test/`
- Bất kỳ file dưới `sources/backend/order-service/src/main/` hoặc `sources/backend/order-service/src/test/`
- Bất kỳ file dưới `sources/backend/api-gateway/src/`
- Bất kỳ file dưới `sources/frontend/src/` hoặc `sources/frontend/e2e/`
- `docker-compose.yml` ngoài comment lịch sử (env vars VNP_* phải biến mất)
</acceptable_matches>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Grep audit toàn codebase + smoke test compile + tạo 26.1-CLEANUP-VERIFY.md</name>
  <read_first>
    - .planning/phases/26.1-migrate-payment-gateway-vnpay-to-momo/26.1-01-SUMMARY.md (nếu đã tạo bởi Plan 01)
    - .planning/phases/26.1-migrate-payment-gateway-vnpay-to-momo/26.1-02-SUMMARY.md (nếu đã tạo bởi Plan 02)
    - .planning/phases/26.1-migrate-payment-gateway-vnpay-to-momo/26.1-03-SUMMARY.md (nếu đã tạo bởi Plan 03)
    - sources/backend/payment-service/src/main/java/com/ptit/htpt/paymentservice/momo/MomoConfig.java (Plan 01 output — verify EXISTS)
    - sources/backend/order-service/src/main/resources/db/migration/V7__rename_vnp_transaction_no.sql (Plan 02 output — verify EXISTS)
    - sources/frontend/e2e/12-momo-payment.spec.ts (Plan 03 output — verify EXISTS)
  </read_first>
  <action>
    **Bước 1 — Chạy grep audit code source** (loại trừ `.planning/`, `.git/`, `node_modules/`, `target/`, build artifacts):

    Pattern tìm: `VNPay|vnpay|VNPAY|vnp_|HmacSHA512|VNPayConfig|VNPaySignature|VNPayController|VNPayService|vnpTransactionNo|getVNPayReturn|createVNPaySession`

    Trên Windows PowerShell hoặc cross-platform, ưu tiên dùng tool Grep (ripgrep) với glob exclude. Lệnh tương đương:
    ```bash
    # Pseudo — implementer chuyển sang tool Grep:
    rg -e "VNPay|vnpay|VNPAY|vnp_|HmacSHA512|vnpTransactionNo|getVNPayReturn|createVNPaySession" \
      sources/ \
      --glob '!**/target/**' \
      --glob '!**/node_modules/**' \
      --glob '!**/.next/**' \
      --glob '!**/dist/**' \
      --glob '!**/*.lock' \
      --glob '!**/results.json' \
      -c
    ```

    Đếm số match theo file. Mỗi file có match trong `sources/` → ghi nhận vào report.

    **Bước 2 — Audit docker-compose.yml**:
    ```bash
    grep -nE "VNP_|vnpay|VNPay|VNPAY" docker-compose.yml
    ```
    Chấp nhận 0 match HOẶC chỉ match trong comment line (bắt đầu `#`).

    **Bước 3 — Smoke test compile** (xác nhận Plan 01-03 không break build):
    - Backend payment-service: `mvn -q compile -pl payment-service` (nếu Maven available)
    - Backend order-service: `mvn -q compile -pl order-service`
    - Frontend: `cd sources/frontend && npx tsc --noEmit`
    - Frontend Playwright list: `cd sources/frontend && npx playwright test 12-momo-payment --list`

    Nếu Maven defer trên Windows env (precedent Phase 19/23/26) → ghi rõ "Maven defer Windows — compile-clean verify cho /gsd-verify-work" trong report. Frontend tsc + playwright list PHẢI chạy được (npm tools always available trên dev box).

    **Bước 4 — Tạo `.planning/phases/26.1-migrate-payment-gateway-vnpay-to-momo/26.1-CLEANUP-VERIFY.md`** ghi log:
    ```markdown
    ---
    phase: 26.1
    plan: 04
    verified_at: <YYYY-MM-DD>
    sc5_status: PASS | FAIL
    ---

    # Phase 26.1 — Cleanup Verification (SC5 enforcement)

    ## Grep Audit — Source Code (sources/)

    **Pattern:** `VNPay|vnpay|VNPAY|vnp_|HmacSHA512|vnpTransactionNo|getVNPayReturn|createVNPaySession`

    **Excludes:** `**/target/**`, `**/node_modules/**`, `**/.next/**`, `**/dist/**`, `**/*.lock`, `**/results.json`

    ### Results

    | File | Match Count | Acceptable? | Note |
    |------|-------------|-------------|------|
    | (paste rg output) | | | |

    **Total UNACCEPTABLE matches:** `<N>` (must be 0 for SC5 PASS)

    ## Docker-Compose Audit

    `grep -nE "VNP_|vnpay|VNPay|VNPAY" docker-compose.yml`:
    ```
    (paste output — phải 0 match hoặc chỉ comment line)
    ```

    ## Smoke Tests

    | Test | Command | Status |
    |------|---------|--------|
    | payment-service compile | `mvn -q compile -pl payment-service` | PASS / DEFER (Windows Maven) |
    | order-service compile | `mvn -q compile -pl order-service` | PASS / DEFER |
    | Frontend tsc | `cd sources/frontend && npx tsc --noEmit` | PASS / FAIL |
    | Playwright list | `npx playwright test 12-momo-payment --list` | PASS / FAIL (`<N>` tests) |

    ## Artifacts Verification

    | Artifact | Path | Exists |
    |----------|------|--------|
    | MomoConfig.java | sources/backend/payment-service/src/main/java/com/ptit/htpt/paymentservice/momo/MomoConfig.java | ✅ / ❌ |
    | MomoSignature.java | (same dir) | ✅ / ❌ |
    | MomoService.java | (same dir) | ✅ / ❌ |
    | MomoController.java | (same dir) | ✅ / ❌ |
    | V7__rename_vnp_transaction_no.sql | sources/backend/order-service/src/main/resources/db/migration/V7__rename_vnp_transaction_no.sql | ✅ / ❌ |
    | 12-momo-payment.spec.ts | sources/frontend/e2e/12-momo-payment.spec.ts | ✅ / ❌ |
    | (VNPay residue) vnpay/ dir | sources/backend/payment-service/src/main/java/com/ptit/htpt/paymentservice/vnpay/ | ❌ (expected — KHÔNG tồn tại) |
    | (VNPay residue) 12-vnpay-payment.spec.ts | sources/frontend/e2e/12-vnpay-payment.spec.ts | ❌ (expected — KHÔNG tồn tại) |

    ## SC5 Verdict

    `<PASS | FAIL>` — 5 Success Criteria của Phase 26.1:
    - SC1: Khách chọn MoMo + redirect — verified by Plan 03 acceptance + UAT
    - SC2: Trang kết quả 5 trạng thái — verified by Plan 03 acceptance + UAT
    - SC3: IPN HMAC SHA256 + idempotent — verified by Plan 01 MomoIpnControllerIT
    - SC4: Order display payment fields MoMo — verified by Plan 03 acceptance
    - SC5: Zero VNPay references trong source code — verified by THIS audit

    ## Remediation (nếu SC5 FAIL)

    Liệt kê file có UNACCEPTABLE match + action plan để cleanup.
    ```

    **Bước 5 — Nếu phát hiện UNACCEPTABLE residue** (rare — Plan 01-03 đã grep từng phần):
    - Báo trong CLEANUP-VERIFY.md status FAIL.
    - Tạo follow-up task ngay trong commit này: xóa/cập nhật residue file → re-run audit. KHÔNG ship FAIL về `/gsd-verify-work`.

    **Bước 6 — Commit file CLEANUP-VERIFY.md** với message `docs(26.1-04): SC5 cleanup verification — zero VNPay references audit`.
  </action>
  <verify>
    <automated>test -f .planning/phases/26.1-migrate-payment-gateway-vnpay-to-momo/26.1-CLEANUP-VERIFY.md && grep -q "sc5_status: PASS" .planning/phases/26.1-migrate-payment-gateway-vnpay-to-momo/26.1-CLEANUP-VERIFY.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/26.1-migrate-payment-gateway-vnpay-to-momo/26.1-CLEANUP-VERIFY.md` tồn tại với frontmatter + 5 section (Grep Audit / Docker-Compose / Smoke Tests / Artifacts / SC5 Verdict)
    - `grep "sc5_status: PASS" 26.1-CLEANUP-VERIFY.md` khớp (status PASS — nếu FAIL phải remediate trước)
    - Section "Grep Audit" liệt kê output thật của ripgrep — KHÔNG để placeholder `(paste rg output)`
    - Section "Smoke Tests" có status concrete cho từng command (PASS / DEFER / FAIL — không để rỗng)
    - Section "Artifacts Verification" check 6+ artifact path với ✅/❌ chính xác
    - 0 UNACCEPTABLE match trong section grep audit (chỉ acceptable matches dưới `.planning/`)
  </acceptance_criteria>
  <done>26.1-CLEANUP-VERIFY.md commit; SC5 verdict PASS; zero VNPay UNACCEPTABLE residue trong source code; smoke test status documented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Cleanup verification — không thêm code path mới, không thay đổi attack surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26.1-15 | Tampering (incomplete cleanup) | SC5 enforcement | mitigate | Grep audit toàn `sources/` + docker-compose với pattern toàn diện (`VNPay|vnpay|VNPAY|vnp_|HmacSHA512|vnpTransactionNo|getVNPayReturn|createVNPaySession`); ghi log kết quả vào CLEANUP-VERIFY.md để traceable |
</threat_model>

<verification>
- `26.1-CLEANUP-VERIFY.md` tồn tại + `sc5_status: PASS`
- Grep audit log thật (không placeholder)
- Smoke test status documented cho 4 command (payment-svc compile / order-svc compile / tsc / playwright list)
- 0 UNACCEPTABLE match trong source code
</verification>

<success_criteria>
- File CLEANUP-VERIFY.md committed với sc5_status PASS
- Grep audit confirm zero VNPay references trong sources/
- Smoke tests (frontend tsc + playwright list) PASS; backend compile PASS hoặc DEFER documented
</success_criteria>

<output>
After completion, create `.planning/phases/26.1-migrate-payment-gateway-vnpay-to-momo/26.1-04-SUMMARY.md`
</output>
