---
phase: 26.1
slug: migrate-payment-gateway-vnpay-to-momo
status: draft
nyquist_compliant: true
wave_0_complete: false
created: 2026-05-23
---

# Phase 26.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution. Migration phase — kế thừa pattern Validation từ Phase 26.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | JUnit 5 + Spring Boot Test (backend, Maven Surefire/Failsafe); Playwright (frontend E2E) |
| **Config file** | `sources/backend/*/pom.xml`; `sources/frontend/playwright.config.ts` (testDir: `./e2e`) |
| **Quick run command** | `mvn -q test -pl payment-service` hoặc `mvn -q test -pl order-service` (per-service) |
| **Full suite command** | `mvn -q verify` (payment-service + order-service IT) + `cd sources/frontend && npx tsc --noEmit && npx playwright test 12-momo-payment --list` |
| **Estimated runtime** | ~120 seconds (đa số test offline; Maven defer Windows env như Phase 26) |

---

## Sampling Rate

- **After every task commit:** Run `mvn -q test -pl <service>` cho service vừa sửa (hoặc `npx tsc --noEmit` cho FE task)
- **After every plan wave:** Run `mvn -q verify` cho service liên quan + frontend tsc + playwright list
- **Before `/gsd-verify-work`:** Full suite phải xanh + 26.1-CLEANUP-VERIFY.md có sc5_status=PASS
- **Max feedback latency:** 120 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 26.1-01-01 | 01 | 1 | PAY-01, PAY-03 | T-26.1-01, T-26.1-05 | Xóa sạch vnpay/; HMAC SHA256 ký/verify đúng (data string thứ tự cố định create + lexicographic IPN); secret không hardcode | unit | `mvn -q test -pl payment-service -Dtest=MomoSignatureTest` | ❌ W0 (`MomoSignatureTest.java`) | ⬜ pending |
| 26.1-01-02 | 01 | 1 | PAY-03 | T-26.1-01, T-26.1-02, T-26.1-03, T-26.1-04 | IPN verify chữ ký + so khớp amount + idempotent terminal + return URL chỉ hiển thị; HTTP 204 No Content; envelope field rename paymentTransactionNo | integration (Mockito plain) | `mvn -q test -pl payment-service -Dtest=MomoIpnControllerIT` | ❌ W0 (`MomoIpnControllerIT.java`) | ⬜ pending |
| 26.1-01-03 | 01 | 1 | PAY-01 | T-26.1-05, T-26.1-06 | POST /payments/sessions provider=MOMO trả payUrl từ MoMo create endpoint; gateway whitelist POST momo/ipn + GET momo/return; docker-compose env MOMO_* | integration | `mvn -q test -pl payment-service` | ❌ W0 (`application-test.yml` momo block) | ⬜ pending |
| 26.1-02-01 | 02 | 1 | PAY-04 | T-26.1-08 | V7 RENAME COLUMN vnp_transaction_no → payment_transaction_no atomic; OrderEntity/OrderDto/OrderMapper field paymentTransactionNo; Flyway validate qua ddl-auto=validate | integration | `mvn -q test -pl order-service -Dtest=OrderMapper*` | ❌ W0 (validate gián tiếp Spring context) | ⬜ pending |
| 26.1-02-02 | 02 | 1 | PAY-01, PAY-04 | T-26.1-09, T-26.1-11 | PaymentEventEnvelope shape khớp Plan 01 (paymentTransactionNo); OrderCrudService switch MOMO; PaymentSessionClient.createMomoSession provider=MOMO; PaymentEventListener setPaymentTransactionNo idempotent | integration | `mvn -q test -pl order-service -Dtest=OrderCrudServiceMomoIT,PaymentEventListenerIT` | ❌ W0 (`OrderCrudServiceMomoIT.java` mới) | ⬜ pending |
| 26.1-03-01 | 03 | 2 | PAY-01, PAY-04 | T-26.1-13, T-26.1-14 | Type Order rename field; orderLabels MOMO; services/payments.ts getMomoReturn endpoint; checkout selector option MoMo + redirect window.location paymentUrl | type-check | `cd sources/frontend && npx tsc --noEmit` | ✅ tooling sẵn có | ⬜ pending |
| 26.1-03-02 | 03 | 2 | PAY-02, PAY-04 | T-26.1-12, T-26.1-13 | Trang /checkout/result parse MoMo query (resultCode/orderId/transId) + giữ polling 3s×5 + 5 trạng thái UI-SPEC verbatim; order display "Mã giao dịch MoMo"; Playwright spec 12-momo thay 12-vnpay | type-check + smoke | `cd sources/frontend && npx tsc --noEmit && npx playwright test 12-momo-payment --list` | ❌ W0 (`12-momo-payment.spec.ts` mới) | ⬜ pending |
| 26.1-04-01 | 04 | 3 | PAY-01, PAY-02, PAY-03, PAY-04 | T-26.1-15 | SC5 enforcement: grep audit toàn `sources/` + docker-compose zero VNPay UNACCEPTABLE matches; smoke compile + Playwright list documented trong CLEANUP-VERIFY.md | meta-audit | `test -f .planning/phases/26.1-migrate-payment-gateway-vnpay-to-momo/26.1-CLEANUP-VERIFY.md && grep -q "sc5_status: PASS" .planning/phases/26.1-migrate-payment-gateway-vnpay-to-momo/26.1-CLEANUP-VERIFY.md` | ✅ tooling sẵn có | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] Test class `MomoSignatureTest.java` — 5 @Test: HMAC SHA256 round-trip + verify valid/tampered + buildSignData create (fixed order) + buildSignData IPN (lexicographic) (PAY-03)
- [ ] Test class `MomoIpnControllerIT.java` — 5 @Test: valid IPN succeeded + duplicate skip + invalid signature + amount mismatch + resultCode non-zero (PAY-03)
- [ ] Test class `OrderCrudServiceMomoIT.java` — 3 @Test: MOMO PENDING + paymentUrl + KHÔNG publish; COD publish ngay; PaymentSessionClient fail → 502 (PAY-01)
- [ ] Update `PaymentEventListenerIT.java` field rename `vnpTransactionNo` → `paymentTransactionNo` (giữ ≥ 3 @Test) (PAY-04)
- [ ] Playwright spec `12-momo-payment.spec.ts` — ≥ 3 smoke tests (selector + result page resultCode=0 / resultCode=1006 / valid=false empty) (PAY-01, PAY-02)
- [ ] `application-test.yml` payment-service: block `momo:` thay `vnpay:` với sandbox creds

*Existing infrastructure (JUnit 5, Spring Boot Test, Playwright e2e/) đã có từ Phase 26 — không cần cài framework mới.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Redirect thật sang cổng MoMo sandbox + thanh toán trên sandbox | PAY-01 | Cần tương tác với cổng MoMo sandbox bên ngoài (test-payment.momo.vn) | Chọn MoMo tại `/checkout`, đặt hàng, hoàn tất thanh toán trên trang sandbox MoMo (quét QR / nhập OTP test) |
| IPN callback server-to-server từ MoMo tới localhost | PAY-03 | MoMo gửi POST JSON từ internet; demo local cần tunnel (ngrok/cloudflared) hoặc script curl mô phỏng | Dùng ngrok/cloudflared expose api-gateway:8080 + đăng ký MOMO_IPN_URL; hoặc dùng `curl -X POST http://localhost:8080/api/payments/momo/ipn -H 'Content-Type: application/json' -d '<payload với chữ ký hợp lệ>'` |
| Đơn legacy `paymentMethod='VNPAY'` render fallback (nếu có) | PAY-04 | Orphan data từ Phase 26 test (D-09 không migrate) | Truy cập `/profile/orders/[id]` của đơn legacy → confirm UI render gracefully (không crash, có thể hiển thị "Thanh toán qua VNPay (đã ngừng)" hoặc fallback default) — *task này không bắt buộc* |

*Các kiểm thử logic ký/verify, idempotency, cập nhật trạng thái, rename field, SC5 audit đều có verify tự động.*

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify (mỗi task có command riêng)
- [x] Wave 0 covers all MISSING references (6 items liệt kê)
- [x] No watch-mode flags
- [x] Feedback latency < 120s
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** approved 2026-05-23 (planner sign-off; sẽ flip `wave_0_complete: true` sau khi executor commit Plan 01 Task 1 + Plan 02 Task 2 + Plan 03 Task 2)
