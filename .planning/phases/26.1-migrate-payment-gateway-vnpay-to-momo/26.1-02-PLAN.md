---
phase: 26.1-migrate-payment-gateway-vnpay-to-momo
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - sources/backend/order-service/src/main/resources/db/migration/V7__rename_vnp_transaction_no.sql
  - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/domain/OrderEntity.java
  - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/domain/OrderDto.java
  - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/domain/OrderMapper.java
  - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/messaging/event/PaymentEventEnvelope.java
  - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/messaging/consumer/PaymentEventListener.java
  - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/service/OrderCrudService.java
  - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/service/PaymentSessionClient.java
  - sources/backend/order-service/src/test/java/com/ptit/htpt/orderservice/OrderCrudServiceMomoIT.java
  - sources/backend/order-service/src/test/java/com/ptit/htpt/orderservice/OrderCrudServiceVNPayIT.java
autonomous: true
requirements: [PAY-01, PAY-04]

must_haves:
  truths:
    - "Cột DB `vnp_transaction_no` được rename thành `payment_transaction_no` qua Flyway V7"
    - "OrderEntity field `vnpTransactionNo` rename thành `paymentTransactionNo` (cả @Column annotation + getter/setter)"
    - "OrderDto + OrderMapper expose field `paymentTransactionNo` thay `vnpTransactionNo`"
    - "OrderCrudService.createOrderFromCommand nhận paymentMethod=MOMO → gọi PaymentSessionClient với provider=MOMO + trì hoãn OrderPlaced (D-09 inherited)"
    - "PaymentEventListener consume PaymentSucceeded → set paymentTransactionNo (rename field) + publish OrderPlaced"
    - "Test class OrderCrudServiceVNPayIT bị xóa, thay bằng OrderCrudServiceMomoIT"
  artifacts:
    - path: "sources/backend/order-service/src/main/resources/db/migration/V7__rename_vnp_transaction_no.sql"
      provides: "ALTER TABLE rename column"
      contains: "RENAME COLUMN vnp_transaction_no TO payment_transaction_no"
    - path: "sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/messaging/event/PaymentEventEnvelope.java"
      provides: "Envelope shape khớp payment-service (field paymentTransactionNo)"
      contains: "paymentTransactionNo"
  key_links:
    - from: "OrderCrudService switch(paymentMethod)"
      to: "PaymentSessionClient.createSession(provider=MOMO)"
      via: "nhánh MOMO gọi REST với provider MOMO thay VNPAY"
      pattern: "MOMO"
    - from: "PaymentEventListener.onPaymentEvent"
      to: "OrderEntity.setPaymentTransactionNo"
      via: "PaymentSucceeded → lưu transId từ MoMo"
      pattern: "setPaymentTransactionNo"
---

<objective>
Migration data layer order-service từ VNPay sang MoMo: Flyway V7 rename column DB `vnp_transaction_no` → `payment_transaction_no`; rename field trong OrderEntity/OrderDto/OrderMapper; cập nhật enum case `VNPAY` → `MOMO` trong OrderCrudService.createOrderFromCommand switch; rename field trong PaymentEventEnvelope của order-service để khớp shape mới của payment-service; cập nhật PaymentEventListener; xóa OrderCrudServiceVNPayIT, tạo OrderCrudServiceMomoIT thay thế.

Purpose: order-service cột `payment_transaction_no` dùng chung cho mọi gateway (D-07). Enum value đổi `VNPAY` → `MOMO` (D-08). KHÔNG migrate dữ liệu legacy (D-09 — chấp nhận orphan). PaymentSessionClient + PaymentEventListener + RabbitMQConfig topology giữ NGUYÊN (D-05 — chỉ event payload field rename).
Output: V7 migration + entity/dto/mapper rename + OrderCrudService switch MOMO + PaymentEventEnvelope rename + PaymentEventListener cập nhật + OrderCrudServiceMomoIT thay OrderCrudServiceVNPayIT.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26.1-migrate-payment-gateway-vnpay-to-momo/26.1-CONTEXT.md
@.planning/phases/26-vnpay-payment-integration/26-02-SUMMARY.md
@.planning/phases/26-vnpay-payment-integration/26-03-SUMMARY.md
@.planning/phases/26-vnpay-payment-integration/26-02-PLAN.md
@.planning/phases/26-vnpay-payment-integration/26-03-PLAN.md

<interfaces>
<!-- Phase 26 đã có sẵn (giữ NGUYÊN — không tạo lại):
- V6__add_payment_status.sql (cột payment_status + vnp_transaction_no + bảng processed_events) — đã chạy production
- OrderEntity.vnpTransactionNo (@Column "vnp_transaction_no") — rename
- OrderDto.vnpTransactionNo — rename
- OrderMapper map vnpTransactionNo — rename
- PaymentSessionClient.createVNPaySession(orderId, amountVnd, orderInfo, authHeader) — đổi tên method + provider value
- PaymentEventListener (consumer queue order.payment-events) — giữ logic, đổi field setter
- RabbitMQConfig (exchange payment.events + queue order.payment-events) — KHÔNG đụng
- OrderEventPublisher.publishOrderPlaced — KHÔNG đụng
- ProcessedEventRepository.insertIfAbsent — KHÔNG đụng (idempotency infra giữ nguyên)
-->

<!-- PaymentEventEnvelope SHAPE khớp payment-service (Plan 01 đã rename) -->
order-service messaging/event/PaymentEventEnvelope.java hiện có (Phase 26):
```java
record PaymentEventEnvelope(String eventId, String eventType, String occurredAt, String traceId, PaymentPayload payload)
record PaymentPayload(String orderId, String paymentSessionId, String vnpTransactionNo, BigDecimal amount, String currency)
```
→ Rename `vnpTransactionNo` → `paymentTransactionNo` (giữ shape khớp Plan 01 — Jackson deserialize field name matching).

<!-- OrderCrudService.createOrderFromCommand (Phase 26 Plan 03) hiện có nhánh: -->
```java
switch (command.paymentMethod()) {
  case "VNPAY" -> {
    String paymentUrl = paymentSessionClient.createVNPaySession(...);  // provider="VNPAY"
    saved.setPaymentStatus("PENDING");
    dto.setPaymentUrl(paymentUrl);
    // KHÔNG publishOrderPlaced (D-09 trì hoãn)
  }
  case "COD" -> {
    orderEventPublisher.publishOrderPlaced(payload);
  }
  // ...
}
```
→ Đổi `VNPAY` → `MOMO` (D-08); đổi method name `createVNPaySession` → `createMomoSession` (hoặc generalize thành `createPaymentSession(provider, ...)`). Plan giữ tên `createMomoSession` để parallel với Phase 26 naming.

<!-- PaymentSessionClient.createVNPaySession (Phase 26 Plan 03): -->
```java
public String createVNPaySession(String orderId, long amountVnd, String orderInfo, String authHeader) {
  Map<String, Object> body = Map.of("provider", "VNPAY", "orderId", orderId, "amount", amountVnd, "orderInfo", orderInfo);
  // POST api-gateway:8080/api/payments/sessions
  // unwrap ApiResponse → return paymentUrl
}
```
→ Đổi tên `createVNPaySession` → `createMomoSession`; đổi `"VNPAY"` → `"MOMO"` trong body. Logic REST giữ nguyên 100%.

<!-- PaymentEventListener (Phase 26 Plan 03): -->
```java
case "PaymentSucceeded" -> {
  order.setPaymentStatus("PAID");
  order.setVnpTransactionNo(payload.vnpTransactionNo());  // ← rename
  // ...
}
```
→ Đổi `setVnpTransactionNo` → `setPaymentTransactionNo`; đọc `payload.paymentTransactionNo()` (sau rename envelope).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flyway V7 rename column + OrderEntity/OrderDto/OrderMapper rename field</name>
  <read_first>
    - sources/backend/order-service/src/main/resources/db/migration/V6__add_payment_status.sql (analog — đọc tên cột `vnp_transaction_no` đã thêm)
    - sources/backend/order-service/src/main/resources/db/migration/V5__add_coupons.sql (analog ALTER TABLE pattern)
    - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/domain/OrderEntity.java (file sửa — field vnpTransactionNo @Column)
    - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/domain/OrderDto.java (file sửa — field vnpTransactionNo)
    - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/domain/OrderMapper.java (file sửa — map vnpTransactionNo)
    - .planning/phases/26.1-migrate-payment-gateway-vnpay-to-momo/26.1-CONTEXT.md §D-07 Claude's Discretion (rename trực tiếp, KHÔNG add-new+copy+drop)
  </read_first>
  <action>
    **Bước 1 — Tạo `sources/backend/order-service/src/main/resources/db/migration/V7__rename_vnp_transaction_no.sql`** (D-07 Claude's discretion: planner chốt rename trực tiếp — đơn giản, không có data quan trọng cần preserve, chấp nhận orphan VNPAY records nếu có):
    ```sql
    -- order-svc V7 / PAY-04 (D-07): rename column dùng chung cho mọi gateway (VNPay → MoMo migration)
    ALTER TABLE orders RENAME COLUMN vnp_transaction_no TO payment_transaction_no;
    ```
    LƯU Ý: PostgreSQL `ALTER TABLE ... RENAME COLUMN` là DDL atomic — KHÔNG cần xử lý dữ liệu (giá trị giữ nguyên). Đơn legacy có `paymentMethod='VNPAY'` chấp nhận orphan (D-09).

    **Bước 2 — Sửa `OrderEntity.java`** — rename field + @Column:
    ```java
    // CŨ:
    @Column(name = "vnp_transaction_no", length = 50)
    private String vnpTransactionNo;

    // MỚI:
    @Column(name = "payment_transaction_no", length = 50)
    private String paymentTransactionNo;
    ```
    Đổi tên getter record-style `vnpTransactionNo()` → `paymentTransactionNo()`. Đổi tên setter `setVnpTransactionNo(String)` → `setPaymentTransactionNo(String)` (giữ pattern bump `updatedAt` như cũ).

    **Bước 3 — Sửa `OrderDto.java`** — rename field `vnpTransactionNo` → `paymentTransactionNo` (cùng kiểu String nullable). Giữ nguyên 2 field khác `paymentStatus` + `paymentUrl`.

    **Bước 4 — Sửa `OrderMapper.java`** — đổi mapping `entity.vnpTransactionNo()` → `entity.paymentTransactionNo()`, `dto.vnpTransactionNo` → `dto.paymentTransactionNo`.
  </action>
  <verify>
    <!-- Validate gián tiếp qua Spring context: mvn test khởi động ddl-auto=validate, Flyway chạy V7 trên Testcontainers Postgres; JPA validate cột mới khớp OrderEntity. Pattern theo Phase 26 Plan 02 Task 1. -->
    <automated>mvn -q test -pl order-service -Dtest=OrderMapper*</automated>
  </verify>
  <acceptance_criteria>
    - File `V7__rename_vnp_transaction_no.sql` tồn tại (đúng tên — V7, không V8)
    - `grep "RENAME COLUMN vnp_transaction_no TO payment_transaction_no" V7__rename_vnp_transaction_no.sql` khớp
    - `grep "payment_transaction_no" OrderEntity.java` khớp; `grep "vnp_transaction_no" OrderEntity.java` KHÔNG match
    - `grep "paymentTransactionNo" OrderEntity.java` khớp; `grep "vnpTransactionNo" OrderEntity.java` KHÔNG match
    - `grep "paymentTransactionNo" OrderDto.java` khớp; `grep "vnpTransactionNo" OrderDto.java` KHÔNG match
    - `grep "paymentTransactionNo" OrderMapper.java` khớp; `grep "vnpTransactionNo" OrderMapper.java` KHÔNG match
  </acceptance_criteria>
  <done>V7 rename column commit; OrderEntity/OrderDto/OrderMapper field rename xong; Flyway validate qua Spring context.</done>
</task>

<task type="auto">
  <name>Task 2: PaymentEventEnvelope rename + OrderCrudService switch MOMO + PaymentSessionClient rename method + PaymentEventListener cập nhật + OrderCrudServiceMomoIT thay OrderCrudServiceVNPayIT</name>
  <read_first>
    - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/messaging/event/PaymentEventEnvelope.java (file sửa — rename field `vnpTransactionNo` → `paymentTransactionNo`)
    - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/messaging/consumer/PaymentEventListener.java (file sửa — setter rename + payload getter rename)
    - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/service/OrderCrudService.java (file sửa — switch case "VNPAY" → "MOMO")
    - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/service/PaymentSessionClient.java (file sửa — đổi tên method `createVNPaySession` → `createMomoSession`, body provider "VNPAY" → "MOMO")
    - sources/backend/order-service/src/test/java/com/ptit/htpt/orderservice/OrderCrudServiceVNPayIT.java (file XÓA — đọc trước để biết test pattern + 3 case cần re-create cho MoMo)
    - .planning/phases/26-vnpay-payment-integration/26-03-PLAN.md §Task 1 (analog PaymentSessionClient + OrderCrudService nhánh VNPAY)
  </read_first>
  <action>
    **Bước 1 — Sửa `messaging/event/PaymentEventEnvelope.java`** (KHỚP shape Plan 01):
    - Trong record `PaymentPayload`: đổi `String vnpTransactionNo` → `String paymentTransactionNo`. Giữ thứ tự arg (Jackson deserialize qua field name matching). Shape PHẢI IDENTICAL với payment-service envelope.

    **Bước 2 — Sửa `service/PaymentSessionClient.java`** — đổi tên method + provider value:
    ```java
    // CŨ:
    public String createVNPaySession(String orderId, long amountVnd, String orderInfo, String authHeader) {
      Map<String, Object> body = Map.of("provider", "VNPAY", "orderId", orderId, "amount", amountVnd, "orderInfo", orderInfo);
      // ...
    }

    // MỚI:
    public String createMomoSession(String orderId, long amountVnd, String orderInfo, String authHeader) {
      Map<String, Object> body = Map.of("provider", "MOMO", "orderId", orderId, "amount", amountVnd, "orderInfo", orderInfo);
      // ... giữ nguyên logic REST gọi api-gateway:8080/api/payments/sessions + unwrap ApiResponse + ResponseStatusException khi fail
    }
    ```
    URL hằng số giữ nguyên `http://api-gateway:8080/api/payments/sessions` (chung cho mọi provider). Logic forward Bearer JWT giữ nguyên.

    **Bước 3 — Sửa `OrderCrudService.createOrderFromCommand`** — switch case rename:
    ```java
    // CŨ:
    case "VNPAY" -> {
      saved.setPaymentStatus("PENDING");
      String paymentUrl = paymentSessionClient.createVNPaySession(saved.id().toString(), saved.total().longValueExact(), "Đơn hàng " + saved.id(), authHeader);
      dto.setPaymentUrl(paymentUrl);
    }

    // MỚI:
    case "MOMO" -> {
      saved.setPaymentStatus("PENDING");
      String paymentUrl = paymentSessionClient.createMomoSession(saved.id().toString(), saved.total().longValueExact(), "Đơn hàng " + saved.id(), authHeader);
      dto.setPaymentUrl(paymentUrl);
    }
    ```
    KHÔNG đụng nhánh COD/BANK_TRANSFER/E_WALLET. Giữ nguyên D-09 (KHÔNG publishOrderPlaced với MOMO — trì hoãn cho tới khi PaymentSucceeded về).

    **Bước 4 — Sửa `messaging/consumer/PaymentEventListener.java`** — cập nhật setter + payload getter:
    ```java
    // CŨ:
    if ("PaymentSucceeded".equals(eventType)) {
      order.setPaymentStatus("PAID");
      order.setVnpTransactionNo(envelope.payload().vnpTransactionNo());
      // ...
    }

    // MỚI:
    if ("PaymentSucceeded".equals(eventType)) {
      order.setPaymentStatus("PAID");
      order.setPaymentTransactionNo(envelope.payload().paymentTransactionNo());
      // ...
    }
    ```
    Giữ NGUYÊN: @RabbitListener queues "order.payment-events", @Transactional, idempotency-first `processedEventRepository.insertIfAbsent`, error classification, helper `publishOrderPlacedForOrder` (D-09 trừ kho khi PAID), 3 log path `[MQ-CONSUME]`. KHÔNG đụng RabbitMQConfig.

    **Bước 5 — XÓA file test cũ + tạo file test mới**:
    - Xóa `sources/backend/order-service/src/test/java/com/ptit/htpt/orderservice/OrderCrudServiceVNPayIT.java`.
    - Tạo `sources/backend/order-service/src/test/java/com/ptit/htpt/orderservice/OrderCrudServiceMomoIT.java` — copy structure từ file cũ (3 test case Phase 26 Plan 03):
      ```java
      @ExtendWith(MockitoExtension.class)
      class OrderCrudServiceMomoIT {
        // mocks: PaymentSessionClient, OrderEventPublisher, ProductBatchClient, OrderRepository, CouponRedemptionService, ...

        @Test void createOrderMomo_setsPendingAndPaymentUrl_doesNotPublishOrderPlaced() {
          // arrange paymentSessionClient.createMomoSession(...) → "https://test-payment.momo.vn/...?token=abc"
          // assert dto.paymentUrl != null; order.paymentStatus == "PENDING"; verify(orderEventPublisher, never()).publishOrderPlaced(any())
        }
        @Test void createOrderCod_publishesOrderPlacedImmediately() {
          // arrange paymentMethod=COD
          // assert verify(orderEventPublisher).publishOrderPlaced(any()); order.paymentStatus == "PENDING" (default)
        }
        @Test void paymentSessionClientFail_throwsResponseStatusException502() {
          // arrange paymentSessionClient.createMomoSession(...) throws ResponseStatusException(BAD_GATEWAY)
          // assert createOrderFromCommand throws ResponseStatusException with status 502
        }
      }
      ```
    - PaymentEventListenerIT (Phase 26 Plan 03 đã có) — KHÔNG cần re-create vì test event shape vẫn khớp (chỉ field rename). Nếu compile fail do field rename → cập nhật field accessor trong test (đổi `vnpTransactionNo` → `paymentTransactionNo` trong assertion + envelope mock). Đọc file `PaymentEventListenerIT.java` để xác định mức cập nhật cần.
  </action>
  <verify>
    <automated>mvn -q test -pl order-service -Dtest=OrderCrudServiceMomoIT,PaymentEventListenerIT,OrderMapper*</automated>
  </verify>
  <acceptance_criteria>
    - `grep "paymentTransactionNo" PaymentEventEnvelope.java` khớp; `grep "vnpTransactionNo" PaymentEventEnvelope.java` KHÔNG match
    - `grep "createMomoSession" PaymentSessionClient.java` khớp; `grep "createVNPaySession" PaymentSessionClient.java` KHÔNG match
    - `grep "\"MOMO\"" PaymentSessionClient.java` khớp; `grep "\"VNPAY\"" PaymentSessionClient.java` KHÔNG match
    - `grep "case \"MOMO\"" OrderCrudService.java` khớp (hoặc `"MOMO" ->`); `grep "case \"VNPAY\"\\|\"VNPAY\" ->" OrderCrudService.java` KHÔNG match
    - `grep "setPaymentTransactionNo" PaymentEventListener.java` khớp; `grep "setVnpTransactionNo" PaymentEventListener.java` KHÔNG match
    - File `OrderCrudServiceVNPayIT.java` KHÔNG tồn tại
    - File `OrderCrudServiceMomoIT.java` tồn tại với ≥ 3 `@Test`
    - `grep -r "VNPAY\\|vnpTransactionNo\\|createVNPaySession" sources/backend/order-service/src/main/java 2>/dev/null` zero matches
    - `mvn -q test -pl order-service` xanh hoặc compile-clean (ghi rõ SUMMARY)
  </acceptance_criteria>
  <done>Envelope rename khớp Plan 01; OrderCrudService switch MOMO; PaymentSessionClient.createMomoSession; PaymentEventListener setter rename; OrderCrudServiceMomoIT thay OrderCrudServiceVNPayIT; order-service zero VNPay references trong main code.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Flyway V7 migration → DB schema | RENAME COLUMN atomic, không mất dữ liệu |
| RabbitMQ payment.events → order-service consumer | Event nội bộ giữa service, tin cậy ở mức broker (giữ nguyên từ Phase 26) |
| order-service → payment-service REST | Forward Bearer JWT của user, qua gateway đã verify |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26.1-08 | Tampering | V7 migration ddl-auto | mitigate | order-service dùng ddl-auto=validate (precedent Phase 23); Flyway chạy V7 atomic → JPA validate cột mới khớp `payment_transaction_no` |
| T-26.1-09 | Tampering (replay) | PaymentEventListener idempotency | mitigate | insertIfAbsent processed_events ĐẦU TIÊN (giữ nguyên từ Phase 26) — eventId trùng → skip, MoMo retry không gây double-update |
| T-26.1-10 | Information Disclosure | OrderDto.paymentTransactionNo exposure | accept | paymentTransactionNo chỉ đọc — client KHÔNG set được (chỉ IPN consumer cập nhật); mã giao dịch không phải secret nhạy cảm |
| T-26.1-11 | Tampering (data integrity) | Field rename across services | mitigate | PaymentEventEnvelope shape khớp IDENTICAL giữa payment-service (Plan 01) + order-service (Plan 02) — Jackson deserialize bằng field name; KHÔNG có version skew vì 2 service deploy cùng commit |
</threat_model>

<verification>
- `mvn -q test -pl order-service` xanh (hoặc compile-clean Maven defer Windows)
- V7 rename column SQL đúng cú pháp Postgres
- OrderCrudServiceMomoIT ≥ 3 test; PaymentEventListenerIT cập nhật + ≥ 3 test giữ nguyên
- order-service main code zero VNPay references: `grep -r "VNPAY\\|vnpTransactionNo\\|createVNPaySession" sources/backend/order-service/src/main/java 2>/dev/null` zero matches
</verification>

<success_criteria>
- DB cột `payment_transaction_no` (rename từ `vnp_transaction_no` qua V7)
- OrderEntity/OrderDto/OrderMapper field `paymentTransactionNo`
- OrderCrudService.createOrderFromCommand switch case `MOMO` thay `VNPAY`
- PaymentEventListener consume PaymentSucceeded/Failed → lưu `paymentTransactionNo` field rename
- OrderCrudServiceMomoIT thay OrderCrudServiceVNPayIT
</success_criteria>

<output>
After completion, create `.planning/phases/26.1-migrate-payment-gateway-vnpay-to-momo/26.1-02-SUMMARY.md`
</output>
