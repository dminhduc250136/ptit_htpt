---
status: partial
phase: 26.1-migrate-payment-gateway-vnpay-to-momo
source: [26.1-VERIFICATION.md]
started: 2026-05-23T00:00:00Z
updated: 2026-05-23T10:55:00Z
---

## Current Test

[2/3 PASSED via live MoMo sandbox 2026-05-23. Còn 1 item IPN auto-PAID defer cho khi có ngrok tunnel — KHÔNG block implementation verify]

## Tests

### 1. Redirect thật sang MoMo Sandbox
expected: Tại `/checkout` chọn "Thanh toán qua MoMo" → bấm "Đặt hàng" → trình duyệt redirect sang `https://test-payment.momo.vn/...` với đúng số tiền + mã đơn + signature hợp lệ.
result: [passed] 2026-05-23 — Đã verify trực tiếp trên browser. MoMo sandbox accept signature HMAC SHA256, hiển thị trang QR + thẻ ATM với amount đúng. Đi qua NAPAS gateway thành công.

### 2. IPN Callback Server-to-Server
expected: MoMo POST IPN tới `/api/payments/momo/ipn` (qua tunnel công khai), BE verify HMAC SHA256, cập nhật `payment_status=PAID`, phát `PaymentSucceeded` event, order-service consume cập nhật đơn.
result: [deferred] cần ngrok tunnel để test IPN thật từ internet tới localhost. Code path đã verify static (10/10 MomoIpnControllerIT pass) — chỉ thiếu live IPN integration test. Không block phase completion vì là constraint hạ tầng (localhost không reachable từ MoMo server), không phải code defect.

### 3. Trang kết quả FE state machine (resolve + poll + timeout)
expected: MoMo redirect về `/checkout/result` → FE gọi `getMomoReturn` resolve orderId → bắt đầu poll `payment_status` 3s × 5 lần → render đúng 5 trạng thái UI-SPEC.
result: [passed] 2026-05-23 — Verify end-to-end browser:
- `getMomoReturn` verify HMAC SHA256 + resolve orderId nội bộ qua PaymentSession → `valid:true`
- FE setState('polling') → render heading "Đang xác nhận thanh toán" + spinner
- Poll 5 lần × 3s GET order (đơn 5badb352-...) → `paymentStatus=PENDING` mỗi lần (vì IPN không tới được — expected per item #2)
- Sau 15s timeout → setState('timeout') → render heading "Chưa nhận được xác nhận"
- 5 trạng thái UI-SPEC verbatim đúng (cancelled / failed / success / polling / timeout)

## Bugs Discovered & Fixed Trong UAT
1. `payment_status` cột không tồn tại → Flyway V6 chưa apply (out-of-order V100/V103 seed-dev block) → fix: enable `flyway.out-of-order=true` (commit `fix(26.1): enable Flyway out-of-order`)
2. Duplicate `spring:` key trong `payment-service/application.yml` (executor merge sót) → fix: merge sections (commit `fix(26.1): merge duplicate spring keys`)
3. `MOMO_IPN_URL` blank → MoMo reject create session "Field must not be blank" → fix: default placeholder URL trong docker-compose + application.yml (commit `fix(26.1): MOMO_IPN_URL default placeholder`)
4. Gateway chưa whitelist `/api/payments/momo/**` → 401 Unauthorized cho IPN/return → fix: rebuild api-gateway image (commit pre-existed nhưng image stale)
5. `MomoService.buildReturnView` không inject `accessKey` từ config khi verify return URL → `valid:false` (MoMo redirect không kèm accessKey trong query) → fix: inject từ config (commit `fix(26.1): inject accessKey`)
6. `/momo/return` bị SKIP ApiResponseAdvice → response raw `{valid, orderId, ...}` không envelope → FE httpGet auto-unwrap `.data` lấy undefined → `result.valid` throw → catch → empty state → fix: chỉ SKIP cho `/momo/ipn`, `/momo/return` cần envelope (commit `fix(26.1): chỉ skip /momo/ipn`)

## Summary

total: 3
passed: 2
deferred: 1 (IPN — cần ngrok tunnel để test live integration)
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps
