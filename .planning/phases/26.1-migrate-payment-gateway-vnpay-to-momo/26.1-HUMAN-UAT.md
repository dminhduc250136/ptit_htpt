---
status: partial
phase: 26.1-migrate-payment-gateway-vnpay-to-momo
source: [26.1-VERIFICATION.md]
started: 2026-05-23T00:00:00Z
updated: 2026-05-23T00:00:00Z
---

## Current Test

[3/3 cần live MoMo sandbox; có thể chạy ngay vì MoMo provide sandbox credentials công khai trong code (Plan 01 application.yml defaults)]

## Tests

### 1. Redirect thật sang MoMo Sandbox
expected: Tại `/checkout` chọn "Thanh toán qua MoMo" → bấm "Đặt hàng" → trình duyệt redirect sang `https://test-payment.momo.vn/...` với QR code/banking options. Sandbox public credentials (`MOMO_PARTNER_CODE=MOMO`, `MOMO_ACCESS_KEY=F8BBA842ECF85`, `MOMO_SECRET_KEY=K951B6PE1waDMi640xX08PD3vg6EkVlz`) đã set trong docker-compose default — không cần đăng ký.
result: [pending] cần test trên browser sau khi rebuild + restart payment-service + frontend

### 2. IPN Callback Server-to-Server
expected: MoMo POST IPN tới `/api/payments/momo/ipn` (qua tunnel công khai), BE verify HMAC SHA256, cập nhật `payment_status=PAID`, phát `PaymentSucceeded` event, order-service consume cập nhật đơn. Cần tunnel (ngrok/cloudflared) + set `MOMO_IPN_URL` env tới tunnel URL.
result: [pending] cần ngrok setup

### 3. Đơn Legacy VNPAY Render Gracefully
expected: Đơn cũ có `paymentMethod='VNPAY'` (đã tạo trong test Phase 26) hiển thị tại `/profile/orders/[id]` KHÔNG crash, fallback "Thanh toán qua VNPay" (paymentMethodMap nếu không có key sẽ trả undefined → cần verify FE không break).
result: [pending]

## Summary

total: 3
passed: 0
issues: 0
pending: 3
skipped: 0
blocked: 0

## Gaps
