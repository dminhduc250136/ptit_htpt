---
phase: 23
plan: 03
type: execute
wave: 1
depends_on: [23-01, 23-02]
files_modified:
  - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/messaging/config/RabbitMQConfig.java
  - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/messaging/event/OrderEventEnvelope.java
  - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/messaging/exception/TransientMessageException.java
  - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/messaging/exception/PermanentMessageException.java
  - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/messaging/tracing/TraceIdMessagePostProcessor.java
  - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/messaging/publisher/OrderEventPublisher.java
  - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/service/OrderCrudService.java
  - sources/backend/inventory-service/src/main/java/com/ptit/htpt/inventoryservice/messaging/config/RabbitMQConfig.java
  - sources/backend/notification-service/src/main/java/com/ptit/htpt/notificationservice/messaging/config/RabbitMQConfig.java
autonomous: true
requirements: [MQ-02]
tags: [order-service, producer, rabbitmq-topology, afterCommit, publisher-confirms]

must_haves:
  truths:
    - "Topic exchange order.events + DLX order.dlx + DLQ order-events.dlq được declare ở cả 3 service (Spring AmqpAdmin idempotent)"
    - "Queue inventory.order-events + notification.order-events declare với x-dead-letter-exchange=order.dlx"
    - "order-service publish OrderPlaced sau khi DB commit (afterCommit callback)"
    - "Publish dùng CorrelationData per-message + Publisher Confirms timeout 5s"
    - "Header X-Trace-Id được set từ MDC trong producer"
    - "Code legacy deductStock + buildPatchBody trong OrderCrudService bị XÓA hoàn toàn"
    - "Stock validate REST trong validateStock GIỮ NGUYÊN (D-11)"
  artifacts:
    - path: "sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/messaging/publisher/OrderEventPublisher.java"
      provides: "@Component publishOrderPlaced(payload) — registerSynchronization → afterCommit → doPublish với CorrelationData + log [MQ-PUB]"
      min_lines: 60
    - path: "sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/messaging/config/RabbitMQConfig.java"
      provides: "Topology beans (TopicExchange order.events, DirectExchange order.dlx, Queue order-events.dlq, Binding) + RabbitTemplate(setMandatory=true) + Jackson2JsonMessageConverter"
      min_lines: 50
    - path: "sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/messaging/event/OrderEventEnvelope.java"
      provides: "Record OrderEventEnvelope(eventId, eventType, occurredAt, traceId, payload) + nested Payload + Item"
      min_lines: 25
  key_links:
    - from: "OrderCrudService.createOrderFromCommand (sau orderRepository.save)"
      to: "OrderEventPublisher.publishOrderPlaced"
      via: "Constructor inject + replace deductStockAfterPersist call"
      pattern: "publisher.publishOrderPlaced"
    - from: "OrderEventPublisher.publishOrderPlaced"
      to: "RabbitTemplate.convertAndSend EXCHANGE=order.events ROUTING_KEY=order.placed"
      via: "TransactionSynchronizationManager.registerSynchronization afterCommit"
      pattern: "TransactionSynchronizationManager"
---

<objective>
Triển khai producer-side: topology config 3 service, OrderEventPublisher publish-after-commit với Publisher Confirms, xóa deductStock REST legacy. Đáp ứng MQ-02.

Purpose: Per D-01 (1 event OrderPlaced), D-02 (topic exchange + DLX + 2 queue), D-03 (publish afterCommit), D-04 (Publisher Confirms), D-12 (xóa deductStock REST), D-15 (JSON envelope), D-16/D-17 (trace + log).

Output: Topology declare ở 3 service; OrderEventPublisher implement chính; OrderCrudService.createOrderFromCommand chèn publisher + xóa deductStock; XÓA buildPatchBody + deductStockAfterPersist methods.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-message-queue-rabbitmq/23-CONTEXT.md
@.planning/phases/23-message-queue-rabbitmq/23-RESEARCH.md
@.planning/phases/23-message-queue-rabbitmq/23-PATTERNS.md
@.planning/phases/23-message-queue-rabbitmq/23-01-SUMMARY.md
@.planning/phases/23-message-queue-rabbitmq/23-02-SUMMARY.md
@sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/service/OrderCrudService.java
@sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/web/TraceIdFilter.java
@sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/exception/StockShortageException.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tạo topology config (3 service) + envelope record + 2 custom exception + TraceId producer postprocessor</name>
  <files>
    sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/messaging/config/RabbitMQConfig.java,
    sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/messaging/event/OrderEventEnvelope.java,
    sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/messaging/exception/TransientMessageException.java,
    sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/messaging/exception/PermanentMessageException.java,
    sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/messaging/tracing/TraceIdMessagePostProcessor.java,
    sources/backend/inventory-service/src/main/java/com/ptit/htpt/inventoryservice/messaging/config/RabbitMQConfig.java,
    sources/backend/notification-service/src/main/java/com/ptit/htpt/notificationservice/messaging/config/RabbitMQConfig.java
  </files>
  <read_first>
    - 23-RESEARCH.md §"Pattern 2: Topology Config" §lines 299-335 (canonical Java skeleton)
    - 23-RESEARCH.md §"Recommended Project Structure" §lines 213-233 (package layout)
    - 23-PATTERNS.md §"RabbitMQConfig.java" §lines 275-285 (decision: 3 service cùng declare — AmqpAdmin idempotent với matching args)
    - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/web/TraceIdFilter.java (full — ATTR_NAME constant + MDC pattern)
    - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/exception/StockShortageException.java (custom exception pattern reference)
    - 23-CONTEXT.md §D-02, §D-08, §D-09, §D-15
  </read_first>
  <action>
    Tạo các file Java mới. Package convention: `com.ptit.htpt.{service}.messaging.{sub}`. KHÔNG dùng Lombok — explicit constructor.

    (1) **`OrderEventEnvelope.java`** (chỉ ở order-service; inventory/notification sẽ deserialize qua Jackson nên cũng cần copy — nhưng để giảm trùng lặp + giảm scope plan, plan 04/05 sẽ tự copy record này khi cần). File ở order-service `messaging/event/OrderEventEnvelope.java`:

    ```java
    package com.ptit.htpt.orderservice.messaging.event;

    import java.math.BigDecimal;
    import java.time.Instant;
    import java.util.List;
    import java.util.UUID;

    /**
     * JSON envelope cho event OrderPlaced (D-15).
     * Producer build qua factory create(); consumer deserialize qua Jackson2JsonMessageConverter.
     */
    public record OrderEventEnvelope(
        String eventId,
        String eventType,
        String occurredAt,
        String traceId,
        OrderPlacedPayload payload
    ) {
      public static OrderEventEnvelope createOrderPlaced(String traceId, OrderPlacedPayload payload) {
        return new OrderEventEnvelope(
            UUID.randomUUID().toString(),
            "OrderPlaced",
            Instant.now().toString(),
            traceId,
            payload
        );
      }

      public record OrderPlacedPayload(
          String orderId,
          String userId,
          List<Item> items,
          BigDecimal totalAmount,
          String currency
      ) {}

      public record Item(
          String productId,
          int quantity,
          BigDecimal priceAtPurchase
      ) {}
    }
    ```

    (2) **`TransientMessageException.java`** (3 service — copy cùng content, đổi package):

    ```java
    package com.ptit.htpt.orderservice.messaging.exception;

    /** D-08: lỗi tạm thời (DB timeout, network blip) — Spring listener retry theo D-07. */
    public class TransientMessageException extends RuntimeException {
      public TransientMessageException(String message) { super(message); }
      public TransientMessageException(String message, Throwable cause) { super(message, cause); }
    }
    ```

    (3) **`PermanentMessageException.java`** (3 service):

    ```java
    package com.ptit.htpt.orderservice.messaging.exception;

    /** D-08: lỗi vĩnh viễn (payload sai, business invariant vi phạm) — reject ngay vào DLQ, KHÔNG retry. */
    public class PermanentMessageException extends RuntimeException {
      public PermanentMessageException(String message) { super(message); }
      public PermanentMessageException(String message, Throwable cause) { super(message, cause); }
    }
    ```

    (4) **`TraceIdMessagePostProcessor.java`** (3 service):

    ```java
    package com.ptit.htpt.orderservice.messaging.tracing;

    import org.slf4j.MDC;
    import org.springframework.amqp.AmqpException;
    import org.springframework.amqp.core.Message;
    import org.springframework.amqp.core.MessagePostProcessor;

    /** D-16: producer set header X-Trace-Id từ MDC. */
    public class TraceIdMessagePostProcessor implements MessagePostProcessor {
      public static final String HEADER = "X-Trace-Id";
      private final String traceIdSnapshot;

      public TraceIdMessagePostProcessor(String traceIdSnapshot) {
        this.traceIdSnapshot = traceIdSnapshot;
      }

      public static TraceIdMessagePostProcessor capture() {
        String id = MDC.get("traceId");
        return new TraceIdMessagePostProcessor(id != null ? id : "no-trace");
      }

      @Override
      public Message postProcessMessage(Message message) throws AmqpException {
        message.getMessageProperties().setHeader(HEADER, traceIdSnapshot);
        return message;
      }
    }
    ```

    (5) **`RabbitMQConfig.java`** (3 service — same content + đổi package + đổi class name nếu cần). Beans:

    ```java
    package com.ptit.htpt.orderservice.messaging.config;

    import com.fasterxml.jackson.databind.ObjectMapper;
    import org.springframework.amqp.core.*;
    import org.springframework.amqp.rabbit.connection.ConnectionFactory;
    import org.springframework.amqp.rabbit.core.RabbitTemplate;
    import org.springframework.amqp.support.converter.Jackson2JsonMessageConverter;
    import org.springframework.context.annotation.Bean;
    import org.springframework.context.annotation.Configuration;

    /**
     * Topology RabbitMQ Phase 23 (D-02, D-09).
     * Tất cả 3 service declare cùng beans — Spring AmqpAdmin idempotent với matching arguments.
     */
    @Configuration
    public class RabbitMQConfig {

      public static final String EXCHANGE = "order.events";
      public static final String DLX = "order.dlx";
      public static final String DLQ = "order-events.dlq";
      public static final String DLQ_ROUTING = "order-events";
      public static final String INVENTORY_QUEUE = "inventory.order-events";
      public static final String NOTIFICATION_QUEUE = "notification.order-events";
      public static final String BINDING_KEY = "order.#";
      public static final String ROUTING_KEY_ORDER_PLACED = "order.placed";

      @Bean
      public TopicExchange orderEventsExchange() {
        return ExchangeBuilder.topicExchange(EXCHANGE).durable(true).build();
      }

      @Bean
      public DirectExchange deadLetterExchange() {
        return ExchangeBuilder.directExchange(DLX).durable(true).build();
      }

      @Bean
      public Queue deadLetterQueue() {
        return QueueBuilder.durable(DLQ).build();
      }

      @Bean
      public Binding dlqBinding() {
        return BindingBuilder.bind(deadLetterQueue()).to(deadLetterExchange()).with(DLQ_ROUTING);
      }

      @Bean
      public Queue inventoryQueue() {
        return QueueBuilder.durable(INVENTORY_QUEUE)
            .withArgument("x-dead-letter-exchange", DLX)
            .withArgument("x-dead-letter-routing-key", DLQ_ROUTING)
            .build();
      }

      @Bean
      public Binding inventoryBinding() {
        return BindingBuilder.bind(inventoryQueue()).to(orderEventsExchange()).with(BINDING_KEY);
      }

      @Bean
      public Queue notificationQueue() {
        return QueueBuilder.durable(NOTIFICATION_QUEUE)
            .withArgument("x-dead-letter-exchange", DLX)
            .withArgument("x-dead-letter-routing-key", DLQ_ROUTING)
            .build();
      }

      @Bean
      public Binding notificationBinding() {
        return BindingBuilder.bind(notificationQueue()).to(orderEventsExchange()).with(BINDING_KEY);
      }

      @Bean
      public Jackson2JsonMessageConverter jacksonMessageConverter(ObjectMapper objectMapper) {
        return new Jackson2JsonMessageConverter(objectMapper);
      }

      @Bean
      public RabbitTemplate rabbitTemplate(ConnectionFactory connectionFactory,
                                           Jackson2JsonMessageConverter converter) {
        RabbitTemplate template = new RabbitTemplate(connectionFactory);
        template.setMessageConverter(converter);
        template.setMandatory(true); // bật publisher-returns (D-04)
        return template;
      }
    }
    ```

    Cho inventory-service: package `com.ptit.htpt.inventoryservice.messaging.config`. Cho notification-service: package `com.ptit.htpt.notificationservice.messaging.config`. Class body giống nhau (same beans).

    KHÔNG dùng `@RequiredArgsConstructor` (no Lombok). Class `@Configuration` không cần inject — params nhận qua method parameter Spring tự autowire.
  </action>
  <verify>
    <automated>powershell -Command "Test-Path sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/messaging/config/RabbitMQConfig.java; Test-Path sources/backend/inventory-service/src/main/java/com/ptit/htpt/inventoryservice/messaging/config/RabbitMQConfig.java; Test-Path sources/backend/notification-service/src/main/java/com/ptit/htpt/notificationservice/messaging/config/RabbitMQConfig.java; Test-Path sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/messaging/event/OrderEventEnvelope.java"</automated>
  </verify>
  <acceptance_criteria>
    - 3 file `RabbitMQConfig.java` tồn tại ở 3 service (path đúng convention package)
    - `grep -c 'EXCHANGE = \"order.events\"' sources/backend/*/src/main/java/**/RabbitMQConfig.java` >= 3 (PowerShell: dùng Select-String -Path)
    - `grep 'x-dead-letter-exchange' sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/messaging/config/RabbitMQConfig.java` returns 2+ matches (inventory + notification queue)
    - `grep 'setMandatory(true)' sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/messaging/config/RabbitMQConfig.java` returns 1
    - File `OrderEventEnvelope.java` chứa record `OrderPlacedPayload` + record `Item`
    - 2 exception class extend `RuntimeException` (verify qua grep "extends RuntimeException")
    - TraceIdMessagePostProcessor implements MessagePostProcessor + HEADER = "X-Trace-Id"
    - Không có `@RequiredArgsConstructor` annotation (no Lombok) — `grep RequiredArgsConstructor` = 0 matches
  </acceptance_criteria>
  <done>Topology declare ở 3 service; envelope + exceptions + trace postprocessor sẵn cho publisher Task 2 và consumers Wave 2.</done>
</task>

<task type="auto">
  <name>Task 2: Triển khai OrderEventPublisher + chèn vào OrderCrudService + XÓA deductStock legacy</name>
  <files>
    sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/messaging/publisher/OrderEventPublisher.java,
    sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/service/OrderCrudService.java
  </files>
  <read_first>
    - 23-RESEARCH.md §"Pattern 1: Publisher với afterCommit" §lines 240-291 (canonical skeleton)
    - 23-RESEARCH.md §"Pitfall 1: MDC trace lost trong afterCommit callback" §lines 469-474 (capture traceId NGAY)
    - 23-RESEARCH.md §"Pitfall 2: Publisher Confirms timeout" §lines 476-480
    - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/service/OrderCrudService.java §lines 49-59 (constructor injection pattern — KHÔNG Lombok), §lines 115-184 (createOrderFromCommand — chỗ chèn publisher.publishOrderPlaced sau save()), §lines 338-440 (deductStock + buildPatchBody — XÓA hoàn toàn)
    - 23-CONTEXT.md §D-03, §D-04, §D-05, §D-12, §D-15, §D-17
    - 23-PATTERNS.md §"OrderEventPublisher.java" §lines 248-272 + §"Shared Patterns" §lines 528-548
    - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/messaging/event/OrderEventEnvelope.java (sau Task 1)
  </read_first>
  <action>
    (1) Tạo file `sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/messaging/publisher/OrderEventPublisher.java`:

    ```java
    package com.ptit.htpt.orderservice.messaging.publisher;

    import com.ptit.htpt.orderservice.messaging.config.RabbitMQConfig;
    import com.ptit.htpt.orderservice.messaging.event.OrderEventEnvelope;
    import com.ptit.htpt.orderservice.messaging.tracing.TraceIdMessagePostProcessor;
    import org.slf4j.Logger;
    import org.slf4j.LoggerFactory;
    import org.slf4j.MDC;
    import org.springframework.amqp.rabbit.connection.CorrelationData;
    import org.springframework.amqp.rabbit.core.RabbitTemplate;
    import org.springframework.stereotype.Component;
    import org.springframework.transaction.support.TransactionSynchronization;
    import org.springframework.transaction.support.TransactionSynchronizationManager;

    import java.util.concurrent.TimeUnit;
    import java.util.concurrent.TimeoutException;

    /**
     * Publish OrderPlaced event sau khi DB commit (D-03).
     * Publisher Confirms timeout 5s (D-04); thất bại → log error + alert nhưng KHÔNG rollback (D-05).
     * Trace propagate qua header X-Trace-Id (D-16) — capture MDC NGAY ở caller thread.
     */
    @Component
    public class OrderEventPublisher {
      private static final Logger log = LoggerFactory.getLogger(OrderEventPublisher.class);
      private static final long CONFIRM_TIMEOUT_MS = 5000L;

      private final RabbitTemplate rabbitTemplate;

      public OrderEventPublisher(RabbitTemplate rabbitTemplate) {
        this.rabbitTemplate = rabbitTemplate;
      }

      /** Gọi từ OrderCrudService.createOrderFromCommand sau khi save(). Defer publish đến afterCommit. */
      public void publishOrderPlaced(OrderEventEnvelope.OrderPlacedPayload payload) {
        // Capture MDC traceId NGAY (Pitfall 1: afterCommit chạy thread khác/sau MDC.remove)
        String traceId = MDC.get("traceId");
        String safeTraceId = traceId != null ? traceId : "no-trace";
        OrderEventEnvelope envelope = OrderEventEnvelope.createOrderPlaced(safeTraceId, payload);

        if (TransactionSynchronizationManager.isSynchronizationActive()) {
          TransactionSynchronizationManager.registerSynchronization(new TransactionSynchronization() {
            @Override
            public void afterCommit() {
              doPublish(envelope, safeTraceId);
            }
          });
        } else {
          // Fallback: caller không trong transaction (vd: test). Publish ngay.
          doPublish(envelope, safeTraceId);
        }
      }

      private void doPublish(OrderEventEnvelope envelope, String traceId) {
        String eventId = envelope.eventId();
        CorrelationData correlation = new CorrelationData(eventId);
        TraceIdMessagePostProcessor traceProcessor = new TraceIdMessagePostProcessor(traceId);

        try {
          rabbitTemplate.convertAndSend(
              RabbitMQConfig.EXCHANGE,
              RabbitMQConfig.ROUTING_KEY_ORDER_PLACED,
              envelope,
              msg -> {
                msg.getMessageProperties().setMessageId(eventId);
                return traceProcessor.postProcessMessage(msg);
              },
              correlation
          );

          CorrelationData.Confirm confirm = correlation.getFuture().get(CONFIRM_TIMEOUT_MS, TimeUnit.MILLISECONDS);
          if (confirm == null || !confirm.isAck()) {
            String reason = confirm == null ? "null" : confirm.getReason();
            log.error("[MQ-PUB-NACK] eventId={} traceId={} reason={}", eventId, traceId, reason);
          } else {
            log.info("[MQ-PUB] event=OrderPlaced routingKey={} eventId={} traceId={}",
                RabbitMQConfig.ROUTING_KEY_ORDER_PLACED, eventId, traceId);
          }
        } catch (TimeoutException e) {
          log.error("[MQ-PUB-TIMEOUT] eventId={} traceId={}", eventId, traceId);
        } catch (InterruptedException e) {
          Thread.currentThread().interrupt();
          log.error("[MQ-PUB-INTERRUPT] eventId={} traceId={}", eventId, traceId);
        } catch (Exception e) {
          log.error("[MQ-PUB-ERR] eventId={} traceId={} err={}", eventId, traceId, e.getMessage(), e);
        }
      }
    }
    ```

    (2) Edit `sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/service/OrderCrudService.java`:

    **(2a) Inject `OrderEventPublisher publisher`** — thêm vào field + constructor (explicit, KHÔNG Lombok). Ví dụ pattern hiện có lines 49-59:
    - Thêm field: `private final OrderEventPublisher orderEventPublisher;`
    - Thêm vào constructor: parameter `OrderEventPublisher orderEventPublisher` + `this.orderEventPublisher = orderEventPublisher;`
    - Cũng cần inject `OrderEventEnvelope` builder — không, envelope là record build inline.

    **(2b) Chèn publish CALL** sau khi `OrderEntity saved = orderRepository.save(order);` (line ~178-181 area, ngay trước khi return). Build payload từ saved entity:

    ```java
    // D-03: Publish OrderPlaced event sau commit (defer trong afterCommit callback).
    // D-12: Thay thế deductStock REST call cũ (đã xóa).
    var payloadItems = saved.getItems().stream()
        .map(it -> new OrderEventEnvelope.Item(it.getProductId(), it.getQuantity(), it.getUnitPrice()))
        .toList();
    var payload = new OrderEventEnvelope.OrderPlacedPayload(
        saved.getId(),
        saved.getUserId(),
        payloadItems,
        saved.getTotal(),
        "VND"
    );
    orderEventPublisher.publishOrderPlaced(payload);
    ```

    Nếu getter của OrderEntity dùng record-style (`saved.id()`, `saved.userId()`, etc.) thay vì `getX()`, follow convention hiện có trong file (kiểm tra reading first; dùng đúng style hiện hữu cho OrderEntity).

    **(2c) XÓA các phần sau (per D-12):**
    - Method `deductStockAfterPersist(...)` (RESEARCH/PATTERNS chỉ tới lines 381-411 / 338-440 — đọc file thật để xác định range chính xác)
    - Method `buildStockUpdateBody(...)` / `buildPatchBody(...)`
    - Mọi call-site gọi `deductStockAfterPersist(...)` ở createOrderFromCommand (thay bằng publish call ở 2b)
    - Mọi import không còn dùng (vd: RestTemplate cho deduct stock; KHÔNG xóa nếu validateStock vẫn dùng — D-11 giữ validate)

    **(2d) GIỮ NGUYÊN (D-11):** `validateStock(...)`, `validateStockOrThrow(...)`, mọi REST call check stock trước save. KHÔNG đụng.

    **(2e) Sau khi xóa**, lưu lại trong commit body: số dòng đã xóa + tên method để dễ rollback.

    Note quan trọng: KHÔNG dùng `@RequiredArgsConstructor`. Constructor explicit theo pattern lines 49-59 hiện có.
  </action>
  <verify>
    <automated>powershell -Command "Test-Path sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/messaging/publisher/OrderEventPublisher.java; Select-String -Path sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/messaging/publisher/OrderEventPublisher.java -Pattern 'registerSynchronization' -Quiet; Select-String -Path sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/service/OrderCrudService.java -Pattern 'orderEventPublisher.publishOrderPlaced' -Quiet; -not (Select-String -Path sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/service/OrderCrudService.java -Pattern 'deductStockAfterPersist' -Quiet)"</automated>
  </verify>
  <acceptance_criteria>
    - File OrderEventPublisher.java tồn tại với class `OrderEventPublisher` annotated `@Component`
    - `grep 'TransactionSynchronizationManager.registerSynchronization' sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/messaging/publisher/OrderEventPublisher.java` returns 1+ match
    - `grep 'CorrelationData' sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/messaging/publisher/OrderEventPublisher.java` returns 1+ match
    - `grep 'CONFIRM_TIMEOUT_MS = 5000L' sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/messaging/publisher/OrderEventPublisher.java` returns 1 match (D-04 5s)
    - `grep '\[MQ-PUB\]' sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/messaging/publisher/OrderEventPublisher.java` returns 1+ match
    - `grep 'orderEventPublisher.publishOrderPlaced' sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/service/OrderCrudService.java` returns 1 match
    - `grep -c 'deductStockAfterPersist\|buildStockUpdateBody\|buildPatchBody' sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/service/OrderCrudService.java` = 0 (TẤT CẢ legacy methods + calls đã XÓA)
    - `grep 'validateStock' sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/service/OrderCrudService.java` returns 1+ match (D-11 GIỮ)
    - `grep '@RequiredArgsConstructor' sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/messaging/publisher/OrderEventPublisher.java` = 0 (no Lombok)
    - Nếu mvn available: `cd sources/backend/order-service && mvn -q compile` succeed
  </acceptance_criteria>
  <done>OrderEventPublisher implement afterCommit + Publisher Confirms; OrderCrudService inject + call publisher; deductStock REST legacy XÓA hoàn toàn; validateStock GIỮ.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OrderCrudService → RabbitTemplate | In-process trust; rabbitTemplate gọi broker qua TLS-disabled AMQP (intra-docker accept) |
| Producer thread → afterCommit callback thread | MDC không guarantee propagate; capture traceId trước register sync |
| RabbitTemplate → broker | guest/guest credential intra-docker; broker không expose ra public |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-09 | Tampering | Phantom event (publish trước commit) | mitigate | D-03: `registerSynchronization.afterCommit()` — chỉ chạy khi transaction commit thực sự. Test bằng IT rollback scenario (Wave 3 task `OrderEventPublisherAfterCommitTest`). |
| T-23-01 | Information Disclosure | Producer log payload | mitigate | Log chỉ chứa eventId + routingKey + traceId. `[MQ-PUB]` KHÔNG log payload field-level (chỉ eventId UUID). Verify grep `payload=` trong publisher.java = 0 matches. |
| T-23-04 | Spoofing/Tampering | Trace header injection từ client | mitigate | TraceIdFilter (existing) đã sanitize X-Request-Id qua regex `^[a-zA-Z0-9-]{1,64}$`; nếu chưa, scope plan này KHÔNG mở rộng — defer hardening lên Phase 25 gateway. Risk acceptable vì traceId chỉ dùng cho logging, không grant authorization. |
| T-23-10 | DoS | Publisher Confirms timeout block thread | mitigate | CONFIRM_TIMEOUT_MS = 5000L cap blocking; timeout → log error + tiếp tục (D-05 trade-off). Không hold connection. |
</threat_model>

<verification>
- `cd sources/backend/order-service && mvn -q compile` succeed
- 3 RabbitMQConfig.java tồn tại với cùng beans (verify list bean names: orderEventsExchange, deadLetterExchange, deadLetterQueue, inventoryQueue, notificationQueue, rabbitTemplate)
- `grep -r 'deductStockAfterPersist' sources/backend/order-service/src/main/java` returns 0 matches
- `grep -r 'validateStock' sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/service/OrderCrudService.java` returns 1+ matches
- OrderEventPublisher uses `MDC.get("traceId")` capture trước `registerSynchronization`
- Nếu chạy được docker-compose: tạo 1 order qua FE → grep log thấy `[MQ-PUB] event=OrderPlaced eventId=... traceId=...`
</verification>

<success_criteria>
- 3 RabbitMQConfig.java declare cùng topology (idempotent Spring AmqpAdmin)
- OrderEventPublisher publish-after-commit với CorrelationData + 5s timeout
- OrderCrudService inject publisher + chèn vào createOrderFromCommand sau save()
- deductStockAfterPersist + buildPatchBody methods + tất cả call site XÓA hoàn toàn
- validateStock GIỮ NGUYÊN (D-11)
- Trace propagation: MDC capture NGAY + X-Trace-Id header via MessagePostProcessor
- Log contract [MQ-PUB] + [MQ-PUB-NACK] + [MQ-PUB-TIMEOUT] đầy đủ
</success_criteria>

<output>
After completion, create `.planning/phases/23-message-queue-rabbitmq/23-03-SUMMARY.md` recording:
- Liệt kê 9 file mới + diff OrderCrudService (đặc biệt phần XÓA: tên method + range dòng cũ)
- Commit hash để rollback dễ
- Compile result
</output>
