---
phase: 23
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - docker-compose.yml
  - db/init/01-schemas.sql
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [MQ-01]
tags: [rabbitmq, docker-compose, infrastructure, schema-init]

must_haves:
  truths:
    - "Container RabbitMQ chạy được qua docker compose up rabbitmq"
    - "Management UI truy cập được tại http://localhost:15672 với user guest/pass guest"
    - "Schema notification_svc tồn tại khi postgres khởi tạo volume mới"
    - "3 backend service (order/inventory/notification) có env SPRING_RABBITMQ_HOST + depends_on rabbitmq healthy"
    - "REQUIREMENTS.md có section MQ-01..MQ-05 backfilled"
  artifacts:
    - path: "docker-compose.yml"
      provides: "RabbitMQ service block + healthcheck + Management UI port + env vars cho 3 backend service"
      contains: "rabbitmq:\n    image: rabbitmq:3-management"
    - path: "db/init/01-schemas.sql"
      provides: "Schema notification_svc khởi tạo khi Postgres bootstrap volume mới"
      contains: "CREATE SCHEMA IF NOT EXISTS notification_svc"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Section MQ — Message Queue với MQ-01..MQ-05"
      contains: "MQ-01"
  key_links:
    - from: "order-service / inventory-service / notification-service"
      to: "rabbitmq container"
      via: "depends_on: rabbitmq: condition: service_healthy + SPRING_RABBITMQ_HOST=rabbitmq env"
      pattern: "SPRING_RABBITMQ_HOST"
---

<objective>
Bootstrap hạ tầng RabbitMQ + schema notification_svc + backfill REQUIREMENTS.md cho Phase 23.

Purpose: Đáp ứng MQ-01 (RabbitMQ container + Management UI :15672) và mở đường cho các plan downstream (producer/consumer cần host rabbitmq + schema notification_svc). Per D-01..D-02 (topology) và CONTEXT.md §Integration Points.

Output: docker-compose.yml có rabbitmq service; 3 backend service có env vars; db/init thêm schema notification_svc; REQUIREMENTS.md có section MQ.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/23-message-queue-rabbitmq/23-CONTEXT.md
@.planning/phases/23-message-queue-rabbitmq/23-RESEARCH.md
@.planning/phases/23-message-queue-rabbitmq/23-PATTERNS.md
@docker-compose.yml
@db/init/01-schemas.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Thêm RabbitMQ service vào docker-compose.yml + env vars cho 3 backend</name>
  <files>docker-compose.yml</files>
  <read_first>
    - docker-compose.yml (đọc toàn bộ — cần thấy postgres block §lines 2-17 cho healthcheck pattern, user-service block §lines 31-44 cho env pattern, notification-service block §lines 106-109 hiện thiếu env, top-level volumes block §line 128)
    - 23-PATTERNS.md §"docker-compose.yml" §lines 44-92 (analog + concrete YAML)
    - 23-RESEARCH.md §"docker-compose RabbitMQ service" §lines 541-575 (skeleton chính xác)
    - 23-CONTEXT.md §D-02, §Integration Points line 164
  </read_first>
  <action>
    Edit `docker-compose.yml`:

    (1) Thêm block service `rabbitmq` ở root level (đặt ngay sau postgres block để giữ thứ tự infrastructure):
    ```yaml
      rabbitmq:
        image: rabbitmq:3-management
        container_name: tmdt-rabbitmq
        ports:
          - "5672:5672"
          - "15672:15672"
        environment:
          RABBITMQ_DEFAULT_USER: guest
          RABBITMQ_DEFAULT_PASS: guest
        healthcheck:
          test: ["CMD", "rabbitmq-diagnostics", "ping"]
          interval: 10s
          timeout: 5s
          retries: 10
        volumes:
          - tmdt-rabbitmqdata:/var/lib/rabbitmq
    ```

    (2) Cập nhật 3 service block (`order-service`, `inventory-service`, `notification-service`):
    - Thêm vào `depends_on:`:
      ```yaml
          rabbitmq:
            condition: service_healthy
      ```
    - Thêm vào `environment:`:
      ```yaml
          SPRING_RABBITMQ_HOST: rabbitmq
          SPRING_RABBITMQ_USER: guest
          SPRING_RABBITMQ_PASS: guest
      ```

    (3) Block `notification-service` (hiện chỉ có ports/build) PHẢI thêm đầy đủ env DB như user-service (per RESEARCH §Runtime State Inventory line 460):
    ```yaml
        depends_on:
          postgres:
            condition: service_healthy
          rabbitmq:
            condition: service_healthy
        environment:
          SPRING_PROFILES_ACTIVE: dev
          DB_HOST: postgres
          DB_PORT: 5432
          DB_NAME: tmdt
          DB_USER: tmdt
          DB_PASSWORD: tmdt
          SPRING_RABBITMQ_HOST: rabbitmq
          SPRING_RABBITMQ_USER: guest
          SPRING_RABBITMQ_PASS: guest
    ```

    (4) Thêm `tmdt-rabbitmqdata:` vào top-level `volumes:` block (giữ thứ tự alphabet).

    KHÔNG sửa: service blocks khác (user/product/payment/api-gateway/frontend), version: line, postgres block. Per D-01 (chỉ OrderPlaced), D-02 (topology defer config sang service-level Java config).
  </action>
  <verify>
    <automated>powershell -Command "Select-String -Path docker-compose.yml -Pattern 'rabbitmq:3-management' -Quiet; Select-String -Path docker-compose.yml -Pattern 'SPRING_RABBITMQ_HOST: rabbitmq' -Quiet"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'image: rabbitmq:3-management' docker-compose.yml` = 1
    - `grep -c 'SPRING_RABBITMQ_HOST: rabbitmq' docker-compose.yml` >= 3 (order + inventory + notification)
    - `grep -c 'tmdt-rabbitmqdata' docker-compose.yml` >= 2 (volume mount + top-level declare)
    - `grep -c 'rabbitmq-diagnostics ping' docker-compose.yml` = 1
    - notification-service block có dòng `DB_HOST: postgres` (verify qua grep `-A 20 'notification-service:' docker-compose.yml | grep DB_HOST`)
    - Sau `docker compose config` không lỗi YAML
  </acceptance_criteria>
  <done>RabbitMQ service declared trong docker-compose.yml; 3 backend service có env + depends_on rabbitmq; notification-service hoàn thiện env DB block.</done>
</task>

<task type="auto">
  <name>Task 2: Thêm schema notification_svc vào db/init + backfill REQUIREMENTS.md MQ-01..MQ-05</name>
  <files>db/init/01-schemas.sql, .planning/REQUIREMENTS.md</files>
  <read_first>
    - db/init/01-schemas.sql (5 dòng CREATE SCHEMA hiện có — verify pattern)
    - .planning/REQUIREMENTS.md §lines 32-100 (xem section header pattern "### SEED", "### STORE", ... để clone format)
    - 23-RESEARCH.md §phase_requirements §lines 78-85 (bảng MQ-01..MQ-05 + description)
    - 23-PATTERNS.md §"db/init/01-schemas.sql (modify)" §lines 461-479
    - 23-CONTEXT.md §domain (in-scope items)
  </read_first>
  <action>
    (1) Edit `db/init/01-schemas.sql` — thêm 1 dòng:
    ```sql
    CREATE SCHEMA IF NOT EXISTS notification_svc;
    ```
    Giữ thứ tự sau dòng `inventory_svc`. Không xóa schema nào khác.

    Note quan trọng (ghi vào commit body, không vào file SQL): docker-entrypoint-initdb.d chỉ chạy khi volume rỗng. Dev đã chạy compose phải `docker volume rm tmdt-pgdata` HOẶC chạy manual `docker compose exec postgres psql -U tmdt -d tmdt -c "CREATE SCHEMA IF NOT EXISTS notification_svc;"`.

    (2) Edit `.planning/REQUIREMENTS.md` — thêm section "### MQ — Message Queue (RabbitMQ Integration)" ngay sau section "### COUP — Coupon System" (line ~92, trước line `---`), follow format của các section khác (heading + `- [ ] **ID** — description`):

    ```markdown
    ### MQ — Message Queue (RabbitMQ Integration)

    - [ ] **MQ-01** — RabbitMQ container chạy trong docker-compose.yml với Management UI tại http://localhost:15672 (user guest/pass guest cho dev). Image `rabbitmq:3-management`. Healthcheck `rabbitmq-diagnostics ping`. Per D-01, D-02.
    - [ ] **MQ-02** — order-service publish event `OrderPlaced` (routing key `order.placed`) vào topic exchange `order.events` (durable=true) SAU KHI DB commit, với Publisher Confirms bật (`publisher-confirm-type=correlated` + `publisher-returns=true`). JSON envelope `{eventId, eventType, occurredAt, traceId, payload}`. Per D-01, D-03, D-04, D-15.
    - [ ] **MQ-03** — inventory-service consume từ queue `inventory.order-events` (bind `order.#`), trừ `InventoryEntity.quantity` atomic + ghi `inventory_svc.stock_ledger` entry, idempotent qua bảng `inventory_svc.processed_events` (PK event_id, INSERT ... ON CONFLICT DO NOTHING). Per D-06, D-10.
    - [ ] **MQ-04** — notification-service consume từ queue `notification.order-events` (bind `order.#`), ghi `notification_svc.dispatch_log` (status=SENT, channel=email, không gửi SMTP thật), idempotent qua `notification_svc.processed_events`. Per D-14.
    - [ ] **MQ-05** — Consumer throw exception → retry 3 lần exponential backoff (1s → 2s → 4s, config `spring.rabbitmq.listener.simple.retry.*`); `PermanentMessageException` → reject ngay (0 retry) vào DLQ `order-events.dlq` qua DLX `order.dlx` (direct). Verify được message trong DLQ qua Management UI. traceId propagate qua header `X-Trace-Id` xuyên 3 service. Per D-07, D-08, D-09, D-16, D-17.
    ```

    (3) Cập nhật phần `## Traceability` table — thêm 5 dòng cho MQ-01..MQ-05 với status `Active` và Phase 23, follow format dòng "| COUP-01 | Phase 20 | — | Active |".

    (4) Cập nhật dòng total cuối file: `**Total active REQs: 27**` → `**Total active REQs: 32**`; `**Mapped: 27/27**` → `**Mapped: 32/32**`. Cập nhật scope summary `7 trục bổ sung` thành `8 trục bổ sung` và thêm bullet `8. **MQ** — RabbitMQ async messaging cho luồng OrderPlaced (producer + 2 consumer + retry + DLQ + idempotency)`.
  </action>
  <verify>
    <automated>powershell -Command "Select-String -Path db/init/01-schemas.sql -Pattern 'notification_svc' -Quiet; Select-String -Path .planning/REQUIREMENTS.md -Pattern '\*\*MQ-01\*\*' -Quiet; Select-String -Path .planning/REQUIREMENTS.md -Pattern '\*\*MQ-05\*\*' -Quiet"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'CREATE SCHEMA IF NOT EXISTS notification_svc' db/init/01-schemas.sql` = 1
    - `grep -c '\*\*MQ-0[1-5]\*\*' .planning/REQUIREMENTS.md` = 5
    - `grep '| MQ-01 | Phase 23' .planning/REQUIREMENTS.md` returns 1 match
    - `grep 'Total active REQs: 32' .planning/REQUIREMENTS.md` returns 1 match
    - Section "### MQ — Message Queue" tồn tại sau "### COUP" trong REQUIREMENTS.md
  </acceptance_criteria>
  <done>Schema notification_svc khởi tạo qua db/init; REQUIREMENTS.md có MQ-01..MQ-05 + traceability + total count cập nhật.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host machine → docker network | Port 5672 + 15672 bind ra host — chỉ cho dev local; không expose qua firewall |
| guest user → RabbitMQ broker | Default guest/guest credential — RabbitMQ chỉ cho phép guest từ localhost, intra-docker-network coi là localhost từ góc nhìn broker |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-02 | Spoofing | RabbitMQ broker auth | accept | Dev-only setup; guest/guest acceptable vì broker không expose ra mạng ngoài (chỉ localhost host machine + docker internal). Documented in 23-CONTEXT.md §deferred — đổi password sang Phase ops. Verify port 5672 chỉ bind `0.0.0.0:5672` — KHÔNG có public IP firewall opening. |
| T-23-05 | Tampering | db/init/01-schemas.sql | accept | File chỉ thêm `CREATE SCHEMA IF NOT EXISTS notification_svc` — idempotent; không drop schema nào khác. Verify diff trước commit. |
</threat_model>

<verification>
- `docker compose config` returns 0 (YAML valid)
- `docker compose up -d rabbitmq` succeed; `docker compose ps` shows rabbitmq healthy trong 60s
- `curl -u guest:guest http://localhost:15672/api/overview` returns HTTP 200 với JSON body chứa key `rabbitmq_version`
- `psql -h localhost -U tmdt -d tmdt -c "\dn"` (sau khi recreate volume) liệt kê schema `notification_svc`
- `.planning/REQUIREMENTS.md` mở ra thấy section MQ ngay trước `---` Future Requirements
</verification>

<success_criteria>
- docker-compose.yml chứa rabbitmq service với healthcheck rabbitmq-diagnostics ping
- 3 backend service có depends_on rabbitmq + env SPRING_RABBITMQ_*
- notification-service block đủ env DB như user-service
- Volume tmdt-rabbitmqdata declared top-level
- db/init/01-schemas.sql có schema notification_svc
- REQUIREMENTS.md backfilled MQ-01..MQ-05 + traceability + total count 32
</success_criteria>

<output>
After completion, create `.planning/phases/23-message-queue-rabbitmq/23-01-SUMMARY.md` recording:
- Final docker-compose.yml diff (rabbitmq service + 3 env updates + volume)
- Schema list sau init
- Note về cần `docker volume rm tmdt-pgdata` để init script chạy lại nếu volume cũ
</output>
