---
phase: 23
plan: 04
type: execute
wave: 2
depends_on: [23-03]
files_modified:
  - sources/backend/inventory-service/src/main/java/com/ptit/htpt/inventoryservice/messaging/event/OrderEventEnvelope.java
  - sources/backend/inventory-service/src/main/java/com/ptit/htpt/inventoryservice/messaging/exception/TransientMessageException.java
  - sources/backend/inventory-service/src/main/java/com/ptit/htpt/inventoryservice/messaging/exception/PermanentMessageException.java
  - sources/backend/inventory-service/src/main/java/com/ptit/htpt/inventoryservice/messaging/tracing/TraceIdConsumerInterceptor.java
  - sources/backend/inventory-service/src/main/java/com/ptit/htpt/inventoryservice/messaging/consumer/OrderPlacedListener.java
  - sources/backend/inventory-service/src/main/java/com/ptit/htpt/inventoryservice/domain/ProcessedEventEntity.java
  - sources/backend/inventory-service/src/main/java/com/ptit/htpt/inventoryservice/domain/StockLedgerEntity.java
  - sources/backend/inventory-service/src/main/java/com/ptit/htpt/inventoryservice/repository/ProcessedEventRepository.java
  - sources/backend/inventory-service/src/main/java/com/ptit/htpt/inventoryservice/repository/StockLedgerRepository.java
  - sources/backend/inventory-service/src/main/java/com/ptit/htpt/inventoryservice/service/InventoryCrudService.java
  - sources/backend/inventory-service/src/main/resources/db/migration/V2__add_messaging_tables.sql
  - sources/backend/inventory-service/src/main/resources/db/seed-dev/V102__seed_initial_inventory.sql
autonomous: true
requirements: [MQ-03]
tags: [inventory-service, consumer, idempotency, stock-ledger, flyway-v2, seed-dev]

must_haves:
  truths:
    - "inventory-service consume OrderPlaced từ queue inventory.order-events"
    - "Idempotency: INSERT processed_events ON CONFLICT DO NOTHING — duplicate eventId → skip + ACK"
    - "Trừ InventoryEntity.quantity atomic + ghi stock_ledger entry per item"
    - "PermanentMessageException (product không tồn tại) → AmqpRejectAndDontRequeueException → DLQ ngay (0 retry)"
    - "TransientMessageException (DB transient) → throw lên Spring retry interceptor"
    - "MDC traceId set từ X-Trace-Id header trước business logic, remove trong finally"
    - "Flyway V2 tạo processed_events + stock_ledger; seed-dev V102 copy stock từ product_svc.products"
  artifacts:
    - path: "sources/backend/inventory-service/src/main/java/com/ptit/htpt/inventoryservice/messaging/consumer/OrderPlacedListener.java"
      provides: "@RabbitListener queue=inventory.order-events + @Transactional + idempotency check + decrement + ledger"
      min_lines: 70
    - path: "sources/backend/inventory-service/src/main/resources/db/migration/V2__add_messaging_tables.sql"
      provides: "Bảng inventory_svc.processed_events + inventory_svc.stock_ledger + indexes"
      contains: "CREATE TABLE IF NOT EXISTS inventory_svc.processed_events"
    - path: "sources/backend/inventory-service/src/main/resources/db/seed-dev/V102__seed_initial_inventory.sql"
      provides: "Copy product_svc.products.stock → inventory_svc.inventory_items.quantity (idempotent)"
      contains: "INSERT INTO inventory_svc.inventory_items"
  key_links:
    - from: "RabbitMQ queue inventory.order-events"
      to: "OrderPlacedListener.onOrderPlaced"
      via: "@RabbitListener(queues = INVENTORY_QUEUE)"
      pattern: "@RabbitListener"
    - from: "OrderPlacedListener"
      to: "ProcessedEventRepository.insertIfAbsent"
      via: "Idempotency INSERT ... ON CONFLICT DO NOTHING"
      pattern: "ON CONFLICT"
    - from: "OrderPlacedListener"
      to: "InventoryCrudService.decrementForOrder"
      via: "Per-item loop + StockLedgerRepository.save"
      pattern: "decrementForOrder"
---

<objective>
Triển khai consumer inventory-service: lắng nghe OrderPlaced, trừ kho atomic + ghi ledger, idempotent qua processed_events. Đáp ứng MQ-03.

Purpose: Per D-06 (idempotency table), D-08 (Transient vs Permanent exception), D-10 (consumer logic per item: find → decrement → ledger → insert processed_events), D-13 (seed inventory từ products).

Output: Toàn bộ consumer-side cho inventory-service: copy envelope/exceptions, JPA entities ProcessedEvent + StockLedger, repositories, listener, V2 migration, V102 seed-dev.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-message-queue-rabbitmq/23-CONTEXT.md
@.planning/phases/23-message-queue-rabbitmq/23-RESEARCH.md
@.planning/phases/23-message-queue-rabbitmq/23-PATTERNS.md
@.planning/phases/23-message-queue-rabbitmq/23-02-SUMMARY.md
@.planning/phases/23-message-queue-rabbitmq/23-03-SUMMARY.md
@sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/messaging/event/OrderEventEnvelope.java
@sources/backend/inventory-service/src/main/java/com/ptit/htpt/inventoryservice/domain/InventoryEntity.java
@sources/backend/inventory-service/src/main/java/com/ptit/htpt/inventoryservice/repository/InventoryRepository.java
@sources/backend/inventory-service/src/main/java/com/ptit/htpt/inventoryservice/service/InventoryCrudService.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration V2 + V102 seed + JPA entities + repositories + envelope/exception copy</name>
  <files>
    sources/backend/inventory-service/src/main/resources/db/migration/V2__add_messaging_tables.sql,
    sources/backend/inventory-service/src/main/resources/db/seed-dev/V102__seed_initial_inventory.sql,
    sources/backend/inventory-service/src/main/java/com/ptit/htpt/inventoryservice/domain/ProcessedEventEntity.java,
    sources/backend/inventory-service/src/main/java/com/ptit/htpt/inventoryservice/domain/StockLedgerEntity.java,
    sources/backend/inventory-service/src/main/java/com/ptit/htpt/inventoryservice/repository/ProcessedEventRepository.java,
    sources/backend/inventory-service/src/main/java/com/ptit/htpt/inventoryservice/repository/StockLedgerRepository.java,
    sources/backend/inventory-service/src/main/java/com/ptit/htpt/inventoryservice/messaging/event/OrderEventEnvelope.java,
    sources/backend/inventory-service/src/main/java/com/ptit/htpt/inventoryservice/messaging/exception/TransientMessageException.java,
    sources/backend/inventory-service/src/main/java/com/ptit/htpt/inventoryservice/messaging/exception/PermanentMessageException.java
  </files>
  <read_first>
    - sources/backend/inventory-service/src/main/java/com/ptit/htpt/inventoryservice/domain/InventoryEntity.java (full — entity pattern: @Table schema, @Id String UUID, factory create(), record-style accessors `productId()` not `getProductId()`)
    - sources/backend/inventory-service/src/main/java/com/ptit/htpt/inventoryservice/repository/InventoryRepository.java (full — Repository pattern + native @Query)
    - sources/backend/inventory-service/src/main/resources/db/migration/V1__init_schema.sql (full — DDL convention: schema-prefixed table, indexes, Vietnamese comments)
    - sources/backend/inventory-service/src/main/resources/db/seed-dev/V2__seed_dev_data.sql (full — seed pattern)
    - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/messaging/event/OrderEventEnvelope.java (sau Plan 03 — copy nguyên content, đổi package)
    - 23-RESEARCH.md §"Pattern 4: Idempotency Repository" §lines 391-403 + §"Code Examples — Idempotency check pattern" §lines 518-539
    - 23-PATTERNS.md §"V2__add_messaging_tables.sql" §lines 393-429, §"V102__seed_initial_inventory.sql" §lines 433-457, §"JPA entities" §lines 328-362
    - 23-CONTEXT.md §D-06, §D-10, §D-13
  </read_first>
  <action>
    (1) **`V2__add_messaging_tables.sql`** ở `inventory-service/src/main/resources/db/migration/`:

    ```sql
    -- V2: Add messaging tables cho Phase 23 (D-06 idempotency + D-10 stock ledger).

    CREATE TABLE IF NOT EXISTS inventory_svc.processed_events (
      event_id     VARCHAR(36)  PRIMARY KEY,
      event_type   VARCHAR(64)  NOT NULL,
      processed_at TIMESTAMPTZ  NOT NULL DEFAULT now()
    );
    CREATE INDEX IF NOT EXISTS idx_processed_events_type ON inventory_svc.processed_events(event_type);

    CREATE TABLE IF NOT EXISTS inventory_svc.stock_ledger (
      id              BIGSERIAL    PRIMARY KEY,
      event_id        VARCHAR(36)  NOT NULL,
      order_id        VARCHAR(36)  NOT NULL,
      product_id      VARCHAR(36)  NOT NULL,
      quantity_change INT          NOT NULL,
      reason          VARCHAR(32)  NOT NULL,
      created_at      TIMESTAMPTZ  NOT NULL DEFAULT now()
    );
    CREATE INDEX IF NOT EXISTS idx_stock_ledger_order ON inventory_svc.stock_ledger(order_id);
    CREATE INDEX IF NOT EXISTS idx_stock_ledger_event ON inventory_svc.stock_ledger(event_id);
    ```

    (2) **`V102__seed_initial_inventory.sql`** ở `inventory-service/src/main/resources/db/seed-dev/`:

    ```sql
    -- V102 (seed-dev): D-13 — Copy stock từ product_svc.products sang inventory_svc.inventory_items.
    -- Idempotent: chỉ insert nếu product_id chưa có trong inventory_items.

    INSERT INTO inventory_svc.inventory_items (id, product_id, quantity, reserved, created_at, updated_at)
    SELECT gen_random_uuid()::text, p.id, COALESCE(p.stock, 0), 0, NOW(), NOW()
    FROM product_svc.products p
    WHERE p.deleted_at IS NULL
      AND NOT EXISTS (
        SELECT 1 FROM inventory_svc.inventory_items i WHERE i.product_id = p.id
      );
    ```

    Note: Nếu schema column `deleted_at` không có trên products → bỏ điều kiện. Verify trước commit (đọc V101 seed_catalog.sql của product-service).

    (3) **`ProcessedEventEntity.java`** (package `com.ptit.htpt.inventoryservice.domain`):

    ```java
    package com.ptit.htpt.inventoryservice.domain;

    import jakarta.persistence.*;
    import java.time.Instant;

    @Entity
    @Table(name = "processed_events", schema = "inventory_svc")
    public class ProcessedEventEntity {

      @Id
      @Column(name = "event_id", length = 36, nullable = false, updatable = false)
      private String eventId;

      @Column(name = "event_type", length = 64, nullable = false)
      private String eventType;

      @Column(name = "processed_at", nullable = false)
      private Instant processedAt;

      protected ProcessedEventEntity() {}

      public ProcessedEventEntity(String eventId, String eventType, Instant processedAt) {
        this.eventId = eventId;
        this.eventType = eventType;
        this.processedAt = processedAt;
      }

      public String eventId() { return eventId; }
      public String eventType() { return eventType; }
      public Instant processedAt() { return processedAt; }
    }
    ```

    (4) **`StockLedgerEntity.java`** (package `com.ptit.htpt.inventoryservice.domain`):

    ```java
    package com.ptit.htpt.inventoryservice.domain;

    import jakarta.persistence.*;
    import java.time.Instant;
    import java.util.UUID;

    @Entity
    @Table(name = "stock_ledger", schema = "inventory_svc")
    public class StockLedgerEntity {

      @Id
      @GeneratedValue(strategy = GenerationType.IDENTITY)
      private Long id;

      @Column(name = "event_id", length = 36, nullable = false)
      private String eventId;

      @Column(name = "order_id", length = 36, nullable = false)
      private String orderId;

      @Column(name = "product_id", length = 36, nullable = false)
      private String productId;

      @Column(name = "quantity_change", nullable = false)
      private int quantityChange;

      @Column(name = "reason", length = 32, nullable = false)
      private String reason;

      @Column(name = "created_at", nullable = false)
      private Instant createdAt;

      protected StockLedgerEntity() {}

      public StockLedgerEntity(String eventId, String orderId, String productId,
                                int quantityChange, String reason, Instant createdAt) {
        this.eventId = eventId;
        this.orderId = orderId;
        this.productId = productId;
        this.quantityChange = quantityChange;
        this.reason = reason;
        this.createdAt = createdAt;
      }

      public static StockLedgerEntity create(String eventId, String orderId, String productId,
                                              int quantityChange, String reason) {
        return new StockLedgerEntity(eventId, orderId, productId, quantityChange, reason, Instant.now());
      }

      public Long id() { return id; }
      public String eventId() { return eventId; }
      public String orderId() { return orderId; }
      public String productId() { return productId; }
      public int quantityChange() { return quantityChange; }
      public String reason() { return reason; }
      public Instant createdAt() { return createdAt; }
    }
    ```

    (5) **`ProcessedEventRepository.java`**:

    ```java
    package com.ptit.htpt.inventoryservice.repository;

    import com.ptit.htpt.inventoryservice.domain.ProcessedEventEntity;
    import org.springframework.data.jpa.repository.JpaRepository;
    import org.springframework.data.jpa.repository.Modifying;
    import org.springframework.data.jpa.repository.Query;
    import org.springframework.data.repository.query.Param;
    import org.springframework.transaction.annotation.Transactional;

    public interface ProcessedEventRepository extends JpaRepository<ProcessedEventEntity, String> {

      @Modifying
      @Transactional
      @Query(value = "INSERT INTO inventory_svc.processed_events(event_id, event_type) " +
                     "VALUES (:id, :type) ON CONFLICT (event_id) DO NOTHING",
             nativeQuery = true)
      int insertNative(@Param("id") String eventId, @Param("type") String eventType);

      default boolean insertIfAbsent(String eventId, String eventType) {
        return insertNative(eventId, eventType) == 1;
      }
    }
    ```

    (6) **`StockLedgerRepository.java`**:

    ```java
    package com.ptit.htpt.inventoryservice.repository;

    import com.ptit.htpt.inventoryservice.domain.StockLedgerEntity;
    import org.springframework.data.jpa.repository.JpaRepository;
    import java.util.List;

    public interface StockLedgerRepository extends JpaRepository<StockLedgerEntity, Long> {
      List<StockLedgerEntity> findByOrderId(String orderId);
      List<StockLedgerEntity> findByEventId(String eventId);
    }
    ```

    (7) **Copy `OrderEventEnvelope.java`** từ order-service (Plan 03) sang `inventory-service/src/main/java/com/ptit/htpt/inventoryservice/messaging/event/OrderEventEnvelope.java` — đổi `package com.ptit.htpt.inventoryservice.messaging.event;`, giữ nguyên body record + 2 nested record.

    (8) **Copy `TransientMessageException.java`** + **`PermanentMessageException.java`** từ order-service sang `inventory-service/src/main/java/com/ptit/htpt/inventoryservice/messaging/exception/` — đổi package, giữ body.
  </action>
  <verify>
    <automated>powershell -Command "Test-Path sources/backend/inventory-service/src/main/resources/db/migration/V2__add_messaging_tables.sql; Test-Path sources/backend/inventory-service/src/main/resources/db/seed-dev/V102__seed_initial_inventory.sql; Test-Path sources/backend/inventory-service/src/main/java/com/ptit/htpt/inventoryservice/domain/StockLedgerEntity.java; Select-String -Path sources/backend/inventory-service/src/main/java/com/ptit/htpt/inventoryservice/repository/ProcessedEventRepository.java -Pattern 'ON CONFLICT' -Quiet"</automated>
  </verify>
  <acceptance_criteria>
    - File V2__add_messaging_tables.sql tồn tại với `CREATE TABLE IF NOT EXISTS inventory_svc.processed_events` + `CREATE TABLE IF NOT EXISTS inventory_svc.stock_ledger`
    - File V102__seed_initial_inventory.sql tồn tại với `INSERT INTO inventory_svc.inventory_items ... SELECT ... FROM product_svc.products`
    - 4 file Java mới trong `inventoryservice.domain` + `inventoryservice.repository` tồn tại
    - `grep 'ON CONFLICT (event_id) DO NOTHING' sources/backend/inventory-service/src/main/java/com/ptit/htpt/inventoryservice/repository/ProcessedEventRepository.java` returns 1 match
    - OrderEventEnvelope.java + 2 exception class copy đúng package `com.ptit.htpt.inventoryservice.messaging.*`
    - `grep '@Table.*schema = \"inventory_svc\"' sources/backend/inventory-service/src/main/java/com/ptit/htpt/inventoryservice/domain/ProcessedEventEntity.java` returns 1 match
    - `grep '@Table.*schema = \"inventory_svc\"' sources/backend/inventory-service/src/main/java/com/ptit/htpt/inventoryservice/domain/StockLedgerEntity.java` returns 1 match
    - Compile (nếu mvn): `cd sources/backend/inventory-service && mvn -q compile` succeed
  </acceptance_criteria>
  <done>Migration + entities + repos + envelope/exceptions sẵn cho Listener Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: OrderPlacedListener + InventoryCrudService.decrementForOrder + TraceIdConsumerInterceptor</name>
  <files>
    sources/backend/inventory-service/src/main/java/com/ptit/htpt/inventoryservice/messaging/tracing/TraceIdConsumerInterceptor.java,
    sources/backend/inventory-service/src/main/java/com/ptit/htpt/inventoryservice/messaging/consumer/OrderPlacedListener.java,
    sources/backend/inventory-service/src/main/java/com/ptit/htpt/inventoryservice/service/InventoryCrudService.java
  </files>
  <read_first>
    - 23-RESEARCH.md §"Pattern 3: Consumer + Idempotency" §lines 341-385 (canonical listener skeleton)
    - 23-RESEARCH.md §"Pitfall 4: Retry chạy CẢ với PermanentException" §lines 488-492 (AmqpRejectAndDontRequeueException)
    - 23-RESEARCH.md §"Pitfall 8: processed_events insert SAU business logic" §lines 511-514 (INSERT processed_events ĐẦU TIÊN)
    - sources/backend/inventory-service/src/main/java/com/ptit/htpt/inventoryservice/service/InventoryCrudService.java (full — extend với method `decrementForOrder`)
    - sources/backend/inventory-service/src/main/java/com/ptit/htpt/inventoryservice/domain/InventoryEntity.java (full — find method để decrement quantity, hoặc thêm method `adjustQuantity(-amount)`)
    - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/web/TraceIdFilter.java (full — pattern MDC put/remove)
    - 23-CONTEXT.md §D-08, §D-10, §D-16, §D-17
    - 23-PATTERNS.md §"OrderPlacedListener.java" §lines 287-301, §"TraceIdMessagePostProcessor + TraceIdConsumerInterceptor" §lines 304-325
  </read_first>
  <action>
    (1) **`TraceIdConsumerInterceptor.java`** — utility class (không thực sự là Spring AOP interceptor mà chỉ helper) gọi từ listener. Đặt ở `messaging/tracing/`:

    ```java
    package com.ptit.htpt.inventoryservice.messaging.tracing;

    import org.slf4j.MDC;

    /** D-16: consumer set MDC traceId từ AMQP header X-Trace-Id trước business logic; remove trong finally. */
    public final class TraceIdConsumerInterceptor {
      public static final String HEADER = "X-Trace-Id";
      public static final String MDC_KEY = "traceId";

      private TraceIdConsumerInterceptor() {}

      public static void enter(String traceIdHeader) {
        String id = (traceIdHeader != null && !traceIdHeader.isBlank()) ? traceIdHeader : "no-trace";
        MDC.put(MDC_KEY, id);
      }

      public static void exit() {
        MDC.remove(MDC_KEY);
      }
    }
    ```

    (2) **Extend `InventoryCrudService.java`** — thêm method `decrementForOrder(String eventId, String orderId, List<OrderEventEnvelope.Item> items)`. Read file để xác định pattern existing (constructor injection style, có sẵn InventoryRepository field). Thêm vào constructor: `StockLedgerRepository stockLedgerRepository`. Thêm method:

    ```java
    /**
     * D-10: Trừ kho atomic + ghi ledger cho từng item. Gọi từ OrderPlacedListener trong @Transactional.
     * @throws PermanentMessageException nếu productId không có inventory record (data inconsistency).
     */
    @Transactional
    public void decrementForOrder(String eventId, String orderId,
                                   java.util.List<com.ptit.htpt.inventoryservice.messaging.event.OrderEventEnvelope.Item> items) {
      for (var item : items) {
        InventoryEntity inv = inventoryRepository.findByProductId(item.productId())
            .orElseThrow(() -> new com.ptit.htpt.inventoryservice.messaging.exception.PermanentMessageException(
                "No inventory record for productId=" + item.productId()));
        // Giảm quantity. Nếu kết quả < 0 → log warning nhưng vẫn cho phép (D-10 bước 2).
        int before = inv.quantity();
        inv.adjustQuantity(-item.quantity()); // dùng method có sẵn — nếu chưa có, fallback set quantity trực tiếp
        if (inv.quantity() < 0) {
          log.warn("[INV] Negative stock productId={} before={} change={} after={} (audit concurrency)",
              item.productId(), before, -item.quantity(), inv.quantity());
        }
        inventoryRepository.save(inv);
        stockLedgerRepository.save(StockLedgerEntity.create(
            eventId, orderId, item.productId(), -item.quantity(), "order.placed"));
      }
    }
    ```

    Note: Nếu InventoryEntity hiện KHÔNG có method `adjustQuantity(int delta)`, thêm: `public void adjustQuantity(int delta) { this.quantity += delta; this.updatedAt = Instant.now(); }`. Verify field name `quantity` qua reading first.

    Thêm import: `org.springframework.transaction.annotation.Transactional`, `com.ptit.htpt.inventoryservice.repository.StockLedgerRepository`, `com.ptit.htpt.inventoryservice.domain.StockLedgerEntity`, slf4j Logger.

    (3) **`OrderPlacedListener.java`** ở `messaging/consumer/`:

    ```java
    package com.ptit.htpt.inventoryservice.messaging.consumer;

    import com.ptit.htpt.inventoryservice.messaging.event.OrderEventEnvelope;
    import com.ptit.htpt.inventoryservice.messaging.exception.PermanentMessageException;
    import com.ptit.htpt.inventoryservice.messaging.exception.TransientMessageException;
    import com.ptit.htpt.inventoryservice.messaging.tracing.TraceIdConsumerInterceptor;
    import com.ptit.htpt.inventoryservice.repository.ProcessedEventRepository;
    import com.ptit.htpt.inventoryservice.service.InventoryCrudService;
    import org.slf4j.Logger;
    import org.slf4j.LoggerFactory;
    import org.springframework.amqp.AmqpRejectAndDontRequeueException;
    import org.springframework.amqp.rabbit.annotation.RabbitListener;
    import org.springframework.dao.TransientDataAccessException;
    import org.springframework.dao.DataAccessResourceFailureException;
    import org.springframework.messaging.handler.annotation.Header;
    import org.springframework.messaging.handler.annotation.Payload;
    import org.springframework.stereotype.Component;
    import org.springframework.transaction.annotation.Transactional;

    /**
     * D-10: Consume OrderPlaced từ queue inventory.order-events.
     * Idempotent qua processed_events table (D-06).
     * PermanentException → DLQ ngay (D-08 + Pitfall 4).
     */
    @Component
    public class OrderPlacedListener {
      private static final Logger log = LoggerFactory.getLogger(OrderPlacedListener.class);
      private static final String QUEUE = "inventory.order-events";
      private static final String EVENT_TYPE = "OrderPlaced";

      private final ProcessedEventRepository processedEventRepository;
      private final InventoryCrudService inventoryCrudService;

      public OrderPlacedListener(ProcessedEventRepository processedEventRepository,
                                  InventoryCrudService inventoryCrudService) {
        this.processedEventRepository = processedEventRepository;
        this.inventoryCrudService = inventoryCrudService;
      }

      @RabbitListener(queues = QUEUE)
      @Transactional
      public void onOrderPlaced(@Payload OrderEventEnvelope envelope,
                                 @Header(name = "X-Trace-Id", required = false) String traceIdHeader) {
        TraceIdConsumerInterceptor.enter(traceIdHeader);
        String eventId = envelope.eventId();
        try {
          log.info("[MQ-CONSUME] queue={} eventId={} status=received", QUEUE, eventId);

          // D-06 + Pitfall 8: INSERT processed_events ĐẦU TIÊN trong cùng transaction.
          boolean inserted = processedEventRepository.insertIfAbsent(eventId, EVENT_TYPE);
          if (!inserted) {
            log.info("[MQ-CONSUME] queue={} eventId={} status=skipped-duplicate", QUEUE, eventId);
            return;
          }

          // Business logic (D-10): trừ kho + ledger.
          inventoryCrudService.decrementForOrder(eventId, envelope.payload().orderId(), envelope.payload().items());

          log.info("[MQ-CONSUME] queue={} eventId={} status=done", QUEUE, eventId);
        } catch (PermanentMessageException e) {
          log.error("[MQ-DLQ] eventId={} reason={} payload={}", eventId, e.getMessage(), envelope);
          throw new AmqpRejectAndDontRequeueException(e.getMessage(), e);
        } catch (DataAccessResourceFailureException | TransientDataAccessException e) {
          log.warn("[MQ-RETRY] eventId={} error={}", eventId, e.getMessage());
          throw new TransientMessageException("DB transient on consume eventId=" + eventId, e);
        } catch (RuntimeException e) {
          // Mặc định coi như permanent — tránh retry vô tận.
          log.error("[MQ-DLQ] eventId={} reason=unexpected error={} payload={}", eventId, e.getMessage(), envelope);
          throw new AmqpRejectAndDontRequeueException("Unexpected: " + e.getMessage(), e);
        } finally {
          TraceIdConsumerInterceptor.exit();
        }
      }
    }
    ```

    KHÔNG dùng Lombok. KHÔNG log raw payload field-by-field (chỉ envelope.toString() qua Java record — chấp nhận vì payload không chứa PII email).
  </action>
  <verify>
    <automated>powershell -Command "Test-Path sources/backend/inventory-service/src/main/java/com/ptit/htpt/inventoryservice/messaging/consumer/OrderPlacedListener.java; Select-String -Path sources/backend/inventory-service/src/main/java/com/ptit/htpt/inventoryservice/messaging/consumer/OrderPlacedListener.java -Pattern '@RabbitListener' -Quiet; Select-String -Path sources/backend/inventory-service/src/main/java/com/ptit/htpt/inventoryservice/messaging/consumer/OrderPlacedListener.java -Pattern 'AmqpRejectAndDontRequeueException' -Quiet; Select-String -Path sources/backend/inventory-service/src/main/java/com/ptit/htpt/inventoryservice/service/InventoryCrudService.java -Pattern 'decrementForOrder' -Quiet"</automated>
  </verify>
  <acceptance_criteria>
    - OrderPlacedListener.java tồn tại với `@Component` + `@RabbitListener(queues = "inventory.order-events")` + `@Transactional`
    - `grep 'AmqpRejectAndDontRequeueException' sources/backend/inventory-service/src/main/java/com/ptit/htpt/inventoryservice/messaging/consumer/OrderPlacedListener.java` returns 2+ matches (PermanentException catch + RuntimeException fallback)
    - `grep 'insertIfAbsent' sources/backend/inventory-service/src/main/java/com/ptit/htpt/inventoryservice/messaging/consumer/OrderPlacedListener.java` returns 1+ match
    - `grep 'decrementForOrder' sources/backend/inventory-service/src/main/java/com/ptit/htpt/inventoryservice/service/InventoryCrudService.java` returns 1+ match (method định nghĩa)
    - `grep '\[MQ-CONSUME\]' sources/backend/inventory-service/src/main/java/com/ptit/htpt/inventoryservice/messaging/consumer/OrderPlacedListener.java` returns 3+ matches (received + done + skipped-duplicate)
    - Idempotency check BEFORE business logic (line order: `insertIfAbsent(...)` BEFORE `decrementForOrder(...)`) — verify Pitfall 8
    - `grep 'TraceIdConsumerInterceptor.enter' sources/backend/inventory-service/src/main/java/com/ptit/htpt/inventoryservice/messaging/consumer/OrderPlacedListener.java` returns 1
    - `grep '@RequiredArgsConstructor' sources/backend/inventory-service/src/main/java/com/ptit/htpt/inventoryservice/messaging/consumer/OrderPlacedListener.java` = 0 (no Lombok)
    - Compile (nếu mvn): `cd sources/backend/inventory-service && mvn -q compile` succeed
  </acceptance_criteria>
  <done>Inventory consumer listener implement đầy đủ với idempotency, exception classification, MDC trace. InventoryCrudService có method decrementForOrder.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RabbitMQ broker → inventory consumer | Trust message từ broker nội bộ; KHÔNG re-verify signature (chấp nhận risk intra-docker) |
| Consumer → DB | Transactional boundary; processed_events PK đảm bảo no race |
| Consumer log | KHÔNG log full user payload field-level — chỉ envelope.toString() (Java record default) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-03 | Tampering | Concurrent process duplicate eventId | mitigate | D-06: PK constraint `event_id` + `INSERT ... ON CONFLICT DO NOTHING` atomic — race-safe. IT scenario `idempotent_samePayloadTwice` (Wave 3) chứng minh. |
| T-23-11 | DoS | Poison message retry vô tận | mitigate | D-08 + Pitfall 4: `AmqpRejectAndDontRequeueException` cho Permanent + RuntimeException fallback → DLQ ngay (0 retry). Transient mới retry 3 lần. |
| T-23-01 | Information Disclosure | Log raw payload chứa userId/items | mitigate | `[MQ-CONSUME]` log chỉ chứa queue + eventId + status. `[MQ-DLQ]` log payload nhưng chỉ ở level error + chỉ khi exception. userId là internal UUID không phải email/phone — risk thấp. |
| T-23-12 | Tampering | InventoryEntity quantity âm sau decrement | accept | D-10 bước 2: chấp nhận âm; log warning audit; KHÔNG block message (vì stock đã validate đồng bộ trước đó qua REST D-11). |
</threat_model>

<verification>
- `cd sources/backend/inventory-service && mvn -q compile` succeed
- Sau khi `docker compose up inventory-service`, log thấy Flyway apply V2 + V102 (nếu profile dev)
- Tạo 1 order qua FE → grep `docker compose logs inventory-service | grep '[MQ-CONSUME]'` thấy 3 dòng (received → done) cho cùng eventId
- psql query `SELECT count(*) FROM inventory_svc.processed_events WHERE event_type='OrderPlaced'` >= 1 sau test
- psql query `SELECT count(*) FROM inventory_svc.stock_ledger` matches số item của test order
- Repeat publish cùng eventId → log thấy `status=skipped-duplicate`, không có row mới trong stock_ledger
</verification>

<success_criteria>
- V2 migration tạo processed_events + stock_ledger với indexes
- V102 seed-dev copy stock từ product_svc.products (idempotent)
- ProcessedEventEntity + StockLedgerEntity JPA entities đúng schema convention
- ProcessedEventRepository có insertIfAbsent (native ON CONFLICT DO NOTHING)
- InventoryCrudService.decrementForOrder atomic + ghi ledger + throw PermanentMessageException nếu product không tồn tại
- OrderPlacedListener @RabbitListener + @Transactional + idempotency BEFORE business + exception classification + MDC trace
- KHÔNG Lombok ở mọi file mới
</success_criteria>

<output>
After completion, create `.planning/phases/23-message-queue-rabbitmq/23-04-SUMMARY.md` recording:
- List 12 file mới/sửa
- Migration V2 + V102 DDL
- Compile result
- Pattern: Insertion-Order BEFORE business + Idempotency (Pitfall 8)
</output>
