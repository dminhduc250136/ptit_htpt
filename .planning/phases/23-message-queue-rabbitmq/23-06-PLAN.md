---
phase: 23
plan: 06
type: execute
wave: 3
depends_on: [23-03, 23-04, 23-05]
files_modified:
  - sources/backend/order-service/src/test/java/com/ptit/htpt/orderservice/messaging/OrderEventPublisherIT.java
  - sources/backend/inventory-service/src/test/java/com/ptit/htpt/inventoryservice/messaging/OrderPlacedListenerIT.java
  - sources/backend/notification-service/src/test/java/com/ptit/htpt/notificationservice/messaging/OrderPlacedNotifyListenerIT.java
  - sources/backend/notification-service/src/test/resources/test-init/01-schemas.sql
  - sources/backend/inventory-service/src/test/resources/test-init/01-schemas.sql
  - sources/backend/order-service/src/test/resources/test-init/01-schemas.sql
  - scripts/verify-mq.sh
  - architecture/02-sequence-diagrams.md
autonomous: true
requirements: [MQ-02, MQ-03, MQ-04, MQ-05]
tags: [integration-test, testcontainers, rabbitmq, dlq, verification, architecture-doc]

must_haves:
  truths:
    - "4 scenario IT (happy path / idempotent / DLQ permanent / retry transient) pass cho inventory consumer (D-18 lock)"
    - "OrderEventPublisherIT chứng minh afterCommit: rollback transaction → KHÔNG publish"
    - "DLQ verify được qua RabbitAdmin.getQueueProperties hoặc HTTP API /api/queues/%2F/order-events.dlq"
    - "Architecture doc 02-sequence-diagrams.md cập nhật phản ánh RabbitMQ (thay vì Kafka cũ)"
    - "Smoke script verify-mq.sh chạy được sau docker compose up"
  artifacts:
    - path: "sources/backend/inventory-service/src/test/java/com/ptit/htpt/inventoryservice/messaging/OrderPlacedListenerIT.java"
      provides: "4 @Test scenario D-18: happyPath, idempotent_samePayloadTwice, permanentException_routedToDLQ, transientThenSuccess_retryThenAck (FULL 4, no @Disabled)"
      min_lines: 140
    - path: "scripts/verify-mq.sh"
      provides: "Bash smoke script: curl Management UI overview + check exchanges/queues exist"
      min_lines: 20
  key_links:
    - from: "IT test class"
      to: "RabbitMQContainer + PostgreSQLContainer"
      via: "@Container + @ServiceConnection (Spring Boot 3.1+)"
      pattern: "@ServiceConnection"
    - from: "IT test"
      to: "RabbitAdmin queue inspection"
      via: "rabbitAdmin.getQueueProperties(\"order-events.dlq\").get(\"QUEUE_MESSAGE_COUNT\")"
      pattern: "getQueueProperties"
---

<objective>
Integration test + smoke verify + architecture doc cập nhật — đáp ứng MQ-05 (retry/DLQ verify) + MQ-02/03/04 end-to-end + success criterion #7 (IT chứng minh luồng).

Purpose: Per D-18 (4 scenario IT Testcontainers — TẤT CẢ 4, không @Disabled); demo flow cho hội đồng; cập nhật architecture doc reflect RabbitMQ thay Kafka (CONTEXT §canonical_refs).

Output: 3 IT class (1 publisher + 2 consumer), test-init schema scripts (3 service), smoke shell script, architecture doc cập nhật.

[BLOCKING] Chạy full IT suite trong Wave này — chứng minh Flyway migrations + business logic OK trên Testcontainers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-message-queue-rabbitmq/23-CONTEXT.md
@.planning/phases/23-message-queue-rabbitmq/23-RESEARCH.md
@.planning/phases/23-message-queue-rabbitmq/23-PATTERNS.md
@.planning/phases/23-message-queue-rabbitmq/23-VALIDATION.md
@.planning/phases/23-message-queue-rabbitmq/23-03-SUMMARY.md
@.planning/phases/23-message-queue-rabbitmq/23-04-SUMMARY.md
@.planning/phases/23-message-queue-rabbitmq/23-05-SUMMARY.md
@sources/backend/order-service/src/test/java/com/ptit/htpt/orderservice/service/OrderCouponRaceConditionIT.java
@architecture/02-sequence-diagrams.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inventory + Notification consumer IT (4 scenario D-18 đầy đủ cho inventory)</name>
  <files>
    sources/backend/inventory-service/src/test/java/com/ptit/htpt/inventoryservice/messaging/OrderPlacedListenerIT.java,
    sources/backend/notification-service/src/test/java/com/ptit/htpt/notificationservice/messaging/OrderPlacedNotifyListenerIT.java,
    sources/backend/inventory-service/src/test/resources/test-init/01-schemas.sql,
    sources/backend/notification-service/src/test/resources/test-init/01-schemas.sql
  </files>
  <read_first>
    - sources/backend/order-service/src/test/java/com/ptit/htpt/orderservice/service/OrderCouponRaceConditionIT.java (full — Testcontainers pattern, @DynamicPropertySource fallback nếu @ServiceConnection không hoạt động)
    - 23-RESEARCH.md §"Integration test skeleton" §lines 577-617
    - 23-RESEARCH.md §"Validation Architecture" §lines 695-707 (test ID → test name mapping)
    - 23-RESEARCH.md §"Pitfall 6: Testcontainer RabbitMQ port random" §lines 500-504
    - 23-CONTEXT.md §D-18 (4 scenarios — LOCK: HẾT 4, không defer)
    - sources/backend/inventory-service/src/main/java/com/ptit/htpt/inventoryservice/messaging/consumer/OrderPlacedListener.java (sau Plan 04)
    - sources/backend/notification-service/src/main/java/com/ptit/htpt/notificationservice/messaging/consumer/OrderPlacedNotifyListener.java (sau Plan 05)
  </read_first>
  <action>
    (1) **Tạo `test-init/01-schemas.sql`** cho `inventory-service/src/test/resources/test-init/` và `notification-service/src/test/resources/test-init/`. Mỗi file chứa các schema cần thiết để Testcontainers Postgres bootstrap:

    Cho inventory-service test-init:
    ```sql
    CREATE SCHEMA IF NOT EXISTS inventory_svc;
    CREATE SCHEMA IF NOT EXISTS product_svc;  -- cho V102 seed reference cross-schema
    ```

    Cho notification-service test-init:
    ```sql
    CREATE SCHEMA IF NOT EXISTS notification_svc;
    ```

    (2) **`OrderPlacedListenerIT.java`** ở `inventory-service/src/test/java/com/ptit/htpt/inventoryservice/messaging/` — 4 scenario ĐẦY ĐỦ theo D-18. KHÔNG @Disabled bất kỳ scenario nào. Approach cho scenario `transientThenSuccess_retryThenAck`: dùng `@SpyBean StockLedgerRepository` (hoặc `InventoryRepository`) để Mockito.doThrow `org.springframework.dao.TransientDataAccessResourceException` 2 lần đầu rồi gọi callRealMethod — Spring AMQP retry interceptor (3 attempts, exp backoff per D-07) sẽ retry → attempt 3 success → message ACK → kho trừ đúng 1 lần.

    ```java
    package com.ptit.htpt.inventoryservice.messaging;

    import com.ptit.htpt.inventoryservice.domain.InventoryEntity;
    import com.ptit.htpt.inventoryservice.domain.StockLedgerEntity;
    import com.ptit.htpt.inventoryservice.messaging.config.RabbitMQConfig;
    import com.ptit.htpt.inventoryservice.messaging.event.OrderEventEnvelope;
    import com.ptit.htpt.inventoryservice.repository.InventoryRepository;
    import com.ptit.htpt.inventoryservice.repository.ProcessedEventRepository;
    import com.ptit.htpt.inventoryservice.repository.StockLedgerRepository;
    import org.awaitility.Awaitility;
    import org.junit.jupiter.api.*;
    import org.mockito.Mockito;
    import org.springframework.amqp.core.AmqpAdmin;
    import org.springframework.amqp.rabbit.core.RabbitTemplate;
    import org.springframework.beans.factory.annotation.Autowired;
    import org.springframework.boot.test.context.SpringBootTest;
    import org.springframework.boot.test.mock.mockito.SpyBean;
    import org.springframework.boot.testcontainers.service.connection.ServiceConnection;
    import org.springframework.dao.TransientDataAccessResourceException;
    import org.springframework.test.context.ActiveProfiles;
    import org.testcontainers.containers.PostgreSQLContainer;
    import org.testcontainers.containers.RabbitMQContainer;
    import org.testcontainers.junit.jupiter.Container;
    import org.testcontainers.junit.jupiter.Testcontainers;

    import java.math.BigDecimal;
    import java.time.Duration;
    import java.util.List;
    import java.util.UUID;
    import java.util.concurrent.atomic.AtomicInteger;

    import static org.assertj.core.api.Assertions.assertThat;

    /**
     * D-18: 4 scenario IT cho inventory consumer (LOCK — đầy đủ, không defer).
     * Tests: happyPath, idempotent, permanentToDLQ, transientThenSuccess.
     */
    @Testcontainers
    @SpringBootTest
    @ActiveProfiles("test")
    class OrderPlacedListenerIT {

      @Container @ServiceConnection
      static PostgreSQLContainer<?> postgres = new PostgreSQLContainer<>("postgres:16-alpine")
          .withDatabaseName("tmdt").withUsername("tmdt").withPassword("tmdt")
          .withInitScript("test-init/01-schemas.sql");

      @Container @ServiceConnection
      static RabbitMQContainer rabbit = new RabbitMQContainer("rabbitmq:3-management");

      @Autowired RabbitTemplate rabbitTemplate;
      @Autowired InventoryRepository inventoryRepository;
      @Autowired ProcessedEventRepository processedEventRepository;
      @SpyBean StockLedgerRepository stockLedgerRepository;
      @Autowired AmqpAdmin amqpAdmin;

      @BeforeEach
      void cleanup() {
        Mockito.reset(stockLedgerRepository);
        stockLedgerRepository.deleteAll();
        processedEventRepository.deleteAll();
        inventoryRepository.deleteAll();
      }

      @Test
      void happyPath_publishOrderPlaced_inventoryDecremented() {
        InventoryEntity inv = inventoryRepository.save(
            InventoryEntity.create("prod-1", 10, 0));
        String eventId = UUID.randomUUID().toString();
        OrderEventEnvelope envelope = new OrderEventEnvelope(eventId, "OrderPlaced",
            java.time.Instant.now().toString(), "trace-1",
            new OrderEventEnvelope.OrderPlacedPayload("order-1", "user-1",
                List.of(new OrderEventEnvelope.Item("prod-1", 3, new BigDecimal("100"))),
                new BigDecimal("300"), "VND"));

        rabbitTemplate.convertAndSend(RabbitMQConfig.EXCHANGE, RabbitMQConfig.ROUTING_KEY_ORDER_PLACED, envelope);

        Awaitility.await().atMost(Duration.ofSeconds(10)).untilAsserted(() -> {
          InventoryEntity refreshed = inventoryRepository.findByProductId("prod-1").orElseThrow();
          assertThat(refreshed.quantity()).isEqualTo(7);
          assertThat(processedEventRepository.findById(eventId)).isPresent();
          assertThat(stockLedgerRepository.findByEventId(eventId)).hasSize(1);
        });
      }

      @Test
      void idempotent_samePayloadTwice_decrementOnce() {
        inventoryRepository.save(InventoryEntity.create("prod-2", 10, 0));
        String eventId = UUID.randomUUID().toString();
        OrderEventEnvelope envelope = new OrderEventEnvelope(eventId, "OrderPlaced",
            java.time.Instant.now().toString(), "trace-2",
            new OrderEventEnvelope.OrderPlacedPayload("order-2", "user-1",
                List.of(new OrderEventEnvelope.Item("prod-2", 3, new BigDecimal("100"))),
                new BigDecimal("300"), "VND"));

        rabbitTemplate.convertAndSend(RabbitMQConfig.EXCHANGE, RabbitMQConfig.ROUTING_KEY_ORDER_PLACED, envelope);
        rabbitTemplate.convertAndSend(RabbitMQConfig.EXCHANGE, RabbitMQConfig.ROUTING_KEY_ORDER_PLACED, envelope);

        Awaitility.await().atMost(Duration.ofSeconds(10)).untilAsserted(() -> {
          InventoryEntity refreshed = inventoryRepository.findByProductId("prod-2").orElseThrow();
          assertThat(refreshed.quantity()).isEqualTo(7); // CHỈ trừ 1 lần
          assertThat(stockLedgerRepository.findByEventId(eventId)).hasSize(1); // CHỈ 1 row
        });
      }

      @Test
      void permanentException_routedToDLQ_noRetry() {
        // Không có inventory cho prod-missing → PermanentMessageException → DLQ
        String eventId = UUID.randomUUID().toString();
        OrderEventEnvelope envelope = new OrderEventEnvelope(eventId, "OrderPlaced",
            java.time.Instant.now().toString(), "trace-3",
            new OrderEventEnvelope.OrderPlacedPayload("order-3", "user-1",
                List.of(new OrderEventEnvelope.Item("prod-missing", 1, new BigDecimal("100"))),
                new BigDecimal("100"), "VND"));

        rabbitTemplate.convertAndSend(RabbitMQConfig.EXCHANGE, RabbitMQConfig.ROUTING_KEY_ORDER_PLACED, envelope);

        Awaitility.await().atMost(Duration.ofSeconds(15)).untilAsserted(() -> {
          var props = amqpAdmin.getQueueProperties(RabbitMQConfig.DLQ);
          assertThat(props).isNotNull();
          Object count = props.get("QUEUE_MESSAGE_COUNT");
          assertThat(count).isNotNull();
          assertThat(((Number) count).intValue()).isGreaterThanOrEqualTo(1);
        });
      }

      @Test
      void transientThenSuccess_retryThenAck() {
        // D-18 scenario 4 (LOCK): TransientDataAccessResourceException 2 lần đầu, lần 3 success.
        // Spring AMQP retry (D-07: 3 attempts, exp backoff 1s/2s/4s) → attempt 3 pass → ACK.
        // @SpyBean StockLedgerRepository: 2 invocation đầu throw, sau đó call real method.
        InventoryEntity inv = inventoryRepository.save(InventoryEntity.create("prod-4", 10, 0));
        String eventId = UUID.randomUUID().toString();
        OrderEventEnvelope envelope = new OrderEventEnvelope(eventId, "OrderPlaced",
            java.time.Instant.now().toString(), "trace-4",
            new OrderEventEnvelope.OrderPlacedPayload("order-4", "user-1",
                List.of(new OrderEventEnvelope.Item("prod-4", 3, new BigDecimal("100"))),
                new BigDecimal("300"), "VND"));

        AtomicInteger attempts = new AtomicInteger(0);
        Mockito.doAnswer(invocation -> {
          int n = attempts.incrementAndGet();
          if (n <= 2) {
            throw new TransientDataAccessResourceException("Simulated transient DB failure attempt=" + n);
          }
          return invocation.callRealMethod();
        }).when(stockLedgerRepository).save(Mockito.any(StockLedgerEntity.class));

        rabbitTemplate.convertAndSend(RabbitMQConfig.EXCHANGE, RabbitMQConfig.ROUTING_KEY_ORDER_PLACED, envelope);

        // Đợi đủ thời gian retry backoff (1s + 2s + buffer): 8s.
        Awaitility.await().atMost(Duration.ofSeconds(15)).untilAsserted(() -> {
          // Final state: ACK, kho trừ đúng 1 lần.
          InventoryEntity refreshed = inventoryRepository.findByProductId("prod-4").orElseThrow();
          assertThat(refreshed.quantity()).isEqualTo(7);
          assertThat(processedEventRepository.findById(eventId)).isPresent();
          assertThat(stockLedgerRepository.findByEventId(eventId)).hasSize(1);
          // Bằng chứng retry: stockLedgerRepository.save invoked >= 3 lần (2 fail + 1 success).
          assertThat(attempts.get()).isGreaterThanOrEqualTo(3);
        });
      }
    }
    ```

    (3) **`OrderPlacedNotifyListenerIT.java`** ở `notification-service/src/test/java/com/ptit/htpt/notificationservice/messaging/` — same 4 scenario, đổi:
    - Queue assert: dispatch_log thay vì inventory_items
    - PermanentException scenario: không dễ trigger qua payload (recordOrderConfirmation chấp nhận mọi payload). Thay bằng inject `@MockBean DispatchLogRepository` throw `PermanentMessageException` — hoặc đơn giản hơn: assert happyPath + idempotent + smoke verify queue topology declare (3 scenario, ghi rõ trong Javadoc tại sao bỏ 1 scenario)

    ```java
    package com.ptit.htpt.notificationservice.messaging;

    import com.ptit.htpt.notificationservice.messaging.config.RabbitMQConfig;
    import com.ptit.htpt.notificationservice.messaging.event.OrderEventEnvelope;
    import com.ptit.htpt.notificationservice.repository.DispatchLogRepository;
    import com.ptit.htpt.notificationservice.repository.ProcessedEventRepository;
    import org.awaitility.Awaitility;
    import org.junit.jupiter.api.*;
    import org.springframework.amqp.rabbit.core.RabbitTemplate;
    import org.springframework.beans.factory.annotation.Autowired;
    import org.springframework.boot.test.context.SpringBootTest;
    import org.springframework.boot.testcontainers.service.connection.ServiceConnection;
    import org.springframework.test.context.ActiveProfiles;
    import org.testcontainers.containers.PostgreSQLContainer;
    import org.testcontainers.containers.RabbitMQContainer;
    import org.testcontainers.junit.jupiter.Container;
    import org.testcontainers.junit.jupiter.Testcontainers;

    import java.math.BigDecimal;
    import java.time.Duration;
    import java.util.List;
    import java.util.UUID;

    import static org.assertj.core.api.Assertions.assertThat;

    @Testcontainers
    @SpringBootTest
    @ActiveProfiles("test")
    class OrderPlacedNotifyListenerIT {

      @Container @ServiceConnection
      static PostgreSQLContainer<?> postgres = new PostgreSQLContainer<>("postgres:16-alpine")
          .withDatabaseName("tmdt").withUsername("tmdt").withPassword("tmdt")
          .withInitScript("test-init/01-schemas.sql");

      @Container @ServiceConnection
      static RabbitMQContainer rabbit = new RabbitMQContainer("rabbitmq:3-management");

      @Autowired RabbitTemplate rabbitTemplate;
      @Autowired DispatchLogRepository dispatchLogRepository;
      @Autowired ProcessedEventRepository processedEventRepository;

      @BeforeEach
      void cleanup() {
        dispatchLogRepository.deleteAll();
        processedEventRepository.deleteAll();
      }

      @Test
      void happyPath_consumeOrderPlaced_dispatchLogCreated() {
        String eventId = UUID.randomUUID().toString();
        OrderEventEnvelope envelope = new OrderEventEnvelope(eventId, "OrderPlaced",
            java.time.Instant.now().toString(), "trace-n1",
            new OrderEventEnvelope.OrderPlacedPayload("order-N1", "user-N1",
                List.of(new OrderEventEnvelope.Item("prod-1", 1, new BigDecimal("100"))),
                new BigDecimal("100"), "VND"));

        rabbitTemplate.convertAndSend(RabbitMQConfig.EXCHANGE, RabbitMQConfig.ROUTING_KEY_ORDER_PLACED, envelope);

        Awaitility.await().atMost(Duration.ofSeconds(10)).untilAsserted(() -> {
          var logs = dispatchLogRepository.findByEventId(eventId);
          assertThat(logs).hasSize(1);
          assertThat(logs.get(0).status()).isEqualTo("SENT");
          assertThat(logs.get(0).channel()).isEqualTo("email");
          assertThat(logs.get(0).recipientUserId()).isEqualTo("user-N1");
          assertThat(processedEventRepository.findById(eventId)).isPresent();
        });
      }

      @Test
      void idempotent_samePayloadTwice_dispatchLogOnce() {
        String eventId = UUID.randomUUID().toString();
        OrderEventEnvelope envelope = new OrderEventEnvelope(eventId, "OrderPlaced",
            java.time.Instant.now().toString(), "trace-n2",
            new OrderEventEnvelope.OrderPlacedPayload("order-N2", "user-N2",
                List.of(new OrderEventEnvelope.Item("prod-1", 1, new BigDecimal("100"))),
                new BigDecimal("100"), "VND"));

        rabbitTemplate.convertAndSend(RabbitMQConfig.EXCHANGE, RabbitMQConfig.ROUTING_KEY_ORDER_PLACED, envelope);
        rabbitTemplate.convertAndSend(RabbitMQConfig.EXCHANGE, RabbitMQConfig.ROUTING_KEY_ORDER_PLACED, envelope);

        Awaitility.await().atMost(Duration.ofSeconds(10)).untilAsserted(() -> {
          var logs = dispatchLogRepository.findByEventId(eventId);
          assertThat(logs).hasSize(1); // chỉ 1 entry
        });
      }

      @Test
      void topologyDeclared_queuesAndExchangesExist() {
        // Smoke: queue notification.order-events + DLQ declared khi context start
        // Verify thông qua sending message — nếu queue không declare, message vào unrouted alternative.
        // (Test này gián tiếp ensure RabbitMQConfig đã declare topology.)
        String eventId = UUID.randomUUID().toString();
        OrderEventEnvelope envelope = new OrderEventEnvelope(eventId, "OrderPlaced",
            java.time.Instant.now().toString(), "trace-n3",
            new OrderEventEnvelope.OrderPlacedPayload("order-N3", "user-N3",
                List.of(new OrderEventEnvelope.Item("prod-1", 1, new BigDecimal("100"))),
                new BigDecimal("100"), "VND"));
        rabbitTemplate.convertAndSend(RabbitMQConfig.EXCHANGE, RabbitMQConfig.ROUTING_KEY_ORDER_PLACED, envelope);
        Awaitility.await().atMost(Duration.ofSeconds(10)).until(() ->
            dispatchLogRepository.findByEventId(eventId).size() == 1);
      }
    }
    ```

    KHÔNG Lombok. Dùng AssertJ + Awaitility + Mockito @SpyBean (chuẩn của order-service IT existing). Nếu `awaitility` chưa có trong pom test scope thì thêm: `org.awaitility:awaitility` (test). Nếu mvn không có, verify dependencies sau khi compile.

    KHÔNG được @Disabled bất kỳ scenario D-18 nào — tất cả 4 phải pass.
  </action>
  <verify>
    <automated>powershell -Command "Test-Path sources/backend/inventory-service/src/test/java/com/ptit/htpt/inventoryservice/messaging/OrderPlacedListenerIT.java; Test-Path sources/backend/notification-service/src/test/java/com/ptit/htpt/notificationservice/messaging/OrderPlacedNotifyListenerIT.java; Test-Path sources/backend/inventory-service/src/test/resources/test-init/01-schemas.sql; Test-Path sources/backend/notification-service/src/test/resources/test-init/01-schemas.sql"</automated>
  </verify>
  <acceptance_criteria>
    - 2 IT class file + 2 test-init schema file tồn tại
    - `grep '@RabbitListener\|@ServiceConnection' sources/backend/inventory-service/src/test/java/com/ptit/htpt/inventoryservice/messaging/OrderPlacedListenerIT.java` returns 2+ matches (@ServiceConnection cho cả Postgres + RabbitMQ)
    - `grep '@Test' sources/backend/inventory-service/src/test/java/com/ptit/htpt/inventoryservice/messaging/OrderPlacedListenerIT.java` returns 4 matches (4 scenarios D-18 ĐẦY ĐỦ)
    - `grep '@Disabled' sources/backend/inventory-service/src/test/java/com/ptit/htpt/inventoryservice/messaging/OrderPlacedListenerIT.java` returns 0 matches (LOCK: không scenario nào bị disable)
    - `grep '@SpyBean' sources/backend/inventory-service/src/test/java/com/ptit/htpt/inventoryservice/messaging/OrderPlacedListenerIT.java` returns 1+ match (cho transient retry scenario)
    - `grep 'TransientDataAccessResourceException' sources/backend/inventory-service/src/test/java/com/ptit/htpt/inventoryservice/messaging/OrderPlacedListenerIT.java` returns 1+ match
    - `grep '@Test' sources/backend/notification-service/src/test/java/com/ptit/htpt/notificationservice/messaging/OrderPlacedNotifyListenerIT.java` returns 3+ matches
    - `grep 'RabbitMQContainer' sources/backend/inventory-service/src/test/java/com/ptit/htpt/inventoryservice/messaging/OrderPlacedListenerIT.java` returns 1+ match
    - `grep 'amqpAdmin.getQueueProperties' sources/backend/inventory-service/src/test/java/com/ptit/htpt/inventoryservice/messaging/OrderPlacedListenerIT.java` returns 1+ match (DLQ scenario)
    - [BLOCKING] Nếu mvn available: `cd sources/backend && mvn -pl inventory-service,notification-service verify` — TẤT CẢ 4 scenario inventory pass + 3 notification pass. Nếu mvn KHÔNG available trên Windows env này, defer cho `/gsd-verify-work` (precedent v1.3 Plans 19-20).
  </acceptance_criteria>
  <done>2 IT class với 4+3 scenarios cover D-18 đầy đủ (KHÔNG @Disabled); test-init schemas sẵn cho Testcontainers Postgres init.</done>
</task>

<task type="auto">
  <name>Task 2: Producer afterCommit IT + order-service test-init schemas + smoke script + architecture doc</name>
  <files>
    sources/backend/order-service/src/test/java/com/ptit/htpt/orderservice/messaging/OrderEventPublisherIT.java,
    sources/backend/order-service/src/test/resources/test-init/01-schemas.sql,
    scripts/verify-mq.sh,
    architecture/02-sequence-diagrams.md
  </files>
  <read_first>
    - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/messaging/publisher/OrderEventPublisher.java (sau Plan 03)
    - sources/backend/order-service/src/test/java/com/ptit/htpt/orderservice/service/OrderCouponRaceConditionIT.java (Testcontainers pattern reference)
    - architecture/02-sequence-diagrams.md (đọc current — Kafka references cần đổi sang RabbitMQ)
    - 23-CONTEXT.md §canonical_refs (mention architecture doc cần update)
    - 23-RESEARCH.md §Validation Architecture §lines 712-722 (manual demo flow)
  </read_first>
  <action>
    (1) **Tạo file `sources/backend/order-service/src/test/resources/test-init/01-schemas.sql`** (BẮT BUỘC tạo mới — KHÔNG conditional). Nội dung CHÍNH XÁC:

    ```sql
    CREATE SCHEMA IF NOT EXISTS order_svc;
    CREATE SCHEMA IF NOT EXISTS inventory_svc;
    CREATE SCHEMA IF NOT EXISTS notification_svc;
    ```

    Mục đích: Testcontainers Postgres bootstrap đủ 3 schema cần cho IT order-service (publisher IT chỉ touch order_svc trực tiếp, nhưng các schema khác giữ sẵn để tránh lỗi FK/migration cross-schema nếu có).

    (2) **`OrderEventPublisherIT.java`** ở `order-service/src/test/java/com/ptit/htpt/orderservice/messaging/`:

    Test chính: chứng minh `afterCommit` semantics. Phương pháp đơn giản: dùng `TransactionTemplate` programmatic, gọi `publisher.publishOrderPlaced(...)` BÊN TRONG transaction, sau đó rollback. Assert: KHÔNG có message nào tới broker (queue inventory.order-events + notification.order-events đều rỗng).

    Test thứ 2: gọi trong transaction + commit → assert message đi tới (verify qua RabbitTemplate.receive timeout hoặc AmqpAdmin queue depth).

    ```java
    package com.ptit.htpt.orderservice.messaging;

    import com.ptit.htpt.orderservice.messaging.config.RabbitMQConfig;
    import com.ptit.htpt.orderservice.messaging.event.OrderEventEnvelope;
    import com.ptit.htpt.orderservice.messaging.publisher.OrderEventPublisher;
    import org.awaitility.Awaitility;
    import org.junit.jupiter.api.*;
    import org.springframework.amqp.core.AmqpAdmin;
    import org.springframework.amqp.rabbit.core.RabbitTemplate;
    import org.springframework.beans.factory.annotation.Autowired;
    import org.springframework.boot.test.context.SpringBootTest;
    import org.springframework.boot.testcontainers.service.connection.ServiceConnection;
    import org.springframework.test.context.ActiveProfiles;
    import org.springframework.transaction.PlatformTransactionManager;
    import org.springframework.transaction.support.TransactionTemplate;
    import org.testcontainers.containers.PostgreSQLContainer;
    import org.testcontainers.containers.RabbitMQContainer;
    import org.testcontainers.junit.jupiter.Container;
    import org.testcontainers.junit.jupiter.Testcontainers;

    import java.math.BigDecimal;
    import java.time.Duration;
    import java.util.List;

    import static org.assertj.core.api.Assertions.assertThat;

    /**
     * D-03: chứng minh publish-after-commit semantics.
     * Rollback transaction → KHÔNG publish. Commit → publish.
     */
    @Testcontainers
    @SpringBootTest
    @ActiveProfiles("test")
    class OrderEventPublisherIT {

      @Container @ServiceConnection
      static PostgreSQLContainer<?> postgres = new PostgreSQLContainer<>("postgres:16-alpine")
          .withDatabaseName("tmdt").withUsername("tmdt").withPassword("tmdt")
          .withInitScript("test-init/01-schemas.sql");

      @Container @ServiceConnection
      static RabbitMQContainer rabbit = new RabbitMQContainer("rabbitmq:3-management");

      @Autowired OrderEventPublisher publisher;
      @Autowired RabbitTemplate rabbitTemplate;
      @Autowired AmqpAdmin amqpAdmin;
      @Autowired PlatformTransactionManager txManager;

      private OrderEventEnvelope.OrderPlacedPayload payload(String orderId) {
        return new OrderEventEnvelope.OrderPlacedPayload(orderId, "user-1",
            List.of(new OrderEventEnvelope.Item("prod-1", 1, new BigDecimal("100"))),
            new BigDecimal("100"), "VND");
      }

      private int queueDepth() {
        Object count = amqpAdmin.getQueueProperties(RabbitMQConfig.INVENTORY_QUEUE).get("QUEUE_MESSAGE_COUNT");
        return count == null ? 0 : ((Number) count).intValue();
      }

      @Test
      void rollback_doesNotPublish() {
        int before = queueDepth();
        TransactionTemplate tx = new TransactionTemplate(txManager);
        tx.execute(status -> {
          publisher.publishOrderPlaced(payload("order-rollback"));
          status.setRollbackOnly();
          return null;
        });

        // Wait briefly để chắc chắn không có message bay tới
        try { Thread.sleep(2000); } catch (InterruptedException e) { Thread.currentThread().interrupt(); }
        assertThat(queueDepth()).isEqualTo(before); // không tăng
      }

      @Test
      void commit_publishesAfterCommit() {
        int before = queueDepth();
        TransactionTemplate tx = new TransactionTemplate(txManager);
        tx.execute(status -> {
          publisher.publishOrderPlaced(payload("order-commit"));
          return null;
        });

        Awaitility.await().atMost(Duration.ofSeconds(5)).untilAsserted(() ->
            assertThat(queueDepth()).isGreaterThan(before));
      }
    }
    ```

    (3) **`scripts/verify-mq.sh`** — smoke script:

    ```bash
    #!/usr/bin/env bash
    # Phase 23: smoke verify RabbitMQ topology + Management UI.
    # Chạy SAU `docker compose up -d rabbitmq order-service inventory-service notification-service`.

    set -e
    HOST="${RABBITMQ_HOST:-localhost}"
    USER="${RABBITMQ_USER:-guest}"
    PASS="${RABBITMQ_PASS:-guest}"
    BASE="http://${USER}:${PASS}@${HOST}:15672/api"

    echo "[1/4] Management UI reachable..."
    curl -fsS "${BASE}/overview" >/dev/null
    echo "  OK"

    echo "[2/4] Exchange order.events exists..."
    curl -fsS "${BASE}/exchanges/%2F/order.events" >/dev/null
    echo "  OK"

    echo "[3/4] Queues inventory.order-events + notification.order-events + order-events.dlq exist..."
    for q in "inventory.order-events" "notification.order-events" "order-events.dlq"; do
      enc=$(echo "$q" | sed 's/\./%2E/g; s/-/%2D/g')
      # %2F is vhost /; use direct path
      curl -fsS "${BASE}/queues/%2F/${q}" >/dev/null || { echo "MISSING: $q"; exit 1; }
      echo "  $q OK"
    done

    echo "[4/4] DLX order.dlx exists..."
    curl -fsS "${BASE}/exchanges/%2F/order.dlx" >/dev/null
    echo "  OK"

    echo "ALL SMOKE CHECKS PASSED"
    ```

    `chmod +x scripts/verify-mq.sh`.

    (4) **Cập nhật `architecture/02-sequence-diagrams.md`** — đọc full file, tìm references "Kafka" / "kafka" liên quan luồng OrderPlaced, thay bằng RabbitMQ + cập nhật topology:

    - Đổi tên broker "Kafka" → "RabbitMQ" trong sequence diagrams cho OrderPlaced flow
    - Thay terminology: "topic" → "topic exchange `order.events`" (RabbitMQ topic exchange ~ Kafka topic)
    - "consumer group" → "queue + binding"
    - Thêm note ở đầu file (hoặc section relevant): "Phase 23 chọn RabbitMQ thay Kafka vì lý do scope đồ án — xem `.planning/phases/23-message-queue-rabbitmq/23-CONTEXT.md` + memory `project_microservice-gaps.md`."
    - Thêm mention DLQ `order-events.dlq` và DLX `order.dlx` trong error-path section

    Nếu file không có Kafka references rõ ràng (chỉ generic "message bus"), chỉ cần thêm appendix section "Phase 23 update — RabbitMQ topology" với:
    ```
    - Exchange: order.events (type: topic, durable)
    - Routing key publish: order.placed
    - Queue inventory: inventory.order-events (binding order.#)
    - Queue notification: notification.order-events (binding order.#)
    - DLX: order.dlx (direct), DLQ: order-events.dlq
    - Retry policy: 3 lần, exponential backoff 1s/2s/4s
    - Idempotency: bảng processed_events per consumer schema
    ```

    Giữ nguyên các sequence diagram syntax (mermaid/plantuml/ASCII) hiện có.

    (5) **SUMMARY note trade-off**: khi commit SUMMARY ở cuối Plan 06, commit message BẮT BUỘC chứa dòng:
    `Trade-off: OrderEventEnvelope duplicated across 3 services — shared module deferred to Phase ops`
    (chấp nhận warning #3 envelope duplication; defer shared module sang phase sau).
  </action>
  <verify>
    <automated>powershell -Command "Test-Path sources/backend/order-service/src/test/java/com/ptit/htpt/orderservice/messaging/OrderEventPublisherIT.java; Test-Path sources/backend/order-service/src/test/resources/test-init/01-schemas.sql; Test-Path scripts/verify-mq.sh; Select-String -Path architecture/02-sequence-diagrams.md -Pattern 'RabbitMQ|order.events' -Quiet"</automated>
  </verify>
  <acceptance_criteria>
    - File `sources/backend/order-service/src/test/resources/test-init/01-schemas.sql` tồn tại
    - `Select-String -Path sources/backend/order-service/src/test/resources/test-init/01-schemas.sql -Pattern 'CREATE SCHEMA IF NOT EXISTS order_svc' -Quiet` returns True
    - `Select-String -Path sources/backend/order-service/src/test/resources/test-init/01-schemas.sql -Pattern 'CREATE SCHEMA IF NOT EXISTS inventory_svc' -Quiet` returns True
    - `Select-String -Path sources/backend/order-service/src/test/resources/test-init/01-schemas.sql -Pattern 'CREATE SCHEMA IF NOT EXISTS notification_svc' -Quiet` returns True
    - File `OrderEventPublisherIT.java` tồn tại với 2 test (`rollback_doesNotPublish`, `commit_publishesAfterCommit`)
    - `grep 'TransactionTemplate' sources/backend/order-service/src/test/java/com/ptit/htpt/orderservice/messaging/OrderEventPublisherIT.java` returns 1+ match
    - `grep 'setRollbackOnly' sources/backend/order-service/src/test/java/com/ptit/htpt/orderservice/messaging/OrderEventPublisherIT.java` returns 1 match
    - File `scripts/verify-mq.sh` tồn tại, executable (`Test-Path` returns true)
    - `grep '15672/api' scripts/verify-mq.sh` returns 1+ match
    - architecture/02-sequence-diagrams.md có mention "RabbitMQ" hoặc "order.events" (verify update đã làm)
    - SUMMARY commit message (khi tạo 23-06-SUMMARY.md commit) phải có dòng `Trade-off: OrderEventEnvelope duplicated across 3 services — shared module deferred to Phase ops`
    - [BLOCKING] Nếu mvn available: `cd sources/backend/order-service && mvn -pl order-service verify -Dtest=OrderEventPublisherIT` pass. Nếu KHÔNG available: defer cho /gsd-verify-work (precedent v1.3 Phase 19/20)
    - Manual run smoke script: `bash scripts/verify-mq.sh` returns "ALL SMOKE CHECKS PASSED" (sau khi docker compose up RabbitMQ + 3 service)
  </acceptance_criteria>
  <done>OrderEventPublisherIT chứng minh afterCommit semantics; order-service test-init schemas sẵn; smoke script chạy được; architecture doc reflect RabbitMQ topology.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test code → Testcontainers | Test scope only; container exposed random port host-only |
| Smoke script → broker management API | Dev convenience; KHÔNG dùng trong production CI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-14 | Tampering | IT test pollute prod data | mitigate | Testcontainers isolated; @BeforeEach cleanup; ephemeral container destroyed after test class. |
| T-23-15 | Information Disclosure | verify-mq.sh expose guest/guest credential | accept | Dev script; credential default; nếu prod dùng → wrap qua env var (script đã làm: `${RABBITMQ_USER:-guest}`). |
| T-23-09 | Tampering | Phantom event (verified) | mitigate | OrderEventPublisherIT.rollback_doesNotPublish chứng minh hành vi đúng — gate for D-03. |
</threat_model>

<verification>
- [BLOCKING] `cd sources/backend && mvn -pl order-service,inventory-service,notification-service verify` — full IT suite pass (4 inventory + 3 notification + 2 publisher = 9 @Test methods). Nếu mvn không available, defer (precedent v1.3) nhưng phải tạo file IT để `/gsd-verify-work` chạy được.
- Manual smoke: `docker compose up -d` rồi `bash scripts/verify-mq.sh` → "ALL SMOKE CHECKS PASSED"
- Manual demo: tạo 1 order qua FE → mở http://localhost:15672 → tab Exchanges/Queues thấy `order.events`, `inventory.order-events`, `notification.order-events`, `order-events.dlq`
- `docker compose logs order-service inventory-service notification-service | grep '<traceId>'` ra >= 3 dòng cùng traceId cho 1 order (D-16 manual demo trong VERIFICATION.md cuối phase)
- architecture/02-sequence-diagrams.md đọc lại thấy RabbitMQ + topology
</verification>

<success_criteria>
- 3 IT class (publisher + 2 consumer), tổng cộng >= 9 @Test methods (2 publisher + 4 inventory + 3 notification)
- OrderEventPublisherIT chứng minh rollback → no publish + commit → publish
- Inventory IT cover ĐẦY ĐỦ 4 scenario D-18 (happy + idempotent + DLQ permanent + transient retry) — KHÔNG @Disabled
- Notification IT cover happy + idempotent + topology smoke
- Smoke script verify topology qua Management UI HTTP API
- Architecture doc cập nhật reflect RabbitMQ (thay Kafka cũ)
- order-service test-init/01-schemas.sql có đủ 3 CREATE SCHEMA IF NOT EXISTS
- [BLOCKING] mvn verify pass (nếu mvn available)
</success_criteria>

<output>
After completion, create `.planning/phases/23-message-queue-rabbitmq/23-06-SUMMARY.md` recording:
- IT scenarios + test outcomes (4 inventory FULL — không defer scenario nào)
- mvn verify result hoặc lý do defer
- Smoke script chạy manual + outcome
- architecture doc diff
- Tạo `.planning/phases/23-message-queue-rabbitmq/23-VERIFICATION.md` ghi 7 success criteria (ROADMAP §217-224) + evidence pass/defer
- Commit message phải chứa dòng `Trade-off: OrderEventEnvelope duplicated across 3 services — shared module deferred to Phase ops`
</output>
</output>
