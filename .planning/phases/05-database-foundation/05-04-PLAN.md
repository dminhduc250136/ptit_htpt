---
phase: 05-database-foundation
plan: 04
type: execute
wave: 3
depends_on: [02]
files_modified:
  - sources/backend/user-service/pom.xml
  - sources/backend/user-service/src/main/resources/application.yml
  - sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/domain/UserEntity.java
  - sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/domain/UserDto.java
  - sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/domain/UserMapper.java
  - sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/repository/UserRepository.java
  - sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/repository/InMemoryUserRepository.java
  - sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/service/UserCrudService.java
  - sources/backend/user-service/src/main/resources/db/migration/V1__init_schema.sql
  - sources/backend/user-service/src/main/resources/db/seed-dev/V2__seed_dev_data.sql
  - sources/backend/user-service/src/test/java/com/ptit/htpt/userservice/repository/UserRepositoryJpaTest.java
autonomous: true
requirements: [DB-02, DB-03, DB-04, DB-05]
must_haves:
  truths:
    - "user-service start green với JPA + Flyway 10 + Postgres 16 (schema `user_svc`)"
    - "Flyway V1 tạo bảng `user_svc.users`; ddl-auto=validate pass"
    - "Profile `dev` V2 seed populate 1 admin (`admin/admin123` BCrypt verified Plan 01) + 1 demo_user"
    - "JPA `findAll/findById/save` + `findByUsername` hoạt động qua `UserRepository extends JpaRepository<UserEntity, String>`"
    - "Soft-delete `@SQLRestriction(\"deleted = false\")` filter records đã xóa"
    - "Controller layer trả `UserDto` (không chứa `passwordHash`, không chứa `deleted`) — entity KHÔNG leak"
  artifacts:
    - path: "sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/domain/UserEntity.java"
      provides: "JPA @Entity (rename từ UserProfile cho symmetry — PATTERNS cross-cutting note #1)"
      contains: "@Entity"
    - path: "sources/backend/user-service/src/main/resources/db/migration/V1__init_schema.sql"
      provides: "DDL `user_svc.users`"
      contains: "CREATE TABLE user_svc.users"
    - path: "sources/backend/user-service/src/main/resources/db/seed-dev/V2__seed_dev_data.sql"
      provides: "Admin + demo user seed với BCrypt hash verified"
      contains: "$2a$10$TMH2spmmPRD90vJz8w5yz"
  key_links:
    - from: "UserEntity (@Entity)"
      to: "users table (user_svc schema)"
      via: "Hibernate JPA"
      pattern: "@Table\\(name = \"users\", schema = \"user_svc\"\\)"
    - from: "UserCrudService"
      to: "UserRepository (JpaRepository)"
      via: "constructor injection"
      pattern: "UserRepository userRepo"
---

<objective>
Wave 3 (mirror canonical 05-03): refactor user-service từ in-memory → JPA + Flyway + Postgres trên schema `user_svc`. PATTERNS §C/D/E/F/G applied 1:1, đặc thù: rename `UserProfile` → `UserEntity` (cross-cutting note #1) + dùng BCrypt hash đã verified Plan 01.

Purpose: DB-02..05 cho user-service. Phase 6 (Auth) depend on bảng `users` + admin row sẵn sàng.

Output: user-service start green; Phase 6 có thể test login với `admin/admin123` không cần seed thêm.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/05-database-foundation/05-CONTEXT.md
@.planning/phases/05-database-foundation/05-RESEARCH.md
@.planning/phases/05-database-foundation/05-PATTERNS.md
@.planning/phases/05-database-foundation/baseline/openapi-user-service.json
@.planning/phases/05-database-foundation/baseline/bcrypt-hash-verified.txt

<interfaces>
Entity rename: UserProfile (record cũ) -> UserEntity (mutable @Entity class). DTO mới: UserDto (drop `passwordHash`, drop `deleted`).
Schema: user_svc. Table: users.
V1 columns (RESEARCH lines 345-358): id VARCHAR(36) PK, username VARCHAR(80) UNIQUE NOT NULL, email VARCHAR(200) UNIQUE NOT NULL, password_hash VARCHAR(120) NOT NULL, roles VARCHAR(200) NOT NULL DEFAULT 'USER', deleted BOOLEAN, created_at, updated_at.
JPA repository extras: findByUsername(String), findByEmail(String) — Phase 6 sẽ dùng.
</interfaces>

<cross_service_ids>
Canonical IDs dùng XUYÊN suốt 5 services. KHÔNG đổi literal:
  admin_user_id = 00000000-0000-0000-0000-000000000001  (V2 seed Plan 04 này)
  demo_user_id  = 00000000-0000-0000-0000-000000000002  (V2 seed Plan 04 này; Plan 05 orders.user_id REFERENCES literal này)
  product IDs   = prod-001 .. prod-010                  (Plan 03 + Plan 07)
Cross-service consistency = Plan 08 Task 8.1 sẽ assert NOT EXISTS subquery cho orders.user_id orphan check.
</cross_service_ids>
</context>

<tasks>

<task type="auto">
  <name>Task 4.1: Add deps + application.yml datasource/JPA/Flyway (schema=user_svc)</name>
  <files>sources/backend/user-service/pom.xml, sources/backend/user-service/src/main/resources/application.yml</files>
  <read_first>
    - sources/backend/user-service/pom.xml
    - sources/backend/user-service/src/main/resources/application.yml
    - .planning/phases/05-database-foundation/05-PATTERNS.md §A (pom deps), §B (yml pattern)
    - .planning/phases/05-database-foundation/05-03-PLAN.md (canonical Task 3.1 reference)
  </read_first>
  <action>
    Apply 05-03 Task 3.1 pattern, substitute schema name `product_svc` → `user_svc`. Specifically:

    **(a)** Add 6 deps vào pom.xml (giống 05-03 Task 3.1).

    **(b)** application.yml block default:
    ```yaml
    spring:
      application:
        name: user-service
      datasource:
        url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:tmdt}?currentSchema=user_svc
        username: ${DB_USER:tmdt}
        password: ${DB_PASSWORD:tmdt}
        driver-class-name: org.postgresql.Driver
      jpa:
        hibernate:
          ddl-auto: validate
        properties:
          hibernate:
            default_schema: user_svc
            format_sql: true
        open-in-view: false
      flyway:
        enabled: true
        schemas: user_svc
        default-schema: user_svc
        baseline-on-migrate: false
        locations: classpath:db/migration
    ```

    Profile dev block (cuối file):
    ```yaml
    ---
    spring:
      config:
        activate:
          on-profile: dev
      flyway:
        locations: classpath:db/migration,classpath:db/seed-dev
    ```

    Giữ nguyên block `server`, `management`, `springdoc`.
  </action>
  <verify>
    <automated>cd sources/backend/user-service && mvn -q -DskipTests compile && grep -q "currentSchema=user_svc" src/main/resources/application.yml && grep -q "default_schema: user_svc" src/main/resources/application.yml && grep -q "flyway-database-postgresql" pom.xml</automated>
  </verify>
  <acceptance_criteria>
    - Compile xanh
    - application.yml có `currentSchema=user_svc`, `default_schema: user_svc`, `default-schema: user_svc`
    - pom.xml có cả `flyway-core` và `flyway-database-postgresql`
    - Document separator + profile dev block tồn tại
  </acceptance_criteria>
  <done>user-service compile xanh với JPA + Flyway deps + datasource config.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4.2: Refactor UserProfile → UserEntity; tạo UserDto + UserMapper + UserRepository; cập nhật UserCrudService; xóa InMemoryUserRepository</name>
  <files>
    sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/domain/UserEntity.java,
    sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/domain/UserDto.java,
    sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/domain/UserMapper.java,
    sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/repository/UserRepository.java,
    sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/service/UserCrudService.java,
    sources/backend/user-service/src/test/java/com/ptit/htpt/userservice/repository/UserRepositoryJpaTest.java
  </files>
  <read_first>
    - sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/domain/UserProfile.java (hoặc UserProfileEntity.java — record cũ)
    - sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/repository/InMemoryUserRepository.java (toàn bộ)
    - sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/service/UserCrudService.java (toàn bộ)
    - .planning/phases/05-database-foundation/05-PATTERNS.md §C (entity), §D (DTO), §E (Mapper), §F (Repository), §G (Service)
    - .planning/phases/05-database-foundation/05-PATTERNS.md cross-cutting note #1 (rename UserProfile → UserEntity)
    - .planning/phases/05-database-foundation/05-03-PLAN.md Task 3.2 (canonical pattern reference)
  </read_first>
  <behavior>
    - Test 1: save UserEntity rồi findById trả entity với cùng id, username, email
    - Test 2: `findByUsername("admin")` sau khi save user với username="admin" trả Optional có entity; `findByUsername("missing")` trả empty
    - Test 3: `findByEmail("...")` tương tự
    - Test 4: soft-delete: save 2 users, gọi `softDelete()` + `save()` cho 1, `findAll()` chỉ trả 1 còn lại
    - Test 5: UserDto KHÔNG có field `passwordHash` và KHÔNG có field `deleted` (compile-time check + reflection nếu cần)
  </behavior>
  <execution_hint>
  Refactor + rename touches ~6-7 files. Executor MAY commit incrementally (UserEntity rename + DTO + Mapper trong 1 commit; Repository + service swap trong commit khác; test + delete InMemory cuối). Whole task = 1 logical unit, multiple commits OK để diff reviewable. KHÔNG split task.
  </execution_hint>
  <action>
    Apply 05-03 Task 3.2 pattern với 3 đặc thù:

    **(a) Rename:** Class entity hiện là `UserProfile` → đổi thành `UserEntity`. Fields theo RESEARCH §V1 user-service:
    ```
    String id (length=36, PK)
    String username (length=80, unique, not null)
    String email (length=200, unique, not null)
    String passwordHash (length=120, not null) — column `password_hash`
    String roles (length=200, not null, default "USER")
    boolean deleted (default false)
    Instant createdAt, updatedAt
    ```

    Annotations:
    ```java
    @Entity
    @Table(name = "users", schema = "user_svc")
    @SQLRestriction("deleted = false")
    @SQLDelete(sql = "UPDATE user_svc.users SET deleted = true, updated_at = NOW() WHERE id = ?")
    public class UserEntity { ... }
    ```

    Accessor: giữ record-style (`username()`, `email()`, `passwordHash()`, `roles()`, `deleted()`...).

    **(b) UserDto** record — DROP `passwordHash` và `deleted`:
    ```java
    public record UserDto(
        String id, String username, String email, String roles,
        Instant createdAt, Instant updatedAt
    ) {}
    ```

    **(c) UserMapper**:
    ```java
    public final class UserMapper {
      private UserMapper() {}
      public static UserDto toDto(UserEntity e) {
        return new UserDto(e.id(), e.username(), e.email(), e.roles(), e.createdAt(), e.updatedAt());
      }
    }
    ```

    **(d) UserRepository** interface:
    ```java
    public interface UserRepository extends JpaRepository<UserEntity, String> {
      Optional<UserEntity> findByUsername(String username);
      Optional<UserEntity> findByEmail(String email);
    }
    ```

    **(e) UserCrudService:** Replace inject `InMemoryUserRepository` → `UserRepository`. Apply method mapping pattern (`findAllUsers()` → `findAll()`, etc. — match PATTERNS §G). Insert `UserMapper.toDto(entity)` boundary tại tất cả method trả ra controller. KHÔNG đổi controller signatures + validation records (OpenAPI zero-diff).

    **Find-and-replace check:** Search toàn project user-service cho `UserProfile` → rename `UserEntity` ở mọi reference. Nếu có file `UserProfileEntity.java` riêng, gộp/xoá.

    **(f) DELETE** `InMemoryUserRepository.java`.

    **(g) UserRepositoryJpaTest** — Testcontainers `@DataJpaTest` similar 05-03, schema = `user_svc`. Cover 5 behaviors trên.

    **Critical:** Class `BCryptSeedHashTest.java` từ Plan 01 GIỮ NGUYÊN — KHÔNG xóa.
  </action>
  <verify>
    <automated>cd sources/backend/user-service && mvn -q -DskipTests compile && test ! -f src/main/java/com/ptit/htpt/userservice/repository/InMemoryUserRepository.java && grep -q "@Entity" src/main/java/com/ptit/htpt/userservice/domain/UserEntity.java && grep -q "extends JpaRepository" src/main/java/com/ptit/htpt/userservice/repository/UserRepository.java && grep -q "findByUsername" src/main/java/com/ptit/htpt/userservice/repository/UserRepository.java && ! grep -q "passwordHash\|deleted" src/main/java/com/ptit/htpt/userservice/domain/UserDto.java && mvn -q test -Dtest=UserRepositoryJpaTest</automated>
  </verify>
  <acceptance_criteria>
    - File `UserEntity.java` chứa `@Entity`, `@Table(name = "users", schema = "user_svc")`, `@SQLRestriction`, `@SQLDelete`
    - File `UserDto.java` là record, KHÔNG chứa `passwordHash` hay `deleted`
    - File `UserRepository.java` extends `JpaRepository<UserEntity, String>` với `findByUsername` + `findByEmail`
    - File `InMemoryUserRepository.java` không tồn tại
    - `UserCrudService.java` không reference `InMemoryUserRepository`
    - `mvn compile` xanh
    - `UserRepositoryJpaTest` 5 test pass green
    - `BCryptSeedHashTest` (Plan 01) vẫn pass green
  </acceptance_criteria>
  <done>user-service JPA layer compile + test xanh; UserProfile rename xong.</done>
</task>

<task type="auto">
  <name>Task 4.3: Tạo Flyway V1 + V2 migrations cho user-service</name>
  <files>
    sources/backend/user-service/src/main/resources/db/migration/V1__init_schema.sql,
    sources/backend/user-service/src/main/resources/db/seed-dev/V2__seed_dev_data.sql
  </files>
  <read_first>
    - .planning/phases/05-database-foundation/05-RESEARCH.md §Migration SQL Outline §user-service V1 + V2
    - .planning/phases/05-database-foundation/baseline/bcrypt-hash-verified.txt (Plan 01 evidence — bằng chứng hash dùng được)
    - sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/domain/UserEntity.java (verify column names match)
  </read_first>
  <action>
    **(a)** `V1__init_schema.sql`:
    ```sql
    CREATE TABLE user_svc.users (
      id VARCHAR(36) PRIMARY KEY,
      username VARCHAR(80) NOT NULL,
      email VARCHAR(200) NOT NULL,
      password_hash VARCHAR(120) NOT NULL,
      roles VARCHAR(200) NOT NULL DEFAULT 'USER',
      deleted BOOLEAN NOT NULL DEFAULT FALSE,
      created_at TIMESTAMP WITH TIME ZONE NOT NULL,
      updated_at TIMESTAMP WITH TIME ZONE NOT NULL,
      CONSTRAINT uq_users_username UNIQUE (username),
      CONSTRAINT uq_users_email UNIQUE (email)
    );
    ```

    **(b)** `V2__seed_dev_data.sql`:
    ```sql
    INSERT INTO user_svc.users (id, username, email, password_hash, roles, deleted, created_at, updated_at)
    VALUES
      ('00000000-0000-0000-0000-000000000001', 'admin',     'admin@tmdt.local',
       '$2a$10$TMH2spmmPRD90vJz8w5yz.G0o4AR/Hio2RU1yBwjjT1ClTLqF5lFu', 'ADMIN', FALSE, NOW(), NOW()),
      ('00000000-0000-0000-0000-000000000002', 'demo_user', 'demo@tmdt.local',
       '$2a$10$TMH2spmmPRD90vJz8w5yz.G0o4AR/Hio2RU1yBwjjT1ClTLqF5lFu', 'USER',  FALSE, NOW(), NOW());
    ```

    Hash literal phải khớp chính xác `bcrypt-hash-verified.txt` từ Plan 01 — KHÔNG đổi hash bừa bãi (sẽ break Phase 6 admin login).

    **(c) End-to-end smoke verify** (mirror Plan 03 Task 3.3 (c)) — đảm bảo postgres container đang running từ Wave 2:
    ```bash
    cd sources/backend/user-service
    SPRING_PROFILES_ACTIVE=dev DB_HOST=localhost DB_PORT=5432 DB_NAME=tmdt DB_USER=tmdt DB_PASSWORD=tmdt       mvn -q spring-boot:run -Dspring-boot.run.profiles=dev &
    APP_PID=$!
    sleep 25  # đợi Flyway apply V1+V2 + Hibernate validate
    kill $APP_PID
    ```

    Verify trực tiếp DB (row counts + Flyway history applied):
    ```bash
    docker compose exec postgres psql -U tmdt -d tmdt -c "SELECT COUNT(*) FROM user_svc.users WHERE deleted=false;"
    # Expect: 2 (admin + demo_user)
    docker compose exec postgres psql -U tmdt -d tmdt -c "SELECT username, roles FROM user_svc.users ORDER BY username;"
    # Expect: admin/ADMIN, demo_user/USER
    docker compose exec postgres psql -U tmdt -d tmdt -c "SELECT version, success FROM user_svc.flyway_schema_history;"
    # Expect: rows for V1 + V2, success=true
    ```

    Catch V1+V2 failures sớm (thay vì chờ Plan 08).
  </action>
  <verify>
    <automated>test -f sources/backend/user-service/src/main/resources/db/migration/V1__init_schema.sql && test -f sources/backend/user-service/src/main/resources/db/seed-dev/V2__seed_dev_data.sql && grep -q "CREATE TABLE user_svc.users" sources/backend/user-service/src/main/resources/db/migration/V1__init_schema.sql && grep -q "uq_users_username" sources/backend/user-service/src/main/resources/db/migration/V1__init_schema.sql && grep -q "\$2a\$10\$TMH2spmmPRD90vJz8w5yz" sources/backend/user-service/src/main/resources/db/seed-dev/V2__seed_dev_data.sql</automated>
  </verify>
  <acceptance_criteria>
    - V1 file tồn tại; chứa `CREATE TABLE user_svc.users`, UNIQUE constraints cho `username` + `email`, password_hash NOT NULL VARCHAR(120)
    - V2 file tồn tại; chứa 2 INSERT rows: admin (roles=ADMIN) + demo_user (roles=USER)
    - V2 password_hash literal khớp 100% hash đã verify trong Plan 01
    - V2 IDs dùng UUID-format string (`00000000-0000-0000-0000-000000000001`, `...002`) — khớp `<cross_service_ids>` literal
    - Smoke run `mvn spring-boot:run -Dspring-boot.run.profiles=dev` lên green; `SELECT COUNT(*) FROM user_svc.users WHERE deleted=false` = 2; `user_svc.flyway_schema_history` chứa V1 + V2 success=true
  </acceptance_criteria>
  <done>V1 + V2 committed; admin row sẵn sàng cho Phase 6 login test.</done>
</task>

</tasks>

<verification>
- `mvn -pl sources/backend/user-service test` xanh
- `mvn spring-boot:run` profile dev → 2 users seeded vào `user_svc.users`
- BCrypt hash trong V2 SQL = hash verified Plan 01
</verification>

<success_criteria>
1. user-service compile + start green với Postgres backend
2. V1 schema + V2 seed (2 users) applied
3. UserRepositoryJpaTest pass green
4. UserProfile renamed → UserEntity throughout
</success_criteria>

<output>
Create `.planning/phases/05-database-foundation/05-04-SUMMARY.md` ghi rõ rename UserProfile→UserEntity scope, hash literal verified, Phase 6 dependency satisfied.
</output>
