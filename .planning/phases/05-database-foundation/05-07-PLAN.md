---
phase: 05-database-foundation
plan: 07
type: execute
wave: 3
depends_on: [02]
files_modified:
  - sources/backend/inventory-service/pom.xml
  - sources/backend/inventory-service/src/main/resources/application.yml
  - sources/backend/inventory-service/src/main/java/com/ptit/htpt/inventoryservice/domain/InventoryEntity.java
  - sources/backend/inventory-service/src/main/java/com/ptit/htpt/inventoryservice/domain/InventoryDto.java
  - sources/backend/inventory-service/src/main/java/com/ptit/htpt/inventoryservice/domain/InventoryMapper.java
  - sources/backend/inventory-service/src/main/java/com/ptit/htpt/inventoryservice/repository/InventoryRepository.java
  - sources/backend/inventory-service/src/main/java/com/ptit/htpt/inventoryservice/repository/InMemoryInventoryRepository.java
  - sources/backend/inventory-service/src/main/java/com/ptit/htpt/inventoryservice/service/InventoryCrudService.java
  - sources/backend/inventory-service/src/main/resources/db/migration/V1__init_schema.sql
  - sources/backend/inventory-service/src/main/resources/db/seed-dev/V2__seed_dev_data.sql
  - sources/backend/inventory-service/src/test/java/com/ptit/htpt/inventoryservice/repository/InventoryRepositoryJpaTest.java
autonomous: true
requirements: [DB-02, DB-03, DB-04, DB-05]
must_haves:
  truths:
    - "inventory-service start green với JPA + Flyway 10 + Postgres 16 (schema `inventory_svc`)"
    - "Flyway V1 tạo bảng `inventory_svc.inventory_items`; ddl-auto=validate pass"
    - "Profile `dev` V2 seed populate 10 inventory rows tương ứng `prod-001`..`prod-010` (cross-service IDs khớp Plan 03 product seed)"
    - "JPA `findAll/findById/save` + `findByProductId` qua `InventoryRepository extends JpaRepository<InventoryEntity, String>`"
    - "Rename: class hiện tại `InventoryItem` đổi tên thành `InventoryEntity` cho symmetry (PATTERNS cross-cutting note #1)"
  artifacts:
    - path: "sources/backend/inventory-service/src/main/java/com/ptit/htpt/inventoryservice/domain/InventoryEntity.java"
      provides: "JPA @Entity (renamed từ InventoryItem)"
      contains: "@Entity"
    - path: "sources/backend/inventory-service/src/main/resources/db/migration/V1__init_schema.sql"
      provides: "DDL `inventory_svc.inventory_items` với UNIQUE constraint product_id"
      contains: "CREATE TABLE inventory_svc.inventory_items"
    - path: "sources/backend/inventory-service/src/main/resources/db/seed-dev/V2__seed_dev_data.sql"
      provides: "10 inventory rows cho prod-001..prod-010"
      contains: "prod-001"
  key_links:
    - from: "InventoryEntity (@Entity)"
      to: "inventory_items table (inventory_svc schema)"
      via: "Hibernate JPA"
      pattern: "@Table\\(name = \"inventory_items\", schema = \"inventory_svc\"\\)"
    - from: "InventoryEntity.productId"
      to: "product_svc.products.id (logical only)"
      via: "service layer validation, no FK cross-schema"
      pattern: "product_id VARCHAR\\(36\\) NOT NULL"
---

<objective>
Wave 3 (mirror canonical 05-03): refactor inventory-service từ in-memory → JPA + Flyway + Postgres trên schema `inventory_svc`. Đặc thù: rename `InventoryItem` → `InventoryEntity` (PATTERNS cross-cutting note #1) + V2 seed product_id phải KHỚP `prod-001`..`prod-010` của Plan 03 (RESEARCH Open Q #4).

**Phase 5 scope-cut:** Phase 8 sẽ extend với reservation flow + stock decrement on order. Phase 5 chỉ ship CRUD basic + seed.

Output: inventory-service start green; 10 inventory rows tương ứng products.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/05-database-foundation/05-CONTEXT.md
@.planning/phases/05-database-foundation/05-RESEARCH.md
@.planning/phases/05-database-foundation/05-PATTERNS.md
@.planning/phases/05-database-foundation/baseline/openapi-inventory-service.json

<interfaces>
Schema: inventory_svc. Table: inventory_items.
V1 columns: id VARCHAR(36) PK, product_id VARCHAR(36) NOT NULL UNIQUE, quantity INT NOT NULL DEFAULT 0, reserved INT NOT NULL DEFAULT 0, created_at, updated_at.
INDEX/CONSTRAINT: uq_inventory_product UNIQUE (product_id).
JPA repository extras: Optional<InventoryEntity> findByProductId(String productId).
Cross-service consistency: V2 seed product_id MUST match `prod-001`..`prod-010` từ Plan 03 product-service V2 seed.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 7.1: Add deps + application.yml (schema=inventory_svc)</name>
  <files>sources/backend/inventory-service/pom.xml, sources/backend/inventory-service/src/main/resources/application.yml</files>
  <read_first>
    - sources/backend/inventory-service/pom.xml
    - sources/backend/inventory-service/src/main/resources/application.yml
    - .planning/phases/05-database-foundation/05-PATTERNS.md §A, §B
    - .planning/phases/05-database-foundation/05-03-PLAN.md Task 3.1 (canonical)
  </read_first>
  <action>
    Apply 05-03 Task 3.1 pattern, schema = `inventory_svc`. Add 6 deps. Insert datasource/jpa/flyway block với:
    - `currentSchema=inventory_svc`
    - `hibernate.default_schema: inventory_svc`
    - `flyway.schemas: inventory_svc` + `default-schema: inventory_svc`
    - `baseline-on-migrate: false`, `locations: classpath:db/migration`
    - Doc separator + `on-profile: dev` block với `locations: classpath:db/migration,classpath:db/seed-dev`
  </action>
  <verify>
    <automated>cd sources/backend/inventory-service && mvn -q -DskipTests compile && grep -q "currentSchema=inventory_svc" src/main/resources/application.yml && grep -q "default_schema: inventory_svc" src/main/resources/application.yml && grep -q "flyway-database-postgresql" pom.xml</automated>
  </verify>
  <acceptance_criteria>
    - Compile xanh
    - application.yml có `currentSchema=inventory_svc`, `default_schema: inventory_svc`
    - pom.xml có `flyway-core` + `flyway-database-postgresql` + 2 testcontainers test deps
  </acceptance_criteria>
  <done>inventory-service compile xanh.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 7.2: Rename InventoryItem → InventoryEntity; tạo Dto + Mapper + Repository; cập nhật service; xóa In-memory repo</name>
  <files>
    sources/backend/inventory-service/src/main/java/com/ptit/htpt/inventoryservice/domain/InventoryEntity.java,
    sources/backend/inventory-service/src/main/java/com/ptit/htpt/inventoryservice/domain/InventoryDto.java,
    sources/backend/inventory-service/src/main/java/com/ptit/htpt/inventoryservice/domain/InventoryMapper.java,
    sources/backend/inventory-service/src/main/java/com/ptit/htpt/inventoryservice/repository/InventoryRepository.java,
    sources/backend/inventory-service/src/main/java/com/ptit/htpt/inventoryservice/service/InventoryCrudService.java,
    sources/backend/inventory-service/src/test/java/com/ptit/htpt/inventoryservice/repository/InventoryRepositoryJpaTest.java
  </files>
  <read_first>
    - sources/backend/inventory-service/src/main/java/com/ptit/htpt/inventoryservice/domain/InventoryItem.java (record cũ)
    - sources/backend/inventory-service/src/main/java/com/ptit/htpt/inventoryservice/repository/InMemoryInventoryRepository.java
    - sources/backend/inventory-service/src/main/java/com/ptit/htpt/inventoryservice/service/InventoryCrudService.java
    - .planning/phases/05-database-foundation/05-PATTERNS.md cross-cutting note #1 (rename InventoryItem → InventoryEntity)
    - .planning/phases/05-database-foundation/05-03-PLAN.md Task 3.2 (canonical)
  </read_first>
  <behavior>
    - Test 1: save InventoryEntity → findById trả entity với cùng id, productId, quantity, reserved
    - Test 2: `findByProductId("prod-001")` sau khi save trả Optional có entity
    - Test 3: UNIQUE constraint product_id: save 2 entities với cùng productId → DataIntegrityViolationException
    - Test 4: InventoryDto round-trip: toDto giữ nguyên id, productId, quantity, reserved
  </behavior>
  <action>
    Apply 05-03 Task 3.2 pattern. Đặc thù:

    **(a) Rename:** `InventoryItem` → `InventoryEntity`. Find-and-replace toàn project inventory-service. Nếu có file `InventoryItem.java`, xóa sau khi tạo `InventoryEntity.java`.

    **(b) InventoryEntity** — `@Table(name="inventory_items", schema="inventory_svc")`. Fields:
    ```java
    @Id @Column(length=36) private String id;
    @Column(name="product_id", length=36, nullable=false, unique=true) private String productId;
    @Column(nullable=false) private int quantity;
    @Column(nullable=false) private int reserved;
    @Column(name="created_at", nullable=false, updatable=false) private Instant createdAt;
    @Column(name="updated_at", nullable=false) private Instant updatedAt;
    ```

    **Note:** Inventory KHÔNG có soft-delete trong record cũ → KHÔNG add `@SQLRestriction`/`@SQLDelete` + KHÔNG add `deleted` column. Nếu record cũ có thêm field nào (e.g., `lowStockThreshold`), giữ.

    **(c) InventoryDto** — record với cùng shape Entity (KHÔNG cần drop field gì vì không có `deleted`/`passwordHash`).

    **(d) InventoryMapper** — `toDto(entity)`.

    **(e) InventoryRepository**:
    ```java
    public interface InventoryRepository extends JpaRepository<InventoryEntity, String> {
      Optional<InventoryEntity> findByProductId(String productId);
    }
    ```

    **(f) InventoryCrudService** — swap inject + insert mapper boundary nếu service hiện trả Entity ra controller. (Inventory thường dùng nội bộ — kiểm tra controller trả gì.)

    **(g) DELETE** `InMemoryInventoryRepository.java` và `InventoryItem.java` (sau khi rename).

    **(h) InventoryRepositoryJpaTest** — Testcontainers, schema=inventory_svc, cover 4 behaviors.
  </action>
  <verify>
    <automated>cd sources/backend/inventory-service && mvn -q -DskipTests compile && test ! -f src/main/java/com/ptit/htpt/inventoryservice/repository/InMemoryInventoryRepository.java && grep -q "@Entity" src/main/java/com/ptit/htpt/inventoryservice/domain/InventoryEntity.java && grep -q "extends JpaRepository" src/main/java/com/ptit/htpt/inventoryservice/repository/InventoryRepository.java && grep -q "findByProductId" src/main/java/com/ptit/htpt/inventoryservice/repository/InventoryRepository.java && mvn -q test -Dtest=InventoryRepositoryJpaTest</automated>
  </verify>
  <acceptance_criteria>
    - InventoryEntity.java có `@Entity`, `@Table(name="inventory_items", schema="inventory_svc")`, field `productId` `@Column(unique=true)`
    - InventoryRepository extends JpaRepository với `findByProductId`
    - InMemoryInventoryRepository.java không tồn tại
    - InventoryItem.java không tồn tại (đã rename)
    - `mvn compile` xanh
    - InventoryRepositoryJpaTest 4 test pass green
  </acceptance_criteria>
  <done>inventory-service JPA layer compile + test xanh; rename complete.</done>
</task>

<task type="auto">
  <name>Task 7.3: Tạo Flyway V1 + V2 migrations cho inventory-service</name>
  <files>
    sources/backend/inventory-service/src/main/resources/db/migration/V1__init_schema.sql,
    sources/backend/inventory-service/src/main/resources/db/seed-dev/V2__seed_dev_data.sql
  </files>
  <read_first>
    - .planning/phases/05-database-foundation/05-RESEARCH.md §Migration SQL Outline §inventory-service V1 + V2
    - .planning/phases/05-database-foundation/05-03-PLAN.md Task 3.3 (canonical — verify product IDs `prod-001`..`prod-010` đã seed Plan 03)
    - sources/backend/inventory-service/src/main/java/com/ptit/htpt/inventoryservice/domain/InventoryEntity.java (Task 7.2 — verify columns match)
  </read_first>
  <action>
    **(a)** `V1__init_schema.sql`:
    ```sql
    CREATE TABLE inventory_svc.inventory_items (
      id VARCHAR(36) PRIMARY KEY,
      product_id VARCHAR(36) NOT NULL,
      quantity INT NOT NULL DEFAULT 0,
      reserved INT NOT NULL DEFAULT 0,
      created_at TIMESTAMP WITH TIME ZONE NOT NULL,
      updated_at TIMESTAMP WITH TIME ZONE NOT NULL,
      CONSTRAINT uq_inventory_product UNIQUE (product_id)
    );
    ```

    **(b)** `V2__seed_dev_data.sql` — product_id PHẢI khớp `prod-001`..`prod-010` từ Plan 03:
    ```sql
    INSERT INTO inventory_svc.inventory_items (id, product_id, quantity, reserved, created_at, updated_at) VALUES
      ('inv-001', 'prod-001', 25, 0, NOW(), NOW()),
      ('inv-002', 'prod-002', 40, 0, NOW(), NOW()),
      ('inv-003', 'prod-003', 120, 0, NOW(), NOW()),
      ('inv-004', 'prod-004', 80, 0, NOW(), NOW()),
      ('inv-005', 'prod-005', 15, 0, NOW(), NOW()),
      ('inv-006', 'prod-006', 50, 0, NOW(), NOW()),
      ('inv-007', 'prod-007', 30, 0, NOW(), NOW()),
      ('inv-008', 'prod-008', 70, 0, NOW(), NOW()),
      ('inv-009', 'prod-009', 45, 0, NOW(), NOW()),
      ('inv-010', 'prod-010', 60, 0, NOW(), NOW());
    ```

    Cross-check Plan 03 product IDs trước khi commit (RESEARCH Open Q #4).
  </action>
  <verify>
    <automated>test -f sources/backend/inventory-service/src/main/resources/db/migration/V1__init_schema.sql && test -f sources/backend/inventory-service/src/main/resources/db/seed-dev/V2__seed_dev_data.sql && grep -q "CREATE TABLE inventory_svc.inventory_items" sources/backend/inventory-service/src/main/resources/db/migration/V1__init_schema.sql && grep -q "uq_inventory_product" sources/backend/inventory-service/src/main/resources/db/migration/V1__init_schema.sql && grep -c "prod-0" sources/backend/inventory-service/src/main/resources/db/seed-dev/V2__seed_dev_data.sql | grep -qE "^(10|11)$"</automated>
  </verify>
  <acceptance_criteria>
    - V1 chứa `CREATE TABLE inventory_svc.inventory_items` + UNIQUE `uq_inventory_product` trên product_id
    - V2 chứa đúng 10 INSERT rows với product_id `prod-001`..`prod-010`
    - product_id khớp 100% với product seed (Plan 03 V2)
  </acceptance_criteria>
  <done>V1 + V2 committed; 10 inventory rows align với products.</done>
</task>

</tasks>

<verification>
- `mvn -pl sources/backend/inventory-service test` xanh
- 10 inventory rows trong `inventory_svc.inventory_items`
- product_id cross-service consistent với Plan 03
</verification>

<success_criteria>
1. inventory-service compile + start green
2. V1 schema + V2 seed (10 rows) applied
3. InventoryRepositoryJpaTest pass green
4. Rename InventoryItem → InventoryEntity throughout
</success_criteria>

<output>
Create `.planning/phases/05-database-foundation/05-07-SUMMARY.md` ghi rõ rename scope, cross-service product_id consistency.
</output>
