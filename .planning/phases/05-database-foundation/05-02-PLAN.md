---
phase: 05-database-foundation
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - docker-compose.yml
  - db/init/01-schemas.sql
autonomous: false
requirements: [DB-01]
must_haves:
  truths:
    - "`docker compose up postgres` lên container green với healthcheck PASS (`pg_isready -U tmdt -d tmdt` exit 0)"
    - "Volume `tmdt-pgdata` persist data giữa restarts (`docker compose down` rồi `up` lại không mất schemas)"
    - "5 schemas (`user_svc`, `product_svc`, `order_svc`, `payment_svc`, `inventory_svc`) đã được pre-create qua init script"
  artifacts:
    - path: "docker-compose.yml"
      provides: "Postgres service block + volume `tmdt-pgdata` + 5 services backend `depends_on: postgres: condition: service_healthy` + env vars DB_*"
      contains: "image: postgres:16-alpine"
    - path: "db/init/01-schemas.sql"
      provides: "Init script tạo 5 schemas qua `/docker-entrypoint-initdb.d/` mount"
      contains: "CREATE SCHEMA IF NOT EXISTS user_svc"
  key_links:
    - from: "docker-compose.yml services.postgres"
      to: "db/init/01-schemas.sql"
      via: "volume mount `./db/init:/docker-entrypoint-initdb.d:ro`"
      pattern: "/docker-entrypoint-initdb.d"
    - from: "docker-compose.yml services.{user,product,order,payment,inventory}-service"
      to: "services.postgres"
      via: "depends_on condition service_healthy"
      pattern: "condition: service_healthy"
---

<objective>
Wave 2 (Infra): thêm Postgres container vào root `docker-compose.yml`, mount init script tạo 5 schemas, declare named volume `tmdt-pgdata`, và wire 5 services backend (user/product/order/payment/inventory) `depends_on` healthcheck của Postgres + env vars `DB_HOST/DB_PORT/DB_NAME/DB_USER/DB_PASSWORD/SPRING_PROFILES_ACTIVE=dev`. Notification + api-gateway + frontend KHÔNG động (D-09).

Purpose: Nền tảng infra cho toàn bộ Wave 3 (5 services parallel refactor). Healthcheck `pg_isready` đảm bảo services chỉ start sau khi DB ready — tránh race condition Flyway migration.
Output: Postgres container green; 5 schemas pre-created; 5 services backend đã có env vars datasource sẵn sàng.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/05-database-foundation/05-CONTEXT.md
@.planning/phases/05-database-foundation/05-RESEARCH.md
@.planning/phases/05-database-foundation/05-PATTERNS.md

<interfaces>
<!-- Existing docker-compose.yml format reference (Compose Spec mới, KHÔNG có version: line). -->
<!-- 5 service blocks hiện tại sẽ được modify để thêm depends_on + environment. -->
<!-- Healthcheck pattern: pg_isready -U tmdt -d tmdt (RESEARCH §Decision #9). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 2.1: Tạo db/init/01-schemas.sql</name>
  <files>db/init/01-schemas.sql</files>
  <read_first>
    - .planning/phases/05-database-foundation/05-RESEARCH.md §Decision #3 (multi-schema pre-create)
    - .planning/phases/05-database-foundation/05-PATTERNS.md §I (db/init/01-schemas.sql full content)
  </read_first>
  <action>
    Tạo file `db/init/01-schemas.sql` với content chính xác:

    ```sql
    CREATE SCHEMA IF NOT EXISTS user_svc;
    CREATE SCHEMA IF NOT EXISTS product_svc;
    CREATE SCHEMA IF NOT EXISTS order_svc;
    CREATE SCHEMA IF NOT EXISTS payment_svc;
    CREATE SCHEMA IF NOT EXISTS inventory_svc;
    ```

    File này sẽ được mount vào `/docker-entrypoint-initdb.d/01-schemas.sql` qua docker-compose volume — Postgres image tự chạy 1 lần khi volume `tmdt-pgdata` lần đầu init (Pitfall 11, RESEARCH §Risks).
  </action>
  <verify>
    <automated>test -f db/init/01-schemas.sql && grep -c "CREATE SCHEMA IF NOT EXISTS" db/init/01-schemas.sql | grep -q "^5$"</automated>
  </verify>
  <acceptance_criteria>
    - File `db/init/01-schemas.sql` tồn tại
    - Chứa đúng 5 dòng `CREATE SCHEMA IF NOT EXISTS` cho `user_svc`, `product_svc`, `order_svc`, `payment_svc`, `inventory_svc`
    - Không có DDL khác (chỉ schema creation)
  </acceptance_criteria>
  <done>File commit, sẵn sàng mount.</done>
</task>

<task type="auto">
  <name>Task 2.2: Modify docker-compose.yml — thêm postgres service + wire 5 backend services</name>
  <files>docker-compose.yml</files>
  <read_first>
    - docker-compose.yml (toàn bộ file hiện tại — verify port mapping của 5 services + format Compose Spec)
    - .planning/phases/05-database-foundation/05-RESEARCH.md §Decision #9 (full snippet docker-compose)
    - .planning/phases/05-database-foundation/05-PATTERNS.md §H (per-service modify pattern)
    - db/init/01-schemas.sql (Task 2.1 output — phải tồn tại trước khi compose mount)
  </read_first>
  <action>
    Modify root `docker-compose.yml` theo 3 thay đổi:

    **(a) Thêm `postgres` service** ở đầu `services:` block (TRƯỚC api-gateway), copy chính xác:

    ```yaml
      postgres:
        image: postgres:16-alpine
        environment:
          POSTGRES_DB: tmdt
          POSTGRES_USER: tmdt
          POSTGRES_PASSWORD: tmdt
        volumes:
          - tmdt-pgdata:/var/lib/postgresql/data
          - ./db/init:/docker-entrypoint-initdb.d:ro
        healthcheck:
          test: ["CMD-SHELL", "pg_isready -U tmdt -d tmdt"]
          interval: 5s
          timeout: 5s
          retries: 10
        ports:
          - "5432:5432"
    ```

    **(b) Modify 5 service blocks** (`user-service`, `product-service`, `order-service`, `payment-service`, `inventory-service`) — thêm `depends_on` + `environment` block. KHÔNG động `notification-service`, `api-gateway`, `frontend`. Pattern (apply per service, GIỮ NGUYÊN block `build` + `ports` hiện tại):

    ```yaml
      product-service:
        build: ./sources/backend/product-service
        depends_on:
          postgres:
            condition: service_healthy
        environment:
          SPRING_PROFILES_ACTIVE: dev
          DB_HOST: postgres
          DB_PORT: 5432
          DB_NAME: tmdt
          DB_USER: tmdt
          DB_PASSWORD: tmdt
        ports:
          - "8082:8080"
    ```

    Lặp pattern này cho cả 5 services backend (giữ nguyên port mapping của mỗi service đã có sẵn — đọc file hiện tại để biết port chính xác).

    **(c) Thêm `volumes:` block cuối file** (nếu chưa có):

    ```yaml
    volumes:
      tmdt-pgdata:
    ```

    Nếu file đã có `volumes:` block, thêm `tmdt-pgdata:` vào danh sách.

    **Critical:** Notification-service KHÔNG add depends_on Postgres (D-09 — giữ in-memory). API gateway + frontend không động.
  </action>
  <verify>
    <automated>docker compose config --quiet && docker compose config | grep -q "image: postgres:16-alpine" && docker compose config | grep -c "condition: service_healthy" | grep -qE "^5$" && docker compose config | grep -q "tmdt-pgdata"</automated>
  </verify>
  <acceptance_criteria>
    - `docker compose config --quiet` exit code 0 (YAML valid)
    - Output `docker compose config` chứa `image: postgres:16-alpine`
    - Đúng 5 services có `condition: service_healthy` (user/product/order/payment/inventory; notification + gateway KHÔNG có)
    - Volume `tmdt-pgdata` được declare
    - Mount `./db/init:/docker-entrypoint-initdb.d:ro` được declare trên service `postgres`
    - Notification-service KHÔNG có `depends_on: postgres`
    - 5 services backend có env vars `SPRING_PROFILES_ACTIVE=dev`, `DB_HOST=postgres`, `DB_PORT=5432`, `DB_NAME=tmdt`, `DB_USER=tmdt`, `DB_PASSWORD=tmdt`
  </acceptance_criteria>
  <done>docker-compose.yml syntactically valid, postgres service + 5 services wired, volume declared.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2.3: Smoke test Postgres container + healthcheck + 5 schemas</name>
  <what-built>
    Postgres 16-alpine container declared trong docker-compose; init script `db/init/01-schemas.sql` tạo 5 schemas; volume `tmdt-pgdata` persist data; healthcheck `pg_isready` running.
  </what-built>
  <how-to-verify>
    1. Reset volume sạch (đảm bảo init script chạy):
       ```bash
       docker compose down -v
       ```
    2. Start chỉ Postgres:
       ```bash
       docker compose up -d postgres
       ```
    3. Đợi ~10 giây, verify healthcheck PASS:
       ```bash
       docker compose ps postgres
       ```
       Expect: cột `STATUS` chứa `(healthy)`.
    4. Verify 5 schemas đã được tạo:
       ```bash
       docker compose exec postgres psql -U tmdt -d tmdt -c "\dn"
       ```
       Expect: bảng `List of schemas` chứa `user_svc`, `product_svc`, `order_svc`, `payment_svc`, `inventory_svc` (cùng với `public`, `information_schema`, etc.).
    5. Verify volume persistence:
       ```bash
       docker compose stop postgres
       docker compose start postgres
       sleep 5
       docker compose exec postgres psql -U tmdt -d tmdt -c "\dn" | grep user_svc
       ```
       Expect: schema `user_svc` vẫn còn (không bị mất).

    6. Tear down: `docker compose down` (KHÔNG `-v` — giữ volume cho Wave 3).
  </how-to-verify>
  <resume-signal>Type "approved" nếu cả 4 verify PASS; type "issue: <description>" nếu có lỗi</resume-signal>
</task>

</tasks>

<verification>
- `docker compose config` valid
- Container `postgres` lên `(healthy)` qua healthcheck
- 5 schemas pre-created
- Volume persist qua restart
</verification>

<success_criteria>
1. docker-compose.yml + db/init/01-schemas.sql committed
2. `docker compose up -d postgres` đạt status `(healthy)` trong < 30s
3. 5 schemas (`user_svc`, `product_svc`, `order_svc`, `payment_svc`, `inventory_svc`) tồn tại
4. Volume `tmdt-pgdata` persist qua stop/start cycle
</success_criteria>

<output>
After completion, create `.planning/phases/05-database-foundation/05-02-SUMMARY.md` mô tả container settings, port mapping, evidence từ checkpoint manual verify.
</output>
