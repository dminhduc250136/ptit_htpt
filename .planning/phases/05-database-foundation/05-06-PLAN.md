---
phase: 05-database-foundation
plan: 06
type: execute
wave: 3
depends_on: [02]
files_modified:
  - sources/backend/payment-service/pom.xml
  - sources/backend/payment-service/src/main/resources/application.yml
  - sources/backend/payment-service/src/main/java/com/ptit/htpt/paymentservice/domain/PaymentTransactionEntity.java
  - sources/backend/payment-service/src/main/java/com/ptit/htpt/paymentservice/domain/PaymentTransactionDto.java
  - sources/backend/payment-service/src/main/java/com/ptit/htpt/paymentservice/domain/PaymentTransactionMapper.java
  - sources/backend/payment-service/src/main/java/com/ptit/htpt/paymentservice/repository/PaymentTransactionRepository.java
  - sources/backend/payment-service/src/main/java/com/ptit/htpt/paymentservice/repository/InMemoryPaymentTransactionRepository.java
  - sources/backend/payment-service/src/main/java/com/ptit/htpt/paymentservice/service/PaymentCrudService.java
  - sources/backend/payment-service/src/main/resources/db/migration/V1__init_schema.sql
  - sources/backend/payment-service/src/test/java/com/ptit/htpt/paymentservice/repository/PaymentTransactionRepositoryJpaTest.java
autonomous: true
requirements: [DB-02, DB-03, DB-04]
must_haves:
  truths:
    - "payment-service start green với JPA + Flyway 10 + Postgres 16 (schema `payment_svc`)"
    - "Flyway V1 tạo bảng `payment_svc.payments` với fields khớp record cũ (`session_id`, `reference`, `message` GIỮ — cross-cutting note #4 PATTERNS)"
    - "ddl-auto=validate pass (entity ↔ V1 schema match)"
    - "JPA `findAll/findById/save` hoạt động qua `PaymentTransactionRepository`"
    - "KHÔNG có V2 seed cho payment-service (DB-05 không yêu cầu — payments tạo runtime ở Phase 8)"
  artifacts:
    - path: "sources/backend/payment-service/src/main/java/com/ptit/htpt/paymentservice/domain/PaymentTransactionEntity.java"
      provides: "JPA @Entity với fields session_id/reference/message/amount/method/status preserved"
      contains: "@Entity"
    - path: "sources/backend/payment-service/src/main/resources/db/migration/V1__init_schema.sql"
      provides: "DDL `payment_svc.payments` align ACTUAL entity fields"
      contains: "CREATE TABLE payment_svc.payments"
  key_links:
    - from: "PaymentTransactionEntity (@Entity)"
      to: "payments table (payment_svc schema)"
      via: "Hibernate JPA"
      pattern: "@Table\\(name = \"payments\", schema = \"payment_svc\"\\)"
---

<objective>
Wave 3 (mirror canonical 05-03): refactor payment-service từ in-memory → JPA + Flyway + Postgres trên schema `payment_svc`. **Cross-cutting note (PATTERNS.md #4):** Record cũ có fields `sessionId, reference, message` — RESEARCH outline payments table dùng `order_id, amount, method`. Phase 5 KHÔNG rename schema rộng — GIỮ FIELD CŨ trong V1, defer rename (nếu cần) sang Phase 8.

Phase 5 KHÔNG seed payment data (DB-05 không yêu cầu; Phase 8 sẽ tạo runtime khi checkout flow).

Output: payment-service start green; V1 schema khớp entity; KHÔNG có V2 seed file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/05-database-foundation/05-CONTEXT.md
@.planning/phases/05-database-foundation/05-RESEARCH.md
@.planning/phases/05-database-foundation/05-PATTERNS.md
@.planning/phases/05-database-foundation/baseline/openapi-payment-service.json

<interfaces>
Schema: payment_svc. Table: payments.
Cross-cutting note #4 PATTERNS: ACTUAL record fields = sessionId, reference, message (+ amount, method, status, createdAt, updatedAt). PHẢI giữ trong V1 — NOT dùng RESEARCH outline order_id/amount/method.
V1 columns (align entity actual): id VARCHAR(36) PK, session_id VARCHAR(120), reference VARCHAR(120), message VARCHAR(500), amount NUMERIC(12,2), method VARCHAR(50), status VARCHAR(30), created_at, updated_at, deleted BOOLEAN.
KHÔNG có V2 seed file cho payment-service (DB-05 specific list không bao gồm payment).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 6.1: Add deps + application.yml (schema=payment_svc)</name>
  <files>sources/backend/payment-service/pom.xml, sources/backend/payment-service/src/main/resources/application.yml</files>
  <read_first>
    - sources/backend/payment-service/pom.xml
    - sources/backend/payment-service/src/main/resources/application.yml
    - .planning/phases/05-database-foundation/05-PATTERNS.md §A, §B
    - .planning/phases/05-database-foundation/05-03-PLAN.md Task 3.1 (canonical)
  </read_first>
  <action>
    Apply 05-03 Task 3.1 pattern, schema = `payment_svc`. Add 6 deps. Insert datasource/jpa/flyway block với:
    - `currentSchema=payment_svc`
    - `hibernate.default_schema: payment_svc`
    - `flyway.schemas: payment_svc` + `default-schema: payment_svc`
    - `baseline-on-migrate: false`, `locations: classpath:db/migration`
    - Doc separator + `on-profile: dev` block với `locations: classpath:db/migration,classpath:db/seed-dev`

    Lưu ý: Mặc dù KHÔNG có V2 seed file thực tế, profile dev block vẫn declare `db/seed-dev` location — Flyway sẽ ignore nếu folder rỗng/không tồn tại (test cẩn trọng — nếu Flyway lỗi "location not found" thì chỉ giữ `classpath:db/migration` cho profile dev).
  </action>
  <verify>
    <automated>cd sources/backend/payment-service && mvn -q -DskipTests compile && grep -q "currentSchema=payment_svc" src/main/resources/application.yml && grep -q "default_schema: payment_svc" src/main/resources/application.yml && grep -q "flyway-database-postgresql" pom.xml</automated>
  </verify>
  <acceptance_criteria>
    - Compile xanh
    - application.yml có `currentSchema=payment_svc`, `default_schema: payment_svc`
    - pom.xml có cả `flyway-core` + `flyway-database-postgresql` + 2 testcontainers test deps
  </acceptance_criteria>
  <done>payment-service compile xanh.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 6.2: Refactor PaymentTransactionEntity record → @Entity class; tạo Dto + Mapper + Repository; xóa In-memory repo</name>
  <files>
    sources/backend/payment-service/src/main/java/com/ptit/htpt/paymentservice/domain/PaymentTransactionEntity.java,
    sources/backend/payment-service/src/main/java/com/ptit/htpt/paymentservice/domain/PaymentTransactionDto.java,
    sources/backend/payment-service/src/main/java/com/ptit/htpt/paymentservice/domain/PaymentTransactionMapper.java,
    sources/backend/payment-service/src/main/java/com/ptit/htpt/paymentservice/repository/PaymentTransactionRepository.java,
    sources/backend/payment-service/src/main/java/com/ptit/htpt/paymentservice/service/PaymentCrudService.java,
    sources/backend/payment-service/src/test/java/com/ptit/htpt/paymentservice/repository/PaymentTransactionRepositoryJpaTest.java
  </files>
  <read_first>
    - sources/backend/payment-service/src/main/java/com/ptit/htpt/paymentservice/domain/PaymentTransactionEntity.java (record cũ — verify ALL fields)
    - sources/backend/payment-service/src/main/java/com/ptit/htpt/paymentservice/repository/InMemoryPaymentTransactionRepository.java
    - sources/backend/payment-service/src/main/java/com/ptit/htpt/paymentservice/service/PaymentCrudService.java
    - .planning/phases/05-database-foundation/05-PATTERNS.md cross-cutting note #4 (giữ field actual cũ)
    - .planning/phases/05-database-foundation/05-03-PLAN.md Task 3.2 (canonical)
  </read_first>
  <behavior>
    - Test 1: save PaymentTransactionEntity → findById trả entity với cùng id, sessionId, reference, message, amount, method, status
    - Test 2: soft-delete: 2 records, softDelete 1, findAll() trả 1
    - Test 3: PaymentTransactionDto KHÔNG chứa `deleted` field
    - Test 4: round-trip giữ nguyên `sessionId`, `reference`, `message` (cross-cutting note #4)
  </behavior>
  <action>
    Apply 05-03 Task 3.2 pattern. Đặc thù:

    **(a) PaymentTransactionEntity** — `@Table(name="payments", schema="payment_svc")`. PHẢI giữ ALL fields từ record cũ:
    ```java
    @Id @Column(length=36) private String id;
    @Column(name="session_id", length=120) private String sessionId;
    @Column(length=120) private String reference;
    @Column(length=500) private String message;
    @Column(precision=12, scale=2) private BigDecimal amount;
    @Column(length=50) private String method;
    @Column(length=30) private String status;
    @Column(nullable=false) private boolean deleted = false;
    @Column(name="created_at", nullable=false, updatable=false) private Instant createdAt;
    @Column(name="updated_at", nullable=false) private Instant updatedAt;
    ```

    Soft-delete annotations giống 05-03.

    Nếu record cũ có field nào KHÁC (e.g., `idempotencyKey`, `gatewayResponse`), GIỮ — Phase 5 không drop field.

    **(b) PaymentTransactionDto** — drop `deleted`, GIỮ tất cả fields business (sessionId, reference, message).

    **(c) PaymentTransactionMapper** — `toDto(entity)`.

    **(d) PaymentTransactionRepository**:
    ```java
    public interface PaymentTransactionRepository extends JpaRepository<PaymentTransactionEntity, String> {
      Optional<PaymentTransactionEntity> findBySessionId(String sessionId);
      Optional<PaymentTransactionEntity> findByReference(String reference);
    }
    ```

    **(e) PaymentCrudService** — swap inject + insert mapper boundary.

    **(f) DELETE** `InMemoryPaymentTransactionRepository.java`.

    **(g) PaymentTransactionRepositoryJpaTest** — Testcontainers, schema=payment_svc, cover 4 behaviors.
  </action>
  <verify>
    <automated>cd sources/backend/payment-service && mvn -q -DskipTests compile && test ! -f src/main/java/com/ptit/htpt/paymentservice/repository/InMemoryPaymentTransactionRepository.java && grep -q "@Entity" src/main/java/com/ptit/htpt/paymentservice/domain/PaymentTransactionEntity.java && grep -qE "session_?[Ii]d" src/main/java/com/ptit/htpt/paymentservice/domain/PaymentTransactionEntity.java && grep -q "extends JpaRepository" src/main/java/com/ptit/htpt/paymentservice/repository/PaymentTransactionRepository.java && mvn -q test -Dtest=PaymentTransactionRepositoryJpaTest</automated>
  </verify>
  <acceptance_criteria>
    - PaymentTransactionEntity có `@Entity`, `@Table(name="payments", schema="payment_svc")`, fields `sessionId`, `reference`, `message`, `amount`, `method`, `status`, `deleted`, `createdAt`, `updatedAt`
    - PaymentTransactionDto KHÔNG chứa `deleted`
    - PaymentTransactionRepository extends JpaRepository
    - InMemoryPaymentTransactionRepository không tồn tại
    - `mvn compile` xanh
    - PaymentTransactionRepositoryJpaTest 4 test pass green
  </acceptance_criteria>
  <done>payment-service JPA layer compile + test xanh; field cũ preserved.</done>
</task>

<task type="auto">
  <name>Task 6.3: Tạo Flyway V1 migration cho payment-service (KHÔNG có V2 seed)</name>
  <files>sources/backend/payment-service/src/main/resources/db/migration/V1__init_schema.sql</files>
  <read_first>
    - sources/backend/payment-service/src/main/java/com/ptit/htpt/paymentservice/domain/PaymentTransactionEntity.java (Task 6.2 — verify columns match)
    - .planning/phases/05-database-foundation/05-PATTERNS.md cross-cutting note #4
  </read_first>
  <action>
    `V1__init_schema.sql` — DDL khớp ENTITY actual (KHÔNG dùng RESEARCH outline order_id/amount/method):
    ```sql
    CREATE TABLE payment_svc.payments (
      id VARCHAR(36) PRIMARY KEY,
      session_id VARCHAR(120),
      reference VARCHAR(120),
      message VARCHAR(500),
      amount NUMERIC(12,2),
      method VARCHAR(50),
      status VARCHAR(30) NOT NULL,
      deleted BOOLEAN NOT NULL DEFAULT FALSE,
      created_at TIMESTAMP WITH TIME ZONE NOT NULL,
      updated_at TIMESTAMP WITH TIME ZONE NOT NULL
    );
    CREATE INDEX idx_payments_session_id ON payment_svc.payments(session_id) WHERE session_id IS NOT NULL;
    CREATE INDEX idx_payments_reference ON payment_svc.payments(reference) WHERE reference IS NOT NULL;
    ```

    Nếu entity có thêm field (idempotencyKey, gatewayResponse, etc.), thêm column tương ứng vào V1.

    KHÔNG tạo `V2__seed_dev_data.sql` cho payment-service (Phase 8 sẽ tạo payment runtime; DB-05 không yêu cầu seed payment).
  </action>
  <verify>
    <automated>test -f sources/backend/payment-service/src/main/resources/db/migration/V1__init_schema.sql && grep -q "CREATE TABLE payment_svc.payments" sources/backend/payment-service/src/main/resources/db/migration/V1__init_schema.sql && grep -q "session_id" sources/backend/payment-service/src/main/resources/db/migration/V1__init_schema.sql && grep -q "reference" sources/backend/payment-service/src/main/resources/db/migration/V1__init_schema.sql && grep -q "message" sources/backend/payment-service/src/main/resources/db/migration/V1__init_schema.sql && test ! -f sources/backend/payment-service/src/main/resources/db/seed-dev/V2__seed_dev_data.sql</automated>
  </verify>
  <acceptance_criteria>
    - V1 chứa `CREATE TABLE payment_svc.payments` + columns `session_id`, `reference`, `message`, `amount`, `method`, `status`
    - KHÔNG có file V2 trong db/seed-dev/
    - Mỗi column nullable phù hợp với entity (sessionId/reference/message có thể nullable, status NOT NULL)
  </acceptance_criteria>
  <done>V1 committed; entity ↔ schema match; Hibernate validate pass.</done>
</task>

</tasks>

<verification>
- `mvn -pl sources/backend/payment-service test` xanh
- `mvn spring-boot:run` profile dev → app start, V1 applied, no V2 (folder rỗng/không tồn tại)
- Hibernate `ddl-auto: validate` không complain field mismatch
</verification>

<success_criteria>
1. payment-service compile + start green
2. V1 schema applied khớp entity actual fields (sessionId, reference, message preserved)
3. KHÔNG có V2 seed file
4. PaymentTransactionRepositoryJpaTest pass green
</success_criteria>

<output>
Create `.planning/phases/05-database-foundation/05-06-SUMMARY.md` ghi rõ field preservation rationale, lý do skip V2 seed.
</output>
