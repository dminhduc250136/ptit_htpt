---
phase: 05-database-foundation
plan: 05
type: execute
wave: 3
depends_on: [02]
files_modified:
  - sources/backend/order-service/pom.xml
  - sources/backend/order-service/src/main/resources/application.yml
  - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/domain/OrderEntity.java
  - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/domain/OrderDto.java
  - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/domain/OrderMapper.java
  - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/repository/OrderRepository.java
  - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/repository/InMemoryOrderRepository.java
  - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/service/OrderCrudService.java
  - sources/backend/order-service/src/main/resources/db/migration/V1__init_schema.sql
  - sources/backend/order-service/src/main/resources/db/seed-dev/V2__seed_dev_data.sql
  - sources/backend/order-service/src/test/java/com/ptit/htpt/orderservice/repository/OrderRepositoryJpaTest.java
autonomous: true
requirements: [DB-02, DB-03, DB-04, DB-05]
must_haves:
  truths:
    - "order-service start green với JPA + Flyway 10 + Postgres 16 (schema `order_svc`)"
    - "Flyway V1 tạo bảng `order_svc.orders`; ddl-auto=validate pass"
    - "Profile `dev` V2 seed populate 2 demo orders cho `demo_user` (id=`00000000-0000-0000-0000-000000000002`)"
    - "JPA `findAll/findById/save` + `findByUserId` hoạt động qua `OrderRepository extends JpaRepository<OrderEntity, String>`"
    - "Soft-delete `@SQLRestriction` filter records đã xóa"
    - "Phase 8 fields (per-item, shippingAddress, paymentMethod) DEFER — chỉ basic OrderEntity trong Phase 5"
  artifacts:
    - path: "sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/domain/OrderEntity.java"
      provides: "JPA @Entity basic (Phase 8 sẽ thêm OrderItemEntity)"
      contains: "@Entity"
    - path: "sources/backend/order-service/src/main/resources/db/migration/V1__init_schema.sql"
      provides: "DDL `order_svc.orders` basic + index user_id"
      contains: "CREATE TABLE order_svc.orders"
    - path: "sources/backend/order-service/src/main/resources/db/seed-dev/V2__seed_dev_data.sql"
      provides: "2 demo orders gắn với demo_user"
      contains: "ord-demo-001"
  key_links:
    - from: "OrderEntity (@Entity)"
      to: "orders table (order_svc schema)"
      via: "Hibernate JPA"
      pattern: "@Table\\(name = \"orders\", schema = \"order_svc\"\\)"
    - from: "OrderEntity.userId"
      to: "user_svc.users.id (logical only — no FK cross-schema)"
      via: "service layer validation"
      pattern: "user_id VARCHAR\\(36\\) NOT NULL"
---

<objective>
Wave 3 (mirror canonical 05-03): refactor order-service từ in-memory → JPA + Flyway + Postgres trên schema `order_svc`. Phase 5 chỉ ship OrderEntity basic — Phase 8 sẽ extend OrderItemEntity per-row + shippingAddress + paymentMethod (PERSIST-02).

**Cross-cutting note (PATTERNS.md):** Record cũ `OrderEntity` có field `note` — V1 DDL PHẢI giữ field này (`note VARCHAR(500)` nullable) để FE không vỡ.

Output: order-service start green; 2 demo orders seeded cho `demo_user`; FE `/account/orders` (Phase 7) có data hiển thị thật.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/05-database-foundation/05-CONTEXT.md
@.planning/phases/05-database-foundation/05-RESEARCH.md
@.planning/phases/05-database-foundation/05-PATTERNS.md
@.planning/phases/05-database-foundation/baseline/openapi-order-service.json

<interfaces>
Schema: order_svc. Table: orders.
V1 columns: id VARCHAR(36) PK, user_id VARCHAR(36) NOT NULL, total NUMERIC(12,2) NOT NULL, status VARCHAR(30) NOT NULL, note VARCHAR(500) NULL (cross-cutting note #3 PATTERNS), deleted BOOLEAN, created_at, updated_at.
INDEX idx_orders_user_id ON orders(user_id).
JPA repository extras: List<OrderEntity> findByUserIdAndDeletedFalse(String userId) (hoặc List<OrderEntity> findByUserId — `@SQLRestriction` đã filter deleted).
Phase 8 sẽ extend (PERSIST-02): order_items table + shipping_address (JSON/embedded) + payment_method. Phase 5 SKIP.
</interfaces>

<cross_service_ids>
Canonical IDs dùng XUYÊN suốt 5 services. KHÔNG đổi literal:
  admin_user_id = 00000000-0000-0000-0000-000000000001  (Plan 04 user_svc.users)
  demo_user_id  = 00000000-0000-0000-0000-000000000002  (V2 seed Plan 05 dùng làm orders.user_id — PHẢI khớp Plan 04 literal)
  product IDs   = prod-001 .. prod-010                  (Plan 03 + Plan 07)
Cross-service consistency = Plan 08 Task 8.1 sẽ assert NOT EXISTS (SELECT 1 FROM user_svc.users WHERE u.id = o.user_id) = 0 rows.
</cross_service_ids>
</context>

<tasks>

<task type="auto">
  <name>Task 5.1: Add deps + application.yml (schema=order_svc)</name>
  <files>sources/backend/order-service/pom.xml, sources/backend/order-service/src/main/resources/application.yml</files>
  <read_first>
    - sources/backend/order-service/pom.xml
    - sources/backend/order-service/src/main/resources/application.yml
    - .planning/phases/05-database-foundation/05-PATTERNS.md §A, §B
    - .planning/phases/05-database-foundation/05-03-PLAN.md Task 3.1 (canonical)
  </read_first>
  <action>
    Apply 05-03 Task 3.1 pattern, schema = `order_svc`. Add 6 deps (data-jpa, postgresql runtime, flyway-core, flyway-database-postgresql, testcontainers postgresql + junit-jupiter test-scope). Insert datasource/jpa/flyway block với:
    - `currentSchema=order_svc`
    - `hibernate.default_schema: order_svc`
    - `flyway.schemas: order_svc` + `default-schema: order_svc`
    - `baseline-on-migrate: false`, `locations: classpath:db/migration`
    - Doc separator + `on-profile: dev` block với `locations: classpath:db/migration,classpath:db/seed-dev`
  </action>
  <verify>
    <automated>cd sources/backend/order-service && mvn -q -DskipTests compile && grep -q "currentSchema=order_svc" src/main/resources/application.yml && grep -q "default_schema: order_svc" src/main/resources/application.yml && grep -q "flyway-database-postgresql" pom.xml</automated>
  </verify>
  <acceptance_criteria>
    - Compile xanh
    - application.yml có `currentSchema=order_svc`, `default_schema: order_svc`
    - pom.xml có cả `flyway-core` + `flyway-database-postgresql` + 2 testcontainers test deps
    - Profile dev block tồn tại
  </acceptance_criteria>
  <done>order-service compile xanh với deps + datasource config.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5.2: Refactor OrderEntity record → @Entity class; tạo OrderDto + OrderMapper + OrderRepository; cập nhật OrderCrudService; xóa InMemoryOrderRepository</name>
  <files>
    sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/domain/OrderEntity.java,
    sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/domain/OrderDto.java,
    sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/domain/OrderMapper.java,
    sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/repository/OrderRepository.java,
    sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/service/OrderCrudService.java,
    sources/backend/order-service/src/test/java/com/ptit/htpt/orderservice/repository/OrderRepositoryJpaTest.java
  </files>
  <read_first>
    - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/domain/OrderEntity.java (record cũ — verify field `note` có tồn tại)
    - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/repository/InMemoryOrderRepository.java
    - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/service/OrderCrudService.java
    - .planning/phases/05-database-foundation/05-PATTERNS.md §C..G + cross-cutting note #3 (giữ field `note`)
    - .planning/phases/05-database-foundation/05-03-PLAN.md Task 3.2 (canonical pattern)
  </read_first>
  <behavior>
    - Test 1: save OrderEntity → findById trả entity với cùng id, userId, total, status
    - Test 2: `findByUserId("00000000-0000-0000-0000-000000000002")` sau khi save 2 orders cho user này trả List size=2
    - Test 3: soft-delete: 2 orders, softDelete 1, findAll() trả 1
    - Test 4: OrderDto KHÔNG có field `deleted` (compile-time grep)
    - Test 5: `note` field round-trip: save với `note="Giao gấp"` → findById → entity.note() == "Giao gấp"
  </behavior>
  <execution_hint>
  Refactor touches ~6 files. Executor MAY commit incrementally (entity + DTO + Mapper, sau đó repo + service swap, cuối cùng test + delete InMemory). Whole task = 1 logical unit, multiple commits OK. KHÔNG split task.
  </execution_hint>
  <action>
    Apply 05-03 Task 3.2 pattern. Đặc thù:

    **(a) OrderEntity** — `@Table(name="orders", schema="order_svc")` + `@SQLRestriction("deleted = false")` + `@SQLDelete("UPDATE order_svc.orders SET deleted = true, updated_at = NOW() WHERE id = ?")`. Fields theo `<interfaces>` ở trên — GIỮ field `note` từ record cũ:
    ```java
    @Column(length = 500)
    private String note;  // nullable — cross-cutting note #3
    ```

    Nếu record cũ có thêm field nào (e.g., `currency`, `paymentMethodHint`), giữ trong V1 DDL; nếu record cũ thiếu field nào RESEARCH ghi nhưng vẫn essential, đối chiếu — Phase 5 không thêm field mới (defer Phase 8 PERSIST-02).

    **(b) OrderDto** record — drop `deleted`, KHÔNG drop `note` (FE có thể consume).

    **(c) OrderMapper** — `toDto(entity)`.

    **(d) OrderRepository**:
    ```java
    public interface OrderRepository extends JpaRepository<OrderEntity, String> {
      List<OrderEntity> findByUserId(String userId);
    }
    ```

    **(e) OrderCrudService** — swap inject + insert OrderMapper.toDto boundary.

    **(f) DELETE** `InMemoryOrderRepository.java`.

    **(g) OrderRepositoryJpaTest** — Testcontainers `@DataJpaTest`, schema=order_svc, cover 5 behaviors.
  </action>
  <verify>
    <automated>cd sources/backend/order-service && mvn -q -DskipTests compile && test ! -f src/main/java/com/ptit/htpt/orderservice/repository/InMemoryOrderRepository.java && grep -q "@Entity" src/main/java/com/ptit/htpt/orderservice/domain/OrderEntity.java && grep -q "extends JpaRepository" src/main/java/com/ptit/htpt/orderservice/repository/OrderRepository.java && grep -q "findByUserId" src/main/java/com/ptit/htpt/orderservice/repository/OrderRepository.java && ! grep -qE "\bdeleted\b" src/main/java/com/ptit/htpt/orderservice/domain/OrderDto.java && mvn -q test -Dtest=OrderRepositoryJpaTest</automated>
  </verify>
  <acceptance_criteria>
    - File `OrderEntity.java` chứa `@Entity`, `@Table(name = "orders", schema = "order_svc")`, `@SQLRestriction`, `@SQLDelete`, field `note`
    - File `OrderDto.java` là record, KHÔNG chứa `deleted`
    - File `OrderRepository.java` extends JpaRepository và có `findByUserId`
    - File `InMemoryOrderRepository.java` không tồn tại
    - `mvn compile` xanh
    - `OrderRepositoryJpaTest` 5 test pass green
  </acceptance_criteria>
  <done>order-service JPA layer compile + test xanh.</done>
</task>

<task type="auto">
  <name>Task 5.3: Tạo Flyway V1 + V2 migrations cho order-service</name>
  <files>
    sources/backend/order-service/src/main/resources/db/migration/V1__init_schema.sql,
    sources/backend/order-service/src/main/resources/db/seed-dev/V2__seed_dev_data.sql
  </files>
  <read_first>
    - .planning/phases/05-database-foundation/05-RESEARCH.md §Migration SQL Outline §order-service V1 + V2
    - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/domain/OrderEntity.java (Task 5.2 output — verify columns match)
  </read_first>
  <action>
    **(a)** `V1__init_schema.sql`:
    ```sql
    CREATE TABLE order_svc.orders (
      id VARCHAR(36) PRIMARY KEY,
      user_id VARCHAR(36) NOT NULL,
      total NUMERIC(12,2) NOT NULL,
      status VARCHAR(30) NOT NULL,
      note VARCHAR(500),
      deleted BOOLEAN NOT NULL DEFAULT FALSE,
      created_at TIMESTAMP WITH TIME ZONE NOT NULL,
      updated_at TIMESTAMP WITH TIME ZONE NOT NULL
    );
    CREATE INDEX idx_orders_user_id ON order_svc.orders(user_id);
    ```

    > FK cross-schema (orders.user_id → users.id) NOT enforced — vi phạm microservice boundary; eventual consistency.

    **(b)** `V2__seed_dev_data.sql`:
    ```sql
    INSERT INTO order_svc.orders (id, user_id, total, status, note, deleted, created_at, updated_at) VALUES
      ('ord-demo-001', '00000000-0000-0000-0000-000000000002', 8489000.00, 'DELIVERED', 'Đơn demo 1', FALSE, NOW() - INTERVAL '7 days', NOW() - INTERVAL '5 days'),
      ('ord-demo-002', '00000000-0000-0000-0000-000000000002',  500000.00, 'PENDING',   'Đơn demo 2', FALSE, NOW() - INTERVAL '1 day',  NOW() - INTERVAL '1 day');
    ```

    user_id `00000000-0000-0000-0000-000000000002` PHẢI khớp với `demo_user` id seed trong Plan 04 (cross-service consistency — RESEARCH Open Q #4 + `<cross_service_ids>` block).

    **(c) End-to-end smoke verify** (mirror Plan 03 Task 3.3 (c)) — đảm bảo postgres container đang running từ Wave 2:
    ```bash
    cd sources/backend/order-service
    SPRING_PROFILES_ACTIVE=dev DB_HOST=localhost DB_PORT=5432 DB_NAME=tmdt DB_USER=tmdt DB_PASSWORD=tmdt       mvn -q spring-boot:run -Dspring-boot.run.profiles=dev &
    APP_PID=$!
    sleep 25
    kill $APP_PID
    ```

    Verify DB:
    ```bash
    docker compose exec postgres psql -U tmdt -d tmdt -c "SELECT COUNT(*) FROM order_svc.orders WHERE deleted=false;"
    # Expect: 2
    docker compose exec postgres psql -U tmdt -d tmdt -c "SELECT id, user_id, status FROM order_svc.orders ORDER BY id;"
    # Expect: ord-demo-001 (DELIVERED) + ord-demo-002 (PENDING), cả 2 user_id = 00000000-0000-0000-0000-000000000002
    docker compose exec postgres psql -U tmdt -d tmdt -c "SELECT version, success FROM order_svc.flyway_schema_history;"
    # Expect: V1 + V2 success=true
    ```

    Catch V1+V2 failures sớm thay vì chờ Plan 08.
  </action>
  <verify>
    <automated>test -f sources/backend/order-service/src/main/resources/db/migration/V1__init_schema.sql && test -f sources/backend/order-service/src/main/resources/db/seed-dev/V2__seed_dev_data.sql && grep -q "CREATE TABLE order_svc.orders" sources/backend/order-service/src/main/resources/db/migration/V1__init_schema.sql && grep -q "note VARCHAR(500)" sources/backend/order-service/src/main/resources/db/migration/V1__init_schema.sql && grep -c "ord-demo-" sources/backend/order-service/src/main/resources/db/seed-dev/V2__seed_dev_data.sql | grep -qE "^[2-3]$"</automated>
  </verify>
  <acceptance_criteria>
    - V1 chứa `CREATE TABLE order_svc.orders`, column `note VARCHAR(500)` nullable, INDEX `idx_orders_user_id`
    - V2 chứa 2 INSERT rows: `ord-demo-001` (DELIVERED) và `ord-demo-002` (PENDING)
    - V2 user_id khớp `00000000-0000-0000-0000-000000000002` (demo_user trong Plan 04 — `<cross_service_ids>` literal)
    - Smoke run `mvn spring-boot:run -Dspring-boot.run.profiles=dev` lên green; `SELECT COUNT(*) FROM order_svc.orders WHERE deleted=false` = 2; `order_svc.flyway_schema_history` chứa V1 + V2 success=true
  </acceptance_criteria>
  <done>V1 + V2 committed; 2 demo orders gắn đúng user.</done>
</task>

</tasks>

<verification>
- `mvn -pl sources/backend/order-service test` xanh
- 2 demo orders trong `order_svc.orders` sau khi spring-boot:run profile dev
- user_id seed cross-service consistent với Plan 04
</verification>

<success_criteria>
1. order-service compile + start green
2. V1 schema basic + V2 seed (2 demo orders) applied
3. OrderRepositoryJpaTest pass green
4. Field `note` preserved
</success_criteria>

<output>
Create `.planning/phases/05-database-foundation/05-05-SUMMARY.md` ghi rõ V1 fields preserved (note), Phase 8 deferred items (per-item, shipping, payment).
</output>
