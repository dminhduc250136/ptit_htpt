---
phase: 05-database-foundation
plan: 09
type: execute
wave: 5
depends_on: [08]
files_modified:
  - sources/frontend/src/mock-data/products.ts
  - sources/frontend/src/mock-data/orders.ts
  - sources/frontend/src/mock-data/
  - sources/frontend/src/**/*.ts (files importing @/mock-data — to be rewired/cleaned)
  - .planning/phases/05-database-foundation/baseline/fe-flow-evidence.md
autonomous: false
requirements: [DB-06]
must_haves:
  truths:
    - "Folder `sources/frontend/src/mock-data/` đã bị xóa khỏi git tracking"
    - "`grep -r \"mock-data\" sources/frontend/src/` không trả kết quả nào (ngoại trừ comments lịch sử nếu có — tốt nhất là 0)"
    - "`npm run build` (Next.js) trong `sources/frontend` xanh sau khi xóa mock-data"
    - "FE flow chính (browse → product detail → add to cart → checkout → confirmation) PASS bằng data thật từ DB qua gateway"
    - "Playwright spec subset (nếu tồn tại) trên flow chính PASS — hoặc, nếu spec đang assert UUID hardcoded từ mock-data, được update để query data-driven"
  artifacts:
    - path: ".planning/phases/05-database-foundation/baseline/fe-flow-evidence.md"
      provides: "Screenshot/log evidence của FE flow chính PASS với seeded data"
  key_links:
    - from: "FE pages (e.g., /products)"
      to: "API gateway /api/products"
      via: "FE typed module (Springdoc OpenAPI codegen output)"
      pattern: "fetch.*api/products|listProducts"
---

<objective>
Wave 5 (final cleanup): xóa `sources/frontend/src/mock-data/` folder + grep tất cả import references → gỡ hoặc rewire sang typed API modules; verify FE build xanh + flow chính (browse → cart → checkout → confirmation) hoạt động với seeded data thật từ Postgres.

**CRITICAL:** Plan này CHỈ chạy sau khi Plan 08 sign-off PASS. Nếu Wave 4 evidence missing/fail → STOP, không xóa mock-data (Pitfall: aggressive deletion break FE midway — D-07 lock).

**Scope-cut clarification:** Phase 5 KHÔNG rewire admin pages hoặc /search (đó là Phase 7 UI-01..04). Phase 5 chỉ đảm bảo `npm run build` xanh + flow chính (browse/cart/checkout/confirm) PASS sau khi xóa mock-data. Nếu admin pages hiện đang import mock-data và sẽ vỡ build, Phase 5 phải replace bằng stub minimal (e.g., empty array fallback `[]` + TODO comment Phase 7) — KHÔNG cần wire lên gateway.

Output: Folder mock-data deleted; FE build xanh; flow chính evidence captured.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/05-database-foundation/05-CONTEXT.md (D-07: xóa mock-data CUỐI cùng)
@.planning/phases/05-database-foundation/05-RESEARCH.md (Pitfall 8: Playwright UUID hardcoded; Pitfall 9: codegen whitespace diff)
@.planning/phases/05-database-foundation/baseline/integration-evidence.md (Wave 4 sign-off)

<interfaces>
Mock-data folder structure: `sources/frontend/src/mock-data/{products.ts, orders.ts}`.
FE typed API modules từ Springdoc codegen: thường tại `sources/frontend/src/lib/api/` hoặc `sources/frontend/src/services/`.
Flow chính UI routes (xác nhận khi đọc FE codebase):
  - /products (or /) — browse
  - /products/[slug] — product detail
  - /cart — cart page (hoặc cart drawer)
  - /checkout — checkout form
  - /checkout/success or /order/{id} — confirmation
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 9.1: Grep + audit tất cả import references mock-data; rewire flow chính sang typed API; stub admin pages defer Phase 7</name>
  <files>sources/frontend/src/**/*.ts, sources/frontend/src/**/*.tsx</files>
  <read_first>
    - sources/frontend/src/mock-data/products.ts
    - sources/frontend/src/mock-data/orders.ts
    - Output `grep -rn "mock-data" sources/frontend/src/` (run before edits)
    - sources/frontend/src/ structure (xác định nơi typed API client)
    - .planning/phases/05-database-foundation/05-RESEARCH.md §Decision #8 (Risk identified)
  </read_first>
  <action>
    **(a) Audit imports:**
    ```bash
    grep -rn "mock-data\|from '@/mock-data\|from \"@/mock-data" sources/frontend/src/ > /tmp/mock-imports.txt
    cat /tmp/mock-imports.txt
    ```

    Phân loại các matches thành 3 nhóm:
    - **Flow chính (rewire bắt buộc):** files thuộc browse/cart/checkout/confirmation pages (e.g., `app/page.tsx`, `app/products/[slug]/page.tsx`, `app/checkout/...`, `components/Cart.tsx`).
    - **Admin pages (stub defer Phase 7):** files thuộc `app/admin/*` (UI-02..04 sẽ wire ở Phase 7).
    - **Search page (stub defer Phase 7):** `app/search/...` (UI-01).

    **(b) Rewire flow chính** — replace `import { products } from '@/mock-data/products'` thành call typed API qua gateway. Pattern:
    ```typescript
    // OLD:
    import { products } from '@/mock-data/products';

    // NEW (server component / route handler):
    const response = await fetch(`${process.env.NEXT_PUBLIC_API_BASE_URL}/api/products`, { cache: 'no-store' });
    const products = await response.json();
    ```

    HOẶC dùng existing typed module (preferred — kiểm tra `services/products.ts` nếu Springdoc codegen đã tạo `listProducts()`):
    ```typescript
    import { listProducts } from '@/services/products';
    const products = await listProducts({ page: 0, size: 50 });
    ```

    Cùng pattern cho orders trên `/account/orders` nếu thuộc flow chính.

    **(c) Stub admin + search pages** — replace import mock-data bằng empty array với TODO comment:
    ```typescript
    // TODO Phase 7 (UI-02): wire to listProducts(admin scope) qua gateway
    const products: Product[] = [];
    ```

    KHÔNG xóa file admin/search — chỉ stub data. Build phải xanh.

    **(d) Verify FE build:**
    ```bash
    cd sources/frontend
    npm install   # nếu cần
    npm run build
    ```

    Nếu build fail → check stub types khớp Product interface; fix import paths.

    **(e) Re-grep verify:**
    ```bash
    grep -rn "mock-data" sources/frontend/src/
    ```
    Expect: 0 kết quả (hoặc chỉ comment lịch sử — log lại trong evidence).
  </action>
  <verify>
    <automated>cd sources/frontend && npm run build 2>&1 | tail -20 && ! grep -rn "from '@/mock-data\|from \"@/mock-data" sources/frontend/src/</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exit 0 (FE build xanh)
    - `grep -rn "from '@/mock-data\|from \"@/mock-data" sources/frontend/src/` trả 0 results
    - Flow chính pages (browse/detail/cart/checkout/confirm) sử dụng typed API call hoặc fetch qua gateway
    - Admin/search pages có stub `[]` + TODO Phase 7 comment (KHÔNG còn import mock-data)
  </acceptance_criteria>
  <done>FE build xanh; tất cả import mock-data đã được xử lý.</done>
</task>

<task type="auto">
  <name>Task 9.2: Xóa folder mock-data + Playwright spec audit</name>
  <files>sources/frontend/src/mock-data/products.ts, sources/frontend/src/mock-data/orders.ts, sources/frontend/src/mock-data/</files>
  <read_first>
    - Output Task 9.1 (đã verify zero imports)
    - sources/frontend/tests/ hoặc sources/frontend/e2e/ hoặc playwright/ — search Playwright spec subset (browse/cart/checkout/confirm)
    - .planning/phases/05-database-foundation/05-RESEARCH.md Pitfall 8 (Playwright UUID hardcoded)
  </read_first>
  <action>
    **(a) Xóa folder mock-data:**
    ```bash
    git rm -r sources/frontend/src/mock-data/
    ```

    **(b) Audit Playwright specs:**
    ```bash
    grep -rn "abc-123\|mock-product\|hardcoded UUID" sources/frontend/{tests,e2e,playwright,specs}/ 2>/dev/null
    grep -rn "/products/[a-f0-9-]\{36\}" sources/frontend/{tests,e2e,playwright,specs}/ 2>/dev/null
    grep -rn "tai-nghe-sony\|prod-001" sources/frontend/{tests,e2e,playwright,specs}/ 2>/dev/null
    ```

    Nếu spec có hardcoded UUID/slug từ mock cũ:
    - **Option A (preferred):** Update spec dùng data-driven query — spec đầu tiên fetch 1 product từ API, lưu slug/id, dùng cho navigation. Pattern:
      ```typescript
      const products = await page.request.get('/api/products').then(r => r.json());
      const firstSlug = products[0].slug;  // dynamic
      await page.goto(`/products/${firstSlug}`);
      ```
    - **Option B (fallback):** Update spec hardcode về seed data mới: `prod-001`, slug `tai-nghe-sony-wh-1000xm5`.

    Nếu không có Playwright spec nào, ghi note vào evidence.

    **(c) Re-build FE để confirm zero broken imports:**
    ```bash
    cd sources/frontend && npm run build
    ```
  </action>
  <verify>
    <automated>test ! -d sources/frontend/src/mock-data && cd sources/frontend && npm run build 2>&1 | grep -qE "Compiled successfully|build successful|build complete"</automated>
  </verify>
  <acceptance_criteria>
    - Folder `sources/frontend/src/mock-data/` không còn tồn tại trên filesystem
    - `git status` cho thấy folder đã được rm-tracked
    - `npm run build` vẫn xanh sau xóa
    - Nếu Playwright spec có hardcoded mock UUIDs/slugs, đã update sang seed mới hoặc data-driven
  </acceptance_criteria>
  <done>Mock-data folder gone; FE build still green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 9.3: FE flow chính manual verify với data thật từ DB</name>
  <what-built>
    Stack `docker compose up -d` đang chạy với Postgres + 5 services + gateway + frontend; mock-data folder đã xóa; FE build xanh; flow chính rewired sang gateway API.
  </what-built>
  <how-to-verify>
    Đảm bảo stack đang up. Mở browser tại FE URL (thường http://localhost:3000) và thực hiện flow:

    1. **Browse:** mở `/` hoặc `/products` → expect: nhìn thấy 10 sản phẩm với tên Vietnamese từ seed (Tai nghe Sony, Bàn phím Keychron, Áo thun cotton, ...). Nếu hiển thị 0 hoặc tên cũ từ mock → fail.

    2. **Product detail:** click 1 product (e.g., "Tai nghe bluetooth Sony WH-1000XM5") → URL chuyển sang `/products/tai-nghe-sony-wh-1000xm5` (hoặc tương tự) → expect: detail page render với name + price `7,990,000` + category Điện tử.

    3. **Add to cart:** click "Thêm vào giỏ" → cart UI update (badge count tăng / drawer mở) — verify có item.

    4. **Checkout:** đến `/checkout` (hoặc nút "Thanh toán") → form load (có thể chưa cần submit thật vì payment-service Phase 5 chưa wire vào checkout chain — đó là defer D2 v1.2). Nếu checkout submit fail vì payment, đó là OK miễn không phải lỗi mock-data.

    5. **Confirmation:** sau checkout → `/checkout/success` hoặc `/account/orders/{id}` render xác nhận. Phase 5 chấp nhận confirmation page hiển thị mock-style nếu order persistence chưa wire (Phase 8 PERSIST-01..03). Mục tiêu Phase 5 là "không vỡ", không phải "full real breakdown".

    6. **Playwright (nếu có):** chạy
       ```bash
       cd sources/frontend && npx playwright test --grep "browse|cart|checkout"
       ```
       Pass = OK. Fail có thể acceptable nếu Phase 7/8 sẽ fix.

    7. **Capture evidence:** screenshot 5 màn hình trên (hoặc copy console logs network requests cho thấy gọi `/api/products` qua gateway). Append vào `.planning/phases/05-database-foundation/baseline/fe-flow-evidence.md`.

    Phê duyệt nếu flow chính không vỡ; reject nếu browse/detail vỡ vì gateway/data thật không match expectations.
  </how-to-verify>
  <resume-signal>Type "approved" nếu flow chính PASS; type "issue: <description>" nếu có lỗi blocker (KHÔNG count: payment chưa wire, order persistence chưa full)</resume-signal>
</task>

</tasks>

<verification>
- Folder mock-data deleted from git
- `npm run build` xanh
- `grep mock-data` zero results
- FE flow chính verify approved
</verification>

<success_criteria>
1. `sources/frontend/src/mock-data/` folder đã bị xóa khỏi repo
2. FE build xanh sau xóa
3. FE flow chính (browse → cart → checkout → confirmation) PASS với seeded data thật
4. Evidence file `fe-flow-evidence.md` đầy đủ + manual sign-off
5. Phase 5 closed: 19/19 success criteria DB-01..DB-06 đạt
</success_criteria>

<output>
Create `.planning/phases/05-database-foundation/05-09-SUMMARY.md` ghi rõ files rewired (flow chính), files stubbed (admin/search defer Phase 7), Playwright spec changes nếu có. Đây cũng là PHASE 5 closing summary — link tới integration-evidence.md + fe-flow-evidence.md.
</output>
