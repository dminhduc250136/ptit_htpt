---
phase: 05-database-foundation
plan: 09
type: execute
wave: 5
depends_on: [08]
files_modified:
  - sources/frontend/src/mock-data/products.ts
  - sources/frontend/src/mock-data/orders.ts
  - sources/frontend/src/mock-data/
  - sources/frontend/src/**/*.ts (files importing @/mock-data — to be rewired/cleaned)
  - .planning/phases/05-database-foundation/baseline/fe-flow-evidence.md
autonomous: false
requirements: [DB-06]
must_haves:
  truths:
    - "Folder `sources/frontend/src/mock-data/` đã bị xóa khỏi git tracking"
    - "`grep -r \"mock-data\" sources/frontend/src/` không trả kết quả nào (ngoại trừ comments lịch sử nếu có — tốt nhất là 0)"
    - "`npm run build` (Next.js) trong `sources/frontend` xanh sau khi xóa mock-data"
    - "FE flow VISIBLE-FIRST (browse danh mục, product detail, add-to-cart) PASS bằng seeded data thật từ DB qua gateway (must-pass per ROADMAP SC#5)"
    - "FE checkout + confirmation routes render-without-crash (build xanh + route reachable + không 5xx) — full breakdown defer Phase 8 PERSIST-01..03"
    - "Playwright spec subset (nếu tồn tại) trên flow chính PASS — hoặc, nếu spec đang assert UUID hardcoded từ mock-data, được update để query data-driven"
  artifacts:
    - path: ".planning/phases/05-database-foundation/baseline/fe-flow-evidence.md"
      provides: "Screenshot/log evidence của FE flow chính PASS với seeded data + checkout/confirmation render-without-crash evidence"
  key_links:
    - from: "FE pages (e.g., /products)"
      to: "API gateway /api/products"
      via: "FE typed module (Springdoc OpenAPI codegen output)"
      pattern: "fetch.*api/products|listProducts"
---

<objective>
Wave 5 (final cleanup): xóa `sources/frontend/src/mock-data/` folder + grep tất cả import references → gỡ hoặc rewire sang typed API modules; verify FE build xanh + flow VISIBLE-FIRST (browse → product detail → add-to-cart) hoạt động với seeded data thật từ Postgres + checkout/confirmation render-without-crash.

**CRITICAL:** Plan này CHỈ chạy sau khi Plan 08 sign-off PASS. Nếu Wave 4 evidence missing/fail → STOP, không xóa mock-data (Pitfall: aggressive deletion break FE midway — D-07 lock).

**Scope alignment với ROADMAP SC#5 (đã update):**
- **MUST PASS (blocking):** browse danh mục (home/products page) renders seeded products từ Postgres; product detail renders cho ≥ 1 seeded product; add-to-cart action succeeds (cart line appears) cho seeded product. Đây là cam kết visible-first của Phase 5.
- **MUST NOT VỠ (non-blocking nhưng verify):** /checkout + confirmation routes render without runtime errors / 5xx; build phải xanh + route phải reachable. Page có thể show empty/placeholder state nếu persistence chưa wire — Phase 5 chỉ commits "render-without-crash"; full-real breakdown là Phase 8 PERSIST-01..03.
- Phase 5 KHÔNG rewire admin pages hoặc /search (đó là Phase 7 UI-01..04). Phase 5 chỉ đảm bảo `npm run build` xanh + visible flow PASS sau khi xóa mock-data. Nếu admin pages hiện đang import mock-data và sẽ vỡ build, replace bằng stub minimal (e.g., empty array fallback `[]` + TODO comment Phase 7) — KHÔNG cần wire lên gateway.

Output: Folder mock-data deleted; FE build xanh; visible-first flow evidence captured + checkout/confirm render-without-crash evidence captured.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/05-database-foundation/05-CONTEXT.md (D-07: xóa mock-data CUỐI cùng)
@.planning/phases/05-database-foundation/05-RESEARCH.md (Pitfall 8: Playwright UUID hardcoded; Pitfall 9: codegen whitespace diff)
@.planning/phases/05-database-foundation/baseline/integration-evidence.md (Wave 4 sign-off)
@.planning/ROADMAP.md (Phase 5 SC#5 — visible-first commitment)

<interfaces>
Mock-data folder structure: `sources/frontend/src/mock-data/{products.ts, orders.ts}`.
FE typed API modules từ Springdoc codegen: thường tại `sources/frontend/src/lib/api/` hoặc `sources/frontend/src/services/`.
Flow chính UI routes (xác nhận khi đọc FE codebase):
  - /products (or /) — browse                  [MUST PASS]
  - /products/[slug] — product detail          [MUST PASS]
  - /cart — cart page (hoặc cart drawer)       [MUST PASS — add-to-cart]
  - /checkout — checkout form                  [MUST NOT VỠ — render only]
  - /checkout/success or /order/{id} — confirm [MUST NOT VỠ — render only]
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 9.1: Grep + audit tất cả import references mock-data; rewire flow chính sang typed API; stub admin pages defer Phase 7</name>
  <files>sources/frontend/src/**/*.ts, sources/frontend/src/**/*.tsx</files>
  <read_first>
    - sources/frontend/src/mock-data/products.ts
    - sources/frontend/src/mock-data/orders.ts
    - Output `grep -rn "mock-data" sources/frontend/src/` (run before edits)
    - sources/frontend/src/ structure (xác định nơi typed API client)
    - .planning/phases/05-database-foundation/05-RESEARCH.md §Decision #8 (Risk identified)
  </read_first>
  <action>
    **(a) Audit imports:**
    ```bash
    grep -rn "mock-data\|from '@/mock-data\|from \"@/mock-data" sources/frontend/src/ > /tmp/mock-imports.txt
    cat /tmp/mock-imports.txt
    ```

    Phân loại các matches thành 3 nhóm:
    - **Flow chính (rewire bắt buộc):** files thuộc browse/cart/checkout/confirmation pages (e.g., `app/page.tsx`, `app/products/[slug]/page.tsx`, `app/checkout/...`, `components/Cart.tsx`).
    - **Admin pages (stub defer Phase 7):** files thuộc `app/admin/*` (UI-02..04 sẽ wire ở Phase 7).
    - **Search page (stub defer Phase 7):** `app/search/...` (UI-01).

    **(b) Rewire flow chính** — replace `import { products } from '@/mock-data/products'` thành call typed API qua gateway. Pattern:
    ```typescript
    // OLD:
    import { products } from '@/mock-data/products';

    // NEW (server component / route handler):
    const response = await fetch(`${process.env.NEXT_PUBLIC_API_BASE_URL}/api/products`, { cache: 'no-store' });
    const products = await response.json();
    ```

    HOẶC dùng existing typed module (preferred — kiểm tra `services/products.ts` nếu Springdoc codegen đã tạo `listProducts()`):
    ```typescript
    import { listProducts } from '@/services/products';
    const products = await listProducts({ page: 0, size: 50 });
    ```

    Cùng pattern cho orders trên `/account/orders` nếu thuộc flow chính.

    **(c) Stub admin + search pages** — replace import mock-data bằng empty array với TODO comment:
    ```typescript
    // TODO Phase 7 (UI-02): wire to listProducts(admin scope) qua gateway
    const products: Product[] = [];
    ```

    KHÔNG xóa file admin/search — chỉ stub data. Build phải xanh.

    **(d) Verify FE build:**
    ```bash
    cd sources/frontend
    npm install   # nếu cần
    npm run build
    ```

    Nếu build fail → check stub types khớp Product interface; fix import paths.

    **(e) Re-grep verify:**
    ```bash
    grep -rn "mock-data" sources/frontend/src/
    ```
    Expect: 0 kết quả (hoặc chỉ comment lịch sử — log lại trong evidence).
  </action>
  <verify>
    <automated>cd sources/frontend && npm run build 2>&1 | tail -20 && ! grep -rn "from '@/mock-data\|from \"@/mock-data" sources/frontend/src/</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exit 0 (FE build xanh)
    - `grep -rn "from '@/mock-data\|from \"@/mock-data" sources/frontend/src/` trả 0 results
    - Flow chính pages (browse/detail/cart/checkout/confirm) sử dụng typed API call hoặc fetch qua gateway
    - Admin/search pages có stub `[]` + TODO Phase 7 comment (KHÔNG còn import mock-data)
  </acceptance_criteria>
  <done>FE build xanh; tất cả import mock-data đã được xử lý.</done>
</task>

<task type="auto">
  <name>Task 9.2: Xóa folder mock-data + Playwright spec audit</name>
  <files>sources/frontend/src/mock-data/products.ts, sources/frontend/src/mock-data/orders.ts, sources/frontend/src/mock-data/</files>
  <read_first>
    - Output Task 9.1 (đã verify zero imports)
    - sources/frontend/tests/ hoặc sources/frontend/e2e/ hoặc playwright/ — search Playwright spec subset (browse/cart/checkout/confirm)
    - .planning/phases/05-database-foundation/05-RESEARCH.md Pitfall 8 (Playwright UUID hardcoded)
  </read_first>
  <action>
    **(a) Xóa folder mock-data:**
    ```bash
    git rm -r sources/frontend/src/mock-data/
    ```

    **(b) Audit Playwright specs:**
    ```bash
    grep -rn "abc-123\|mock-product\|hardcoded UUID" sources/frontend/{tests,e2e,playwright,specs}/ 2>/dev/null
    grep -rn "/products/[a-f0-9-]\{36\}" sources/frontend/{tests,e2e,playwright,specs}/ 2>/dev/null
    grep -rn "tai-nghe-sony\|prod-001" sources/frontend/{tests,e2e,playwright,specs}/ 2>/dev/null
    ```

    Nếu spec có hardcoded UUID/slug từ mock cũ:
    - **Option A (preferred):** Update spec dùng data-driven query — spec đầu tiên fetch 1 product từ API, lưu slug/id, dùng cho navigation. Pattern:
      ```typescript
      const products = await page.request.get('/api/products').then(r => r.json());
      const firstSlug = products[0].slug;  // dynamic
      await page.goto(`/products/${firstSlug}`);
      ```
    - **Option B (fallback):** Update spec hardcode về seed data mới: `prod-001`, slug `tai-nghe-sony-wh-1000xm5`.

    Nếu không có Playwright spec nào, ghi note vào evidence.

    **(c) Re-build FE để confirm zero broken imports:**
    ```bash
    cd sources/frontend && npm run build
    ```
  </action>
  <verify>
    <automated>test ! -d sources/frontend/src/mock-data && cd sources/frontend && npm run build 2>&1 | grep -qE "Compiled successfully|build successful|build complete"</automated>
  </verify>
  <acceptance_criteria>
    - Folder `sources/frontend/src/mock-data/` không còn tồn tại trên filesystem
    - `git status` cho thấy folder đã được rm-tracked
    - `npm run build` vẫn xanh sau xóa
    - Nếu Playwright spec có hardcoded mock UUIDs/slugs, đã update sang seed mới hoặc data-driven
  </acceptance_criteria>
  <done>Mock-data folder gone; FE build still green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 9.3: FE visible-first flow manual verify với data thật từ DB (per ROADMAP SC#5)</name>
  <what-built>
    Stack `docker compose up -d` đang chạy với Postgres + 5 services + gateway + frontend; mock-data folder đã xóa; FE build xanh; visible flow rewired sang gateway API; checkout/confirmation routes reachable (có thể chưa wire full persistence — Phase 8).
  </what-built>
  <how-to-verify>
    Đảm bảo stack đang up. Mở browser tại FE URL (thường http://localhost:3000) và thực hiện 2 nhóm verify riêng biệt:

    ## NHÓM A — MUST PASS (blocking; nếu fail → reject checkpoint)

    1. **Browse danh mục:** mở `/` hoặc `/products` → expect: render từ seeded Postgres data, nhìn thấy 10 sản phẩm với tên Vietnamese từ seed (Tai nghe Sony, Bàn phím Keychron, Áo thun cotton, ...). Verify network tab: request đi qua gateway `/api/products` (KHÔNG phải bundled mock). Nếu hiển thị 0 sản phẩm hoặc tên cũ từ mock → FAIL.

    2. **Product detail:** click ≥ 1 product (e.g., "Tai nghe bluetooth Sony WH-1000XM5") → URL chuyển sang `/products/tai-nghe-sony-wh-1000xm5` (hoặc tương tự) → expect: detail page render với name + price `7,990,000` + category Điện tử từ seeded data. Nếu page vỡ / 404 / hiển thị mock → FAIL.

    3. **Add to cart:** ở product detail (hoặc product card), click "Thêm vào giỏ" → cart UI update (badge count tăng / drawer mở / cart line appears) → verify cart có item với name + price khớp seeded product. Nếu add-to-cart action không hoạt động → FAIL.

    ## NHÓM B — MUST NOT VỠ (non-blocking; render-without-crash only)

    4. **Checkout route reachable:** đến `/checkout` (hoặc nút "Thanh toán") → expect: route trả 2xx HTTP, page render KHÔNG có runtime error / 5xx / unhandled exception. Page có thể show empty form, placeholder, hoặc "chưa wire" state — đó là OK. Phase 5 chỉ cam kết "không vỡ build/render"; full submit flow defer Phase 8.

    5. **Confirmation route reachable:** thử navigate `/checkout/success` hoặc `/account/orders/{id}` (manual URL nếu cần) → expect: route trả 2xx, page render không crash. Nếu page show empty/placeholder do persistence chưa wire → OK (Phase 8 PERSIST-01..03 sẽ fix). Nếu page throw runtime error / 5xx / build vỡ → cần investigate (có thể do mock-data import sót).

    ## NHÓM C — Automated guards (chạy trước khi sign-off)

    6. **Build & grep guards:**
       ```bash
       cd sources/frontend && npm run build  # MUST exit 0
       grep -r "mock-data" sources/frontend/src/ ; echo "exit=$?"  # MUST exit 1 (no matches)
       ```

    7. **Route reachability curl** (FE up tại :3000):
       ```bash
       for path in / /products /checkout /checkout/success; do
         code=$(curl -s -o /dev/null -w "%{http_code}" "http://localhost:3000${path}")
         echo "${path} -> ${code}"  # expect 200 hoặc 2xx/3xx, KHÔNG 5xx
       done
       ```

    8. **Playwright (nếu có spec):** chạy
       ```bash
       cd sources/frontend && npx playwright test --grep "browse|detail|cart"
       ```
       Pass = OK. Fail trên browse/detail/cart → blocking (Nhóm A). Fail trên checkout/confirm submit flow → non-blocking (Phase 8).

    ## Capture evidence

    9. Screenshot 5 màn hình + paste output Nhóm C (build + grep + curl) vào `.planning/phases/05-database-foundation/baseline/fe-flow-evidence.md`. Ghi rõ acceptance: "Phase 5 commits to render-without-crash for /checkout + /checkout/success; full-real breakdown is Phase 8 PERSIST-01..03".

    Phê duyệt nếu Nhóm A PASS + Nhóm B render-without-crash + Nhóm C guards xanh.
    Reject nếu Nhóm A fail (browse/detail/cart vỡ) hoặc Nhóm C automated guards fail (build vỡ / mock-data còn / route 5xx).
  </how-to-verify>
  <acceptance_criteria>
    - **Nhóm A (must pass):** browse render seeded products từ Postgres, product detail render ≥ 1 seeded product, add-to-cart succeeds với cart line appears
    - **Nhóm B (must not vỡ):** /checkout + /checkout/success routes trả 2xx (không 5xx) và render KHÔNG runtime error; placeholder/empty state acceptable
    - **Nhóm C (automated):** `npm run build` exit 0; `grep -r "mock-data" sources/frontend/src/` exit 1 (no matches); `curl` cho `/`, `/products`, `/checkout`, `/checkout/success` trả status code không phải 5xx
    - Evidence file `fe-flow-evidence.md` chứa screenshots + output Nhóm C + ghi chú "Phase 5 = render-without-crash, full breakdown defer Phase 8 PERSIST-01..03"
  </acceptance_criteria>
  <resume-signal>Type "approved" nếu Nhóm A PASS + Nhóm B render-without-crash + Nhóm C guards xanh; type "issue: <description>" nếu Nhóm A fail hoặc Nhóm C fail. KHÔNG count fail: checkout submit thật chưa wire, order persistence chưa full breakdown (đó là Phase 8 scope).</resume-signal>
</task>

</tasks>

<verification>
- Folder mock-data deleted from git
- `npm run build` xanh
- `grep mock-data` zero results
- FE visible-first flow (browse/detail/cart) verify approved
- Checkout + confirmation routes render-without-crash verified
</verification>

<success_criteria>
1. `sources/frontend/src/mock-data/` folder đã bị xóa khỏi repo
2. FE build xanh sau xóa
3. FE visible-first flow (browse → detail → add-to-cart) PASS với seeded data thật (per ROADMAP SC#5)
4. /checkout + /checkout/success routes render-without-crash (build xanh + reachable + không 5xx)
5. Evidence file `fe-flow-evidence.md` đầy đủ + manual sign-off
6. Phase 5 closed: 19/19 success criteria DB-01..DB-06 đạt (full breakdown checkout = Phase 8)
</success_criteria>

<output>
Create `.planning/phases/05-database-foundation/05-09-SUMMARY.md` ghi rõ files rewired (visible-first flow), files stubbed (admin/search defer Phase 7), checkout/confirm render-without-crash status, Playwright spec changes nếu có. Đây cũng là PHASE 5 closing summary — link tới integration-evidence.md + fe-flow-evidence.md.
</output>
