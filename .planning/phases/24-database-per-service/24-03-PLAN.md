# Phase 24 — Plan 03: Seed migration V102 độc lập + FE chat-schema-init refactor

**Phase:** 24
**Plan:** 03 of 04
**Depends on:** 24-02

## Goal

- V102 seed inventory (nếu Phase 23 đã merge tạo file dạng JOIN cross-schema) → refactor sang dữ liệu hardcoded độc lập, không JOIN cross-DB.
- FE `services/chat-schema-init.ts` (Phase 22) chuyển DB target sang `postgres-chat`, schema init độc lập (không assume shared postgres).

Reference: 24-CONTEXT.md D-08, D-09.

## Tasks

### T-24-03-01: Audit V102 trạng thái

- Đọc `sources/backend/inventory-service/src/main/resources/db/seed-dev/V102__seed_initial_inventory.sql` (nếu tồn tại).
- Nếu file dùng `INSERT INTO inventory_items SELECT id, stock FROM product_svc.products` → cross-DB không khả thi nữa → cần refactor.
- Nếu chưa có file → Phase 23 chưa merge → bỏ qua T-24-03-02; skip sang T-24-03-03.

### T-24-03-02: Refactor V102 sang dữ liệu hardcoded

- Đọc `sources/backend/product-service/src/main/resources/db/seed-dev/V101__seed_catalog_realistic.sql` để lấy danh sách `(id, stock)` của 100 sản phẩm seed.
- Viết script tạm (PowerShell hoặc Node) parse V101 → xuất 100 INSERT statement dạng `INSERT INTO inventory_items (product_id, quantity) VALUES ('<uuid>', <stock>) ON CONFLICT (product_id) DO NOTHING;`.
- Replace nội dung V102 bằng INSERT list này. Header comment ghi rõ "Snapshot từ V101 product seed, regenerate khi V101 đổi".
- Verify: `docker compose down -v && docker compose up -d` → `docker exec postgres-inventory psql -U inventory_svc -d inventory_svc -c "SELECT count(*) FROM inventory_items"` → 100.

### T-24-03-03: FE chat-schema-init refactor

- Đọc `sources/frontend/src/services/chat-schema-init.ts` (hoặc tương đương — grep `chat_sessions\|chat_messages`).
- Connection pool đang dùng env `DB_HOST` chung. Đảm bảo nó đọc đúng env mới (`postgres-chat`) — Plan 01 đã set FE env vars.
- Schema init SQL `CREATE TABLE IF NOT EXISTS chat_sessions ...` → KHÔNG cần đổi (chỉ chạy trên DB chat_svc thay vì tmdt cũ).
- Verify: chạy FE `docker compose up -d frontend` → trigger 1 chat request → `docker exec postgres-chat psql -U chat_svc -d chat_svc -c "\dt"` → có 2 bảng `chat_sessions`, `chat_messages`.

### T-24-03-04: Cleanup migration cũ shared

- Nếu Flyway history table cũ tồn tại trong DB cũ (đã bị xóa cùng volume `tmdt-pgdata`), không cần action.
- Verify mỗi DB mới có `flyway_schema_history` riêng + Flyway baseline đúng.

## Acceptance

- V102 inventory seed (nếu có) chạy clean trên `postgres-inventory` đơn độc, count = 100.
- FE chat dùng `postgres-chat`, schema init tạo 2 bảng thành công.
- `docker exec postgres-product psql -U product_svc -d product_svc -c "SELECT count(*) FROM products"` = 100.
- `docker exec postgres-inventory psql -U inventory_svc -d inventory_svc -c "SELECT count(*) FROM inventory_items"` = 100 (nếu V102 áp dụng).

## Commit

`feat(24): V102 seed độc lập + FE chat dùng postgres-chat container`
