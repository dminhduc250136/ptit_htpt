---
phase: 09-residual-closure-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sources/frontend/middleware.ts
  - sources/frontend/src/middleware.ts
autonomous: false
requirements:
  - AUTH-06
must_haves:
  truths:
    - "User chưa login truy cập /profile/orders → 307 redirect /login?returnTo=/profile/orders, KHÔNG flash content"
    - "User chưa login truy cập /admin → 307 redirect /login?returnTo=/admin"
    - "User chưa login truy cập /checkout → 307 redirect /login?returnTo=/checkout"
    - "User logged-in role=USER truy cập /admin → 307 redirect /403 (giữ logic Phase 6 D-09)"
    - "Chỉ tồn tại 1 file middleware (sources/frontend/src/middleware.ts), file root middleware.ts đã xóa"
  artifacts:
    - path: "sources/frontend/src/middleware.ts"
      provides: "Canonical Edge middleware bảo vệ /admin, /profile, /account, /checkout"
      contains: "matcher: ['/admin/:path*', '/account/:path*', '/profile/:path*', '/checkout/:path*']"
    - path: "sources/frontend/middleware.ts"
      provides: "DELETED — stale duplicate (D-01)"
  key_links:
    - from: "Browser direct visit /profile/orders"
      to: "src/middleware.ts"
      via: "Next.js Edge runtime matcher"
      pattern: "matcher.*profile"
    - from: "src/middleware.ts"
      to: "/login?returnTo=…"
      via: "NextResponse.redirect"
      pattern: "NextResponse\\.redirect.*login"
---

<objective>
Đóng AUTH-06: hợp nhất middleware về 1 file canonical (`src/middleware.ts`), mở rộng matcher để bảo vệ tất cả page routes nhạy cảm (`/admin`, `/account`, `/profile`, `/checkout`).

Purpose: User chưa login truy cập trực tiếp `/profile/orders` (link share, refresh, bookmark) bị redirect 307 sang `/login?returnTo=…` trước khi React render — KHÔNG flash UI orders rỗng. Đây là gap số 1 trong v1.1 audit (middleware chỉ match `/admin/*`).

Output:
- `sources/frontend/middleware.ts` (root) bị xóa (D-01).
- `sources/frontend/src/middleware.ts` matcher mở rộng + giữ nguyên logic /403 cho non-ADMIN tại `/admin/*`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-residual-closure-verification/09-CONTEXT.md
@sources/frontend/src/middleware.ts
@sources/frontend/middleware.ts
</context>

<interfaces>
<!-- Canonical middleware đã có sẵn ở `sources/frontend/middleware.ts` (root), nhưng D-01 quyết định giữ `src/middleware.ts` làm canonical. Lý do: Next.js prefer `src/middleware.ts` khi cả 2 cùng tồn tại, root file đang dead code. Nội dung canonical sau khi xong PHẢI gồm: -->

```typescript
// sources/frontend/src/middleware.ts (sau Plan 09-01)
import { NextResponse } from 'next/server';
import type { NextRequest } from 'next/server';

export function middleware(req: NextRequest) {
  const { pathname } = req.nextUrl;
  const authPresent = req.cookies.get('auth_present')?.value;

  if (!authPresent) {
    const url = req.nextUrl.clone();
    url.pathname = '/login';
    url.searchParams.set('returnTo', pathname + req.nextUrl.search);
    return NextResponse.redirect(url);
  }

  // Admin route gating (D-03 — giữ logic Phase 6 D-09)
  if (pathname.startsWith('/admin')) {
    const userRole = req.cookies.get('user_role')?.value;
    if (!userRole?.includes('ADMIN')) {
      const url = req.nextUrl.clone();
      url.pathname = '/403';
      return NextResponse.redirect(url);
    }
  }

  return NextResponse.next();
}

export const config = {
  matcher: ['/admin/:path*', '/account/:path*', '/profile/:path*', '/checkout/:path*'],
};
```

<!-- Cookie semantics (Phase 6 D-08):
- `auth_present` (httpOnly hoặc thường): chỉ presence-check (UX), không signature-verify
- `user_role` (non-httpOnly): chứa 'ADMIN' | 'USER' | 'ADMIN,USER'; backend mới là source of truth bảo mật thật
-->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Xóa root middleware duplicate + viết canonical src/middleware.ts</name>
  <files>sources/frontend/middleware.ts (delete), sources/frontend/src/middleware.ts (overwrite)</files>
  <read_first>
    - sources/frontend/middleware.ts (root, sẽ xóa) — verify nội dung khớp expected stale snapshot.
    - sources/frontend/src/middleware.ts (canonical, sẽ overwrite) — verify matcher hiện tại = `['/admin/:path*']`.
    - .planning/phases/09-residual-closure-verification/09-CONTEXT.md §Middleware (D-01..D-03) — locked decisions.
  </read_first>
  <action>
    Bước 1: Xóa file `sources/frontend/middleware.ts` (root, stale duplicate per D-01). Dùng Bash `rm sources/frontend/middleware.ts` (KHÔNG dùng git rm để giữ Write tool quyền).

    Bước 2: Overwrite `sources/frontend/src/middleware.ts` bằng đúng nội dung sau (per D-02 mở matcher + D-03 giữ logic /403):

    ```typescript
    /**
     * Edge middleware — Phase 9 / Plan 09-01 (AUTH-06 closure).
     *
     * D-01: file root `middleware.ts` bị xóa (stale duplicate).
     * D-02: matcher mở rộng cover /admin, /account, /profile, /checkout.
     * D-03: giữ logic /403 redirect cho non-ADMIN tại /admin/* (Phase 6 D-09).
     *
     * NOTE: Edge runtime KHÔNG verify JWT signature — `auth_present` chỉ
     * UX presence-check; `user_role` cookie cho admin gating UX.
     * Bảo mật thật ở backend qua Authorization: Bearer header.
     */
    import { NextResponse } from 'next/server';
    import type { NextRequest } from 'next/server';

    export function middleware(req: NextRequest) {
      const { pathname } = req.nextUrl;
      const authPresent = req.cookies.get('auth_present')?.value;

      if (!authPresent) {
        const url = req.nextUrl.clone();
        url.pathname = '/login';
        url.searchParams.set('returnTo', pathname + req.nextUrl.search);
        return NextResponse.redirect(url);
      }

      if (pathname.startsWith('/admin')) {
        const userRole = req.cookies.get('user_role')?.value;
        if (!userRole?.includes('ADMIN')) {
          const url = req.nextUrl.clone();
          url.pathname = '/403';
          return NextResponse.redirect(url);
        }
      }

      return NextResponse.next();
    }

    export const config = {
      matcher: ['/admin/:path*', '/account/:path*', '/profile/:path*', '/checkout/:path*'],
    };
    ```

    Lưu ý:
    - Giữ `pathname + req.nextUrl.search` để preserve query string khi redirect.
    - KHÔNG encodeURIComponent thủ công — `searchParams.set` đã tự encode (root file cũ encode 2 lần là bug).
    - KHÔNG include `/api/*` trong matcher (D-02 — matcher chỉ list page routes).
  </action>
  <verify>
    <automated>
    # 1. File root đã xóa
    test ! -f sources/frontend/middleware.ts

    # 2. Canonical matcher đúng pattern
    grep -E "matcher:\\s*\\['/admin/:path\\*',\\s*'/account/:path\\*',\\s*'/profile/:path\\*',\\s*'/checkout/:path\\*'\\]" sources/frontend/src/middleware.ts

    # 3. Logic /403 vẫn còn (D-03)
    grep -E "url\\.pathname\\s*=\\s*'/403'" sources/frontend/src/middleware.ts

    # 4. searchParams.set returnTo đúng (KHÔNG manual encode)
    grep -E "searchParams\\.set\\('returnTo'" sources/frontend/src/middleware.ts

    # 5. Build pass (Edge runtime syntax check)
    cd sources/frontend && npx next build 2>&1 | tail -20
    </automated>
  </verify>
  <done>
    - File `sources/frontend/middleware.ts` không còn tồn tại trên disk.
    - File `sources/frontend/src/middleware.ts` chứa đúng matcher 4 routes + logic /403.
    - `npx next build` exits 0 (Edge runtime compile pass).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual verify direct-visit redirect (4 routes)</name>
  <what-built>
    - `src/middleware.ts` matcher mở rộng `/admin`, `/account`, `/profile`, `/checkout`.
    - Root duplicate `middleware.ts` đã xóa.
    - Logic /403 cho non-ADMIN giữ nguyên.
  </what-built>
  <how-to-verify>
    Yêu cầu stack đang chạy: `cd sources && docker compose up -d && cd frontend && npm run dev` (port 3000).

    Mở **trình duyệt incognito/private mới (KHÔNG cookie)** và test 4 case:

    1. Truy cập `http://localhost:3000/profile/orders`
       → Kỳ vọng: URL bar nhảy sang `http://localhost:3000/login?returnTo=%2Fprofile%2Forders`, KHÔNG flash UI `/profile/orders` trước (kiểm tra DevTools Network tab — chỉ có 1 request `/profile/orders` trả 307, KHÔNG có HTML render).

    2. Truy cập `http://localhost:3000/checkout`
       → Kỳ vọng: redirect `http://localhost:3000/login?returnTo=%2Fcheckout`.

    3. Truy cập `http://localhost:3000/admin`
       → Kỳ vọng: redirect `http://localhost:3000/login?returnTo=%2Fadmin`.

    4. Login bằng user role=USER (KHÔNG ADMIN), sau đó truy cập `http://localhost:3000/admin`
       → Kỳ vọng: redirect sang `http://localhost:3000/403` (D-03).

    Take screenshot mỗi case lưu vào `e2e/screenshots/09-01-AUTH-06-{case}.png` (4 file). Optional — không bắt buộc nếu manual quan sát đủ rõ.
  </how-to-verify>
  <acceptance_criteria>
    - 4/4 case PASS đúng kỳ vọng.
    - DevTools Network tab xác nhận status 307 trên request đầu, KHÔNG có 200 với HTML protected page trước redirect.
    - `/profile/orders` (chưa login) → URL `?returnTo=%2Fprofile%2Forders` (URL-encoded forward slash).
    - User USER vào `/admin` → URL bar `/403` (KHÔNG `/login`).
  </acceptance_criteria>
  <resume-signal>Type "approved" hoặc mô tả issue (vd "case 4 vẫn redirect /login thay vì /403").</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Next.js Edge runtime | Cookie `auth_present` + `user_role` từ client; có thể tampered. Middleware chỉ presence-check (UX), KHÔNG security enforcement thật. |
| Next.js Edge → Backend (qua /api/* fetch) | Backend bảo mật thật qua Authorization: Bearer header verify JWT signature. Middleware bypass không escalate quyền API. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01-01 | Spoofing | `auth_present` cookie | accept | Cookie chỉ UX presence-check (Phase 6 D-08 documented). User tampered cookie → vào `/profile/*` thấy UI rỗng vì backend reject 401 thiếu Bearer token. |
| T-09-01-02 | Elevation of Privilege | `user_role=ADMIN` cookie tampered | accept | Edge runtime KHÔNG verify JWT signature (Edge limitation). User self-set `user_role=ADMIN` cookie → bypass UI redirect /403, nhưng admin API endpoints vẫn reject 403 vì backend verify JWT roles claim. Documented Phase 6. |
| T-09-01-03 | Information Disclosure | Flash protected UI trước redirect | mitigate | Matcher mở rộng `/admin`, `/account`, `/profile`, `/checkout` per D-02 — Edge middleware chạy TRƯỚC React render, NextResponse.redirect ngắt request → KHÔNG flash. |
| T-09-01-04 | Tampering | returnTo open redirect | mitigate | `returnTo` chỉ chứa `pathname + search` từ same-origin request (`req.nextUrl.pathname`), KHÔNG nhận user-controlled URL. Login page khi consume returnTo PHẢI verify start với `/` (relative). Note cho Phase 10 login flow. |
</threat_model>

<verification>
- `test ! -f sources/frontend/middleware.ts` PASS
- `grep` matcher 4 routes PASS
- `grep` /403 redirect PASS
- `npx next build` exits 0
- 4 case manual checkpoint approved
</verification>

<success_criteria>
- AUTH-06 đóng: direct-visit `/profile/orders` (chưa login) → 307 `/login?returnTo=%2Fprofile%2Forders` (Success Criteria #1 của Phase 9).
- Codebase chỉ còn 1 file middleware (canonical `src/middleware.ts`).
- Logic /403 cho non-ADMIN không hồi quy.
</success_criteria>

<output>
Sau khi xong, tạo `.planning/phases/09-residual-closure-verification/09-01-SUMMARY.md` ghi:
- File deleted: `sources/frontend/middleware.ts`
- Matcher final: `['/admin/:path*', '/account/:path*', '/profile/:path*', '/checkout/:path*']`
- 4 case manual verify pass (kèm screenshot path nếu có)
- Note nhắc Phase 10 login page implement returnTo allowlist (only `/`-prefixed paths) — closure T-09-01-04
</output>
