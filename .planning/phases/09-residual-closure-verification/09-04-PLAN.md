---
phase: 09-residual-closure-verification
plan: 04
type: execute
wave: 2
depends_on:
  - 09-02
  - 09-03
files_modified:
  - sources/frontend/src/app/admin/page.tsx
  - sources/frontend/src/app/admin/page.module.css
  - sources/frontend/src/services/stats.ts
  - sources/frontend/src/services/users.ts
  - sources/frontend/src/app/profile/settings/page.tsx
  - sources/frontend/src/app/profile/settings/page.module.css
  - sources/frontend/src/services/http.ts
  - sources/frontend/src/services/errors.ts
autonomous: false
requirements:
  - UI-02
  - AUTH-07
must_haves:
  truths:
    - "Admin login /admin thấy 4 KPI cards với số THẬT (Total products, Total orders, Total users, Pending orders) thay vì 0"
    - "1 endpoint fail (vd order-svc down) → 3 cards còn lại vẫn render success, card lỗi hiển thị '--' + retry icon"
    - "Click retry icon trên card lỗi → re-fetch CHỈ endpoint đó (KHÔNG reload page)"
    - "Admin dashboard KHÔNG còn totalRevenue card / recent orders table / 'Tổng quan nhanh' panel / mock arrays"
    - "User logged-in vào /profile/settings thấy form đổi password 3 fields (current/new/confirm) — chưa có Phase 10 profile editing yet"
    - "Submit form đúng currentPassword + newPassword hợp lệ + confirm match → success toast 'Đã đổi mật khẩu', form reset"
    - "Submit sai currentPassword → field-level error 'Mật khẩu hiện tại không đúng' tại field 'Mật khẩu hiện tại' (mapped từ errorCode AUTH_INVALID_PASSWORD)"
    - "Validation client-side: newPassword min 8 + ít nhất 1 letter + 1 number + confirm match — submit button disabled cho đến khi valid"
  artifacts:
    - path: "sources/frontend/src/services/stats.ts"
      provides: "fetchProductStats / fetchOrderStats / fetchUserStats — typed wrappers Promise.allSettled"
    - path: "sources/frontend/src/app/admin/page.tsx"
      provides: "Trimmed dashboard với 4 KPI cards + per-card useState + Promise.allSettled"
      contains: "Promise.allSettled"
    - path: "sources/frontend/src/services/users.ts"
      provides: "changeMyPassword(body) → POST /api/users/me/password"
      contains: "changeMyPassword"
    - path: "sources/frontend/src/app/profile/settings/page.tsx"
      provides: "Settings page với password change form (3 fields)"
      contains: "AUTH_INVALID_PASSWORD"
  key_links:
    - from: "Admin dashboard mount"
      to: "Promise.allSettled([fetchProductStats(), fetchOrderStats(), fetchUserStats()])"
      via: "useEffect"
      pattern: "Promise\\.allSettled"
    - from: "Password form submit"
      to: "changeMyPassword({oldPassword, newPassword})"
      via: "POST /api/users/me/password"
      pattern: "changeMyPassword"
    - from: "422 response errorCode"
      to: "Field-level error tại 'Mật khẩu hiện tại'"
      via: "parse error.errorCode === 'AUTH_INVALID_PASSWORD' → setFieldError"
      pattern: "AUTH_INVALID_PASSWORD"
---

<objective>
Đóng frontend half cho UI-02 (admin dashboard wire real KPIs) + AUTH-07 (password change form). Hai feature gom chung 1 plan vì cùng FE concern + size phù hợp 2 task.

Purpose:
1. Admin login thấy số liệu thật ngay landing page — không còn placeholder 0/mock.
2. User logged-in tự đổi password được trong `/profile/settings` (page mới — Phase 10 sẽ extend với fullName/phone/avatar).

Output:
- Service file `stats.ts` với 3 typed wrappers.
- Trimmed `admin/page.tsx` đúng 4 KPI + per-card loading/error state + retry per card.
- Service `users.ts` thêm `changeMyPassword`.
- Page mới `profile/settings/page.tsx` với form 3 fields + manual zod-light validation (KHÔNG cài rhf+zod sớm — Phase 10 mới setup foundation).
- CSS module updates cho 2 page.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-residual-closure-verification/09-CONTEXT.md
@.planning/phases/09-residual-closure-verification/09-02-PLAN.md
@.planning/phases/09-residual-closure-verification/09-03-PLAN.md
@sources/frontend/src/app/admin/page.tsx
@sources/frontend/src/services/users.ts
@sources/frontend/src/services/http.ts
@sources/frontend/src/services/errors.ts
</context>

<interfaces>
<!-- Backend endpoints (từ Plan 09-02 + 09-03 — Wave 1+2 deps):
- GET /api/products/admin/stats → ApiResponse {data: {totalProducts: number}}
- GET /api/orders/admin/stats   → ApiResponse {data: {totalOrders: number, pendingOrders: number}}
- GET /api/users/admin/stats    → ApiResponse {data: {totalUsers: number}}
- POST /api/users/me/password   → ApiResponse {data: {changed: true}} (200)
                                  hoặc 422 ApiErrorResponse {errorCode: "AUTH_INVALID_PASSWORD", message, traceId}

Service wrappers existing (`http.ts` `httpGet`/`httpPost` tự attach Bearer token + parse envelope).
-->

<!-- Toast pattern: kiểm tra existing toast utility trong frontend (Phase 5/6 khả năng có ToastProvider hoặc react-hot-toast). Nếu KHÔNG có toast hệ thống → fallback dùng `window.alert("Đã đổi mật khẩu")` (acceptable cho v1.2 demo, defer toast lib sang Phase 10). Document choice trong SUMMARY.
-->

<!-- Validation strategy (CONTEXT.md "Claude's Discretion"):
- Min 8 chars
- Ít nhất 1 letter (regex /[A-Za-z]/)
- Ít nhất 1 number (regex /\d/)
- confirmPassword === newPassword

KHÔNG dùng zod/rhf — Phase 10 sẽ setup foundation. Phase 9 dùng raw HTML form + manual validate trong onChange/onSubmit. Lý do: KHÔNG conflict pattern Phase 10 (sẽ refactor sau).
-->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Service stats.ts + Trim admin/page.tsx về 4 KPI + Promise.allSettled + per-card loading/error/retry</name>
  <files>
    sources/frontend/src/services/stats.ts,
    sources/frontend/src/app/admin/page.tsx,
    sources/frontend/src/app/admin/page.module.css
  </files>
  <read_first>
    - sources/frontend/src/app/admin/page.tsx — file hiện tại (verify mock arrays + cards extras để xóa).
    - sources/frontend/src/app/admin/page.module.css — verify class names hiện có (statsGrid, statCard, statValue, statLabel, statIcon, grid2, card, cardTitle, table, statusBadge, quickStats, quickStat, quickStatNum, quickStatLabel) — Plan này chỉ giữ statsGrid/statCard/statIcon/statValue/statLabel + thêm class skeleton/error.
    - sources/frontend/src/services/http.ts — verify `httpGet<T>(path)` signature + Bearer token auto-attach + envelope unwrap (return `data` field).
    - sources/frontend/src/services/users.ts — pattern service file (import http + types + named exports).
    - sources/frontend/src/services/errors.ts — verify error class shape (httpGet throw error có `.status` + `.errorCode`?). Sẽ dùng tương tự cho retry handler.
  </read_first>
  <action>
    **Bước 1: Tạo `sources/frontend/src/services/stats.ts`:**

    ```typescript
    /**
     * Phase 9 / Plan 09-04 (UI-02). Stats wrappers cho admin dashboard.
     * Endpoints backend: Plan 09-02 (per-svc /admin/stats with manual JWT role check).
     * Bearer token auto-attached bởi http.ts.
     */
    import { httpGet } from './http';

    export interface ProductStats {
      totalProducts: number;
    }
    export interface OrderStats {
      totalOrders: number;
      pendingOrders: number;
    }
    export interface UserStats {
      totalUsers: number;
    }

    export function fetchProductStats(): Promise<ProductStats> {
      return httpGet<ProductStats>('/api/products/admin/stats');
    }

    export function fetchOrderStats(): Promise<OrderStats> {
      return httpGet<OrderStats>('/api/orders/admin/stats');
    }

    export function fetchUserStats(): Promise<UserStats> {
      return httpGet<UserStats>('/api/users/admin/stats');
    }
    ```

    Note: nếu `httpGet` KHÔNG tự unwrap envelope `{data}` (kiểm tra ở read_first), điều chỉnh return type sang `Promise<{data: ProductStats}>` rồi `.data` ở consumer. Pick branch theo actual.

    **Bước 2: Overwrite `sources/frontend/src/app/admin/page.tsx`** với version trim + Promise.allSettled + per-card state:

    ```tsx
    'use client';

    import { useCallback, useEffect, useState } from 'react';
    import styles from './page.module.css';
    import {
      fetchProductStats,
      fetchOrderStats,
      fetchUserStats,
      type ProductStats,
      type OrderStats,
      type UserStats,
    } from '@/services/stats';

    type CardState<T> = { status: 'loading' | 'success' | 'error'; data?: T; error?: string };

    /**
     * Phase 9 / Plan 09-04 (UI-02). Trimmed dashboard:
     * - D-08: chỉ 4 KPI required, xóa totalRevenue/recent orders table/quick stats panel/mock arrays.
     * - D-09: Promise.allSettled (KHÔNG Promise.all) — 1 endpoint fail không block 3 cards còn lại.
     *         Per-card loading skeleton + error fallback với retry icon (re-fetch chỉ endpoint đó).
     */
    export default function AdminDashboard() {
      const [productCard, setProductCard] = useState<CardState<ProductStats>>({ status: 'loading' });
      const [orderCard, setOrderCard] = useState<CardState<OrderStats>>({ status: 'loading' });
      const [userCard, setUserCard] = useState<CardState<UserStats>>({ status: 'loading' });

      const loadProduct = useCallback(async () => {
        setProductCard({ status: 'loading' });
        try {
          const data = await fetchProductStats();
          setProductCard({ status: 'success', data });
        } catch (e) {
          setProductCard({ status: 'error', error: (e as Error).message ?? 'Không tải được' });
        }
      }, []);

      const loadOrder = useCallback(async () => {
        setOrderCard({ status: 'loading' });
        try {
          const data = await fetchOrderStats();
          setOrderCard({ status: 'success', data });
        } catch (e) {
          setOrderCard({ status: 'error', error: (e as Error).message ?? 'Không tải được' });
        }
      }, []);

      const loadUser = useCallback(async () => {
        setUserCard({ status: 'loading' });
        try {
          const data = await fetchUserStats();
          setUserCard({ status: 'success', data });
        } catch (e) {
          setUserCard({ status: 'error', error: (e as Error).message ?? 'Không tải được' });
        }
      }, []);

      useEffect(() => {
        // D-09: Promise.allSettled — không await trong useEffect cleanup
        Promise.allSettled([loadProduct(), loadOrder(), loadUser()]);
      }, [loadProduct, loadOrder, loadUser]);

      return (
        <div className={styles.dashboard}>
          <h1 className={styles.title}>Dashboard</h1>
          <div className={styles.statsGrid}>
            <KpiCard
              label="Sản phẩm"
              icon="🏷️"
              color="var(--secondary)"
              state={productCard}
              renderValue={(d) => String(d.totalProducts)}
              onRetry={loadProduct}
            />
            <KpiCard
              label="Tổng đơn hàng"
              icon="📦"
              color="var(--primary)"
              state={orderCard}
              renderValue={(d) => String(d.totalOrders)}
              onRetry={loadOrder}
            />
            <KpiCard
              label="Khách hàng"
              icon="👥"
              color="#f59e0b"
              state={userCard}
              renderValue={(d) => String(d.totalUsers)}
              onRetry={loadUser}
            />
            <KpiCard
              label="Đơn chờ xử lý"
              icon="⏳"
              color="#dc2626"
              state={orderCard}
              renderValue={(d) => String(d.pendingOrders)}
              onRetry={loadOrder}
            />
          </div>
        </div>
      );
    }

    interface KpiCardProps<T> {
      label: string;
      icon: string;
      color: string;
      state: CardState<T>;
      renderValue: (d: T) => string;
      onRetry: () => void;
    }

    function KpiCard<T>({ label, icon, color, state, renderValue, onRetry }: KpiCardProps<T>) {
      return (
        <div className={styles.statCard} data-card-label={label}>
          <div className={styles.statIcon}>{icon}</div>
          <div className={styles.statBody}>
            {state.status === 'loading' && (
              <div className={styles.skeleton} aria-label="Đang tải" />
            )}
            {state.status === 'success' && state.data && (
              <p className={styles.statValue} style={{ color }}>{renderValue(state.data)}</p>
            )}
            {state.status === 'error' && (
              <div className={styles.errorRow}>
                <span className={styles.statValue} style={{ color: 'var(--on-surface-variant)' }}>--</span>
                <button
                  type="button"
                  className={styles.retryBtn}
                  onClick={onRetry}
                  aria-label={`Tải lại ${label}`}
                  title={state.error}
                >⟳</button>
              </div>
            )}
            <p className={styles.statLabel}>{label}</p>
          </div>
        </div>
      );
    }
    ```

    **Bước 3: Update `sources/frontend/src/app/admin/page.module.css`** — XÓA classes không dùng (`.grid2`, `.card`, `.cardTitle`, `.table`, `.statusBadge`, `.status_*`, `.tdBold`, `.tdMuted`, `.quickStats`, `.quickStat`, `.quickStatNum`, `.quickStatLabel`); GIỮ `.dashboard`, `.title`, `.statsGrid`, `.statCard`, `.statIcon`, `.statValue`, `.statLabel`; THÊM:

    ```css
    .statBody { display: flex; flex-direction: column; gap: var(--space-1); }
    .skeleton {
      height: 36px;
      width: 80px;
      background: linear-gradient(90deg, var(--surface-variant) 0%, var(--surface) 50%, var(--surface-variant) 100%);
      background-size: 200% 100%;
      animation: shimmer 1.4s infinite;
      border-radius: var(--radius-sm);
    }
    @keyframes shimmer { 0% { background-position: 200% 0; } 100% { background-position: -200% 0; } }
    .errorRow { display: flex; align-items: center; gap: var(--space-2); }
    .retryBtn {
      background: transparent;
      border: 1px solid var(--outline);
      border-radius: var(--radius-sm);
      cursor: pointer;
      font-size: 1.2em;
      padding: 0 var(--space-2);
      color: var(--on-surface-variant);
    }
    .retryBtn:hover { background: var(--surface-variant); }
    ```

    Nếu CSS variables (`--surface-variant`, `--outline`...) không tồn tại trong project → fallback hex màu tương đương (vd `#e5e7eb` cho surface-variant, `#d1d5db` cho outline). Verify bằng grep `--surface-variant` trong `app/globals.css`.
  </action>
  <verify>
    <automated>
    test -f sources/frontend/src/services/stats.ts
    grep -E "fetchProductStats|fetchOrderStats|fetchUserStats" sources/frontend/src/services/stats.ts | wc -l | grep -E "^[3-9]"

    # Admin page trimmed
    grep -E "Promise\\.allSettled" sources/frontend/src/app/admin/page.tsx
    grep -E "totalProducts|totalOrders|totalUsers|pendingOrders" sources/frontend/src/app/admin/page.tsx | wc -l
    # Verify mock arrays + extras đã xóa (run in bash; if found → exit 1)
    if grep -E "mockOrders|mockProducts|mockUsers|formatPrice|totalRevenue|Đơn hàng gần đây|Tổng quan nhanh" sources/frontend/src/app/admin/page.tsx; then exit 1; fi

    # CSS classes mới
    grep -E "\\.skeleton|\\.errorRow|\\.retryBtn" sources/frontend/src/app/admin/page.module.css

    # CSS classes cũ đã xóa (run in bash; if found → exit 1)
    if grep -E "\\.quickStats|\\.statusBadge|\\.cardTitle" sources/frontend/src/app/admin/page.module.css; then exit 1; fi

    # Build pass
    cd sources/frontend && npx next build 2>&1 | tail -10
    </automated>
  </verify>
  <acceptance_criteria>
    - `stats.ts` export 3 functions + 3 types.
    - `admin/page.tsx` chứa Promise.allSettled, KHÔNG còn mock arrays / formatPrice / totalRevenue / recent orders.
    - 4 cards rendered với label đúng: "Sản phẩm", "Tổng đơn hàng", "Khách hàng", "Đơn chờ xử lý".
    - CSS skeleton + retry button defined.
    - `npx next build` exit 0.
  </acceptance_criteria>
  <done>
    Build pass + 4 cards visible khi chạy dev + grep checks pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Profile settings page với password change form + service users.changeMyPassword</name>
  <files>
    sources/frontend/src/services/users.ts,
    sources/frontend/src/app/profile/settings/page.tsx,
    sources/frontend/src/app/profile/settings/page.module.css
  </files>
  <read_first>
    - sources/frontend/src/services/users.ts — pattern existing (import httpGet/httpPatch/httpDelete + service path conventions).
    - sources/frontend/src/services/http.ts — verify `httpPost<T>(path, body)` exists. Nếu chưa, dùng generic `request` helper hoặc thêm `httpPost`.
    - sources/frontend/src/services/errors.ts — verify error class chứa `errorCode` field (mapping từ ApiErrorResponse). Nếu KHÔNG, parse error.message hoặc thêm thuộc tính `errorCode` vào ApiError class.
    - sources/frontend/src/app/profile/page.tsx — pattern protected page (đã có middleware bảo vệ).
    - .planning/phases/09-residual-closure-verification/09-03-PLAN.md §interfaces — confirm response shape backend.
  </read_first>
  <action>
    **Bước 1: Thêm `changeMyPassword` vào `sources/frontend/src/services/users.ts`:**

    Append sau hàm `deleteAdminUser`:

    ```typescript
    // ============================================================
    // Phase 9 / Plan 09-04 (AUTH-07). Self-service password change.
    // Endpoint backend: POST /api/users/me/password (Plan 09-03).
    // ============================================================
    import { httpPost } from './http'; // adjust import path nếu file đã import sẵn

    export interface ChangePasswordBody {
      oldPassword: string;
      newPassword: string;
    }

    export function changeMyPassword(body: ChangePasswordBody): Promise<{ changed: true }> {
      return httpPost<{ changed: true }>('/api/users/me/password', body);
    }
    ```

    Nếu `httpPost` chưa tồn tại trong `http.ts` → đọc file http.ts và adapt theo pattern actual (vd có thể là `request('POST', path, body)` hoặc `httpJson('POST', path, body)`). KHÔNG tạo httpPost mới ad-hoc — nếu thiếu, ưu tiên dùng pattern hiện tại; nếu pattern không có POST helper, thêm function `httpPost<T>(path, body)` vào http.ts theo cùng style với `httpPatch`.

    **Bước 2: Tạo `sources/frontend/src/app/profile/settings/page.tsx`:**

    ```tsx
    'use client';

    import { useState, type FormEvent } from 'react';
    import styles from './page.module.css';
    import { changeMyPassword } from '@/services/users';

    /**
     * Phase 9 / Plan 09-04 (AUTH-07 frontend).
     * Page mới /profile/settings — Phase 10 sẽ extend với fullName/phone/avatar.
     * Phase 9 chỉ ship password change form (3 fields).
     *
     * Validation client-side (CONTEXT.md Claude's Discretion):
     * - newPassword min 8 + ít nhất 1 letter + 1 number
     * - confirmPassword === newPassword
     *
     * Error mapping:
     * - 422 errorCode AUTH_INVALID_PASSWORD → field-level error tại "Mật khẩu hiện tại"
     * - 400 (validation backend) → form-level generic error
     *
     * D-10: KHÔNG force logout sau success — chỉ toast + reset form.
     */
    export default function SettingsPage() {
      const [oldPassword, setOldPassword] = useState('');
      const [newPassword, setNewPassword] = useState('');
      const [confirmPassword, setConfirmPassword] = useState('');
      const [submitting, setSubmitting] = useState(false);
      const [oldError, setOldError] = useState<string | null>(null);
      const [formError, setFormError] = useState<string | null>(null);
      const [successMsg, setSuccessMsg] = useState<string | null>(null);

      function validate(): string | null {
        if (newPassword.length < 8) return 'Mật khẩu mới phải có ít nhất 8 ký tự';
        if (!/[A-Za-z]/.test(newPassword)) return 'Mật khẩu mới phải có ít nhất 1 chữ cái';
        if (!/\d/.test(newPassword)) return 'Mật khẩu mới phải có ít nhất 1 số';
        if (newPassword !== confirmPassword) return 'Xác nhận mật khẩu không khớp';
        if (!oldPassword) return 'Vui lòng nhập mật khẩu hiện tại';
        return null;
      }

      const isValid = validate() === null;

      async function handleSubmit(e: FormEvent) {
        e.preventDefault();
        setOldError(null);
        setFormError(null);
        setSuccessMsg(null);
        const v = validate();
        if (v) {
          setFormError(v);
          return;
        }
        setSubmitting(true);
        try {
          await changeMyPassword({ oldPassword, newPassword });
          setSuccessMsg('Đã đổi mật khẩu');
          setOldPassword('');
          setNewPassword('');
          setConfirmPassword('');
        } catch (err) {
          // Error shape (Phase 9 / Plan 09-03 backend):
          // {status: 422, errorCode: "AUTH_INVALID_PASSWORD", message, traceId}
          const e = err as { status?: number; errorCode?: string; message?: string };
          if (e.status === 422 && e.errorCode === 'AUTH_INVALID_PASSWORD') {
            setOldError('Mật khẩu hiện tại không đúng');
          } else {
            setFormError(e.message ?? 'Có lỗi xảy ra, vui lòng thử lại');
          }
        } finally {
          setSubmitting(false);
        }
      }

      return (
        <div className={styles.page}>
          <h1 className={styles.title}>Cài đặt tài khoản</h1>
          <section className={styles.section}>
            <h2 className={styles.sectionTitle}>Đổi mật khẩu</h2>
            <form onSubmit={handleSubmit} className={styles.form} noValidate>
              <div className={styles.field}>
                <label htmlFor="oldPassword" className={styles.label}>Mật khẩu hiện tại</label>
                <input
                  id="oldPassword"
                  type="password"
                  className={styles.input}
                  value={oldPassword}
                  onChange={(e) => { setOldPassword(e.target.value); setOldError(null); }}
                  autoComplete="current-password"
                  required
                />
                {oldError && <p className={styles.fieldError} data-testid="oldPasswordError">{oldError}</p>}
              </div>
              <div className={styles.field}>
                <label htmlFor="newPassword" className={styles.label}>Mật khẩu mới</label>
                <input
                  id="newPassword"
                  type="password"
                  className={styles.input}
                  value={newPassword}
                  onChange={(e) => setNewPassword(e.target.value)}
                  autoComplete="new-password"
                  required
                  minLength={8}
                />
                <p className={styles.hint}>Tối thiểu 8 ký tự, gồm ít nhất 1 chữ cái + 1 số.</p>
              </div>
              <div className={styles.field}>
                <label htmlFor="confirmPassword" className={styles.label}>Xác nhận mật khẩu mới</label>
                <input
                  id="confirmPassword"
                  type="password"
                  className={styles.input}
                  value={confirmPassword}
                  onChange={(e) => setConfirmPassword(e.target.value)}
                  autoComplete="new-password"
                  required
                />
              </div>
              {formError && <p className={styles.formError} data-testid="formError">{formError}</p>}
              {successMsg && <p className={styles.success} data-testid="successMsg" role="status">{successMsg}</p>}
              <button
                type="submit"
                className={styles.submit}
                disabled={submitting || !isValid}
                data-testid="submitPassword"
              >
                {submitting ? 'Đang đổi...' : 'Đổi mật khẩu'}
              </button>
            </form>
          </section>
        </div>
      );
    }
    ```

    **Bước 3: Tạo `sources/frontend/src/app/profile/settings/page.module.css`:**

    ```css
    .page { max-width: 640px; margin: 0 auto; padding: var(--space-6) var(--space-4); }
    .title { font-size: 1.75rem; margin-bottom: var(--space-4); }
    .section { background: var(--surface); border: 1px solid var(--outline-variant); border-radius: var(--radius-md); padding: var(--space-5); }
    .sectionTitle { font-size: 1.25rem; margin-bottom: var(--space-4); }
    .form { display: flex; flex-direction: column; gap: var(--space-4); }
    .field { display: flex; flex-direction: column; gap: var(--space-1); }
    .label { font-weight: 500; }
    .input { padding: var(--space-2) var(--space-3); border: 1px solid var(--outline); border-radius: var(--radius-sm); font-size: 1rem; }
    .input:focus { outline: 2px solid var(--primary); outline-offset: 1px; }
    .hint { font-size: 0.85rem; color: var(--on-surface-variant); }
    .fieldError { color: #dc2626; font-size: 0.875rem; }
    .formError { color: #dc2626; padding: var(--space-2); background: #fee2e2; border-radius: var(--radius-sm); }
    .success { color: #16a34a; padding: var(--space-2); background: #dcfce7; border-radius: var(--radius-sm); }
    .submit { background: var(--primary); color: white; padding: var(--space-3) var(--space-5); border: none; border-radius: var(--radius-sm); cursor: pointer; font-size: 1rem; }
    .submit:disabled { opacity: 0.5; cursor: not-allowed; }
    ```

    Cùng note CSS variable fallback như Task 1 nếu thiếu var.

    **Bước 4: Verify error class shape của http.ts:**

    Đọc `sources/frontend/src/services/errors.ts`. Nếu Error class throw từ http.ts (vd `class ApiError extends Error`) chỉ có `.message` mà KHÔNG có `.errorCode` / `.status` → cần extend class để parse từ ApiErrorResponse body. Edit:

    ```typescript
    // errors.ts (nếu cần)
    export class ApiError extends Error {
      status: number;
      errorCode?: string;
      traceId?: string;
      constructor(message: string, status: number, errorCode?: string, traceId?: string) {
        super(message);
        this.name = 'ApiError';
        this.status = status;
        this.errorCode = errorCode;
        this.traceId = traceId;
      }
    }
    ```

    Và http.ts khi parse error response:

    ```typescript
    // pseudo trong http error handler:
    const body = await res.json().catch(() => ({}));
    throw new ApiError(body.message ?? res.statusText, res.status, body.errorCode, body.traceId);
    ```

    Adapt theo actual code. Document edit trong SUMMARY nếu phải mod errors.ts/http.ts.
  </action>
  <verify>
    <automated>
    test -f sources/frontend/src/app/profile/settings/page.tsx
    test -f sources/frontend/src/app/profile/settings/page.module.css

    grep -E "changeMyPassword" sources/frontend/src/services/users.ts
    grep -E "AUTH_INVALID_PASSWORD" sources/frontend/src/app/profile/settings/page.tsx
    grep -E "Mật khẩu hiện tại không đúng" sources/frontend/src/app/profile/settings/page.tsx
    grep -E "Đã đổi mật khẩu" sources/frontend/src/app/profile/settings/page.tsx
    grep -E "data-testid=\"submitPassword\"" sources/frontend/src/app/profile/settings/page.tsx
    grep -E "data-testid=\"oldPasswordError\"" sources/frontend/src/app/profile/settings/page.tsx
    grep -E "minLength=\\{8\\}|minLength=\"8\"" sources/frontend/src/app/profile/settings/page.tsx

    cd sources/frontend && npx next build 2>&1 | tail -10
    </automated>
  </verify>
  <acceptance_criteria>
    - 3 file mới/sửa.
    - `changeMyPassword` exported, gọi `POST /api/users/me/password`.
    - Form 3 fields với data-testid (cho Plan 05 Playwright).
    - Mapping errorCode AUTH_INVALID_PASSWORD → field-level error tại oldPassword.
    - Toast/inline success message "Đã đổi mật khẩu".
    - `npx next build` exit 0.
  </acceptance_criteria>
  <done>
    Build pass + grep checks PASS + form render đúng khi truy cập `/profile/settings`.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual verify admin dashboard (4 KPI thật + retry) + password form (success + sai mật khẩu)</name>
  <what-built>
    - Admin dashboard `/admin` chỉ còn 4 KPI cards với số thật + Promise.allSettled + per-card retry.
    - Page `/profile/settings` với form 3 fields đổi password.
  </what-built>
  <how-to-verify>
    Stack chạy: `cd sources && docker compose up -d --build && cd frontend && npm run dev`.

    **A. Admin dashboard (`/admin`)** — login bằng admin account:
    1. Vào `/admin` → kỳ vọng 4 cards "Sản phẩm", "Tổng đơn hàng", "Khách hàng", "Đơn chờ xử lý" với số thật từ DB. KHÔNG còn "Tổng doanh thu" / "Đơn hàng gần đây" / "Tổng quan nhanh".
    2. DevTools Network tab: thấy 3 request tới `/api/products/admin/stats`, `/api/orders/admin/stats`, `/api/users/admin/stats` → mỗi request 200.
    3. Test fault tolerance: `docker compose stop order-service` rồi reload `/admin` → kỳ vọng 2 cards (Sản phẩm + Khách hàng) vẫn render thành công, 2 cards order-related ("Tổng đơn hàng" + "Đơn chờ xử lý") hiển thị `--` + nút retry `⟳`. Click retry → KHÔNG reload page, chỉ re-fetch order endpoint (vẫn fail vì service down). Restart `docker compose start order-service` → click retry lần nữa → cards thành công.

    **B. Password form (`/profile/settings`)** — login bằng user account:
    1. Vào `/profile/settings` → form 3 fields visible.
    2. Submit với newPassword "abc" (< 8 chars) → form error "Mật khẩu mới phải có ít nhất 8 ký tự", submit button disabled.
    3. Submit với confirmPassword khác newPassword → form error "Xác nhận mật khẩu không khớp".
    4. Submit với SAI oldPassword + newPassword hợp lệ "NewPass123" + confirm match → field-level error "Mật khẩu hiện tại không đúng" tại field oldPassword. Form KHÔNG reset.
    5. Submit với ĐÚNG oldPassword + newPassword "NewPass123" + confirm match → success message "Đã đổi mật khẩu", form reset (3 input rỗng). KHÔNG bị logout (cookie session vẫn còn — refresh page vẫn ở `/profile/settings`).
    6. Logout, login lại với newPassword "NewPass123" → đăng nhập thành công (xác nhận password đã thực sự đổi trong DB).

    Take screenshots cho 3 mốc quan trọng: (1) dashboard 4 KPI success, (2) dashboard với 2 cards error sau khi stop order-service, (3) form sau khi submit sai oldPassword. Lưu vào `e2e/screenshots/09-04-{1,2,3}.png`.
  </how-to-verify>
  <acceptance_criteria>
    - A.1: 4 cards visible với label đúng + số > 0 (giả sử DB có data).
    - A.2: 3 network calls success.
    - A.3: 2 cards lỗi → '--' + retry, 2 cards thành công vẫn render.
    - B.4: error text "Mật khẩu hiện tại không đúng" hiển thị đúng tại field oldPassword.
    - B.5: success message "Đã đổi mật khẩu" + form reset, KHÔNG redirect /login.
    - B.6: re-login với password mới thành công.
  </acceptance_criteria>
  <resume-signal>Type "approved" hoặc mô tả issue.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → /api/users/me/password | newPassword plaintext qua HTTPS (production); localhost demo qua HTTP (acceptable). |
| Frontend state → DOM | Password input type="password" mask UI; KHÔNG persist trong localStorage. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04-01 | Information Disclosure | Password log trong console / DevTools | mitigate | KHÔNG `console.log(body)` trong service / form. State chỉ giữ trong React useState (clear sau success). Input type="password" mask. |
| T-09-04-02 | Information Disclosure | Stats endpoints leak count cho non-admin | mitigate | Backend Plan 09-02 đã enforce 403. Frontend chỉ render khi admin login (middleware Plan 09-01 redirect non-admin sang /403). |
| T-09-04-03 | Tampering | XSS qua error message render | mitigate | React JSX auto-escape; KHÔNG dùng dangerouslySetInnerHTML. |
| T-09-04-04 | Spoofing | CSRF qua POST /me/password | accept | Bearer token qua Authorization header (KHÔNG cookie). CSRF không áp dụng cho header-based auth. |
| T-09-04-05 | Denial of Service | Brute force form submit | accept | Rate limit defer. Demo scope. Submit button disabled trong khi `submitting=true` ngăn double-submit. |
| T-09-04-06 | Information Disclosure | Toast / error chứa traceId leak | accept | TraceId là correlation ID nội bộ; không leak credential. |
</threat_model>

<verification>
- 6 file mới/sửa compile pass.
- `npx next build` exit 0.
- 3 manual case A + 6 manual case B pass.
- 3 screenshot evidence saved.
</verification>

<success_criteria>
- UI-02 frontend đóng: 4 KPI cards với số thật, per-card error tolerance + retry (Success Criteria #3 Phase 9).
- AUTH-07 frontend đóng: form đổi password + field-level error AUTH_INVALID_PASSWORD + success toast (Success Criteria #2 Phase 9).
- Admin dashboard trim đúng D-08.
- KHÔNG conflict pattern Phase 10 (raw form, KHÔNG cài rhf+zod sớm).
</success_criteria>

<output>
Sau khi xong, tạo `.planning/phases/09-residual-closure-verification/09-04-SUMMARY.md`:
- Files: 6 files (3 mới + 3 sửa).
- Decision log: form pattern raw HTML (KHÔNG rhf+zod) — Phase 10 sẽ refactor.
- Toast pattern dùng (inline message vs lib).
- Manual verify 9 case results (A.1..A.3 + B.1..B.6).
- 3 screenshot paths.
- Note: Plan 05 Playwright sẽ dùng `data-testid="submitPassword"`, `data-testid="oldPasswordError"`, `data-testid="successMsg"` selectors.
</output>
