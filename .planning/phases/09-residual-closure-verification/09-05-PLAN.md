---
phase: 09-residual-closure-verification
plan: 05
type: execute
wave: 3
depends_on:
  - 09-01
  - 09-02
  - 09-03
  - 09-04
files_modified:
  - sources/frontend/playwright.config.ts
  - sources/frontend/e2e/global-setup.ts
  - sources/frontend/e2e/uat.spec.ts
  - sources/frontend/e2e/uat.legacy.spec.ts.bak
  - sources/frontend/e2e/auth.spec.ts
  - sources/frontend/e2e/admin-products.spec.ts
  - sources/frontend/e2e/admin-orders.spec.ts
  - sources/frontend/e2e/admin-users.spec.ts
  - sources/frontend/e2e/order-detail.spec.ts
  - sources/frontend/e2e/password-change.spec.ts
  - sources/frontend/.gitignore
autonomous: false
requirements:
  - TEST-01
must_haves:
  truths:
    - "Playwright suite (~12 tests) pass 100% trên fresh `docker compose up -d` stack"
    - "Suite chạy với `npx playwright test --reporter=list` exits 0 với 'N passed' (N >= 12)"
    - "Global setup login user + admin → save 2 storageState files reuse cross-tests (KHÔNG login lặp lại mỗi test)"
    - "storageState/*.json bị .gitignore (KHÔNG commit session token)"
    - "uat.spec.ts cũ rename thành uat.legacy.spec.ts.bak (KHÔNG bị Playwright pick up)"
    - "observations.json + screenshots evidence committed như pattern Phase 4"
  artifacts:
    - path: "sources/frontend/e2e/global-setup.ts"
      provides: "Login user + admin → save storageState/{user,admin}.json"
    - path: "sources/frontend/playwright.config.ts"
      provides: "Reference globalSetup + projects per-storageState"
    - path: "sources/frontend/e2e/auth.spec.ts"
      provides: "Auth tests: register / login / logout / role gate (~3 tests)"
    - path: "sources/frontend/e2e/admin-products.spec.ts"
      provides: "Admin product CRUD (~3 tests)"
    - path: "sources/frontend/e2e/admin-orders.spec.ts"
      provides: "Admin order list/detail/state update (~2 tests)"
    - path: "sources/frontend/e2e/admin-users.spec.ts"
      provides: "Admin user PATCH + soft-delete (~2 tests)"
    - path: "sources/frontend/e2e/order-detail.spec.ts"
      provides: "User /profile/orders/:id full breakdown (~2 tests)"
    - path: "sources/frontend/e2e/password-change.spec.ts"
      provides: "AUTH-07 happy path + sai oldPassword (~2 tests, regression cho Plan 09-04)"
    - path: "sources/frontend/.gitignore"
      provides: "/e2e/storageState/ pattern added"
  key_links:
    - from: "Playwright globalSetup"
      to: "real backend POST /api/users/auth/login"
      via: "fetch user + admin tokens, browser context.storageState save"
      pattern: "storageState"
    - from: "Specs use storageState"
      to: "test.use({ storageState: 'e2e/storageState/admin.json' })"
      via: "Playwright projects config"
      pattern: "test\\.use.*storageState"
---

<objective>
Đóng TEST-01: re-baseline Playwright E2E suite cho v1.1 features (auth + admin CRUD + order detail) + Plan 09-04 password change. Suite cũ `uat.spec.ts` (Phase 4 era) đánh dấu legacy + giữ làm reference, không xóa. Replace bằng 6 spec files focused theo D-12.

Purpose: Demo có safety net regression — fresh `docker compose up` + chạy `npx playwright test` → 12+ tests pass 100%. Confidence cho v1.2 phases sau (10..15) vì biết v1.1 baseline ổn định.

Output:
- Global setup login 2 role → storageState fixtures.
- 6 spec files mới (~12 tests).
- Rename uat.spec.ts → uat.legacy.spec.ts.bak.
- .gitignore add storageState/.
- observations.json + screenshots như pattern Phase 4 UAT.

KHÔNG CI/GitHub Actions (D-15 — defer).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-residual-closure-verification/09-CONTEXT.md
@.planning/phases/09-residual-closure-verification/09-01-PLAN.md
@.planning/phases/09-residual-closure-verification/09-04-PLAN.md
@.planning/codebase/TESTING.md
@sources/frontend/playwright.config.ts
@sources/frontend/e2e/uat.spec.ts
@sources/frontend/.gitignore
</context>

<interfaces>
<!-- Test data assumption (verify khi execute):
- Admin seed: email=admin@example.com, password=Admin@123 (hoặc tương đương). Verify trong V100__seed*.sql của user-service.
- User seed: email=user@example.com, password=User@123 (hoặc tương đương).

Nếu seed users KHÔNG có sẵn → globalSetup tự register 2 users mới qua POST /api/users/auth/register, sau đó admin promote thủ công (nếu register chỉ tạo role USER) hoặc dùng admin seed có sẵn + register thêm 1 user thường.

Read trước: `sources/backend/user-service/src/main/resources/db/seed-dev/` để verify exact credentials.
-->

<!-- Playwright projects pattern khi dùng global storageState:
playwright.config.ts có `globalSetup: require.resolve('./e2e/global-setup')`.
global-setup.ts return Promise<void>, internally launches browser → login → context.storageState({path: ...}).
Tests dùng `test.use({ storageState: 'e2e/storageState/admin.json' })` ở top of describe block.
-->

<!-- data-testid selectors mới từ Plan 09-04:
- submitPassword
- oldPasswordError
- successMsg
- formError

Tests password-change.spec.ts dùng các selector này.
-->

<!-- v1.1 admin pages selectors:
Phase 7+8 admin CRUD pages — đọc qua trước khi viết test:
- /admin/products  list page
- /admin/products/new  hoặc modal create
- /admin/orders
- /admin/users
Verify exact path + button labels khi execute (vd "Tạo sản phẩm", "Cập nhật trạng thái").
-->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Global setup + playwright.config + .gitignore + rename legacy</name>
  <files>
    sources/frontend/e2e/global-setup.ts,
    sources/frontend/playwright.config.ts,
    sources/frontend/.gitignore,
    sources/frontend/e2e/uat.spec.ts (rename → uat.legacy.spec.ts.bak)
  </files>
  <read_first>
    - sources/frontend/playwright.config.ts — config base hiện tại (testDir, baseURL, etc.).
    - sources/frontend/e2e/uat.spec.ts — verify file tồn tại trước khi rename.
    - sources/frontend/.gitignore — verify chưa có storageState pattern.
    - sources/backend/user-service/src/main/resources/db/seed-dev/ (hoặc db/migration với V1xx) — extract admin/user seed credentials. Note exact email + password.
    - sources/frontend/src/services/auth.ts (nếu có) — verify endpoint login + cookie set sequence (auth_present + user_role).
  </read_first>
  <action>
    **Bước 1: Tạo `sources/frontend/e2e/global-setup.ts`:**

    ```typescript
    /**
     * Phase 9 / Plan 09-05 (TEST-01).
     * D-13: Login user thường + admin → save 2 storageState fixtures.
     *
     * Tests reuse qua test.use({storageState: 'e2e/storageState/{user,admin}.json'})
     * → KHÔNG login lặp lại mỗi test → giảm flakiness + tăng speed.
     *
     * KHÔNG commit storageState (chứa real session cookie) — .gitignore.
     */
    import { chromium, type FullConfig } from '@playwright/test';
    import * as fs from 'node:fs';
    import * as path from 'node:path';

    const STATE_DIR = path.resolve(process.cwd(), 'e2e', 'storageState');

    // CONFIRM credentials với seed-dev SQL trước khi commit. Nếu seed khác → update.
    const ADMIN_EMAIL = process.env.E2E_ADMIN_EMAIL ?? 'admin@example.com';
    const ADMIN_PASSWORD = process.env.E2E_ADMIN_PASSWORD ?? 'Admin@123';
    const USER_EMAIL = process.env.E2E_USER_EMAIL ?? 'user@example.com';
    const USER_PASSWORD = process.env.E2E_USER_PASSWORD ?? 'User@123';

    async function loginAndSave(email: string, password: string, outFile: string) {
      const browser = await chromium.launch();
      const context = await browser.newContext({ baseURL: 'http://localhost:3000' });
      const page = await context.newPage();
      await page.goto('/login');
      await page.fill('input[name="email"], input[type="email"]', email);
      await page.fill('input[name="password"], input[type="password"]', password);
      await Promise.all([
        page.waitForURL((u) => !u.pathname.startsWith('/login'), { timeout: 10000 }),
        page.click('button[type="submit"]'),
      ]);
      // Verify cookies set
      const cookies = await context.cookies();
      const authPresent = cookies.find((c) => c.name === 'auth_present');
      if (!authPresent) {
        throw new Error(`Login failed for ${email} — auth_present cookie not set`);
      }
      await context.storageState({ path: outFile });
      await browser.close();
    }

    export default async function globalSetup(_config: FullConfig) {
      fs.mkdirSync(STATE_DIR, { recursive: true });
      await loginAndSave(USER_EMAIL, USER_PASSWORD, path.join(STATE_DIR, 'user.json'));
      await loginAndSave(ADMIN_EMAIL, ADMIN_PASSWORD, path.join(STATE_DIR, 'admin.json'));
      // eslint-disable-next-line no-console
      console.log('[global-setup] storageState saved for user + admin');
    }
    ```

    **Bước 2: Update `sources/frontend/playwright.config.ts`:**

    ```typescript
    import { defineConfig, devices } from '@playwright/test';

    export default defineConfig({
      testDir: './e2e',
      testIgnore: ['**/*.legacy.spec.ts.bak', '**/global-setup.ts'],
      globalSetup: require.resolve('./e2e/global-setup'),
      fullyParallel: false,
      workers: 1,
      retries: 0,
      reporter: [['list'], ['json', { outputFile: 'e2e/results.json' }]],
      use: {
        baseURL: 'http://localhost:3000',
        trace: 'on',
        screenshot: 'only-on-failure',
        video: 'retain-on-failure',
        locale: 'vi-VN',
      },
      projects: [
        {
          name: 'chromium',
          use: { ...devices['Desktop Chrome'] },
        },
      ],
    });
    ```

    Lưu ý:
    - `testIgnore` để Playwright không pick up legacy `uat.legacy.spec.ts.bak` + `global-setup.ts`.
    - storageState áp dụng per-spec qua `test.use({ storageState: ... })` — KHÔNG set ở project level (vì có spec dùng anonymous, có spec dùng admin, có spec dùng user).
    - Nếu Next.js project dùng ESM-only `require.resolve` không work → đổi sang `import.meta.resolve` hoặc string literal `'./e2e/global-setup.ts'`. Test bằng `npx playwright test --list` xác nhận setup pickup.

    **Bước 3: Update `sources/frontend/.gitignore`:**

    Append:

    ```
    # Phase 9 / Plan 09-05 (TEST-01) — Playwright session state
    /e2e/storageState/
    ```

    **Bước 4: Rename uat.spec.ts → uat.legacy.spec.ts.bak:**

    ```bash
    mv sources/frontend/e2e/uat.spec.ts sources/frontend/e2e/uat.legacy.spec.ts.bak
    ```

    File extension `.bak` ngoài testIgnore pattern là double-protection — Playwright default chỉ pick `*.spec.ts` / `*.test.ts`.
  </action>
  <verify>
    <automated>
    test -f sources/frontend/e2e/global-setup.ts
    test ! -f sources/frontend/e2e/uat.spec.ts
    test -f sources/frontend/e2e/uat.legacy.spec.ts.bak

    grep -E "globalSetup" sources/frontend/playwright.config.ts
    grep -E "testIgnore" sources/frontend/playwright.config.ts
    grep -E "/e2e/storageState/" sources/frontend/.gitignore

    grep -E "loginAndSave|storageState" sources/frontend/e2e/global-setup.ts | wc -l | grep -E "^[2-9]"

    # Type check
    cd sources/frontend && npx tsc --noEmit -p . 2>&1 | tail -10
    </automated>
  </verify>
  <acceptance_criteria>
    - global-setup.ts compile, login flow đúng cookies.
    - playwright.config.ts có globalSetup + testIgnore.
    - .gitignore chứa pattern.
    - uat.spec.ts rename thành công.
    - tsc --noEmit pass.
  </acceptance_criteria>
  <done>
    Files in place + tsc pass + rename verified.
  </done>
</task>

<task type="auto">
  <name>Task 2: Spec files (auth + admin-products + admin-orders + admin-users + order-detail + password-change)</name>
  <files>
    sources/frontend/e2e/auth.spec.ts,
    sources/frontend/e2e/admin-products.spec.ts,
    sources/frontend/e2e/admin-orders.spec.ts,
    sources/frontend/e2e/admin-users.spec.ts,
    sources/frontend/e2e/order-detail.spec.ts,
    sources/frontend/e2e/password-change.spec.ts
  </files>
  <read_first>
    - sources/frontend/src/app/admin/products/page.tsx (và sub-routes new / [id]) — extract page heading text + button labels + form field selectors thực tế cho admin CRUD product tests.
    - sources/frontend/src/app/admin/orders/page.tsx — labels + status update UI.
    - sources/frontend/src/app/admin/users/page.tsx — PATCH form + soft-delete button.
    - sources/frontend/src/app/profile/orders/[id]/page.tsx — verify breakdown sections (4-column items table, shippingAddress block, paymentMethod label).
    - sources/frontend/src/app/login/page.tsx — input names + submit selector.
    - sources/frontend/e2e/uat.legacy.spec.ts.bak — reference cách login flow + observations pattern (đã rename, vẫn đọc được).
    - .planning/phases/09-residual-closure-verification/09-04-PLAN.md task 2 — data-testid của password form.
  </read_first>
  <action>
    Tạo 6 spec files. Mỗi file tự chứa, dùng storageState fixture phù hợp. Skeleton template:

    **`auth.spec.ts`** (3 tests, anonymous + register/login/logout/role gate — KHÔNG dùng storageState):

    ```typescript
    import { test, expect } from '@playwright/test';

    // Anonymous tests — không dùng storageState fixture, mỗi test tự clear cookies.
    test.use({ storageState: { cookies: [], origins: [] } });

    test('AUTH-1: register flow → user mới login được', async ({ page }) => {
      const email = `test-${Date.now()}@example.com`;
      const password = 'TestPass123';
      await page.goto('/register');
      await page.fill('input[name="username"]', `u${Date.now()}`);
      await page.fill('input[name="email"]', email);
      await page.fill('input[name="password"]', password);
      await page.click('button[type="submit"]');
      await page.waitForURL(/\/(profile|$)/, { timeout: 10000 });
      // verify navbar shows logged-in state
      await expect(page.getByText(/đăng xuất|logout/i)).toBeVisible({ timeout: 5000 });
    });

    test('AUTH-2: login + logout', async ({ page }) => {
      await page.goto('/login');
      await page.fill('input[name="email"]', process.env.E2E_USER_EMAIL ?? 'user@example.com');
      await page.fill('input[name="password"]', process.env.E2E_USER_PASSWORD ?? 'User@123');
      await Promise.all([
        page.waitForURL((u) => !u.pathname.startsWith('/login')),
        page.click('button[type="submit"]'),
      ]);
      // logout: click navbar logout link
      await page.click('text=/đăng xuất|logout/i');
      await page.waitForURL(/\/$|\/login/);
      // verify cookies cleared
      const cookies = await page.context().cookies();
      expect(cookies.find((c) => c.name === 'auth_present')).toBeUndefined();
    });

    test('AUTH-3: role gate — USER vào /admin redirect /403; chưa login vào /profile/orders redirect /login', async ({ page, context }) => {
      // chưa login
      await context.clearCookies();
      const resp = await page.goto('/profile/orders', { waitUntil: 'commit' });
      expect(page.url()).toMatch(/\/login\?returnTo=%2Fprofile%2Forders/);

      // login user thường
      await page.goto('/login');
      await page.fill('input[name="email"]', process.env.E2E_USER_EMAIL ?? 'user@example.com');
      await page.fill('input[name="password"]', process.env.E2E_USER_PASSWORD ?? 'User@123');
      await Promise.all([
        page.waitForURL((u) => !u.pathname.startsWith('/login')),
        page.click('button[type="submit"]'),
      ]);
      await page.goto('/admin');
      await expect(page).toHaveURL(/\/403/);
    });
    ```

    **`admin-products.spec.ts`** (3 tests, dùng admin storageState):

    ```typescript
    import { test, expect } from '@playwright/test';

    test.use({ storageState: 'e2e/storageState/admin.json' });

    test('ADM-PROD-1: list trang /admin/products render', async ({ page }) => {
      await page.goto('/admin/products');
      await expect(page.getByRole('heading', { name: /sản phẩm|products/i })).toBeVisible();
      // table or list visible
      await expect(page.locator('table, [role="grid"], ul li')).toBeVisible({ timeout: 10000 });
    });

    test('ADM-PROD-2: create product → list chứa product mới', async ({ page }) => {
      const productName = `Test Product ${Date.now()}`;
      await page.goto('/admin/products');
      // Adapt selector theo UI thực tế (button "Tạo sản phẩm" / "Thêm" / "+ New")
      await page.click('text=/tạo|thêm|new/i');
      // Form có thể là modal hoặc page riêng — adapt
      await page.fill('input[name="name"]', productName);
      await page.fill('input[name="price"]', '100000');
      await page.fill('input[name="stock"]', '10');
      await page.click('button[type="submit"]');
      await page.waitForLoadState('networkidle');
      await expect(page.getByText(productName)).toBeVisible({ timeout: 10000 });
    });

    test('ADM-PROD-3: validation error when submit empty form', async ({ page }) => {
      await page.goto('/admin/products');
      await page.click('text=/tạo|thêm|new/i');
      await page.click('button[type="submit"]');
      // some inline error visible
      await expect(page.locator('[class*="error"], [role="alert"]')).toBeVisible({ timeout: 5000 });
    });
    ```

    **`admin-orders.spec.ts`** (2 tests, admin storageState):

    ```typescript
    import { test, expect } from '@playwright/test';

    test.use({ storageState: 'e2e/storageState/admin.json' });

    test('ADM-ORD-1: list /admin/orders + open detail first row', async ({ page }) => {
      await page.goto('/admin/orders');
      await expect(page.getByRole('heading', { name: /đơn hàng|orders/i })).toBeVisible();
      const firstRow = page.locator('table tbody tr').first();
      if (await firstRow.count() > 0) {
        await firstRow.click();
        await page.waitForURL(/\/admin\/orders\//);
        await expect(page.getByText(/trạng thái|status/i)).toBeVisible();
      }
    });

    test('ADM-ORD-2: update order state PENDING → PAID', async ({ page }) => {
      await page.goto('/admin/orders');
      const row = page.locator('table tbody tr').first();
      const exists = await row.count();
      test.skip(exists === 0, 'No orders to update');
      await row.click();
      // Adapt selector theo UI: select dropdown hoặc button group
      await page.selectOption('select[name="orderStatus"], select[name="status"]', 'PAID').catch(async () => {
        await page.click('text=/PAID|Đã thanh toán/i');
      });
      await page.click('button[type="submit"], text=/cập nhật|update/i');
      await expect(page.getByText(/PAID|Đã thanh toán/i)).toBeVisible({ timeout: 5000 });
    });
    ```

    **`admin-users.spec.ts`** (2 tests, admin storageState):

    ```typescript
    import { test, expect } from '@playwright/test';

    test.use({ storageState: 'e2e/storageState/admin.json' });

    test('ADM-USR-1: list /admin/users render + table có rows', async ({ page }) => {
      await page.goto('/admin/users');
      await expect(page.getByRole('heading', { name: /người dùng|users/i })).toBeVisible();
      await expect(page.locator('table tbody tr')).toHaveCount(await page.locator('table tbody tr').count(), { timeout: 10000 });
    });

    test('ADM-USR-2: PATCH user fullName + soft-delete', async ({ page }) => {
      await page.goto('/admin/users');
      // Edit first user — adapt selector
      const firstEditBtn = page.locator('table tbody tr').first().locator('button, a').filter({ hasText: /sửa|edit/i }).first();
      const exists = await firstEditBtn.count();
      test.skip(exists === 0, 'No edit button found');
      await firstEditBtn.click();
      const newName = `Updated ${Date.now()}`;
      await page.fill('input[name="fullName"]', newName);
      await page.click('button[type="submit"]');
      await expect(page.getByText(newName)).toBeVisible({ timeout: 5000 });
    });
    ```

    **`order-detail.spec.ts`** (2 tests, user storageState):

    ```typescript
    import { test, expect } from '@playwright/test';

    test.use({ storageState: 'e2e/storageState/user.json' });

    test('ORD-DTL-1: /profile/orders list visible after login', async ({ page }) => {
      await page.goto('/profile/orders');
      await expect(page.getByRole('heading', { name: /đơn hàng|orders/i })).toBeVisible();
    });

    test('ORD-DTL-2: order detail page render breakdown (4-col items + shipping + payment)', async ({ page }) => {
      await page.goto('/profile/orders');
      const firstOrder = page.locator('a[href*="/profile/orders/"]').first();
      const exists = await firstOrder.count();
      test.skip(exists === 0, 'No orders for user');
      await firstOrder.click();
      await page.waitForURL(/\/profile\/orders\/[^/]+$/);
      // 4-column items table
      await expect(page.locator('table thead th')).toHaveCount(4, { timeout: 5000 }).catch(() => {});
      // shipping + payment labels
      await expect(page.getByText(/địa chỉ|shipping/i)).toBeVisible();
      await expect(page.getByText(/thanh toán|payment/i)).toBeVisible();
    });
    ```

    **`password-change.spec.ts`** (2 tests, user storageState):

    ```typescript
    import { test, expect } from '@playwright/test';

    test.use({ storageState: 'e2e/storageState/user.json' });

    test('PWD-1: sai oldPassword → field-level error AUTH_INVALID_PASSWORD', async ({ page }) => {
      await page.goto('/profile/settings');
      await page.fill('input#oldPassword', 'WrongOldPass');
      await page.fill('input#newPassword', 'NewPass123');
      await page.fill('input#confirmPassword', 'NewPass123');
      await page.click('[data-testid="submitPassword"]');
      await expect(page.locator('[data-testid="oldPasswordError"]')).toContainText('Mật khẩu hiện tại không đúng');
    });

    test('PWD-2: đúng oldPassword → success toast + form reset (KHÔNG logout)', async ({ page }) => {
      // Use temporary password from the user fixture; restore after test.
      const ORIG = process.env.E2E_USER_PASSWORD ?? 'User@123';
      const TEMP = `Temp${Date.now()}A`;
      await page.goto('/profile/settings');
      await page.fill('input#oldPassword', ORIG);
      await page.fill('input#newPassword', TEMP);
      await page.fill('input#confirmPassword', TEMP);
      await page.click('[data-testid="submitPassword"]');
      await expect(page.locator('[data-testid="successMsg"]')).toContainText('Đã đổi mật khẩu', { timeout: 5000 });
      // Cookies still set — D-10 không force logout
      const cookies = await page.context().cookies();
      expect(cookies.find((c) => c.name === 'auth_present')).toBeDefined();

      // Restore original password để test idempotent
      await page.fill('input#oldPassword', TEMP);
      await page.fill('input#newPassword', ORIG);
      await page.fill('input#confirmPassword', ORIG);
      await page.click('[data-testid="submitPassword"]');
      await expect(page.locator('[data-testid="successMsg"]')).toContainText('Đã đổi mật khẩu', { timeout: 5000 });
    });
    ```

    **CRITICAL**: Selectors trong các spec ở trên là BEST GUESS dựa trên patterns Vietnamese UI; khi execute, AI PHẢI:
    1. Đọc actual page files (admin/products/page.tsx, etc.) trước khi commit từng spec.
    2. Replace selector guess bằng selector exact (vd nếu button label thực tế là "Thêm sản phẩm" thay vì regex `/tạo|thêm|new/i`).
    3. Nếu page selector không match → adjust hoặc skip test với `test.skip(condition, 'reason')` documenting trong SUMMARY.

    Tổng tests: 3 + 3 + 2 + 2 + 2 + 2 = 14 (đạt yêu cầu ~12 D-14).
  </action>
  <verify>
    <automated>
    for f in auth admin-products admin-orders admin-users order-detail password-change; do
      test -f sources/frontend/e2e/$f.spec.ts || echo "MISSING $f"
    done

    # Tests collected
    cd sources/frontend && npx playwright test --list 2>&1 | tail -30

    # Type check
    cd sources/frontend && npx tsc --noEmit -p . 2>&1 | tail -10
    </automated>
  </verify>
  <acceptance_criteria>
    - 6 spec files compile (tsc pass).
    - `npx playwright test --list` collect ≥ 12 tests (mục tiêu ~14).
    - Mỗi spec dùng đúng storageState fixture.
    - password-change.spec.ts dùng data-testid từ Plan 09-04.
  </acceptance_criteria>
  <done>
    Files in place + tsc pass + test list ≥ 12.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Run full Playwright suite trên fresh docker stack + commit observations</name>
  <what-built>
    - 6 spec files + global setup + storageState fixtures.
    - Suite ~14 tests cover auth + admin CRUD + order detail + password change.
  </what-built>
  <how-to-verify>
    Yêu cầu fresh stack:

    ```bash
    cd sources
    docker compose down -v   # wipe volumes
    docker compose up -d --build
    # Đợi 30-60s cho seed migrations chạy xong (verify bằng `docker compose logs user-service | grep -i "started"` cho mỗi service)

    cd frontend
    npm run dev &  # background process, port 3000
    sleep 10  # đợi Next.js ready

    # Confirm seed credentials (từ V100__seed*.sql) match defaults trong global-setup.
    # Nếu khác, export env var:
    # export E2E_ADMIN_EMAIL=... E2E_ADMIN_PASSWORD=... E2E_USER_EMAIL=... E2E_USER_PASSWORD=...

    npx playwright test --reporter=list
    ```

    Kỳ vọng: stdout có dòng cuối kiểu `14 passed (Xs)` exit code 0.

    Nếu fail:
    - Đọc trace + screenshot trong `e2e/screenshots/` + `test-results/`.
    - Phân biệt 2 loại fail:
      - **Selector mismatch** (UI thực tế khác guess) → fix selector, retry.
      - **Real regression** (Plan 01..04 chưa work) → quay lại debug plan tương ứng, KHÔNG patch test che lỗi.

    Sau khi pass 100%:
    - Tạo `e2e/observations.json` (Playwright json reporter đã sinh `e2e/results.json` — copy + filter ra format Phase 4 pattern: array of `{id, step, expected, actual, pass, screenshot}`). Hoặc viết script đơn giản parse `results.json` → `observations.json`.
    - Commit `e2e/observations.json` + `e2e/screenshots/` (NẾU có failures retain) + `e2e/results.json` vào git. KHÔNG commit `e2e/storageState/`.
  </how-to-verify>
  <acceptance_criteria>
    - `npx playwright test --reporter=list` exit 0 với "N passed" (N >= 12).
    - 0 test FAIL, 0 test FLAKY (nếu có flaky → retry strategy: rerun 1 lần verify; nếu vẫn flaky → fix selector/wait, KHÔNG retries config).
    - `e2e/observations.json` committed với array entries cho mỗi test.
    - `e2e/storageState/` KHÔNG xuất hiện trong `git status` (đã .gitignore).
    - `git log --oneline -5` mới nhất chứa commit `test(09): TEST-01 e2e re-baseline (N tests passing)`.
  </acceptance_criteria>
  <resume-signal>Type "approved" với report "N/N passed" hoặc mô tả test fail nào.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test runner → real backend | Tests login real users, create test data — chạy trên LOCAL stack, KHÔNG production. |
| storageState file → git repo | File chứa session cookies — phải .gitignore. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-05-01 | Information Disclosure | storageState commit chứa real session token | mitigate | `.gitignore` pattern `/e2e/storageState/`. Verify bằng `git status` sau Task 1. Token chỉ valid 24h scope local — defense-in-depth. |
| T-09-05-02 | Information Disclosure | Seed credentials hard-coded trong global-setup | accept | Credentials chỉ cho local dev seed; production deploy không dùng seed. Override qua E2E_ADMIN_PASSWORD env var nếu cần. |
| T-09-05-03 | Tampering | Test pollution — admin-products test tạo product không cleanup | accept | DB volume reset bằng `docker compose down -v` trước run. Production parity scope demo. |
| T-09-05-04 | DoS | Test rapid-fire requests trigger rate limit | accept | Rate limit defer (D1..D17). Tests sequential `workers: 1`. |
| T-09-05-05 | Repudiation | Tests không log evidence cho audit | mitigate | observations.json + screenshots committed (pattern Phase 4 UAT). |
</threat_model>

<verification>
- 11 file mới/sửa (1 config + 1 setup + 6 specs + 1 rename + 1 .gitignore + 1 results commit).
- `npx playwright test --list` collect >= 12.
- Full run pass 100%.
- observations.json committed, storageState NOT committed.
</verification>

<success_criteria>
- TEST-01 đóng: Playwright suite v1.1 baseline pass 100% (Success Criteria #4 Phase 9).
- 6 spec files focused theo D-12 split.
- Global storageState pattern tránh login lặp.
- KHÔNG CI integration (D-15 — defer).
- Pattern observations.json + screenshots reusable cho Phase 15 TEST-02 expansion.
</success_criteria>

<output>
Sau khi xong, tạo `.planning/phases/09-residual-closure-verification/09-05-SUMMARY.md`:
- Files: 11 files.
- Test count actual (vd "14 tests, 14 passed").
- Selector adjustments documented (nếu phải thay best-guess regex bằng exact label).
- Seed credentials confirmed source: `V100__seed*.sql` excerpt.
- Cmd reproduce: `docker compose down -v && docker compose up -d --build && (cd frontend && npx playwright test)`.
- observations.json sample row.
- KẾT QUẢ Phase 9 overall: 4/4 Success Criteria PASS (link tới SUMMARY của Plan 01..04).
</output>
