---
phase: 09-residual-closure-verification
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/web/UserMeController.java
  - sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/web/ChangePasswordRequest.java
  - sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/service/UserPasswordService.java
  - sources/backend/api-gateway/src/main/resources/application.yml
autonomous: true
requirements:
  - AUTH-07
must_haves:
  truths:
    - "POST /api/users/me/password với valid token + đúng oldPassword + newPassword hợp lệ → 200, BCrypt hash trong DB thay đổi"
    - "POST /api/users/me/password với SAI oldPassword → 422 + body chứa errorCode='AUTH_INVALID_PASSWORD' (passwordHash KHÔNG đổi)"
    - "POST /api/users/me/password thiếu Authorization header → 401"
    - "POST /api/users/me/password với newPassword < 8 ký tự → 400 (validation)"
    - "Gateway route mapping: /api/users/me/password → user-service /users/me/password"
    - "JWT cũ vẫn valid sau đổi password (D-10: KHÔNG rotate token, KHÔNG force logout)"
  artifacts:
    - path: "sources/backend/user-service/.../UserMeController.java"
      provides: "POST /users/me/password endpoint"
      contains: "@PostMapping(\"/me/password\")"
    - path: "sources/backend/user-service/.../ChangePasswordRequest.java"
      provides: "DTO record {oldPassword, newPassword}"
      contains: "record ChangePasswordRequest"
    - path: "sources/backend/user-service/.../UserPasswordService.java"
      provides: "BCrypt verify oldPassword + encode newPassword + save"
      contains: "AUTH_INVALID_PASSWORD"
    - path: "sources/backend/api-gateway/.../application.yml"
      provides: "Route /api/users/me/** rewrite → /users/me/** (đứng TRƯỚC user-service-base)"
  key_links:
    - from: "Frontend POST /api/users/me/password"
      to: "Gateway route id=user-service-me"
      via: "Spring Cloud Gateway predicate Path=/api/users/me/**"
      pattern: "user-service-me"
    - from: "UserMeController.changePassword()"
      to: "UserPasswordService.changePassword(userId, body)"
      via: "JWT parse → load entity → BCrypt verify → save"
      pattern: "passwordEncoder\\.matches"
---

<objective>
Đóng AUTH-07 backend half: tạo endpoint dedicated `POST /api/users/me/password` nhận `{oldPassword, newPassword}`, BCrypt verify oldPassword TRƯỚC khi update. Wrong oldPassword → 422 với error code `AUTH_INVALID_PASSWORD`.

Purpose: Cho phép user logged-in tự đổi password an toàn (re-authentication). Endpoint TÁCH RIÊNG khỏi PATCH chung (per AUTH-07 requirement: "KHÔNG cho phép password trong PATCH chung").

Output:
- Controller `UserMeController` với `@PostMapping("/me/password")` (file mới — Phase 10 sẽ extend với `/me` PATCH).
- DTO `ChangePasswordRequest` (record).
- Service `UserPasswordService` (tách khỏi UserCrudService để cô lập password concern).
- Gateway route mới `user-service-me` ĐỨNG TRƯỚC `user-service-base` (chuẩn bị cho Phase 10 me/* routes).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-residual-closure-verification/09-CONTEXT.md
@sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/service/AuthService.java
@sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/jwt/JwtUtils.java
@sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/web/AuthController.java
@sources/backend/api-gateway/src/main/resources/application.yml
</context>

<interfaces>
<!-- ApiResponse + ApiErrorResponse pattern v1.0:
- Success: ApiResponse.of(200, "message", data) → JSON {status, message, data, traceId}
- Error: GlobalExceptionHandler bắt ResponseStatusException → ApiErrorResponse {status, message, errorCode?, traceId}

Phase 9 yêu cầu errorCode = "AUTH_INVALID_PASSWORD". Phải verify GlobalExceptionHandler có support `errorCode` field. Nếu CHƯA, có 2 path:
  Option A: throw custom exception class `InvalidPasswordException extends RuntimeException` + `@ResponseStatus(422)` + handler trả body `{message, errorCode}`.
  Option B: thêm `errorCode` vào ResponseStatusException reason field bằng pattern "AUTH_INVALID_PASSWORD: <human msg>" + frontend split colon. UGLY → prefer A.

Khi execute, đọc `sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/exception/` (hoặc `api/`) để tìm `ApiErrorResponse` + `GlobalExceptionHandler`. Nếu envelope đã hỗ trợ errorCode optional → reuse. Nếu KHÔNG → tạo custom exception (Option A).
-->

<!-- BCrypt pattern (đã dùng trong AuthService.login):
- passwordEncoder.matches(rawOldPassword, entity.passwordHash()) → boolean
- passwordEncoder.encode(rawNewPassword) → String hash
- PasswordEncoder bean đã được Spring auto-config (BCryptPasswordEncoder mặc định) ở user-service Phase 6.
-->

<!-- UserEntity (Phase 5+6 baseline):
- entity.passwordHash() → getter
- Cần setter / withPasswordHash để update. Verify field final immutable hay mutable. Nếu immutable → tạo `entity = entity.withPasswordHash(newHash)` rồi `userRepo.save(entity)`. Nếu mutable JPA-managed → `entity.setPasswordHash(newHash)` + Hibernate dirty checking.
-->

<!-- JWT extract userId trong endpoint /me:
- JwtUtils.parseToken(token).getSubject() → userId (string UUID)
- Pattern manual parse Bearer header (giống JwtRoleGuard ở Plan 09-02 nhưng cần userId, không phải role check).
-->

<!-- Gateway route order (Spring Cloud Gateway):
Routes match theo thứ tự khai báo trong application.yml — first match wins. Vậy `user-service-me` PHẢI khai báo TRƯỚC `user-service-base`. Routes hiện tại order: auth-base → auth → admin-base → admin → base → catch-all. Phải insert me-base + me sau admin nhưng TRƯỚC base.
-->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Backend POST /users/me/password endpoint + BCrypt verify + AUTH_INVALID_PASSWORD error</name>
  <files>
    sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/web/ChangePasswordRequest.java,
    sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/service/UserPasswordService.java,
    sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/web/UserMeController.java
  </files>
  <read_first>
    - sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/service/AuthService.java — pattern PasswordEncoder + UserRepository + ResponseStatusException.
    - sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/jwt/JwtUtils.java — `parseToken(token).getSubject()` để extract userId.
    - sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/web/AuthController.java — pattern @RestController + ApiResponse.
    - sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/domain/UserEntity.java — verify field `passwordHash` mutable hay immutable record.
    - sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/api/ApiErrorResponse.java (hoặc tương đương) — verify envelope có support `errorCode` field.
    - sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/exception/ — verify có `GlobalExceptionHandler` không + cách bind errorCode.
  </read_first>
  <action>
    **Decision matrix dựa trên kết quả `read_first`:**
    - Nếu `ApiErrorResponse` + `GlobalExceptionHandler` đã hỗ trợ `errorCode` field → service throw `ResponseStatusException` với reason `"AUTH_INVALID_PASSWORD"` + handler bind sang errorCode. CONFIRM bằng grep `errorCode` trong handler trước khi commit.
    - Nếu CHƯA hỗ trợ → tạo class custom exception `InvalidPasswordException` (subclass `RuntimeException`) + thêm `@ExceptionHandler(InvalidPasswordException.class)` trả body JSON `{status: 422, errorCode: "AUTH_INVALID_PASSWORD", message: "Mật khẩu hiện tại không đúng", traceId}`.

    **Bước 1: Tạo DTO `ChangePasswordRequest.java`:**

    ```java
    package com.ptit.htpt.userservice.web;

    import jakarta.validation.constraints.NotBlank;
    import jakarta.validation.constraints.Pattern;
    import jakarta.validation.constraints.Size;

    /**
     * Phase 9 / Plan 09-03 (AUTH-07).
     * D-11: endpoint dedicated POST /users/me/password.
     *
     * Validation: newPassword min 8 + ít nhất 1 letter + 1 number (Claude's Discretion CONTEXT.md).
     */
    public record ChangePasswordRequest(
        @NotBlank(message = "oldPassword required")
        String oldPassword,

        @NotBlank(message = "newPassword required")
        @Size(min = 8, message = "newPassword must be at least 8 characters")
        @Pattern(regexp = ".*[A-Za-z].*", message = "newPassword must contain at least 1 letter")
        @Pattern(regexp = ".*\\d.*", message = "newPassword must contain at least 1 number")
        String newPassword
    ) {}
    ```

    **Bước 2: Tạo `UserPasswordService.java`:**

    ```java
    package com.ptit.htpt.userservice.service;

    import com.ptit.htpt.userservice.domain.UserEntity;
    import com.ptit.htpt.userservice.repository.UserRepository;
    import com.ptit.htpt.userservice.web.ChangePasswordRequest;
    import org.springframework.http.HttpStatus;
    import org.springframework.security.crypto.password.PasswordEncoder;
    import org.springframework.stereotype.Service;
    import org.springframework.transaction.annotation.Transactional;
    import org.springframework.web.server.ResponseStatusException;

    /**
     * Phase 9 / Plan 09-03 (AUTH-07). Cô lập password concern khỏi UserCrudService.
     *
     * D-10: sau success — KHÔNG rotate JWT, KHÔNG force logout (token cũ vẫn valid 24h).
     * D-11: wrong oldPassword → 422 errorCode AUTH_INVALID_PASSWORD.
     */
    @Service
    @Transactional
    public class UserPasswordService {
      private final UserRepository userRepo;
      private final PasswordEncoder passwordEncoder;

      public UserPasswordService(UserRepository userRepo, PasswordEncoder passwordEncoder) {
        this.userRepo = userRepo;
        this.passwordEncoder = passwordEncoder;
      }

      public void changePassword(String userId, ChangePasswordRequest req) {
        UserEntity entity = userRepo.findById(userId)
            .orElseThrow(() -> new ResponseStatusException(HttpStatus.NOT_FOUND, "User not found"));

        if (!passwordEncoder.matches(req.oldPassword(), entity.passwordHash())) {
          // 422 + errorCode AUTH_INVALID_PASSWORD (D-11).
          // Reason field "AUTH_INVALID_PASSWORD" sẽ được GlobalExceptionHandler bind sang body.errorCode.
          // Nếu codebase chưa support errorCode binding → đổi sang custom exception class (xem decision matrix).
          throw new ResponseStatusException(HttpStatus.UNPROCESSABLE_ENTITY, "AUTH_INVALID_PASSWORD");
        }

        String newHash = passwordEncoder.encode(req.newPassword());
        // UserEntity mutable JPA-managed: setter. Nếu immutable record/withers — adapt theo UserEntity actual API.
        UserEntity updated = entity.withPasswordHash(newHash); // hoặc entity.setPasswordHash(newHash)
        userRepo.save(updated);
      }
    }
    ```

    Note: nếu `UserEntity` KHÔNG có `withPasswordHash` và là JPA mutable → đổi thành `entity.setPasswordHash(newHash); userRepo.save(entity);`. Pick branch dựa trên file đọc ở `read_first`.

    **Bước 3: Tạo `UserMeController.java`:**

    ```java
    package com.ptit.htpt.userservice.web;

    import com.ptit.htpt.userservice.api.ApiResponse;
    import com.ptit.htpt.userservice.jwt.JwtUtils;
    import com.ptit.htpt.userservice.service.UserPasswordService;
    import io.jsonwebtoken.Claims;
    import jakarta.validation.Valid;
    import java.util.Map;
    import org.springframework.http.HttpHeaders;
    import org.springframework.http.HttpStatus;
    import org.springframework.web.bind.annotation.PostMapping;
    import org.springframework.web.bind.annotation.RequestBody;
    import org.springframework.web.bind.annotation.RequestHeader;
    import org.springframework.web.bind.annotation.RequestMapping;
    import org.springframework.web.bind.annotation.RestController;
    import org.springframework.web.server.ResponseStatusException;

    /**
     * Phase 9 / Plan 09-03 (AUTH-07). Endpoint user-self /me/* group.
     * Phase 10 sẽ extend với GET/PATCH /me cho profile editing — controller này
     * là foundation. Trong Phase 9 chỉ map password change.
     *
     * Gateway route /api/users/me/** ĐỨNG TRƯỚC user-service-base (xem application.yml).
     */
    @RestController
    @RequestMapping("/users/me")
    public class UserMeController {
      private final UserPasswordService passwordService;
      private final JwtUtils jwtUtils;

      public UserMeController(UserPasswordService passwordService, JwtUtils jwtUtils) {
        this.passwordService = passwordService;
        this.jwtUtils = jwtUtils;
      }

      @PostMapping("/password")
      public ApiResponse<Map<String, Object>> changePassword(
          @RequestHeader(value = HttpHeaders.AUTHORIZATION, required = false) String authHeader,
          @Valid @RequestBody ChangePasswordRequest body
      ) {
        String userId = extractUserIdFromBearer(authHeader);
        passwordService.changePassword(userId, body);
        return ApiResponse.of(200, "Đã đổi mật khẩu", Map.of("changed", true));
      }

      private String extractUserIdFromBearer(String authHeader) {
        if (authHeader == null || !authHeader.startsWith("Bearer ")) {
          throw new ResponseStatusException(HttpStatus.UNAUTHORIZED, "Missing or invalid Authorization header");
        }
        String token = authHeader.substring("Bearer ".length()).trim();
        try {
          Claims claims = jwtUtils.parseToken(token);
          return claims.getSubject();
        } catch (Exception e) {
          throw new ResponseStatusException(HttpStatus.UNAUTHORIZED, "Invalid token");
        }
      }
    }
    ```

    **Bước 4: GlobalExceptionHandler — verify errorCode binding:**

    Đọc `sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/api/` (và `exception/` nếu có) tìm class `*ExceptionHandler*`. Nếu handler hiện tại bind `ResponseStatusException.getReason()` thành `body.message`, thêm logic:

    ```java
    // Pseudo — adapt theo handler actual:
    String reason = ex.getReason();
    String errorCode = null;
    String message = reason;
    // Nếu reason là 1 token uppercase + underscores → treat as errorCode
    if (reason != null && reason.matches("^[A-Z][A-Z_]+$")) {
      errorCode = reason;
      message = "Mật khẩu hiện tại không đúng"; // map theo errorCode hoặc giữ reason
    }
    // build ApiErrorResponse với cả message lẫn errorCode (errorCode optional, null nếu không phải code)
    ```

    Nếu mod handler quá invasive → fallback Option A: tạo `InvalidPasswordException` class + `@ExceptionHandler` riêng trong `GlobalExceptionHandler` trả body `{status:422, errorCode:"AUTH_INVALID_PASSWORD", message:"Mật khẩu hiện tại không đúng", traceId}`. Service throw `InvalidPasswordException` thay vì `ResponseStatusException`. Document choice trong SUMMARY.
  </action>
  <verify>
    <automated>
    cd sources/backend/user-service && mvn clean compile -q 2>&1 | tail -5

    test -f sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/web/ChangePasswordRequest.java
    test -f sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/web/UserMeController.java
    test -f sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/service/UserPasswordService.java

    grep -E "@RequestMapping\\(\"/users/me\"\\)" sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/web/UserMeController.java
    grep -E "@PostMapping\\(\"/password\"\\)" sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/web/UserMeController.java
    grep -E "passwordEncoder\\.matches" sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/service/UserPasswordService.java
    grep -E "AUTH_INVALID_PASSWORD" sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/service/UserPasswordService.java
    grep -E "@Size\\(min = 8" sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/web/ChangePasswordRequest.java

    cd sources/backend/user-service && mvn package -DskipTests -q 2>&1 | tail -5
    </automated>
  </verify>
  <acceptance_criteria>
    - 3 file mới + edit GlobalExceptionHandler (nếu cần) compile pass.
    - `mvn package -DskipTests` exit 0.
    - Endpoint mapped `POST /users/me/password` (bên trong service container, gateway route ở Task 2).
    - BCrypt verify oldPassword TRƯỚC khi encode newPassword (verify code path bằng grep).
    - Validation `@Size(min=8)` + 2 `@Pattern` cho letter + number.
  </acceptance_criteria>
  <done>
    Compile + package pass + grep checks PASS + decision branch (errorCode handler vs custom exception) documented trong SUMMARY.
  </done>
</task>

<task type="auto">
  <name>Task 2: Gateway route user-service-me + curl smoke 4 case</name>
  <files>sources/backend/api-gateway/src/main/resources/application.yml</files>
  <read_first>
    - sources/backend/api-gateway/src/main/resources/application.yml lines 22-62 — verify thứ tự routes hiện tại: auth-base → auth → admin-base → admin → base → catch-all. Phải insert `user-service-me-base` + `user-service-me` SAU admin nhưng TRƯỚC base (line 51).
  </read_first>
  <action>
    Mở `sources/backend/api-gateway/src/main/resources/application.yml`. Tìm block routes user-service. Sau route `id: user-service-admin` (kết thúc khoảng line 48) và TRƯỚC `id: user-service-base` (line 51), insert:

    ```yaml
        # user-service me: /api/users/me/** → /users/me/** (PHẢI đứng TRƯỚC user-service-base
        # để /api/users/me/password KHÔNG match catch-all /api/users/{id} với id="me/password".
        # Phase 10 sẽ extend với GET/PATCH /api/users/me cho profile editing.)
        - id: user-service-me-base
          uri: http://user-service:8080
          predicates:
            - Path=/api/users/me
          filters:
            - RewritePath=/api/users/me, /users/me
        - id: user-service-me
          uri: http://user-service:8080
          predicates:
            - Path=/api/users/me/**
          filters:
            - RewritePath=/api/users/me/(?<seg>.*), /users/me/${seg}
    ```

    Lưu ý:
    - YAML indentation 2 spaces, nested 8 spaces từ root (theo style hiện tại của file — verify bằng cách align với routes lân cận).
    - GIỮ NGUYÊN routes auth/admin/base — chỉ insert 2 routes mới.

    **Verify route order pháp lý:**

    Routes order final cho user-service phải là:
    1. `user-service-auth-base` + `user-service-auth`
    2. `user-service-admin-base` + `user-service-admin`
    3. `user-service-me-base` + `user-service-me` ← MỚI
    4. `user-service-base` + `user-service` (catch-all)

    Nếu route 3 đứng SAU 4 → request `/api/users/me/password` sẽ match catch-all rewrite thành `/users/me/password` (vẫn work tình cờ vì path map vẫn đúng), nhưng Phase 10 PATCH `/api/users/me` (không trailing) sẽ fail vì `user-service-base` chỉ predicate `/api/users` exact. Vậy giữ thứ tự đúng để tương lai.

    **Curl smoke (sau khi `docker compose up -d --build api-gateway user-service` xong):**

    ```bash
    # Login lấy token user thường
    USER_TOKEN=$(curl -s -X POST http://localhost:8080/api/users/auth/login \
      -H "Content-Type: application/json" \
      -d '{"email":"user@example.com","password":"<correct-password>"}' \
      | jq -r '.data.token')

    # Case 1: đúng oldPassword + valid newPassword → 200
    curl -i -X POST http://localhost:8080/api/users/me/password \
      -H "Authorization: Bearer $USER_TOKEN" \
      -H "Content-Type: application/json" \
      -d '{"oldPassword":"<correct-password>","newPassword":"NewPass123"}'
    # Kỳ vọng: HTTP/1.1 200, body chứa "Đã đổi mật khẩu"

    # Case 2: SAI oldPassword → 422 + errorCode AUTH_INVALID_PASSWORD
    curl -i -X POST http://localhost:8080/api/users/me/password \
      -H "Authorization: Bearer $USER_TOKEN" \
      -H "Content-Type: application/json" \
      -d '{"oldPassword":"wrong","newPassword":"NewPass123"}'
    # Kỳ vọng: HTTP/1.1 422, body JSON contain "AUTH_INVALID_PASSWORD"

    # Case 3: thiếu Authorization header → 401
    curl -i -X POST http://localhost:8080/api/users/me/password \
      -H "Content-Type: application/json" \
      -d '{"oldPassword":"x","newPassword":"NewPass123"}'
    # Kỳ vọng: HTTP/1.1 401

    # Case 4: newPassword < 8 ký tự → 400 (validation @Size)
    curl -i -X POST http://localhost:8080/api/users/me/password \
      -H "Authorization: Bearer $USER_TOKEN" \
      -H "Content-Type: application/json" \
      -d '{"oldPassword":"NewPass123","newPassword":"short"}'
    # Kỳ vọng: HTTP/1.1 400
    ```

    Document 4 case + actual responses trong SUMMARY.
  </action>
  <verify>
    <automated>
    # Routes mới có trong application.yml
    grep -E "id:\\s*user-service-me-base" sources/backend/api-gateway/src/main/resources/application.yml
    grep -E "id:\\s*user-service-me$" sources/backend/api-gateway/src/main/resources/application.yml
    grep -E "Path=/api/users/me/\\*\\*" sources/backend/api-gateway/src/main/resources/application.yml

    # Route order verification: me-base/me line numbers PHẢI đứng TRƯỚC base line number
    ME_LINE=$(grep -n "id:\\s*user-service-me$" sources/backend/api-gateway/src/main/resources/application.yml | cut -d: -f1)
    BASE_LINE=$(grep -n "id:\\s*user-service-base" sources/backend/api-gateway/src/main/resources/application.yml | cut -d: -f1)
    [ "$ME_LINE" -lt "$BASE_LINE" ] && echo "ROUTE ORDER OK ($ME_LINE < $BASE_LINE)" || echo "ROUTE ORDER WRONG"

    # Gateway compile (Spring Cloud Gateway parse YAML on startup — build pass)
    cd sources/backend/api-gateway && mvn clean package -DskipTests -q 2>&1 | tail -5
    </automated>
  </verify>
  <acceptance_criteria>
    - 2 routes mới `user-service-me-base` + `user-service-me` xuất hiện trong application.yml.
    - Line number của `user-service-me` < line number của `user-service-base` (route order strict).
    - Gateway `mvn package -DskipTests` exit 0.
    - 4 curl case documented trong SUMMARY (Case 1 → 200, Case 2 → 422 errorCode, Case 3 → 401, Case 4 → 400).
  </acceptance_criteria>
  <done>
    YAML edit + route order check PASS + 4 curl cases verified end-to-end qua gateway.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Logged-in user → /api/users/me/password | Bearer token verified bằng JWT signature; userId trong token MỚI là source of truth (KHÔNG nhận userId từ body/path). |
| User-service → DB | BCrypt hash mới ghi qua `userRepo.save()`; old hash bị overwrite. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03-01 | Elevation of Privilege | Đổi password mà không re-auth (kẻ tấn công có session token nhưng không biết password cũ) | mitigate | `passwordEncoder.matches(req.oldPassword(), entity.passwordHash())` BẮT BUỘC trước save. Wrong → 422 stop. |
| T-09-03-02 | Spoofing | Body chứa userId override để đổi password user khác | mitigate | Endpoint chỉ extract userId từ JWT subject (`claims.getSubject()`). KHÔNG có path/body param userId. |
| T-09-03-03 | Information Disclosure | Password leak trong logs (request body / exception message) | mitigate | `ChangePasswordRequest` KHÔNG override `toString()` → record default include field, có risk. Mitigation: KHÔNG log request body trong controller. Throw `ResponseStatusException` reason chỉ chứa errorCode (KHÔNG password). Verify TraceIdFilter không dump body. |
| T-09-03-04 | Tampering | Hash collision / brute force online | accept | BCrypt strength default 10 rounds; rate limit defer (D1..D17). |
| T-09-03-05 | Denial of Service | Brute force online attack | accept | Rate limit defer. Demo scope. |
| T-09-03-06 | Repudiation | User claim "tôi không đổi password" | accept | Audit log defer. Demo scope. |
| T-09-03-07 | Information Disclosure | Error message tiết lộ user tồn tại / không (vd "User not found" vs "AUTH_INVALID_PASSWORD") | accept | Endpoint `/me/*` với token valid → user phải tồn tại (token issued cho user thật). Edge case userId trong token nhưng đã soft-deleted → 404 generic; KHÔNG leak credential info. |
</threat_model>

<verification>
- 3 file Java mới + 1 file YAML edit + (có thể) 1 file handler edit compile pass.
- `mvn package -DskipTests` cho user-service + api-gateway exit 0.
- Route order check: `user-service-me` line < `user-service-base` line.
- 4 curl case end-to-end qua gateway pass.
</verification>

<success_criteria>
- AUTH-07 backend đóng: endpoint `POST /api/users/me/password` reachable qua gateway, BCrypt verify oldPassword, errorCode AUTH_INVALID_PASSWORD khi sai.
- D-10 honored: KHÔNG rotate JWT, KHÔNG force logout (controller chỉ trả 200, không invalidate session).
- Gateway route foundation cho Phase 10 me/* (tương lai PATCH /me) đã sẵn sàng.
</success_criteria>

<output>
Sau khi xong, tạo `.planning/phases/09-residual-closure-verification/09-03-SUMMARY.md`:
- Files created: ChangePasswordRequest, UserPasswordService, UserMeController.
- Decision branch dùng cho errorCode binding (handler edit vs custom InvalidPasswordException).
- Gateway YAML diff (2 routes mới insert tại line N).
- 4 curl cases với actual HTTP status + body excerpt.
- Note nhắc Plan 09-04 frontend submit form vào endpoint này.
</output>
