---
phase: 09-residual-closure-verification
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - sources/backend/product-service/src/main/java/com/ptit/htpt/productservice/web/AdminStatsController.java
  - sources/backend/product-service/src/main/java/com/ptit/htpt/productservice/service/ProductStatsService.java
  - sources/backend/product-service/src/main/java/com/ptit/htpt/productservice/web/JwtRoleGuard.java
  - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/web/AdminStatsController.java
  - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/service/OrderStatsService.java
  - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/web/JwtRoleGuard.java
  - sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/web/AdminStatsController.java
  - sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/service/UserStatsService.java
  - sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/web/JwtRoleGuard.java
autonomous: true
requirements:
  - UI-02
must_haves:
  truths:
    - "GET /api/products/admin/stats với admin Bearer token → 200 {totalProducts: int}"
    - "GET /api/orders/admin/stats với admin Bearer token → 200 {totalOrders: int, pendingOrders: int} (pending = PENDING ONLY)"
    - "GET /api/users/admin/stats với admin Bearer token → 200 {totalUsers: int}"
    - "Cùng endpoint với user role=USER token → 403"
    - "Cùng endpoint không có Authorization header → 401"
  artifacts:
    - path: "sources/backend/product-service/.../AdminStatsController.java"
      provides: "GET /admin/products/stats endpoint + manual JWT role check"
      contains: "@RequestMapping(\"/admin/products\")"
    - path: "sources/backend/order-service/.../AdminStatsController.java"
      provides: "GET /admin/orders/stats endpoint"
      contains: "pendingOrders"
    - path: "sources/backend/user-service/.../AdminStatsController.java"
      provides: "GET /admin/users/stats endpoint"
      contains: "totalUsers"
    - path: "sources/backend/{product,order,user}-service/.../JwtRoleGuard.java"
      provides: "Manual JWT parser + role check helper (NOT @PreAuthorize per D-05 REVISED)"
  key_links:
    - from: "Gateway route /api/products/admin/stats"
      to: "product-service AdminStatsController.stats()"
      via: "Spring Cloud Gateway RewritePath /api/products/admin/(?<seg>.*) → /admin/products/${seg}"
      pattern: "RewritePath=/api/products/admin"
    - from: "AdminStatsController"
      to: "JwtRoleGuard.requireAdmin(authHeader)"
      via: "manual call before service invocation"
      pattern: "requireAdmin"
---

<objective>
Phục vụ UI-02 backend half: tạo 3 stats endpoints admin-only ở 3 service (product/order/user) trả về số liệu thật cho admin dashboard. Mỗi endpoint dùng path convention `/admin/{svc}/stats` + manual JWT role check trong controller (per D-05 REVISED — KHÔNG `@PreAuthorize`, KHÔNG Spring Security setup mới).

Purpose: Admin login `/admin` thấy 4 KPI thật (Total products, Total orders, Total users, Pending orders) thay vì 0. Frontend (Plan 04) sẽ Promise.allSettled qua 3 endpoints này.

Output:
- 3 controllers `AdminStatsController` (mỗi service 1 file).
- 3 service layer methods (count query — `.count()` JPA repo).
- 3 `JwtRoleGuard` helper classes (parse Bearer token, reject 403/401).
- Gateway routes ĐÃ có (`/api/{products,orders,users}/admin/**` rewrite sang `/admin/{svc}/**`) — verify pattern khớp `/admin/products/stats`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-residual-closure-verification/09-CONTEXT.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/INTEGRATIONS.md
@sources/backend/api-gateway/src/main/resources/application.yml
@sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/web/AdminUserController.java
@sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/jwt/JwtUtils.java
@sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/web/AdminOrderController.java
</context>

<interfaces>
<!-- Gateway routes (đã verify trong api-gateway/application.yml — KHÔNG cần edit):
- /api/products/admin/**  → product-service:8080 RewritePath → /admin/products/**
- /api/orders/admin/**    → order-service:8080   RewritePath → /admin/orders/**
- /api/users/admin/**     → user-service:8080    RewritePath → /admin/users/**

Vậy controller path TRONG service phải là:
- product-service: `@RequestMapping("/admin/products")` + `@GetMapping("/stats")`
- order-service:   `@RequestMapping("/admin/orders")` + `@GetMapping("/stats")`
- user-service:    `@RequestMapping("/admin/users")` + `@GetMapping("/stats")`

CHÚ Ý: user-service đã có `AdminUserController` với `@RequestMapping("/admin/users")`. Tạo MỚI `AdminStatsController` với `@RequestMapping("/admin/users")` Spring sẽ allow vì chỉ trùng base path, method `/stats` chưa map ở AdminUserController. ALTERNATIVE: thêm `@GetMapping("/stats")` vào `AdminUserController` hiện tại. Kế hoạch: TẠO MỚI `AdminStatsController` để cô lập concern + dễ test (KHÔNG đụng AdminUserController).
-->

<!-- JWT shape (từ JwtUtils.java):
- Algorithm: HS256
- Claims: sub=userId, username, roles (string "USER" hoặc "ADMIN" hoặc "ADMIN,USER")
- Secret: ${app.jwt.secret} (mỗi service đã có config — copy y hệt user-service)
- Parse: Jwts.parser().verifyWith(key).build().parseSignedClaims(token).getPayload()
-->

<!-- ApiResponse envelope (từ ApiResponse.java pattern v1.0):
ApiResponse.of(200, "message", data) → JSON `{status, message, data, traceId}`
-->

<!-- JPA repos đã có (giả định v1.1 baseline):
- product-service: ProductRepository extends JpaRepository<ProductEntity, String> — có `.count()`
- order-service:   OrderRepository extends JpaRepository<OrderEntity, String>     — có `.count()` + cần thêm `countByOrderStatus(String status)`
- user-service:    UserRepository extends JpaRepository<UserEntity, String>       — có `.count()`
-->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: product-service + user-service stats endpoints + JwtRoleGuard helper</name>
  <files>
    sources/backend/product-service/src/main/java/com/ptit/htpt/productservice/web/JwtRoleGuard.java,
    sources/backend/product-service/src/main/java/com/ptit/htpt/productservice/web/AdminStatsController.java,
    sources/backend/product-service/src/main/java/com/ptit/htpt/productservice/service/ProductStatsService.java,
    sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/web/JwtRoleGuard.java,
    sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/web/AdminStatsController.java,
    sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/service/UserStatsService.java
  </files>
  <read_first>
    - sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/jwt/JwtUtils.java — JJWT 0.12.x parse pattern + claim "roles" shape (string "USER" / "ADMIN" / "ADMIN,USER").
    - sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/web/AdminUserController.java — pattern `@RestController` + `@RequestMapping("/admin/users")` + `ApiResponse.of(...)`.
    - sources/backend/product-service/src/main/java/com/ptit/htpt/productservice/repository/ — verify ProductRepository tồn tại + extends JpaRepository (có `.count()`).
    - sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/repository/UserRepository.java — verify pattern.
    - sources/backend/api-gateway/src/main/resources/application.yml — confirm route `/api/products/admin/**` + `/api/users/admin/**` đã có (lines 64-76 cho products, 36-48 cho users).
  </read_first>
  <action>
    **Phần A — product-service:**

    1. Tạo `sources/backend/product-service/src/main/java/com/ptit/htpt/productservice/web/JwtRoleGuard.java`:

    ```java
    package com.ptit.htpt.productservice.web;

    import io.jsonwebtoken.Claims;
    import io.jsonwebtoken.Jwts;
    import io.jsonwebtoken.security.Keys;
    import java.nio.charset.StandardCharsets;
    import javax.crypto.SecretKey;
    import org.springframework.beans.factory.annotation.Value;
    import org.springframework.http.HttpStatus;
    import org.springframework.stereotype.Component;
    import org.springframework.web.server.ResponseStatusException;

    /**
     * Phase 9 / Plan 09-02 (UI-02 backend, D-05 REVISED).
     *
     * Manual JWT role check — KHÔNG dùng @PreAuthorize (codebase chưa setup Spring Security).
     * Dùng cho stats endpoints admin-only. Spring Security setup defer cho future hardening.
     *
     * Behavior:
     * - Header thiếu hoặc không phải Bearer → 401 UNAUTHORIZED "Missing or invalid Authorization header"
     * - Token invalid/expired → 401 UNAUTHORIZED "Invalid token"
     * - Token valid nhưng roles claim không chứa ADMIN → 403 FORBIDDEN "ADMIN role required"
     */
    @Component
    public class JwtRoleGuard {
      @Value("${app.jwt.secret}")
      private String jwtSecret;

      private SecretKey getSigningKey() {
        return Keys.hmacShaKeyFor(jwtSecret.getBytes(StandardCharsets.UTF_8));
      }

      public void requireAdmin(String authorizationHeader) {
        if (authorizationHeader == null || !authorizationHeader.startsWith("Bearer ")) {
          throw new ResponseStatusException(HttpStatus.UNAUTHORIZED, "Missing or invalid Authorization header");
        }
        String token = authorizationHeader.substring("Bearer ".length()).trim();
        Claims claims;
        try {
          claims = Jwts.parser().verifyWith(getSigningKey()).build()
              .parseSignedClaims(token).getPayload();
        } catch (Exception e) {
          throw new ResponseStatusException(HttpStatus.UNAUTHORIZED, "Invalid token");
        }
        Object rolesClaim = claims.get("roles");
        String roles = rolesClaim == null ? "" : rolesClaim.toString();
        // Roles shape: "USER" | "ADMIN" | "ADMIN,USER" — split bằng comma
        boolean isAdmin = false;
        for (String r : roles.split(",")) {
          if ("ADMIN".equals(r.trim())) { isAdmin = true; break; }
        }
        if (!isAdmin) {
          throw new ResponseStatusException(HttpStatus.FORBIDDEN, "ADMIN role required");
        }
      }
    }
    ```

    2. Tạo `sources/backend/product-service/src/main/java/com/ptit/htpt/productservice/service/ProductStatsService.java`:

    ```java
    package com.ptit.htpt.productservice.service;

    import com.ptit.htpt.productservice.repository.ProductRepository;
    import org.springframework.stereotype.Service;
    import org.springframework.transaction.annotation.Transactional;

    @Service
    public class ProductStatsService {
      private final ProductRepository productRepo;

      public ProductStatsService(ProductRepository productRepo) {
        this.productRepo = productRepo;
      }

      @Transactional(readOnly = true)
      public long totalProducts() {
        return productRepo.count();
      }
    }
    ```

    3. Tạo `sources/backend/product-service/src/main/java/com/ptit/htpt/productservice/web/AdminStatsController.java`:

    ```java
    package com.ptit.htpt.productservice.web;

    import com.ptit.htpt.productservice.api.ApiResponse;
    import com.ptit.htpt.productservice.service.ProductStatsService;
    import java.util.Map;
    import org.springframework.http.HttpHeaders;
    import org.springframework.web.bind.annotation.GetMapping;
    import org.springframework.web.bind.annotation.RequestHeader;
    import org.springframework.web.bind.annotation.RequestMapping;
    import org.springframework.web.bind.annotation.RestController;

    /**
     * Phase 9 / Plan 09-02 (UI-02). Admin-only stats — D-05 REVISED: manual JWT
     * role check (KHÔNG @PreAuthorize). Path /admin/products/stats khớp gateway
     * route `/api/products/admin/stats`.
     */
    @RestController
    @RequestMapping("/admin/products")
    public class AdminStatsController {
      private final ProductStatsService statsService;
      private final JwtRoleGuard jwtRoleGuard;

      public AdminStatsController(ProductStatsService statsService, JwtRoleGuard jwtRoleGuard) {
        this.statsService = statsService;
        this.jwtRoleGuard = jwtRoleGuard;
      }

      @GetMapping("/stats")
      public ApiResponse<Map<String, Long>> stats(
          @RequestHeader(value = HttpHeaders.AUTHORIZATION, required = false) String authHeader
      ) {
        jwtRoleGuard.requireAdmin(authHeader);
        return ApiResponse.of(200, "Product stats", Map.of("totalProducts", statsService.totalProducts()));
      }
    }
    ```

    **Phần B — user-service (mirror cấu trúc, base path `/admin/users`):**

    4. Tạo `sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/web/JwtRoleGuard.java` — copy NGUYÊN VĂN file ở step 1, đổi package thành `com.ptit.htpt.userservice.web`.

    5. Tạo `sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/service/UserStatsService.java`:

    ```java
    package com.ptit.htpt.userservice.service;

    import com.ptit.htpt.userservice.repository.UserRepository;
    import org.springframework.stereotype.Service;
    import org.springframework.transaction.annotation.Transactional;

    @Service
    public class UserStatsService {
      private final UserRepository userRepo;

      public UserStatsService(UserRepository userRepo) {
        this.userRepo = userRepo;
      }

      @Transactional(readOnly = true)
      public long totalUsers() {
        return userRepo.count();
      }
    }
    ```

    6. Tạo `sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/web/AdminStatsController.java`:

    ```java
    package com.ptit.htpt.userservice.web;

    import com.ptit.htpt.userservice.api.ApiResponse;
    import com.ptit.htpt.userservice.service.UserStatsService;
    import java.util.Map;
    import org.springframework.http.HttpHeaders;
    import org.springframework.web.bind.annotation.GetMapping;
    import org.springframework.web.bind.annotation.RequestHeader;
    import org.springframework.web.bind.annotation.RequestMapping;
    import org.springframework.web.bind.annotation.RestController;

    @RestController
    @RequestMapping("/admin/users")
    public class AdminStatsController {
      private final UserStatsService statsService;
      private final JwtRoleGuard jwtRoleGuard;

      public AdminStatsController(UserStatsService statsService, JwtRoleGuard jwtRoleGuard) {
        this.statsService = statsService;
        this.jwtRoleGuard = jwtRoleGuard;
      }

      @GetMapping("/stats")
      public ApiResponse<Map<String, Long>> stats(
          @RequestHeader(value = HttpHeaders.AUTHORIZATION, required = false) String authHeader
      ) {
        jwtRoleGuard.requireAdmin(authHeader);
        return ApiResponse.of(200, "User stats", Map.of("totalUsers", statsService.totalUsers()));
      }
    }
    ```

    **NOTE Spring concerns:**
    - `AdminUserController` đã có `@RequestMapping("/admin/users")` — KHÔNG conflict vì method `/stats` chưa được map ở controller cũ. Spring map handler theo full URL pattern (base + method), KHÔNG yêu cầu unique base path.
    - Nếu Spring vẫn báo Ambiguous Mapping → fallback: thêm method `@GetMapping("/stats")` vào `AdminUserController` hiện tại + inject `UserStatsService` + giữ `JwtRoleGuard.requireAdmin(authHeader)` invoke đầu method. Document trong SUMMARY nếu fallback path được dùng.
  </action>
  <verify>
    <automated>
    # Build product-service
    cd sources/backend/product-service && mvn clean compile -q 2>&1 | tail -5

    # Build user-service
    cd sources/backend/user-service && mvn clean compile -q 2>&1 | tail -5

    # Files tồn tại
    test -f sources/backend/product-service/src/main/java/com/ptit/htpt/productservice/web/JwtRoleGuard.java
    test -f sources/backend/product-service/src/main/java/com/ptit/htpt/productservice/web/AdminStatsController.java
    test -f sources/backend/product-service/src/main/java/com/ptit/htpt/productservice/service/ProductStatsService.java
    test -f sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/web/JwtRoleGuard.java
    test -f sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/web/AdminStatsController.java
    test -f sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/service/UserStatsService.java

    # Pattern check
    grep -E "@RequestMapping\\(\"/admin/products\"\\)" sources/backend/product-service/src/main/java/com/ptit/htpt/productservice/web/AdminStatsController.java
    grep -E "@RequestMapping\\(\"/admin/users\"\\)" sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/web/AdminStatsController.java
    grep -E "totalProducts" sources/backend/product-service/src/main/java/com/ptit/htpt/productservice/web/AdminStatsController.java
    grep -E "totalUsers" sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/web/AdminStatsController.java
    grep -E "ADMIN role required" sources/backend/product-service/src/main/java/com/ptit/htpt/productservice/web/JwtRoleGuard.java
    </automated>
  </verify>
  <acceptance_criteria>
    - 6 file Java mới tồn tại + compile pass (`mvn clean compile` exit 0).
    - `JwtRoleGuard` chứa 3 path: 401 thiếu header, 401 token invalid, 403 không phải ADMIN.
    - `@RequestMapping("/admin/products")` + `@GetMapping("/stats")` (product-service).
    - `@RequestMapping("/admin/users")` + `@GetMapping("/stats")` (user-service) KHÔNG conflict với AdminUserController hiện tại.
    - Response payload key đúng: `totalProducts` (product), `totalUsers` (user).
  </acceptance_criteria>
  <done>
    Compile pass + tất cả grep PASS + base paths khớp gateway routes.
  </done>
</task>

<task type="auto">
  <name>Task 2: order-service stats endpoint (totalOrders + pendingOrders) + integration smoke test 3 services</name>
  <files>
    sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/web/JwtRoleGuard.java,
    sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/web/AdminStatsController.java,
    sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/service/OrderStatsService.java
  </files>
  <read_first>
    - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/web/AdminOrderController.java — verify base path `/admin/orders` + `OrderCrudService` pattern.
    - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/repository/ — verify `OrderRepository` extends JpaRepository<OrderEntity, String>.
    - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/domain/ — verify `OrderEntity` field `orderStatus` (String hoặc enum) + giá trị "PENDING".
    - JwtRoleGuard.java vừa tạo ở user-service (Task 1, file 4) — copy structure.
  </read_first>
  <action>
    **Phần A — Tạo 3 file:**

    1. `sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/web/JwtRoleGuard.java` — copy NGUYÊN VĂN từ Task 1 file 1, đổi package thành `com.ptit.htpt.orderservice.web`.

    2. `sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/service/OrderStatsService.java`:

    ```java
    package com.ptit.htpt.orderservice.service;

    import com.ptit.htpt.orderservice.repository.OrderRepository;
    import org.springframework.stereotype.Service;
    import org.springframework.transaction.annotation.Transactional;

    @Service
    public class OrderStatsService {
      private final OrderRepository orderRepo;

      public OrderStatsService(OrderRepository orderRepo) {
        this.orderRepo = orderRepo;
      }

      @Transactional(readOnly = true)
      public long totalOrders() {
        return orderRepo.count();
      }

      /**
       * D-06: pending = orderStatus = "PENDING" ONLY (không gộp SHIPPING/PAID).
       * Method derived query — Spring Data JPA tự generate.
       */
      @Transactional(readOnly = true)
      public long pendingOrders() {
        return orderRepo.countByOrderStatus("PENDING");
      }
    }
    ```

    3. `sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/web/AdminStatsController.java`:

    ```java
    package com.ptit.htpt.orderservice.web;

    import com.ptit.htpt.orderservice.api.ApiResponse;
    import com.ptit.htpt.orderservice.service.OrderStatsService;
    import java.util.Map;
    import org.springframework.http.HttpHeaders;
    import org.springframework.web.bind.annotation.GetMapping;
    import org.springframework.web.bind.annotation.RequestHeader;
    import org.springframework.web.bind.annotation.RequestMapping;
    import org.springframework.web.bind.annotation.RestController;

    @RestController
    @RequestMapping("/admin/orders")
    public class AdminStatsController {
      private final OrderStatsService statsService;
      private final JwtRoleGuard jwtRoleGuard;

      public AdminStatsController(OrderStatsService statsService, JwtRoleGuard jwtRoleGuard) {
        this.statsService = statsService;
        this.jwtRoleGuard = jwtRoleGuard;
      }

      /**
       * D-04: response shape `{totalOrders, pendingOrders}` (D-06 pending = PENDING ONLY).
       */
      @GetMapping("/stats")
      public ApiResponse<Map<String, Long>> stats(
          @RequestHeader(value = HttpHeaders.AUTHORIZATION, required = false) String authHeader
      ) {
        jwtRoleGuard.requireAdmin(authHeader);
        return ApiResponse.of(200, "Order stats", Map.of(
            "totalOrders", statsService.totalOrders(),
            "pendingOrders", statsService.pendingOrders()
        ));
      }
    }
    ```

    **Phần B — Bổ sung repo method nếu thiếu:**

    Mở `sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/repository/OrderRepository.java`. Nếu CHƯA có method `countByOrderStatus(String orderStatus)`, thêm dòng:

    ```java
    long countByOrderStatus(String orderStatus);
    ```

    (Spring Data JPA derive query tự động từ tên method — không cần `@Query`.)

    Nếu `OrderEntity.orderStatus` là enum thay vì String (vd `OrderStatus`), đổi signature thành `long countByOrderStatus(OrderStatus orderStatus);` và truyền `OrderStatus.PENDING` từ service. Verify bằng cách đọc `OrderEntity.java` — pick branch khớp.

    **Phần C — Integration smoke test (manual qua docker stack, KHÔNG viết @SpringBootTest mới):**

    Sau khi compile pass, dev sẽ chạy `docker compose up -d --build` rồi test bằng curl. Document curl commands trong SUMMARY:

    ```bash
    # Login admin lấy token
    ADMIN_TOKEN=$(curl -s -X POST http://localhost:8080/api/users/auth/login \
      -H "Content-Type: application/json" \
      -d '{"email":"admin@example.com","password":"<admin-password>"}' \
      | jq -r '.data.token')

    # Login user thường lấy token
    USER_TOKEN=$(curl -s -X POST http://localhost:8080/api/users/auth/login \
      -H "Content-Type: application/json" \
      -d '{"email":"user@example.com","password":"<user-password>"}' \
      | jq -r '.data.token')

    # Test 3 endpoints với admin token (kỳ vọng 200)
    curl -i -H "Authorization: Bearer $ADMIN_TOKEN" http://localhost:8080/api/products/admin/stats
    curl -i -H "Authorization: Bearer $ADMIN_TOKEN" http://localhost:8080/api/orders/admin/stats
    curl -i -H "Authorization: Bearer $ADMIN_TOKEN" http://localhost:8080/api/users/admin/stats

    # Test với user token (kỳ vọng 403)
    curl -i -H "Authorization: Bearer $USER_TOKEN" http://localhost:8080/api/products/admin/stats

    # Test KHÔNG header (kỳ vọng 401)
    curl -i http://localhost:8080/api/products/admin/stats
    ```
  </action>
  <verify>
    <automated>
    # Build order-service
    cd sources/backend/order-service && mvn clean compile -q 2>&1 | tail -5

    # Files
    test -f sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/web/JwtRoleGuard.java
    test -f sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/web/AdminStatsController.java
    test -f sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/service/OrderStatsService.java

    # Pattern checks
    grep -E "@RequestMapping\\(\"/admin/orders\"\\)" sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/web/AdminStatsController.java
    grep -E "totalOrders.*pendingOrders|pendingOrders.*totalOrders" sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/web/AdminStatsController.java
    grep -E "countByOrderStatus" sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/service/OrderStatsService.java
    grep -E "countByOrderStatus" sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/repository/OrderRepository.java

    # Build all 3 services together
    cd sources/backend && (cd product-service && mvn clean package -DskipTests -q) && (cd order-service && mvn clean package -DskipTests -q) && (cd user-service && mvn clean package -DskipTests -q)
    </automated>
  </verify>
  <acceptance_criteria>
    - 3 file Java mới + sửa OrderRepository thêm `countByOrderStatus`.
    - `mvn package -DskipTests` cho cả 3 service exit 0.
    - Manual curl 5 case (3 admin → 200, 1 user → 403, 1 no-header → 401) cho kết quả khớp khi stack up. (Dev chạy sau, document trong SUMMARY.)
  </acceptance_criteria>
  <done>
    Compile + package pass + grep checks PASS + 5 curl case documented trong SUMMARY (chạy ở Plan 04 hoặc verification stage).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Gateway → backend service | Bearer token forward qua Authorization header. Mỗi service tự verify JWT signature qua `JwtRoleGuard`. |
| Anonymous client → /api/{svc}/admin/stats | Endpoint phải reject 401/403 trước khi đụng DB. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-02-01 | Information Disclosure | Stats endpoint leak count tới non-admin | mitigate | `JwtRoleGuard.requireAdmin(authHeader)` invoke đầu method controller → throw 403 trước `statsService.*()` chạy. Test bằng curl với USER token. |
| T-09-02-02 | Spoofing | JWT signature forgery | mitigate | `Jwts.parser().verifyWith(getSigningKey())` HS256 verify. `app.jwt.secret` shared cross-service (env var). Token forge sẽ throw → 401. |
| T-09-02-03 | Tampering | Roles claim tampered (vd ai đó tự build payload với roles="ADMIN") | mitigate | Signature verify trước parse — payload tampered → signature mismatch → 401. |
| T-09-02-04 | Denial of Service | Endpoint enumeration / brute force token | accept | Scope demo. Rate limit defer (D1..D17 carry-over backend hardening). |
| T-09-02-05 | Information Disclosure | Token logged trong console khi exception | mitigate | `JwtRoleGuard` chỉ throw `ResponseStatusException` với generic message ("Invalid token") — KHÔNG log raw token. |
| T-09-02-06 | Repudiation | Admin truy cập stats không có audit trail | accept | Phase 9 scope không cover audit log. Defer. |
</threat_model>

<verification>
- 9 file Java mới + 1 file edit (`OrderRepository.java`) compile pass.
- 3 base paths khớp gateway routes (verified bằng grep).
- `mvn package -DskipTests` cho 3 service đều exit 0.
- Manual curl 5 case documented trong SUMMARY.
</verification>

<success_criteria>
- 3 endpoints `/api/{products,orders,users}/admin/stats` reachable qua gateway từ port 8080.
- Admin → 200 + payload đúng shape D-04. User → 403. No-header → 401.
- pendingOrders count chỉ đếm `orderStatus = "PENDING"` (D-06).
- KHÔNG mở Spring Security setup (D-05 REVISED).
</success_criteria>

<output>
Sau khi xong, tạo `.planning/phases/09-residual-closure-verification/09-02-SUMMARY.md`:
- 9 files created + 1 file edited (OrderRepository).
- Base paths confirm: `/admin/products`, `/admin/orders`, `/admin/users` + `/stats`.
- Curl smoke results (5 case): admin/products → 200 {totalProducts:N}, admin/orders → 200 {totalOrders:N, pendingOrders:M}, admin/users → 200 {totalUsers:K}, user → 403, no-header → 401.
- Note nếu Spring AmbiguousMapping force fallback (merge stats vào AdminUserController hiện tại).
</output>
