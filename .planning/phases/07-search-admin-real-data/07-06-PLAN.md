---
phase: 07-search-admin-real-data
plan: 06
type: execute
wave: 2
depends_on:
  - "07-03"
  - "07-04"
files_modified:
  - sources/frontend/src/app/admin/users/page.tsx
autonomous: true
requirements:
  - UI-04
must_haves:
  truths:
    - "Admin /admin/users hiển thị list users từ API (không phải stub)"
    - "Cột Họ tên hiển thị fullName nếu có, fallback username nếu fullName null/empty"
    - "Cột Điện thoại hiển thị phone nếu có, fallback '—'"
    - "Cột Vai trò hiển thị Badge ADMIN hoặc Khách hàng đúng theo roles string"
    - "Click ✏️ trên user row mở UserEditModal với form pre-filled (fullName, phone, roles)"
    - "Submit UserEditModal → PATCH /api/users/admin/{id} → toast success → list refresh"
    - "Click 🗑️ trên user row (chỉ CUSTOMER) → confirm modal → DELETE /api/users/admin/{id} → toast success"
    - "Loading skeleton rows khi đang fetch"
    - "RetrySection khi fetch fail"
  artifacts:
    - path: "sources/frontend/src/app/admin/users/page.tsx"
      provides: "Admin users page với real API + edit modal + delete confirm + column fallbacks"
      contains: "listAdminUsers\|patchAdminUser\|deleteAdminUser"
  key_links:
    - from: "admin/users/page.tsx useEffect load"
      to: "services/users.ts listAdminUsers"
      via: "import từ @/services/users"
      pattern: "listAdminUsers"
    - from: "UserEditModal handleSaveUser"
      to: "services/users.ts patchAdminUser"
      via: "await patchAdminUser(editTarget.id, editForm)"
      pattern: "patchAdminUser"
---

<objective>
Plan 07-06 wire admin/users page với real API và thêm UserEditModal (D-09, D-10).

Thực hiện:
1. Thay `_stubUsers` bằng real API call `listAdminUsers()`
2. Adapt table columns theo real UserDto shape (D-09): fullName fallback username, phone fallback "—", roles Badge với format `USER`/`ADMIN` (format thực tế trong DB, không phải `ROLE_*`)
3. Thêm UserEditModal inline: form với fullName, phone, roles select → PATCH endpoint
4. Delete confirm modal (giữ pattern hiện có, wire thật)

Lưu ý roles format: Backend lưu `USER`/`ADMIN` (không có `ROLE_` prefix). Badge mapping: `ADMIN` → variant "hot" label "Admin"; `USER` → variant "default" label "Khách hàng".

Purpose: Admin users page hoạt động với data thật; admin có thể edit user info.
Output: admin/users/page.tsx với real API, edit modal, delete confirm.
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-search-admin-real-data/07-CONTEXT.md
@.planning/phases/07-search-admin-real-data/07-UI-SPEC.md
@.planning/phases/07-search-admin-real-data/07-PATTERNS.md
@.planning/ROADMAP.md

<interfaces>
<!-- Từ services/users.ts (sau Plan 07-04): -->
<!-- listAdminUsers(params?) → Promise<PaginatedResponse<User>> -->
<!-- patchAdminUser(id, body: AdminUserPatchBody) → Promise<User> -->
<!-- deleteAdminUser(id) → Promise<void> -->
<!-- AdminUserPatchBody: { fullName?: string; phone?: string; roles?: string } -->

<!-- Từ types/index.ts (đã verified): -->
<!-- interface User { id: string; username: string; email: string; roles?: string; -->
<!--   fullName?: string; phone?: string; createdAt?: string; updatedAt?: string; } -->

<!-- Từ UserEntity + AuthService + V2 seed (đã đọc trực tiếp): -->
<!-- roles format trong DB: "USER" và "ADMIN" (không có ROLE_ prefix) -->
<!-- AuthService.register() gọi UserEntity.create(..., "USER") -->
<!-- V2 seed: roles='ADMIN' cho admin user, roles='USER' cho demo_user -->
<!-- UI-SPEC dùng ROLE_ADMIN/ROLE_CUSTOMER — nhưng thực tế cần match DB format: USER/ADMIN -->
<!-- Badge mapping: ADMIN → variant "hot" label "Admin"; USER → variant "default" label "Khách hàng" -->

<!-- Từ admin/products/page.module.css (shared): -->
<!-- .overlay, .modal, .modalHeader, .modalForm, .modalActions, .closeBtn -->
<!-- .confirmModal, .confirmIcon, .confirmTitle, .confirmDesc, .confirmActions -->
<!-- .tableWrapper, .actionBtn, .tdBold, .tdMuted -->
<!-- admin/users/page.tsx đã import CSS từ '../products/page.module.css' (pattern đã có) -->

<!-- Toast messages từ UI-SPEC: -->
<!-- edit success: "Thông tin tài khoản đã được cập nhật" -->
<!-- edit error: "Không thể cập nhật tài khoản. Vui lòng thử lại" -->
<!-- delete success: "Tài khoản đã được xóa" -->
<!-- delete error: "Không thể xóa tài khoản. Vui lòng thử lại" -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Admin Users page — wire real API + adapt columns + UserEditModal + delete confirm (D-09, D-10)</name>
  <files>sources/frontend/src/app/admin/users/page.tsx</files>
  <read_first>
    - sources/frontend/src/app/admin/users/page.tsx — đọc TOÀN BỘ: state, table structure, modal nếu có, CSS import
    - sources/frontend/src/app/admin/products/page.tsx — xem useCallback/useEffect pattern vừa implement ở Plan 05 để follow cùng pattern
    - sources/frontend/src/components/ui/Input/Input.tsx — xác nhận props
    - sources/frontend/src/services/users.ts — xác nhận đã có các functions từ Plan 07-04
  </read_first>
  <action>
Đọc file admin/users/page.tsx hiện tại. Refactor hoàn toàn theo spec:

**Imports:**
```typescript
'use client';
import React, { useCallback, useEffect, useState } from 'react';
import styles from '../products/page.module.css';
import Button from '@/components/ui/Button/Button';
import Input from '@/components/ui/Input/Input';
import Badge from '@/components/ui/Badge/Badge';
import RetrySection from '@/components/ui/RetrySection/RetrySection';
import { useToast } from '@/components/ui/Toast/Toast';
import { listAdminUsers, patchAdminUser, deleteAdminUser, type AdminUserPatchBody } from '@/services/users';
import type { User } from '@/types';
```

**State:**
```typescript
export default function AdminUsersPage() {
  const [users, setUsers] = useState<User[]>([]);
  const [loading, setLoading] = useState(false);
  const [failed, setFailed] = useState(false);
  const [editTarget, setEditTarget] = useState<User | null>(null);
  const [deleteTarget, setDeleteTarget] = useState<string | null>(null);
  const [saving, setSaving] = useState(false);
  const [editForm, setEditForm] = useState<AdminUserPatchBody>({ fullName: '', phone: '', roles: 'USER' });
  const { showToast } = useToast();
```

**Load function:**
```typescript
  const load = useCallback(async () => {
    setLoading(true);
    setFailed(false);
    try {
      const resp = await listAdminUsers();
      setUsers(resp?.content ?? []);
    } catch {
      setFailed(true);
    } finally {
      setLoading(false);
    }
  }, []);

  useEffect(() => { load(); }, [load]);
```

**Open edit modal:**
```typescript
  const openEditModal = (user: User) => {
    setEditForm({
      fullName: user.fullName ?? '',
      phone: user.phone ?? '',
      roles: user.roles ?? 'USER'
    });
    setEditTarget(user);
  };
```

**Save user handler:**
```typescript
  const handleSaveUser = async (e: React.FormEvent) => {
    e.preventDefault();
    if (!editTarget) return;
    setSaving(true);
    try {
      await patchAdminUser(editTarget.id, {
        fullName: editForm.fullName || undefined,  // empty string → undefined (không update)
        phone: editForm.phone || undefined,
        roles: editForm.roles || undefined,
      });
      showToast('Thông tin tài khoản đã được cập nhật', 'success');
      setEditTarget(null);
      await load();
    } catch {
      showToast('Không thể cập nhật tài khoản. Vui lòng thử lại', 'error');
    } finally {
      setSaving(false);
    }
  };
```

**Delete handler:**
```typescript
  const handleDelete = async () => {
    if (!deleteTarget) return;
    try {
      await deleteAdminUser(deleteTarget);
      showToast('Tài khoản đã được xóa', 'success');
      setDeleteTarget(null);
      await load();
    } catch {
      showToast('Không thể xóa tài khoản. Vui lòng thử lại', 'error');
    }
  };
```

**Table structure** (7 colums per UI-SPEC — nhưng bỏ cột index nếu hiện tại có):

Page header:
```typescript
  return (
    <div className={styles.page}>
      <div className={styles.header}>
        <h1 className={styles.title}>Quản lý tài khoản</h1>
        <span style={{ fontSize: 'var(--text-body-sm)', color: 'var(--on-surface-variant)' }}>
          {users.length} tài khoản
        </span>
      </div>
```

Table body (thay `_stubUsers`):
```typescript
      <div className={styles.tableWrapper}>
        <table style={{ width: '100%', borderCollapse: 'collapse' }}>
          <thead>
            <tr style={{ background: 'var(--surface-container-low)' }}>
              <th style={thStyle}>Họ tên</th>
              <th style={thStyle}>Email</th>
              <th style={thStyle}>Điện thoại</th>
              <th style={thStyle}>Vai trò</th>
              <th style={thStyle}>Ngày tạo</th>
              <th style={thStyle}>Thao tác</th>
            </tr>
          </thead>
          <tbody>
            {/* Loading */}
            {loading && [...Array(5)].map((_, i) => (
              <tr key={i}><td colSpan={6}>
                <div className="skeleton" style={{ height: 60, borderRadius: 'var(--radius-md)' }} />
              </td></tr>
            ))}
            {/* Error */}
            {!loading && failed && (
              <tr><td colSpan={6}><RetrySection onRetry={load} loading={loading} /></td></tr>
            )}
            {/* Empty */}
            {!loading && !failed && users.length === 0 && (
              <tr><td colSpan={6}>
                <div style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', gap: 'var(--space-3)', padding: 'var(--space-7) 0' }}>
                  <h3 style={{ fontSize: 'var(--text-title-lg)', color: 'var(--on-surface)' }}>Chưa có tài khoản nào</h3>
                  <p style={{ fontSize: 'var(--text-body-md)', color: 'var(--on-surface-variant)' }}>Khi có tài khoản đăng ký, chúng sẽ hiển thị tại đây</p>
                </div>
              </td></tr>
            )}
            {/* Data rows */}
            {!loading && !failed && users.map(u => (
              <tr key={u.id} style={{ borderBottom: '1px solid rgba(195,198,214,0.08)' }}>
                {/* Họ tên: fullName fallback username per D-09 */}
                <td className={styles.tdBold} style={{ padding: 'var(--space-3) var(--space-4)' }}>
                  {u.fullName && u.fullName.trim() ? u.fullName : u.username}
                </td>
                <td style={{ padding: 'var(--space-3) var(--space-4)' }}>{u.email}</td>
                {/* Điện thoại: phone fallback "—" per D-09 */}
                <td style={{ padding: 'var(--space-3) var(--space-4)' }}>
                  {u.phone && u.phone.trim() ? u.phone : '—'}
                </td>
                {/* Vai trò: ADMIN → hot badge, USER → default badge */}
                <td style={{ padding: 'var(--space-3) var(--space-4)' }}>
                  <Badge variant={u.roles === 'ADMIN' ? 'hot' : 'default'}>
                    {u.roles === 'ADMIN' ? 'Admin' : 'Khách hàng'}
                  </Badge>
                </td>
                <td className={styles.tdMuted} style={{ padding: 'var(--space-3) var(--space-4)' }}>
                  {u.createdAt ? new Date(u.createdAt).toLocaleDateString('vi-VN') : '—'}
                </td>
                <td style={{ padding: 'var(--space-3) var(--space-4)', display: 'flex', gap: 'var(--space-2)' }}>
                  <button className={styles.actionBtn} aria-label="Chỉnh sửa tài khoản"
                    onClick={() => openEditModal(u)}>✏️</button>
                  {/* Ẩn nút xóa cho ADMIN user */}
                  {u.roles !== 'ADMIN' && (
                    <button className={styles.actionBtn} aria-label="Xóa tài khoản"
                      onClick={() => setDeleteTarget(u.id)}>🗑️</button>
                  )}
                </td>
              </tr>
            ))}
          </tbody>
        </table>
      </div>
```

**UserEditModal** (inline, per D-10):
```typescript
      {/* Edit modal */}
      {editTarget && (
        <div className={styles.overlay} onClick={() => setEditTarget(null)}>
          <div className={styles.modal} onClick={e => e.stopPropagation()}>
            <div className={styles.modalHeader}>
              <h3>Chỉnh sửa tài khoản</h3>
              <button className={styles.closeBtn} onClick={() => setEditTarget(null)}>✕</button>
            </div>
            <form className={styles.modalForm} onSubmit={handleSaveUser}>
              <Input
                label="Họ và tên"
                placeholder="Nguyễn Văn A"
                value={editForm.fullName ?? ''}
                onChange={e => setEditForm(p => ({ ...p, fullName: e.target.value }))}
                fullWidth
              />
              <Input
                label="Số điện thoại"
                placeholder="0901 234 567"
                value={editForm.phone ?? ''}
                onChange={e => setEditForm(p => ({ ...p, phone: e.target.value }))}
                fullWidth
              />
              <div>
                <label style={{ fontSize: 'var(--text-body-md)', marginBottom: 'var(--space-2)', display: 'block' }}>
                  Vai trò
                </label>
                <select
                  value={editForm.roles ?? 'USER'}
                  onChange={e => setEditForm(p => ({ ...p, roles: e.target.value }))}
                  style={{ width: '100%', padding: 'var(--space-3)', borderRadius: 'var(--radius-lg)', border: '1.5px solid rgba(195,198,214,0.2)', fontSize: 'var(--text-body-md)', fontFamily: 'var(--font-family-body)', background: 'var(--surface-container-lowest)', cursor: 'pointer' }}
                >
                  <option value="USER">Khách hàng</option>
                  <option value="ADMIN">Quản trị viên</option>
                </select>
              </div>
              <div className={styles.modalActions}>
                <Button variant="secondary" type="button" onClick={() => setEditTarget(null)}>Hủy</Button>
                <Button type="submit" loading={saving}>Lưu thay đổi</Button>
              </div>
            </form>
          </div>
        </div>
      )}
```

**Delete confirm modal:**
```typescript
      {/* Delete confirm */}
      {deleteTarget && (
        <div className={styles.overlay} onClick={() => setDeleteTarget(null)}>
          <div className={styles.confirmModal} onClick={e => e.stopPropagation()}>
            <h3 className={styles.confirmTitle}>Xác nhận xóa tài khoản</h3>
            <p className={styles.confirmDesc}>Bạn có chắc chắn muốn xóa tài khoản này? Hành động này không thể hoàn tác.</p>
            <div className={styles.confirmActions}>
              <Button variant="secondary" onClick={() => setDeleteTarget(null)}>Hủy</Button>
              <Button variant="danger" onClick={handleDelete}>Xóa tài khoản</Button>
            </div>
          </div>
        </div>
      )}
    </div>
  );
}
```

**QUAN TRỌNG:** Xóa `_stubUsers` array và bất kỳ mock data nào. Xóa `getUserName()` helper dùng stub. Xóa các inline modal cũ nếu có (inline selectedUser modal, không phải edit modal mới).

`thStyle` constant có thể định nghĩa:
```typescript
const thStyle = {
  padding: 'var(--space-3) var(--space-4)',
  fontSize: 'var(--text-label-md)',
  fontWeight: 700,
  textTransform: 'uppercase' as const,
  letterSpacing: '0.02em',
  color: 'var(--on-surface-variant)',
  textAlign: 'left' as const
};
```
  </action>
  <verify>
    <automated>grep -n "_stubUsers\|getUserName" sources/frontend/src/app/admin/users/page.tsx</automated>
    Không có match nào.
    <automated>grep -n "listAdminUsers\|patchAdminUser\|deleteAdminUser" sources/frontend/src/app/admin/users/page.tsx | wc -l</automated>
    Kết quả: >= 3
    <automated>grep -n "editTarget\|editForm\|handleSaveUser" sources/frontend/src/app/admin/users/page.tsx | wc -l</automated>
    Kết quả: >= 3
    <automated>grep -n "fullName.*trim\|u.fullName" sources/frontend/src/app/admin/users/page.tsx | wc -l</automated>
    Kết quả: >= 1 (D-09 fallback pattern)
  </verify>
  <acceptance_criteria>
    - `grep "_stubUsers" sources/frontend/src/app/admin/users/page.tsx` — 0 matches
    - `grep "listAdminUsers" sources/frontend/src/app/admin/users/page.tsx` — >= 1 match
    - `grep "patchAdminUser" sources/frontend/src/app/admin/users/page.tsx` — >= 1 match
    - `grep "deleteAdminUser" sources/frontend/src/app/admin/users/page.tsx` — >= 1 match
    - `grep "u.fullName" sources/frontend/src/app/admin/users/page.tsx` — >= 1 match (D-09 column)
    - `grep "u.phone\|editForm.phone" sources/frontend/src/app/admin/users/page.tsx` — >= 1 match
    - `grep "editTarget" sources/frontend/src/app/admin/users/page.tsx` — >= 3 matches (state + modal condition + open handler)
    - `grep "Thông tin tài khoản đã được cập nhật" sources/frontend/src/app/admin/users/page.tsx` — 1 match
    - `grep "Tài khoản đã được xóa" sources/frontend/src/app/admin/users/page.tsx` — 1 match
    - `grep "Xác nhận xóa tài khoản" sources/frontend/src/app/admin/users/page.tsx` — 1 match
    - `grep "Chỉnh sửa tài khoản" sources/frontend/src/app/admin/users/page.tsx` — 1 match (modal title per UI-SPEC)
    - `grep "RetrySection" sources/frontend/src/app/admin/users/page.tsx` — >= 1 match
    - `grep "roles !== 'ADMIN'" sources/frontend/src/app/admin/users/page.tsx` — 1 match (ẩn delete cho admin)
  </acceptance_criteria>
  <done>admin/users/page.tsx wire real API; columns adapt theo D-09 (fullName fallback, phone fallback, roles Badge); UserEditModal D-10 với PATCH call; delete confirm modal; _stub* đã xóa</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin browser → PATCH /api/users/admin/{id} | Admin edit user; gateway forward với Bearer token |
| Admin browser → DELETE /api/users/admin/{id} | Admin delete user — soft-delete via @SQLDelete |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-06-01 | Elevation of Privilege | Admin có thể set roles="ADMIN" qua edit modal | accept | Demo scope: admin role management là intended feature. Production cần audit log + super-admin gate — defer v1.2. |
| T-07-06-02 | Tampering | Admin delete user — không có undo | mitigate | (1) Delete button ẩn cho ADMIN users (không thể tự xóa admin). (2) Confirm modal trước khi delete. (3) Backend dùng soft-delete (@SQLDelete → deleted=true, không xóa physical row). |
| T-07-06-03 | Information Disclosure | listAdminUsers trả fullName + phone (PII) | accept | Demo scope — không có sensitive PII thật. FE middleware protect /admin/* routes. Gateway auth defer D-14. |
</threat_model>

<verification>
1. `grep "_stubUsers\|getUserName" sources/frontend/src/app/admin/users/page.tsx` — 0 matches
2. `grep "listAdminUsers\|patchAdminUser\|deleteAdminUser" sources/frontend/src/app/admin/users/page.tsx | wc -l` >= 3
3. `grep "u.fullName" sources/frontend/src/app/admin/users/page.tsx` — 1 match
4. `grep "editTarget" sources/frontend/src/app/admin/users/page.tsx | wc -l` >= 3
5. `cd sources/frontend && npx tsc --noEmit 2>&1 | grep "admin/users" | head -5` — 0 errors
</verification>

<success_criteria>
- admin/users/page.tsx wire real API (listAdminUsers)
- Table columns: fullName fallback username, phone fallback "—", roles Badge (ADMIN/USER format)
- Delete button ẩn cho ADMIN users
- UserEditModal: fullName, phone, roles select (USER/ADMIN) → patchAdminUser PATCH call
- Delete confirm modal → deleteAdminUser DELETE call
- Toast messages per UI-SPEC
- _stubUsers đã xóa
</success_criteria>

<output>
Sau khi hoàn thành, tạo `.planning/phases/07-search-admin-real-data/07-06-SUMMARY.md` với:
- Files modified
- D-09 column mapping (roles format decision: USER/ADMIN từ DB)
- D-10 edit modal PATCH flow
- Roles format quyết định: dùng `USER`/`ADMIN` (không có ROLE_ prefix) cho badge + select values
- TypeScript compile status
- Smoke test kết quả (nếu đã test)
</output>
