---
phase: 07-search-admin-real-data
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - sources/backend/product-service/src/main/resources/db/migration/V3__add_product_extended_fields.sql
  - sources/backend/product-service/src/main/java/com/ptit/htpt/productservice/domain/ProductEntity.java
  - sources/backend/product-service/src/main/java/com/ptit/htpt/productservice/service/ProductCrudService.java
autonomous: true
requirements:
  - UI-02
must_haves:
  truths:
    - "Admin POST /admin/products với brand='Apple' → persist brand='Apple' trong DB"
    - "Admin POST /admin/products với thumbnailUrl='https://img.jpg' → persist thumbnail_url trong DB"
    - "Admin GET /admin/products trả product với brand, thumbnailUrl, shortDescription, originalPrice fields"
    - "ProductUpsertRequest accept các field mới nullable — không break POST với body thiếu brand/thumbnail"
  artifacts:
    - path: "sources/backend/product-service/src/main/resources/db/migration/V3__add_product_extended_fields.sql"
      provides: "4 nullable columns mới trong product_svc.products"
      contains: "ADD COLUMN IF NOT EXISTS brand"
    - path: "sources/backend/product-service/src/main/java/com/ptit/htpt/productservice/domain/ProductEntity.java"
      provides: "4 JPA fields mới với @Column mapping"
      contains: "private String brand"
    - path: "sources/backend/product-service/src/main/java/com/ptit/htpt/productservice/service/ProductCrudService.java"
      provides: "ProductUpsertRequest mở rộng với 4 fields nullable + create/update methods cập nhật"
      contains: "String brand"
  key_links:
    - from: "ProductCrudService.ProductUpsertRequest.brand"
      to: "ProductEntity.brand"
      via: "create() / update() method assignment"
      pattern: "entity.brand = request.brand()"
    - from: "Flyway V3 migration"
      to: "ProductEntity @Column brand"
      via: "JPA schema validation (ddl-auto: validate)"
      pattern: "ADD COLUMN IF NOT EXISTS brand"
---

<objective>
Plan 07-02 mở rộng product-service để persist 4 fields mới (brand, thumbnailUrl, shortDescription, originalPrice) nhằm đủ điều kiện cho admin form create/edit product (D-03).

Thực hiện theo đúng thứ tự:
1. Flyway V3 migration thêm 4 columns vào product_svc.products
2. ProductEntity thêm 4 JPA fields với @Column mapping
3. ProductCrudService.ProductUpsertRequest thêm 4 nullable fields + cập nhật create/update

Không có Flyway migration → ProductEntity có fields nhưng DB không có columns → JPA validate fail khi startup. Luôn phải đồng bộ cả 3 layers.

Purpose: Admin form trong Phase 7 có thể gửi brand/thumbnail và data được persist thật thay vì bị silent-drop.
Output: V3 migration SQL + ProductEntity fields + ProductUpsertRequest extended.
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-search-admin-real-data/07-CONTEXT.md
@.planning/phases/07-search-admin-real-data/07-PATTERNS.md
@.planning/ROADMAP.md

<interfaces>
<!-- Từ sources/backend/product-service/src/main/resources/application.yml -->
<!-- flyway.locations: classpath:db/migration (default) -->
<!-- Profile dev: classpath:db/migration,classpath:db/seed-dev -->
<!-- V1 tồn tại trong db/migration/, V2 tồn tại trong db/seed-dev/ -->
<!-- Khi profile dev, Flyway count V1 + V2 → V3 là version tiếp theo an toàn cho db/migration/ -->
<!-- Khi không có profile dev, chỉ scan db/migration/ → V1 + V3 (V2 bị skip = không OK) -->
<!-- CRITICAL: Flyway yêu cầu version continuous — KHÔNG được skip version -->
<!-- GIẢI PHÁP: Đặt file là V2__add_product_extended_fields.sql trong db/migration/ (không phải V3) -->
<!-- Vì db/migration/ chỉ có V1, next version hợp lệ trong folder này là V2 -->
<!-- V2 seed-dev chỉ chạy khi profile dev → không conflict với V2 migration vì folder khác nhau -->
<!-- Flyway track migration theo (version, checksum) từ mỗi location config — không trộn version giữa locations -->

<!-- Từ sources/backend/product-service/domain/ProductEntity.java (excerpt pattern): -->
<!-- @Column(nullable = false, length = 300) private String name; -->
<!-- @Column(name = "category_id", nullable = false, length = 36) private String categoryId; -->
<!-- @Column(name = "thumbnail_url", ...) — chưa có, sẽ thêm -->
<!-- Accessor pattern: public String name() { return name; } -->

<!-- Từ ProductCrudService.java — ProductUpsertRequest hiện tại (5 fields): -->
<!-- public record ProductUpsertRequest(@NotBlank String name, @NotBlank String slug, -->
<!--   @NotBlank String categoryId, @DecimalMin("0.0") BigDecimal price, @NotBlank String status) {} -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flyway migration — thêm 4 columns vào product_svc.products (D-03)</name>
  <files>sources/backend/product-service/src/main/resources/db/migration/V2__add_product_extended_fields.sql</files>
  <read_first>
    - sources/backend/product-service/src/main/resources/application.yml — xác nhận Flyway locations config
    - sources/backend/product-service/src/main/resources/db/migration/V1__init_schema.sql — xem schema hiện tại của product_svc.products
  </read_first>
  <action>
Xác nhận Flyway config: `locations: classpath:db/migration` (production). Profile dev thêm `db/seed-dev`. Folder `db/migration/` chỉ có V1 → version tiếp theo trong folder này là **V2** (không phải V3, vì V2 seed-dev nằm ở folder khác, Flyway không trộn version giữa locations khi scan riêng từng classpath entry).

**QUAN TRỌNG:** Kiểm tra xem Flyway có flag `out-of-order: true` không trong application.yml. Nếu không có (mặc định false), Flyway sẽ reject V3 nếu V2 chưa apply (khi chạy mà không có profile dev). Vì vậy dùng V2 cho db/migration/ là an toàn nhất.

Tạo file `sources/backend/product-service/src/main/resources/db/migration/V2__add_product_extended_fields.sql`:

```sql
-- Phase 7 / Plan 02 (D-03): Mở rộng product_svc.products với 4 fields nullable.
-- Dùng IF NOT EXISTS để idempotent khi chạy lại (test environments).
ALTER TABLE product_svc.products ADD COLUMN IF NOT EXISTS brand VARCHAR(200);
ALTER TABLE product_svc.products ADD COLUMN IF NOT EXISTS thumbnail_url VARCHAR(500);
ALTER TABLE product_svc.products ADD COLUMN IF NOT EXISTS short_description VARCHAR(500);
ALTER TABLE product_svc.products ADD COLUMN IF NOT EXISTS original_price NUMERIC(12,2);
```

Tất cả columns đều nullable (không có NOT NULL constraint) — đảm bảo backward compatible với existing seeded products.
  </action>
  <verify>
    <automated>cat sources/backend/product-service/src/main/resources/db/migration/V2__add_product_extended_fields.sql</automated>
    File tồn tại và có đủ 4 ALTER TABLE statements.
    <automated>grep -c "ADD COLUMN IF NOT EXISTS" sources/backend/product-service/src/main/resources/db/migration/V2__add_product_extended_fields.sql</automated>
    Kết quả: 4
  </verify>
  <acceptance_criteria>
    - File `V2__add_product_extended_fields.sql` tồn tại trong `db/migration/`
    - `grep "ADD COLUMN IF NOT EXISTS brand" V2__add_product_extended_fields.sql` trả về 1 match
    - `grep "ADD COLUMN IF NOT EXISTS thumbnail_url" V2__add_product_extended_fields.sql` trả về 1 match
    - `grep "ADD COLUMN IF NOT EXISTS short_description" V2__add_product_extended_fields.sql` trả về 1 match
    - `grep "ADD COLUMN IF NOT EXISTS original_price" V2__add_product_extended_fields.sql` trả về 1 match
    - Không có `NOT NULL` constraint trong bất kỳ ADD COLUMN nào (columns phải nullable)
  </acceptance_criteria>
  <done>Migration file tạo xong với 4 nullable columns; idempotent với IF NOT EXISTS</done>
</task>

<task type="auto">
  <name>Task 2: ProductEntity + ProductUpsertRequest — thêm 4 fields mới (D-03)</name>
  <files>
    sources/backend/product-service/src/main/java/com/ptit/htpt/productservice/domain/ProductEntity.java
    sources/backend/product-service/src/main/java/com/ptit/htpt/productservice/service/ProductCrudService.java
  </files>
  <read_first>
    - sources/backend/product-service/src/main/java/com/ptit/htpt/productservice/domain/ProductEntity.java — đọc toàn bộ: fields, constructor, create(), update(), accessors
    - sources/backend/product-service/src/main/java/com/ptit/htpt/productservice/service/ProductCrudService.java — đọc toàn bộ: ProductUpsertRequest record, createProduct(), updateProduct()
  </read_first>
  <action>
**File 1: ProductEntity.java**

Thêm 4 JPA fields vào class (sau field `status`, trước field `deleted` hoặc timestamps — đặt nhất quán với field order hiện tại):

```java
@Column(length = 200)
private String brand;

@Column(name = "thumbnail_url", length = 500)
private String thumbnailUrl;

@Column(name = "short_description", length = 500)
private String shortDescription;

@Column(name = "original_price", precision = 12, scale = 2)
private BigDecimal originalPrice;
```

Import `java.math.BigDecimal` nếu chưa có.

Thêm 4 accessor methods (theo pattern record-style `fieldName()`):
```java
public String brand() { return brand; }
public String thumbnailUrl() { return thumbnailUrl; }
public String shortDescription() { return shortDescription; }
public BigDecimal originalPrice() { return originalPrice; }
```

Cập nhật method `create()` (static factory) để nhận và set 4 fields mới. Tìm signature hiện tại của `create()` và thêm 4 params nullable cuối cùng:
```java
public static ProductEntity create(String name, String slug, String categoryId,
                                   BigDecimal price, String status,
                                   String brand, String thumbnailUrl,
                                   String shortDescription, BigDecimal originalPrice) {
  // ... existing logic
  entity.brand = brand;
  entity.thumbnailUrl = thumbnailUrl;
  entity.shortDescription = shortDescription;
  entity.originalPrice = originalPrice;
  return entity;
}
```

Cập nhật method `update()` tương tự — thêm 4 params và set chúng.

**File 2: ProductCrudService.java**

Cập nhật `ProductUpsertRequest` record — thêm 4 nullable fields SAU các fields hiện tại:
```java
public record ProductUpsertRequest(
    @NotBlank String name,
    @NotBlank String slug,
    @NotBlank String categoryId,
    @DecimalMin("0.0") BigDecimal price,
    @NotBlank String status,
    String brand,               // nullable — D-03
    String thumbnailUrl,        // nullable — D-03
    String shortDescription,    // nullable — D-03
    BigDecimal originalPrice    // nullable — D-03
) {}
```

Cập nhật `createProduct()` — truyền 4 fields mới vào `ProductEntity.create()`:
```java
ProductEntity.create(request.name(), request.slug(), request.categoryId(),
    request.price(), request.status(),
    request.brand(), request.thumbnailUrl(),
    request.shortDescription(), request.originalPrice())
```

Cập nhật `updateProduct()` tương tự.

Chạy `mvn compile -pl sources/backend/product-service -q` để verify không lỗi.
  </action>
  <verify>
    <automated>mvn compile -pl sources/backend/product-service -q 2>&1 | tail -5</automated>
    Không có compilation error.
    <automated>grep -n "private String brand\|private String thumbnailUrl\|private String shortDescription\|private BigDecimal originalPrice" sources/backend/product-service/src/main/java/com/ptit/htpt/productservice/domain/ProductEntity.java | wc -l</automated>
    Kết quả: 4
    <automated>grep -n "String brand\|String thumbnailUrl\|String shortDescription\|BigDecimal originalPrice" sources/backend/product-service/src/main/java/com/ptit/htpt/productservice/service/ProductCrudService.java | wc -l</automated>
    Kết quả: >= 4 (trong ProductUpsertRequest + method calls)
  </verify>
  <acceptance_criteria>
    - `mvn compile -pl sources/backend/product-service` thành công (exit code 0)
    - `grep "private String brand" ProductEntity.java` trả về 1 match
    - `grep "@Column(name = \"thumbnail_url\"" ProductEntity.java` trả về 1 match
    - `grep "@Column(name = \"short_description\"" ProductEntity.java` trả về 1 match
    - `grep "precision = 12, scale = 2" ProductEntity.java` trả về 1 match (cho originalPrice)
    - `grep "String brand" ProductCrudService.java` trả về >= 1 match (trong ProductUpsertRequest)
    - `grep "BigDecimal originalPrice" ProductCrudService.java` trả về >= 1 match
    - `grep "brand() { return brand; }" ProductEntity.java` trả về 1 match
  </acceptance_criteria>
  <done>ProductEntity có 4 fields mới với @Column mapping; ProductUpsertRequest có 4 nullable fields; create/update methods truyền 4 fields sang entity; product-service compile clean</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin form → AdminProductController | Admin gửi ProductUpsertRequest với fields mới; không có auth enforcement ở gateway |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02-01 | Tampering | ProductUpsertRequest brand/thumbnailUrl fields | mitigate | thumbnailUrl là URL string — không execute, chỉ store và render. Validation `@NotBlank` chỉ apply cho required fields; nullable fields không inject SQL (JPA parameterized). |
| T-07-02-02 | Tampering | Flyway V2 migration modify schema | accept | Flyway track checksum — nếu file bị modify sau apply, service crash on startup (known behavior, không silent). Migration chỉ ADD COLUMN (không DROP, không ALTER TYPE). |
| T-07-02-03 | Information Disclosure | originalPrice field exposed | accept | originalPrice là business data — không sensitive. Admin-only endpoint protected by FE middleware. |
</threat_model>

<verification>
1. `mvn compile -pl sources/backend/product-service` — exit code 0
2. `ls sources/backend/product-service/src/main/resources/db/migration/` — có V1 và V2
3. `grep "ADD COLUMN IF NOT EXISTS" sources/backend/product-service/src/main/resources/db/migration/V2__add_product_extended_fields.sql | wc -l` = 4
4. `grep "private String brand" sources/backend/product-service/src/main/java/com/ptit/htpt/productservice/domain/ProductEntity.java` — 1 match
</verification>

<success_criteria>
- Flyway V2 migration SQL tạo xong với 4 nullable columns (brand, thumbnail_url, short_description, original_price)
- ProductEntity có 4 JPA fields với @Column mapping và record-style accessors
- ProductUpsertRequest extended với 4 nullable fields
- createProduct() và updateProduct() truyền 4 fields mới vào entity
- product-service compile thành công
</success_criteria>

<output>
Sau khi hoàn thành, tạo `.planning/phases/07-search-admin-real-data/07-02-SUMMARY.md` với:
- Files created/modified
- Flyway version numbering decision (V2 trong db/migration/ — lý do)
- Compile status
- Bất kỳ deviation nào so với plan
</output>
