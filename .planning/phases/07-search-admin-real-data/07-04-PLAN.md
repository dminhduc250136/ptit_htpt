---
phase: 07-search-admin-real-data
plan: 04
type: execute
wave: 2
depends_on:
  - "07-01"
  - "07-02"
  - "07-03"
files_modified:
  - sources/frontend/src/services/products.ts
  - sources/frontend/src/services/orders.ts
  - sources/frontend/src/services/users.ts
  - sources/frontend/src/app/admin/layout.tsx
autonomous: true
requirements:
  - UI-01
  - UI-02
  - UI-03
  - UI-04
must_haves:
  truths:
    - "listAdminProducts() gọi GET /api/products/admin và trả PaginatedResponse<Product>"
    - "createProduct(body) gọi POST /api/products/admin"
    - "updateProduct(id, body) gọi PUT /api/products/admin/{id}"
    - "deleteProduct(id) gọi DELETE /api/products/admin/{id}"
    - "listAdminCategories() gọi GET /api/products/admin/categories"
    - "listAdminOrders() gọi GET /api/orders/admin"
    - "getAdminOrderById(id) gọi GET /api/orders/admin/{id}"
    - "updateOrderState(id, status) gọi PATCH /api/orders/admin/{id}/state"
    - "listAdminUsers() gọi GET /api/users/admin"
    - "patchAdminUser(id, body) gọi PATCH /api/users/admin/{id}"
    - "deleteAdminUser(id) gọi DELETE /api/users/admin/{id}"
    - "useToast() trong admin pages không throw context error (ToastProvider wraps layout)"
  artifacts:
    - path: "sources/frontend/src/services/products.ts"
      provides: "listAdminProducts, createProduct, updateProduct, deleteProduct, listAdminCategories exports"
      contains: "export function listAdminProducts"
    - path: "sources/frontend/src/services/orders.ts"
      provides: "listAdminOrders, getAdminOrderById, updateOrderState exports"
      contains: "export function listAdminOrders"
    - path: "sources/frontend/src/services/users.ts"
      provides: "New file — listAdminUsers, patchAdminUser, deleteAdminUser exports"
      contains: "export function listAdminUsers"
    - path: "sources/frontend/src/app/admin/layout.tsx"
      provides: "ToastProvider wrapping admin layout"
      contains: "ToastProvider"
  key_links:
    - from: "admin/products/page.tsx"
      to: "services/products.ts listAdminProducts"
      via: "import từ @/services/products"
      pattern: "listAdminProducts|createProduct|updateProduct|deleteProduct"
    - from: "admin/layout.tsx ToastProvider"
      to: "useToast() trong admin pages"
      via: "React Context"
      pattern: "ToastProvider"
---

<objective>
Plan 07-04 tạo FE service layer cho admin operations và wrap ToastProvider vào admin layout.

Các admin pages ở Wave 2 (plans 05 và 06) PHỤ THUỘC vào các service functions này. Plan này phải hoàn thành trước khi executor của plans 05/06 có thể import functions.

Thực hiện:
1. `services/products.ts` — thêm 5 admin functions (listAdminProducts, createProduct, updateProduct, deleteProduct, listAdminCategories) + interface ProductUpsertBody
2. `services/orders.ts` — thêm 3 admin functions (listAdminOrders, getAdminOrderById, updateOrderState) + interface ListOrdersParams
3. `services/users.ts` — tạo mới: listAdminUsers, patchAdminUser, deleteAdminUser + interfaces
4. `admin/layout.tsx` — wrap ToastProvider

Purpose: Tạo typed HTTP layer cho admin CRUD; enable useToast() trong tất cả admin pages.
Output: 3 service files updated/created + layout.tsx với ToastProvider.
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-search-admin-real-data/07-CONTEXT.md
@.planning/phases/07-search-admin-real-data/07-PATTERNS.md
@.planning/ROADMAP.md

<interfaces>
<!-- Từ sources/frontend/src/services/http.ts (đã verified trong RESEARCH.md): -->
<!-- export function httpGet<T>(url: string): Promise<T> -->
<!-- export function httpPost<T>(url: string, body: unknown): Promise<T> -->
<!-- export function httpPut<T>(url: string, body: unknown): Promise<T> -->
<!-- export function httpPatch<T>(url: string, body: unknown): Promise<T> -->
<!-- export function httpDelete<T>(url: string): Promise<T> -->
<!-- Auto-attach Bearer token từ localStorage, auto-401 redirect, envelope unwrap (.data field) -->

<!-- Từ sources/frontend/src/services/products.ts (existing): -->
<!-- import { httpGet } from './http'; -->
<!-- interface ListProductsParams { page?, size?, sort?, categoryId?, keyword? } -->
<!-- export function listProducts(params?: ListProductsParams): Promise<PaginatedResponse<Product>> -->

<!-- Từ sources/frontend/src/services/orders.ts (existing): -->
<!-- import { httpGet, httpPost } from './http' -->
<!-- export function listMyOrders(): Promise<Order[]> -->
<!-- export function getOrderById(id: string): Promise<Order> -->
<!-- export function createOrder(command): Promise<Order> -->

<!-- Từ sources/frontend/src/types/index.ts: -->
<!-- interface User { id: string; username: string; email: string; roles?: string; -->
<!--   fullName?: string; phone?: string; createdAt?: string; updatedAt?: string; } -->
<!-- interface Product { id: string; name: string; slug?: string; ... } -->
<!-- interface Order { id: string; userId: string; total: number; status: string; ... } -->
<!-- interface Category { id: string; name: string; ... } -->
<!-- interface PaginatedResponse<T> { content: T[]; totalElements: number; ... } -->

<!-- Từ sources/frontend/src/components/ui/Toast/Toast.tsx (verified): -->
<!-- export const ToastProvider = ...; -->
<!-- export const useToast = () => useContext(ToastContext); -->
<!-- useToast() returns { showToast: (message: string, type: 'success'|'error'|'info') => void } -->

<!-- Gateway path mapping (từ Plan 07-01 — phải đã apply): -->
<!-- GET /api/products/admin → /admin/products (list) -->
<!-- POST /api/products/admin → /admin/products (create) -->
<!-- PUT /api/products/admin/{id} → /admin/products/{id} -->
<!-- DELETE /api/products/admin/{id} → /admin/products/{id} -->
<!-- GET /api/products/admin/categories → /admin/products/categories -->
<!-- GET /api/orders/admin → /admin/orders -->
<!-- GET /api/orders/admin/{id} → /admin/orders/{id} -->
<!-- PATCH /api/orders/admin/{id}/state → /admin/orders/{id}/state -->
<!-- GET /api/users/admin → /admin/users -->
<!-- PATCH /api/users/admin/{id} → /admin/users/{id} -->
<!-- DELETE /api/users/admin/{id} → /admin/users/{id} -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: services/products.ts — thêm admin CRUD functions (D-06, D-07)</name>
  <files>sources/frontend/src/services/products.ts</files>
  <read_first>
    - sources/frontend/src/services/products.ts — đọc toàn bộ để nắm imports, interfaces, và functions hiện có
    - sources/frontend/src/services/http.ts — kiểm tra httpPut, httpDelete đã export chưa
    - sources/frontend/src/types/index.ts — xác nhận Product, Category, PaginatedResponse interface shapes
  </read_first>
  <action>
Đọc file products.ts hiện tại. Thêm vào cuối file (KHÔNG xóa bất kỳ code hiện có nào):

**1. Cập nhật import từ http.ts** — thêm httpPost, httpPut, httpDelete nếu chưa có:
```typescript
import { httpGet, httpPost, httpPut, httpDelete } from './http';
```
(Giữ import httpGet đã có, chỉ thêm phần thiếu)

**2. Thêm interface ProductUpsertBody:**
```typescript
// Admin product create/update body — D-03 extended fields
export interface ProductUpsertBody {
  name: string;
  slug?: string;          // optional — nếu không cung cấp, backend hoặc FE auto-gen từ name
  categoryId: string;
  price: number;
  status: string;         // "ACTIVE" | "INACTIVE" | "OUT_OF_STOCK"
  stock?: number;
  brand?: string;
  thumbnailUrl?: string;
  shortDescription?: string;
  originalPrice?: number;
}
```

**3. Thêm 5 admin functions:**
```typescript
// ============================================================
// Admin product functions — gateway: /api/products/admin → /admin/products
// ============================================================

export function listAdminProducts(params?: ListProductsParams): Promise<PaginatedResponse<Product>> {
  const qs = new URLSearchParams();
  if (params?.page !== undefined) qs.set('page', String(params.page));
  if (params?.size  !== undefined) qs.set('size',  String(params.size));
  if (params?.sort)                qs.set('sort',  params.sort);
  if (params?.keyword)             qs.set('keyword', params.keyword);
  const suffix = qs.toString() ? `?${qs}` : '';
  return httpGet<PaginatedResponse<Product>>(`/api/products/admin${suffix}`);
}

export function createProduct(body: ProductUpsertBody): Promise<Product> {
  return httpPost<Product>('/api/products/admin', body);
}

export function updateProduct(id: string, body: ProductUpsertBody): Promise<Product> {
  return httpPut<Product>(`/api/products/admin/${encodeURIComponent(id)}`, body);
}

export function deleteProduct(id: string): Promise<void> {
  return httpDelete<void>(`/api/products/admin/${encodeURIComponent(id)}`);
}

export function listAdminCategories(): Promise<PaginatedResponse<Category>> {
  return httpGet<PaginatedResponse<Category>>('/api/products/admin/categories');
}
```

Kiểm tra TypeScript compile sau khi sửa: `cd sources/frontend && npx tsc --noEmit 2>&1 | grep "services/products" | head -5`
  </action>
  <verify>
    <automated>grep -n "export function listAdminProducts\|export function createProduct\|export function updateProduct\|export function deleteProduct\|export function listAdminCategories" sources/frontend/src/services/products.ts | wc -l</automated>
    Kết quả: 5
    <automated>grep -n "ProductUpsertBody" sources/frontend/src/services/products.ts | wc -l</automated>
    Kết quả: >= 1 (interface definition)
  </verify>
  <acceptance_criteria>
    - `grep "export function listAdminProducts" products.ts` — 1 match
    - `grep "export function createProduct" products.ts` — 1 match
    - `grep "export function updateProduct" products.ts` — 1 match
    - `grep "export function deleteProduct" products.ts` — 1 match
    - `grep "export function listAdminCategories" products.ts` — 1 match
    - `grep "ProductUpsertBody" products.ts` — >= 1 match
    - `grep "/api/products/admin" products.ts` — >= 4 matches (các function gọi admin endpoint)
    - Các functions hiện có (listProducts, getProductById, listCategories) vẫn còn nguyên
  </acceptance_criteria>
  <done>5 admin product functions và ProductUpsertBody interface thêm vào products.ts; functions hiện có không bị thay đổi</done>
</task>

<task type="auto">
  <name>Task 2: services/orders.ts + services/users.ts (NEW) + admin/layout.tsx ToastProvider (D-08, D-09, D-10)</name>
  <files>
    sources/frontend/src/services/orders.ts
    sources/frontend/src/services/users.ts
    sources/frontend/src/app/admin/layout.tsx
  </files>
  <read_first>
    - sources/frontend/src/services/orders.ts — đọc toàn bộ: imports, existing functions
    - sources/frontend/src/app/admin/layout.tsx — đọc toàn bộ: imports, component structure
    - sources/frontend/src/components/ui/Toast/Toast.tsx — xác nhận ToastProvider export name
    - sources/frontend/src/types/index.ts — xác nhận Order, User, PaginatedResponse interfaces
  </read_first>
  <action>
**File 1: services/orders.ts**

Đọc file hiện tại. Thêm vào cuối file:

```typescript
// Admin order functions — gateway: /api/orders/admin → /admin/orders

export interface ListOrdersParams {
  page?: number;
  size?: number;
  sort?: string;
}

export function listAdminOrders(params?: ListOrdersParams): Promise<PaginatedResponse<Order>> {
  const qs = new URLSearchParams();
  if (params?.page !== undefined) qs.set('page', String(params.page));
  if (params?.size  !== undefined) qs.set('size',  String(params.size));
  if (params?.sort)                qs.set('sort',  params.sort);
  const suffix = qs.toString() ? `?${qs}` : '';
  return httpGet<PaginatedResponse<Order>>(`/api/orders/admin${suffix}`);
}

export function getAdminOrderById(id: string): Promise<Order> {
  return httpGet<Order>(`/api/orders/admin/${encodeURIComponent(id)}`);
}

export function updateOrderState(id: string, status: string): Promise<Order> {
  return httpPatch<Order>(`/api/orders/admin/${encodeURIComponent(id)}/state`, { status });
}
```

Đảm bảo `httpPatch` và `httpGet` đều được import từ './http'. Cập nhật import nếu cần:
```typescript
import { httpGet, httpPost, httpPatch } from './http';
```

Đảm bảo `PaginatedResponse` được import từ '@/types'. Kiểm tra imports hiện tại.

**File 2: services/users.ts (TẠO MỚI)**

Tạo file mới hoàn toàn:
```typescript
/**
 * Admin user service functions.
 * Gateway path: /api/users/admin → /admin/users
 * Bearer token auto-attached bởi http.ts.
 */
import type { User, PaginatedResponse } from '@/types';
import { httpGet, httpPatch, httpDelete } from './http';

export interface ListUsersParams {
  page?: number;
  size?: number;
  sort?: string;
}

export interface AdminUserPatchBody {
  fullName?: string;
  phone?: string;
  roles?: string;
}

export function listAdminUsers(params?: ListUsersParams): Promise<PaginatedResponse<User>> {
  const qs = new URLSearchParams();
  if (params?.page !== undefined) qs.set('page', String(params.page));
  if (params?.size  !== undefined) qs.set('size',  String(params.size));
  if (params?.sort)                qs.set('sort',  params.sort);
  const suffix = qs.toString() ? `?${qs}` : '';
  return httpGet<PaginatedResponse<User>>(`/api/users/admin${suffix}`);
}

export function patchAdminUser(id: string, body: AdminUserPatchBody): Promise<User> {
  return httpPatch<User>(`/api/users/admin/${encodeURIComponent(id)}`, body);
}

export function deleteAdminUser(id: string): Promise<void> {
  return httpDelete<void>(`/api/users/admin/${encodeURIComponent(id)}`);
}
```

**File 3: admin/layout.tsx**

Đọc file hiện tại để biết structure (sidebar + mainContent pattern). Thêm:
- Import: `import { ToastProvider } from '@/components/ui/Toast/Toast';`
- Wrap toàn bộ JSX return content trong `<ToastProvider>...</ToastProvider>`

Pattern:
```typescript
import { ToastProvider } from '@/components/ui/Toast/Toast';

export default function AdminLayout({ children }: { children: React.ReactNode }) {
  return (
    <ToastProvider>
      {/* giữ nguyên nội dung hiện tại của layout — chỉ wrap thêm ToastProvider */}
      <div className={styles.adminLayout}>
        ...sidebar...
        <main className={styles.mainContent}>{children}</main>
      </div>
    </ToastProvider>
  );
}
```

KHÔNG thay đổi bất kỳ gì khác trong layout (sidebar items, CSS classes, href, v.v.).

Kiểm tra TypeScript: `cd sources/frontend && npx tsc --noEmit 2>&1 | grep "services/\|layout" | head -10`
  </action>
  <verify>
    <automated>grep -n "export function listAdminOrders\|export function getAdminOrderById\|export function updateOrderState" sources/frontend/src/services/orders.ts | wc -l</automated>
    Kết quả: 3
    <automated>test -f sources/frontend/src/services/users.ts && echo "EXISTS" || echo "MISSING"</automated>
    Kết quả: EXISTS
    <automated>grep -n "export function listAdminUsers\|export function patchAdminUser\|export function deleteAdminUser" sources/frontend/src/services/users.ts | wc -l</automated>
    Kết quả: 3
    <automated>grep -n "ToastProvider" sources/frontend/src/app/admin/layout.tsx | wc -l</automated>
    Kết quả: >= 2 (import + usage)
  </verify>
  <acceptance_criteria>
    - `grep "export function listAdminOrders" orders.ts` — 1 match
    - `grep "export function getAdminOrderById" orders.ts` — 1 match
    - `grep "export function updateOrderState" orders.ts` — 1 match
    - `grep "/api/orders/admin" orders.ts` — >= 3 matches
    - File `sources/frontend/src/services/users.ts` tồn tại
    - `grep "export function listAdminUsers" users.ts` — 1 match
    - `grep "export function patchAdminUser" users.ts` — 1 match
    - `grep "export function deleteAdminUser" users.ts` — 1 match
    - `grep "AdminUserPatchBody" users.ts` — >= 1 match
    - `grep "/api/users/admin" users.ts` — >= 3 matches
    - `grep "ToastProvider" sources/frontend/src/app/admin/layout.tsx` — >= 2 matches (import + JSX)
    - Existing functions trong orders.ts (listMyOrders, createOrder) vẫn còn nguyên
  </acceptance_criteria>
  <done>orders.ts có 3 admin functions; users.ts được tạo mới với 3 admin functions; admin/layout.tsx wrap ToastProvider</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FE services → API Gateway | HTTP calls tự động attach Bearer token (http.ts). Token từ localStorage. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04-01 | Information Disclosure | listAdminUsers trả danh sách user | accept | http.ts auto-attach Bearer token; FE middleware Phase 6 protect /admin/* routes. Demo scope — không có sensitive PII. |
| T-07-04-02 | Tampering | deleteAdminUser — không có confirm trước khi gọi | mitigate | FE admin/users/page.tsx (Plan 06) sẽ implement confirm modal trước khi gọi deleteAdminUser. Service function chỉ gọi API. |
| T-07-04-03 | Spoofing | Bearer token trong localStorage | accept | Đây là pattern từ Phase 6 (D-11). Refresh token và secure storage defer v1.2. |
</threat_model>

<verification>
1. `grep "export function listAdminProducts" sources/frontend/src/services/products.ts` — 1 match
2. `grep "export function listAdminOrders" sources/frontend/src/services/orders.ts` — 1 match
3. `test -f sources/frontend/src/services/users.ts && echo OK` — OK
4. `grep "export function listAdminUsers" sources/frontend/src/services/users.ts` — 1 match
5. `grep "ToastProvider" sources/frontend/src/app/admin/layout.tsx | wc -l` >= 2
6. `cd sources/frontend && npx tsc --noEmit 2>&1 | grep -E "error TS" | wc -l` — 0 new errors
</verification>

<success_criteria>
- products.ts export 5 admin functions + ProductUpsertBody interface
- orders.ts export 3 admin functions + ListOrdersParams interface
- users.ts (new file) export 3 admin functions + ListUsersParams + AdminUserPatchBody interfaces
- admin/layout.tsx wrap ToastProvider — useToast() trong admin pages không throw context error
- Không có TypeScript compilation errors mới
</success_criteria>

<output>
Sau khi hoàn thành, tạo `.planning/phases/07-search-admin-real-data/07-04-SUMMARY.md` với:
- Files modified/created
- Gateway path mapping cho từng function (FE URL → backend endpoint)
- ToastProvider integration
- TypeScript compile status
</output>
