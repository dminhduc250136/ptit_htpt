---
phase: 07-search-admin-real-data
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - sources/backend/user-service/src/main/resources/db/migration/V2__add_fullname_phone.sql
  - sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/domain/UserEntity.java
  - sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/domain/UserDto.java
  - sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/domain/UserMapper.java
  - sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/web/AdminUserController.java
  - sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/service/UserCrudService.java
autonomous: true
requirements:
  - UI-04
must_haves:
  truths:
    - "UserDto trả trong GET /admin/users response có field fullName (nullable) và phone (nullable)"
    - "PATCH /admin/users/{id} với body {fullName: 'Nguyen Van A'} cập nhật fullName trong DB"
    - "PATCH /admin/users/{id} với body {} (empty) không thay đổi gì (partial update)"
    - "user-service startup với Flyway V2 migration apply thành công (không crash)"
    - "RegisterRequest KHÔNG có fullName/phone — register flow vẫn hoạt động"
  artifacts:
    - path: "sources/backend/user-service/src/main/resources/db/migration/V2__add_fullname_phone.sql"
      provides: "2 nullable columns full_name + phone vào user_svc.users"
      contains: "ADD COLUMN IF NOT EXISTS full_name"
    - path: "sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/domain/UserEntity.java"
      provides: "fullName + phone fields + setters"
      contains: "private String fullName"
    - path: "sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/domain/UserDto.java"
      provides: "UserDto record với fullName + phone"
      contains: "String fullName"
    - path: "sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/domain/UserMapper.java"
      provides: "toDto() map fullName + phone"
      contains: "e.fullName()"
    - path: "sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/web/AdminUserController.java"
      provides: "PATCH /admin/users/{id} endpoint"
      contains: "@PatchMapping(\"/{id}\")"
    - path: "sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/service/UserCrudService.java"
      provides: "AdminUserPatchRequest record + patchUser() method"
      contains: "AdminUserPatchRequest"
  key_links:
    - from: "AdminUserController.patchUser"
      to: "UserCrudService.patchUser"
      via: "method call"
      pattern: "userCrudService.patchUser"
    - from: "UserCrudService.patchUser → UserEntity.setFullName"
      to: "user_svc.users.full_name"
      via: "JPA userRepo.save(user)"
      pattern: "user.setFullName|userRepo.save"
    - from: "UserMapper.toDto"
      to: "UserDto.fullName"
      via: "e.fullName() accessor"
      pattern: "e.fullName()"
---

<objective>
Plan 07-03 thực hiện D-04 + D-05: thêm fullName/phone vào user-service và thêm PATCH endpoint cho admin user edit.

Chain thực hiện (phải theo thứ tự):
1. Flyway V2 migration — thêm full_name + phone columns vào user_svc.users
2. UserEntity — thêm 2 fields + setters + touch() + setRoles()
3. UserDto — thêm 2 fields trong record
4. UserMapper.toDto() — map 2 fields mới
5. UserCrudService — thêm AdminUserPatchRequest record + patchUser() method
6. AdminUserController — thêm @PatchMapping("/{id}")

Nếu thiếu bất kỳ bước nào trong chain (ví dụ: có UserEntity field nhưng không có Flyway migration) → service crash khi startup với JPA validate error.

Purpose: Admin users page có thể gọi PATCH để update fullName/phone/roles; UserDto trả đầy đủ để FE render.
Output: user-service compile clean với Flyway V2 migration + toàn bộ chain UserEntity→UserDto→PATCH endpoint.
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-search-admin-real-data/07-CONTEXT.md
@.planning/phases/07-search-admin-real-data/07-PATTERNS.md
@.planning/ROADMAP.md

<interfaces>
<!-- Từ sources/backend/user-service/domain/UserEntity.java (đọc trực tiếp): -->
<!-- Constructor: UserEntity(id, username, email, passwordHash, roles, deleted, createdAt, updatedAt) -->
<!-- Accessors: id(), username(), email(), passwordHash(), roles(), deleted(), createdAt(), updatedAt() -->
<!-- Methods: create(), update(), changePasswordHash(), softDelete() -->
<!-- CHƯA CÓ: fullName, phone fields; setFullName, setPhone, setRoles, touch methods -->

<!-- Từ sources/backend/user-service/domain/UserDto.java (đọc trực tiếp): -->
<!-- public record UserDto(String id, String username, String email, String roles, Instant createdAt, Instant updatedAt) -->
<!-- CHƯA CÓ: fullName, phone fields -->

<!-- Từ sources/backend/user-service/domain/UserMapper.java (đọc trực tiếp): -->
<!-- toDto: new UserDto(e.id(), e.username(), e.email(), e.roles(), e.createdAt(), e.updatedAt()) -->
<!-- CHƯA CÓ: e.fullName(), e.phone() trong toDto call -->

<!-- Từ sources/backend/user-service/web/AdminUserController.java (đọc trực tiếp): -->
<!-- Có: GET /admin/users, GET /admin/users/{id}, POST /admin/users, PUT /admin/users/{id}, DELETE /admin/users/{id} -->
<!-- CHƯA CÓ: PATCH /admin/users/{id} -->

<!-- Từ sources/backend/user-service/service/UserCrudService.java (đọc trực tiếp): -->
<!-- Có: listUsers, getUser, createUser, updateUser, deleteUser -->
<!-- loadUser(id) private method: orElseThrow(NOT_FOUND) -->
<!-- CHƯA CÓ: patchUser, AdminUserPatchRequest -->

<!-- roles format trong DB: "USER" và "ADMIN" (không có ROLE_ prefix) -->
<!-- AuthService.register() gọi UserEntity.create(..., "USER") -->
<!-- V2 seed tạo user với roles='ADMIN' -->
<!-- FE UI-SPEC dùng ROLE_ADMIN → xử lý bằng cách FE badge check cả 2 format, hoặc backend normalize -->
<!-- Plan 03 KHÔNG migrate roles format — đó là separate concern, defer hoặc FE handle -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flyway V2 migration + UserEntity + UserDto + UserMapper chain (D-04)</name>
  <files>
    sources/backend/user-service/src/main/resources/db/migration/V2__add_fullname_phone.sql
    sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/domain/UserEntity.java
    sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/domain/UserDto.java
    sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/domain/UserMapper.java
  </files>
  <read_first>
    - sources/backend/user-service/src/main/resources/application.yml — xác nhận Flyway locations
    - sources/backend/user-service/src/main/resources/db/migration/V1__init_schema.sql — xem schema hiện tại
    - sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/domain/UserEntity.java
    - sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/domain/UserDto.java
    - sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/domain/UserMapper.java
  </read_first>
  <action>
**1a. Tạo file Flyway V2 migration:**

Tương tự product-service, user-service `db/migration/` chỉ có V1; V2 seed ở `db/seed-dev/`. Dùng V2 cho migration schema change trong `db/migration/`.

Tạo `sources/backend/user-service/src/main/resources/db/migration/V2__add_fullname_phone.sql`:
```sql
-- Phase 7 / Plan 03 (D-04): Thêm fullName + phone vào user_svc.users.
ALTER TABLE user_svc.users ADD COLUMN IF NOT EXISTS full_name VARCHAR(120);
ALTER TABLE user_svc.users ADD COLUMN IF NOT EXISTS phone VARCHAR(20);
```

**1b. Cập nhật UserEntity.java:**

Đọc file hiện tại. Thêm 2 fields sau field `roles` và trước `deleted`:
```java
@Column(name = "full_name", length = 120)
private String fullName;

@Column(length = 20)
private String phone;
```

Thêm 4 methods sau `changePasswordHash()`:
```java
public void setFullName(String fullName) {
  this.fullName = fullName;
  this.updatedAt = Instant.now();
}

public void setPhone(String phone) {
  this.phone = phone;
  this.updatedAt = Instant.now();
}

public void setRoles(String roles) {
  this.roles = roles;
  this.updatedAt = Instant.now();
}

public void touch() {
  this.updatedAt = Instant.now();
}
```

Thêm 2 accessors sau `roles()`:
```java
public String fullName() { return fullName; }
public String phone() { return phone; }
```

Constructor `protected UserEntity(...)` — thêm `fullName` và `phone` params. Khi `create()` gọi constructor, truyền `null` cho cả hai (user mới không có fullName/phone):
```java
protected UserEntity(String id, String username, String email, String passwordHash,
                     String roles, String fullName, String phone,
                     boolean deleted, Instant createdAt, Instant updatedAt) {
  // existing assignments...
  this.fullName = fullName;
  this.phone = phone;
}
```

Cập nhật `create()` static factory — thêm `null, null` cho fullName + phone trong constructor call:
```java
return new UserEntity(UUID.randomUUID().toString(), username, email, passwordHash,
    roles == null || roles.isBlank() ? "USER" : roles, null, null, false, now, now);
```

**1c. Cập nhật UserDto.java:**

Thêm `String fullName` và `String phone` vào record — sau `roles`, trước `createdAt`:
```java
public record UserDto(
    String id,
    String username,
    String email,
    String roles,
    String fullName,      // nullable — D-04
    String phone,         // nullable — D-04
    Instant createdAt,
    Instant updatedAt
) {}
```

**1d. Cập nhật UserMapper.java:**

Cập nhật `toDto()` — thêm `e.fullName()` và `e.phone()` vào UserDto constructor call theo đúng thứ tự mới:
```java
public static UserDto toDto(UserEntity e) {
  return new UserDto(
      e.id(),
      e.username(),
      e.email(),
      e.roles(),
      e.fullName(),     // D-04
      e.phone(),        // D-04
      e.createdAt(),
      e.updatedAt()
  );
}
```

Sau khi sửa tất cả files, chạy `mvn compile -pl sources/backend/user-service -q` để verify.
  </action>
  <verify>
    <automated>mvn compile -pl sources/backend/user-service -q 2>&1 | tail -5</automated>
    Không có compilation error.
    <automated>grep -n "private String fullName\|private String phone" sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/domain/UserEntity.java</automated>
    Phải có 2 matches.
    <automated>grep -n "String fullName\|String phone" sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/domain/UserDto.java</automated>
    Phải có 2 matches.
    <automated>grep -n "e.fullName()\|e.phone()" sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/domain/UserMapper.java</automated>
    Phải có 2 matches.
  </verify>
  <acceptance_criteria>
    - File `V2__add_fullname_phone.sql` tồn tại trong `db/migration/`
    - `grep "ADD COLUMN IF NOT EXISTS full_name" V2__add_fullname_phone.sql` — 1 match
    - `grep "ADD COLUMN IF NOT EXISTS phone" V2__add_fullname_phone.sql` — 1 match
    - `grep "private String fullName" UserEntity.java` — 1 match
    - `grep "private String phone" UserEntity.java` — 1 match
    - `grep "public void setFullName" UserEntity.java` — 1 match
    - `grep "public void setPhone" UserEntity.java` — 1 match
    - `grep "public void setRoles" UserEntity.java` — 1 match
    - `grep "public void touch" UserEntity.java` — 1 match
    - `grep "String fullName" UserDto.java` — 1 match (trong record definition)
    - `grep "e.fullName()" UserMapper.java` — 1 match
    - `mvn compile -pl sources/backend/user-service` — exit code 0
  </acceptance_criteria>
  <done>D-04 chain hoàn chỉnh: migration SQL + UserEntity fields/setters + UserDto fields + UserMapper.toDto() cập nhật; user-service compile clean</done>
</task>

<task type="auto">
  <name>Task 2: PATCH /admin/users/{id} — AdminUserPatchRequest + AdminUserController (D-05)</name>
  <files>
    sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/service/UserCrudService.java
    sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/web/AdminUserController.java
  </files>
  <read_first>
    - sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/service/UserCrudService.java
    - sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/web/AdminUserController.java
  </read_first>
  <action>
**File 1: UserCrudService.java**

Thêm vào cuối class (trước closing brace của class, sau existing inner records/methods):

```java
// D-05: AdminUserPatchRequest — partial update (không expose passwordHash)
public record AdminUserPatchRequest(
    String fullName,   // nullable — update nếu not null
    String phone,      // nullable — update nếu not null
    String roles       // nullable — update nếu not null và not blank
) {}

public UserDto patchUser(String id, AdminUserPatchRequest request) {
  UserEntity user = loadUser(id);
  if (request.fullName() != null) user.setFullName(request.fullName());
  if (request.phone() != null) user.setPhone(request.phone());
  if (request.roles() != null && !request.roles().isBlank()) user.setRoles(request.roles());
  user.touch();
  return UserMapper.toDto(userRepo.save(user));
}
```

Phụ thuộc vào Task 1: UserEntity phải đã có `setFullName()`, `setPhone()`, `setRoles()`, `touch()` methods.

**File 2: AdminUserController.java**

Đọc file hiện tại để biết imports, class-level annotations, và các method hiện có.

Thêm import nếu chưa có:
```java
import org.springframework.web.bind.annotation.PatchMapping;
import com.ptit.htpt.userservice.service.UserCrudService.AdminUserPatchRequest;
```

Thêm method vào controller class (SAU method `deleteUser`, trước closing brace):
```java
// D-05: Partial update — dùng cho admin edit modal (không yêu cầu passwordHash)
@PatchMapping("/{id}")
public ApiResponse<Object> patchUser(
    @PathVariable String id,
    @RequestBody AdminUserPatchRequest request     // không dùng @Valid — tất cả fields nullable
) {
  return ApiResponse.of(200, "Admin user patched", userCrudService.patchUser(id, request));
}
```

Chạy `mvn compile -pl sources/backend/user-service -q` sau khi sửa cả 2 files.
  </action>
  <verify>
    <automated>mvn compile -pl sources/backend/user-service -q 2>&1 | tail -5</automated>
    Không có compilation error.
    <automated>grep -n "AdminUserPatchRequest" sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/service/UserCrudService.java</automated>
    Phải có >= 2 matches (record definition + method signature).
    <automated>grep -n "@PatchMapping.*/{id}" sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/web/AdminUserController.java</automated>
    Phải có 1 match.
    <automated>grep -n "patchUser" sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/web/AdminUserController.java</automated>
    Phải có 1 match.
  </verify>
  <acceptance_criteria>
    - `grep "public record AdminUserPatchRequest" UserCrudService.java` — 1 match
    - `grep "public UserDto patchUser" UserCrudService.java` — 1 match
    - `grep "request.fullName() != null" UserCrudService.java` — 1 match
    - `grep "request.phone() != null" UserCrudService.java` — 1 match
    - `grep "request.roles() != null" UserCrudService.java` — 1 match
    - `grep "@PatchMapping" AdminUserController.java` — 1 match
    - `grep "userCrudService.patchUser" AdminUserController.java` — 1 match
    - `mvn compile -pl sources/backend/user-service` — exit code 0
  </acceptance_criteria>
  <done>D-05 hoàn chỉnh: AdminUserPatchRequest record trong UserCrudService + patchUser() method + PATCH endpoint trong AdminUserController; user-service compile clean</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FE Admin → PATCH /admin/users/{id} | Admin gửi partial update; gateway forward mà không verify JWT (D-14 deferred) |
| PATCH request body → UserEntity fields | AdminUserPatchRequest chỉ expose fullName, phone, roles — không expose passwordHash |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03-01 | Tampering | PATCH /admin/users/{id} — mass assignment | mitigate | `AdminUserPatchRequest` chỉ có 3 fields (fullName, phone, roles) — không expose id, passwordHash, email, deleted, createdAt. Admin không thể escalate privilege thông qua PATCH body. |
| T-07-03-02 | Elevation of Privilege | Admin có thể set roles="ADMIN" cho bất kỳ user nào | accept | Demo scope: admin role management là feature, không phải threat. Production cần audit log — defer v1.2. |
| T-07-03-03 | Tampering | Flyway V2 migration thay đổi schema | accept | Migration chỉ ADD COLUMN nullable — không DROP, không MODIFY existing columns. Idempotent với IF NOT EXISTS. |
</threat_model>

<verification>
1. `mvn compile -pl sources/backend/user-service` — exit code 0
2. `ls sources/backend/user-service/src/main/resources/db/migration/` — có V1 và V2
3. `grep "ADD COLUMN IF NOT EXISTS full_name" sources/backend/user-service/src/main/resources/db/migration/V2__add_fullname_phone.sql` — 1 match
4. `grep "@PatchMapping" sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/web/AdminUserController.java` — 1 match
5. `grep "e.fullName()" sources/backend/user-service/src/main/java/com/ptit/htpt/userservice/domain/UserMapper.java` — 1 match
</verification>

<success_criteria>
- Flyway V2 migration SQL thêm full_name + phone columns vào user_svc.users
- UserEntity có fullName + phone fields + setFullName/setPhone/setRoles/touch methods
- UserDto record có fullName + phone fields
- UserMapper.toDto() map 2 fields mới
- UserCrudService có AdminUserPatchRequest record + patchUser() method
- AdminUserController có @PatchMapping("/{id}") endpoint
- user-service compile thành công
</success_criteria>

<output>
Sau khi hoàn thành, tạo `.planning/phases/07-search-admin-real-data/07-03-SUMMARY.md` với:
- Files modified/created
- D-04 chain: Flyway V2 → UserEntity → UserDto → UserMapper
- D-05: AdminUserPatchRequest + patchUser + @PatchMapping
- Flyway version numbering decision
- Compile status
</output>
