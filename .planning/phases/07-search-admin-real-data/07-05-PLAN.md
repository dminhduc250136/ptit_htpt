---
phase: 07-search-admin-real-data
plan: 05
type: execute
wave: 2
depends_on:
  - "07-01"
  - "07-02"
  - "07-04"
files_modified:
  - sources/frontend/src/app/admin/products/page.tsx
  - sources/frontend/src/app/admin/orders/page.tsx
  - sources/frontend/src/app/admin/orders/[id]/page.tsx
autonomous: true
requirements:
  - UI-02
  - UI-03
must_haves:
  truths:
    - "Admin /admin/products hiển thị list products từ API (không phải stub empty)"
    - "Admin click '+ Thêm sản phẩm' → modal mở với empty form → submit → POST /api/products/admin → toast success → list refresh"
    - "Admin click ✏️ trên product row → modal mở với pre-filled data → submit → PUT /api/products/admin/{id} → toast success"
    - "Admin click 🗑️ → confirm modal → confirm → DELETE /api/products/admin/{id} → toast success → list refresh"
    - "Category dropdown trong modal load từ /api/products/admin/categories"
    - "Admin /admin/orders hiển thị list orders từ API"
    - "Admin click 📋 trên order row → router.push('/admin/orders/{id}')"
    - "Admin /admin/orders/{id} hiển thị order detail: mã đơn, khách hàng (userId), ngày đặt, trạng thái"
    - "Admin chọn status mới trong dropdown → click 'Cập nhật trạng thái' → PATCH /api/orders/admin/{id}/state → toast success"
    - "Loading state hiển thị skeleton rows khi đang fetch"
    - "Error state hiển thị RetrySection khi fetch fail"
  artifacts:
    - path: "sources/frontend/src/app/admin/products/page.tsx"
      provides: "Admin products page với real API + modal add/edit + confirm delete"
      contains: "listAdminProducts\|createProduct\|updateProduct\|deleteProduct"
    - path: "sources/frontend/src/app/admin/orders/page.tsx"
      provides: "Admin orders page với real API + router.push navigate"
      contains: "listAdminOrders\|router.push"
    - path: "sources/frontend/src/app/admin/orders/[id]/page.tsx"
      provides: "Admin order detail page (new file)"
      contains: "getAdminOrderById\|updateOrderState"
  key_links:
    - from: "admin/products/page.tsx"
      to: "services/products.ts listAdminProducts"
      via: "import + useEffect/useCallback load"
      pattern: "listAdminProducts"
    - from: "admin/orders/page.tsx row click"
      to: "admin/orders/[id]/page.tsx"
      via: "router.push('/admin/orders/${o.id}')"
      pattern: "router.push.*admin/orders"
    - from: "admin/orders/[id]/page.tsx"
      to: "services/orders.ts getAdminOrderById + updateOrderState"
      via: "useCallback load + handleUpdateStatus"
      pattern: "getAdminOrderById|updateOrderState"
---

<objective>
Plan 07-05 wire 3 admin pages: products CRUD + orders list + orders detail (new file).

Admin products (D-06, D-07): Thay _stubProducts bằng real API calls; extend modal để hỗ trợ cả add mode và edit mode (editTarget state); category dropdown load từ API; CRUD actions → toast messages per UI-SPEC.

Admin orders (D-08): Thay _stubOrders bằng real API; remove inline modal; add router.push navigate sang detail page; tạo mới `admin/orders/[id]/page.tsx` với order detail + status update dropdown.

Purpose: Admin có thể manage products và view/update orders với real data.
Output: admin/products/page.tsx + admin/orders/page.tsx updated; admin/orders/[id]/page.tsx (new).
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-search-admin-real-data/07-CONTEXT.md
@.planning/phases/07-search-admin-real-data/07-UI-SPEC.md
@.planning/phases/07-search-admin-real-data/07-PATTERNS.md
@.planning/ROADMAP.md

<interfaces>
<!-- Từ services/products.ts (sau Plan 07-04): -->
<!-- listAdminProducts(params?) → Promise<PaginatedResponse<Product>> -->
<!-- createProduct(body: ProductUpsertBody) → Promise<Product> -->
<!-- updateProduct(id, body) → Promise<Product> -->
<!-- deleteProduct(id) → Promise<void> -->
<!-- listAdminCategories() → Promise<PaginatedResponse<Category>> -->
<!-- ProductUpsertBody: { name, slug?, categoryId, price, status, stock?, brand?, thumbnailUrl?, shortDescription?, originalPrice? } -->

<!-- Từ services/orders.ts (sau Plan 07-04): -->
<!-- listAdminOrders(params?) → Promise<PaginatedResponse<Order>> -->
<!-- getAdminOrderById(id) → Promise<Order> -->
<!-- updateOrderState(id, status) → Promise<Order> -->

<!-- Từ types/index.ts: -->
<!-- interface Product { id, name, slug?, categoryId?, price, status, stock?, brand?, thumbnailUrl?, shortDescription?, originalPrice?, createdAt?, updatedAt? } -->
<!-- interface Order { id, userId, total, totalAmount?, status, note?, createdAt, updatedAt } -->
<!-- interface Category { id, name, slug?, ... } -->

<!-- Từ admin/products/page.module.css (đã có): -->
<!-- .overlay, .modal, .modalHeader, .modalForm, .modalActions, .closeBtn -->
<!-- .confirmModal, .confirmIcon, .confirmTitle, .confirmDesc, .confirmActions -->
<!-- .tableWrapper, .actionBtn, .tdBold, .tdMuted, .skeleton -->
<!-- Page dùng import styles from './page.module.css' -->

<!-- Từ UI-SPEC: -->
<!-- Roles format thực tế trong DB: "USER" / "ADMIN" (không có ROLE_ prefix) -->
<!-- Order table "Khách hàng" column: hiển userId (OrderDto chỉ có userId, không có username) -->
<!-- OrderDto fields: id, userId, total, totalAmount (alias), status, note, createdAt, updatedAt -->
<!-- Order status options: PENDING | CONFIRMED | SHIPPING | DELIVERED | CANCELLED -->

<!-- Toast messages từ UI-SPEC: -->
<!-- create success: "Sản phẩm đã được thêm thành công" -->
<!-- create error: "Không thể thêm sản phẩm. Vui lòng thử lại" -->
<!-- update success: "Sản phẩm đã được cập nhật" -->
<!-- update error: "Không thể cập nhật sản phẩm. Vui lòng thử lại" -->
<!-- delete success: "Sản phẩm đã được xóa" -->
<!-- delete error: "Không thể xóa sản phẩm. Vui lòng thử lại" -->
<!-- order status update success: "Trạng thái đơn hàng đã được cập nhật" -->
<!-- order status update error: "Không thể cập nhật trạng thái. Vui lòng thử lại" -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Admin Products page — wire real API + add/edit modal + delete confirm (D-06, D-07)</name>
  <files>sources/frontend/src/app/admin/products/page.tsx</files>
  <read_first>
    - sources/frontend/src/app/admin/products/page.tsx — đọc TOÀN BỘ để nắm state, JSX structure hiện có
    - sources/frontend/src/app/admin/products/page.module.css — đọc class names có sẵn
    - sources/frontend/src/components/ui/Button/Button.tsx — xác nhận Button props (variant, loading, onClick)
    - sources/frontend/src/components/ui/Input/Input.tsx — xác nhận Input props (label, value, onChange, fullWidth)
    - sources/frontend/src/components/ui/RetrySection/RetrySection.tsx — xác nhận props
  </read_first>
  <action>
Đọc file admin/products/page.tsx hiện tại. Thực hiện refactor hoàn toàn theo spec:

**Imports cần thêm:**
```typescript
import { useCallback, useEffect, useState } from 'react';
import { useToast } from '@/components/ui/Toast/Toast';
import RetrySection from '@/components/ui/RetrySection/RetrySection';
import {
  listAdminProducts, createProduct, updateProduct, deleteProduct, listAdminCategories,
  type ProductUpsertBody
} from '@/services/products';
```

**State variables:**
```typescript
const [products, setProducts] = useState<Product[]>([]);
const [categories, setCategories] = useState<Category[]>([]);
const [loading, setLoading] = useState(false);
const [failed, setFailed] = useState(false);
const [showAddModal, setShowAddModal] = useState(false);
const [editTarget, setEditTarget] = useState<Product | null>(null);  // null = add mode
const [deleteTarget, setDeleteTarget] = useState<string | null>(null);
const [saving, setSaving] = useState(false);
const [loadingCategories, setLoadingCategories] = useState(false);
const [search, setSearch] = useState('');
const { showToast } = useToast();
```

**Form state** (dùng khi modal mở):
```typescript
const emptyForm: ProductUpsertBody = {
  name: '', slug: '', categoryId: '', price: 0, status: 'ACTIVE',
  stock: 0, brand: '', thumbnailUrl: '', shortDescription: '', originalPrice: undefined
};
const [formData, setFormData] = useState<ProductUpsertBody>(emptyForm);
```

**Load products** (useCallback + useEffect pattern từ search/page.tsx):
```typescript
const load = useCallback(async () => {
  setLoading(true);
  setFailed(false);
  try {
    const resp = await listAdminProducts();
    setProducts(resp?.content ?? []);
  } catch {
    setFailed(true);
  } finally {
    setLoading(false);
  }
}, []);

useEffect(() => { load(); }, [load]);
```

**Load categories** (gọi khi modal mở):
```typescript
const loadCategories = useCallback(async () => {
  setLoadingCategories(true);
  try {
    const resp = await listAdminCategories();
    setCategories(resp?.content ?? []);
  } catch {
    // silent fail — hiển thị option "Không thể tải danh mục"
  } finally {
    setLoadingCategories(false);
  }
}, []);
```

**Open modal handlers:**
```typescript
const openAddModal = () => {
  setFormData(emptyForm);
  setEditTarget(null);
  setShowAddModal(true);
  loadCategories();
};

const openEditModal = (product: Product) => {
  setFormData({
    name: product.name ?? '',
    slug: product.slug ?? '',
    categoryId: product.categoryId ?? '',
    price: product.price ?? 0,
    status: product.status ?? 'ACTIVE',
    stock: product.stock ?? 0,
    brand: product.brand ?? '',
    thumbnailUrl: product.thumbnailUrl ?? '',
    shortDescription: product.shortDescription ?? '',
    originalPrice: product.originalPrice ?? undefined,
  });
  setEditTarget(product);
  setShowAddModal(true);
  loadCategories();
};

const closeModal = () => {
  setShowAddModal(false);
  setEditTarget(null);
};
```

**Submit handler:**
```typescript
const handleSubmit = async (e: React.FormEvent) => {
  e.preventDefault();
  // FE validation per UI-SPEC
  if (!formData.name.trim()) { showToast('Vui lòng nhập tên sản phẩm', 'error'); return; }
  if (!formData.price || formData.price <= 0) { showToast('Vui lòng nhập giá bán', 'error'); return; }
  if (!formData.categoryId) { showToast('Vui lòng chọn danh mục', 'error'); return; }

  setSaving(true);
  try {
    // slug auto-gen từ name nếu không có
    const body: ProductUpsertBody = {
      ...formData,
      slug: formData.slug?.trim() || formData.name.toLowerCase().replace(/\s+/g, '-').replace(/[^a-z0-9-]/g, ''),
      brand: formData.brand || undefined,
      thumbnailUrl: formData.thumbnailUrl || undefined,
      shortDescription: formData.shortDescription || undefined,
      originalPrice: formData.originalPrice || undefined,
    };
    if (editTarget) {
      await updateProduct(editTarget.id, body);
      showToast('Sản phẩm đã được cập nhật', 'success');
    } else {
      await createProduct(body);
      showToast('Sản phẩm đã được thêm thành công', 'success');
    }
    closeModal();
    await load();
  } catch {
    showToast(editTarget ? 'Không thể cập nhật sản phẩm. Vui lòng thử lại'
                         : 'Không thể thêm sản phẩm. Vui lòng thử lại', 'error');
  } finally {
    setSaving(false);
  }
};
```

**Delete handler:**
```typescript
const handleDelete = async () => {
  if (!deleteTarget) return;
  try {
    await deleteProduct(deleteTarget);
    showToast('Sản phẩm đã được xóa', 'success');
    setDeleteTarget(null);
    await load();
  } catch {
    showToast('Không thể xóa sản phẩm. Vui lòng thử lại', 'error');
  }
};
```

**Table body rendering** (thay `_stubProducts` bằng `products`):
```typescript
// Loading state
{loading && [...Array(5)].map((_, i) => (
  <tr key={i}><td colSpan={6}>
    <div className="skeleton" style={{ height: 60, borderRadius: 'var(--radius-md)' }} />
  </td></tr>
))}
// Error state
{!loading && failed && (
  <tr><td colSpan={6}><RetrySection onRetry={load} loading={loading} /></td></tr>
)}
// Empty state
{!loading && !failed && products.length === 0 && (
  <tr><td colSpan={6}>
    <div style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', gap: 'var(--space-3)', padding: 'var(--space-7) 0' }}>
      <h3 style={{ fontSize: 'var(--text-title-lg)', color: 'var(--on-surface)' }}>Chưa có sản phẩm nào</h3>
      <p style={{ fontSize: 'var(--text-body-md)', color: 'var(--on-surface-variant)' }}>Thêm sản phẩm đầu tiên bằng nút "+ Thêm sản phẩm" ở trên</p>
    </div>
  </td></tr>
)}
// Data rows — filter by search state
{!loading && !failed && products
  .filter(p => !search || p.name.toLowerCase().includes(search.toLowerCase()))
  .map(p => (
    <tr key={p.id}>
      <td>
        {p.thumbnailUrl && <img src={p.thumbnailUrl} alt={p.name} style={{ width: 48, height: 48, borderRadius: 'var(--radius-md)', objectFit: 'cover', marginRight: 'var(--space-2)' }} />}
        <span>{p.name}</span>
        {p.brand && <span style={{ fontSize: 'var(--text-body-sm)', color: 'var(--outline)' }}><br/>{p.brand}</span>}
      </td>
      <td>{p.categoryId}</td>  {/* hoặc category name nếu có */}
      <td style={{ color: 'var(--primary)' }}>{p.price?.toLocaleString('vi-VN')}₫</td>
      <td style={{ color: (p.stock ?? 0) < 10 ? 'var(--error)' : undefined, fontWeight: (p.stock ?? 0) < 10 ? 700 : 400 }}>
        {p.stock ?? 0}
      </td>
      <td><Badge variant={p.status === 'ACTIVE' ? 'new' : p.status === 'OUT_OF_STOCK' ? 'out-of-stock' : 'default'}>
        {p.status === 'ACTIVE' ? 'Đang bán' : p.status === 'OUT_OF_STOCK' ? 'Hết hàng' : 'Ẩn'}
      </Badge></td>
      <td style={{ display: 'flex', gap: 'var(--space-2)' }}>
        <button className={styles.actionBtn} aria-label="Chỉnh sửa sản phẩm" onClick={() => openEditModal(p)}>✏️</button>
        <button className={styles.actionBtn} aria-label="Xóa sản phẩm" onClick={() => setDeleteTarget(p.id)}>🗑️</button>
      </td>
    </tr>
))}
```

**Modal form** (thay modal hiện có để support cả add + edit mode, 8 fields per UI-SPEC):
```typescript
{(showAddModal) && (
  <div className={styles.overlay} onClick={closeModal}>
    <div className={styles.modal} onClick={e => e.stopPropagation()} style={{ maxWidth: 560, maxHeight: '90vh', overflowY: 'auto' }}>
      <div className={styles.modalHeader}>
        <h3>{editTarget ? 'Chỉnh sửa sản phẩm' : 'Thêm sản phẩm mới'}</h3>
        <button className={styles.closeBtn} onClick={closeModal}>✕</button>
      </div>
      <form className={styles.modalForm} onSubmit={handleSubmit}>
        {/* Tên sản phẩm — full width */}
        <Input label="Tên sản phẩm" value={formData.name} onChange={e => setFormData(p => ({...p, name: e.target.value}))} fullWidth required />
        {/* Giá bán + Giá gốc — grid 2 cột */}
        <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: 'var(--space-3)' }}>
          <Input label="Giá bán" type="number" value={String(formData.price)} onChange={e => setFormData(p => ({...p, price: Number(e.target.value)}))} placeholder="0" />
          <Input label="Giá gốc" type="number" value={String(formData.originalPrice ?? '')} onChange={e => setFormData(p => ({...p, originalPrice: e.target.value ? Number(e.target.value) : undefined}))} placeholder="0" />
        </div>
        {/* Mô tả ngắn — full width */}
        <Input label="Mô tả ngắn" value={formData.shortDescription ?? ''} onChange={e => setFormData(p => ({...p, shortDescription: e.target.value}))} fullWidth />
        {/* Danh mục + Thương hiệu — grid 2 cột */}
        <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: 'var(--space-3)' }}>
          <div>
            <label style={{ fontSize: 'var(--text-body-md)', marginBottom: 'var(--space-2)', display: 'block' }}>Danh mục</label>
            <select
              value={formData.categoryId}
              onChange={e => setFormData(p => ({...p, categoryId: e.target.value}))}
              style={{ width: '100%', padding: 'var(--space-3)', borderRadius: 'var(--radius-lg)', border: '1.5px solid rgba(195,198,214,0.2)', fontSize: 'var(--text-body-md)', fontFamily: 'var(--font-family-body)', background: 'var(--surface-container-lowest)', cursor: 'pointer' }}
            >
              {loadingCategories
                ? <option disabled value="">Đang tải danh mục...</option>
                : categories.length === 0
                  ? <option disabled value="">Không thể tải danh mục — thử lại</option>
                  : <><option value="">-- Chọn danh mục --</option>{categories.map(c => <option key={c.id} value={c.id}>{c.name}</option>)}</>
              }
            </select>
          </div>
          <Input label="Thương hiệu" value={formData.brand ?? ''} onChange={e => setFormData(p => ({...p, brand: e.target.value}))} />
        </div>
        {/* Tồn kho + URL hình ảnh — grid 2 cột */}
        <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: 'var(--space-3)' }}>
          <Input label="Tồn kho" type="number" value={String(formData.stock ?? 0)} onChange={e => setFormData(p => ({...p, stock: Number(e.target.value)}))} placeholder="0" />
          <Input label="URL hình ảnh" value={formData.thumbnailUrl ?? ''} onChange={e => setFormData(p => ({...p, thumbnailUrl: e.target.value}))} placeholder="https://..." />
        </div>
        <div className={styles.modalActions}>
          <Button variant="secondary" type="button" onClick={closeModal}>Hủy</Button>
          <Button type="submit" loading={saving}>{editTarget ? 'Lưu thay đổi' : 'Thêm sản phẩm'}</Button>
        </div>
      </form>
    </div>
  </div>
)}
```

**Confirm delete modal** (giữ nguyên pattern hiện có, chỉ wire thật):
```typescript
{deleteTarget && (
  <div className={styles.overlay} onClick={() => setDeleteTarget(null)}>
    <div className={styles.confirmModal} onClick={e => e.stopPropagation()}>
      <h3 className={styles.confirmTitle}>Xác nhận xóa</h3>
      <p className={styles.confirmDesc}>Bạn có chắc chắn muốn xóa sản phẩm này? Hành động này không thể hoàn tác.</p>
      <div className={styles.confirmActions}>
        <Button variant="secondary" onClick={() => setDeleteTarget(null)}>Hủy</Button>
        <Button variant="danger" onClick={handleDelete}>Xóa sản phẩm</Button>
      </div>
    </div>
  </div>
)}
```

**Xóa toàn bộ `_stubProducts` array** và bất kỳ mock data nào. File phải không còn dùng `_stub*` biến.
  </action>
  <verify>
    <automated>grep -n "_stubProducts\|_stubOrders\|_stubUsers" sources/frontend/src/app/admin/products/page.tsx</automated>
    Không có match nào.
    <automated>grep -n "listAdminProducts\|createProduct\|updateProduct\|deleteProduct\|listAdminCategories" sources/frontend/src/app/admin/products/page.tsx | wc -l</automated>
    Kết quả: >= 5 matches.
    <automated>grep -n "editTarget\|openEditModal" sources/frontend/src/app/admin/products/page.tsx | wc -l</automated>
    Kết quả: >= 3 matches.
  </verify>
  <acceptance_criteria>
    - `grep "_stubProducts" sources/frontend/src/app/admin/products/page.tsx` — 0 matches
    - `grep "listAdminProducts" sources/frontend/src/app/admin/products/page.tsx` — >= 1 match
    - `grep "createProduct" sources/frontend/src/app/admin/products/page.tsx` — >= 1 match (import + usage)
    - `grep "updateProduct" sources/frontend/src/app/admin/products/page.tsx` — >= 1 match
    - `grep "deleteProduct" sources/frontend/src/app/admin/products/page.tsx` — >= 1 match
    - `grep "editTarget" sources/frontend/src/app/admin/products/page.tsx` — >= 2 matches (state + usage)
    - `grep "listAdminCategories" sources/frontend/src/app/admin/products/page.tsx` — >= 1 match
    - `grep "showToast" sources/frontend/src/app/admin/products/page.tsx` — >= 3 matches (create/update/delete)
    - `grep "Sản phẩm đã được thêm thành công" sources/frontend/src/app/admin/products/page.tsx` — 1 match
    - `grep "RetrySection" sources/frontend/src/app/admin/products/page.tsx` — >= 1 match
  </acceptance_criteria>
  <done>Admin products page wire real API; modal hỗ trợ add + edit mode; CRUD actions trigger toast; _stubProducts đã xóa</done>
</task>

<task type="auto">
  <name>Task 2: Admin Orders list (router.push) + Orders detail page (new) (D-08)</name>
  <files>
    sources/frontend/src/app/admin/orders/page.tsx
    sources/frontend/src/app/admin/orders/[id]/page.tsx
  </files>
  <read_first>
    - sources/frontend/src/app/admin/orders/page.tsx — đọc toàn bộ: state, modal hiện có, table structure
    - sources/frontend/src/app/admin/products/page.module.css — kiểm tra class names (.tableWrapper, .skeleton, .actionBtn, v.v.) — orders page import cùng CSS file này
    - sources/frontend/src/components/ui/Badge/Badge.tsx — xác nhận variant names
    - sources/frontend/src/services/orders.ts — xác nhận function signatures (sau Plan 07-04)
  </read_first>
  <action>
**File 1: admin/orders/page.tsx**

Đọc file hiện tại. Thực hiện:

1. Thêm imports:
```typescript
import { useCallback, useEffect } from 'react';
import { useRouter } from 'next/navigation';
import { useToast } from '@/components/ui/Toast/Toast';
import RetrySection from '@/components/ui/RetrySection/RetrySection';
import { listAdminOrders } from '@/services/orders';
```

2. Cập nhật state: Thay `_stubOrders` bằng `useState<Order[]>([])`. Thêm `loading`, `failed` states. Xóa state cho inline modal (`selectedOrder`, `showDetailModal`, v.v.) nếu có.

3. Thêm `useRouter` và load function:
```typescript
const router = useRouter();
const { showToast } = useToast();

const load = useCallback(async () => {
  setLoading(true);
  setFailed(false);
  try {
    const resp = await listAdminOrders();
    setOrders(resp?.content ?? []);
  } catch {
    setFailed(true);
  } finally {
    setLoading(false);
  }
}, []);

useEffect(() => { load(); }, [load]);
```

4. Table body: thay `_stubOrders.map` bằng logic loading/error/empty/data:
- Loading: skeleton rows (5 rows, colSpan=7, height=60)
- Error: RetrySection
- Empty: "Chưa có đơn hàng nào"
- Data rows — columns per UI-SPEC:
  - Mã đơn: `o.id.slice(0, 8)` (partial UUID) dùng `className={styles.tdBold}`
  - Khách hàng: `o.userId.slice(0, 8)` (OrderDto không có username — hiển partial userId)
  - Số sản phẩm: `"—"` (OrderDto chưa có items array trong Phase 7 — Phase 8 implement)
  - Tổng tiền: `(o.totalAmount ?? o.total)?.toLocaleString('vi-VN')}₫` với color `--primary`
  - Trạng thái: `<Badge variant={...}>` theo status (PENDING=default, CONFIRMED=new, SHIPPING=hot, DELIVERED=sale, CANCELLED=out-of-stock)
  - Ngày đặt: `new Date(o.createdAt).toLocaleDateString('vi-VN')` với className `.tdMuted`
  - Thao tác: button 📋 → `router.push(`/admin/orders/${o.id}`)`

5. REMOVE toàn bộ inline modal code nếu có. REMOVE `_stubOrders`, REMOVE `_stubUsers`.

**Nhãn status tiếng Việt:**
```typescript
const statusLabel: Record<string, string> = {
  PENDING: 'Chờ xác nhận', CONFIRMED: 'Đã xác nhận',
  SHIPPING: 'Đang giao', DELIVERED: 'Đã giao', CANCELLED: 'Đã hủy'
};
const statusVariant: Record<string, string> = {
  PENDING: 'default', CONFIRMED: 'new', SHIPPING: 'hot', DELIVERED: 'sale', CANCELLED: 'out-of-stock'
};
```

**File 2: admin/orders/[id]/page.tsx (TẠO MỚI)**

Tạo thư mục `sources/frontend/src/app/admin/orders/[id]/` nếu chưa có, rồi tạo file `page.tsx`:

```typescript
'use client';
import React, { useCallback, useEffect, useState } from 'react';
import { useParams, useRouter } from 'next/navigation';
import styles from '../../products/page.module.css';  // reuse admin CSS
import Button from '@/components/ui/Button/Button';
import Badge from '@/components/ui/Badge/Badge';
import RetrySection from '@/components/ui/RetrySection/RetrySection';
import { useToast } from '@/components/ui/Toast/Toast';
import { getAdminOrderById, updateOrderState } from '@/services/orders';
import type { Order } from '@/types';

const STATUS_OPTIONS = ['PENDING', 'CONFIRMED', 'SHIPPING', 'DELIVERED', 'CANCELLED'];
const STATUS_LABELS: Record<string, string> = {
  PENDING: 'Chờ xác nhận', CONFIRMED: 'Đã xác nhận',
  SHIPPING: 'Đang giao', DELIVERED: 'Đã giao', CANCELLED: 'Đã hủy'
};
const STATUS_VARIANTS: Record<string, 'default' | 'new' | 'hot' | 'sale' | 'out-of-stock'> = {
  PENDING: 'default', CONFIRMED: 'new', SHIPPING: 'hot', DELIVERED: 'sale', CANCELLED: 'out-of-stock'
};

export default function AdminOrderDetailPage() {
  const params = useParams<{ id: string }>();
  const id = Array.isArray(params.id) ? params.id[0] : params.id;
  const router = useRouter();
  const { showToast } = useToast();

  const [order, setOrder] = useState<Order | null>(null);
  const [loading, setLoading] = useState(true);
  const [failed, setFailed] = useState(false);
  const [newStatus, setNewStatus] = useState('');
  const [saving, setSaving] = useState(false);

  const load = useCallback(async () => {
    if (!id) return;
    setLoading(true);
    setFailed(false);
    try {
      const data = await getAdminOrderById(id);
      setOrder(data);
      setNewStatus(data.status);
    } catch {
      setFailed(true);
    } finally {
      setLoading(false);
    }
  }, [id]);

  useEffect(() => { load(); }, [load]);

  const handleUpdateStatus = async () => {
    if (!order || newStatus === order.status) return;
    setSaving(true);
    try {
      await updateOrderState(order.id, newStatus);
      showToast('Trạng thái đơn hàng đã được cập nhật', 'success');
      await load();
    } catch {
      showToast('Không thể cập nhật trạng thái. Vui lòng thử lại', 'error');
    } finally {
      setSaving(false);
    }
  };

  if (loading) return (
    <div style={{ display: 'flex', flexDirection: 'column', gap: 'var(--space-4)' }}>
      <div className="skeleton" style={{ height: 120, borderRadius: 'var(--radius-xl)' }} />
      <div className="skeleton" style={{ height: 120, borderRadius: 'var(--radius-xl)' }} />
      <div className="skeleton" style={{ height: 200, borderRadius: 'var(--radius-xl)' }} />
    </div>
  );

  if (failed) return <RetrySection onRetry={load} loading={loading} />;

  if (!order) return null;

  const cardStyle = {
    background: 'var(--surface-container-lowest)',
    borderRadius: 'var(--radius-xl)',
    padding: 'var(--space-5)'
  };
  const labelStyle = { color: 'var(--on-surface-variant)', fontSize: 'var(--text-body-md)' };

  return (
    <div style={{ display: 'flex', flexDirection: 'column', gap: 'var(--space-4)' }}>
      {/* Header */}
      <div>
        <button onClick={() => router.back()} style={{ color: 'var(--on-surface-variant)', fontSize: 'var(--text-body-md)', background: 'none', border: 'none', cursor: 'pointer', marginBottom: 'var(--space-2)' }}>
          ← Quay lại danh sách đơn hàng
        </button>
        <h1 style={{ fontSize: 'var(--text-headline-md)', fontWeight: 700 }}>
          Chi tiết đơn hàng {order.id.slice(0, 8)}
        </h1>
      </div>

      {/* Info grid — 2 cols */}
      <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: 'var(--space-4)' }}>
        {/* Left: Order info */}
        <div style={cardStyle}>
          <h3 style={{ fontSize: 'var(--text-title-lg)', fontWeight: 700, marginBottom: 'var(--space-3)' }}>Thông tin đơn hàng</h3>
          <p style={labelStyle}>Mã đơn: <strong>{order.id.slice(0, 8)}</strong></p>
          <p style={labelStyle}>Khách hàng: <strong>{order.userId.slice(0, 8)}</strong></p>
          <p style={labelStyle}>Ngày đặt: <strong>{new Date(order.createdAt).toLocaleDateString('vi-VN')}</strong></p>
          <p style={labelStyle}>Trạng thái: <Badge variant={STATUS_VARIANTS[order.status] ?? 'default'}>{STATUS_LABELS[order.status] ?? order.status}</Badge></p>
        </div>

        {/* Right: Shipping info */}
        <div style={cardStyle}>
          <h3 style={{ fontSize: 'var(--text-title-lg)', fontWeight: 700, marginBottom: 'var(--space-3)' }}>Thông tin giao hàng</h3>
          <p style={labelStyle}>Địa chỉ: <strong>—</strong></p>
          <p style={labelStyle}>Thanh toán: <strong>—</strong></p>
          {order.note && <p style={labelStyle}>Ghi chú: <strong>{order.note}</strong></p>}
        </div>
      </div>

      {/* Line items card */}
      <div style={cardStyle}>
        <h3 style={{ fontSize: 'var(--text-title-lg)', fontWeight: 700, marginBottom: 'var(--space-3)' }}>Sản phẩm</h3>
        <p style={{ color: 'var(--on-surface-variant)' }}>Chi tiết sản phẩm sẽ khả dụng sau khi Phase 8 hoàn thiện</p>
        <div style={{ borderTop: '1px solid rgba(195,198,214,0.15)', marginTop: 'var(--space-3)', paddingTop: 'var(--space-3)' }}>
          <span style={{ fontWeight: 700, fontSize: 'var(--text-title-sm)' }}>Tổng cộng: </span>
          <span style={{ color: 'var(--primary)', fontWeight: 700 }}>
            {(order.totalAmount ?? order.total)?.toLocaleString('vi-VN')}₫
          </span>
        </div>
      </div>

      {/* Status update card */}
      <div style={cardStyle}>
        <h3 style={{ fontSize: 'var(--text-title-lg)', fontWeight: 700, marginBottom: 'var(--space-3)' }}>Cập nhật trạng thái</h3>
        <select
          value={newStatus}
          onChange={e => setNewStatus(e.target.value)}
          style={{ width: '100%', padding: 'var(--space-3)', borderRadius: 'var(--radius-lg)', border: '1.5px solid rgba(195,198,214,0.2)', fontSize: 'var(--text-body-md)', fontFamily: 'var(--font-family-body)', background: 'var(--surface-container-lowest)', cursor: 'pointer', marginBottom: 'var(--space-3)' }}
        >
          {STATUS_OPTIONS.map(s => <option key={s} value={s}>{STATUS_LABELS[s]}</option>)}
        </select>
        <Button
          onClick={handleUpdateStatus}
          disabled={newStatus === order.status}
          loading={saving}
        >
          Cập nhật trạng thái
        </Button>
      </div>
    </div>
  );
}
```
  </action>
  <verify>
    <automated>grep -n "_stubOrders\|_stubUsers" sources/frontend/src/app/admin/orders/page.tsx</automated>
    Không có match nào.
    <automated>grep -n "router.push.*admin/orders" sources/frontend/src/app/admin/orders/page.tsx</automated>
    Phải có >= 1 match.
    <automated>test -f "sources/frontend/src/app/admin/orders/[id]/page.tsx" && echo "EXISTS" || echo "MISSING"</automated>
    Kết quả: EXISTS
    <automated>grep -n "getAdminOrderById\|updateOrderState" "sources/frontend/src/app/admin/orders/[id]/page.tsx" | wc -l</automated>
    Kết quả: >= 2
  </verify>
  <acceptance_criteria>
    - `grep "_stubOrders" sources/frontend/src/app/admin/orders/page.tsx` — 0 matches
    - `grep "listAdminOrders" sources/frontend/src/app/admin/orders/page.tsx` — >= 1 match
    - `grep "router.push" sources/frontend/src/app/admin/orders/page.tsx` — >= 1 match
    - File `sources/frontend/src/app/admin/orders/[id]/page.tsx` tồn tại
    - `grep "getAdminOrderById" sources/frontend/src/app/admin/orders/[id]/page.tsx` — >= 1 match
    - `grep "updateOrderState" sources/frontend/src/app/admin/orders/[id]/page.tsx` — >= 1 match
    - `grep "Trạng thái đơn hàng đã được cập nhật" sources/frontend/src/app/admin/orders/[id]/page.tsx` — 1 match
    - `grep "Quay lại danh sách đơn hàng" sources/frontend/src/app/admin/orders/[id]/page.tsx` — 1 match
    - `grep "useParams" sources/frontend/src/app/admin/orders/[id]/page.tsx` — 1 match
    - `grep "STATUS_OPTIONS" sources/frontend/src/app/admin/orders/[id]/page.tsx` — >= 1 match (5 status options)
  </acceptance_criteria>
  <done>admin/orders/page.tsx wire real API + router.push navigate; admin/orders/[id]/page.tsx tạo mới với order detail + status update; _stub* đã xóa</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin browser → Admin pages | Authenticated admin (FE middleware check) — pages render real data |
| Admin pages → Gateway admin routes | Bearer token auto-attached bởi http.ts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05-01 | Tampering | Product create/update — admin có thể set bất kỳ status nào | accept | Admin role là trusted. Status field accept ACTIVE/INACTIVE/OUT_OF_STOCK — không có business constraint trong Phase 7. |
| T-07-05-02 | Tampering | thumbnailUrl rendered as img src | mitigate | URL được store từ admin input (trusted actor). FE render `<img src={url}>` — không execute script. Không dùng dangerouslySetInnerHTML. |
| T-07-05-03 | Elevation of Privilege | Order status update — PATCH /admin/orders/{id}/state không có gateway auth | accept | FE middleware Phase 6 protect /admin/* routes. Demo scope — gateway auth defer D-14. |
</threat_model>

<verification>
1. `grep "_stubProducts\|_stubOrders" sources/frontend/src/app/admin/products/page.tsx sources/frontend/src/app/admin/orders/page.tsx` — 0 matches
2. `grep "listAdminProducts" sources/frontend/src/app/admin/products/page.tsx` — >= 1 match
3. `grep "router.push" sources/frontend/src/app/admin/orders/page.tsx` — 1 match
4. `test -f "sources/frontend/src/app/admin/orders/[id]/page.tsx"` — exists
5. `grep "getAdminOrderById" "sources/frontend/src/app/admin/orders/[id]/page.tsx"` — 1 match
6. `cd sources/frontend && npx tsc --noEmit 2>&1 | grep "admin/products\|admin/orders" | head -5` — 0 errors
</verification>

<success_criteria>
- admin/products/page.tsx: real API calls, add modal, edit modal (editTarget), delete confirm, toast messages, skeleton loading, RetrySection error, empty state
- admin/orders/page.tsx: real API calls, router.push navigate, skeleton loading, RetrySection, empty state
- admin/orders/[id]/page.tsx: created — order detail, status update dropdown (5 options), toast success/error, back navigation, loading skeleton, RetrySection
</success_criteria>

<output>
Sau khi hoàn thành, tạo `.planning/phases/07-search-admin-real-data/07-05-SUMMARY.md` với:
- Files modified/created
- D-06 edit mode implementation detail (editTarget state)
- D-07 category dropdown loading
- D-08 detail page navigation pattern
- TypeScript compile status
</output>
