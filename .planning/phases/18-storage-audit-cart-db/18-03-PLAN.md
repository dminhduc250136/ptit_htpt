---
phase: 18-storage-audit-cart-db
plan: 03
type: execute
wave: 3
depends_on: [18-02]
files_modified:
  - sources/frontend/src/services/cart.ts
  - sources/frontend/src/hooks/useCart.ts
autonomous: true
requirements:
  - STORE-02
tags:
  - frontend
  - react-query
  - cart
  - service
must_haves:
  truths:
    - "services/cart.ts wrapper route theo getAccessToken(): có token → API; không token → localStorage namespace _localCart()"
    - "Public API exports: readCart, writeCart, addToCart, removeFromCart, updateQuantity, clearCart, mergeGuestCartToServer giữ tương đồng signature cũ (caller không phải sửa import)"
    - "useCart hook subscribe React Query key ['cart'] với staleTime: Infinity + invalidateQueries on mutation success"
    - "Mutations useAddToCart, useUpdateCartItem, useRemoveCartItem, useClearCart parse 409 STOCK_SHORTAGE và toast error message vi"
    - "SSR-safe: typeof window guard ở mọi localStorage access"
  artifacts:
    - path: "sources/frontend/src/services/cart.ts"
      provides: "Cart service wrapper guest+user routing"
      contains: "function getAccessToken"
    - path: "sources/frontend/src/hooks/useCart.ts"
      provides: "React Query hooks: useCart, useAddToCart, useUpdateCartItem, useRemoveCartItem, useClearCart"
      contains: "useQuery"
  key_links:
    - from: "services/cart.ts addToCart"
      to: "POST /api/orders/cart/items hoặc localStorage _localCart()"
      via: "getAccessToken() branch"
      pattern: "getAccessToken\\(\\)"
    - from: "useCart hook"
      to: "['cart'] query key"
      via: "useQuery({queryKey: ['cart']})"
      pattern: "queryKey: \\['cart'\\]"
---

<objective>
Rewrite `services/cart.ts` thành dual-backend wrapper (guest = localStorage, user = API) và tạo React Query hooks `useCart` + mutations.

Pattern routing: kiểm `getAccessToken()` mỗi function call. Caller (cart page, header badge, product detail, checkout) không cần biết — KHÔNG breaking change cho call sites.

Purpose: FE service layer hoàn thiện trước khi consumers (Plan 04) refactor sang React Query. Plan 05 (AuthProvider merge) cũng cần `mergeGuestCartToServer()` export từ plan này.

Output: Service wrapper dual-backend + hooks ready cho cart page + checkout + header.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/18-storage-audit-cart-db/18-CONTEXT.md
@.planning/phases/18-storage-audit-cart-db/18-RESEARCH.md
@.planning/phases/18-storage-audit-cart-db/18-02-SUMMARY.md
@sources/frontend/src/services/cart.ts
@sources/frontend/src/services/http.ts
@sources/frontend/src/services/token.ts
@sources/frontend/src/services/errors.ts

<interfaces>
Existing http.ts wrappers (auto-attach Bearer token, ApiResponse unwrap, throw ApiError):
```typescript
httpGet<T>(path: string): Promise<T>
httpPost<T>(path: string, body?: unknown): Promise<T>
httpPatch<T>(path: string, body?: unknown): Promise<T>
httpDelete<T>(path: string): Promise<T>
```

Existing token.ts:
```typescript
getAccessToken(): string | null  // returns null if no token or SSR
```

Existing services/cart.ts (cũ, sẽ rewrite):
```typescript
export interface CartItem {
  productId: string;
  name: string;
  thumbnailUrl: string;
  price: number;
  quantity: number;
  stock: number;
}
export function readCart(): CartItem[]
export function writeCart(items: CartItem[]): void
export function addToCart(product: Pick<Product, 'id'|'name'|'thumbnailUrl'|'price'|'stock'>, qty?: number): void
export function removeFromCart(productId: string): void
export function updateQuantity(productId: string, qty: number): void
export function clearCart(): void
```

BE CartDto wire format (từ Plan 02):
```typescript
interface CartDto {
  id: string;
  userId: string;
  items: Array<{ id: string; productId: string; quantity: number }>;
}
```

ApiError code 'STOCK_SHORTAGE' với details.items: `[{ productId, productName, requested, available }]`.

React Query đã có sẵn (verify trong package.json: `@tanstack/react-query` version 5.x — Phase 7+ đã add).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite services/cart.ts thành dual-backend wrapper (guest localStorage + user API)</name>
  <read_first>
    - sources/frontend/src/services/cart.ts (hiện tại — guest logic phải preserve y nguyên trong _localCart namespace)
    - sources/frontend/src/services/http.ts (httpGet/Post/Patch/Delete signatures + ApiError throw)
    - sources/frontend/src/services/token.ts (getAccessToken signature)
    - sources/frontend/src/services/errors.ts (ApiError + isApiError shape)
    - .planning/phases/18-storage-audit-cart-db/18-CONTEXT.md §D-09..D-15 (wrapper routing + merge flow)
  </read_first>
  <files>
    sources/frontend/src/services/cart.ts
  </files>
  <action>
REWRITE toàn bộ `services/cart.ts`. Cấu trúc mới:

```typescript
/**
 * Phase 18 / STORE-02: Dual-backend cart service.
 *
 * Routing:
 * - Guest (no accessToken): localStorage['cart'] — preserve behavior cũ qua _localCart() namespace.
 * - User (logged-in): API calls → BE order-svc CartController → DB persist.
 *
 * Public API giữ signature tương đồng v1.2 cho backward-compat tại call sites.
 * Tuy nhiên, vì user-path là async, các mutation function trở thành async — caller PHẢI await.
 *
 * Merge flow (Plan 05 sẽ wire vào AuthProvider.login):
 *   AuthProvider.login → setTokens → mergeGuestCartToServer() → success: clearCart() localStorage
 *
 * SSR-safe: typeof window guard ở mọi localStorage access.
 */

import type { Product } from '@/types';
import { httpGet, httpPost, httpPatch, httpDelete } from './http';
import { getAccessToken } from './token';
import { isApiError } from './errors';

// ===== Types =====

export interface CartItem {
  productId: string;
  name: string;
  thumbnailUrl: string;
  price: number;
  quantity: number;
  stock: number;
}

interface ServerCartItem {
  id: string;
  productId: string;
  quantity: number;
}

interface ServerCartDto {
  id: string;
  userId: string;
  items: ServerCartItem[];
}

// ===== Internal: Local (guest) namespace =====

const CART_KEY = 'cart';

function _localRead(): CartItem[] {
  if (typeof window === 'undefined') return [];
  const raw = window.localStorage.getItem(CART_KEY);
  if (!raw) return [];
  try { return JSON.parse(raw) as CartItem[]; } catch { return []; }
}

function _localWrite(items: CartItem[]): void {
  if (typeof window === 'undefined') return;
  window.localStorage.setItem(CART_KEY, JSON.stringify(items));
  window.dispatchEvent(new CustomEvent('cart:change'));
}

function _localClear(): void {
  if (typeof window === 'undefined') return;
  window.localStorage.removeItem(CART_KEY);
  window.dispatchEvent(new CustomEvent('cart:change'));
}

function _localAdd(product: Pick<Product, 'id'|'name'|'thumbnailUrl'|'price'|'stock'>, qty: number): void {
  const items = _localRead();
  const existing = items.find(i => i.productId === product.id);
  const stockLimit = product.stock ?? 0;
  if (existing) {
    existing.quantity = Math.min(existing.quantity + qty, stockLimit);
    existing.stock = stockLimit;
  } else {
    items.push({
      productId: product.id,
      name: product.name,
      thumbnailUrl: product.thumbnailUrl,
      price: product.price,
      quantity: Math.min(qty, stockLimit),
      stock: stockLimit,
    });
  }
  _localWrite(items);
}

function _localRemove(productId: string): void {
  _localWrite(_localRead().filter(i => i.productId !== productId));
}

function _localUpdate(productId: string, qty: number): void {
  if (qty <= 0) { _localRemove(productId); return; }
  const items = _localRead().map(i => {
    if (i.productId !== productId) return i;
    const capped = i.stock > 0 ? Math.min(qty, i.stock) : qty;
    return { ...i, quantity: capped };
  });
  _localWrite(items);
}

// ===== Internal: Server (user) namespace =====

/**
 * Server cart trả về chỉ có productId + quantity.
 * Để FE render UI (name, price, image, stock), hydrate từ product-svc.
 * Trade-off: GET /cart sau đó GET /products/{id} cho mỗi item — N+1 calls.
 * MVP acceptable; future optimization: BE cart endpoint join với product-svc.
 */
async function hydrateServerCartItems(serverItems: ServerCartItem[]): Promise<CartItem[]> {
  if (serverItems.length === 0) return [];
  const hydrated = await Promise.all(serverItems.map(async (si) => {
    try {
      const p = await httpGet<Product>(`/api/products/${si.productId}`);
      return {
        productId: si.productId,
        name: p.name,
        thumbnailUrl: p.thumbnailUrl ?? '',
        price: p.price,
        quantity: si.quantity,
        stock: p.stock ?? 0,
      } satisfies CartItem;
    } catch {
      // Product bị soft-delete hoặc product-svc fail — placeholder render
      return {
        productId: si.productId,
        name: '(Sản phẩm không khả dụng)',
        thumbnailUrl: '',
        price: 0,
        quantity: si.quantity,
        stock: 0,
      } satisfies CartItem;
    }
  }));
  return hydrated;
}

async function _serverGet(): Promise<CartItem[]> {
  const dto = await httpGet<ServerCartDto>('/api/orders/cart');
  return hydrateServerCartItems(dto.items);
}

async function _serverAdd(productId: string, quantity: number): Promise<CartItem[]> {
  const dto = await httpPost<ServerCartDto>('/api/orders/cart/items', { productId, quantity });
  return hydrateServerCartItems(dto.items);
}

async function _serverSet(productId: string, quantity: number): Promise<CartItem[]> {
  const dto = await httpPatch<ServerCartDto>(`/api/orders/cart/items/${productId}`, { quantity });
  return hydrateServerCartItems(dto.items);
}

async function _serverRemove(productId: string): Promise<CartItem[]> {
  const dto = await httpDelete<ServerCartDto>(`/api/orders/cart/items/${productId}`);
  return hydrateServerCartItems(dto.items);
}

async function _serverClear(): Promise<CartItem[]> {
  const dto = await httpDelete<ServerCartDto>('/api/orders/cart');
  return hydrateServerCartItems(dto.items);
}

async function _serverMerge(items: Array<{ productId: string; quantity: number }>): Promise<ServerCartDto> {
  return httpPost<ServerCartDto>('/api/orders/cart/merge', { items });
}

// ===== Public API =====

function isLoggedIn(): boolean {
  return getAccessToken() !== null;
}

/**
 * SYNC read — guest path only (immediate localStorage).
 * Logged-in caller PHẢI dùng useCart() React Query hook để fetch async.
 * Giữ signature sync cho backward compat — return empty array khi user logged-in
 * (caller dùng hook fetch riêng).
 */
export function readCart(): CartItem[] {
  if (isLoggedIn()) return [];
  return _localRead();
}

export function writeCart(items: CartItem[]): void {
  if (isLoggedIn()) return; // server path không dùng writeCart full-replace
  _localWrite(items);
}

/** ASYNC fetch — cho cả guest và user, trả Promise<CartItem[]>. Hook layer wrap. */
export async function fetchCart(): Promise<CartItem[]> {
  if (isLoggedIn()) return _serverGet();
  return _localRead();
}

/** ASYNC add — return updated cart items. */
export async function addToCart(
  product: Pick<Product, 'id' | 'name' | 'thumbnailUrl' | 'price' | 'stock'>,
  qty: number = 1,
): Promise<CartItem[]> {
  if (isLoggedIn()) return _serverAdd(product.id, qty);
  _localAdd(product, qty);
  return _localRead();
}

export async function updateQuantity(productId: string, qty: number): Promise<CartItem[]> {
  if (isLoggedIn()) return _serverSet(productId, qty);
  _localUpdate(productId, qty);
  return _localRead();
}

export async function removeFromCart(productId: string): Promise<CartItem[]> {
  if (isLoggedIn()) return _serverRemove(productId);
  _localRemove(productId);
  return _localRead();
}

export async function clearCart(): Promise<CartItem[]> {
  if (isLoggedIn()) return _serverClear();
  _localClear();
  return [];
}

/** Used by AuthProvider.logout — luôn clear localStorage bất kể state (D-15). */
export function clearLocalCart(): void {
  _localClear();
}

/**
 * Merge guest cart (localStorage) vào server cart sau khi login.
 * Wired vào AuthProvider.login Plan 05.
 *
 * Flow (D-13, D-14):
 *  1. Đọc _localRead() — nếu empty: return early.
 *  2. POST /merge với items array.
 *  3. Success: _localClear() để tránh duplicate sync mutation tiếp theo.
 *  4. Fail: log error, KHÔNG throw, KHÔNG clear localStorage. Caller (AuthProvider) hiển thị toast warning.
 */
export async function mergeGuestCartToServer(): Promise<{ ok: boolean; error?: unknown }> {
  const guestItems = _localRead();
  if (guestItems.length === 0) return { ok: true };

  try {
    await _serverMerge(guestItems.map(i => ({ productId: i.productId, quantity: i.quantity })));
    _localClear();
    return { ok: true };
  } catch (err) {
    if (typeof console !== 'undefined') {
      console.error('[cart-merge] failed', isApiError(err) ? err.code : err);
    }
    return { ok: false, error: err };
  }
}
```

**Lưu ý critical:**
- `readCart()` SYNC trả `[]` khi user logged-in (KHÔNG break callers chưa migrate sang hook). Hook layer (Plan 03 Task 2) cung cấp data thật cho user path.
- `writeCart()` no-op cho user path — server-side là source of truth.
- Hydrate từ product-svc sau mỗi server cart fetch — N+1 trade-off acceptable (Pattern 4 from research).
- `clearLocalCart()` export riêng cho AuthProvider.logout (D-15) bất kể login state hiện tại.
- `cart:change` event KHÔNG fire cho user path — invalidateQueries trong React Query thay thế (D-12).
  </action>
  <verify>
    <automated>cd sources/frontend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "function isLoggedIn" cart.ts` returns 1.
    - `grep -c "getAccessToken" cart.ts` returns 1.
    - `grep -c "function _localRead\|function _localWrite\|function _localAdd\|function _localUpdate\|function _localRemove\|function _localClear" cart.ts` returns 6.
    - `grep -c "async function _serverGet\|async function _serverAdd\|async function _serverSet\|async function _serverRemove\|async function _serverClear\|async function _serverMerge" cart.ts` returns 6.
    - `grep -c "export async function fetchCart\|export async function addToCart\|export async function updateQuantity\|export async function removeFromCart\|export async function clearCart\|export async function mergeGuestCartToServer" cart.ts` returns 6.
    - `grep -c "export function readCart\|export function writeCart\|export function clearLocalCart" cart.ts` returns 3.
    - `grep -c "/api/orders/cart" cart.ts` >= 5 (GET, POST items, PATCH items, DELETE items, DELETE cart, POST merge).
    - `grep -c "typeof window === 'undefined'" cart.ts` >= 3 (SSR guards).
    - `npx tsc --noEmit` exits 0 (no TS errors).
  </acceptance_criteria>
  <done>cart.ts rewrite hoàn thành. Dual-backend routing OK. TypeScript compile clean. Public API backward-compat (signature compatible cho mọi caller chưa migrate).</done>
</task>

<task type="auto">
  <name>Task 2: Tạo useCart React Query hooks (useCart, useAddToCart, useUpdateCartItem, useRemoveCartItem, useClearCart)</name>
  <read_first>
    - sources/frontend/src/services/cart.ts (Plan 03 Task 1 output — exports fetchCart, addToCart, updateQuantity, removeFromCart, clearCart)
    - sources/frontend/src/providers/AuthProvider.tsx (useAuth hook signature — `{ isAuthenticated }`)
    - .planning/phases/18-storage-audit-cart-db/18-RESEARCH.md §Pattern 4 (React Query write-through pattern)
    - sources/frontend/package.json (verify @tanstack/react-query version)
    - Một file existing dùng React Query trong codebase (e.g., grep `useQuery` in src/) để xác nhận import path + version idioms
  </read_first>
  <files>
    sources/frontend/src/hooks/useCart.ts
  </files>
  <action>
Tạo file mới `sources/frontend/src/hooks/useCart.ts`:

```typescript
/**
 * Phase 18 / STORE-02: React Query hooks cho cart state.
 *
 * Pattern (D-12, Pattern 4 research):
 * - useCart: useQuery(['cart']) — staleTime: Infinity, manual invalidate.
 *   enabled CHO CẢ guest và user (cả 2 đều cần fetch — guest từ localStorage, user từ API).
 *   fetchCart() trong cart.ts đã handle routing.
 * - useAddToCart / useUpdateCartItem / useRemoveCartItem / useClearCart: useMutation
 *   với onSuccess → invalidateQueries(['cart']).
 * - Error 409 STOCK_SHORTAGE → toast vi: "Số lượng vượt quá tồn kho".
 *
 * SSR-safe: hooks chỉ chạy client-side ('use client' parent component).
 */

'use client';

import { useQuery, useMutation, useQueryClient } from '@tanstack/react-query';
import {
  fetchCart,
  addToCart as cartServiceAdd,
  updateQuantity as cartServiceUpdate,
  removeFromCart as cartServiceRemove,
  clearCart as cartServiceClear,
  type CartItem,
} from '@/services/cart';
import type { Product } from '@/types';
import { isApiError } from '@/services/errors';

const CART_QUERY_KEY = ['cart'] as const;

interface CartErrorContext {
  code: string;
  message: string;
  shortageItems?: Array<{ productId: string; productName: string; requested: number; available: number }>;
}

function parseCartError(err: unknown): CartErrorContext {
  if (isApiError(err)) {
    if (err.code === 'STOCK_SHORTAGE') {
      const details = err.details as { items?: CartErrorContext['shortageItems'] } | undefined;
      return {
        code: 'STOCK_SHORTAGE',
        message: 'Số lượng vượt quá tồn kho',
        shortageItems: details?.items,
      };
    }
    if (err.code === 'UNAUTHORIZED') {
      return { code: 'UNAUTHORIZED', message: 'Phiên đăng nhập hết hạn' };
    }
    return { code: err.code, message: err.message || 'Không thực hiện được thao tác giỏ hàng' };
  }
  return { code: 'INTERNAL_ERROR', message: 'Lỗi mạng — vui lòng thử lại' };
}

/** Subscribe cart state. Cả guest và user đều dùng — service layer route theo getAccessToken(). */
export function useCart() {
  return useQuery<CartItem[]>({
    queryKey: CART_QUERY_KEY,
    queryFn: fetchCart,
    staleTime: Infinity, // chỉ refetch khi explicit invalidate
    refetchOnWindowFocus: false,
  });
}

export function useAddToCart() {
  const qc = useQueryClient();
  return useMutation({
    mutationFn: ({ product, qty }: {
      product: Pick<Product, 'id' | 'name' | 'thumbnailUrl' | 'price' | 'stock'>;
      qty?: number;
    }) => cartServiceAdd(product, qty ?? 1),
    onSuccess: () => qc.invalidateQueries({ queryKey: CART_QUERY_KEY }),
  });
}

export function useUpdateCartItem() {
  const qc = useQueryClient();
  return useMutation({
    mutationFn: ({ productId, qty }: { productId: string; qty: number }) =>
      cartServiceUpdate(productId, qty),
    onSuccess: () => qc.invalidateQueries({ queryKey: CART_QUERY_KEY }),
  });
}

export function useRemoveCartItem() {
  const qc = useQueryClient();
  return useMutation({
    mutationFn: (productId: string) => cartServiceRemove(productId),
    onSuccess: () => qc.invalidateQueries({ queryKey: CART_QUERY_KEY }),
  });
}

export function useClearCart() {
  const qc = useQueryClient();
  return useMutation({
    mutationFn: () => cartServiceClear(),
    onSuccess: () => qc.invalidateQueries({ queryKey: CART_QUERY_KEY }),
  });
}

export { CART_QUERY_KEY, parseCartError };
export type { CartErrorContext };
```

**Lưu ý:**
- `useCart()` chạy cho cả guest và user — `fetchCart()` trong service layer route. Header badge subscribe cùng key.
- `staleTime: Infinity` + manual `invalidateQueries` = write-through behavior locked (D-11, KHÔNG optimistic UI).
- `parseCartError` export ra cho callers (cart page, checkout) reuse khi xử lý mutation onError.
- `'use client'` directive ở đầu file vì hook dùng React state.
- Caller chịu trách nhiệm hiển thị toast (mutation onError handler trong component, không ở hook — tránh hard-couple toast system vào hook).
  </action>
  <verify>
    <automated>cd sources/frontend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - File `sources/frontend/src/hooks/useCart.ts` tồn tại.
    - `grep -c "'use client'" useCart.ts` returns 1.
    - `grep -c "import { useQuery, useMutation, useQueryClient } from '@tanstack/react-query'" useCart.ts` returns 1.
    - `grep -c "export function useCart" useCart.ts` returns 1.
    - `grep -c "export function useAddToCart\|export function useUpdateCartItem\|export function useRemoveCartItem\|export function useClearCart" useCart.ts` returns 4.
    - `grep -c "queryKey: CART_QUERY_KEY" useCart.ts` >= 5 (1 query + 4 invalidate).
    - `grep -c "staleTime: Infinity" useCart.ts` returns 1.
    - `grep -c "STOCK_SHORTAGE" useCart.ts` returns 1.
    - `grep -c "Số lượng vượt quá tồn kho" useCart.ts` returns 1.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>useCart.ts có 5 hooks compile clean. Error parser sẵn sàng. Hook ready cho consumers (Plan 04).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FE service layer ↔ localStorage | localStorage same-origin browser-enforced (T-05 accepted) |
| FE service layer → BE qua http.ts | Bearer token tự attach từ localStorage; 401 redirect (đã có) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-10 | Information Disclosure | Guest cart leak qua localStorage cross-origin | accept | localStorage same-origin browser-enforced; KHÔNG mitigation FE-side cần thiết (T-05 phase model) |
| T-18-11 | Tampering | User edit localStorage['cart'] với productId fake | mitigate | BE re-validate ở mọi mutation (Plan 02 stock check + UNIQUE constraint). FE chỉ là cache — DB là truth |
| T-18-12 | Spoofing | XSS inject script đọc accessToken từ localStorage | accept | Phase 6 D-11/D-12 tradeoff đã accept; STORE-04 deferred |
| T-18-13 | Tampering | React Query cache stale sau merge → user thấy cart cũ | mitigate | invalidateQueries(['cart']) sau mỗi mutation success; staleTime: Infinity buộc refetch on invalidate |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0 (cả cart.ts + useCart.ts).
- Lint check: `npm run lint -- src/services/cart.ts src/hooks/useCart.ts` exits 0.
- Manual: import useCart từ một component test, verify React Query DevTools hiển thị query `['cart']`.
- Backward-compat: tìm tất cả `import.*from '@/services/cart'` trong codebase và confirm signatures cũ (readCart, addToCart, updateQuantity, removeFromCart, clearCart) vẫn work — chỉ với caveat addToCart/etc. trở thành async (Plan 04 sẽ refactor consumers).
</verification>

<success_criteria>
1. services/cart.ts dual-backend routing với 9 exports public, 12 internal helpers.
2. useCart.ts có 5 hooks: useCart, useAddToCart, useUpdateCartItem, useRemoveCartItem, useClearCart.
3. parseCartError + CART_QUERY_KEY exported reuse cho consumers.
4. mergeGuestCartToServer ready cho Plan 05 wire vào AuthProvider.
5. clearLocalCart export riêng cho AuthProvider.logout (D-15).
6. TypeScript compile clean.
</success_criteria>

<output>
After completion, create `.planning/phases/18-storage-audit-cart-db/18-03-SUMMARY.md` documenting:
- Public API surface mới + signature changes (sync→async cho mutations)
- Hydration strategy (N+1 calls, MVP acceptable)
- React Query key + invalidation pattern
- Backward-compat: callers chưa migrate vẫn compile (chỉ cần await mutations)
</output>
