---
phase: 18-storage-audit-cart-db
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sources/backend/order-service/src/main/resources/db/migration/V4__add_cart_tables.sql
  - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/domain/CartEntity.java
  - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/domain/CartItemEntity.java
  - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/domain/CartDto.java
  - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/domain/CartItemDto.java
  - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/domain/CartMapper.java
  - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/repository/CartRepository.java
  - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/repository/CartItemRepository.java
  - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/repository/InMemoryCartRepository.java
  - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/service/OrderCrudService.java
autonomous: true
requirements:
  - STORE-02
tags:
  - backend
  - jpa
  - flyway
  - cart
must_haves:
  truths:
    - "Database has order_svc.carts table với UNIQUE(user_id) + order_svc.cart_items với UNIQUE(cart_id, product_id) + CHECK(quantity > 0) + FK ON DELETE CASCADE"
    - "CartEntity JPA entity (NEW shape) có @OneToMany items với cascade=ALL + orphanRemoval=true"
    - "CartItemEntity JPA entity có @ManyToOne cart + product_id + quantity với UniqueConstraint"
    - "CartRepository extends JpaRepository<CartEntity,String> có findByUserId; CartItemRepository có native upsert query"
    - "InMemoryCartRepository.java đã bị xóa khỏi codebase"
    - "OrderCrudService không còn import/reference InMemoryCartRepository, CartUpsertRequest, hay các method listCarts/getCart/createCart/updateCart/deleteCart"
    - "mvn -pl order-service compile chạy OK (không broken reference)"
  artifacts:
    - path: "sources/backend/order-service/src/main/resources/db/migration/V4__add_cart_tables.sql"
      provides: "Flyway migration tạo carts + cart_items"
      contains: "CREATE TABLE order_svc.carts"
    - path: "sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/domain/CartEntity.java"
      provides: "JPA entity CartEntity (REPLACE stub cũ)"
      contains: "@OneToMany"
    - path: "sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/domain/CartItemEntity.java"
      provides: "JPA entity CartItemEntity (NEW)"
      contains: "@ManyToOne"
    - path: "sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/repository/CartRepository.java"
      provides: "Spring Data JPA repo cho CartEntity"
      contains: "findByUserId"
    - path: "sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/repository/CartItemRepository.java"
      provides: "Spring Data JPA repo + native upsertAddQuantity query"
      contains: "ON CONFLICT"
  key_links:
    - from: "CartEntity"
      to: "CartItemEntity"
      via: "@OneToMany(mappedBy=\"cart\", cascade=ALL, orphanRemoval=true)"
      pattern: "@OneToMany.*mappedBy.*cart"
    - from: "CartItemRepository.upsertAddQuantity"
      to: "PostgreSQL ON CONFLICT (cart_id, product_id)"
      via: "@Modifying @Query nativeQuery=true"
      pattern: "ON CONFLICT \\(cart_id, product_id\\)"
---

<objective>
Tạo BE foundation cho Cart→DB migration: Flyway V4 schema (carts + cart_items với UNIQUE constraints), JPA entities theo Phase 8 OrderEntity↔OrderItemEntity pattern, repositories (bao gồm native SQL upsert cho idempotent ADD/MERGE), và XÓA HOÀN TOÀN cart-stub cũ (InMemoryCartRepository + cart legacy methods trong OrderCrudService).

Purpose: Chuẩn bị data layer để Plan 02 build CartCrudService + CartController. Đây là foundation Wave 1 không phụ thuộc gì, chạy song song với mọi BE foundation phase khác.

Output: Schema V4 migrate-able, JPA entities + repositories ready, codebase compile-clean sau khi xóa stub.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-storage-audit-cart-db/18-CONTEXT.md
@.planning/phases/18-storage-audit-cart-db/18-RESEARCH.md
@.planning/phases/08-cart-order-persistence/08-CONTEXT.md
@sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/domain/OrderItemEntity.java
@sources/backend/order-service/src/main/resources/db/migration/V2__add_order_items.sql
@sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/service/OrderCrudService.java

<interfaces>
<!-- Patterns reuse từ Phase 8 OrderEntity↔OrderItemEntity -->

OrderItemEntity (Phase 8) — pattern cho CartItemEntity:
```java
@Entity
@Table(name = "order_items", schema = "order_svc")
public class OrderItemEntity {
  @Id @Column(length = 36, nullable = false, updatable = false) private String id;
  @ManyToOne(fetch = FetchType.LAZY)
  @JoinColumn(name = "order_id", nullable = false)
  private OrderEntity order;
  @Column(name = "product_id", nullable = false, length = 36) private String productId;
  @Column(nullable = false) private int quantity;
  // ... record-style getters: id(), productId(), quantity()
}
```

V2__add_order_items.sql — pattern cho V4:
```sql
CREATE TABLE IF NOT EXISTS order_svc.order_items (
  id VARCHAR(36) PRIMARY KEY,
  order_id VARCHAR(36) NOT NULL,
  ...
);
CREATE INDEX IF NOT EXISTS idx_order_items_order_id ON order_svc.order_items(order_id);
```

OrderCrudService methods cần XÓA (legacy cart, không có endpoint nào gọi):
- `listCarts(int, int, String, boolean)`
- `getCart(String, boolean)`
- `createCart(CartUpsertRequest)`
- `updateCart(String, CartUpsertRequest)`
- `deleteCart(String)`
- `cartComparator(String)`
- record `CartUpsertRequest`
- field `private final InMemoryCartRepository cartRepository`
- constructor param `InMemoryCartRepository cartRepository` + assignment
- import `com.ptit.htpt.orderservice.domain.CartEntity` (cũ)
- import `com.ptit.htpt.orderservice.repository.InMemoryCartRepository`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tạo Flyway V4 migration carts + cart_items + JPA entities + DTOs + Mapper</name>
  <read_first>
    - sources/backend/order-service/src/main/resources/db/migration/V2__add_order_items.sql (Phase 8 pattern: schema-qualified table name + IF NOT EXISTS + INDEX)
    - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/domain/OrderItemEntity.java (JPA pattern: @ManyToOne LAZY, @JoinColumn, record-style getters)
    - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/domain/OrderEntity.java (parent entity pattern: @OneToMany cascade=ALL orphanRemoval=true, addItem helper bidirectional)
    - .planning/phases/18-storage-audit-cart-db/18-CONTEXT.md §D-01, D-02 (schema spec exact)
  </read_first>
  <files>
    sources/backend/order-service/src/main/resources/db/migration/V4__add_cart_tables.sql,
    sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/domain/CartEntity.java,
    sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/domain/CartItemEntity.java,
    sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/domain/CartDto.java,
    sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/domain/CartItemDto.java,
    sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/domain/CartMapper.java
  </files>
  <action>
**1. V4__add_cart_tables.sql** (per D-01, EXACT DDL):
```sql
-- Phase 18 / STORE-02 (D-01): Cart persistence schema.
-- carts: 1 row per user_id (UNIQUE). cart_items: per-product line, UNIQUE(cart_id, product_id) cho idempotent upsert.

CREATE TABLE IF NOT EXISTS order_svc.carts (
  id          VARCHAR(36)   PRIMARY KEY,
  user_id     VARCHAR(36)   NOT NULL UNIQUE,
  created_at  TIMESTAMPTZ   NOT NULL DEFAULT now(),
  updated_at  TIMESTAMPTZ   NOT NULL DEFAULT now()
);

CREATE TABLE IF NOT EXISTS order_svc.cart_items (
  id          VARCHAR(36)   PRIMARY KEY,
  cart_id     VARCHAR(36)   NOT NULL REFERENCES order_svc.carts(id) ON DELETE CASCADE,
  product_id  VARCHAR(36)   NOT NULL,
  quantity    INT           NOT NULL CHECK (quantity > 0),
  created_at  TIMESTAMPTZ   NOT NULL DEFAULT now(),
  updated_at  TIMESTAMPTZ   NOT NULL DEFAULT now(),
  UNIQUE (cart_id, product_id)
);

CREATE INDEX IF NOT EXISTS idx_cart_items_cart_id ON order_svc.cart_items(cart_id);
```

**2. CartEntity.java** — REPLACE file cũ hoàn toàn (xóa record stub `userId/productId/quantity/status/deleted/...`):
```java
package com.ptit.htpt.orderservice.domain;

import jakarta.persistence.*;
import java.time.Instant;
import java.util.ArrayList;
import java.util.List;
import java.util.UUID;

@Entity
@Table(name = "carts", schema = "order_svc")
public class CartEntity {
  @Id @Column(length = 36, nullable = false, updatable = false) private String id;
  @Column(name = "user_id", length = 36, nullable = false, unique = true) private String userId;

  @OneToMany(mappedBy = "cart", cascade = CascadeType.ALL, orphanRemoval = true, fetch = FetchType.LAZY)
  private List<CartItemEntity> items = new ArrayList<>();

  @Column(name = "created_at", nullable = false) private Instant createdAt;
  @Column(name = "updated_at", nullable = false) private Instant updatedAt;

  protected CartEntity() {}

  public static CartEntity create(String userId) {
    CartEntity c = new CartEntity();
    c.id = UUID.randomUUID().toString();
    c.userId = userId;
    c.createdAt = Instant.now();
    c.updatedAt = Instant.now();
    return c;
  }

  public void addItem(CartItemEntity item) {
    items.add(item);
    item.setCart(this);
    this.updatedAt = Instant.now();
  }

  public void removeItem(CartItemEntity item) {
    items.remove(item);
    item.setCart(null);
    this.updatedAt = Instant.now();
  }

  public void touch() { this.updatedAt = Instant.now(); }

  public String id() { return id; }
  public String userId() { return userId; }
  public List<CartItemEntity> items() { return items; }
  public Instant createdAt() { return createdAt; }
  public Instant updatedAt() { return updatedAt; }
}
```

**3. CartItemEntity.java** (NEW):
```java
package com.ptit.htpt.orderservice.domain;

import jakarta.persistence.*;
import java.time.Instant;
import java.util.UUID;

@Entity
@Table(name = "cart_items", schema = "order_svc",
       uniqueConstraints = @UniqueConstraint(columnNames = {"cart_id", "product_id"}))
public class CartItemEntity {
  @Id @Column(length = 36, nullable = false, updatable = false) private String id;

  @ManyToOne(fetch = FetchType.LAZY)
  @JoinColumn(name = "cart_id", nullable = false)
  private CartEntity cart;

  @Column(name = "product_id", nullable = false, length = 36) private String productId;
  @Column(nullable = false) private int quantity;
  @Column(name = "created_at", nullable = false) private Instant createdAt;
  @Column(name = "updated_at", nullable = false) private Instant updatedAt;

  protected CartItemEntity() {}

  public static CartItemEntity create(CartEntity cart, String productId, int quantity) {
    CartItemEntity i = new CartItemEntity();
    i.id = UUID.randomUUID().toString();
    i.cart = cart;
    i.productId = productId;
    i.quantity = quantity;
    i.createdAt = Instant.now();
    i.updatedAt = Instant.now();
    return i;
  }

  public void setQuantity(int quantity) {
    this.quantity = quantity;
    this.updatedAt = Instant.now();
  }

  public void setCart(CartEntity cart) { this.cart = cart; }

  public String id() { return id; }
  public String productId() { return productId; }
  public int quantity() { return quantity; }
  public Instant createdAt() { return createdAt; }
  public Instant updatedAt() { return updatedAt; }

  // equals/hashCode based on id only — tránh LazyInit infinite loop trên collection
  @Override public boolean equals(Object o) {
    if (this == o) return true;
    if (!(o instanceof CartItemEntity that)) return false;
    return id != null && id.equals(that.id);
  }
  @Override public int hashCode() { return id == null ? 0 : id.hashCode(); }
}
```

**4. CartItemDto.java** (NEW — wire format FE):
```java
package com.ptit.htpt.orderservice.domain;

public record CartItemDto(
    String id,
    String productId,
    int quantity
) {}
```

**5. CartDto.java** (NEW):
```java
package com.ptit.htpt.orderservice.domain;

import java.util.List;

public record CartDto(
    String id,
    String userId,
    List<CartItemDto> items
) {}
```

**6. CartMapper.java** (NEW — entity → DTO):
```java
package com.ptit.htpt.orderservice.domain;

import java.util.List;

public final class CartMapper {
  private CartMapper() {}

  public static CartDto toDto(CartEntity cart) {
    List<CartItemDto> items = cart.items().stream()
        .map(i -> new CartItemDto(i.id(), i.productId(), i.quantity()))
        .toList();
    return new CartDto(cart.id(), cart.userId(), items);
  }
}
```

**Lưu ý:**
- Schema-qualified table name `order_svc.cart_items` BẮT BUỘC trong DDL (Phase 8 pattern).
- equals/hashCode trên `id` only để tránh `LazyInitializationException` khi entity nằm trong HashSet/HashMap (Pitfall 2 từ research).
- KHÔNG thêm `unit_price_at_add` snapshot — cart hiển thị live price từ product-svc, chỉ snapshot khi tạo order (Claude's discretion D-CONTEXT đã decide default NO).
  </action>
  <verify>
    <automated>cd sources/backend/order-service && mvn -q compile -DskipTests</automated>
  </verify>
  <acceptance_criteria>
    - File `V4__add_cart_tables.sql` exists.
    - `grep -c "CREATE TABLE IF NOT EXISTS order_svc.carts" V4__add_cart_tables.sql` returns 1.
    - `grep -c "UNIQUE (cart_id, product_id)" V4__add_cart_tables.sql` returns 1.
    - `grep -c "ON DELETE CASCADE" V4__add_cart_tables.sql` returns 1.
    - `grep -c "CHECK (quantity > 0)" V4__add_cart_tables.sql` returns 1.
    - `grep -c "@OneToMany(mappedBy = \"cart\", cascade = CascadeType.ALL, orphanRemoval = true" CartEntity.java` returns 1.
    - `grep -c "@Table(name = \"carts\", schema = \"order_svc\")" CartEntity.java` returns 1.
    - `grep -c "@UniqueConstraint(columnNames = {\"cart_id\", \"product_id\"})" CartItemEntity.java` returns 1.
    - `grep -c "public record CartDto" CartDto.java` returns 1.
    - `grep -c "public record CartItemDto" CartItemDto.java` returns 1.
    - `grep -c "public static CartDto toDto" CartMapper.java` returns 1.
    - CartEntity.java NOT contains keyword `record` (đã chuyển thành class JPA).
    - CartEntity.java NOT contains `private final boolean deleted` hay `softDelete()` (stub cũ removed).
    - `mvn -q compile -DskipTests` exits 0.
  </acceptance_criteria>
  <done>V4 migration file viết đúng DDL spec. CartEntity, CartItemEntity, CartDto, CartItemDto, CartMapper compile clean. Stub cart cũ (record-style) bị thay thế hoàn toàn.</done>
</task>

<task type="auto">
  <name>Task 2: Tạo CartRepository + CartItemRepository (JPA + native upsert)</name>
  <read_first>
    - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/repository/OrderRepository.java (Spring Data JPA pattern + custom @Query @Modifying)
    - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/repository/OrderItemRepository.java (sibling repo pattern)
    - .planning/phases/18-storage-audit-cart-db/18-RESEARCH.md §Pattern 2 (PostgreSQL ON CONFLICT upsert exact SQL)
    - .planning/phases/18-storage-audit-cart-db/18-CONTEXT.md §D-05 (idempotent ADD semantics)
  </read_first>
  <files>
    sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/repository/CartRepository.java,
    sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/repository/CartItemRepository.java
  </files>
  <action>
**1. CartRepository.java** (NEW):
```java
package com.ptit.htpt.orderservice.repository;

import com.ptit.htpt.orderservice.domain.CartEntity;
import java.util.Optional;
import org.springframework.data.jpa.repository.EntityGraph;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.data.jpa.repository.Query;
import org.springframework.data.repository.query.Param;

public interface CartRepository extends JpaRepository<CartEntity, String> {

  /**
   * Lookup cart by user_id (UNIQUE constraint guarantees ≤1 row).
   * EntityGraph "items" để fetch-join tránh LazyInitializationException khi
   * mapper iterate items ngoài transaction (cùng pattern Phase 8 findByIdWithItems).
   */
  @EntityGraph(attributePaths = "items")
  Optional<CartEntity> findByUserId(@Param("userId") String userId);
}
```

**2. CartItemRepository.java** (NEW — bao gồm native upsert cho idempotent ADD):
```java
package com.ptit.htpt.orderservice.repository;

import com.ptit.htpt.orderservice.domain.CartItemEntity;
import java.util.Optional;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.data.jpa.repository.Modifying;
import org.springframework.data.jpa.repository.Query;
import org.springframework.data.repository.query.Param;

public interface CartItemRepository extends JpaRepository<CartItemEntity, String> {

  Optional<CartItemEntity> findByCartIdAndProductId(@Param("cartId") String cartId,
                                                    @Param("productId") String productId);

  /**
   * Idempotent ADD: INSERT new line, hoặc nếu (cart_id, product_id) đã tồn tại
   * thì cộng dồn quantity. Native SQL atomic single-statement → race-safe.
   *
   * D-05 (CONTEXT): POST /cart/items semantics = ADD (cộng dồn). Schema-qualified
   * `order_svc.cart_items` bắt buộc trong DO UPDATE clause vì self-reference
   * (PostgreSQL syntax requirement).
   *
   * Caller PHẢI flush + clear EntityManager sau khi upsert nếu sau đó query lại
   * cart vì native bypass JPA persistence context cache.
   */
  @Modifying
  @Query(value = """
      INSERT INTO order_svc.cart_items (id, cart_id, product_id, quantity, created_at, updated_at)
      VALUES (:id, :cartId, :productId, :quantity, now(), now())
      ON CONFLICT (cart_id, product_id)
      DO UPDATE SET
        quantity = order_svc.cart_items.quantity + EXCLUDED.quantity,
        updated_at = now()
      """, nativeQuery = true)
  int upsertAddQuantity(@Param("id") String id,
                        @Param("cartId") String cartId,
                        @Param("productId") String productId,
                        @Param("quantity") int quantity);
}
```

**Note:** KHÔNG dùng `upsertAddQuantity` cho `PATCH` (set absolute) — `PATCH` dùng `findByCartIdAndProductId` + `setQuantity` JPA standard.
  </action>
  <verify>
    <automated>cd sources/backend/order-service && mvn -q compile -DskipTests</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "extends JpaRepository<CartEntity, String>" CartRepository.java` returns 1.
    - `grep -c "Optional<CartEntity> findByUserId" CartRepository.java` returns 1.
    - `grep -c "@EntityGraph(attributePaths = \"items\")" CartRepository.java` returns 1.
    - `grep -c "extends JpaRepository<CartItemEntity, String>" CartItemRepository.java` returns 1.
    - `grep -c "Optional<CartItemEntity> findByCartIdAndProductId" CartItemRepository.java` returns 1.
    - `grep -c "ON CONFLICT (cart_id, product_id)" CartItemRepository.java` returns 1.
    - `grep -c "EXCLUDED.quantity" CartItemRepository.java` returns 1.
    - `grep -c "@Modifying" CartItemRepository.java` returns 1.
    - `grep -c "nativeQuery = true" CartItemRepository.java` returns 1.
    - `mvn -q compile -DskipTests` exits 0.
  </acceptance_criteria>
  <done>CartRepository + CartItemRepository compile clean. Native upsert SQL chứa đúng `ON CONFLICT (cart_id, product_id) DO UPDATE` syntax với schema-qualified table.</done>
</task>

<task type="auto">
  <name>Task 3: Xóa InMemoryCartRepository + dọn cart legacy methods khỏi OrderCrudService</name>
  <read_first>
    - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/service/OrderCrudService.java (5 method cart legacy + record CartUpsertRequest + import InMemoryCartRepository — TẤT CẢ phải xóa)
    - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/repository/InMemoryCartRepository.java (sẽ xóa file)
    - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/web/OrderController.java (verify KHÔNG endpoint nào reference các method cart legacy — research confirmed no callers)
  </read_first>
  <files>
    sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/repository/InMemoryCartRepository.java,
    sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/service/OrderCrudService.java
  </files>
  <action>
**Step 1:** XÓA file `InMemoryCartRepository.java` hoàn toàn (`rm` hoặc Delete).

**Step 2:** Edit `OrderCrudService.java` — XÓA chính xác các phần sau:

a) Imports xóa:
```
import com.ptit.htpt.orderservice.domain.CartEntity;
import com.ptit.htpt.orderservice.repository.InMemoryCartRepository;
```
(GIỮ lại các import khác đang dùng cho order flow)

b) Field xóa:
```
private final InMemoryCartRepository cartRepository;
```

c) Constructor — xóa param `InMemoryCartRepository cartRepository` và assignment `this.cartRepository = cartRepository;`. Constructor mới chỉ còn 4 params: `OrderRepository, OrderItemRepository, ObjectMapper, RestTemplate`.

d) Methods XÓA toàn bộ (5 methods + 1 helper):
- `public Map<String, Object> listCarts(int page, int size, String sort, boolean includeDeleted)` (~6 dòng)
- `public CartEntity getCart(String id, boolean includeDeleted)` (~7 dòng)
- `public CartEntity createCart(CartUpsertRequest request)` (~3 dòng)
- `public CartEntity updateCart(String id, CartUpsertRequest request)` (~4 dòng)
- `public void deleteCart(String id)` (~3 dòng)
- `private Comparator<CartEntity> cartComparator(String sort)` (~10 dòng helper)

e) Record XÓA:
```
public record CartUpsertRequest(
    @NotBlank String userId,
    @NotBlank String productId,
    @Min(1) int quantity,
    @NotBlank String status
) {}
```

f) GIỮ NGUYÊN:
- Toàn bộ order-related methods (`listOrders`, `getOrder`, `createOrder`, `createOrderFromCommand`, `updateOrder`, `updateOrderState`, `deleteOrder`, `findOrdersByUserId`, `listMyOrders`)
- `orderComparator`, `paginate`
- `OrderUpsertRequest`, `OrderStateRequest`, `CreateOrderCommand`, `OrderItemRequest`, `ShippingAddressRequest`, `ListMyOrdersQuery` records
- `validateStockOrThrow`, `deductStockAfterPersist`, `buildStockUpdateBody` (Phase 8 stock logic — Plan 02 sẽ reuse pattern này cho CartCrudService)

**Lý do an toàn xóa:** Research §Component Responsibilities confirmed các method này KHÔNG có endpoint nào gọi (audit Phase 8). CartUpsertRequest record cũng chỉ được dùng nội bộ trong các method bị xóa.
  </action>
  <verify>
    <automated>cd sources/backend/order-service && mvn -q compile -DskipTests && test ! -f src/main/java/com/ptit/htpt/orderservice/repository/InMemoryCartRepository.java</automated>
  </verify>
  <acceptance_criteria>
    - File `InMemoryCartRepository.java` KHÔNG còn tồn tại (`test ! -f` returns true).
    - `grep -c "InMemoryCartRepository" OrderCrudService.java` returns 0.
    - `grep -c "CartUpsertRequest" OrderCrudService.java` returns 0.
    - `grep -c "listCarts\|createCart\|updateCart\|deleteCart\|cartComparator" OrderCrudService.java` returns 0.
    - `grep -c "public CartEntity getCart" OrderCrudService.java` returns 0.
    - `grep -rn "InMemoryCartRepository" sources/backend/order-service/src/` returns no matches.
    - `grep -rn "CartUpsertRequest" sources/backend/order-service/src/` returns no matches.
    - `mvn -q compile -DskipTests` exits 0 (no broken references).
    - Order-flow methods còn nguyên: `grep -c "createOrderFromCommand\|listMyOrders\|validateStockOrThrow" OrderCrudService.java` returns 3.
  </acceptance_criteria>
  <done>InMemoryCartRepository.java đã xóa. OrderCrudService.java compile clean với 0 reference tới cart legacy code. Order flow methods nguyên vẹn.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Flyway migration → Postgres | DDL chạy ở startup; nếu user chạy migration với schema khác có thể conflict — đã mitigate bằng `IF NOT EXISTS` |
| JPA entity ↔ DB | UNIQUE/CHECK constraints enforced ở DB layer (defense-in-depth ngoài Bean Validation) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Tampering | cart_items.quantity | mitigate | DB CHECK (quantity > 0) chặn quantity ≤ 0 ngay cả khi service layer bypass |
| T-18-02 | Tampering | (cart_id, product_id) duplicate | mitigate | UNIQUE constraint chặn race-condition double-INSERT (Pattern 2 ON CONFLICT) |
| T-18-03 | Information Disclosure | Stale cart_items khi product bị soft-delete | accept | Defer cleanup job; FE GET /cart sẽ tolerate missing product (Plan 03 placeholder rendering) |
| T-18-04 | Denial of Service | Migration chạy trên prod làm downtime | accept | Flyway baseline đã có v1.1; V4 = additive CREATE TABLE IF NOT EXISTS, không lock table existing |
</threat_model>

<verification>
- `mvn -pl order-service compile -DskipTests` exits 0.
- `mvn -pl order-service flyway:info` (nếu plugin available) shows V4 PENDING.
- `grep -rn "InMemoryCartRepository\|CartUpsertRequest" sources/backend/order-service/src/` returns no matches.
- Java imports clean: `mvn -q compile` không warning về unused imports trong OrderCrudService.
</verification>

<success_criteria>
1. V4 migration file đúng DDL spec (carts UNIQUE user_id + cart_items UNIQUE(cart_id, product_id) + CHECK + FK CASCADE).
2. CartEntity, CartItemEntity JPA entities với @OneToMany cascade=ALL orphanRemoval=true pattern (reuse Phase 8).
3. CartDto, CartItemDto, CartMapper ready cho controller layer.
4. CartRepository + CartItemRepository compile clean với native upsert query.
5. InMemoryCartRepository.java đã xóa, OrderCrudService 0 reference legacy cart.
6. `mvn compile` exits 0 cho order-service.
</success_criteria>

<output>
After completion, create `.planning/phases/18-storage-audit-cart-db/18-01-SUMMARY.md` documenting:
- Schema V4 đã apply pattern + index strategy
- JPA mapping decisions (cascade=ALL, orphanRemoval=true, fetch=LAZY default, equals/hashCode by id)
- Native upsert SQL + lưu ý flush+clear sau khi gọi
- Cleanup audit: 0 reference InMemoryCartRepository còn lại
</output>
