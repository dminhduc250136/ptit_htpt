---
phase: 18-storage-audit-cart-db
plan: 05
type: execute
wave: 4
depends_on: [18-03]
files_modified:
  - sources/frontend/src/providers/AuthProvider.tsx
  - sources/frontend/src/services/auth.ts
autonomous: true
requirements:
  - STORE-02
tags:
  - frontend
  - auth
  - cart-merge
must_haves:
  truths:
    - "AuthProvider.login() async — sau khi setTokens, gọi mergeGuestCartToServer(); merge fail → log + toast warning, KHÔNG block login"
    - "AuthProvider.logout() — gọi clearLocalCart() (D-15) để cart user A không leak sang guest session tiếp theo"
    - "AuthProvider.logout() — invalidate React Query cache (queryClient.removeQueries(['cart'])) tránh stale data"
    - "Login flow: localStorage cart non-empty + login OK → POST /api/orders/cart/merge → success: clearLocalCart"
    - "Toast warning vi nếu merge fail: 'Không đồng bộ được giỏ hàng cũ — vui lòng kiểm tra lại'"
  artifacts:
    - path: "sources/frontend/src/providers/AuthProvider.tsx"
      provides: "Auth state với cart merge wired"
      contains: "mergeGuestCartToServer"
  key_links:
    - from: "AuthProvider.login"
      to: "POST /api/orders/cart/merge"
      via: "mergeGuestCartToServer()"
      pattern: "mergeGuestCartToServer"
    - from: "AuthProvider.logout"
      to: "clearLocalCart()"
      via: "direct call"
      pattern: "clearLocalCart"
---

<objective>
Wire cart merge vào auth lifecycle:
- **Login flow** (D-13, D-14): sau khi token set thành công, gọi `mergeGuestCartToServer()` từ services/cart.ts. Success → localStorage cart đã clear bên trong helper. Fail → log + toast warning, KHÔNG block login.
- **Logout flow** (D-15): gọi `clearLocalCart()` để xóa cart localStorage. Bonus: `queryClient.removeQueries(['cart'])` invalidate React Query cache tránh stale UI.

Purpose: Hoàn tất guest→user merge UX. Sau plan này, user A login với guest cart non-empty → cart sync vào DB; logout → cart clear, user B login cùng browser thấy DB cart riêng.

Output: AuthProvider hoàn chỉnh login/logout cart lifecycle.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/18-storage-audit-cart-db/18-CONTEXT.md
@.planning/phases/18-storage-audit-cart-db/18-RESEARCH.md
@.planning/phases/18-storage-audit-cart-db/18-03-SUMMARY.md
@sources/frontend/src/providers/AuthProvider.tsx
@sources/frontend/src/services/auth.ts

<interfaces>
Plan 03 services/cart.ts exports:
```typescript
export async function mergeGuestCartToServer(): Promise<{ ok: boolean; error?: unknown }>
export function clearLocalCart(): void
```

AuthProvider.tsx (cũ, hiện tại):
```typescript
const login = useCallback((u: AuthState['user']) => {
  setUser(u);
  setIsAuthenticated(true);
  if (u && typeof window !== 'undefined') {
    window.localStorage.setItem('userProfile', JSON.stringify(u));
  }
}, []);

const logout = useCallback(() => {
  clearTokensHelper();
  if (typeof window !== 'undefined') {
    window.localStorage.removeItem('userProfile');
  }
  setUser(null);
  setIsAuthenticated(false);
}, []);
```

services/auth.ts login() (cũ):
```typescript
export async function login(body: LoginRequest): Promise<AuthResponse> {
  const data = await httpPost<AuthResponse>('/api/users/auth/login', body);
  setTokens(data.accessToken, data.refreshToken ?? undefined);
  if (data.user?.roles) setUserRole(data.user.roles);
  return data;
}
```

QueryClient access: cần `useQueryClient()` trong AuthProvider để invalidate ['cart'] cache. AuthProvider phải nằm BÊN TRONG QueryClientProvider trong app shell — verify trong app/layout.tsx hoặc providers wrapper.

useToast hook signature: `const { showToast } = useToast()` — `showToast(message: string, variant: 'error'|'warning'|'success')`. Verify export name + signature từ Toast component.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update AuthProvider.login() async + merge cart, AuthProvider.logout() clear cart</name>
  <read_first>
    - sources/frontend/src/providers/AuthProvider.tsx (full hiện tại)
    - sources/frontend/src/services/cart.ts (Plan 03 — mergeGuestCartToServer + clearLocalCart exports)
    - sources/frontend/src/services/auth.ts (login() trả AuthResponse + setTokens trước login() callback của AuthProvider)
    - sources/frontend/src/app/layout.tsx HOẶC sources/frontend/src/providers/* (verify QueryClientProvider wrap AuthProvider — nếu KHÔNG, useQueryClient sẽ throw)
    - sources/frontend/src/components/ui/Toast/Toast.tsx (useToast signature, hoặc một component đã dùng useToast như checkout/page.tsx)
  </read_first>
  <files>
    sources/frontend/src/providers/AuthProvider.tsx
  </files>
  <action>
**Step 0:** Verify AuthProvider nested INSIDE QueryClientProvider trong app shell. Nếu KHÔNG, di chuyển AuthProvider vào trong (or hoist QueryClient up).

**Step 1:** Update `AuthProvider.tsx`:

```typescript
'use client';

import React, { createContext, useContext, useState, useEffect, useCallback } from 'react';
import { useQueryClient } from '@tanstack/react-query';
import { getAccessToken, clearTokens as clearTokensHelper } from '@/services/token';
import { mergeGuestCartToServer, clearLocalCart } from '@/services/cart';

interface AuthState {
  isAuthenticated: boolean;
  user: { id: string; email: string; name: string } | null;
}

const AuthContext = createContext<AuthState & {
  login: (user: AuthState['user']) => Promise<void>; // ASYNC giờ
  logout: () => void;
}>({
  isAuthenticated: false,
  user: null,
  login: async () => {},
  logout: () => {},
});

export const useAuth = () => useContext(AuthContext);

export function AuthProvider({ children }: { children: React.ReactNode }) {
  const queryClient = useQueryClient();

  const [user, setUser] = useState<AuthState['user']>(() => {
    if (typeof window === 'undefined') return null;
    try {
      const raw = window.localStorage.getItem('userProfile');
      return raw ? (JSON.parse(raw) as AuthState['user']) : null;
    } catch { return null; }
  });
  const [isAuthenticated, setIsAuthenticated] = useState<boolean>(() => {
    if (typeof window === 'undefined') return false;
    return getAccessToken() !== null;
  });

  // Cross-tab storage sync (giữ nguyên hiện tại)
  useEffect(() => {
    function onStorage(e: StorageEvent) {
      if (e.key === 'accessToken' || e.key === 'userProfile' || e.key === null) {
        const token = getAccessToken();
        setIsAuthenticated(token !== null);
        try {
          const raw = typeof window !== 'undefined'
            ? window.localStorage.getItem('userProfile') : null;
          setUser(raw ? (JSON.parse(raw) as AuthState['user']) : null);
        } catch { setUser(null); }
      }
    }
    if (typeof window !== 'undefined') {
      window.addEventListener('storage', onStorage);
      return () => window.removeEventListener('storage', onStorage);
    }
  }, []);

  /**
   * D-13, D-14: login() async — sau khi setUser + setIsAuthenticated,
   * gọi mergeGuestCartToServer() để merge guest cart vào DB.
   *
   * Caller (services/auth.ts.login) đã setTokens(accessToken) TRƯỚC khi gọi
   * provider login(), nên httpPost trong mergeGuestCartToServer sẽ có Bearer token.
   *
   * Merge fail → log + toast warning, KHÔNG throw, KHÔNG block login (D-14).
   */
  const login = useCallback(async (u: AuthState['user']) => {
    setUser(u);
    setIsAuthenticated(true);
    if (u && typeof window !== 'undefined') {
      window.localStorage.setItem('userProfile', JSON.stringify(u));
    }

    // Phase 18 / D-13: merge guest cart → DB
    const result = await mergeGuestCartToServer();
    if (!result.ok) {
      // D-14: log + warning, KHÔNG block login
      console.warn('[auth] cart merge failed during login:', result.error);
      // Toast warning — dùng window event hoặc dispatch tới Toast system.
      // Nếu useToast không khả dụng trong AuthProvider context (ToastProvider phải wrap đúng vị trí),
      // dùng window event 'cart:merge-failed' để Toast system listen.
      if (typeof window !== 'undefined') {
        window.dispatchEvent(new CustomEvent('cart:merge-failed', {
          detail: { message: 'Không đồng bộ được giỏ hàng cũ — vui lòng kiểm tra lại' },
        }));
      }
    }

    // Invalidate cart query để useCart() refetch DB cart mới (sau merge or fresh login).
    queryClient.invalidateQueries({ queryKey: ['cart'] });
  }, [queryClient]);

  /**
   * D-15: logout — clear localStorage cart để cart user A không leak sang
   * guest session tiếp theo cùng browser. Cũng remove ['cart'] query cache.
   */
  const logout = useCallback(() => {
    clearTokensHelper();
    if (typeof window !== 'undefined') {
      window.localStorage.removeItem('userProfile');
    }

    // Phase 18 / D-15: clear cart localStorage + React Query cache
    clearLocalCart();
    queryClient.removeQueries({ queryKey: ['cart'] });

    setUser(null);
    setIsAuthenticated(false);
  }, [queryClient]);

  return (
    <AuthContext.Provider value={{ isAuthenticated, user, login, logout }}>
      {children}
    </AuthContext.Provider>
  );
}
```

**Lưu ý critical:**
- Context value `login` thay từ sync `() => void` thành `async () => Promise<void>`. Callers (login page, register page) phải `await login(user)` — VERIFY các nơi gọi `login()` trong codebase. Nếu callers không await, login flow vẫn work (fire-and-forget) nhưng router.push có thể chạy trước merge.
- `useQueryClient()` BẮT BUỘC AuthProvider nested inside QueryClientProvider. Nếu KHÔNG, throw "No QueryClient set, use QueryClientProvider to set one". Verify app shell.
- Toast: dùng `window.dispatchEvent(CustomEvent)` thay vì `useToast` trực tiếp vì `useToast` cần ToastProvider wrap. Toast system phải có listener cho `cart:merge-failed` event — nếu KHÔNG, planner thay thế bằng `useToast` trực tiếp (verify ToastProvider wrap order trong app shell).
  </action>
  <verify>
    <automated>cd sources/frontend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "import { useQueryClient } from '@tanstack/react-query'" AuthProvider.tsx` returns 1.
    - `grep -c "import { mergeGuestCartToServer, clearLocalCart }" AuthProvider.tsx` returns 1.
    - `grep -c "const login = useCallback(async" AuthProvider.tsx` returns 1.
    - `grep -c "await mergeGuestCartToServer" AuthProvider.tsx` returns 1.
    - `grep -c "clearLocalCart()" AuthProvider.tsx` returns 1.
    - `grep -c "queryClient.invalidateQueries.*'cart'" AuthProvider.tsx` returns 1.
    - `grep -c "queryClient.removeQueries.*'cart'" AuthProvider.tsx` returns 1.
    - `grep -c "Không đồng bộ được giỏ hàng" AuthProvider.tsx` returns 1.
    - Context type updated: `grep -c "login: (user: AuthState\['user'\]) => Promise<void>" AuthProvider.tsx` returns 1.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>AuthProvider login async với cart merge + invalidate. logout clear cart + remove query cache. Toast event dispatched on merge fail.</done>
</task>

<task type="auto">
  <name>Task 2: Update login/register page callers để await async login(), xác nhận Toast listener cho cart:merge-failed</name>
  <read_first>
    - Tìm callers của `useAuth().login`: `grep -rn "auth.*login\|login(.*user" sources/frontend/src/app/login/ sources/frontend/src/app/register/`
    - sources/frontend/src/app/login/page.tsx (xác định vị trí gọi login từ AuthProvider context)
    - sources/frontend/src/app/register/page.tsx
    - sources/frontend/src/components/ui/Toast/Toast.tsx HOẶC ToastProvider — verify event listener pattern hoặc thêm mới
  </read_first>
  <files>
    sources/frontend/src/services/auth.ts
  </files>
  <action>
**Step 1:** Locate login + register page callers (planner grep). Trong tất cả nơi gọi `const { login } = useAuth(); login(user)`:
- Add `await` trước `login(user)`. Vì login giờ async — nếu không await, router.push chạy trước merge xong → user thấy UI flicker hoặc empty cart 1 giây.

Ví dụ login page (giả định pattern):
```typescript
const { login } = useAuth();
// CŨ: login(authResponse.user); router.push('/');
// MỚI:
await login(authResponse.user);
router.push('/');
```

**Step 2:** services/auth.ts — KHÔNG cần thay đổi (services/auth.ts.login() đã async return AuthResponse, caller đã await để gọi setUser → AuthProvider.login(user)). Confirm pattern đúng và để file note giải thích phụ thuộc tiếng Việt:

```typescript
// Phase 18 / D-13 note (top of file or near login()):
// AuthProvider.login() đã trở thành async (merge cart sau setTokens).
// Login/register page MUỐN guarantee cart merge complete trước router.push
// PHẢI: await authProvider.login(authResponse.user); router.push(...);
```

(Đây là content note thuần túy nếu services/auth.ts không cần thay logic — chỉ thêm comment Vietnamese.)

**Step 3:** Toast event listener — verify hoặc thêm vào ToastProvider/Toast component:
```typescript
// Toast.tsx hoặc ToastProvider.tsx (nếu có):
useEffect(() => {
  function onCartMergeFailed(e: Event) {
    const detail = (e as CustomEvent).detail as { message: string } | undefined;
    if (detail?.message) showToast(detail.message, 'warning');
  }
  window.addEventListener('cart:merge-failed', onCartMergeFailed);
  return () => window.removeEventListener('cart:merge-failed', onCartMergeFailed);
}, []);
```

Nếu Toast architecture không support event listener pattern, ALTERNATIVE: replace `window.dispatchEvent` trong AuthProvider Task 1 bằng `useToast()` direct call — VERIFY ToastProvider wrap AuthProvider trong app shell.

**Files modified scope note:** Plan 05 frontmatter `files_modified` chỉ list AuthProvider + services/auth.ts. Login page + Toast file là caller-side updates — nếu cần edit, planner thêm vào files_modified và update acceptance criteria. Nếu Toast component đã có CustomEvent listener khác (e.g., `cart:change`), thêm `cart:merge-failed` cùng pattern.
  </action>
  <verify>
    <automated>cd sources/frontend && npx tsc --noEmit && grep -rn "await.*login(" src/app/login src/app/register | head -5</automated>
  </verify>
  <acceptance_criteria>
    - Tất cả callers `useAuth().login(...)` trong app/login/ + app/register/ có `await` prefix (verify: `grep -rn "login(.*\\.user" src/app/login src/app/register` matches đều có `await` trước).
    - services/auth.ts có comment Vietnamese giải thích phụ thuộc async login (search "Phase 18" or "merge cart" comment).
    - Toast component HOẶC ToastProvider HOẶC một consumer có event listener cho `cart:merge-failed` (`grep -rn "cart:merge-failed" sources/frontend/src/`) >= 1 listener.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Login/register callers await login(). Toast listener wired cho cart:merge-failed. Comment Vietnamese ghi chú phụ thuộc.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Login flow → cart merge | Token đã set TRƯỚC merge call → httpPost có Bearer token (verify auth.ts.login order) |
| Logout → cart cleanup | clearTokens + clearLocalCart + removeQueries — phải atomic feel cho user |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-17 | Information Disclosure | Logout không clear cart → user B login cùng browser thấy cart user A | mitigate | clearLocalCart() trong logout (D-15) + queryClient.removeQueries(['cart']). T-04 from phase model |
| T-18-18 | Tampering | Race: 2 tabs login đồng thời → 2 merge calls → double quantity | mitigate | BE upsert ON CONFLICT atomic — quantity = current + EXCLUDED. Tab 2 gọi merge với guest items giống tab 1 → BE detect đã merged (cart đã có items) → cộng dồn lần 2 SẼ tạo double. ACCEPT: localStorage clear ngay sau merge tab 1 thành công → tab 2 đọc localStorage empty → merge no-op |
| T-18-19 | Spoofing | Merge fail → user click Login lại → guest items merge 2 lần | mitigate | mergeGuestCartToServer chỉ clearLocalCart khi merge success. Fail → giữ localStorage để retry. Nếu user retry login thành công → merge OK → clear. KHÔNG double-merge vì guest items vẫn nguyên cho 1 lần merge thành công duy nhất |
| T-18-20 | DoS | mergeGuestCartToServer chậm → login UX block | mitigate | Caller có thể không await (trade-off: cart UI flicker). Plan 05 chọn await để đảm bảo correctness. Latency ~200-500ms acceptable cho login flow |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0.
- Manual test cart merge:
  1. Logout. Add 2 items vào cart (localStorage). 
  2. Login. → mergeGuestCartToServer fires → POST /merge. Sau login → /cart page hiển thị 2 items từ DB.
  3. localStorage.getItem('cart') === null (đã clear).
- Manual test logout cleanup:
  1. Login user A. Add items vào cart (DB).
  2. Logout. → localStorage.getItem('cart') === null.
  3. Add items vào cart (guest localStorage).
  4. Login user B. → user B's cart merge với guest items, KHÔNG thấy items user A.
- Manual test merge fail:
  1. Stop order-service. Add guest items. Login.
  2. Toast warning "Không đồng bộ được giỏ hàng cũ" hiện.
  3. localStorage.getItem('cart') VẪN có items (chưa clear).
  4. Login flow KHÔNG block — router.push tiếp tục.
</verification>

<success_criteria>
1. AuthProvider.login async, await mergeGuestCartToServer, invalidate ['cart'] query.
2. AuthProvider.logout clearLocalCart + removeQueries(['cart']).
3. Login/register callers await login().
4. Toast warning "Không đồng bộ được giỏ hàng cũ — vui lòng kiểm tra lại" hiển thị khi merge fail.
5. localStorage.getItem('cart') === null sau logout (T-04 mitigated, must_have #4 phase).
6. Cross-user browser test: user A logout → user B login KHÔNG thấy cart user A.
</success_criteria>

<output>
After completion, create `.planning/phases/18-storage-audit-cart-db/18-05-SUMMARY.md` documenting:
- Login flow merge sequence
- Logout cleanup steps
- Toast event pattern
- Cross-user browser test results
</output>
