---
phase: 18-storage-audit-cart-db
plan: 04
type: execute
wave: 4
depends_on: [18-03]
files_modified:
  - sources/frontend/src/app/cart/page.tsx
  - sources/frontend/src/app/checkout/page.tsx
  - sources/frontend/src/components/layout/Header.tsx
autonomous: true
requirements:
  - STORE-02
tags:
  - frontend
  - react-query
  - cart-consumers
must_haves:
  truths:
    - "Cart page subscribe useCart() hook — hiển thị data thật cho cả guest (localStorage) và user (DB)"
    - "Cart page mutations (qty +/-, remove, clear) gọi useUpdateCartItem/useRemoveCartItem/useClearCart với onError parse 409 STOCK_SHORTAGE → toast"
    - "Checkout page fetch cart async qua useCart, submit order với cart hydrated từ hook (KHÔNG đọc localStorage trực tiếp khi user logged-in)"
    - "Header badge subscribe ['cart'] query key — auto-update khi mutation invalidate (cả guest path qua cart:change event listener vẫn fire)"
    - "Sau order tạo thành công, gọi useClearCart().mutate() — clear cả localStorage (guest) lẫn DB cart (user)"
  artifacts:
    - path: "sources/frontend/src/app/cart/page.tsx"
      provides: "Cart page với React Query + mutation handlers"
      contains: "useCart"
    - path: "sources/frontend/src/app/checkout/page.tsx"
      provides: "Checkout page fetch cart async"
      contains: "useCart"
  key_links:
    - from: "Cart page mutation onError"
      to: "STOCK_SHORTAGE toast vi"
      via: "parseCartError"
      pattern: "STOCK_SHORTAGE"
    - from: "Header badge"
      to: "['cart'] query"
      via: "useCart subscription"
      pattern: "useCart"
---

<objective>
Refactor 3 consumers của cart service để dùng React Query hooks thay vì sync localStorage:
1. `/cart` page — hiển thị + mutate cart items
2. `/checkout` page — fetch cart async khi user logged-in
3. `Header.tsx` — cart badge count

Purpose: Hoàn tất FE consumer side. Sau plan này, user logged-in mở cart page sẽ thấy DB cart (cross-device persist), guest vẫn thấy localStorage cart. Mutation +/- buttons → React Query write-through.

Output: 3 files refactored, đầy đủ behavior visible cho user.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/18-storage-audit-cart-db/18-CONTEXT.md
@.planning/phases/18-storage-audit-cart-db/18-03-SUMMARY.md
@sources/frontend/src/app/cart/page.tsx
@sources/frontend/src/app/checkout/page.tsx
@sources/frontend/src/services/cart.ts

<interfaces>
Plan 03 outputs:
```typescript
// services/cart.ts
export type CartItem = { productId, name, thumbnailUrl, price, quantity, stock };
export function readCart(): CartItem[]                    // guest only sync
export async function fetchCart(): Promise<CartItem[]>     // both routes
export async function addToCart(product, qty?): Promise<CartItem[]>
export async function updateQuantity(productId, qty): Promise<CartItem[]>
export async function removeFromCart(productId): Promise<CartItem[]>
export async function clearCart(): Promise<CartItem[]>
export function clearLocalCart(): void                    // for logout

// hooks/useCart.ts
export function useCart(): UseQueryResult<CartItem[]>
export function useAddToCart(): UseMutationResult<CartItem[], unknown, { product, qty? }>
export function useUpdateCartItem(): UseMutationResult<CartItem[], unknown, { productId, qty }>
export function useRemoveCartItem(): UseMutationResult<CartItem[], unknown, string>
export function useClearCart(): UseMutationResult<CartItem[], unknown, void>
export function parseCartError(err: unknown): { code, message, shortageItems? }
```

Existing Header.tsx (planner cần đọc để xác định pattern hiện tại — có thể dùng `cart:change` event listener):
- Phải subscribe useCart() thay vì readCart() trực tiếp
- Vẫn keep `cart:change` event listener cho guest path để giữ compat (D-12)

Existing checkout/page.tsx (đã đọc):
- Dùng `readCart()` + `cart:change` listener — phải refactor sang useCart()
- `clearCart()` sau order thành công — phải dùng useClearCart().mutate()
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor cart/page.tsx sang useCart + mutation hooks</name>
  <read_first>
    - sources/frontend/src/app/cart/page.tsx (full hiện tại — sync readCart + cart:change listener)
    - sources/frontend/src/hooks/useCart.ts (Plan 03 output — 5 hooks + parseCartError)
    - sources/frontend/src/components/ui/Toast/Toast.tsx (useToast hook signature — đã dùng trong checkout)
  </read_first>
  <files>
    sources/frontend/src/app/cart/page.tsx
  </files>
  <action>
Refactor `cart/page.tsx`:

**Thay đổi:**
1. Replace `useState<CartItem[]>(() => readCart())` + `useEffect cart:change listener` bằng `const { data: cartItems = [], isLoading } = useCart()`.
2. Replace `removeFromCart(item.productId)` direct call bằng `removeMutation.mutate(item.productId)` từ `useRemoveCartItem()`.
3. Replace `updateQuantity(item.productId, item.quantity ± 1)` bằng `updateMutation.mutate({ productId, qty })` từ `useUpdateCartItem()`.
4. Add `onError` handler trên mutations dùng `parseCartError` + `useToast` để show vi message khi STOCK_SHORTAGE.
5. Hydrated logic: `const hydrated = !isLoading;` thay vì useState manual.

**Skeleton (preserve toàn bộ JSX layout, chỉ thay state source + handlers):**

```typescript
'use client';

import React from 'react';
import Image from 'next/image';
import Link from 'next/link';
import styles from './page.module.css';
import Button from '@/components/ui/Button/Button';
import { useToast } from '@/components/ui/Toast/Toast';
import {
  useCart,
  useUpdateCartItem,
  useRemoveCartItem,
  parseCartError,
} from '@/hooks/useCart';
import { formatPrice } from '@/services/api';

export default function CartPage() {
  const { showToast } = useToast();
  const { data: cartItems = [], isLoading } = useCart();
  const updateMutation = useUpdateCartItem();
  const removeMutation = useRemoveCartItem();

  const hydrated = !isLoading;

  const handleQuantityChange = (productId: string, qty: number) => {
    updateMutation.mutate(
      { productId, qty },
      {
        onError: (err) => {
          const ctx = parseCartError(err);
          showToast(ctx.message, 'error');
        },
      }
    );
  };

  const handleRemove = (productId: string) => {
    removeMutation.mutate(productId, {
      onError: (err) => {
        const ctx = parseCartError(err);
        showToast(ctx.message, 'error');
      },
    });
  };

  const subtotal = cartItems.reduce((sum, item) => sum + item.price * item.quantity, 0);
  const shippingFee = subtotal >= 1000000 ? 0 : 30000;
  const total = subtotal + shippingFee;

  return (
    <div className={styles.page}>
      {/* ... JSX layout giữ nguyên 100% ... */}
      {/* Replace handlers: */}
      {/*   onClick={() => removeFromCart(item.productId)}  → onClick={() => handleRemove(item.productId)} */}
      {/*   onClick={() => updateQuantity(item.productId, item.quantity - 1)}  → onClick={() => handleQuantityChange(item.productId, item.quantity - 1)} */}
      {/*   onClick={() => updateQuantity(item.productId, item.quantity + 1)}  → onClick={() => handleQuantityChange(item.productId, item.quantity + 1)} */}
      {/* Disable +/− buttons khi mutation pending: disabled={updateMutation.isPending || atStockLimit} */}
    </div>
  );
}
```

**Lưu ý:**
- KHÔNG xóa `cart:change` event listener khỏi page nếu có — giữ cho guest path compat. Nhưng useCart() qua `fetchCart()` đã handle cả guest path nên có thể bỏ listener. Confirm: `useCart` queryFn `fetchCart` đọc `_localRead()` cho guest → khi mutation invalidate → query refetch → call `_localRead()` lại → fresh data. KHÔNG CẦN listener.
- Disable +/− nút khi mutation pending để tránh double-click race.
- Preserve toàn bộ markup CSS classes và component refs (Button, Link, Image) nguyên vẹn.
  </action>
  <verify>
    <automated>cd sources/frontend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "useCart\|useUpdateCartItem\|useRemoveCartItem" cart/page.tsx` returns 3.
    - `grep -c "import.*useState.*from 'react'" cart/page.tsx` should NOT include cartItems useState (still OK for other state if any).
    - `grep -c "readCart\(\)" cart/page.tsx` returns 0 (đã thay sang hook).
    - `grep -c "removeFromCart(\|updateQuantity(" cart/page.tsx` returns 0 (mutations thay direct service call).
    - `grep -c "parseCartError" cart/page.tsx` returns 1.
    - `grep -c "showToast" cart/page.tsx` returns 1.
    - `grep -c "cart:change" cart/page.tsx` returns 0 (listener removed — useCart handle).
    - `grep -c "Tóm tắt đơn hàng\|Giỏ hàng trống" cart/page.tsx` returns 2 (markup preserved).
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>cart/page.tsx render data từ useCart, mutations qua hooks, error toast vi. JSX/CSS markup preserved 100%.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor checkout/page.tsx sang useCart + useClearCart cho clear sau order</name>
  <read_first>
    - sources/frontend/src/app/checkout/page.tsx (full hiện tại — readCart + clearCart sau submit)
    - sources/frontend/src/hooks/useCart.ts
    - sources/frontend/src/services/orders.ts (createOrder signature — không thay đổi)
  </read_first>
  <files>
    sources/frontend/src/app/checkout/page.tsx
  </files>
  <action>
Refactor `checkout/page.tsx`:

**Thay đổi:**
1. Replace `useState<CartItem[]>(() => readCart())` + `cart:change listener` bằng `const { data: cartItems = [] } = useCart()`.
2. Replace `clearCart()` (sync) sau `createOrder` thành công bằng `await clearMutation.mutateAsync()` từ `useClearCart()`.
3. Replace stockModal handlers `updateQuantity(item.productId, item.availableQuantity)` và `removeFromCart(item.productId)` bằng mutations:
   - "Cập nhật số lượng" button: `await Promise.all(stockModal.map(item => updateMutation.mutateAsync({ productId: item.productId, qty: item.availableQuantity })))`
   - "Xóa khỏi giỏ" button: `await Promise.all(stockModal.map(item => removeMutation.mutateAsync(item.productId)))`
4. Hydrated check `const hydrated = !cartQuery.isLoading;` (rename biến nếu cần preserve UI conditional).

**Lưu ý:**
- Preserve toàn bộ form logic (shippingAddress, paymentMethod, fieldErrors handling, banner, stockModal, paymentModal).
- `submitOrder` async function CHỈ thay clearCart() sang `await clearMutation.mutateAsync()` để đảm bảo cart đã clear (cả localStorage cho guest, cả DB cho user) TRƯỚC khi router.push.
- Nếu user thanh toán thành công nhưng clear cart fail → log error nhưng vẫn router.push (order đã tạo OK).

**Skeleton thay đổi (chỉ phần state + handler, JSX preserve):**

```typescript
'use client';

import { useCart, useUpdateCartItem, useRemoveCartItem, useClearCart } from '@/hooks/useCart';
// ... existing imports trừ readCart, clearCart, removeFromCart, updateQuantity từ services/cart

export default function CheckoutPage() {
  const { data: cartItems = [], isLoading: cartLoading } = useCart();
  const updateMutation = useUpdateCartItem();
  const removeMutation = useRemoveCartItem();
  const clearMutation = useClearCart();

  const hydrated = !cartLoading;
  // ... rest of state (form, fieldErrors, modals) giữ nguyên

  async function submitOrder() {
    setLoading(true);
    setBannerVisible(false);
    setFieldErrors({});
    try {
      const order = await createOrder({
        items: cartItems.map((i) => ({
          productId: i.productId,
          productName: i.name,
          quantity: i.quantity,
          unitPrice: i.price,
        })),
        shippingAddress: { ... },
        paymentMethod: form.paymentMethod,
        note: form.note || undefined,
      }, user?.id);

      // Phase 18: clear cart qua mutation (cả guest localStorage + user DB)
      try {
        await clearMutation.mutateAsync();
      } catch (clearErr) {
        console.error('[checkout] cart clear failed (non-blocking):', clearErr);
      }
      router.push('/profile/orders/' + order.id);
    } catch (err) {
      // ... error handling giữ nguyên
    } finally {
      setLoading(false);
    }
  }

  // ... JSX preserve. Stock modal handlers update:
  // primaryAction: async () => {
  //   if (!stockModal) return;
  //   await Promise.all(stockModal.map(it => updateMutation.mutateAsync({ productId: it.productId, qty: it.availableQuantity })));
  //   setStockModal(null);
  // }
  // secondaryAction: async () => {
  //   if (!stockModal) return;
  //   await Promise.all(stockModal.map(it => removeMutation.mutateAsync(it.productId)));
  //   setStockModal(null);
  // }
}
```
  </action>
  <verify>
    <automated>cd sources/frontend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "useCart\|useUpdateCartItem\|useRemoveCartItem\|useClearCart" checkout/page.tsx` returns 4.
    - `grep -c "readCart\(\)\|^import.*readCart" checkout/page.tsx` returns 0.
    - `grep -c "clearCart\(\)" checkout/page.tsx` returns 0 (replaced by clearMutation.mutateAsync()).
    - `grep -c "updateQuantity(\|removeFromCart(" checkout/page.tsx` returns 0 (mutations thay).
    - `grep -c "clearMutation.mutateAsync" checkout/page.tsx` returns 1.
    - `grep -c "cart:change" checkout/page.tsx` returns 0.
    - `grep -c "Thanh toán\|Đặt hàng\|Phương thức thanh toán" checkout/page.tsx` returns 3 (markup preserved).
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>checkout/page.tsx fetch cart từ useCart, clear cart sau order qua clearMutation. Stock modal handlers dùng mutations. Form/JSX preserved.</done>
</task>

<task type="auto">
  <name>Task 3: Update Header.tsx cart badge subscribe useCart()</name>
  <read_first>
    - Tìm Header component: `grep -rn "cart:change\|readCart" sources/frontend/src/components/` để locate file (có thể là `layout/Header.tsx` hoặc `Header/Header.tsx`).
    - File Header hiện tại — note pattern subscribe cart count.
    - sources/frontend/src/hooks/useCart.ts
  </read_first>
  <files>
    sources/frontend/src/components/layout/Header.tsx
  </files>
  <action>
Locate file Header chứa cart badge (planner grep `readCart\|cart:change` trong components/). Sau khi xác định:

**Thay đổi:**
1. Replace `useState(readCart().length)` + `cart:change` listener bằng `const { data: cartItems = [] } = useCart()` rồi tính `const cartCount = cartItems.reduce((s, i) => s + i.quantity, 0);` (hoặc `cartItems.length` tùy logic hiện tại).
2. Remove cart:change event listener — useCart subscribe React Query cache, auto-update khi mutation invalidate.

**Skeleton thay đổi:**

```typescript
'use client';

// REMOVE: import { readCart } from '@/services/cart';
// ADD:
import { useCart } from '@/hooks/useCart';

export function Header(/* props */) {
  // REMOVE useState(() => readCart().length) + useEffect cart:change listener.
  // ADD:
  const { data: cartItems = [] } = useCart();
  const cartCount = cartItems.reduce((sum, i) => sum + i.quantity, 0);

  // ... rest of header JSX preserved, badge render {cartCount}
}
```

**Lưu ý:**
- Nếu Header không phải client component (`'use client'`), thêm directive ở đầu file.
- File path chính xác: cần grep để xác định. Có thể là một trong:
  - `sources/frontend/src/components/layout/Header.tsx`
  - `sources/frontend/src/components/Header/Header.tsx`
  - `sources/frontend/src/app/(layout)/Header.tsx`
- Cập nhật `files_modified` frontmatter của plan này nếu path khác giả định.
- KHÔNG break logic auth menu, search bar, hoặc các parts khác của Header — chỉ thay cart badge subscription.
  </action>
  <verify>
    <automated>cd sources/frontend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - File Header (path xác định bằng grep) chứa `import.*useCart.*from '@/hooks/useCart'`.
    - File Header KHÔNG còn `import.*readCart.*from '@/services/cart'`.
    - File Header KHÔNG còn `cart:change` event listener (`grep -c "cart:change"` returns 0).
    - File Header có `useCart()` invocation (`grep -c "useCart\(\)" returns >= 1`).
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Header cart badge subscribe ['cart'] query. Auto-update khi mutation invalidate. Existing auth/nav features preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cart UI ↔ React Query cache | Cache invalidation đảm bảo UI sync sau mutation |
| Checkout submit → BE order | Cart state truyền vào createOrder phải sync với BE cart trước khi clear |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-14 | Tampering | Stock modal handler race (user click "Update qty" 2 lần) | mitigate | mutation.isPending → disable button (cart page Task 1 acceptance). Stock modal Promise.all đã serialize per-item |
| T-18-15 | Information Disclosure | Header badge stale sau merge login | mitigate | invalidateQueries(['cart']) sau mergeGuestCartToServer (Plan 05 wire) → header subscribe cùng key auto-refresh |
| T-18-16 | Repudiation | Order tạo OK nhưng cart clear fail → user có cart "ghost" | accept | Best-effort log error, KHÔNG rollback order (đã tạo). User có thể manual clear sau |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0.
- Manual test (post Plan 05 wire): user login → vào /cart → thấy items từ DB. +/- quantity → mutation success → re-render với DB count mới. Stock shortage → toast vi "Số lượng vượt quá tồn kho". Header badge auto-update.
- Guest test: vào /cart không login → readCart từ localStorage qua fetchCart. +/- buttons mutation OK qua _localUpdate path.
- Checkout test: cart có items → "Đặt hàng" → order tạo OK → cart clear cả localStorage + DB → router push to order detail.
</verification>

<success_criteria>
1. cart/page.tsx subscribe useCart, mutations qua hooks với toast vi error.
2. checkout/page.tsx fetch cart async, clear sau order qua mutation, stock modal handlers dùng mutations.
3. Header.tsx cart badge subscribe ['cart'] query, no cart:change listener.
4. TypeScript compile clean, JSX/markup preserved 100%.
5. Manual smoke: guest cart vẫn work, user cart hiển thị từ DB.
</success_criteria>

<output>
After completion, create `.planning/phases/18-storage-audit-cart-db/18-04-SUMMARY.md` documenting:
- 3 files refactored với mutation patterns
- Behavior visible cho user (cross-device persist confirmed nếu Plan 05 đã merge)
- Manual smoke test results
</output>
