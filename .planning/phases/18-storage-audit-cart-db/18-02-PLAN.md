---
phase: 18-storage-audit-cart-db
plan: 02
type: execute
wave: 2
depends_on: [18-01]
files_modified:
  - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/exception/StockShortageException.java
  - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/service/CartCrudService.java
  - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/web/CartController.java
autonomous: true
requirements:
  - STORE-02
tags:
  - backend
  - rest-controller
  - cart
must_haves:
  truths:
    - "User authenticated gọi GET /api/orders/cart trả CartDto (lazy-create cart row nếu chưa có)"
    - "POST /api/orders/cart/items với productId+quantity → idempotent ADD (cộng dồn) qua native upsert; quantity > stock → 409 STOCK_SHORTAGE"
    - "PATCH /api/orders/cart/items/{productId} set absolute quantity; quantity <= 0 → DELETE alias"
    - "DELETE /api/orders/cart/items/{productId} remove single; DELETE /api/orders/cart clear toàn bộ items giữ cart row"
    - "POST /api/orders/cart/merge body {items[]} idempotent merge guest cart vào DB cart"
    - "Cart user_id resolution lookup qua X-User-Id header (cùng pattern OrderController Phase 8)"
    - "Stock validation reuse StockShortageException + 409 error code (KHÔNG tạo error code mới)"
  artifacts:
    - path: "sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/service/CartCrudService.java"
      provides: "Business logic: getOrCreate, addItem upsert, setItem, removeItem, clearItems, mergeFromGuest, validateStock"
      min_lines: 150
    - path: "sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/web/CartController.java"
      provides: "REST endpoints /orders/cart + /orders/cart/items/** + /orders/cart/merge"
      contains: "@RequestMapping(\"/orders/cart\")"
  key_links:
    - from: "CartController"
      to: "CartCrudService"
      via: "constructor DI"
      pattern: "CartCrudService.*cartService"
    - from: "CartCrudService.validateStockOrThrow"
      to: "product-svc qua gateway"
      via: "RestTemplate GET http://api-gateway:8080/api/products/{id}"
      pattern: "api-gateway:8080/api/products/"
    - from: "CartCrudService.addItem"
      to: "CartItemRepository.upsertAddQuantity"
      via: "native @Modifying @Query"
      pattern: "upsertAddQuantity"
---

<objective>
Implement CartCrudService (business logic + stock validation reuse Phase 8 pattern) và CartController (REST endpoints theo gateway-rewrite mapping).

Endpoint mapping (gateway rewrite `/api/orders/(?<seg>.*)` → `/orders/${seg}` đã có trong api-gateway/application.yml):
- FE call `GET /api/orders/cart` → BE controller `GET /orders/cart`
- FE call `POST /api/orders/cart/items` → BE `POST /orders/cart/items`
- FE call `PATCH /api/orders/cart/items/{productId}` → BE `PATCH /orders/cart/items/{productId}`
- FE call `DELETE /api/orders/cart/items/{productId}` → BE `DELETE /orders/cart/items/{productId}`
- FE call `DELETE /api/orders/cart` → BE `DELETE /orders/cart`
- FE call `POST /api/orders/cart/merge` → BE `POST /orders/cart/merge`

Purpose: Hoàn thành BE side cart persistence. Sau plan này FE có thể bắt đầu integrate (Wave 3).

Output: CartController hoạt động end-to-end qua gateway, validation stock reuse 409 STOCK_SHORTAGE.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/18-storage-audit-cart-db/18-CONTEXT.md
@.planning/phases/18-storage-audit-cart-db/18-RESEARCH.md
@.planning/phases/18-storage-audit-cart-db/18-01-SUMMARY.md
@sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/service/OrderCrudService.java
@sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/web/OrderController.java
@sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/exception/StockShortageException.java
@sources/backend/api-gateway/src/main/resources/application.yml

<interfaces>
StockShortageException (Phase 8 — reuse, KHÔNG tạo mới):
```java
public class StockShortageException extends RuntimeException {
  private final List<StockShortageItem> shortages;
  public record StockShortageItem(String productId, String productName, int requested, int available) {}
  // throw → @ExceptionHandler trả 409 với error code STOCK_SHORTAGE + details.items
}
```

OrderCrudService.validateStockOrThrow pattern (reuse cho cart):
```java
String url = "http://api-gateway:8080/api/products/" + productId;
Map<String, Object> product = restTemplate.getForObject(url, Map.class);
int stock = ((Number) product.get("stock")).intValue();
if (requestedQuantity > stock) shortages.add(new StockShortageItem(...));
if (!shortages.isEmpty()) throw new StockShortageException(shortages);
```

OrderController X-User-Id header pattern:
```java
@PostMapping
public ResponseEntity<OrderDto> createOrder(
    @RequestHeader(name = "X-User-Id", required = false) String userId,
    @Valid @RequestBody CreateOrderCommand command) {
  // userId from JWT sub forwarded by gateway as X-User-Id header
  if (userId == null || userId.isBlank()) throw new ResponseStatusException(BAD_REQUEST, "Missing X-User-Id");
  ...
}
```

Plan 01 outputs:
- CartEntity.create(String userId), addItem(CartItemEntity), removeItem(CartItemEntity), touch()
- CartItemEntity.create(CartEntity, String productId, int quantity), setQuantity(int)
- CartRepository.findByUserId(String) → Optional<CartEntity>
- CartItemRepository.upsertAddQuantity(id, cartId, productId, quantity) (native), findByCartIdAndProductId(cartId, productId)
- CartMapper.toDto(CartEntity) → CartDto
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement CartCrudService (getOrCreate, add, set, remove, clear, merge, stock validation)</name>
  <read_first>
    - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/service/OrderCrudService.java (validateStockOrThrow pattern + RestTemplate gateway call)
    - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/exception/StockShortageException.java (constructor signature + StockShortageItem record)
    - .planning/phases/18-storage-audit-cart-db/18-CONTEXT.md §D-04..D-08 (endpoint semantics + idempotent ADD vs SET)
    - .planning/phases/18-storage-audit-cart-db/18-RESEARCH.md §Pattern 3 (getOrCreate race-condition mitigation)
    - .planning/phases/18-storage-audit-cart-db/18-01-SUMMARY.md (Plan 01 outputs)
  </read_first>
  <files>
    sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/service/CartCrudService.java
  </files>
  <action>
Tạo `CartCrudService.java` với toàn bộ business logic. Skeleton:

```java
package com.ptit.htpt.orderservice.service;

import com.ptit.htpt.orderservice.domain.*;
import com.ptit.htpt.orderservice.exception.StockShortageException;
import com.ptit.htpt.orderservice.exception.StockShortageException.StockShortageItem;
import com.ptit.htpt.orderservice.repository.CartItemRepository;
import com.ptit.htpt.orderservice.repository.CartRepository;
import jakarta.persistence.EntityManager;
import jakarta.persistence.PersistenceContext;
import jakarta.validation.Valid;
import jakarta.validation.constraints.Min;
import jakarta.validation.constraints.NotBlank;
import jakarta.validation.constraints.NotNull;
import java.util.*;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.dao.DataIntegrityViolationException;
import org.springframework.http.HttpStatus;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Transactional;
import org.springframework.web.client.RestTemplate;
import org.springframework.web.server.ResponseStatusException;

@Service
public class CartCrudService {
  private static final Logger log = LoggerFactory.getLogger(CartCrudService.class);

  private final CartRepository cartRepository;
  private final CartItemRepository cartItemRepository;
  private final RestTemplate restTemplate;
  @PersistenceContext private EntityManager entityManager;

  public CartCrudService(CartRepository cartRepository,
                         CartItemRepository cartItemRepository,
                         RestTemplate restTemplate) {
    this.cartRepository = cartRepository;
    this.cartItemRepository = cartItemRepository;
    this.restTemplate = restTemplate;
  }

  /** GET /cart — lazy-create empty cart row nếu chưa có (D-04). */
  @Transactional
  public CartDto getOrCreateCart(String userId) {
    requireUserId(userId);
    return CartMapper.toDto(getOrCreateEntity(userId));
  }

  /** POST /cart/items — idempotent ADD (cộng dồn quantity). D-05. */
  @Transactional
  public CartDto addItem(String userId, AddItemRequest req) {
    requireUserId(userId);
    CartEntity cart = getOrCreateEntity(userId);

    int currentQty = cartItemRepository.findByCartIdAndProductId(cart.id(), req.productId())
        .map(CartItemEntity::quantity).orElse(0);
    int newTotalQty = currentQty + req.quantity();
    validateStockOrThrow(req.productId(), newTotalQty);

    String newItemId = UUID.randomUUID().toString();
    cartItemRepository.upsertAddQuantity(newItemId, cart.id(), req.productId(), req.quantity());
    cart.touch();
    cartRepository.save(cart);

    // Native bypass JPA cache → flush+clear để query lại thấy giá trị mới
    entityManager.flush();
    entityManager.clear();
    return CartMapper.toDto(cartRepository.findByUserId(userId).orElseThrow());
  }

  /** PATCH /cart/items/{productId} — SET absolute quantity. D-06. quantity <= 0 → DELETE alias. */
  @Transactional
  public CartDto setItemQuantity(String userId, String productId, SetQuantityRequest req) {
    requireUserId(userId);
    if (req.quantity() <= 0) {
      return removeItem(userId, productId);
    }
    CartEntity cart = getOrCreateEntity(userId);
    validateStockOrThrow(productId, req.quantity());

    Optional<CartItemEntity> existing = cartItemRepository.findByCartIdAndProductId(cart.id(), productId);
    if (existing.isPresent()) {
      existing.get().setQuantity(req.quantity());
      cartItemRepository.save(existing.get());
    } else {
      // SET trên item chưa tồn tại → tạo mới
      CartItemEntity item = CartItemEntity.create(cart, productId, req.quantity());
      cart.addItem(item);
      cartItemRepository.save(item);
    }
    cart.touch();
    cartRepository.save(cart);
    return CartMapper.toDto(getOrCreateEntity(userId));
  }

  /** DELETE /cart/items/{productId} — remove single. D-07. */
  @Transactional
  public CartDto removeItem(String userId, String productId) {
    requireUserId(userId);
    CartEntity cart = getOrCreateEntity(userId);
    cartItemRepository.findByCartIdAndProductId(cart.id(), productId)
        .ifPresent(item -> {
          cart.removeItem(item);
          cartItemRepository.delete(item);
        });
    cart.touch();
    cartRepository.save(cart);
    return CartMapper.toDto(cart);
  }

  /** DELETE /cart — clear toàn bộ items, giữ cart row. D-07. */
  @Transactional
  public CartDto clearItems(String userId) {
    requireUserId(userId);
    CartEntity cart = getOrCreateEntity(userId);
    cart.items().clear(); // orphanRemoval=true → cascade DELETE
    cart.touch();
    cartRepository.save(cart);
    return CartMapper.toDto(cart);
  }

  /**
   * POST /cart/merge — idempotent merge guest cart vào DB cart. D-08.
   * Cho mỗi item: cộng dồn quantity với DB hiện tại, clamp by stock (best-effort).
   * Stock fail cho 1 item KHÔNG block toàn bộ merge — clamp về stock available.
   */
  @Transactional
  public CartDto mergeFromGuest(String userId, MergeCartRequest req) {
    requireUserId(userId);
    CartEntity cart = getOrCreateEntity(userId);

    for (MergeItem item : req.items()) {
      int currentQty = cartItemRepository.findByCartIdAndProductId(cart.id(), item.productId())
          .map(CartItemEntity::quantity).orElse(0);
      int desiredTotal = currentQty + item.quantity();
      int clampedTotal = clampToStock(item.productId(), desiredTotal);
      int delta = clampedTotal - currentQty;
      if (delta <= 0) continue; // đã >= stock, skip

      String newItemId = UUID.randomUUID().toString();
      cartItemRepository.upsertAddQuantity(newItemId, cart.id(), item.productId(), delta);
    }
    cart.touch();
    cartRepository.save(cart);
    entityManager.flush();
    entityManager.clear();
    return CartMapper.toDto(cartRepository.findByUserId(userId).orElseThrow());
  }

  // ===== helpers =====

  private CartEntity getOrCreateEntity(String userId) {
    Optional<CartEntity> existing = cartRepository.findByUserId(userId);
    if (existing.isPresent()) return existing.get();
    try {
      return cartRepository.save(CartEntity.create(userId));
    } catch (DataIntegrityViolationException race) {
      // 2 request cùng user_id race INSERT → unique constraint violation → retry findByUserId
      log.warn("[cart] race on getOrCreate userId={}: retrying find", userId);
      return cartRepository.findByUserId(userId).orElseThrow(() -> race);
    }
  }

  private void requireUserId(String userId) {
    if (userId == null || userId.isBlank()) {
      throw new ResponseStatusException(HttpStatus.UNAUTHORIZED, "Missing X-User-Id session header");
    }
  }

  /** Validate quantity <= stock. Fail → throw StockShortageException (409). Pattern reuse OrderCrudService. */
  private void validateStockOrThrow(String productId, int requestedQuantity) {
    try {
      String url = "http://api-gateway:8080/api/products/" + productId;
      @SuppressWarnings("unchecked")
      Map<String, Object> product = restTemplate.getForObject(url, Map.class);
      if (product == null) {
        log.warn("[cart] product-service returned null for productId={}", productId);
        return; // best-effort: nếu product-svc không response, KHÔNG block
      }
      Object stockObj = product.get("stock");
      int stock = stockObj == null ? 0 : ((Number) stockObj).intValue();
      String productName = product.get("name") == null ? productId : product.get("name").toString();
      if (requestedQuantity > stock) {
        throw new StockShortageException(List.of(
            new StockShortageItem(productId, productName, requestedQuantity, stock)
        ));
      }
    } catch (StockShortageException ex) {
      throw ex;
    } catch (Exception ex) {
      log.warn("[cart] stock fetch failed productId={}: {}", productId, ex.getMessage());
      // Best-effort MVP — không block mutation nếu product-svc unreachable
    }
  }

  /** Clamp desired quantity về stock available cho merge flow (no throw). */
  private int clampToStock(String productId, int desired) {
    try {
      String url = "http://api-gateway:8080/api/products/" + productId;
      @SuppressWarnings("unchecked")
      Map<String, Object> product = restTemplate.getForObject(url, Map.class);
      if (product == null) return desired;
      Object stockObj = product.get("stock");
      int stock = stockObj == null ? Integer.MAX_VALUE : ((Number) stockObj).intValue();
      return Math.min(desired, stock);
    } catch (Exception ex) {
      log.warn("[cart-merge] clamp fetch failed productId={}: {}", productId, ex.getMessage());
      return desired;
    }
  }

  // ===== request records =====

  public record AddItemRequest(@NotBlank String productId, @Min(1) int quantity) {}
  public record SetQuantityRequest(@NotNull Integer quantity) {} // allow 0 = delete alias

  public record MergeCartRequest(@NotNull List<@Valid MergeItem> items) {}
  public record MergeItem(@NotBlank String productId, @Min(1) int quantity) {}
}
```

**Lưu ý critical:**
- `flush() + clear()` SAU `upsertAddQuantity` BẮT BUỘC — nếu không, query `findByUserId` tiếp theo sẽ trả entity từ JPA cache (chưa thấy upsert).
- `getOrCreateEntity` catch `DataIntegrityViolationException` để xử lý race condition (Pattern 3 from research).
- `validateStockOrThrow`: best-effort behavior (log + continue) khi product-svc unreachable — match Phase 8 pattern.
- `mergeFromGuest`: stock clamp (KHÔNG throw) thay vì validate-or-throw vì merge là batch operation login flow, throw 1 item sẽ block toàn bộ.
  </action>
  <verify>
    <automated>cd sources/backend/order-service && mvn -q compile -DskipTests</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "@Service" CartCrudService.java` returns 1.
    - `grep -c "public CartDto getOrCreateCart" CartCrudService.java` returns 1.
    - `grep -c "public CartDto addItem" CartCrudService.java` returns 1.
    - `grep -c "public CartDto setItemQuantity" CartCrudService.java` returns 1.
    - `grep -c "public CartDto removeItem" CartCrudService.java` returns 1.
    - `grep -c "public CartDto clearItems" CartCrudService.java` returns 1.
    - `grep -c "public CartDto mergeFromGuest" CartCrudService.java` returns 1.
    - `grep -c "throw new StockShortageException" CartCrudService.java` returns 1 (validateStockOrThrow only — clampToStock không throw).
    - `grep -c "entityManager.flush" CartCrudService.java` >= 2 (addItem + mergeFromGuest).
    - `grep -c "entityManager.clear" CartCrudService.java` >= 2.
    - `grep -c "DataIntegrityViolationException" CartCrudService.java` returns 1 (race-handling).
    - `grep -c "@Transactional" CartCrudService.java` >= 6.
    - `grep -c "api-gateway:8080/api/products/" CartCrudService.java` >= 2 (validate + clamp).
    - `mvn -q compile -DskipTests` exits 0.
  </acceptance_criteria>
  <done>CartCrudService compile clean với 6 public methods + helpers. Stock validation reuse StockShortageException. Merge flow clamp (không throw).</done>
</task>

<task type="auto">
  <name>Task 2: Implement CartController với 6 endpoints + path mapping qua gateway</name>
  <read_first>
    - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/web/OrderController.java (X-User-Id header pattern + ApiResponse envelope wrapping pattern)
    - sources/backend/api-gateway/src/main/resources/application.yml (route order-service: `/api/orders/(?<seg>.*)` → `/orders/${seg}` — controller mount tại `/orders/cart`)
    - sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/web/ApiResponse.java (envelope shape)
    - .planning/phases/18-storage-audit-cart-db/18-CONTEXT.md §D-04..D-08
  </read_first>
  <files>
    sources/backend/order-service/src/main/java/com/ptit/htpt/orderservice/web/CartController.java
  </files>
  <action>
Tạo `CartController.java`:

```java
package com.ptit.htpt.orderservice.web;

import com.ptit.htpt.orderservice.domain.CartDto;
import com.ptit.htpt.orderservice.service.CartCrudService;
import com.ptit.htpt.orderservice.service.CartCrudService.AddItemRequest;
import com.ptit.htpt.orderservice.service.CartCrudService.MergeCartRequest;
import com.ptit.htpt.orderservice.service.CartCrudService.SetQuantityRequest;
import jakarta.validation.Valid;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.*;

/**
 * Phase 18 / STORE-02: Cart REST endpoints.
 *
 * Gateway rewrite: client gọi /api/orders/cart/** → gateway rewrite → /orders/cart/** ở service này.
 * X-User-Id header forward từ gateway sau khi parse JWT sub claim (pattern Phase 6 + Phase 8).
 *
 * Error contract: 401 missing X-User-Id; 409 STOCK_SHORTAGE qua StockShortageException
 * → GlobalExceptionHandler (Phase 8) trả ApiErrorResponse với code=STOCK_SHORTAGE + details.items.
 */
@RestController
@RequestMapping("/orders/cart")
public class CartController {

  private final CartCrudService cartService;

  public CartController(CartCrudService cartService) {
    this.cartService = cartService;
  }

  @GetMapping
  public ResponseEntity<ApiResponse<CartDto>> getCart(
      @RequestHeader(name = "X-User-Id", required = false) String userId) {
    return ResponseEntity.ok(ApiResponse.ok(cartService.getOrCreateCart(userId)));
  }

  @PostMapping("/items")
  public ResponseEntity<ApiResponse<CartDto>> addItem(
      @RequestHeader(name = "X-User-Id", required = false) String userId,
      @Valid @RequestBody AddItemRequest body) {
    return ResponseEntity.ok(ApiResponse.ok(cartService.addItem(userId, body)));
  }

  @PatchMapping("/items/{productId}")
  public ResponseEntity<ApiResponse<CartDto>> setItem(
      @RequestHeader(name = "X-User-Id", required = false) String userId,
      @PathVariable String productId,
      @Valid @RequestBody SetQuantityRequest body) {
    return ResponseEntity.ok(ApiResponse.ok(cartService.setItemQuantity(userId, productId, body)));
  }

  @DeleteMapping("/items/{productId}")
  public ResponseEntity<ApiResponse<CartDto>> removeItem(
      @RequestHeader(name = "X-User-Id", required = false) String userId,
      @PathVariable String productId) {
    return ResponseEntity.ok(ApiResponse.ok(cartService.removeItem(userId, productId)));
  }

  @DeleteMapping
  public ResponseEntity<ApiResponse<CartDto>> clearCart(
      @RequestHeader(name = "X-User-Id", required = false) String userId) {
    return ResponseEntity.ok(ApiResponse.ok(cartService.clearItems(userId)));
  }

  @PostMapping("/merge")
  public ResponseEntity<ApiResponse<CartDto>> merge(
      @RequestHeader(name = "X-User-Id", required = false) String userId,
      @Valid @RequestBody MergeCartRequest body) {
    return ResponseEntity.ok(ApiResponse.ok(cartService.mergeFromGuest(userId, body)));
  }
}
```

**Lưu ý:**
- `@RequestMapping("/orders/cart")` (KHÔNG có `/api/` prefix vì gateway đã rewrite).
- Gateway route `order-service` predicate `Path=/api/orders/**` filter `RewritePath=/api/orders/(?<seg>.*), /orders/${seg}` đã có trong `application.yml` — KHÔNG cần thêm route mới.
- Nếu `ApiResponse.ok(...)` static helper chưa có, dùng pattern từ OrderController hiện tại (planner verify class signature trong codebase trước khi implement).
- `X-User-Id` header `required = false` để controller có thể throw 401 từ service layer với message rõ ràng "Missing X-User-Id" thay vì 400 Spring auto-error.
  </action>
  <verify>
    <automated>cd sources/backend/order-service && mvn -q compile -DskipTests && mvn -q spring-boot:run -Dspring-boot.run.profiles=test &amp; sleep 30 &amp; curl -s -o /dev/null -w "%{http_code}" -X GET http://localhost:8080/orders/cart -H "X-User-Id: test-user-1" | grep -E "^(200|401|500)$"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "@RestController" CartController.java` returns 1.
    - `grep -c "@RequestMapping(\"/orders/cart\")" CartController.java` returns 1.
    - `grep -c "@GetMapping" CartController.java` returns 1.
    - `grep -c "@PostMapping(\"/items\")" CartController.java` returns 1.
    - `grep -c "@PatchMapping(\"/items/{productId}\")" CartController.java` returns 1.
    - `grep -c "@DeleteMapping(\"/items/{productId}\")" CartController.java` returns 1.
    - `grep -c "@DeleteMapping$" CartController.java` returns 1 (clear cart endpoint).
    - `grep -c "@PostMapping(\"/merge\")" CartController.java` returns 1.
    - `grep -c "X-User-Id" CartController.java` returns 6 (mỗi endpoint).
    - `mvn -q compile -DskipTests` exits 0.
    - Manual smoke (sau khi spring boot lên + Flyway V4 apply): `curl -X GET http://localhost:8080/orders/cart -H "X-User-Id: test-user-1"` returns HTTP 200 với `{"data":{"items":[]}}` JSON envelope, `curl -X POST http://localhost:8080/orders/cart/items -H "X-User-Id: test-user-1" -H "Content-Type: application/json" -d '{"productId":"non-existent","quantity":1}'` returns 200 (best-effort stock validate, product không tồn tại nhưng không block).
  </acceptance_criteria>
  <done>CartController có 6 endpoints đúng path. Compile clean. Manual curl smoke tests trả 200 với envelope đúng.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → API gateway → order-service | JWT verified at gateway, sub claim forward as X-User-Id header |
| order-service → product-service | Internal docker network call (RestTemplate) — không expose ngoài |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-05 | Spoofing | User A authenticate as B đọc/sửa cart B | mitigate | Cart lookup CHỈ qua X-User-Id từ JWT.sub (gateway forward); KHÔNG accept user_id trong body. CartCrudService.requireUserId() chặn missing header. (T-01 from phase threat model) |
| T-18-06 | Tampering | Race condition merge 2 tabs cùng login double quantity | mitigate | UNIQUE(cart_id, product_id) constraint + native ON CONFLICT atomic single-statement chặn duplicate row. Quantity cộng dồn ở DB layer. (T-02 from phase) |
| T-18-07 | Tampering | Client gửi quantity > stock | mitigate | validateStockOrThrow gọi product-svc lấy live stock mỗi mutation; quantity > stock → 409 STOCK_SHORTAGE (T-03 from phase) |
| T-18-08 | Information Disclosure | product-svc unreachable → cart accept any quantity | accept | Best-effort MVP per Phase 8 pattern; checkout re-validate là gate cuối (Phase 8 D-04) |
| T-18-09 | Denial of Service | User spam POST /cart/items với quantity=1 mỗi click → tạo nhiều cart_items rows? | mitigate | UNIQUE(cart_id, product_id) → upsert cộng dồn, KHÔNG tạo row mới. Max rows = số distinct products user thêm |
</threat_model>

<verification>
- `mvn -pl order-service compile -DskipTests` exits 0.
- Spring Boot startup không error (Flyway V4 apply OK).
- Manual smoke test: GET /orders/cart with valid X-User-Id returns 200 + empty items.
- Manual smoke test: POST /orders/cart/items idempotent (gọi 2 lần cùng productId+quantity=1 → DB chỉ có 1 row với quantity=2).
- Manual smoke test: GET /orders/cart KHÔNG có X-User-Id header returns 401 với message "Missing X-User-Id session header".
</verification>

<success_criteria>
1. CartCrudService 6 public methods compile clean với @Transactional + flush+clear strategy.
2. CartController 6 endpoints mount đúng path (`/orders/cart` + `/orders/cart/items/**` + `/orders/cart/merge`).
3. Gateway rewrite `/api/orders/cart/**` → `/orders/cart/**` hoạt động (không cần thêm route trong application.yml).
4. 409 STOCK_SHORTAGE reuse exception code (KHÔNG tạo error code mới).
5. 401 trả khi missing X-User-Id.
6. STORE-02 BE side hoàn thành — sẵn sàng cho FE Wave 3.
</success_criteria>

<output>
After completion, create `.planning/phases/18-storage-audit-cart-db/18-02-SUMMARY.md` documenting:
- 6 endpoints + exact path mapping qua gateway
- Stock validation strategy (validate-or-throw cho mutation, clamp cho merge)
- Race condition handling (DataIntegrityViolationException retry trong getOrCreateEntity)
- flush+clear pattern sau native upsert
- Manual smoke test results
</output>
