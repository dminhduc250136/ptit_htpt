---
phase: 18-storage-audit-cart-db
plan: 06
type: execute
wave: 5
depends_on: [18-04, 18-05]
files_modified:
  - .planning/phases/18-storage-audit-cart-db/18-SUMMARY.md
autonomous: false
requirements:
  - STORE-01
  - STORE-03
tags:
  - audit
  - documentation
  - storage
must_haves:
  truths:
    - "18-SUMMARY.md có table classify mọi key localStorage/sessionStorage tìm được toàn FE codebase"
    - "Mỗi key có cột: source file, purpose, classification (DB-migrated / UI-kept / auth-deferred), reason"
    - "Cart key documented là 'DB-migrated (Phase 18)' với reference Plan 02 endpoints"
    - "Auth tokens (accessToken, refreshToken) documented 'auth-deferred (STORE-04)' với reference"
    - "userProfile documented 'UI-kept' với reason 'session cache, DB là source of truth via user-svc Phase 10'"
    - "STORE-03: nếu phát hiện wishlist/recently-viewed/search-history → migrate plan note hoặc fold-in tasks; nếu không → đóng với note 'no additional leaks found beyond cart'"
    - "Manual UAT confirm 4 phase success criteria (truths #1-4 từ phase must_haves)"
  artifacts:
    - path: ".planning/phases/18-storage-audit-cart-db/18-SUMMARY.md"
      provides: "Phase deliverable: storage audit report + UAT confirmation"
      contains: "Storage Audit Table"
  key_links:
    - from: "audit grep result"
      to: "classification table"
      via: "manual review"
      pattern: "localStorage|sessionStorage"
---

<objective>
**Final phase deliverable.** Hoàn thành 2 việc:
1. **Storage audit (STORE-01 + STORE-03):** Grep toàn FE codebase, classify mọi `localStorage`/`sessionStorage` key vào 3 nhóm (DB-migrated / UI-kept / auth-deferred). Output bảng classify trong `18-SUMMARY.md`. Nếu phát hiện key user-data leak chưa biết → fold-in migrate plan hoặc document đóng với rationale.
2. **Phase UAT (manual checkpoint):** Confirm 4 must_haves cốt lõi của Phase 18:
   - User add cart → đóng tab → mở browser khác same login → cart persist
   - Guest add cart → login → DB merge không duplicate
   - Logout → localStorage.getItem('cart') === null
   - Audit table có classify mọi key

Purpose: Đóng phase với báo cáo audit final + UAT confirm. Đây là deliverable cuối cùng phase 18.

Output: `18-SUMMARY.md` với audit table + UAT result. STORE-01, STORE-02 (cross-ref Plans 01-05), STORE-03 closed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/18-storage-audit-cart-db/18-CONTEXT.md
@.planning/phases/18-storage-audit-cart-db/18-RESEARCH.md
@.planning/phases/18-storage-audit-cart-db/18-01-SUMMARY.md
@.planning/phases/18-storage-audit-cart-db/18-02-SUMMARY.md
@.planning/phases/18-storage-audit-cart-db/18-03-SUMMARY.md
@.planning/phases/18-storage-audit-cart-db/18-04-SUMMARY.md
@.planning/phases/18-storage-audit-cart-db/18-05-SUMMARY.md

<interfaces>
Audit baseline (preview từ research): các key tìm được:
- `cart` (localStorage) — DB-migrated Phase 18
- `userProfile` (localStorage) — UI-kept (session cache, user-svc Phase 10 source of truth)
- `accessToken` (localStorage) — auth-deferred (STORE-04)
- `refreshToken` (localStorage) — auth-deferred (STORE-04)
- `auth_present` (cookie, NOT localStorage — middleware D-09 Phase 6) — note riêng

STORE-03 fold-in policy (D-19): nếu phát hiện key user-data leak khác (wishlist, recently-viewed, search-history) → option:
  A. Migrate trong phase này (thêm task fold-in)
  B. Document defer với rationale (defer reason locked v1.4 nếu non-essential)

User confirmed (CONTEXT D-19): "audit kỹ, fold-in if found, OK skip if not".

User confirmed (D-21): userProfile KHÔNG phải data leak — UI session cache, KHÔNG cần migrate.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Grep toàn FE codebase, build classification table, viết draft 18-SUMMARY.md</name>
  <read_first>
    - Run: `grep -rn "localStorage\|sessionStorage" sources/frontend/src/ --include="*.ts" --include="*.tsx"` (lấy danh sách full)
    - sources/frontend/src/services/token.ts (auth tokens - hiểu purpose)
    - sources/frontend/src/providers/AuthProvider.tsx (userProfile - hiểu purpose)
    - .planning/phases/06-real-auth-flow/06-CONTEXT.md (D-11/D-12 auth-token tradeoff đã accepted)
    - .planning/phases/18-storage-audit-cart-db/18-CONTEXT.md §D-18..D-21 (audit policy)
  </read_first>
  <files>
    .planning/phases/18-storage-audit-cart-db/18-SUMMARY.md
  </files>
  <action>
**Step 1:** Run grep để có raw audit data:
```bash
grep -rn "localStorage\|sessionStorage" sources/frontend/src/ --include="*.ts" --include="*.tsx"
```
Capture toàn bộ output. Cross-reference với `cookie` accesses cho `auth_present`:
```bash
grep -rn "document.cookie\|auth_present" sources/frontend/src/ --include="*.ts" --include="*.tsx"
```

**Step 2:** Classify mỗi unique key vào 1 trong 3 nhóm:
- **DB-migrated:** Key đã được migrate sang DB trong phase này (hoặc trước đó).
- **UI-kept:** Key giữ localStorage hợp lý (UI preference, session cache, KHÔNG phải user-data leak).
- **auth-deferred:** Auth token đã review nhưng defer migrate (STORE-04 visible-first).

**Step 3:** Phát hiện key chưa biết:
- Nếu xuất hiện key user-data leak (wishlist, recently-viewed, search-history, favorites...) chưa lock plan → DECISION:
  - Nếu đơn giản (1 tactical migrate, 1 file FE + 0 BE schema change) → fold-in migrate task (planner thêm task vào plan này, update files_modified).
  - Nếu phức tạp (cần BE migration mới) → document defer với rationale ("scope locked v1.3 STORE — fold-in defer to v1.4 ACCT-01 wishlist hoặc tương tự").
- Nếu KHÔNG có key user-data leak nào ngoài cart → STORE-03 closed với note "no additional leaks found beyond cart".

**Step 4:** Viết `18-SUMMARY.md` với cấu trúc:

```markdown
# Phase 18: Kiểm Toán Storage + Cart→DB — SUMMARY

**Phase:** 18-storage-audit-cart-db
**Status:** COMPLETED
**Date:** {YYYY-MM-DD}
**Plans:** 6 plans (01-05 implementation, 06 audit + UAT)

---

## Storage Audit Report (STORE-01)

**Method:** `grep -rn "localStorage\|sessionStorage" sources/frontend/src/` + manual classification.

**Date:** {YYYY-MM-DD}

### Storage Keys Found

| Key | Storage | Source File(s) | Purpose | Classification | Reason |
|-----|---------|----------------|---------|----------------|--------|
| `cart` | localStorage | services/cart.ts (guest path: `_localRead`/`_localWrite`/`_localClear`) | Guest cart items | **DB-migrated (Phase 18)** | User logged-in giờ persist qua `order_svc.carts` + `cart_items` (Plan 01-02). Guest path giữ localStorage cho MVP (anonymous server-side cart deferred). Logout clear localStorage (Plan 05) chống leak |
| `userProfile` | localStorage | providers/AuthProvider.tsx (lazy initializer + storage event sync) | UI session cache for auth state hydration | **UI-kept** | Tránh flash unauth state khi SSR→client hydrate. DB là source of truth via user-svc Phase 10 GET /api/users/me. KHÔNG phải data leak (D-21 CONTEXT) |
| `accessToken` | localStorage | services/token.ts | JWT access token (15-min) cho Authorization Bearer header | **auth-deferred (STORE-04)** | Phase 6 D-11/D-12 tradeoff accepted (XSS-able nhưng visible-first defer). STORE-04 v1.4+ sẽ migrate sang httpOnly cookie. KHÔNG sửa logic phase này |
| `refreshToken` | localStorage | services/token.ts | JWT refresh token (7-day) | **auth-deferred (STORE-04)** | Cùng tradeoff như accessToken. Note: refreshToken hiện chưa wire (auth.ts comment), planner ghi nhận status hiện tại |
| `user_role` | localStorage / cookie? | services/token.ts (setUserRole, clearUserRole) | RBAC check cho middleware admin route | **UI-kept** | Cookie used by middleware (server-side), localStorage sync optional. KHÔNG sensitive — derived từ JWT roles claim |
| `auth_present` | cookie | middleware.ts hoặc token.ts | Browser-readable flag indicating auth state | **UI-kept (cookie, không phải localStorage)** | NOT localStorage; Cookie SameSite Lax, không httpOnly để FE đọc được. Phase 6 D-09 |
| {fold-in nếu phát hiện key khác} | ... | ... | ... | ... | ... |

### Audit Coverage

- [x] Toàn `sources/frontend/src/` grep cho `localStorage` + `sessionStorage`
- [x] Cross-check `document.cookie` accesses
- [x] Classified ALL keys vào 1 trong 3 categories
- [x] Auth tokens reference STORE-04 deferred
- [x] STORE-03 closure: {"closed - no additional leaks found beyond cart" HOẶC "fold-in: migrated key X to DB in Plan ZZ"}

### STORE-03 Disposition

**Result:** {chọn 1}
- (a) **No additional user-data leaks found.** Audit confirmed only `cart` (DB-migrated), `userProfile` (UI cache), and auth tokens (deferred STORE-04). Closed.
- (b) **Fold-in migrate:** {key X migrated} — see Plan ZZ.
- (c) **Defer rationale:** {key X found but defer reason: scope locked v1.4 ACCT-01 wishlist / etc.}

---

## Implementation Summary (STORE-02)

| Plan | Output | Status |
|------|--------|--------|
| 18-01 | Flyway V4 + JPA entities + repos + InMemoryCartRepository delete | ✅ |
| 18-02 | CartCrudService + CartController (6 endpoints) | ✅ |
| 18-03 | services/cart.ts dual-backend + useCart hooks | ✅ |
| 18-04 | cart/page.tsx + checkout/page.tsx + Header.tsx refactor | ✅ |
| 18-05 | AuthProvider login merge + logout clear | ✅ |
| 18-06 | Audit + UAT (this plan) | ✅ |

---

## Phase Success Criteria UAT (Manual)

Performed by: {tester name / Claude verifier}
Date: {YYYY-MM-DD}

| # | Truth | Verification Steps | Result |
|---|-------|--------------------|--------|
| 1 | User add product → đóng tab → mở lại browser khác same login → cart persist từ DB | (1) Browser A: login user-X. (2) Add 3 sản phẩm vào cart. (3) Đóng browser A. (4) Browser B (incognito hoặc khác máy): login user-X. (5) Vào /cart. **Expect:** thấy 3 sản phẩm | ✅ / ❌ |
| 2 | Guest add cart → login → DB merge KHÔNG duplicate | (1) Logout. Add 2 items vào cart (localStorage). (2) Login user-Y (chưa có DB cart). (3) Vào /cart. **Expect:** 2 items đúng quantity, KHÔNG duplicate. (4) Verify DB: `SELECT * FROM order_svc.cart_items WHERE cart_id = (SELECT id FROM order_svc.carts WHERE user_id = 'user-Y')` returns 2 rows. | ✅ / ❌ |
| 3 | Logout → localStorage.getItem('cart') === null | (1) Login any user. Add items. (2) Logout. (3) Browser DevTools console: `localStorage.getItem('cart')`. **Expect:** `null` | ✅ / ❌ |
| 4 | Audit report classify mọi key | Inspect §Storage Audit Table trong file này. **Expect:** ≥5 rows, mọi row có Classification + Reason filled | ✅ / ❌ |

### Bonus UAT Tests

| # | Test | Result |
|---|------|--------|
| 5 | Cross-tab race: 2 tabs login đồng thời với guest cart [{prod-A: 2}] → DB cart [{prod-A: 2}] (KHÔNG double quantity 4) | ✅ / ❌ |
| 6 | Stock shortage: cart qty = stock + 1 → toast vi "Số lượng vượt quá tồn kho" hiện | ✅ / ❌ |
| 7 | Merge fail (stop order-svc): login với guest cart → toast warning hiện, login KHÔNG block | ✅ / ❌ |

---

## Decisions & Tradeoffs

- **N+1 hydration:** Server cart trả productId-only, FE hydrate từ product-svc per item. MVP acceptable; future optimization defer.
- **Write-through (no optimistic UI):** ~200ms latency mỗi click acceptable per user lock D-11.
- **Stock best-effort:** product-svc unreachable → KHÔNG block mutation (Phase 8 pattern).
- **Auth tokens deferred:** STORE-04 visible-first defer giữ nguyên.
- **userProfile UI-kept:** session cache, KHÔNG phải data leak (D-21).

---

## Next Phase

**Phase 19: Hoàn Thiện Admin Charts + Low-Stock**
- Depends on Phase 16 + Phase 17 (KHÔNG depend Phase 18 trực tiếp).
- Phase 18 unblocks Phase 20 (Coupon — server-side cart total cho coupon validation).

---

*Phase 18 closed: {YYYY-MM-DD}*
```

**Lưu ý:**
- Step 4 KHÔNG yêu cầu run UAT trong task này — task tạo file template với placeholder ✅/❌. UAT thực sự run trong checkpoint Task 2.
- Bảng classify phải reflect grep output thực tế — KHÔNG bịa key.
- Nếu phát hiện key chưa biết và simple (e.g. localStorage 'recentlyViewed' chỉ ở 1 file), planner add fold-in task vào plan này VÀ update frontmatter `files_modified`. Document quyết định trong SUMMARY.
  </action>
  <verify>
    <automated>test -f .planning/phases/18-storage-audit-cart-db/18-SUMMARY.md &amp;&amp; grep -c "Storage Audit Report\|Classification" .planning/phases/18-storage-audit-cart-db/18-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/18-storage-audit-cart-db/18-SUMMARY.md` tồn tại.
    - `grep -c "## Storage Audit Report" 18-SUMMARY.md` returns 1.
    - `grep -c "| Classification |" 18-SUMMARY.md` returns >= 1 (header table).
    - Bảng có ≥5 rows (cart, userProfile, accessToken, refreshToken, auth_present + bất kỳ key nào khác phát hiện).
    - Mỗi row có `DB-migrated\|UI-kept\|auth-deferred` classification.
    - `grep -c "STORE-04" 18-SUMMARY.md` >= 1 (auth deferred reference).
    - `grep -c "STORE-03 Disposition" 18-SUMMARY.md` returns 1.
    - `grep -c "Phase Success Criteria UAT" 18-SUMMARY.md` returns 1.
    - 4 truth UAT rows present trong table.
  </acceptance_criteria>
  <done>18-SUMMARY.md draft với audit table + UAT placeholder template + STORE-03 disposition. Sẵn sàng cho UAT checkpoint Task 2.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual UAT — confirm 4 phase success criteria</name>
  <what-built>
    Phase 18 hoàn chỉnh: cart→DB migration (STORE-02), storage audit report (STORE-01), STORE-03 disposition. Cần human verify 4 truth observable từ user perspective.
  </what-built>
  <how-to-verify>
**Setup:**
1. Đảm bảo full stack đã start (`docker compose up -d` hoặc cách khởi động project tương đương).
2. Verify Flyway V4 đã apply: kết nối Postgres `order_svc`, chạy `\dt order_svc.*` — expect tables `carts` + `cart_items`.

**Test 1 — Cart persist cross-browser (Truth #1):**
1. Browser A (Chrome): login user "buyer1@test.com".
2. Vào `/products`, add 3 SP khác nhau vào cart.
3. Đóng tab browser A.
4. Browser B (Firefox hoặc Chrome incognito): login same user "buyer1@test.com".
5. Vào `/cart`.
   **Expected:** Hiện 3 SP đã add từ browser A.
6. DevTools console browser B: `localStorage.getItem('cart')`.
   **Expected:** `null` (cart đến từ DB, không phải localStorage).

**Test 2 — Guest→user merge KHÔNG duplicate (Truth #2):**
1. Browser incognito: KHÔNG login. Vào `/products`, add 2 SP vào cart (localStorage).
2. DevTools: `localStorage.getItem('cart')` — confirm thấy items.
3. Login user "buyer2@test.com" (user chưa có DB cart trước).
4. Vào `/cart`.
   **Expected:** Thấy 2 SP, đúng quantity, KHÔNG duplicate.
5. DevTools: `localStorage.getItem('cart')`.
   **Expected:** `null` (đã clear sau merge).
6. Verify DB:
   ```sql
   SELECT product_id, quantity FROM order_svc.cart_items
   WHERE cart_id = (SELECT id FROM order_svc.carts WHERE user_id = (SELECT id FROM user_svc.users WHERE email = 'buyer2@test.com'));
   ```
   **Expected:** 2 rows, quantity đúng (KHÔNG x2).

**Test 3 — Logout clear cart localStorage (Truth #4 phase + T-04 mitigation):**
1. Login user "buyer3@test.com". Add 1 SP vào cart.
2. Click Logout.
3. DevTools: `localStorage.getItem('cart')`.
   **Expected:** `null`.
4. KHÔNG login. Vào `/cart`.
   **Expected:** Cart trống.
5. Login user khác "buyer4@test.com".
6. Vào `/cart`.
   **Expected:** Thấy cart RIÊNG của buyer4 (nếu có), KHÔNG thấy cart của buyer3.

**Test 4 — Audit report complete (Truth #3 phase):**
1. Mở `.planning/phases/18-storage-audit-cart-db/18-SUMMARY.md`.
2. Verify table §Storage Audit Report có ≥5 rows.
3. Verify mỗi row có:
   - Source file path
   - Purpose (Vietnamese OK)
   - Classification (DB-migrated / UI-kept / auth-deferred)
   - Reason
4. Verify §STORE-03 Disposition có closure decision (no leaks found / fold-in / defer rationale).

**Bonus tests:**
5. Stock shortage: try add SP với qty > stock → expect toast "Số lượng vượt quá tồn kho".
6. Merge fail: stop order-svc container, login với guest cart → expect toast warning, login KHÔNG block.

**Cập nhật 18-SUMMARY.md:**
- Sau khi PASS, update các ✅/❌ trong UAT table.
- Nếu FAIL bất kỳ test nào → document failure trong SUMMARY, KHÔNG mark phase closed; thay vào đó tạo gap closure plan (`/gsd-plan-phase 18 --gaps`).
  </how-to-verify>
  <resume-signal>
    Type "approved" nếu tất cả 4 truth tests PASS + audit table complete.
    Type "issues: {description}" nếu có vấn đề — cụ thể test nào fail và behavior observed.
  </resume-signal>
  <acceptance_criteria>
    - 4 truth tests trong UAT table đều marked ✅.
    - Audit table có ≥5 rows với mọi cột filled.
    - STORE-03 disposition documented.
    - Bonus tests (5-7) marked ✅ hoặc note clearly nếu skip.
  </acceptance_criteria>
  <done>Phase 18 closed với 4 success criteria PASS, audit complete, STORE-03 disposed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Audit grep result → classification | Manual review — risk of misclassification |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-21 | Information Disclosure | Audit miss key user-data | mitigate | Grep toàn `sources/frontend/src/` recursive với cả `localStorage` + `sessionStorage`; cross-check `document.cookie`. Manual review từng kết quả |
| T-18-22 | Spoofing | UAT placeholder marked ✅ mà chưa run thực sự | mitigate | Checkpoint Task 2 BLOCKING — human phải gõ "approved" hoặc "issues:". KHÔNG auto-pass |
</threat_model>

<verification>
- 18-SUMMARY.md tồn tại với toàn bộ sections required.
- Storage Audit Table có ≥5 rows và classify đầy đủ.
- UAT table với 4 truth rows + 3 bonus rows.
- STORE-03 disposition documented.
- Manual UAT checkpoint approved.
</verification>

<success_criteria>
1. STORE-01: Audit table classify ALL `localStorage`/`sessionStorage` keys phát hiện được.
2. STORE-03: Disposition (closed / fold-in / defer rationale).
3. Phase 4 must_haves UAT confirmed:
   - Cross-browser cart persist ✅
   - Guest→user merge no duplicate ✅
   - Logout localStorage cart === null ✅
   - Audit report complete ✅
4. 18-SUMMARY.md committed như phase deliverable.
</success_criteria>

<output>
Final phase deliverable: `.planning/phases/18-storage-audit-cart-db/18-SUMMARY.md` chứa:
- Storage audit table (STORE-01)
- Implementation summary cross-ref Plans 01-05 (STORE-02)
- STORE-03 disposition
- Manual UAT results (4 truth + 3 bonus tests)
- Decisions & tradeoffs
- Next phase pointer

Sau khi UAT approved, phase 18 đóng. Update STATE.md với `Phase 18: COMPLETE`.
</output>
